#! /bin/sh
# Guess values for system-dependent variables and create Makefiles.
# Generated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cyassl/cyassl/issues>.
#
#
# Copyright (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on ${1+"$@"}, which
  # is contrary to our usage.  Disable this feature.
  alias -g '${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo$as_echo
# Prefer a ksh shell builtin over an external printf program on Solaris,
# but without wasting forks for bash or zsh.
if test -z "$BASH_VERSION$ZSH_VERSION" \
    && (test "X`print -r -- $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
  as_echo_n='print -rn --'
elif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %s'
else
  if test "X`(/usr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_body='eval /usr/ucb/echo -n "$1$as_nl"'
    as_echo_n='/usr/ucb/echo -n'
  else
    as_echo_body='eval expr "X$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg in #(
      *"$as_nl"*)
	expr "X$arg" : "X\\(.*\\)$as_nl";
	arg=`expr "X$arg" : ".*$as_nl\\(.*\\)"`;;
      esac;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c $as_echo_n_body as_echo'
  fi
  export as_echo_body
  as_echo='sh -c $as_echo_body as_echo'
fi

# The user is always right.
if test "${PATH_SEPARATOR+set}" != set; then
  PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 && {
    (PATH='/bin:/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 ||
      PATH_SEPARATOR=';'
  }
fi


# IFS
# We need space, tab and new line, in precisely that order.  Quoting is
# there to prevent editors from complaining about space-tab.
# (If _AS_PATH_WALK were called with IFS unset, it would disable word
# splitting by setting IFS to empty value.)
IFS=" ""	$as_nl"

# Find who we are.  Look in the path if we contain no directory separator.
case $0 in #((
  *[\\/]* ) as_myself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves, most probably we were run as `sh COMMAND'
# in which case we are not to be found in the path.
if test "x$as_myself" = x; then
  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  But do not cause bugs in bash 2.01; the "|| exit 1"
# suppresses any "Segmentation fault" message there.  '((' could
# trigger a bug in pdksh 5.2.14.
for as_var in BASH_ENV ENV MAIL MAILPATH
do eval test x\${$as_var+set} = xset \
  && ( (unset $as_var) || exit 1) >/dev/null 2>&1 && unset $as_var || :
done
PS1='$ '
PS2='> '
PS4='+ '

# NLS nuisances.
LC_ALL=C
export LC_ALL
LANGUAGE=C
export LANGUAGE

# CDPATH.
(unset CDPATH) >/dev/null 2>&1 && unset CDPATH

if test "x$CONFIG_SHELL" = x; then
  as_bourne_compatible="if test -n \"\${ZSH_VERSION+set}\" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on \${1+\"\$@\"}, which
  # is contrary to our usage.  Disable this feature.
  alias -g '\${1+\"\$@\"}'='\"\$@\"'
  setopt NO_GLOB_SUBST
else
  case \`(set -o) 2>/dev/null\` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
fi
"
  as_required="as_fn_return () { (exit \$1); }
as_fn_success () { as_fn_return 0; }
as_fn_failure () { as_fn_return 1; }
as_fn_ret_success () { return 0; }
as_fn_ret_failure () { return 1; }

exitcode=0
as_fn_success || { exitcode=1; echo as_fn_success failed.; }
as_fn_failure && { exitcode=1; echo as_fn_failure succeeded.; }
as_fn_ret_success || { exitcode=1; echo as_fn_ret_success failed.; }
as_fn_ret_failure && { exitcode=1; echo as_fn_ret_failure succeeded.; }
if ( set x; as_fn_ret_success y && test x = \"\$1\" ); then :

else
  exitcode=1; echo positional parameters were not saved.
fi
test x\$exitcode = x0 || exit 1"
  as_suggested="  as_lineno_1=";as_suggested=$as_suggested$LINENO;as_suggested=$as_suggested" as_lineno_1a=\$LINENO
  as_lineno_2=";as_suggested=$as_suggested$LINENO;as_suggested=$as_suggested" as_lineno_2a=\$LINENO
  eval 'test \"x\$as_lineno_1'\$as_run'\" != \"x\$as_lineno_2'\$as_run'\" &&
  test \"x\`expr \$as_lineno_1'\$as_run' + 1\`\" = \"x\$as_lineno_2'\$as_run'\"' || exit 1
test \$(( 1 + 1 )) = 2 || exit 1"
  if (eval "$as_required") 2>/dev/null; then :
  as_have_required=yes
else
  as_have_required=no
fi
  if test x$as_have_required = xyes && (eval "$as_suggested") 2>/dev/null; then :

else
  as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
as_found=false
for as_dir in /bin$PATH_SEPARATOR/usr/bin$PATH_SEPARATOR$PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
  as_found=:
  case $as_dir in #(
	 /*)
	   for as_base in sh bash ksh sh5; do
	     # Try only shells that exist, to save several forks.
	     as_shell=$as_dir/$as_base
	     if { test -f "$as_shell" || test -f "$as_shell.exe"; } &&
		    { $as_echo "$as_bourne_compatible""$as_required" | as_run=a "$as_shell"; } 2>/dev/null; then :
  CONFIG_SHELL=$as_shell as_have_required=yes
		   if { $as_echo "$as_bourne_compatible""$as_suggested" | as_run=a "$as_shell"; } 2>/dev/null; then :
  break 2
fi
fi
	   done;;
       esac
  as_found=false
done
$as_found || { if { test -f "$SHELL" || test -f "$SHELL.exe"; } &&
	      { $as_echo "$as_bourne_compatible""$as_required" | as_run=a "$SHELL"; } 2>/dev/null; then :
  CONFIG_SHELL=$SHELL as_have_required=yes
fi; }
IFS=$as_save_IFS


      if test "x$CONFIG_SHELL" != x; then :
  # We cannot yet assume a decent shell, so we have to provide a
	# neutralization value for shells without unset; and this also
	# works around shells that cannot unset nonexistent variables.
	BASH_ENV=/dev/null
	ENV=/dev/null
	(unset BASH_ENV) >/dev/null 2>&1 && unset BASH_ENV ENV
	export CONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_dir" || eval $as_mkdir_p || {
    as_dirs=
    while :; do
      case $as_dir in #(
      *\'*) as_qdir=`$as_echo "$as_dir" | sed "s/'/'\\\\\\\\''/g"`;; #'(
      *) as_qdir=$as_dir;;
      esac
      as_dirs="'$as_qdir' $as_dirs"
      as_dir=`$as_dirname -- "$as_dir" ||
$as_expr X"$as_dir" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
	 X"$as_dir" : 'X\(//\)[^/]' \| \
	 X"$as_dir" : 'X\(//\)$' \| \
	 X"$as_dir" : 'X\(/\)' \| . 2>/dev/null ||
$as_echo X"$as_dir" |
    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{
	    s//\1/
	    q
	  }
	  /^X\(\/\/\)[^/].*/{
	    s//\1/
	    q
	  }
	  /^X\(\/\/\)$/{
	    s//\1/
	    q
	  }
	  /^X\(\/\).*/{
	    s//\1/
	    q
	  }
	  s/.*/./; q'`
      test -d "$as_dir" && break
    done
    test -z "$as_dirs" || eval "mkdir $as_dirs"
  } || test -d "$as_dir" || as_fn_error "cannot create directory $as_dir"


} # as_fn_mkdir_p
# as_fn_append VAR VALUE
# ----------------------
# Append the text in VALUE to the end of the definition contained in VAR. Take
# advantage of any shell optimizations that allow amortized linear growth over
# repeated appends, instead of the typical quadratic growth present in naive
# implementations.
if (eval "as_var=1; as_var+=2; test x\$as_var = x12") 2>/dev/null; then :
  eval 'as_fn_append ()
  {
    eval $1+=\$2
  }'
else
  as_fn_append ()
  {
    eval $1=\$$1\$2
  }
fi # as_fn_append

# as_fn_arith ARG...
# ------------------
# Perform arithmetic evaluation on the ARGs, and store the result in the
# global $as_val. Take advantage of shells that can avoid forks. The arguments
# must be portable across $(()) and expr.
if (eval "test \$(( 1 + 1 )) = 2") 2>/dev/null; then :
  eval 'as_fn_arith ()
  {
    as_val=$(( $* ))
  }'
else
  as_fn_arith ()
  {
    as_val=`expr "$@" || test $? -eq 1`
  }
fi # as_fn_arith


# as_fn_error ERROR [LINENO LOG_FD]
# ---------------------------------
# Output "`basename $0`: error: ERROR" to stderr. If LINENO and LOG_FD are
# provided, also output the error to LOG_FD, referencing LINENO. Then exit the
# script with status $?, using 1 if that was 0.
as_fn_error ()
{
  as_status=$?; test $as_status -eq 0 && as_status=1
  if test "$3"; then
    as_lineno=${as_lineno-"$2"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
    $as_echo "$as_me:${as_lineno-$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error: $1" >&2
  as_fn_exit $as_status
} # as_fn_error

if expr a : '\(a\)' >/dev/null 2>&1 &&
   test "X`expr 00001 : '.*\(...\)'`" = X001; then
  as_expr=expr
else
  as_expr=false
fi

if (basename -- /) >/dev/null 2>&1 && test "X`basename -- / 2>&1`" = "X/"; then
  as_basename=basename
else
  as_basename=false
fi

if (as_dir=`dirname -- /` && test "X$as_dir" = X/) >/dev/null 2>&1; then
  as_dirname=dirname
else
  as_dirname=false
fi

as_me=`$as_basename -- "$0" ||
$as_expr X/"$0" : '.*/\([^/][^/]*\)/*$' \| \
	 X"$0" : 'X\(//\)$' \| \
	 X"$0" : 'X\(/\)' \| . 2>/dev/null ||
$as_echo X/"$0" |
    sed '/^.*\/\([^/][^/]*\)\/*$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\).*/{
	    s//\1/
	    q
	  }
	  s/.*/./; q'`

# Avoid depending upon Character Ranges.
as_cr_letters='abcdefghijklmnopqrstuvwxyz'
as_cr_LETTERS='ABCDEFGHIJKLMNOPQRSTUVWXYZ'
as_cr_Letters=$as_cr_letters$as_cr_LETTERS
as_cr_digits='0123456789'
as_cr_alnum=$as_cr_Letters$as_cr_digits


  as_lineno_1=$LINENO as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  eval 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run' + 1`" = "x$as_lineno_2'$as_run'"' || {
  # Blame Lee E. McMahon (1931-1989) for sed's syntax.  :-)
  sed -n '
    p
    /[$]LINENO/=
  ' <$as_myself |
    sed '
      s/[$]LINENO.*/&-/
      t lineno
      b
      :lineno
      N
      :loop
      s/[$]LINENO\([^'$as_cr_alnum'_].*\n\)\(.*\)/\2\1\2/
      t loop
      s/-\n.*//
    ' >$as_me.lineno &&
  chmod +x "$as_me.lineno" ||
    { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2; as_fn_exit 1; }

  # Don't try to exec as it changes $[0], causing all sort of problems
  # (the dirname of $[0] is not the place where we might find the
  # original and so on.  Autoconf is especially sensitive to this).
  . "./$as_me.lineno"
  # Exit status is that of the last command.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #(((((
-n*)
  case `echo 'xy\c'` in
  *c*) ECHO_T='	';;	# ECHO_T is single tab character.
  xy)  ECHO_C='\c';;
  *)   echo `echo ksh88 bug on AIX 6.1` > /dev/null
       ECHO_T='	';;
  esac;;
*)
  ECHO_N='-n';;
esac

rm -f conf$$ conf$$.exe conf$$.file
if test -d conf$$.dir; then
  rm -f conf$$.dir/conf$$.file
else
  rm -f conf$$.dir
  mkdir conf$$.dir 2>/dev/null
fi
if (echo >conf$$.file) 2>/dev/null; then
  if ln -s conf$$.file conf$$ 2>/dev/null; then
    as_ln_s='ln -s'
    # ... but there are two gotchas:
    # 1) On MSYS, both `ln -s file dir' and `ln file dir' fail.
    # 2) DJGPP < 2.04 has no symlinks; `ln -s' creates a wrapper executable.
    # In both cases, we have to default to `cp -p'.
    ln -s conf$$.file conf$$.dir 2>/dev/null && test ! -f conf$$.exe ||
      as_ln_s='cp -p'
  elif ln conf$$.file conf$$ 2>/dev/null; then
    as_ln_s=ln
  else
    as_ln_s='cp -p'
  fi
else
  as_ln_s='cp -p'
fi
rm -f conf$$ conf$$.exe conf$$.dir/conf$$.file conf$$.file
rmdir conf$$.dir 2>/dev/null

if mkdir -p . 2>/dev/null; then
  as_mkdir_p='mkdir -p "$as_dir"'
else
  test -d ./-p && rmdir ./-p
  as_mkdir_p=false
fi

if test -x / >/dev/null 2>&1; then
  as_test_x='test -x'
else
  if ls -dL / >/dev/null 2>&1; then
    as_ls_L_option=L
  else
    as_ls_L_option=
  fi
  as_test_x='
    eval sh -c '\''
      if test -d "$1"; then
	test -d "$1/.";
      else
	case $1 in #(
	-*)set "./$1";;
	esac;
	case `ls -ld'$as_ls_L_option' "$1" 2>/dev/null` in #((
	???[sx]*):;;*)false;;esac;fi
    '\'' sh
  '
fi
as_executable_p=$as_test_x

# Sed expression to map a string onto a valid CPP name.
as_tr_cpp="eval sed 'y%*$as_cr_letters%P$as_cr_LETTERS%;s%[^_$as_cr_alnum]%_%g'"

# Sed expression to map a string onto a valid variable name.
as_tr_sh="eval sed 'y%*+%pp%;s%[^_$as_cr_alnum]%_%g'"



# Check that we are running under the correct shell.
SHELL=${CONFIG_SHELL-/bin/sh}

case X$lt_ECHO in
X*--fallback-echo)
  # Remove one level of quotation (which was required for Make).
  ECHO=`echo "$lt_ECHO" | sed 's,\\\\\$\\$0,'$0','`
  ;;
esac

ECHO=${lt_ECHO-echo}
if test "X$1" = X--no-reexec; then
  # Discard the --no-reexec flag, and continue.
  shift
elif test "X$1" = X--fallback-echo; then
  # Avoid inline document here, it may be left over
  :
elif test "X`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' ; then
  # Yippee, $ECHO works!
  :
else
  # Restart under the correct shell.
  exec $SHELL "$0" --no-reexec ${1+"$@"}
fi

if test "X$1" = X--fallback-echo; then
  # used as fallback echo
  shift
  cat <<_LT_EOF
$*
_LT_EOF
  exit 0
fi

# The HP-UX ksh and POSIX shell print the target directory to stdout
# if CDPATH is set.
(unset CDPATH) >/dev/null 2>&1 && unset CDPATH

if test -z "$lt_ECHO"; then
  if test "X${echo_test_string+set}" != Xset; then
    # find a string as large as possible, as long as the shell can cope with it
    for cmd in 'sed 50q "$0"' 'sed 20q "$0"' 'sed 10q "$0"' 'sed 2q "$0"' 'echo test'; do
      # expected sizes: less than 2Kb, 1Kb, 512 bytes, 16 bytes, ...
      if { echo_test_string=`eval $cmd`; } 2>/dev/null &&
	 { test "X$echo_test_string" = "X$echo_test_string"; } 2>/dev/null
      then
        break
      fi
    done
  fi

  if test "X`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
     echo_testing_string=`{ $ECHO "$echo_test_string"; } 2>/dev/null` &&
     test "X$echo_testing_string" = "X$echo_test_string"; then
    :
  else
    # The Solaris, AIX, and Digital Unix default echo programs unquote
    # backslashes.  This makes it impossible to quote backslashes using
    #   echo "$something" | sed 's/\\/\\\\/g'
    #
    # So, first we look for a working echo in the user's PATH.

    lt_save_ifs="$IFS"; IFS=$PATH_SEPARATOR
    for dir in $PATH /usr/ucb; do
      IFS="$lt_save_ifs"
      if (test -f $dir/echo || test -f $dir/echo$ac_exeext) &&
         test "X`($dir/echo '\t') 2>/dev/null`" = 'X\t' &&
         echo_testing_string=`($dir/echo "$echo_test_string") 2>/dev/null` &&
         test "X$echo_testing_string" = "X$echo_test_string"; then
        ECHO="$dir/echo"
        break
      fi
    done
    IFS="$lt_save_ifs"

    if test "X$ECHO" = Xecho; then
      # We didn't find a better echo, so look for alternatives.
      if test "X`{ print -r '\t'; } 2>/dev/null`" = 'X\t' &&
         echo_testing_string=`{ print -r "$echo_test_string"; } 2>/dev/null` &&
         test "X$echo_testing_string" = "X$echo_test_string"; then
        # This shell has a builtin print -r that does the trick.
        ECHO='print -r'
      elif { test -f /bin/ksh || test -f /bin/ksh$ac_exeext; } &&
	   test "X$CONFIG_SHELL" != X/bin/ksh; then
        # If we have ksh, try running configure again with it.
        ORIGINAL_CONFIG_SHELL=${CONFIG_SHELL-/bin/sh}
        export ORIGINAL_CONFIG_SHELL
        CONFIG_SHELL=/bin/ksh
        export CONFIG_SHELL
        exec $CONFIG_SHELL "$0" --no-reexec ${1+"$@"}
      else
        # Try using printf.
        ECHO='printf %s\n'
        if test "X`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
	   echo_testing_string=`{ $ECHO "$echo_test_string"; } 2>/dev/null` &&
	   test "X$echo_testing_string" = "X$echo_test_string"; then
	  # Cool, printf works
	  :
        elif echo_testing_string=`($ORIGINAL_CONFIG_SHELL "$0" --fallback-echo '\t') 2>/dev/null` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($ORIGINAL_CONFIG_SHELL "$0" --fallback-echo "$echo_test_string") 2>/dev/null` &&
	     test "X$echo_testing_string" = "X$echo_test_string"; then
	  CONFIG_SHELL=$ORIGINAL_CONFIG_SHELL
	  export CONFIG_SHELL
	  SHELL="$CONFIG_SHELL"
	  export SHELL
	  ECHO="$CONFIG_SHELL $0 --fallback-echo"
        elif echo_testing_string=`($CONFIG_SHELL "$0" --fallback-echo '\t') 2>/dev/null` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($CONFIG_SHELL "$0" --fallback-echo "$echo_test_string") 2>/dev/null` &&
	     test "X$echo_testing_string" = "X$echo_test_string"; then
	  ECHO="$CONFIG_SHELL $0 --fallback-echo"
        else
	  # maybe with a smaller string...
	  prev=:

	  for cmd in 'echo test' 'sed 2q "$0"' 'sed 10q "$0"' 'sed 20q "$0"' 'sed 50q "$0"'; do
	    if { test "X$echo_test_string" = "X`eval $cmd`"; } 2>/dev/null
	    then
	      break
	    fi
	    prev="$cmd"
	  done

	  if test "$prev" != 'sed 50q "$0"'; then
	    echo_test_string=`eval $prev`
	    export echo_test_string
	    exec ${ORIGINAL_CONFIG_SHELL-${CONFIG_SHELL-/bin/sh}} "$0" ${1+"$@"}
	  else
	    # Oops.  We lost completely, so just stick with echo.
	    ECHO=echo
	  fi
        fi
      fi
    fi
  fi
fi

# Copy echo and quote the copy suitably for passing to libtool from
# the Makefile, instead of quoting the original, which is used later.
lt_ECHO=$ECHO
if test "X$lt_ECHO" = "X$CONFIG_SHELL $0 --fallback-echo"; then
   lt_ECHO="$CONFIG_SHELL \\\$\$0 --fallback-echo"
fi




test -n "$DJDIR" || exec 7<&0 </dev/null
exec 6>&1

# Name of the host.
# hostname on some systems (SVR3.2, Linux) returns a bogus exit status,
# so uname gets run too.
ac_hostname=`(hostname || uname -n) 2>/dev/null | sed 1q`

#
# Initializations.
#
ac_default_prefix=/usr/local
ac_clean_files=
ac_config_libobj_dir=.
LIBOBJS=
cross_compiling=no
subdirs=
MFLAGS=
MAKEFLAGS=

# Identity of this package.
PACKAGE_NAME='cyassl'
PACKAGE_TARNAME='cyassl'
PACKAGE_VERSION='3.3.2'
PACKAGE_STRING='cyassl 3.3.2'
PACKAGE_BUGREPORT='https://github.com/cyassl/cyassl/issues'
PACKAGE_URL='http://www.wolfssl.com'

# Factoring default headers for most tests.
ac_includes_default="\
#include <stdio.h>
#ifdef HAVE_SYS_TYPES_H
# include <sys/types.h>
#endif
#ifdef HAVE_SYS_STAT_H
# include <sys/stat.h>
#endif
#ifdef STDC_HEADERS
# include <stdlib.h>
# include <stddef.h>
#else
# ifdef HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#endif
#ifdef HAVE_STRING_H
# if !defined STDC_HEADERS && defined HAVE_MEMORY_H
#  include <memory.h>
# endif
# include <string.h>
#endif
#ifdef HAVE_STRINGS_H
# include <strings.h>
#endif
#ifdef HAVE_INTTYPES_H
# include <inttypes.h>
#endif
#ifdef HAVE_STDINT_H
# include <stdint.h>
#endif
#ifdef HAVE_UNISTD_H
# include <unistd.h>
#endif"

ac_subst_vars='am__EXEEXT_FALSE
am__EXEEXT_TRUE
LTLIBOBJS
LIBOBJS
INC_AMINCLUDE
AMINCLUDE
GENERIC_CONFIG
AM_LDFLAGS
AM_CFLAGS
AM_CPPFLAGS
HEX_VERSION
IS_VCS_CHECKOUT_FALSE
IS_VCS_CHECKOUT_TRUE
BUILD_MCAPI_FALSE
BUILD_MCAPI_TRUE
BUILD_LIBZ_FALSE
BUILD_LIBZ_TRUE
BUILD_EXAMPLES_FALSE
BUILD_EXAMPLES_TRUE
BUILD_SLOWMATH_FALSE
BUILD_SLOWMATH_TRUE
BUILD_FASTMATH_FALSE
BUILD_FASTMATH_TRUE
USE_VALGRIND_FALSE
USE_VALGRIND_TRUE
HAVE_VALGRIND
BUILD_PKCS7_FALSE
BUILD_PKCS7_TRUE
BUILD_NTRU_FALSE
BUILD_NTRU_TRUE
BUILD_CRL_MONITOR_FALSE
BUILD_CRL_MONITOR_TRUE
BUILD_CRL_FALSE
BUILD_CRL_TRUE
HAVE_OPENSSL_CMD
BUILD_OCSP_FALSE
BUILD_OCSP_TRUE
BUILD_INLINE_FALSE
BUILD_INLINE_TRUE
BUILD_FIPS_FALSE
BUILD_FIPS_TRUE
BUILD_CHACHA_FALSE
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUILD_HC128_TRUE
BUILD_PWDBASED_FALSE
BUILD_PWDBASED_TRUE
BUILD_MD4_FALSE
BUILD_MD4_TRUE
BUILD_SHA_FALSE
BUILD_SHA_TRUE
BUILD_MD5_FALSE
BUILD_MD5_TRUE
BUILD_RC4_FALSE
BUILD_RC4_TRUE
BUILD_DES3_FALSE
BUILD_DES3_TRUE
BUILD_CODING_FALSE
BUILD_CODING_TRUE
BUILD_AES_FALSE
BUILD_AES_TRUE
BUILD_ASN_FALSE
BUILD_ASN_TRUE
BUILD_DH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILD_RSA_TRUE
BUILD_MEMORY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_SHA512_TRUE
BUILD_BLAKE2_FALSE
BUILD_BLAKE2_TRUE
BUILD_RIPEMD_FALSE
BUILD_RIPEMD_TRUE
BUILD_MD2_FALSE
BUILD_MD2_TRUE
BUILD_CAMELLIA_FALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALSE
BUILD_POLY1305_TRUE
BUILD_AESNI_FALSE
BUILD_AESNI_TRUE
BUILD_AESCCM_FALSE
BUILD_AESCCM_TRUE
BUILD_AESGCM_FALSE
BUILD_AESGCM_TRUE
BUILD_SNIFFTEST_FALSE
BUILD_SNIFFTEST_TRUE
BUILD_SNIFFER_FALSE
BUILD_SNIFFER_TRUE
BUILD_LEANPSK_FALSE
BUILD_LEANPSK_TRUE
PTHREAD_CFLAGS
PTHREAD_LIBS
PTHREAD_CC
ax_pthread_config
DEBUG_FALSE
DEBUG_TRUE
MCHECK
LIBM
am__fastdepCCAS_FALSE
am__fastdepCCAS_TRUE
CCASDEPMODE
CCASFLAGS
CCAS
AM_BACKSLASH
AM_DEFAULT_VERBOSITY
HAVE_VISIBILITY
CFLAG_VISIBILITY
CXXCPP
am__fastdepCXX_FALSE
am__fastdepCXX_TRUE
CXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOOL64
OTOOL
LIPO
NMEDIT
DSYMUTIL
lt_ECHO
RANLIB
AR
OBJDUMP
LN_S
NM
ac_ct_DUMPBIN
DUMPBIN
LD
FGREP
EGREP
GREP
SED
am__fastdepCC_FALSE
am__fastdepCC_TRUE
CCDEPMODE
AMDEPBACKSLASH
AMDEP_FALSE
AMDEP_TRUE
am__quote
am__include
DEPDIR
OBJEXT
EXEEXT
ac_ct_CC
CPPFLAGS
LDFLAGS
CFLAGS
CC
LIBTOOL
CYASSL_LIBRARY_VERSION
am__untar
am__tar
AMTAR
am__leading_dot
SET_MAKE
AWK
mkdir_p
MKDIR_P
INSTALL_STRIP_PROGRAM
STRIP
install_sh
MAKEINFO
AUTOHEADER
AUTOMAKE
AUTOCONF
ACLOCAL
VERSION
PACKAGE
CYGPATH_W
am__isrc
INSTALL_DATA
INSTALL_SCRIPT
INSTALL_PROGRAM
host_os
host_vendor
host_cpu
host
build_os
build_vendor
build_cpu
build
target_alias
host_alias
build_alias
LIBS
ECHO_T
ECHO_N
ECHO_C
DEFS
mandir
localedir
libdir
psdir
pdfdir
dvidir
htmldir
infodir
docdir
oldincludedir
includedir
localstatedir
sharedstatedir
sysconfdir
datadir
datarootdir
libexecdir
sbindir
bindir
program_transform_name
prefix
exec_prefix
PACKAGE_URL
PACKAGE_BUGREPORT
PACKAGE_STRING
PACKAGE_VERSION
PACKAGE_TARNAME
PACKAGE_NAME
PATH_SEPARATOR
SHELL'
ac_subst_files=''
ac_user_opts='
enable_option_checking
enable_static
enable_shared
with_pic
enable_fast_install
enable_dependency_tracking
with_gnu_ld
enable_libtool_lock
enable_silent_rules
enable_debug
enable_singlethreaded
enable_dtls
enable_opensslextra
enable_ipv6
enable_fortress
enable_bump
enable_leanpsk
enable_bigcache
enable_hugecache
enable_smallcache
enable_savesession
enable_savecert
enable_atomicuser
enable_pkcallbacks
enable_sniffer
enable_aesgcm
enable_aesccm
enable_aesni
enable_poly1305
enable_camellia
enable_md2
enable_nullcipher
enable_ripemd
enable_blake2
enable_sha512
enable_sessioncerts
enable_keygen
enable_certgen
enable_certreq
enable_sep
enable_hkdf
enable_dsa
enable_ecc
enable_fpecc
enable_eccencrypt
enable_psk
enable_errorstrings
enable_oldtls
enable_stacksize
enable_memory
enable_rsa
enable_dh
enable_anon
enable_asn
enable_aes
enable_coding
enable_des3
enable_arc4
enable_md5
enable_sha
enable_md4
enable_webserver
enable_pwdbased
enable_hc128
enable_rabbit
enable_chacha
enable_fips
enable_hashdrbg
enable_filesystem
enable_inline
enable_ocsp
enable_crl
enable_crl_monitor
with_ntru
enable_sni
enable_maxfragment
enable_truncatedhmac
enable_renegotiation_indication
enable_secure_renegotiation
enable_supportedcurves
enable_session_ticket
enable_tlsx
enable_pkcs7
enable_scep
enable_smallstack
enable_valgrind
enable_testcert
enable_iopool
enable_certservice
enable_fastmath
enable_fasthugemath
enable_examples
with_libz
with_cavium
enable_mcapi
enable_jobserver
'
      ac_precious_vars='build_alias
host_alias
target_alias
CC
CFLAGS
LDFLAGS
LIBS
CPPFLAGS
CPP
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some variables set by options.
ac_init_help=
ac_init_version=false
ac_unrecognized_opts=
ac_unrecognized_sep=
# The variables have the same names as the options, with
# dashes changed to underlines.
cache_file=/dev/null
exec_prefix=NONE
no_create=
no_recursion=
prefix=NONE
program_prefix=NONE
program_suffix=NONE
program_transform_name=s,x,x,
silent=
site=
srcdir=
verbose=
x_includes=NONE
x_libraries=NONE

# Installation directory options.
# These are left unexpanded so users can "make install exec_prefix=/foo"
# and all the variables that are supposed to be based on exec_prefix
# by default will actually change.
# Use braces instead of parens because sh, perl, etc. also accept them.
# (The list follows the same order as the GNU Coding Standards.)
bindir='${exec_prefix}/bin'
sbindir='${exec_prefix}/sbin'
libexecdir='${exec_prefix}/libexec'
datarootdir='${prefix}/share'
datadir='${datarootdir}'
sysconfdir='${prefix}/etc'
sharedstatedir='${prefix}/com'
localstatedir='${prefix}/var'
includedir='${prefix}/include'
oldincludedir='/usr/include'
docdir='${datarootdir}/doc/${PACKAGE_TARNAME}'
infodir='${datarootdir}/info'
htmldir='${docdir}'
dvidir='${docdir}'
pdfdir='${docdir}'
psdir='${docdir}'
libdir='${exec_prefix}/lib'
localedir='${datarootdir}/locale'
mandir='${datarootdir}/man'

ac_prev=
ac_dashdash=
for ac_option
do
  # If the previous option needs an argument, assign it.
  if test -n "$ac_prev"; then
    eval $ac_prev=\$ac_option
    ac_prev=
    continue
  fi

  case $ac_option in
  *=*)	ac_optarg=`expr "X$ac_option" : '[^=]*=\(.*\)'` ;;
  *)	ac_optarg=yes ;;
  esac

  # Accept the important Cygnus configure options, so we can diagnose typos.

  case $ac_dashdash$ac_option in
  --)
    ac_dashdash=yes ;;

  -bindir | --bindir | --bindi | --bind | --bin | --bi)
    ac_prev=bindir ;;
  -bindir=* | --bindir=* | --bindi=* | --bind=* | --bin=* | --bi=*)
    bindir=$ac_optarg ;;

  -build | --build | --buil | --bui | --bu)
    ac_prev=build_alias ;;
  -build=* | --build=* | --buil=* | --bui=* | --bu=*)
    build_alias=$ac_optarg ;;

  -cache-file | --cache-file | --cache-fil | --cache-fi \
  | --cache-f | --cache- | --cache | --cach | --cac | --ca | --c)
    ac_prev=cache_file ;;
  -cache-file=* | --cache-file=* | --cache-fil=* | --cache-fi=* \
  | --cache-f=* | --cache-=* | --cache=* | --cach=* | --cac=* | --ca=* | --c=*)
    cache_file=$ac_optarg ;;

  --config-cache | -C)
    cache_file=config.cache ;;

  -datadir | --datadir | --datadi | --datad)
    ac_prev=datadir ;;
  -datadir=* | --datadir=* | --datadi=* | --datad=*)
    datadir=$ac_optarg ;;

  -datarootdir | --datarootdir | --datarootdi | --datarootd | --dataroot \
  | --dataroo | --dataro | --datar)
    ac_prev=datarootdir ;;
  -datarootdir=* | --datarootdir=* | --datarootdi=* | --datarootd=* \
  | --dataroot=* | --dataroo=* | --dataro=* | --datar=*)
    datarootdir=$ac_optarg ;;

  -disable-* | --disable-*)
    ac_useropt=`expr "x$ac_option" : 'x-*disable-\(.*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_error "invalid feature name: $ac_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"enable_$ac_useropt"
"*) ;;
      *) ac_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_sep--disable-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
    eval enable_$ac_useropt=no ;;

  -docdir | --docdir | --docdi | --doc | --do)
    ac_prev=docdir ;;
  -docdir=* | --docdir=* | --docdi=* | --doc=* | --do=*)
    docdir=$ac_optarg ;;

  -dvidir | --dvidir | --dvidi | --dvid | --dvi | --dv)
    ac_prev=dvidir ;;
  -dvidir=* | --dvidir=* | --dvidi=* | --dvid=* | --dvi=* | --dv=*)
    dvidir=$ac_optarg ;;

  -enable-* | --enable-*)
    ac_useropt=`expr "x$ac_option" : 'x-*enable-\([^=]*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_error "invalid feature name: $ac_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"enable_$ac_useropt"
"*) ;;
      *) ac_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_sep--enable-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
    eval enable_$ac_useropt=\$ac_optarg ;;

  -exec-prefix | --exec_prefix | --exec-prefix | --exec-prefi \
  | --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \
  | --exec | --exe | --ex)
    ac_prev=exec_prefix ;;
  -exec-prefix=* | --exec_prefix=* | --exec-prefix=* | --exec-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* | --ex=*)
    exec_prefix=$ac_optarg ;;

  -gas | --gas | --ga | --g)
    # Obsolete; use --with-gas.
    with_gas=yes ;;

  -help | --help | --hel | --he | -h)
    ac_init_help=long ;;
  -help=r* | --help=r* | --hel=r* | --he=r* | -hr*)
    ac_init_help=recursive ;;
  -help=s* | --help=s* | --hel=s* | --he=s* | -hs*)
    ac_init_help=short ;;

  -host | --host | --hos | --ho)
    ac_prev=host_alias ;;
  -host=* | --host=* | --hos=* | --ho=*)
    host_alias=$ac_optarg ;;

  -htmldir | --htmldir | --htmldi | --htmld | --html | --htm | --ht)
    ac_prev=htmldir ;;
  -htmldir=* | --htmldir=* | --htmldi=* | --htmld=* | --html=* | --htm=* \
  | --ht=*)
    htmldir=$ac_optarg ;;

  -includedir | --includedir | --includedi | --included | --include \
  | --includ | --inclu | --incl | --inc)
    ac_prev=includedir ;;
  -includedir=* | --includedir=* | --includedi=* | --included=* | --include=* \
  | --includ=* | --inclu=* | --incl=* | --inc=*)
    includedir=$ac_optarg ;;

  -infodir | --infodir | --infodi | --infod | --info | --inf)
    ac_prev=infodir ;;
  -infodir=* | --infodir=* | --infodi=* | --infod=* | --info=* | --inf=*)
    infodir=$ac_optarg ;;

  -libdir | --libdir | --libdi | --libd)
    ac_prev=libdir ;;
  -libdir=* | --libdir=* | --libdi=* | --libd=*)
    libdir=$ac_optarg ;;

  -libexecdir | --libexecdir | --libexecdi | --libexecd | --libexec \
  | --libexe | --libex | --libe)
    ac_prev=libexecdir ;;
  -libexecdir=* | --libexecdir=* | --libexecdi=* | --libexecd=* | --libexec=* \
  | --libexe=* | --libex=* | --libe=*)
    libexecdir=$ac_optarg ;;

  -localedir | --localedir | --localedi | --localed | --locale)
    ac_prev=localedir ;;
  -localedir=* | --localedir=* | --localedi=* | --localed=* | --locale=*)
    localedir=$ac_optarg ;;

  -localstatedir | --localstatedir | --localstatedi | --localstated \
  | --localstate | --localstat | --localsta | --localst | --locals)
    ac_prev=localstatedir ;;
  -localstatedir=* | --localstatedir=* | --localstatedi=* | --localstated=* \
  | --localstate=* | --localstat=* | --localsta=* | --localst=* | --locals=*)
    localstatedir=$ac_optarg ;;

  -mandir | --mandir | --mandi | --mand | --man | --ma | --m)
    ac_prev=mandir ;;
  -mandir=* | --mandir=* | --mandi=* | --mand=* | --man=* | --ma=* | --m=*)
    mandir=$ac_optarg ;;

  -nfp | --nfp | --nf)
    # Obsolete; use --without-fp.
    with_fp=no ;;

  -no-create | --no-create | --no-creat | --no-crea | --no-cre \
  | --no-cr | --no-c | -n)
    no_create=yes ;;

  -no-recursion | --no-recursion | --no-recursio | --no-recursi \
  | --no-recurs | --no-recur | --no-recu | --no-rec | --no-re | --no-r)
    no_recursion=yes ;;

  -oldincludedir | --oldincludedir | --oldincludedi | --oldincluded \
  | --oldinclude | --oldinclud | --oldinclu | --oldincl | --oldinc \
  | --oldin | --oldi | --old | --ol | --o)
    ac_prev=oldincludedir ;;
  -oldincludedir=* | --oldincludedir=* | --oldincludedi=* | --oldincluded=* \
  | --oldinclude=* | --oldinclud=* | --oldinclu=* | --oldincl=* | --oldinc=* \
  | --oldin=* | --oldi=* | --old=* | --ol=* | --o=*)
    oldincludedir=$ac_optarg ;;

  -prefix | --prefix | --prefi | --pref | --pre | --pr | --p)
    ac_prev=prefix ;;
  -prefix=* | --prefix=* | --prefi=* | --pref=* | --pre=* | --pr=* | --p=*)
    prefix=$ac_optarg ;;

  -program-prefix | --program-prefix | --program-prefi | --program-pref \
  | --program-pre | --program-pr | --program-p)
    ac_prev=program_prefix ;;
  -program-prefix=* | --program-prefix=* | --program-prefi=* \
  | --program-pref=* | --program-pre=* | --program-pr=* | --program-p=*)
    program_prefix=$ac_optarg ;;

  -program-suffix | --program-suffix | --program-suffi | --program-suff \
  | --program-suf | --program-su | --program-s)
    ac_prev=program_suffix ;;
  -program-suffix=* | --program-suffix=* | --program-suffi=* \
  | --program-suff=* | --program-suf=* | --program-su=* | --program-s=*)
    program_suffix=$ac_optarg ;;

  -program-transform-name | --program-transform-name \
  | --program-transform-nam | --program-transform-na \
  | --program-transform-n | --program-transform- \
  | --program-transform | --program-transfor \
  | --program-transfo | --program-transf \
  | --program-trans | --program-tran \
  | --progr-tra | --program-tr | --program-t)
    ac_prev=program_transform_name ;;
  -program-transform-name=* | --program-transform-name=* \
  | --program-transform-nam=* | --program-transform-na=* \
  | --program-transform-n=* | --program-transform-=* \
  | --program-transform=* | --program-transfor=* \
  | --program-transfo=* | --program-transf=* \
  | --program-trans=* | --program-tran=* \
  | --progr-tra=* | --program-tr=* | --program-t=*)
    program_transform_name=$ac_optarg ;;

  -pdfdir | --pdfdir | --pdfdi | --pdfd | --pdf | --pd)
    ac_prev=pdfdir ;;
  -pdfdir=* | --pdfdir=* | --pdfdi=* | --pdfd=* | --pdf=* | --pd=*)
    pdfdir=$ac_optarg ;;

  -psdir | --psdir | --psdi | --psd | --ps)
    ac_prev=psdir ;;
  -psdir=* | --psdir=* | --psdi=* | --psd=* | --ps=*)
    psdir=$ac_optarg ;;

  -q | -quiet | --quiet | --quie | --qui | --qu | --q \
  | -silent | --silent | --silen | --sile | --sil)
    silent=yes ;;

  -sbindir | --sbindir | --sbindi | --sbind | --sbin | --sbi | --sb)
    ac_prev=sbindir ;;
  -sbindir=* | --sbindir=* | --sbindi=* | --sbind=* | --sbin=* \
  | --sbi=* | --sb=*)
    sbindir=$ac_optarg ;;

  -sharedstatedir | --sharedstatedir | --sharedstatedi \
  | --sharedstated | --sharedstate | --sharedstat | --sharedsta \
  | --sharedst | --shareds | --shared | --share | --shar \
  | --sha | --sh)
    ac_prev=sharedstatedir ;;
  -sharedstatedir=* | --sharedstatedir=* | --sharedstatedi=* \
  | --sharedstated=* | --sharedstate=* | --sharedstat=* | --sharedsta=* \
  | --sharedst=* | --shareds=* | --shared=* | --share=* | --shar=* \
  | --sha=* | --sh=*)
    sharedstatedir=$ac_optarg ;;

  -site | --site | --sit)
    ac_prev=site ;;
  -site=* | --site=* | --sit=*)
    site=$ac_optarg ;;

  -srcdir | --srcdir | --srcdi | --srcd | --src | --sr)
    ac_prev=srcdir ;;
  -srcdir=* | --srcdir=* | --srcdi=* | --srcd=* | --src=* | --sr=*)
    srcdir=$ac_optarg ;;

  -sysconfdir | --sysconfdir | --sysconfdi | --sysconfd | --sysconf \
  | --syscon | --sysco | --sysc | --sys | --sy)
    ac_prev=sysconfdir ;;
  -sysconfdir=* | --sysconfdir=* | --sysconfdi=* | --sysconfd=* | --sysconf=* \
  | --syscon=* | --sysco=* | --sysc=* | --sys=* | --sy=*)
    sysconfdir=$ac_optarg ;;

  -target | --target | --targe | --targ | --tar | --ta | --t)
    ac_prev=target_alias ;;
  -target=* | --target=* | --targe=* | --targ=* | --tar=* | --ta=* | --t=*)
    target_alias=$ac_optarg ;;

  -v | -verbose | --verbose | --verbos | --verbo | --verb)
    verbose=yes ;;

  -version | --version | --versio | --versi | --vers | -V)
    ac_init_version=: ;;

  -with-* | --with-*)
    ac_useropt=`expr "x$ac_option" : 'x-*with-\([^=]*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_error "invalid package name: $ac_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"with_$ac_useropt"
"*) ;;
      *) ac_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_sep--with-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
    eval with_$ac_useropt=\$ac_optarg ;;

  -without-* | --without-*)
    ac_useropt=`expr "x$ac_option" : 'x-*without-\(.*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_error "invalid package name: $ac_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"with_$ac_useropt"
"*) ;;
      *) ac_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_sep--without-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
    eval with_$ac_useropt=no ;;

  --x)
    # Obsolete; use --with-x.
    with_x=yes ;;

  -x-includes | --x-includes | --x-include | --x-includ | --x-inclu \
  | --x-incl | --x-inc | --x-in | --x-i)
    ac_prev=x_includes ;;
  -x-includes=* | --x-includes=* | --x-include=* | --x-includ=* | --x-inclu=* \
  | --x-incl=* | --x-inc=* | --x-in=* | --x-i=*)
    x_includes=$ac_optarg ;;

  -x-libraries | --x-libraries | --x-librarie | --x-librari \
  | --x-librar | --x-libra | --x-libr | --x-lib | --x-li | --x-l)
    ac_prev=x_libraries ;;
  -x-libraries=* | --x-libraries=* | --x-librarie=* | --x-librari=* \
  | --x-librar=* | --x-libra=* | --x-libr=* | --x-lib=* | --x-li=* | --x-l=*)
    x_libraries=$ac_optarg ;;

  -*) as_fn_error "unrecognized option: \`$ac_option'
Try \`$0 --help' for more information."
    ;;

  *=*)
    ac_envvar=`expr "x$ac_option" : 'x\([^=]*\)='`
    # Reject names that are not valid shell variable names.
    case $ac_envvar in #(
      '' | [0-9]* | *[!_$as_cr_alnum]* )
      as_fn_error "invalid variable name: \`$ac_envvar'" ;;
    esac
    eval $ac_envvar=\$ac_optarg
    export $ac_envvar ;;

  *)
    # FIXME: should be removed in autoconf 3.0.
    $as_echo "$as_me: WARNING: you should use --build, --host, --target" >&2
    expr "x$ac_option" : ".*[^-._$as_cr_alnum]" >/dev/null &&
      $as_echo "$as_me: WARNING: invalid host type: $ac_option" >&2
    : ${build_alias=$ac_option} ${host_alias=$ac_option} ${target_alias=$ac_option}
    ;;

  esac
done

if test -n "$ac_prev"; then
  ac_option=--`echo $ac_prev | sed 's/_/-/g'`
  as_fn_error "missing argument to $ac_option"
fi

if test -n "$ac_unrecognized_opts"; then
  case $enable_option_checking in
    no) ;;
    fatal) as_fn_error "unrecognized options: $ac_unrecognized_opts" ;;
    *)     $as_echo "$as_me: WARNING: unrecognized options: $ac_unrecognized_opts" >&2 ;;
  esac
fi

# Check all directory arguments for consistency.
for ac_var in	exec_prefix prefix bindir sbindir libexecdir datarootdir \
		datadir sysconfdir sharedstatedir localstatedir includedir \
		oldincludedir docdir infodir htmldir dvidir pdfdir psdir \
		libdir localedir mandir
do
  eval ac_val=\$$ac_var
  # Remove trailing slashes.
  case $ac_val in
    */ )
      ac_val=`expr "X$ac_val" : 'X\(.*[^/]\)' \| "X$ac_val" : 'X\(.*\)'`
      eval $ac_var=\$ac_val;;
  esac
  # Be sure to have absolute directory names.
  case $ac_val in
    [\\/$]* | ?:[\\/]* )  continue;;
    NONE | '' ) case $ac_var in *prefix ) continue;; esac;;
  esac
  as_fn_error "expected an absolute directory name for --$ac_var: $ac_val"
done

# There might be people who depend on the old broken behavior: `$host'
# used to hold the argument of --host etc.
# FIXME: To remove some day.
build=$build_alias
host=$host_alias
target=$target_alias

# FIXME: To remove some day.
if test "x$host_alias" != x; then
  if test "x$build_alias" = x; then
    cross_compiling=maybe
    $as_echo "$as_me: WARNING: If you wanted to set the --build type, don't use --host.
    If a cross compiler is detected then cross compile mode will be used." >&2
  elif test "x$build_alias" != "x$host_alias"; then
    cross_compiling=yes
  fi
fi

ac_tool_prefix=
test -n "$host_alias" && ac_tool_prefix=$host_alias-

test "$silent" = yes && exec 6>/dev/null


ac_pwd=`pwd` && test -n "$ac_pwd" &&
ac_ls_di=`ls -di .` &&
ac_pwd_ls_di=`cd "$ac_pwd" && ls -di .` ||
  as_fn_error "working directory cannot be determined"
test "X$ac_ls_di" = "X$ac_pwd_ls_di" ||
  as_fn_error "pwd does not report name of working directory"


# Find the source files, if location was not specified.
if test -z "$srcdir"; then
  ac_srcdir_defaulted=yes
  # Try the directory containing this script, then the parent directory.
  ac_confdir=`$as_dirname -- "$as_myself" ||
$as_expr X"$as_myself" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
	 X"$as_myself" : 'X\(//\)[^/]' \| \
	 X"$as_myself" : 'X\(//\)$' \| \
	 X"$as_myself" : 'X\(/\)' \| . 2>/dev/null ||
$as_echo X"$as_myself" |
    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{
	    s//\1/
	    q
	  }
	  /^X\(\/\/\)[^/].*/{
	    s//\1/
	    q
	  }
	  /^X\(\/\/\)$/{
	    s//\1/
	    q
	  }
	  /^X\(\/\).*/{
	    s//\1/
	    q
	  }
	  s/.*/./; q'`
  srcdir=$ac_confdir
  if test ! -r "$srcdir/$ac_unique_file"; then
    srcdir=..
  fi
else
  ac_srcdir_defaulted=no
fi
if test ! -r "$srcdir/$ac_unique_file"; then
  test "$ac_srcdir_defaulted" = yes && srcdir="$ac_confdir or .."
  as_fn_error "cannot find sources ($ac_unique_file) in $srcdir"
fi
ac_msg="sources are in $srcdir, but \`cd $srcdir' does not work"
ac_abs_confdir=`(
	cd "$srcdir" && test -r "./$ac_unique_file" || as_fn_error "$ac_msg"
	pwd)`
# When building in place, set srcdir=.
if test "$ac_abs_confdir" = "$ac_pwd"; then
  srcdir=.
fi
# Remove unnecessary trailing slashes from srcdir.
# Double slashes in file names in object file debugging info
# mess up M-x gdb in Emacs.
case $srcdir in
*/) srcdir=`expr "X$srcdir" : 'X\(.*[^/]\)' \| "X$srcdir" : 'X\(.*\)'`;;
esac
for ac_var in $ac_precious_vars; do
  eval ac_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_env_${ac_var}_value=\$${ac_var}
  eval ac_cv_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

#
# Report the --help message.
#
if test "$ac_init_help" = "long"; then
  # Omit some internal or obsolete options to make the list less imposing.
  # This message is too long to be a string in the A/UX 3.1 sh.
  cat <<_ACEOF
\`configure' configures cyassl 3.3.2 to adapt to many kinds of systems.

Usage: $0 [OPTION]... [VAR=VALUE]...

To assign environment variables (e.g., CC, CFLAGS...), specify them as
VAR=VALUE.  See below for descriptions of some of the useful variables.

Defaults for the options are specified in brackets.

Configuration:
  -h, --help              display this help and exit
      --help=short        display options specific to this package
      --help=recursive    display the short help of all the included packages
  -V, --version           display version information and exit
  -q, --quiet, --silent   do not print \`checking...' messages
      --cache-file=FILE   cache test results in FILE [disabled]
  -C, --config-cache      alias for \`--cache-file=config.cache'
  -n, --no-create         do not create output files
      --srcdir=DIR        find the sources in DIR [configure dir or \`..']

Installation directories:
  --prefix=PREFIX         install architecture-independent files in PREFIX
                          [$ac_default_prefix]
  --exec-prefix=EPREFIX   install architecture-dependent files in EPREFIX
                          [PREFIX]

By default, \`make install' will install all the files in
\`$ac_default_prefix/bin', \`$ac_default_prefix/lib' etc.  You can specify
an installation prefix other than \`$ac_default_prefix' using \`--prefix',
for instance \`--prefix=\$HOME'.

For better control, use the options below.

Fine tuning of the installation directories:
  --bindir=DIR            user executables [EPREFIX/bin]
  --sbindir=DIR           system admin executables [EPREFIX/sbin]
  --libexecdir=DIR        program executables [EPREFIX/libexec]
  --sysconfdir=DIR        read-only single-machine data [PREFIX/etc]
  --sharedstatedir=DIR    modifiable architecture-independent data [PREFIX/com]
  --localstatedir=DIR     modifiable single-machine data [PREFIX/var]
  --libdir=DIR            object code libraries [EPREFIX/lib]
  --includedir=DIR        C header files [PREFIX/include]
  --oldincludedir=DIR     C header files for non-gcc [/usr/include]
  --datarootdir=DIR       read-only arch.-independent data root [PREFIX/share]
  --datadir=DIR           read-only architecture-independent data [DATAROOTDIR]
  --infodir=DIR           info documentation [DATAROOTDIR/info]
  --localedir=DIR         locale-dependent data [DATAROOTDIR/locale]
  --mandir=DIR            man documentation [DATAROOTDIR/man]
  --docdir=DIR            documentation root [DATAROOTDIR/doc/cyassl]
  --htmldir=DIR           html documentation [DOCDIR]
  --dvidir=DIR            dvi documentation [DOCDIR]
  --pdfdir=DIR            pdf documentation [DOCDIR]
  --psdir=DIR             ps documentation [DOCDIR]
_ACEOF

  cat <<\_ACEOF

Program names:
  --program-prefix=PREFIX            prepend PREFIX to installed program names
  --program-suffix=SUFFIX            append SUFFIX to installed program names
  --program-transform-name=PROGRAM   run sed PROGRAM on installed program names

System types:
  --build=BUILD     configure for building on BUILD [guessed]
  --host=HOST       cross-compile to build programs to run on HOST [BUILD]
_ACEOF
fi

if test -n "$ac_init_help"; then
  case $ac_init_help in
     short | recursive ) echo "Configuration of cyassl 3.3.2:";;
   esac
  cat <<\_ACEOF

Optional Features:
  --disable-option-checking  ignore unrecognized --enable/--with options
  --disable-FEATURE       do not include FEATURE (same as --enable-FEATURE=no)
  --enable-FEATURE[=ARG]  include FEATURE [ARG=yes]
  --enable-static[=PKGS]  build static libraries [default=no]
  --enable-shared[=PKGS]  build shared libraries [default=yes]
  --enable-fast-install[=PKGS]
                          optimize for fast installation [default=yes]
  --disable-dependency-tracking  speeds up one-time build
  --enable-dependency-tracking   do not reject slow dependency extractors
  --disable-libtool-lock  avoid locking (might break parallel builds)
  --enable-silent-rules          less verbose build output (undo: `make V=1')
  --disable-silent-rules         verbose build output (undo: `make V=0')
  --enable-debug          Add debug code/turns off optimizations (yes|no)
                          [default=no]
  --enable-singlethreaded Enable CyaSSL single threaded (default: disabled)
  --enable-dtls           Enable CyaSSL DTLS (default: disabled)
  --enable-opensslextra   Enable extra OpenSSL API, size+ (default: disabled)
  --enable-ipv6           Enable testing of IPV6 (default: disabled)
  --enable-fortress       Enable SSL fortress build (default: disabled)
  --enable-bump           Enable SSL Bump build (default: disabled)
  --enable-leanpsk        Enable Lean PSK build (default: disabled)
  --enable-bigcache       Enable big session cache (default: disabled)
  --enable-hugecache      Enable huge session cache (default: disabled)
  --enable-smallcache     Enable small session cache (default: disabled)
  --enable-savesession    Enable persistent session cache (default: disabled)
  --enable-savecert       Enable persistent cert cache (default: disabled)
  --enable-atomicuser     Enable Atomic User Record Layer (default: disabled)
  --enable-pkcallbacks    Enable Public Key Callbacks (default: disabled)
  --enable-sniffer        Enable CyaSSL sniffer support (default: disabled)
  --enable-aesgcm         Enable CyaSSL AES-GCM support (default: disabled)
  --enable-aesccm         Enable CyaSSL AES-CCM support (default: disabled)
  --enable-aesni          Enable CyaSSL AES-NI support (default: disabled)
  --enable-poly1305         Enable CyaSSL POLY1305 support (default: disabled)
  --enable-camellia       Enable CyaSSL Camellia support (default: disabled)
  --enable-md2            Enable CyaSSL MD2 support (default: disabled)
  --enable-nullcipher     Enable CyaSSL NULL cipher support (default: disabled)
  --enable-ripemd         Enable CyaSSL RIPEMD-160 support (default: disabled)
  --enable-blake2         Enable CyaSSL BLAKE2 support (default: disabled)
  --enable-sha512         Enable CyaSSL SHA-512 support (default: disabled)
  --enable-sessioncerts   Enable session cert storing (default: disabled)
  --enable-keygen         Enable key generation (default: disabled)
  --enable-certgen        Enable cert generation (default: disabled)
  --enable-certreq        Enable cert request generation (default: disabled)
  --enable-sep            Enable sep extensions (default: disabled)
  --enable-hkdf           Enable HKDF (HMAC-KDF) support (default: disabled)
  --enable-dsa            Enable DSA (default: disabled)
  --enable-ecc            Enable ECC (default: disabled)
  --enable-fpecc          Enable Fixed Point cache ECC (default: disabled)
  --enable-eccencrypt     Enable ECC encrypt (default: disabled)
  --enable-psk            Enable PSK (default: disabled)
  --enable-errorstrings   Enable error strings table (default: enabled)
  --enable-oldtls         Enable old TLS versions < 1.2 (default: enabled)
  --enable-stacksize      Enable stack size info on examples (default: disabled)
  --enable-memory         Enable memory callbacks (default: enabled)
  --enable-rsa            Enable RSA (default: enabled)
  --enable-dh             Enable DH (default: disabled)
  --enable-anon           Enable Anonymous (default: disabled)
  --enable-asn            Enable ASN (default: enabled)
  --enable-aes            Enable AES (default: enabled)
  --enable-coding         Enable Coding base 16/64 (default: enabled)
  --enable-des3           Enable DES3 (default: enabled)
  --enable-arc4           Enable ARC4 (default: enabled)
  --enable-md5            Enable MD5 (default: enabled)
  --enable-sha            Enable SHA (default: enabled)
  --enable-md4            Enable MD4 (default: disabled)
  --enable-webserver      Enable Web Server (default: disabled)
  --enable-pwdbased       Enable PWDBASED (default: disabled)
  --enable-hc128          Enable HC-128 (default: disabled)
  --enable-rabbit         Enable RABBIT (default: disabled)
  --enable-chacha         Enable CHACHA (default: disabled)
  --enable-fips           Enable FIPS 140-2 (default: disabled)
  --enable-hashdrbg       Enable Hash DRBG support (default: enabled)
  --enable-filesystem     Enable Filesystem support (default: enabled)
  --enable-inline         Enable inline functions (default: enabled)
  --enable-ocsp           Enable OCSP (default: disabled)
  --enable-crl            Enable CRL (default: disabled)
  --enable-crl-monitor    Enable CRL Monitor (default: disabled)
  --enable-sni            Enable SNI (default: disabled)
  --enable-maxfragment    Enable Maximum Fragment Length (default: disabled)
  --enable-truncatedhmac  Enable Truncated HMAC (default: disabled)
  --enable-renegotiation-indication  Enable Renegotiation Indication (default: disabled)
  --enable-secure-renegotiation Enable Secure Renegotiation (default: disabled)
  --enable-supportedcurves Enable Supported Elliptic Curves (default: disabled)
  --enable-session-ticket  Enable Session Ticket (default: disabled)
  --enable-tlsx           Enable all TLS Extensions (default: disabled)
  --enable-pkcs7          Enable PKCS7 (default: disabled)
  --enable-scep           Enable wolfSCEP (default: disabled)
  --enable-smallstack     Enable Small Stack Usage (default: disabled)
  --enable-valgrind       Enable valgrind for unit tests (default: disabled)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (default: disabled)
  --enable-certservice    Enable cert service (default: disabled)
  --enable-fastmath       Enable fast math ops (default: enabled on x86_64)
  --enable-fasthugemath   Enable fast math + huge code (default: disabled)
  --enable-examples       Enable Examples  (default: enabled)
  --enable-mcapi          Enable Microchip API (default: disabled)
  --enable-jobserver[=no/yes/#] default=yes
                        Enable up to # make jobs
                        yes: enable one more than CPU count


Optional Packages:
  --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --with-pic              try to use only PIC/non-PIC objects [default=use
                          both]
  --with-gnu-ld           assume the C compiler uses GNU ld [default=no]
  --with-ntru=PATH        Path to NTRU install (default /usr/)
  --with-libz=PATH        PATH to libz install (default /usr/)
  --with-cavium=PATH      PATH to cavium/software dir

Some influential environment variables:
  CC          C compiler command
  CFLAGS      C compiler flags
  LDFLAGS     linker flags, e.g. -L<lib dir> if you have libraries in a
              nonstandard directory <lib dir>
  LIBS        libraries to pass to the linker, e.g. -l<library>
  CPPFLAGS    (Objective) C/C++ preprocessor flags, e.g. -I<include dir> if
              you have headers in a nonstandard directory <include dir>
  CPP         C preprocessor
  CXX         C++ compiler command
  CXXFLAGS    C++ compiler flags
  CXXCPP      C++ preprocessor
  CCAS        assembler compiler command (defaults to CC)
  CCASFLAGS   assembler compiler flags (defaults to CFLAGS)

Use these variables to override the choices made by `configure' or to help
it to find libraries and programs with nonstandard names/locations.

Report bugs to <https://github.com/cyassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test "$ac_init_help" = "recursive"; then
  # If there are subdirs, report their specific --help.
  for ac_dir in : $ac_subdirs_all; do test "x$ac_dir" = x: && continue
    test -d "$ac_dir" ||
      { cd "$srcdir" && ac_pwd=`pwd` && srcdir=. && test -d "$ac_dir"; } ||
      continue
    ac_builddir=.

case "$ac_dir" in
.) ac_dir_suffix= ac_top_builddir_sub=. ac_top_build_prefix= ;;
*)
  ac_dir_suffix=/`$as_echo "$ac_dir" | sed 's|^\.[\\/]||'`
  # A ".." for each directory in $ac_dir_suffix.
  ac_top_builddir_sub=`$as_echo "$ac_dir_suffix" | sed 's|/[^\\/]*|/..|g;s|/||'`
  case $ac_top_builddir_sub in
  "") ac_top_builddir_sub=. ac_top_build_prefix= ;;
  *)  ac_top_build_prefix=$ac_top_builddir_sub/ ;;
  esac ;;
esac
ac_abs_top_builddir=$ac_pwd
ac_abs_builddir=$ac_pwd$ac_dir_suffix
# for backward compatibility:
ac_top_builddir=$ac_top_build_prefix

case $srcdir in
  .)  # We are building in place.
    ac_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute name.
    ac_srcdir=$srcdir$ac_dir_suffix;
    ac_top_srcdir=$srcdir
    ac_abs_top_srcdir=$srcdir ;;
  *) # Relative name.
    ac_srcdir=$ac_top_build_prefix$srcdir$ac_dir_suffix
    ac_top_srcdir=$ac_top_build_prefix$srcdir
    ac_abs_top_srcdir=$ac_pwd/$srcdir ;;
esac
ac_abs_srcdir=$ac_abs_top_srcdir$ac_dir_suffix

    cd "$ac_dir" || { ac_status=$?; continue; }
    # Check for guested configure.
    if test -f "$ac_srcdir/configure.gnu"; then
      echo &&
      $SHELL "$ac_srcdir/configure.gnu" --help=recursive
    elif test -f "$ac_srcdir/configure"; then
      echo &&
      $SHELL "$ac_srcdir/configure" --help=recursive
    else
      $as_echo "$as_me: WARNING: no configuration information is in $ac_dir" >&2
    fi || ac_status=$?
    cd "$ac_pwd" || { ac_status=$?; break; }
  done
fi

test -n "$ac_init_help" && exit $ac_status
if $ac_init_version; then
  cat <<\_ACEOF
cyassl configure 3.3.2
generated by GNU Autoconf 2.65

Copyright (C) 2009 Free Software Foundation, Inc.
This configure script is free software; the Free Software Foundation
gives unlimited permission to copy, distribute and modify it.
_ACEOF
  exit
fi

## ------------------------ ##
## Autoconf initialization. ##
## ------------------------ ##

# ac_fn_c_try_compile LINENO
# --------------------------
# Try to compile conftest.$ac_ext, and return whether this succeeded.
ac_fn_c_try_compile ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext
  if { { ac_try="$ac_compile"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_compile") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_c_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_try_compile

# ac_fn_c_try_link LINENO
# -----------------------
# Try to link conftest.$ac_ext, and return whether this succeeded.
ac_fn_c_try_link ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_c_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest$ac_exeext && {
	 test "$cross_compiling" = yes ||
	 $as_test_x conftest$ac_exeext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) information
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next link command; also delete a directory that is
  # left behind by Apple's compiler.  We do this before executing the actions.
  rm -rf conftest.dSYM conftest_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_try_link

# ac_fn_c_check_header_compile LINENO HEADER VAR INCLUDES
# -------------------------------------------------------
# Tests whether HEADER exists and can be compiled using the include files in
# INCLUDES, setting the cache variable VAR accordingly.
ac_fn_c_check_header_compile ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  eval "$3=yes"
else
  eval "$3=no"
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_header_compile

# ac_fn_c_try_cpp LINENO
# ----------------------
# Try to preprocess conftest.$ac_ext, and return whether this succeeded.
ac_fn_c_try_cpp ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_cpp conftest.$ac_ext"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_cpp conftest.$ac_ext") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } >/dev/null && {
	 test -z "$ac_c_preproc_warn_flag$ac_c_werror_flag" ||
	 test ! -s conftest.err
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

    ac_retval=1
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_try_cpp

# ac_fn_c_try_run LINENO
# ----------------------
# Try to link conftest.$ac_ext, and return whether this succeeded. Assumes
# that executables *can* be run.
ac_fn_c_try_run ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && { ac_try='./conftest$ac_exeext'
  { { case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_try") 2>&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; }; }; then :
  ac_retval=0
else
  $as_echo "$as_me: program exited with status $ac_status" >&5
       $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

       ac_retval=$ac_status
fi
  rm -rf conftest.dSYM conftest_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_try_run

# ac_fn_c_check_func LINENO FUNC VAR
# ----------------------------------
# Tests whether FUNC exists, setting the cache variable VAR accordingly
ac_fn_c_check_func ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
/* Define $2 to an innocuous variant, in case <limits.h> declares $2.
   For example, HP-UX 11i <limits.h> declares gettimeofday.  */
#define $2 innocuous_$2

/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $2 (); below.
    Prefer <limits.h> to <assert.h> if __STDC__ is defined, since
    <limits.h> exists even on freestanding compilers.  */

#ifdef __STDC__
# include <limits.h>
#else
# include <assert.h>
#endif

#undef $2

/* Override any GCC internal prototype to avoid an error.
   Use char because int might match the return type of a GCC
   builtin and then its argument prototype would still apply.  */
#ifdef __cplusplus
extern "C"
#endif
char $2 ();
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined __stub_$2 || defined __stub___$2
choke me
#endif

int
main ()
{
return $2 ();
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_link "$LINENO"; then :
  eval "$3=yes"
else
  eval "$3=no"
fi
rm -f core conftest.err conftest.$ac_objext \
    conftest$ac_exeext conftest.$ac_ext
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_func

# ac_fn_cxx_try_compile LINENO
# ----------------------------
# Try to compile conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_compile ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext
  if { { ac_try="$ac_compile"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_compile") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_compile

# ac_fn_cxx_try_cpp LINENO
# ------------------------
# Try to preprocess conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_cpp ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_cpp conftest.$ac_ext"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_cpp conftest.$ac_ext") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } >/dev/null && {
	 test -z "$ac_cxx_preproc_warn_flag$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

    ac_retval=1
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_cpp

# ac_fn_cxx_try_link LINENO
# -------------------------
# Try to link conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_link ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest$ac_exeext && {
	 test "$cross_compiling" = yes ||
	 $as_test_x conftest$ac_exeext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) information
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next link command; also delete a directory that is
  # left behind by Apple's compiler.  We do this before executing the actions.
  rm -rf conftest.dSYM conftest_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_link

# ac_fn_c_check_header_mongrel LINENO HEADER VAR INCLUDES
# -------------------------------------------------------
# Tests whether HEADER exists, giving a warning if it cannot be compiled using
# the include files in INCLUDES and setting the cache variable VAR
# accordingly.
ac_fn_c_check_header_mongrel ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
else
  # Is the header compilable?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 usability" >&5
$as_echo_n "checking $2 usability... " >&6; }
cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_header_compiler=yes
else
  ac_header_compiler=no
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_header_compiler" >&5
$as_echo "$ac_header_compiler" >&6; }

# Is the header present?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 presence" >&5
$as_echo_n "checking $2 presence... " >&6; }
cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
#include <$2>
_ACEOF
if ac_fn_c_try_cpp "$LINENO"; then :
  ac_header_preproc=yes
else
  ac_header_preproc=no
fi
rm -f conftest.err conftest.$ac_ext
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_header_preproc" >&5
$as_echo "$ac_header_preproc" >&6; }

# So?  What about this header?
case $ac_header_compiler:$ac_header_preproc:$ac_c_preproc_warn_flag in #((
  yes:no: )
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: accepted by the compiler, rejected by the preprocessor!" >&5
$as_echo "$as_me: WARNING: $2: accepted by the compiler, rejected by the preprocessor!" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
    ;;
  no:yes:* )
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: present but cannot be compiled" >&5
$as_echo "$as_me: WARNING: $2: present but cannot be compiled" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2:     check for missing prerequisite headers?" >&5
$as_echo "$as_me: WARNING: $2:     check for missing prerequisite headers?" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: see the Autoconf documentation" >&5
$as_echo "$as_me: WARNING: $2: see the Autoconf documentation" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&5
$as_echo "$as_me: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
( cat <<\_ASBOX
## ------------------------------------------------------ ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## ------------------------------------------------------ ##
_ASBOX
     ) | sed "s/^/$as_me: WARNING:     /" >&2
    ;;
esac
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval "$3=\$ac_header_compiler"
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_header_mongrel

# ac_fn_c_compute_int LINENO EXPR VAR INCLUDES
# --------------------------------------------
# Tries to find the compile-time value of EXPR in a program that includes
# INCLUDES, setting VAR accordingly. Returns whether the value could be
# computed
ac_fn_c_compute_int ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if test "$cross_compiling" = yes; then
    # Depending upon the size, compute the lo and hi bounds.
cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) >= 0)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_lo=0 ac_mid=0
  while :; do
    cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) <= $ac_mid)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_hi=$ac_mid; break
else
  as_fn_arith $ac_mid + 1 && ac_lo=$as_val
			if test $ac_lo -le $ac_mid; then
			  ac_lo= ac_hi=
			  break
			fi
			as_fn_arith 2 '*' $ac_mid + 1 && ac_mid=$as_val
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
  done
else
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) < 0)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_hi=-1 ac_mid=-1
  while :; do
    cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) >= $ac_mid)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_lo=$ac_mid; break
else
  as_fn_arith '(' $ac_mid ')' - 1 && ac_hi=$as_val
			if test $ac_mid -le $ac_hi; then
			  ac_lo= ac_hi=
			  break
			fi
			as_fn_arith 2 '*' $ac_mid && ac_mid=$as_val
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
  done
else
  ac_lo= ac_hi=
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  as_fn_arith '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo && ac_mid=$as_val
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) <= $ac_mid)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_hi=$ac_mid
else
  as_fn_arith '(' $ac_mid ')' + 1 && ac_lo=$as_val
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
done
case $ac_lo in #((
?*) eval "$3=\$ac_lo"; ac_retval=0 ;;
'') ac_retval=1 ;;
esac
  else
    cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
static long int longval () { return $2; }
static unsigned long int ulongval () { return $2; }
#include <stdio.h>
#include <stdlib.h>
int
main ()
{

  FILE *f = fopen ("conftest.val", "w");
  if (! f)
    return 1;
  if (($2) < 0)
    {
      long int i = longval ();
      if (i != ($2))
	return 1;
      fprintf (f, "%ld", i);
    }
  else
    {
      unsigned long int i = ulongval ();
      if (i != ($2))
	return 1;
      fprintf (f, "%lu", i);
    }
  /* Do not output a trailing newline, as this causes \r\n confusion
     on some platforms.  */
  return ferror (f) || fclose (f) != 0;

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_run "$LINENO"; then :
  echo >>conftest.val; read $3 <conftest.val; ac_retval=0
else
  ac_retval=1
fi
rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_compute_int

# ac_fn_c_check_type LINENO TYPE VAR INCLUDES
# -------------------------------------------
# Tests whether TYPE exists after having included INCLUDES, setting cache
# variable VAR accordingly.
ac_fn_c_check_type ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval "$3=no"
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof ($2))
	 return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof (($2)))
	    return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :

else
  eval "$3=yes"
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_type

# ac_fn_c_find_uintX_t LINENO BITS VAR
# ------------------------------------
# Finds an unsigned integer type with width BITS, setting cache variable VAR
# accordingly.
ac_fn_c_find_uintX_t ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for uint$2_t" >&5
$as_echo_n "checking for uint$2_t... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval "$3=no"
     # Order is important - never check a type that is potentially smaller
     # than half of the expected target width.
     for ac_type in uint$2_t 'unsigned int' 'unsigned long int' \
	 'unsigned long long int' 'unsigned short int' 'unsigned char'; do
       cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$ac_includes_default
int
main ()
{
static int test_array [1 - 2 * !((($ac_type) -1 >> ($2 / 2 - 1)) >> ($2 / 2 - 1) == 3)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  case $ac_type in #(
  uint$2_t) :
    eval "$3=yes" ;; #(
  *) :
    eval "$3=\$ac_type" ;;
esac
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
       eval as_val=\$$3
   if test "x$as_val" = x""no; then :

else
  break
fi
     done
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_find_uintX_t
cat >config.log <<_ACEOF
This file contains any messages produced by compilers while
running configure, to aid debugging if configure makes a mistake.

It was created by cyassl $as_me 3.3.2, which was
generated by GNU Autoconf 2.65.  Invocation command line was

  $ $0 $@

_ACEOF
exec 5>>config.log
{
cat <<_ASUNAME
## --------- ##
## Platform. ##
## --------- ##

hostname = `(hostname || uname -n) 2>/dev/null | sed 1q`
uname -m = `(uname -m) 2>/dev/null || echo unknown`
uname -r = `(uname -r) 2>/dev/null || echo unknown`
uname -s = `(uname -s) 2>/dev/null || echo unknown`
uname -v = `(uname -v) 2>/dev/null || echo unknown`

/usr/bin/uname -p = `(/usr/bin/uname -p) 2>/dev/null || echo unknown`
/bin/uname -X     = `(/bin/uname -X) 2>/dev/null     || echo unknown`

/bin/arch              = `(/bin/arch) 2>/dev/null              || echo unknown`
/usr/bin/arch -k       = `(/usr/bin/arch -k) 2>/dev/null       || echo unknown`
/usr/convex/getsysinfo = `(/usr/convex/getsysinfo) 2>/dev/null || echo unknown`
/usr/bin/hostinfo      = `(/usr/bin/hostinfo) 2>/dev/null      || echo unknown`
/bin/machine           = `(/bin/machine) 2>/dev/null           || echo unknown`
/usr/bin/oslevel       = `(/usr/bin/oslevel) 2>/dev/null       || echo unknown`
/bin/universe          = `(/bin/universe) 2>/dev/null          || echo unknown`

_ASUNAME

as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing shell meta-characters.
# Make two passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_keep_next=false
for ac_pass in 1 2
do
  for ac_arg
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) continue ;;
    -q | -quiet | --quiet | --quie | --qui | --qu | --q \
    | -silent | --silent | --silen | --sile | --sil)
      continue ;;
    *\'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_configure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_next=true ;;
	esac
      fi
      as_fn_append ac_configure_args " '$ac_arg'"
      ;;
    esac
  done
done
{ ac_configure_args0=; unset ac_configure_args0;}
{ ac_configure_args1=; unset ac_configure_args1;}

# When interrupted or exit'd, cleanup temporary files, and complete
# config.log.  We remove comments because anyway the quotes in there
# would cause problems or look ugly.
# WARNING: Use '\'' to represent an apostrophe within the trap.
# WARNING: Do not start the trap code with a newline, due to a FreeBSD 4.0 bug.
trap 'exit_status=$?
  # Save into config.log some information that might help in debugging.
  {
    echo

    cat <<\_ASBOX
## ---------------- ##
## Cache variables. ##
## ---------------- ##
_ASBOX
    echo
    # The following way of writing the cache mishandles newlines in values,
(
  for ac_var in `(set) 2>&1 | sed -n '\''s/^\([a-zA-Z_][a-zA-Z0-9_]*\)=.*/\1/p'\''`; do
    eval ac_val=\$$ac_var
    case $ac_val in #(
    *${as_nl}*)
      case $ac_var in #(
      *_cv_*) { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: cache variable $ac_var contains a newline" >&5
$as_echo "$as_me: WARNING: cache variable $ac_var contains a newline" >&2;} ;;
      esac
      case $ac_var in #(
      _ | IFS | as_nl) ;; #(
      BASH_ARGV | BASH_SOURCE) eval $ac_var= ;; #(
      *) { eval $ac_var=; unset $ac_var;} ;;
      esac ;;
    esac
  done
  (set) 2>&1 |
    case $as_nl`(ac_space='\'' '\''; set) 2>&1` in #(
    *${as_nl}ac_space=\ *)
      sed -n \
	"s/'\''/'\''\\\\'\'''\''/g;
	  s/^\\([_$as_cr_alnum]*_cv_[_$as_cr_alnum]*\\)=\\(.*\\)/\\1='\''\\2'\''/p"
      ;; #(
    *)
      sed -n "/^[_$as_cr_alnum]*_cv_[_$as_cr_alnum]*=/p"
      ;;
    esac |
    sort
)
    echo

    cat <<\_ASBOX
## ----------------- ##
## Output variables. ##
## ----------------- ##
_ASBOX
    echo
    for ac_var in $ac_subst_vars
    do
      eval ac_val=\$$ac_var
      case $ac_val in
      *\'\''*) ac_val=`$as_echo "$ac_val" | sed "s/'\''/'\''\\\\\\\\'\'''\''/g"`;;
      esac
      $as_echo "$ac_var='\''$ac_val'\''"
    done | sort
    echo

    if test -n "$ac_subst_files"; then
      cat <<\_ASBOX
## ------------------- ##
## File substitutions. ##
## ------------------- ##
_ASBOX
      echo
      for ac_var in $ac_subst_files
      do
	eval ac_val=\$$ac_var
	case $ac_val in
	*\'\''*) ac_val=`$as_echo "$ac_val" | sed "s/'\''/'\''\\\\\\\\'\'''\''/g"`;;
	esac
	$as_echo "$ac_var='\''$ac_val'\''"
      done | sort
      echo
    fi

    if test -s confdefs.h; then
      cat <<\_ASBOX
## ----------- ##
## confdefs.h. ##
## ----------- ##
_ASBOX
      echo
      cat confdefs.h
      echo
    fi
    test "$ac_signal" != 0 &&
      $as_echo "$as_me: caught signal $ac_signal"
    $as_echo "$as_me: exit $exit_status"
  } >&5
  rm -f core *.core core.conftest.* &&
    rm -f -r conftest* confdefs* conf$$* $ac_clean_files &&
    exit $exit_status
' 0
for ac_signal in 1 2 13 15; do
  trap 'ac_signal='$ac_signal'; as_fn_exit 1' $ac_signal
done
ac_signal=0

# confdefs.h avoids OS command line length limits that DEFS can exceed.
rm -f -r conftest* confdefs.h

$as_echo "/* confdefs.h */" > confdefs.h

# Predefined preprocessor variables.

cat >>confdefs.h <<_ACEOF
#define PACKAGE_NAME "$PACKAGE_NAME"
_ACEOF

cat >>confdefs.h <<_ACEOF
#define PACKAGE_TARNAME "$PACKAGE_TARNAME"
_ACEOF

cat >>confdefs.h <<_ACEOF
#define PACKAGE_VERSION "$PACKAGE_VERSION"
_ACEOF

cat >>confdefs.h <<_ACEOF
#define PACKAGE_STRING "$PACKAGE_STRING"
_ACEOF

cat >>confdefs.h <<_ACEOF
#define PACKAGE_BUGREPORT "$PACKAGE_BUGREPORT"
_ACEOF

cat >>confdefs.h <<_ACEOF
#define PACKAGE_URL "$PACKAGE_URL"
_ACEOF


# Let the site file select an alternate cache file if it wants to.
# Prefer an explicitly selected file to automatically selected ones.
ac_site_file1=NONE
ac_site_file2=NONE
if test -n "$CONFIG_SITE"; then
  ac_site_file1=$CONFIG_SITE
elif test "x$prefix" != xNONE; then
  ac_site_file1=$prefix/share/config.site
  ac_site_file2=$prefix/etc/config.site
else
  ac_site_file1=$ac_default_prefix/share/config.site
  ac_site_file2=$ac_default_prefix/etc/config.site
fi
for ac_site_file in "$ac_site_file1" "$ac_site_file2"
do
  test "x$ac_site_file" = xNONE && continue
  if test /dev/null != "$ac_site_file" && test -r "$ac_site_file"; then
    { $as_echo "$as_me:${as_lineno-$LINENO}: loading site script $ac_site_file" >&5
$as_echo "$as_me: loading site script $ac_site_file" >&6;}
    sed 's/^/| /' "$ac_site_file" >&5
    . "$ac_site_file"
  fi
done

if test -r "$cache_file"; then
  # Some versions of bash will fail to source /dev/null (special files
  # actually), so we avoid doing that.  DJGPP emulates it as a regular file.
  if test /dev/null != "$cache_file" && test -f "$cache_file"; then
    { $as_echo "$as_me:${as_lineno-$LINENO}: loading cache $cache_file" >&5
$as_echo "$as_me: loading cache $cache_file" >&6;}
    case $cache_file in
      [\\/]* | ?:[\\/]* ) . "$cache_file";;
      *)                      . "./$cache_file";;
    esac
  fi
else
  { $as_echo "$as_me:${as_lineno-$LINENO}: creating cache $cache_file" >&5
$as_echo "$as_me: creating cache $cache_file" >&6;}
  >$cache_file
fi

# Check that the precious variables saved in the cache have kept the same
# value.
ac_cache_corrupted=false
for ac_var in $ac_precious_vars; do
  eval ac_old_set=\$ac_cv_env_${ac_var}_set
  eval ac_new_set=\$ac_env_${ac_var}_set
  eval ac_old_val=\$ac_cv_env_${ac_var}_value
  eval ac_new_val=\$ac_env_${ac_var}_value
  case $ac_old_set,$ac_new_set in
    set,)
      { $as_echo "$as_me:${as_lineno-$LINENO}: error: \`$ac_var' was set to \`$ac_old_val' in the previous run" >&5
$as_echo "$as_me: error: \`$ac_var' was set to \`$ac_old_val' in the previous run" >&2;}
      ac_cache_corrupted=: ;;
    ,set)
      { $as_echo "$as_me:${as_lineno-$LINENO}: error: \`$ac_var' was not set in the previous run" >&5
$as_echo "$as_me: error: \`$ac_var' was not set in the previous run" >&2;}
      ac_cache_corrupted=: ;;
    ,);;
    *)
      if test "x$ac_old_val" != "x$ac_new_val"; then
	# differences in whitespace do not lead to failure.
	ac_old_val_w=`echo x $ac_old_val`
	ac_new_val_w=`echo x $ac_new_val`
	if test "$ac_old_val_w" != "$ac_new_val_w"; then
	  { $as_echo "$as_me:${as_lineno-$LINENO}: error: \`$ac_var' has changed since the previous run:" >&5
$as_echo "$as_me: error: \`$ac_var' has changed since the previous run:" >&2;}
	  ac_cache_corrupted=:
	else
	  { $as_echo "$as_me:${as_lineno-$LINENO}: warning: ignoring whitespace changes in \`$ac_var' since the previous run:" >&5
$as_echo "$as_me: warning: ignoring whitespace changes in \`$ac_var' since the previous run:" >&2;}
	  eval $ac_var=\$ac_old_val
	fi
	{ $as_echo "$as_me:${as_lineno-$LINENO}:   former value:  \`$ac_old_val'" >&5
$as_echo "$as_me:   former value:  \`$ac_old_val'" >&2;}
	{ $as_echo "$as_me:${as_lineno-$LINENO}:   current value: \`$ac_new_val'" >&5
$as_echo "$as_me:   current value: \`$ac_new_val'" >&2;}
      fi;;
  esac
  # Pass precious variables to config.status.
  if test "$ac_new_set" = set; then
    case $ac_new_val in
    *\'*) ac_arg=$ac_var=`$as_echo "$ac_new_val" | sed "s/'/'\\\\\\\\''/g"` ;;
    *) ac_arg=$ac_var=$ac_new_val ;;
    esac
    case " $ac_configure_args " in
      *" '$ac_arg' "*) ;; # Avoid dups.  Use of quotes ensures accuracy.
      *) as_fn_append ac_configure_args " '$ac_arg'" ;;
    esac
  fi
done
if $ac_cache_corrupted; then
  { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
$as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
  { $as_echo "$as_me:${as_lineno-$LINENO}: error: changes in the environment can compromise the build" >&5
$as_echo "$as_me: error: changes in the environment can compromise the build" >&2;}
  as_fn_error "run \`make distclean' and/or \`rm $cache_file' and start over" "$LINENO" 5
fi
## -------------------- ##
## Main body of script. ##
## -------------------- ##

ac_ext=c
ac_cpp='$CPP $CPPFLAGS'
ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_c_compiler_gnu



ac_aux_dir=
for ac_dir in build-aux "$srcdir"/build-aux; do
  for ac_t in install-sh install.sh shtool; do
    if test -f "$ac_dir/$ac_t"; then
      ac_aux_dir=$ac_dir
      ac_install_sh="$ac_aux_dir/$ac_t -c"
      break 2
    fi
  done
done
if test -z "$ac_aux_dir"; then
  as_fn_error "cannot find install-sh, install.sh, or shtool in build-aux \"$srcdir\"/build-aux" "$LINENO" 5
fi

# These three variables are undocumented and unsupported,
# and are intended to be withdrawn in a future Autoconf release.
# They can cause serious problems if a builder's source tree is in a directory
# whose full name contains unusual characters.
ac_config_guess="$SHELL $ac_aux_dir/config.guess"  # Please don't use this var.
ac_config_sub="$SHELL $ac_aux_dir/config.sub"  # Please don't use this var.
ac_configure="$SHELL $ac_aux_dir/configure"  # Please don't use this var.



# Make sure we can run config.sub.
$SHELL "$ac_aux_dir/config.sub" sun4 >/dev/null 2>&1 ||
  as_fn_error "cannot run $SHELL $ac_aux_dir/config.sub" "$LINENO" 5

{ $as_echo "$as_me:${as_lineno-$LINENO}: checking build system type" >&5
$as_echo_n "checking build system type... " >&6; }
if test "${ac_cv_build+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  ac_build_alias=$build_alias
test "x$ac_build_alias" = x &&
  ac_build_alias=`$SHELL "$ac_aux_dir/config.guess"`
test "x$ac_build_alias" = x &&
  as_fn_error "cannot guess build type; you must specify one" "$LINENO" 5
ac_cv_build=`$SHELL "$ac_aux_dir/config.sub" $ac_build_alias` ||
  as_fn_error "$SHELL $ac_aux_dir/config.sub $ac_build_alias failed" "$LINENO" 5

fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_build" >&5
$as_echo "$ac_cv_build" >&6; }
case $ac_cv_build in
*-*-*) ;;
*) as_fn_error "invalid value of canonical build" "$LINENO" 5;;
esac
build=$ac_cv_build
ac_save_IFS=$IFS; IFS='-'
set x $ac_cv_build
shift
build_cpu=$1
build_vendor=$2
shift; shift
# Remember, the first character of IFS is used to create $*,
# except with old shells:
build_os=$*
IFS=$ac_save_IFS
case $build_os in *\ *) build_os=`echo "$build_os" | sed 's/ /-/g'`;; esac


{ $as_echo "$as_me:${as_lineno-$LINENO}: checking host system type" >&5
$as_echo_n "checking host system type... " >&6; }
if test "${ac_cv_host+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  if test "x$host_alias" = x; then
  ac_cv_host=$ac_cv_build
else
  ac_cv_host=`$SHELL "$ac_aux_dir/config.sub" $host_alias` ||
    as_fn_error "$SHELL $ac_aux_dir/config.sub $host_alias failed" "$LINENO" 5
fi

fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_host" >&5
$as_echo "$ac_cv_host" >&6; }
case $ac_cv_host in
*-*-*) ;;
*) as_fn_error "invalid value of canonical host" "$LINENO" 5;;
esac
host=$ac_cv_host
ac_save_IFS=$IFS; IFS='-'
set x $ac_cv_host
shift
host_cpu=$1
host_vendor=$2
shift; shift
# Remember, the first character of IFS is used to create $*,
# except with old shells:
host_os=$*
IFS=$ac_save_IFS
case $host_os in *\ *) host_os=`echo "$host_os" | sed 's/ /-/g'`;; esac




am__api_version='1.11'

# Find a good install program.  We prefer a C program (faster),
# so one script is as good as another.  But avoid the broken or
# incompatible versions:
# SysV /etc/install, /usr/sbin/install
# SunOS /usr/etc/install
# IRIX /sbin/install
# AIX /bin/install
# AmigaOS /C/install, which installs bootblocks on floppy discs
# AIX 4 /usr/bin/installbsd, which doesn't work without a -g flag
# AFS /usr/afsws/bin/install, which mishandles nonexistent args
# SVR4 /usr/ucb/install, which tries to use the nonexistent group "staff"
# OS/2's system install, which has a completely different semantic
# ./install, which can be erroneously created by make from ./install.sh.
# Reject install programs that cannot install multiple files.
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for a BSD-compatible install" >&5
$as_echo_n "checking for a BSD-compatible install... " >&6; }
if test -z "$INSTALL"; then
if test "${ac_cv_path_install+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    # Account for people who put trailing slashes in PATH elements.
case $as_dir/ in #((
  ./ | .// | /[cC]/* | \
  /etc/* | /usr/sbin/* | /usr/etc/* | /sbin/* | /usr/afsws/bin/* | \
  ?:[\\/]os2[\\/]install[\\/]* | ?:[\\/]OS2[\\/]INSTALL[\\/]* | \
  /usr/ucb/* ) ;;
  *)
    # OSF1 and SCO ODT 3.0 have their own names for install.
    # Don't use installbsd from OSF since it installs stuff as root
    # by default.
    for ac_prog in ginstall scoinst install; do
      for ac_exec_ext in '' $ac_executable_extensions; do
	if { test -f "$as_dir/$ac_prog$ac_exec_ext" && $as_test_x "$as_dir/$ac_prog$ac_exec_ext"; }; then
	  if test $ac_prog = install &&
	    grep dspmsg "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
	    # AIX install.  It has an incompatible calling convention.
	    :
	  elif test $ac_prog = install &&
	    grep pwplus "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
	    # program-specific install script used by HP pwplus--don't use.
	    :
	  else
	    rm -rf conftest.one conftest.two conftest.dir
	    echo one > conftest.one
	    echo two > conftest.two
	    mkdir conftest.dir
	    if "$as_dir/$ac_prog$ac_exec_ext" -c conftest.one conftest.two "`pwd`/conftest.dir" &&
	      test -s conftest.one && test -s conftest.two &&
	      test -s conftest.dir/conftest.one &&
	      test -s conftest.dir/conftest.two
	    then
	      ac_cv_path_install="$as_dir/$ac_prog$ac_exec_ext -c"
	      break 3
	    fi
	  fi
	fi
      done
    done
    ;;
esac

  done
IFS=$as_save_IFS

rm -rf conftest.one conftest.two conftest.dir

fi
  if test "${ac_cv_path_install+set}" = set; then
    INSTALL=$ac_cv_path_install
  else
    # As a last resort, use the slow shell script.  Don't cache a
    # value for INSTALL within a source directory, because that will
    # break other packages using the cache if that directory is
    # removed, or if the value is a relative name.
    INSTALL=$ac_install_sh
  fi
fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $INSTALL" >&5
$as_echo "$INSTALL" >&6; }

# Use test -z because SunOS4 sh mishandles braces in ${var-val}.
# It thinks the first close brace ends the variable substitution.
test -z "$INSTALL_PROGRAM" && INSTALL_PROGRAM='${INSTALL}'

test -z "$INSTALL_SCRIPT" && INSTALL_SCRIPT='${INSTALL}'

test -z "$INSTALL_DATA" && INSTALL_DATA='${INSTALL} -m 644'

{ $as_echo "$as_me:${as_lineno-$LINENO}: checking whether build environment is sane" >&5
$as_echo_n "checking whether build environment is sane... " >&6; }
# Just in case
sleep 1
echo timestamp > conftest.file
# Reject unsafe characters in $srcdir or the absolute working directory
# name.  Accept space and tab only in the latter.
am_lf='
'
case `pwd` in
  *[\\\"\#\$\&\'\`$am_lf]*)
    as_fn_error "unsafe absolute working directory name" "$LINENO" 5;;
esac
case $srcdir in
  *[\\\"\#\$\&\'\`$am_lf\ \	]*)
    as_fn_error "unsafe srcdir value: \`$srcdir'" "$LINENO" 5;;
esac

# Do `set' in a subshell so we don't clobber the current shell's
# arguments.  Must try -L first in case configure is actually a
# symlink; some systems play weird games with the mod time of symlinks
# (eg FreeBSD returns the mod time of the symlink's containing
# directory).
if (
   set X `ls -Lt "$srcdir/configure" conftest.file 2> /dev/null`
   if test "$*" = "X"; then
      # -L didn't work.
      set X `ls -t "$srcdir/configure" conftest.file`
   fi
   rm -f conftest.file
   if test "$*" != "X $srcdir/configure conftest.file" \
      && test "$*" != "X conftest.file $srcdir/configure"; then

      # If neither matched, then we have a broken ls.  This can happen
      # if, for instance, CONFIG_SHELL is bash and it inherits a
      # broken ls alias from the environment.  This has actually
      # happened.  Such a system could not be considered "sane".
      as_fn_error "ls -t appears to fail.  Make sure there is not a broken
alias in your environment" "$LINENO" 5
   fi

   test "$2" = conftest.file
   )
then
   # Ok.
   :
else
   as_fn_error "newly created file is older than distributed files!
Check your system clock" "$LINENO" 5
fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
$as_echo "yes" >&6; }
test "$program_prefix" != NONE &&
  program_transform_name="s&^&$program_prefix&;$program_transform_name"
# Use a double $ so make ignores it.
test "$program_suffix" != NONE &&
  program_transform_name="s&\$&$program_suffix&;$program_transform_name"
# Double any \ or $.
# By default was `s,x,x', remove it if useless.
ac_script='s/[\\$]/&&/g;s/;s,x,x,$//'
program_transform_name=`$as_echo "$program_transform_name" | sed "$ac_script"`

# expand $ac_aux_dir to an absolute path
am_aux_dir=`cd $ac_aux_dir && pwd`

if test x"${MISSING+set}" != xset; then
  case $am_aux_dir in
  *\ * | *\	*)
    MISSING="\${SHELL} \"$am_aux_dir/missing\"" ;;
  *)
    MISSING="\${SHELL} $am_aux_dir/missing" ;;
  esac
fi
# Use eval to expand $SHELL
if eval "$MISSING --run true"; then
  am_missing_run="$MISSING --run "
else
  am_missing_run=
  { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: \`missing' script is too old or missing" >&5
$as_echo "$as_me: WARNING: \`missing' script is too old or missing" >&2;}
fi

if test x"${install_sh}" != xset; then
  case $am_aux_dir in
  *\ * | *\	*)
    install_sh="\${SHELL} '$am_aux_dir/install-sh'" ;;
  *)
    install_sh="\${SHELL} $am_aux_dir/install-sh"
  esac
fi

# Installed binaries are usually stripped using `strip' when the user
# run `make install-strip'.  However `strip' might not be the right
# tool to use in cross-compilation environments, therefore Automake
# will honor the `STRIP' environment variable to overrule this program.
if test "$cross_compiling" != no; then
  if test -n "$ac_tool_prefix"; then
  # Extract the first word of "${ac_tool_prefix}strip", so it can be a program name with args.
set dummy ${ac_tool_prefix}strip; ac_word=$2
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
$as_echo_n "checking for $ac_word... " >&6; }
if test "${ac_cv_prog_STRIP+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  if test -n "$STRIP"; then
  ac_cv_prog_STRIP="$STRIP" # Let the user override the test.
else
as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    for ac_exec_ext in '' $ac_executable_extensions; do
  if { test -f "$as_dir/$ac_word$ac_exec_ext" && $as_test_x "$as_dir/$ac_word$ac_exec_ext"; }; then
    ac_cv_prog_STRIP="${ac_tool_prefix}strip"
    $as_echo "$as_me:${as_lineno-$LINENO}: found $as_dir/$ac_word$ac_exec_ext" >&5
    break 2
  fi
done
  done
IFS=$as_save_IFS

fi
fi
STRIP=$ac_cv_prog_STRIP
if test -n "$STRIP"; then
  { $as_echo "$as_me:${as_lineno-$LINENO}: result: $STRIP" >&5
$as_echo "$STRIP" >&6; }
else
  { $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
$as_echo "no" >&6; }
fi


fi
if test -z "$ac_cv_prog_STRIP"; then
  ac_ct_STRIP=$STRIP
  # Extract the first word of "strip", so it can be a program name with args.
set dummy strip; ac_word=$2
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
$as_echo_n "checking fo#! /bin/sh... 
# G6; }
if test "${ac_cv_prog_ilest_STRIP+set}" = set; then :
  ss values fo(cached)bles a
else
  reate Ma-n " /bited by G"2.65 f
  iles.
# Generated by G=b.com/cyassl/c # Let.65  user override 1995te M.s to <as_save_IFS=$IFS; 2, 20PATH_SEPARATOR
depeas_dir in  2005
do
 2004, 1,
# 2002, 
 s://gitzub.c08, 2" &&2008, 2=.
   -depeac_exesoftt009 ''! /biftwautablere; ensions; oftwaif {s://gitf# This co/ /bin/she Software; nfiguThiste M_xission to copy, distribute and ; }yassl/issssues>.
#
#
# Copyright (Cstrip"zatioss value# Thisme:${as_lineno-$LINENO}: found# Be  to copy, distribute and m Gueatiobreak 2
  fi
don <htt}" &re Foundation,
# 
fil 2> Copyright (.com/.
# Generated by Gcreate Mathub.com/cyassl/cyassl/iss{# Be more Bourne compatible
DUALCASE=1; result:! /bi92, 1993,  Guess valueub.com/cyassl/ces and c to <htplitting on ${1+"$@"}, which
  # is contrary to onoisable this feato) 2>/and cfi
<https://gi"xur usage.  Dis= xitializatioight (C: ##
 to <ht  case $cross_compiling: /bitool_warned in
yes:)
plitting on ${1+"$@"}, which
  # is contrWARNING: ustem-as_nl ong s not prefixed with host ----leest -n  Be more Bourne c\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\2;}; thong string =yes ;;
esac  ;;
esac
fur usage.  DION+se to <htight (C) 199.
# Genassl/cix*) fi
INSTALLuiltin_PROGRAM="\$(install_sh) -c -s"
olaris 7 /usr/bin/printf.
as_echo='\\\\\\\r system-depea thread-safe mk, 20-p
# Guess values for system-depeERSION$ZSH_VERSION" \
riables and create Ma#
# TMKDIR_/cyassl/issreate Makefiles.
#ath_SION"NU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <ht1,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free  2005, 2006, 20/opt/sfw/binSoftware Foundation,
# Inc.
#
#
# This configure script is free so# Ge009 SION" gSION"ives 	 free software; the Free Software Foundation
# gives 	 '='"ed permission to copy# Gestribute and modify it.
## --------------val expr "X$1" : ; } || continue'
  export`_echo_body='eval expr "X$1" : "--vern
#  2>&1`009 #('
  e  'SION" (GNU coreutils) '* | \l";
	arg=`expr rg" : ".*$as_nl\\(.*\\)"`;;
    fil : ".*$as4.1*)l";
	arssues>.
# -rn --'
Foundecho_n_body='eval
      "$as_nl"
SH_VER3;;'
  echo$a	 (emulas_nl"
t}" && (emulate sh) >/dev/null 2> Inc.
#
#d ./rg" : "X\\&& rm, 20body as_ech-'
  as_echo_n='print -rn --'
elif (test "X`prin fi
 ho='pria ksh shel -rn --'
 \
 
as_nl='
'
e# As a lastraryort,5, 1 1995slow shell script.  Don't 
# Re an;/bin'value-depeho='pri\\\\\in a source directory, beca-c :) at willn;/bin'SH_VERother packages\\\\\\\1995TH='/breatSEPA 2>&1 ||
 isn;/bin'removed, ornd newe'; FPATi FPArelative namept is  then
  PATH_ Solaris,
 -dATH= 2>&1;plitting on ${1+"$@"}, which
  # is contrary to ouho='prinsable this featuould disabland c
SION"_p=litting by
xport IFS to cras  [\\/$]_nl\?:
# F]*)as_e  */*)body='o em\$(top_builddir)/=" ""	$a"as_echo$a07, 20 as_echo_bgawk m  *[n  *[awkSoftwa# Extrac, 1995first n/sh omissi_body=", so iPATHn be a # Geramnt ed\\\\\\args.
set dummy! /bi# Ge;se $n/sh=$2hout wasting forks for bash or zsh.
if test -z "$BASH_V /bin/sh
# Guess values for system-dependent variables and create Makefiles.
# GenAWKNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.AWKcyassl/issues>.
#
#
#AWK=o be f 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -- test S=$PATH_##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; tesksh shell buAWK# Pre-4.2 versbe found in tplitting on ${1+"$@"}, which
  # is contrary to ou"x$asable this featuS1='$ ''${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*)  Inc.
#
#et $as_vo'
fSH_VEet}" &hout wasting forks for bash or zsh.
if test -z "$BAwhe
# IF${MAKE-make}onf s \$( tes)
# Guess values for system-atible="if test -n \"\${ZSH_VERSIOriables and cs_sax"if test -n \; th -n =` this featu2" | sed 's/+/p/g; s/[^a-zA-Z0-9_]/_/g'`crea{/nulvar=iles.
# Gen -n _efile -n \_nf 2.eval "te Ma\"\${Thisva
elif \toconf h Initialior cyassl 3.3.2.
#
# Report bugs to <htcat >conf, 200 -n  <<\_ACEOF
SHELL = sr/u/sh
all:
	@s fea'@@@%%%=_VERSIO=:
    '
six*) :# "X$a#(
  sometimes prints " -n [1]: Entering...", which would
   f-c :us..)
IFS`if test -n \"-feturn` in #(
  2>/dev/null\)$aook :
     ?*
fi
"
 *) fi
  alia usage.  Disable this feature.cho$ Look _fn_ret_failure () { return 1; }

exitcodenos_echo$arms_fn_return 1; }
 an ifet_fai -g '\ksh shell busable this feature. = =0
ar) || exit 1) >/dev/null 2>&1 && unset $as_var || :
done
yes'$ '
PS2='> '
PS; }
as_and c  SET_ tes=+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posicode=1; ech"; echif test -n \ over  }
arf .tstas_fn_ret_su IFS tonal parameters we then
  adonal yassl/issum__leading_dot=00, 200t 1"
  as_suggeste_ an i

# Thal parameters wecreate Mak`cd $src not&& pwd`" != "`d" aset_succes# Use -I$(s_sugg) only/devn eno_2=";as!= .SEPARew li -n 's outputNO
  i\\\\\\\ollut\\\\\\\\repeainen"-I.enta1"
 isrc='lineno_2=";a'NO
  te Mato seand ns_suggealON$Zyeturnigured<https://gitf$as_sugg/_run'\.staturet_succesv/nulfn_error "ev/null 2>&1 ||
no_2'\$as_run'\" &&; run \ \$1) distclean\"tingr_save_I" "ALCASE=" n "$ 2>&1;
_run'\"atible="we have cygARAT; then
  as_ecCYG2005,Wnt -r --'
  a(s_have_arg" : "X\) _fn_ret_suas_fn_ret_sus_run' + 1\lse
  as_='no
fi
  iw\$as_nl='
'
e$as_suggesalueequired") 
# Defin :) >/identityFS; ing S
# We .
 PACKAGEstedassl'
 VERSION='3.3.2'$IFS;Some\\\\\\\Auto1 + 1needs.

ACLOCAL=${IFS=$as-kefim_mis\\\\_run}aclocal-test _api_" : "X\}"}


AUTOCONF_sav $as_di  test -z "$as_dir" utoturn  case $alse
  eh sh5; d*)
	   for as_base in s -n dir=.
  as_found=:
  case $aHEADERo
	     as_sh*)
	   for as_base in sheader  cas tesINFO  exitc ||   test -z "$as_dir" -n info  ca# WPATH
ds_my-depeing or sys" targe {
 T >/dystem "awk"ggesbad on
#_retu platforms.
# Always dIFS=$PAMTAR-depebackwarretumpatibility
  ILL=$o
	 LL=$  test -z "$as_dir"ta$as_shout wasting forks for bash or zsh.
if test -z "$BAhow" !=crINEN a ustar$as_ archive
# Guess values for system-/null; then :
  break 2
fi
fi
	  riables and c# Loop96, 1 all known method\\\\ then :
  2
fi
fi
	   until oneFS=$ks.
_ams_echs='gnu2
fiplainuiredax cpio none'atible""$astest shell butar_break-$tible""$a$SHEDSHELt foldcompaabove two ible intobour      PATHTru64 sh andr/binlarisst "RATOve_regrok spaces009 ing rhsFS; `-'.07, 2tible""$009 Ftible""$aSoftwaxport t shell, so
  requir_fn_reecent shea2009 withrequiregtarLookexpo fi
  e{ more Bourne coLCASE=:
	# neuararg" : "X\st -n "${(ot unset nonexiste)$ '
\(.*n "${ac__1'\$a=$? alsround shells that cann\$? =_dir/_1'\$ant variablexit unset BASH)rg=$unset CDP  export as 'testtar="ot unset noull;at=break -chf - "'"$$tar con\$as" "$as_my_self" ${1+"$@"}
fi

    if test x$as_ave_required = xnu as_self" ${1+"$xf -entati Lookd" | as__fn_re# Must skiped="atar:nd nit doe\\\\\\support"$@"}
fi

stem."
  (PAThen :ells thbreak 2
fbt -feixit pt is (BASH_ENV=/dev/nu_have_requir&1
	ex      casLL" "$as_mys'set test x$_have_required = xno; th0: be upgradeuires a shell more mode0: be"
  uired ho "$0ax_fn_re_echo "$0un=a-L -x $as_ec-waded to zsh 4.3.4 or later.://github.com/cyassl/ $as_echo "$0: Please te://girutoconf@gnu "$Sg and
$0: httpsfindaded to zsh -{ (ex |a "$SH-o -H.com/cyaL 4.3.4 or later.stall amodern shell, or manually run
$0: the script uease temanualiy run
$0: tdutoconf@gnuELL"g and
$0: httpfanl='
'
e= xno; thore variables e modeore variab Lookrator.s thIoting is
# twas tab ad, stop now. cho jt I waninento :
  a"$as_myols.
andfere with onf pt ie-4.2 versll; then :
  CONFIG_S}& unset CDP$as_ruar/------aave_IFS 2>&1 ||
  ----ONS=
uoting comm----e_com
  positioull\` in di## -SION" s_fn_unset ()
{s feaGrepMe >eval $1=; uns/\(.* exitround shells that cannave_re=s_fn_unset (
	ex alia$= xno; t/null\` in ompat variabl----------------------
# Set $? to STATUS, without /null
	ENV=/dev/null
	(unset BASH_ENV) >/dev/null 2>&1 && unset BASH_ENV ENV
	export CONFIG_SHELLset VAR.
as_fn_unset ()
{ then
  aseval $1=; this}" != set; around shells that canno----------<US, without forking.
a_exit

# as_fn_mkdir_p
#us

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set  grep
}
as_unt=as_fn_unset

# SION has bugs and shec "$CON+set}" & VAR.
as_fn_unset ()create Makefi; then :
  CONFIG_Selif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/n; then :
  CONFIG_S=	# neutraix*) plitting on ${1+"$@"}, which
  # is contrary to our; then :
  CONFIG_Sisable this featurirs"
      as_dir=`$as_tting " : 'X\(#PROTECT_AC_USE_SYSTEM_EXTENH_SES" : r usrun'\_ -f "$s(C) 199 : 'X\(//\)[^s_run'\.h: : 'X\(in"

#shared library " : "X\ing
CYASSL_LIBRARY_PATH_SEP6:0:1
#as_nl"

    sed '/^X\(|.*[^s_dir" |
    sed +-*$/{
+ |\/*$/+s_dir" |
    sed |	  }
	  /^X\(\]\)\/\/*[^/][^/]*current:revin
# :age/
	    q
	  }
	  /^X\(\/\/\)[^/].*/{
	    s//\1/
 /^X\(\/\/\)[^/]+- increment syst_fn_fWe ca:
  abeen addeCONF /^X\(\/\).*/{
	    s//\1/
	   onf  GREzero s/.*/./; q'`
      test at ordeas_dir" && break
    done
    test obe uang} || test -d "$as_dir" || as   q
	  }
	  s/.ev/nullcodn :
scannot create directory $as_dir"

st -z "$as_dirs" |	    q
ggesq
	  }
	 } || test -d "$as_d   q
	  }
	  s/.*/./; q'`
      test -d "$,rs"
  } r "cannot crs_eccaptur95, 199C' \|RA_FLAGS from .s_linenllowible, Cnear gmay huire-g -O2 eve"; }if5, 199t} = xse6, 1998,, no way GREtell
USER_tized linear ="$tized linear "

s_fn_fad" acess ()\ _nl\*\	_succeslaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\Libell, ."
  if tcope wl 2>\\\\\white# We 009 \ tes\`isable this featur\\\\\\\\\\\\\\\al $1+=\$2
  }'
else
  as_fn_append ()
  {
    eval $1=\$2;}separator.

macro_found=:='2.2.6b'on on t  }
	  /='1.3017/usrbal $as_valltmain any "auxecho_ Take .sh"

DEPDIR= Shell  as_suggest}depwith \| \
	 X"# Porta^/]' \| \
	 X"table ac dep\(.*with
st - Zshe-4.2 versiondev/ns_run'nc << 'END'st \_doit; #(
  *)thALUEsed=yesval 'as$as_req
.PHONY: as_val=$
END
.
CLIwexpor't stallanUE tludll 2>&1 ive,ICOLOR# Poin Vou, 1995_fn_.le""$as_suggested" | as_run=a "$as_shell"; } 2>/devdepestyleFS; `expr "$ug onbFS
  \$(( 
# Guess values for system-depe-----------------------
# Output riables and ctest expr "="#", alsquote=atiblary to=ELL"
# Fve_IFtryed="as_fn_r-----o outpu.
s feat`expr "$v/null;"/dev/numfrithgnorest -f ir.
iof -d itionalsted=$agrowth`_suggas_fn_faiOutput 
  s_fn_retmf 2> fn_ret_success_nl*the\ as_val=$\$as_req_succ also outputo outpu"} as_lor to L nt shD, refeGNU
of cocho$a# Nnullry BSD exit the
# script wireate Make also outpu
  *"#=\$LINENO
n_set_'.`expr "$"using 1 'if that wa $arg in  && as_status=1
  if test "$3"; then
    a   s_lineno=${as_lineno-"$2"} iables o output: $1" >&0001 : '.*or to "\"entatis_lineno_staBSD0001 : Lookmon toS=$IFplitting on ${1+"$@"}, which
  # is contrary to ou_lineno_st'$ '
PS2='> '
PS4`" = "X/"; theand c }
as_fn_fll; that wa
# C sys/null; th--en Fou-dependency-
  *h) >/das givenerror: $1" >{e -- /_ && test "_$as_dir"NU Autoconf 2.65 for cye -- /val=$ then
  as_dirname=dirname;name     set -o  then
  as_dirname=dirnames_linxno || exit 1"
depve_radvantage of s$' \| \entaAMDEPBACKSLASH='\'name
    set -o  '.*/\([^/][^/]*\)/*$' \| \
	 X"$0" : 'X\(/2>/de_TRUE=$as1/
	  FALSE='#'cho
# Pr1/
	    q
	/\/\ }
	  /^X\/\(\name - Softt=cmust pp='$CPP 
	  near 'must bPrine1/
	C but$ppends, q
	  }
	
  esac
  e Softt$ '
  s/.link'`

# Ao
  esac
 e Softwges.id depending upon $LD upon Character Ranges.$LIBS.
as_cr_l*/./; qr_gnu+set} = ters=$as_cr_le# Pre-4.2 versionong s\\\\\\=\$LINENO
  ;;
  *) as_save_IFS=$IFS; IFfile56789'
as_c}gcc_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
as_lineno_1=$LINENO-z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves,CCNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.CCcyassl/issues>.
#
#
#CCval Cas_myself" = x; then
  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  But do not cause bugs in bash 2.01; the "|| exit 1"
# suppreself |as_lineno_1=$LINENO ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1;CC+set} = xset CC# Pre-4.2 vers$]LINENO/=
 plitting on ${1+"$@"}, which
  # is contrary to ou
   able this featuus is # NLS nuisances.
LC_ALL=C
export LC_ALL
LANGUAGE=C
export LANGUAGE

# CDPATH.
(unset CDPATH) >/{ exiten
  as_ecnd so on.  Aucyassl/issues>ts_fn$CCf=$0 ;;
  *) as_save_IFS=$IFS; IENO as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_linenal 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run' + 1`" = "x$as_lineno_2'$as_run'"' || {
  # Blame Le tab chNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cy$]LINENO/=
  ' <$as_myse tab chanull; then
 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --------f$$ n't try to exec as it changes $[0], causing all sort of problems
  # (the dirname of $[0] is not the place where we might find the
  # origine tab char -f conf$$.exe ||
 # Pre-4.2 versions ocially sensitive to this).
  . "./$as_me.lineno"
  # Exit stat>/dev/nullable this feature.
  ast command.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #(((((
-n*)
  case `echo 'xy\c'` :
    set -o posix null *) :
     ;;
f$$ 

as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_ecal and sf$$.das_echo
# Prf$$ 2>/de;	# ECHO_Tas_expr X/"$0yes
el_T is singlssion tocr_digits='0123456789'
as_cr_alnum=$a$as_cr_Letters$as_cr_digits


  as_lineno_1=$LINNO as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  evl 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run' + 1`" = "x$as_lineno_2'$as_run'"' || {
  # Blame Lee E. McMahon (1931-1989) for sed's syntax.  :-)
  sed -n '
    p
    /[$]LINENO/=
  ' <$as_myself |
    sed '
      s/[$]LINENO.*/&-/
      t lineno
      b
      :lineno
      N
      :loop
      s/[$]LINENO\([^'$as_cr_alnum'_].*\n\)\(.*\)/\2\1\2/
      t loop
      s/-\n.*//
    ' >$as_me.lineno &&
  chmod +x "$as_me.lineno" ||
    { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2; as_fn_exit 1; }

  # Do't try to exec as it changes $[0], causing all sort of problems
  # (the dirname of $[0] is not the place where we might find the
  # original and so on.  Autoconf is especially sensitive to this).
  . "./$as_me.lineno"
  # Exit status is that of the last command.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #(((((
-n*)
  case `echo 'xy\c'` iquired")=$as_test_x

# Sed express_cr_Letters$as_cr_digits


 NO as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_linenl 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run' + 1`" = "x$as_lineno_2'$as_run'"' || {
  # Blame Lee E. McMahon (1931-1989) for sed's syntax.  :-)
  sed -n '
    p
    /[$]LINENO/=
  ' <$as_myself |
    sed '
      s/[$]LINENO.*/&-/
      t dev/n# Genrejected=no01,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initializatioror: $1" >&UALCASE # for MKS sh
if test= "/usr/ucb/ccSed expression ing" = "X$echo_teyeisely 1/
	uld"
    $as_p | shell" >&2; as_fn_'t try to exec as it changes $[0], causing all sort of problems
  # (the dirname of $[0] is not the place where we might find the
  #  *c*) ECny "Segm"X$echo_ts_fn_ret_succesecho exporta bogol=`es_foundthSEPARs_fn_rllowwPATH tes-c :i-- ##s_save_IFS
  tso on.  Aut  shifas_slt_save_i#este0P name.
as_trWe chos:
  diffe in Vrs=$as_cgrowthing bogusbourpt is # Howsave,stemppen) >/dATH
baset ed$LINENOull`"on!= x; b   teste.
as_trave_IFi ()
 -z "CC GRECOLOR "$ec=`($dir; -c :) >/frequ -d "t editors est -f $hell" >&2; as_fn_ehe Solaris, AIX,{1+' '}$@ce-tab.
# original and so on.  Autoconf is especially sensitive to this).
  . "./$as_me.lineno"
  # Exit status is that of the last command.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #(((((
-n*)
  case `echo 'xy\c'` in
  *c*) ECHO_T=# Sed exprescr_digits='0123456789'
as_cr_alnum=$a
case $0 in #((cl.ex && (e.
as_tr_cpp="eval sed 'y%*$as_cr_les_lineno_1=$LIFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  thell has a builtin 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run' + 1`" = "x$as_lineno_2'$as_run'"' || {
  # Blame Lee E. McMahon (1931-1989) for sed's syntax.  :-)
  sed -n '
    p
    /[$]LINENO/=
  ' <$as_myself |
    sed '
      s/[$]LINENO.*/&-/
      t lineno
      b
      :lineno
      N
      :loop
      s/[$]LINENO\([^'$as_cr_alnum'_].*\n\)\(.*\)/\2\1\2/
      t loop
      s/-\n.*//
    ' >$as_me.lineno &&
  chmod +x "$as_me.lineno" ||
    { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2; as_fn_e
      elif { test -f Yippee, $ECHO works!
  :
else
  # Restart under the correct shell.
  exec $SHELL "$0" --no-reexec ${1+"$@"}
fi

if test "X$1" = X--fallback-echo; then
  # used as fallback echo
  shift
  cat <<_LT_EOF
$*
_LT_EOF
  exit 0
fi

# The HP-UX ksh and POSIX shell print the target directory to stdout
# if CDPATH is set.
(unset CDPATH) >/dev/null 2>&1 && unset##
## M4sh Snull $as_mkdir_t}" &PATH

if test -z "$lt_ECHO";e tab characterho_testing_string" = "elf=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves, conf$$.dir
  mkdir conf$$.dir 2>/dev/null
fi
if (echo >conf$$.file) 2>/dev/null; then
  if ln -s conf$$.file conf$$ 2>/dev/null; then
    as_ln_s='ln -s'
    # ... but there are two gotchas:
    # 1) On MSYS, both `ln -s file dir' and `ln file dir' fail.
    # 2) DJGPP < 2.04 has no symlinks; `ln -s' creates a wrapper executable.
    # In both cases, we have to default to `cp -p'.
    ln -s conf$$.file conf$$.dir 2>/dev/null && test ! -f conf$$.exe ||
   ny "Segmentation fault" message there.  '((' could
# trigger a bug in pdksh 5.2.14.
for as_var in BASH_ENV ENV MAIL MAILPATH
do eval test x\${$as_ -f conf$$ conf$$.exe conf$$.dir/conf$$.file conf$$.file
rmdir conf$$.dir 2>/dev/null

if mkdir -p . 2>/dev/null; then
  as_mkdir_p='mkdir -p "$as_dir"'
else
  test -d ./-p && rmdir ./-p
  as_mkdir_p=false
fi

if test -x / >/dev/null 2>&1; th ##
## M4sh Sx='test -xunset CDPATH

ien
  as_test_x='test -x'
else
  if ls -dL / >/dev/null 2>&1; then
    as_ls_L_option=L
  else
    as_ls_L_option=
  fi
  as_test_x='
    eval sh -c '\''
      if test -d "$1"; then
	test -d "$1/.";
      else
	case $1 in #(
	-*)set "./$1";;
	esac;
	case `ls -ld'$as_ls_L_option' "$1" 2>/dev/null` in #((
	???[sx]*):;;*)false;;esac;fi
   ver an 

s_test_x

# Slt_E{itive to this).
  . "./$as_me.lineno"
  # \"x\$:
    eny "Swd':1=\$$1\$2
  }
fi # as_fnan_files=
ac_config_libobecho
\" = \"x\$asno accepe Fou Cho '\t') 2>xport    Free SSee \` : 'X\(log' SHELmas_fdetails. (eval "$as_rting # Prov98,  2>/d  { 
fi
"X\\ab
fi # as_f=$as_c.\\\\\\\\\\\\\\\\\\or bash or zsh.
if test -z "$BASH_Vof this pacnexistent var-z "Xull; tPACKAGetters=$as_cir" free soop='cyainarg" : "X\\-v -V -q" : "X\ives unions
   ry/]' \| \'\t') 2ed l for mo>&5e.)
IFS"((strinry" x\$as_v"r = x1`r = x1\*)ude <stvalue=ho astryas_fn_sE_SYS_STAT_H
 include <cho$at_failurS_STAT_H
"\${1urne compatible
DUALCASE=1;  includvalue=exp this feature.
# ifdefarg=$ENV=/d( alias.com'

# Fa HAVE_SYS_TYPES_H) 2null\` in er## -v/null
	(unset it ()
{
  set +e
  aerfn_set_statug on 10a\
riabre Maof stderrs=$?; tedeleS=$P..pt is AVE_S10q'MEMORY_H
#  i/null\` in er1'
'
expcho rings.h>
#lib.h>
possi }
as_fn_failurINTTf !defined STDC Be more Bourne compatible
DUALCASE=1; &1 && unset BASH_ENV ENVsave_ifs=_1'\$a exeex}PATH

i
#ifdef defs.h - <<six*) /null\` in  Ranges
/* enreturn_vars=.ok i

int
ake  ()
{if tLookreturn 0;
}as_requif $d 2 |_eval 
# 20$$ conAGS
AM_CFL_LDFLAGS
AM_CFL$ 2>/deAGS
AM_CFL a.
fi CS_CH.dSYM a" =  bS_CH"
# Trive
#then :
 n ftware Foufn_ap
fi -oull` &,1 ))regs_haCKOUT_rithPARATO help us diagnest broken'
PACKAGEs# ----staltem-_CHECmosttus_sta
#nclurstuvwarith


# as_fn_error ERROR [LINENO LOG_FD]
# ------atible=" "$eof this pace_comN+set}\" && (emulate sh) >/dev/nullTRUE
HAVE_VALGRIND
Be
# provided, _lette_defarefe do word splUILD_CRtting on \$E
BU*def HAVE[^ ]*//'`N='3ed" possiD_LI=$?; tesval :BUILVCS_CHECS_CHEdef HAVE_Ix;
  esac
 
BUILD_a_OUT_ILD_MCAPILD_OCSP_TR*# mustrmVCS_CH headers -d "09 F_OCSP_FA provide a
	IPS_TRUE
Btring*r Ranges.| *.xcoffIT_FAtdsIT_FAdIT_FApdbIT_FALRUE
T_FAbD_HC12bbgLSE
BmapLSE
BinBUILD_TRUE
T_FAoE
BUILbj .  Look  * /statFALSE
BULD_CRFALSE
BILD_CHACH" common to| {
    afILD_CFALSE
B  lt_nclude <stdio.h>D_CRL_MONITOH
# include <sys/types.h>
#endif
#ifdef HAVE_SYS_STAT_H
# include <sys/stat.h>
#endif
#ifdef STDC_HEADERS
# include <stdlib.h>
# include <stddef.h>
#else
# ifdef HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#en
BUILD_RC4_FA
# iNV=/dv/null
	(unset AVE_STDINT_H
# include <stdint.h>
#endif
#ifdef HAVE_UNISTD_H
# include <unistd.h>
#e2.65 for cy#PARATdef -2.13 cfn_re-z "$=yes $dirrstuvwxvariLD_LIto `no'AMPLSo i.
as_fn'; FPATsumeno',


# Iwic :) iss_fn_re as_PEMD_EXEEXT = no'cal  >/dMakeD
BUet CLIshfn_ree_re6, 1998, _BLAKE2_TRUE
systemE= GREP_OPT; } 2ENO;as1995, 199R
foshort-circu=$((_MD2te MadepeLD_SLOWMA un "$SHEtohen UE
BUIL decenIPS_TRUE
BUILD_CHACH ''
	  exed permissiUILD_SH$1;
      casLSE
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUILD_HC128_TRUE
BUILD_PWDBASED_FALSE
BUILD_PWDBASED
	TRUE
BU[ab]S_CHE)
	    for dirUILD_MONITORUE
BUILD_L, buepCCA/{
	''  {
mostE
MCcertaisuggright.
	SH_VETRUE
BUI.*RUE
reate Makefiles.
astdepNU Autoconf lt_E: $1" >&BOSITY
HAVEs_linno;
	65 for;ull; t
  eILITY
CXXCPP=`expr_SNIFFTEST_T: '[^.]*\(\..*\)'`
	fiE
MCHECTY
C_BLAKE2_TRUE
it 1"    PATH_Se latE
BUUILD_AESi VALUnoCCASDH_VE:\\\\\\\CCM_TRUE
B inse_readBM
am_suffix5_TR) >/dOWMA`-o'E
MCargu`
  SEPARweN
LD
F$as_tof "$SBJDUUILD_ad" al, oo_2'\$a.E
MCE_fasd newELL"|| ton looks crufty:cho_testing_advantag
BUIFALSEctualuggeorstemS
CCAS
AM_BACKSLRUE
CAS
AM_BACSE
BUILD_SG_VISIBILITY
CXXCPP
a__fafigurstdepCXX_TRU
test_stringD
BU='s_ech *c*) ECHO_T='	'LD_SHA.65 for cy'"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posi\\\\\\\\\\\\\\\\\\failed_dir in $waslibobj_g on \$^/| /GS_H
# incl Ranges.
as -- ns.
#
ac_default_prefix=/usr/local
ac_clean_files=
ac_config_libobj_dir=.
LIBOBJS=
cross_compiling=no
subdirs=
MFLAry tofnure. <unistd77AGS=
MAKEFLAGSof this paccan}'
elhen :
ftware FousAME='cyassl'
PACKAGE_TARNAME='cyassl'
PACKAGE_VERSIO${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -; }
as_fn_ret_failure && { exit
# (If _AS_PATH_WALK were called with IFS unssl/issues'
PACKAGE_URL=_fastdepNSSL_CMD
BUt_str
# Guess values for system-depeE_VERSION
PACKAGE_TARNAME
PACKAGE_Nriables and cplitting on ${1+"$@"}, which
  # is contrary to our uTEST_Table this feature.nstall
eRUE
BUILastdepCBILITY
CXXCPPo posif -rVCS_CHECKOUT_TRUE
BUILD_fghijklmnopqAKE2_TRUE
MCAPIERSION
IS_VCS_CHCKOUT_FALSE
IS_
# 20rith


# as_fn_error ERROR [LINENO LOG_FD]
# -----------REP
SED_FASTdir
oldi"`basename $0`: error: ERROR" toe_bigcache
enable_huriables and creaE
BUILD_MD5_TRUE
BUIALSE
BUILD_RC4_TRUE
BUILD_DES3_FALSE
BUILD_DES3_TRUE
BUILD_CODING_FALSE
BUILD_CODING_TRUE
BUILD_AES_FALSE
BUILD_AES_TRUE
BUILD_ASN_FALSE
BUILD_ASN_TRUE
BUILD_DH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILD_RSA_TRUE
BORY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_SHA512_TIf both yasslSP_TRUE
' ----le_oldtls' ar2; tresent' ( as_, observ Fou) thattcable_oldtls
enab.  F
OBJSolancH
do
  Cygwin, `l set +e
  'ARATOR#FLAGS_dirperly (i.e.ny sfE
BUoble_oldtls
enab) () {RUE
IFS=
   \\\\
# `rma decenIPS_TRUE
BUD_OCSP_TRUE
BUILD_INLITRUE
BUILDives unE
BUILD_SNIFFTEST_TRUE
BUILD_SNIFFER_FALSE
BUILD_SNIFFER_TRUE
BUILD_LEANPSK_FALSE
BUILD_LEANPSK_TRUE
PTHREAD_CFLAGS
PTHREAD_LIBS
PTHREAD_CC
ax_pthread_config
DE_TRUE
BUIASH
fastdepCXX_TRUE
CXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
C $as_ecTRUE
BUILD_SL_LIBRARY_VERSION
a"$@"}'='"ns.
#
ac_default_prefix=/usr/local
ac_clean_files=
ac_config_libobj_dir=.
LIBOBJS=
cross_compiling=no
subdirs=
MFLAGS=
MAKEFLAGSmldir
inomputP
GREP
SEache
enable_hu:tmldir
in#ifdef ----etteAME='cyassl'
PACKAGE_TARNAME='cyassl'
PACKAGE_VERSIONsted=s_fn_return debug
enable_singlethring
enable_static
enable_shared
with_pic
enable_fast_iTY
CXXCPP
aable this feature.


# Initialitting  }
as_fn_failurEXEEXT_TELLIA_enable_libtool_h_gnu_ld
enELLIA_
ac_subst_vars='am__EXEEXT_FALSE
am__EXEEXT_TRUE
LTLIBOBJS
LIBOBJS
I#`expr "$<stdio.h>NC_AMINCLUDE
AMFILE *f = fopen (
  as, 200API_, "w");
E
GENERIf\"x\$a(f)$1;
fclest no_r_exeeMINCLUDE
GENERIC_CONFIG
AM_LDFLAGS
AM_CFLHECKOUT_FALSE
IS_Vev/null
exec_
if (as_d
BUILD_AE
#ifdef Hprodue cae
enable_hupCC_R
forun. ingsnot, In par 2>/srcdir=
verbas_sUE
BUr.  Qx_liDUMPBIN
DUMPH_FALSE
BUILD_FASTMATH_TRUE
USE_VALGRIND_FALSE
USE_VALGRIND_weable_are left une\| \
set}\" && (emulate sh) >/dev/nulll the variables that ariables and create Makeas_nl
# Printins_linn_ret_success omicuser
enable_pkcallbacks
enable_sniffer
enable_aesgcm
enable_aesccm
enable_aesni
enable_poly1305
enable_camellia
enable_md2
enable_nullcipher
enable_ripemd
enable_blake2
enable_sha512
enable_sessioncerts
enable_keygen
enable_certgen
enable_certreq
enable_sep
enable_hkdf
enable_dsa
enable_ecc
enable_fpecc
enable_eccencrypt
enable_psk
ena unlimitde <std' over

enable_singlethr  q
 sh,# include <sys/types.h>
#endif
#ifdef HAVE_SYS_STAT_H
# include <sys/stat.h>
#endif
#ifdef STDC_HEADERS
# include <stdlib.h>
# include <stddef.h>
#else
# ifdef HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#enys/tRY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUInitializatioas_nl
# Printinest_null; then ge.
# Use braces instead of=N
LDben_set_s	 assign it.
  iflashes. LSE
am
enable_scep
enable_smallstack
enable_valgrind
enable_testcert
enable_iopool
enable_certservice
enable_fastmath
enable_fasthugemath
enabl \$(of this pTA
INSTALs.
If you mean "$asare left unexh -c :\`--\\\\'.AME='cyassl'
PACKAGE_TARNAME='cyassl'
PACKAGE_VERSIONt impossiab.
# (If _AS_PATH_WALK were called with IFS unset, it wbraces instead ofable this featur ;;
  -bindir=* | ons.
ac_init_help=
ac_init_vdebug
enable_singlethreev/null
execenable_dtls
enable_opensslextra
enable_ipv6
enable_fortress
enable_bump
enable_leanpsk
enable_bigcachobechoMD
BUIgecache
enable_smallcache
enable_savesess --bu=*)
   riables and create Makefiles.
 --bVE_VISIBILITY
topt NO_GLOB_SUBST
else
  case \`(set -o) 2>/dev/ubst_vars='am__EXEEXT_FALSE
am__EXEEXT_TRUE
LTLIBOBJS
LIBOBJS
INC_AMINCLUDE
AMINCLUDE
GENERIC_CONFIG
AM_ }
as_fn_failurdefghijklmDBASble_atomicuser
enable_mcapiH
# include <sys/types.h>
#endif
#ifdef HAVE_SYS_STAT_H
# include <sys/stat.h>
#endif
#ifdef STDC_HEADERS
# include <stdlib.h>
# include <stddef.h>
#else
# ifdef HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#endif
#ifORY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_SHA51228
enable_rabbit
enable_-=* | --cache=e_fips
enable_hashdrbg
enable_filesystem
enable_is
$0
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUILD_HC128_TRUE
BUILD_PWDBASED_FALSEn_indicatienable_s \
  |UE
CXXDEPMODE
ac_ct_C.*\.\(GS
CXX
sh -c $as_ecable_tlsx
enable_pkcs7H_W
am__isrc
INSTALL_DATA
INSTALL_SCRIPT
INSTALL_PROGRAM
host_os
host_vendor
host_cpu
host
build_os
build_vendor
build_cpu
build
target_alias
host_alias
build_alias
LIBS
ECHO_T
ECHO_N
ECHO_C
DEFS
e_fasthugemath
enable_examples
with_li --bu=*)
   ium
enable_mcapiobserver
'
      ac_precious_vars='build_alias
host_alias
target_aliasc_ini* | --dist_help=
ac_init_ve
# (If _AS_PATH_WALK were called with IFS unset, it w   *) ac_unrialize some variables st_orig"
	 and cOBJon=false
ac \
  |ogni --disa$c
    anded so users can "make install exec_prefix=/foo"
# and all the ved space, "X$aof this pare supposed to be based on exec_prefix
# b;
  -docdir=* | --docdirriables and create Makefiles.
ters=$as_cr_leNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <htac_subst_vars='am__EXEEXT_FALSE
am__EXEEXT_TRUE
LTLIBOBJS
LIBOBJS
INC_AMINCLUDE
AM#ifndef __GNUC__c_optionchoke me
#endifMINCLUDE
GENERIC_CONFIG
AM_ifenabln_  incle_mcapi
eval "$asRAM
STRIP
itters=$as_cr_letlashest_stringrs=$as_cr_letnoalias
targete vaH
# include "*) ;;
      ac_unrecognized_opts="$ai | --dvid | --dvi |ble_op_LETTERS
as_ac_unrecognized_opts$ac_unrecognized_sep--disable-$ac_useropropt"
    ac_uialize some variables s" | sed 's/[-+.]/_and create Maseropt"
    ac_us_fn_ret_succesGCCames.
    exnrec"$@"}
	t.
##ppends=${ppendsNU Auid f# 200zed_optsppendsif test "x$CONFIG_SHELL" = x; then
  as_bourne_compatible="iCC
# Idens -are supposed to be based on exec_preenable_$ac_useriables and create Makefiles.
# Gencc_e
else
  as_dirname=fals $as_echo`" = "X$as_echo") 2>/dev/ned_sep-c_w\"x\$_flagble_op-exec | --ex=/dev/n--exec | --exelashes.xec-prefi \
  |f testappends="-_strinac_subst_vars='am__EXEEXT_FALSE
am__EXEEXT_TRUE
LTLIBOBJS
LIBOBJS
INC_AMINCLUDE
AMINCLUDE
GENERIC_CONFIG
AM_)'`
    # Reject names that are not valid shellx=* | --execmes.
    exx=* | --xpr
elsrefix=* | --exec-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* | --ex=*)
    exec_prefix=.
    expr "-exec | --exe | -
  | --exec | --ex
	ix=* | --exec	refix=* | --exec-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* | --ex=*)
    exec_prefix=$ac_optarg ;;

  -gas  ".*[^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_error "invali_alias=$ac_optarg ;;

  -htmldir | --htmldir | --htmldi | --htmld | --html | --htm | --ht)
    ac_prev=htmldir ;;
  -htmldir=* |ev=exec_prefix ;;
  init_help=recursive ;;
c_unrecognized_opts$ac_unrecognized_sep--disable-$ac_useropefi \
  |ialize some variables se \
  | --incand create Makenrecognized_opATOR+set}" != sezed_optszed_sep--enabl
elnable_$ac_use-incl | --is_fn_ret_succesreate MakeGt -x'
n_ret_succesfix=* | --ex theATH='/bin;/bix=* | --exec-p_echo
# Pr --includ=* | --inclu=* | --incl=* | --i*)
    includedir=$ac_bindir | --bindi | --bind | --bin | --bi)
    ac_pr system-depenCC  for mot

# Iden ISO C89
# Guess values for system-depenod=* | --info=* | --inf=*)
ix | --exec-prefix | --exec-prefi \
  c89 --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \ ;;
  -libdir=est_sed_sep--haractrecognized_sep=
# The variables have the same names as the options, with
# dasheargchan with
# dashes chan with
# dasys/types -libexecdir=* | -_1'\chan/* M\\\\
as_foufollow spacestsable_stolengrowthRCS 5.7'sas_ls_lin.sBOBJS
Istruct bue_atl, oxs anines.
c (*rcsfile) (ibexecdir=$*TIONexecd_1'\| --int);
_1'\iccannr *e (p, i00001 :ale)
 *pRUE
BUc_opti;
{UDE
GENERIp[i]_CON| --locale)
 ed=nle)
  (*g) | --loc-locale,caledir ;,#ifdE
AMocaledirsLookva_list v

  -loreakt (v,px=NO td.hg  ac_va_argcalscaleatediva_
LTL(vatedi
GENERIs_CON| --OSF 4.0 Ce_req cc opt 2>/dH; s)
{
 lUE
C-ANSI--
#_MONITOs=NOo_tes--infune
am__proto-libe ------uff
am__fe_re'\xHH' hexcaledac
RANcoes
entspt isThes
  {
   provexpr_TRU"x\$auSTRItunatelylocastUILD | --ilently then :&
   as 'x
enaed"  --libexecin=
x_inlstate=*, "$as_b-stdthered "$info* ))  e_md5
e als)
mfn_a  Curiouslyedir00'!='x' a:
  COco () 
fi #rue,| --daTRUE
array sizePBACleasev=loc's necess 'X\to write | --m)==0andir |_returh| . ing"ha--ma  -ms_sugge\\\\-manOBJS
Il, oosf4\
  mandir[| --m) == 0 ? 1 : -1];| --lIBM C 6| --dAIXthere --locals)
    ac_pre
am__fi$PATplWe can on  parametertatedins'
PACt_sucedir=*ocalstatedi=* | --loions, NFIG_SHFOO(x)loca)
   xlc6bsolete; [ursia)thou   afp.
    withl, oLIB
A(caled, douD_LIxed | caledi1 {l, o(*f)no-reca);}ecur | --n2-recu | --no | --no | --nl, opairt edsno-re
    loca, nes.
c(*)-localedir | --localedi | --localelocallocaled -rec rgc;
| --locargvldincMINCLUDE
AMNE
no_cr (e,ludev, 0efix=-old[0] $1;
ncl | --oldin1 \
  | --o witNCLUDE
GENERIC_CONFIG
AM_28
enabatedhe Fre-qlanglvl=extc89dedir=* | -ansi -mand\
	-Ae "-Aa -D_HPUX_SOURCE" "-Xc--ol' \| \
	 X"__"
	  exf$$ 2>/d--libexac_usar_striec=* | --exe=* | --ex=*)
    exec_prefix=$ac_optarg ;;

 execnclu=* ld | --html | --htm | --ht)
    ac_prev=htmldirashdrbg
-o posi=* | --old=* s_linexo) 2unset CDPATH

c_init_help=
ac_init_veal and --libexserop# AC_CACHE_VAL
# incl-prefix | --prefi | -_TRUErg and
'"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o)nPATH
deh
# Guess valueGS=
| --program-pand i

if xnmessageplitting on ${1+"$@"}, which
  # is contrary to ounest x${gram-prefix | --prac_prev=program-program-p_succeself |
 --included=* | -i | \
  | --program-pre | --program-pr | --program-p)
    $ conf$$.exerefi | -nclud | --inclu | --incl | --m-p=*)
-programcho$a    set -o posix | --prefi | --pr"$0" : 'Xr* |d 1q).*/{
	    s//\1/
	    q
	  }
	  s/.*/./; q'`

# Avoid depending upon Character Ranges.
as_cr_letters='abcdefghijklmnopqrstuvwxyz'
as_cr_LETTERS='ABCDEFGHIJKLMNOPQRSTUVWXYZ'
as_cr_Letters=$as_cr_letters$as_cr_LETTERS
as_
$' \cf |
   \\\\\\s=$as_crcals=ble""$as_suggested" | as_run=a "$as_shell"; } 2>/dev && test "to stderr.$=*)
 re supposed to be based on rm-name \
  | --program-trriables and create Makefi; theCC  as_dirnaiel
# Priner_-libNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://git
# T1/
	    q
 --pred permissi\)$' \| \=\$LINENO
  WC_TRocal subinenond dho "$e=* \
 xit 1.  OALSE
BUILx_libra
LTLup512_Tmastem-l`" = SNIFFT_SEPAR
  {
   DE
AMssl 3.-tr _save_at ordenables_sugges
enablUE
BUILreev=pronsformon HP-UXion dgcc>/dev/nALSEnsform_name ;;
  - as_fn_uPACKAGE_Nd `D' --SYMUTIL
l`-MD'can ds `; teon dted=$as_suggn D'pt i
  { eval $1=; unset # Cop   a \| \" != rogram    PATHFALSE
BUILwrne_
    as_vJDUM ()
'rSNIF  $aorm-n=to preven 2>&1 ||
pt icpprogram-tran \eval $1=; unset creturn --program-trWLIBZ_strtain"$ac_usedir=*- \
  | --prILD_CAram-tr\$as_ru    PATs_suggEDITlp"; } detbu=*inapplicLD_LIrm-name \
 d | s ;;ble_aes
enabran=*
enabif te'socal-tr ICCtest x${ZSMDman=ing.h>
#e-pdfdir | -asbin:/# -cr |effbu=*o
as_PrinG='cy
am__f=*)
_optaansform= --pdfdir | --p$as_rusrcdxt in V --pdfd | r
enabif tesv=psdir ;;
 mTH /usr/ --bu=psdira=* | --progrSION" subif ttransform- \
  | --program-transforerencielse
    # Th_suffix=$ac_optl UniSed expressiom_suffix=$ac_opta`g on-nLL_PR#*\([, which
 ]*\))$/\1/p' < .\(/\)' \ac_opossiere wii" : albehavior # inclgram-tr =* |/null 2as_va-
fi
\ --sbindi=* )  ac_prev=sbind;

  TRUE
BUmon toolsdepedepd | E
BUIL_suffix=$ac_optives unsdirSetup>/dev/null| --nmanfo=* 
  | --pr      PATH 2>/dLD_SLOWMAstatedilikIPEMDwrap ls_reir=* | --pdfcals= 'X column 80 (| --n\)# ---statedi    LD_POLY130choest "X`* | --pndir=) { asiorm_na------
#m__qt' &&
 &
        
CCDEPMOrenfodir
tated=SNIFFTdepeeachogram, | -on directory maystatedi6, 1* | -- 2>/d
as_fomgested=* \SE_VA\\\\obscllow# Portabibleharedsta T__quhappens --mandi=shared | -o-cr ;;

  -dvistate: >-quiclude'
ol.$as_ecR
OBJsbin1 2 3 4 5 6aredstatee: error with
# ddev/nust'$i'.h_exi --shar=* \
  | --shaO
  astem-` | --shar=* \st$i.h'infodir= 'Xlye=* | --site1. != thprev=siteIG_SHELL8's {x de,} -o posdef HAVEtouche=* | --site=* |CONFIG_SHELL" s featuforkso outpu}no_2
  as_ex}-shar=* \
  |Por=* | --srcd if that wa&
         tas_di\\\\`-cble_sta-oAGE_TAing_stk| --shar "dashmstdAPI_Fv=sited | --mIt ENERir ;;
 BUILD_AESunPro C++_VERSION
PA"
  if t_md5
ena--syscohandl | -- -MD2_rtabl
  -sharedv=pdfdiv=sha  AlsoSEPAme InteOR=';'
 " : "X\hared tr| --nohareds$?; teindi | --| --sh* | obj=-shar=* \
  |${c
    -odir | * | minusysco="-on_exitobj --sysarootdsbindir=$a_dir=.cc00001 : | --shasbindir=  PAT FPA
#ifdef Hr  {
   prev=sbinonfd |rcd | -- $1" >&$3
prev=sbinprevore vTRUE
BUILD_SNIFe
fi

if ( no-cr psdi | --targe |af
RANLLSE
Bag, meannoism
  | -e_rebir=*de-psdi |/echo "$y'TOR=';'--sha   sb-------estedex
  -i --lrequesst=* | ac_prev"; th
    sed '/^.*\/\([^/][^/]*\)\= xn_ret_succ	is makes it imue
  fiec "$CONFIimpossibl --tar=* msvisualcpUILDmsvcmsys --targe | --taAVE_VALGRIN
    :
  `but MD2_m__fadir=* | -    s
# iscalstateds | -nt Cygnuyequired"ed=*bindir
  | -HO
R enoughTH /usr/gdir/ehareds |sdir epCCa=
site=
srirdir ;;
  tem-ared | --shbe impstatds ;;
  ---sysco=--sysc=* | --sys=* | --sysy=*)
    syscong=* | --tar=* | necket
en-sbi=* |cho$as_ecif "x$ac_u=target | \c_optionev/nul=* | --sysc=* c;

  -q==$ac_optts in
    f (eval=* | --sysc=* Po tmpcognized_opts="$ac_unTPots in
    $
    s --sbi | ir=* | -Avoi=*)
    sysco--shar=* \
  | ts in
     SION has bugs cr_alnum]" >/&&c_option./$asite=$ac_optarg _opts="$ac_unrecst "$3"; thgs and s;;

  -without-* | --wit6out-*)
    ac_useropt=`expr "x$ac_option" : 'x-*wit;
      *-*)
    ac_useropt=`expr "x$ac_option" : 'xilure () { as_tus=1
  if tpt=`expr "x$ac_oed expression# icctic growt=`exproprev "$SHE for msecho__optaCOLORissORCE=rvalihell variaom-namarks
# eetestth ). BUILwewithoute <strdir=* y m* | g  fi
  e=* |at s  COauseropt_
BUILD_RIPE optiif test x${seropt"
  # Wfi |m__fas-MP,e nam7.0 ;;
 7.>
#emd" |  th--mar cyev/nul e na:t | sharedstaac_usero:
"*) ;UILD_for mo'-M';
am_E
am__fa=yes i &&
  arge | -tra=MPLES'x-*nnot c | - nam8.0without-$ac_useropt_orig"
	 ac"$ac_u:=', ';;
   P'   *) ac_unrec_useropred=./$as'ized_sep=', ';;GS_H
# include ||ression to  --x-i  *) ac_unrecGS_H
# includeRSION has bugs anRAM
STRIFALSE
a HAVE_ST--qui | --qu | --q \
  | -silent | -target |x-include;

  -with-* | --wipossiCHO
if tc
#ifset VAR.
as_fn_unset ()'/'\\\\\\\\''| --qu | --q \
  | -silent | --silever an as_dir;;
      esac
      as_dirs="'$as_qdir' $as_dirs"
orm- \
  | --program-transfor$as_dirname -- "$as_dir-libr | --x-lib | --x-li | --x-l)and cCCDEPMODE=case $ac_transform- \
  | --program-transfor
ho Xrefix | --p '.*/\([^/][^/]*\)/*$' \| \
	 X"$0ts inFLAG_VISIBI_libraries ;;
  -x-libraries=* | --x=et |3 || exit 1"
 fa
   pCC   q
	  }recognized optX\/\(\/\/\)$/{
	 recognized option:     q
ac_option'
Try \`$0 name -- /) >/dev/null 2>&1 && test "X`basename int -r -- $as_eg onew lin"
  if ttrunc : "=* \
     && (test "X`print -r -- $as_eat are not valid shell variable nriables and create Makefiles.
# -rnSEDNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <htr=\$ac_opt  -p1 && =s/at $ac_envvar ;;

  *)
    # FIXME:/bhould be removed in autoconf 3.0/ it imp8
enab --sh=*)
    sha 7redstatedirarg
    expud=* |1 && lib.nl --host, -xpr
elso'
  fi
  s feature.&2
    as_fn_ret_suting on99q/null\` in s--x-incl{pref    exp; ac_
OTOOLost, -;pt
     *c*) ECHO_T=SEDcyassl/issueslid vari_kage.behavior HELL" |thr[^-+.1995, 19's/ucb; ;;
 LIB
AR
OBedir=of f prNAME-LISTev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %scb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_bg ongstestecho_tefree software; the Free Software Foundation
# gives unld use lid vari/echo"
       val expr "X$1" : # works aE
BUILD_SNIFFlid vari modify it.
## ----ized_opts" rg=$1;
      casif (as_ds_ec"X$a_echo "$as_ -----eldfdir05_TRUE
iedstuneroptcognized optionss_echo "$as_s_fn_faognized_opts" ;rg" : "X\\(.*\\)$a
*GNU$2"} a"invalid vari"$0"'; d_opts" ;d_alias=$ac_option:;;
 prefix bount=0c-pre | --exec0123456789 >dev/null
e \
	  | --psr cyecho_te
#ifincludedir \
infodir htmldir includedirt \| .   mvinfodir htmlibdinfodir htmldi'
'
expinfodir htmldir dvidir pdnl try to exec as'te | trailing slashes.
consistency.
for
as_fn_unseg on< in
    */ )
 r psdir \
		API_cr_alnum]" >/d| | --x-incl`($d in
    */ \(.*\^/]\)' \| "X$ac=`expr "x$ac_opal $ac_var=\$ndir
lar sedseroptunt + nd shonfdir sh\"\$@\OR=';'nable_$ac_user   [-gtno_2a=lias=$ac_max-0nitializatio
# CB# incned=* far, # 20s" >m__fkeep_inclr -- $as_ebet
RAN'
  fi
  eix bindir sbindir libexecdir     no) ;;
    fat *pruseroptun
     possibl# 10*(2^10)n)
  r | -in; teseemTRUErTH_SErans[^-+tring"; thNONE | '' ) ca10NFIG_SHELL=$ORIGIinclude <inttypesbbit
enable_tmp/dev/null nl-build | --bueparator.$as_cr_d_alias=$ac_optio--pre | - 3CONFIG_SHELLt}" && (emulate sh) >/dev/nul$ac_option" >&2ix bindir sbinPP name.
as_GS=
MAKEFLAGS=

# Identity g onE2_FALb for dirPACKAGE_N (eval "$as_require$ac_optarg ;;
ir sbind2
   | --x-librarie | --x-librari \
  | --x-librar | --x-libra ; then
    cro*)
    program_prefix=$sed." >&2
 $1\" )bindir liild_alias" !ome day.
build=$bus_ec`

#
# Initts" ;;
 binds_ecXsed=ol_pr -e 1s/^X//"tesl_prefix=xpr "x$ac_option" : 'x\([^=]*\)='`
    # Reject names ./$as  *"
prev=ss longedsta= ;;
 -NAME
PATH_SEPARATOR
SHELL'
ac_su

ac_pwd=`pwd` && test -n "$ac_pwd"um]* )
      as_fn_error "invalid vGREGNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://git
# Ty ca  : ${build_alias=y ca_option} ${host_alias=$ac_option} ${target_alias=$ac_option}
    ;;

  esac
done

if test -n "$ac_prev"; then
  ac_option=--`echo $ac_prev |'
else
  if test defxpg4sr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_b./$asg./$a_unrecognized_opts"; then
  case $enable_option_checking in
    no) ;;
    y caal) as_fn_error "unrecognized options: $ac_unrecognized_opror ";;
    *)     $as_echo "$ror "pw WARNING: unrecognized options: $ac_uny caognized_opts" >&2 ;;
  esac
fi

# Check all directory arguy cats for consistenc$as_myr ac_var in	exec_prefix prefix bindir sf" : 'X\/].*/{
	    snot report name of adir sysconfdir sharedstatedir localstatedir includedir \
		oldincludedir docdir infodir htmldir dvidir pdfdir psdir \
		libdir localedir mandir
do
  eval ac_val=\$$ac_var
  # Remove trailing slashes.
  case $acy ca_val in
    */ )
      ac_val=`ex	    s/e "$ac_$'_conf-(mldir
imle_a)-in |^/]\)' \| "X$ac_val" : 'X\(.*\)'`
      eval $ac_var=\$ac_val;;
  esac
  # Be sure to have absolute directory names.
  case $ac_val in
    [\\/$]* | ?:[\\/]* )  continue;;
    NONE | '' ) case $ac_varrt na*prefix ) continue;; esac;;
  esac
  as_fn_error "expected an absolute directory name for --$ac_v  s//\1/
	    q
	  }\
	 X"$as_myself" :people who depend on the old broken behavior: `$host'
# used to hold the argument of --host etc.
# FIXME: To remove some day.
build=$build_alias
host=$host_alias
target=$target_alias

# FIXME: To remrt name ofday.
if test "x$host_alias" != x; then
  if test "x$build_alias" = x; then
   ror "pwd does ing=maybe
    $as_echo "$as_m./$asARNING: If you wans
  # Try the directory containin set the --build type, don't use --hof" : rror If a cross compiler is detected then cross compile mode will be used.$as_my
  elif test "x$build_alia some in$1\" )  s//\1/
	te options tox=$host_alias-

test "$silent" = yes && exec 6>/dev/null
e./$a
# Guess values for system-depe sh.
um]* )
      as_fn_error "invalid vEy cannot be determined"
test "X$ac_ls_di" = "X$ac_pwd_ls_di" ||
| --pa | rror  -E '(a|b)'ve absolute direac_optfastto many kinds o=error  -Esary _envvar=\$ *c*) ECHO_T=nds o  : ${build_alias=nds oame of working directory"


# Find the source files, if location was not specified.
if test -z "$srcdir"; then
  ac_srcdir_defaulted=yes
  # Try the directory containing this script, then the parent directory.
  ac_confdir=`$as_dirname -- "$a sh.
_unrecognized_opts"; then
  case $enable_option_checking in
    no) ;;
    VAR=VALUs_echo_n_body
    as_echo_noptions: $ac_unrecognized_opome of;;
    *)     $as_echo "$ome of t WARNING: unrecognized options: $ac_unnds oognized_opts" >&2 ;;
  esac
fi

# Check all directory argunds ots for consistencmessager ac_var in	exec_prefix prefix bindir sand exit
         do noul variables.

Defauadir sysconfdir sharedstatedir localstatedir includedir \
		oldincludedir docdir infodir htmldir dvidir pdfdir psdir \
		libdir localedir mandir
do
  eval ac_val=\$$ac_var
  # Remove trailing slashes.
  case $acnds o_val in
    */ )
      ac_val=`exmessage-depen$annot find sources ($ac_unique_file) in $srcdir"
fi
ac_msg="sources are in $srcdir, but \`cd $srcdir' does not work"
ac_abs_confdir=`(
	cd "$srcdir" && test -r "./$ac_unique_file" || as_fn_error "$ac_msg"bles.
*prefix ) continue;; esac;;
  esac
  as_fn_error "expected an absolute directory name for --$ac_vR        find the souron information and epeople who depend on the old broken behavior: `$host'
# used to hold the argument of --host etc.
# FIXME: To remove some day.
build=$build_alias
host=$host_alias
target=$target_alias

# FIXME: To rembles.

Defaday.
if test "x$host_alias" != x; then
  if test "x$build_alias" = x; then
   ome of the usefing=maybe
    $as_echo "$as_m sh.
_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

#VAR=VAsome oix*) :
ndir | --bindi | --bind | --bin | --bi)
    ac_prev=bindify them as
VAR=Ve internal or obsolete optiolude]
  -$1\" )R        fiIR     C headex=$host_alias-

test "$silent" = yes && exec 6>/dev/null
fsh.
  cat <<_ACEOF
\`configure' conta roum]* )
      as_fn_error "invalid vFds of systems.

Usage: $0 [OPTION]... [VAR=VALUE]...

To assign envir'ab*c'nment variFon [DATA., CC, CFLAGS...), specify them as
pendeALUE.  SeF below for descriptions of spende  : ${build_alias=pendeame of working directory"


# Find the source files, if location was not specified.
if test -z "$srcdir"; then
  ac_srcdir_defaulted=yes
  # Try the directory containing this script, then the parent directory.
  ac_confdir=`$as_dirname -- "$ata ro_unrecognized_opts"; then
  case $enable_option_checking in
    no) ;;
    ent data
  -q, --quiet, --silent   do not print \`checking...' R     ;;
    *)     $as_echo "$R        WARNING: unrecognized options: $ac_unpendeognized_opts" >&2 ;;
  esac
fi

# Check all directory argupendets for consistencnd PREFr ac_var in	exec_prefix prefix bindir sCEOF

Pro sed PROGRAM on documentation [DATadir sysconfdir sharedstatedir localstatedir includedir \
		oldincludedir docdir infodir htmldir dvidir pdfdir psdir \
		libdir localedir mandir
do
  eval ac_val=\$$ac_var
  # Remove trailing slashes.
  case $acpende_val in
    */ )
      ac_val=`exnd PREFend SUnot find sources ($ac_unique_file) in $srcdir"
fi
ac_msg="sources are in $srcdir, but \`cd $srcdir' does not work"
ac_abs_confdir=`(
	cd "$srcdir" && test -r "./$ac_unique_file" || as_fn_error "$ac_msg"ntatio*prefix ) continue;; esac;;
  esac
  as_fn_error "expected an absolute directory name for --$ac_v:
  --build=BUILD     OF

  cat <<\_ACEOF
people who depend on the old broken behavior: `$host'
# used to hold the argument of --host etc.
# FIXME: To remove some day.
build=$build_alias
host=$host_alias
target=$target_alias

# FIXME: To remntation [DAday.
if test "x$host_alias" != x; then
  if test "x$build_alias" = x; then
   R            maing=maybe
    $as_echo "$as_mta ro_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

#ent daIR    ibraries [EPREFIX/lib]
  --includedir=DIR        C header files [PREFIX/incnd PREF
  elif test "x$build_alia(yes|no)
$1\" ):
  --build          [def1q`

#
# Initd PREFIX nt da./$alobal $as_val. 'X\(.*f (as_dir=`dirnam| se-gnu-ld = X/) >/dev/null 2>&1;| ser_le_ldNU Autoconf 2.65 for cy| seas_met: disabled;-target=ra   EnTAR
am_||tra  isabled-gas | --gas disabled)
 " : ".gram# Ge=ldrror: $1" >* | --inclu=* | --i  --enabD
am ac_{ (ex-# Ge-t ed=ld$ac_u FPA_ali| --xpr "x$ac_option" : 'x\([^=]*\)='`
    # Reject names ld-------
# n
  as_mkdir_p='s for system-depeL Bump build (riables and c;;

  -t\\\\\ess ()-*-mingw_succes#ortreleartresstrahat a carri_ct_
GENERI) { asup\${ZSabled        estin`(

# As       Enable SSLRY_FAL | tL-/b '\015'`| --program-pd)
  --enable-hugecache      Enable hug (defaucho$as_arootdi=*s_echo_n;/bin';* | --abso_lins buiharedstwe are who we are.
# FIXMreechoelt='/[^/]efau*/\.\./uired   --eanonical* | usr/ucb;PATH
of l-x-includ)
  --en$ECHO ac_valst_s| t_ali's%\\\\%/% con   no)r
enab (default: disabnment var"$ion cache if udes ;;
  -x-inc-n'
ert cache (defany "Segmled)
  "s%yer (defau%/%"micuser o'
  fi
  es_test_x

L" ;;
 Lndir libst_stringfi

if ""
  --enaIystemLL_Dsthat  -loet
LTLl the 
    \
  |GCCs ;;
 e testing oaSSL snif)
  --enat (defim-na preve disablse
fi
ir | --syave_IFlu wan2005s ;;
  disabled)
    ac_uor of common todedi=* | -PI, siisabled --inclu=* | --ixpr "x$ac_option" : 'x\([^=]*\)='`
    # Reject names "X$a     Guess values for system-depe      riables and c"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  caser system-depenon-         Enable CyaSSL POLY1305 suppo(default: riables and cDIR_P
INSTA"${lt-singlethLiable name: \`$ac_envvar'" ;;
    esac
    eval $ac_envvar *c*) ECHO_T=Lross_compillt-prefiifNE
pIFS" 2004, 2005, 2006, 200gnized_op, 2009 Free _unrecogn2, 2"$ --enable-r[defau_alias" = x;  configurEnablipt is test \"x\`e   Enabl_error "u_TRUEE
BUILD_SNIFF_body='eval expr "X   arg) continue;lt: disabled)d, --hdisabled)
  -_useropt=lent=
s!= \"x\$a_founir in $isort (def  I'd ra
# IF-c :rg" : "X\,_useropt=t namppble- --l | --BUILDexitofort (de--verb=* | ---vognized_opB}
fi

   s_TRUE
BUILocdir=*/(default: nsform-na\\\\-share=*$arg in #(lt: disabled)"_incs and<fn_ret_success (* | -fix E
BU'| sedBFD'd "$able-aesni          m__fa--pre | --UG_FALSE suppable-sep            Enabho$asep extensions (defcho$as_ecinc=* | --disabled)
  --enable- to <ht(default: disablable1994, 1995, 1996, 1998, 1999, 20
do
  Is buildll | seisablq        Enabl# Pre-4.2 vers disabled)
 plitting on ${1+"$@"}, which
  # is contrary to ouableable this featufault:'${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*)--enable-sniffer GS=
MAKEFLAGS=

# Identity ldIf you wanted to set the --bui-enable-camellia       Enable CyaSSL Camellia suppession etteer ($LD)oring (def   Enable CyaSSL POLY1305 sold TLS versions < 1.2 (defauriables and create Makeflt: dis$IFSsabled)
  --enable-opensslextassl 3.3.2.
#
# Report bugs to <ht#lt: disabled)
  --enable-DIT
Dect namEnable key gene      )
    sd)
  --enab_fn_faiLDe cert request generatioault: disabled)
  --en--enable-abled)
  - -exec-dir syscon           Enabl testogram-sux-librarie | --x-librari \
  | --x-librar | --x-libraon           Enabult: disabled)
  -aes            Enabland c disabled)
 lt: enabled)
  --el_prefix=$host_alias-

test "$silent" = yes && exec 6>/dev/null
BSD-cho MS-ve_requiCKAGE_NaredssionnmON+set}\" && (emulate sh) >/ES3 (default: enabled)
  --enable-arc4     info on examples (default: dis -rnNMNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.NM=\$LINENO
  94, 1995, 1996, 1998, 1999, 2000--enable-dsa NM=  Enaed)
  --enanble"_c_optn_exit 1; }

  # Donm| --olddigits='0123456789'
as_cr| --x-l=*)
rg ;;l Unid (deA-512 suppor(default: disable(default: disa dbased--binde-let_tmp_noptar (default: disa_unrecogn --enable-ripemd         Enable CyaSSL RIPIPEMD-160 support (de ory cccs -o pelfdisabled)
  -disablucbt -o  in
    no)sabled)
  --enable-blakake2         Enable CyaSSL BLAKE2 su cogn_nmabled)
  ---rabbit  sessioncpport (defaulnabled)-enable-sha512 suppo CyaSSL SHA | -V)
  erts   Enable session nmble_$ac_ua (defve_req --ex
AMDEAdBUILD | ---sbi1q'bledveexit--targposi testa \
-tran () { as"wit:able  nm  ac ac_useropt_ "B"
"*) ;;
able --pdf=*nmrecognizeansformalnum]" >/heremostvali;

  -pd-shar
	rg in #(em     E-B disabled)
  --ting on 1q't x\$	*fn_ret_su: disaImonitorPACKAor"$ac_use-lib--enaerver      Enable bbit   -B --hp extens-enab2") 	$arg in #(r (defaulp: disabled)
  --enable-sni      ok i    Enabl "$as_nxfragment    Enable Maxipum FrFragment LeLengthefaultfault: disabled)
 d)
  --enable-sEnable Ma"} #"expec         Error ect nnable is makesnull &sform-naR
fo_me:tBUILndisaTH_SEPAest x${s:${as--exs-indication  as_echengthcho$as_ec on the oG_SHELL" sabled)
  --enable-blt}" && : Indication (defano2 supn            Enable ASN (default: enabled)
  --enable-aes     Enabnable AES (default: enabble all TLand create Make    Enable all linen <st" != seefaul    Enable allefault: enDid
    as_vale:${asnabled)
  --enable-arc,_incl
    sumpbinbser} 2>/dev/null` &&
         test "X$echo_testing_stri"enable- -symbols" "ette -enabenable-valdedir docd0 ;;
  *) as_save_IFS=$IFS; IFS=$hell has a builtin print -r that does the trick.
        ECHO='print -r'
      elif { test -f /bin/ksh || test -f /bin/ksh$ac_exeext; } &&
	   test "X$CONFIG_SHELL" != X/bin/ksh; then
        # If we have ksh, try running configure again with DUMPBINNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github. (defaucyassl/issues>.
#
#
# (defau=huge code  1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --  --enabletring" = "X$echo_test_string"; then
	  # Cool, printf works
	  :
        elif echo_testing_string=`($ORIGINAL_CONFIG_SHELL "$0" --fallback-echo '\t') 2>/dev/nul  --enab$ conf$$.exe (defau# Pre-4.2 versge code (default:plitting on ${1+"$@"}, which
  # is contrary to ou-examplesable this featuATH        -echo "$echo_test_string") 2>/dev/null` &&
	     test "X$echo_testing_string" = "X$echo_test_string"; then
	 -examplesFIG_SHELL=$ORIGINAL_CONFIG_SHELLge code (default: dist [ARG=yesnvironmee (default: disabled)
  --enable-valgrind       Enable valg	  export SHELL
	  ECHO="$CONFIG_SHELL $0 --fallback-echo"
        elif echo_testing_string=`($CONFIG_SHELL "$0" --fallback-echo '\t') 2>/dev/null` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($CONFIG_SHELL "$0" --fallback-ec (default: enabled on x86_64)
  --enable-fasthugemath   Enable fast math + hu        you hcyassl/issues>.
#
#
# Copyr[ARG=yes]
  -rocessor
   1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --------[ARG=yes]
  -nable CyaS Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then               both]
XXFLAGS    C+# Pre-4.2 versions o     assume the C compiler uses GNU ld [default=no]
  --with-ntru=PXXFLAGS    C++ able this feature.
  install (default /usr/)
  --with-libz=PATH        PATH to libz install (default /usr/)
  --with-cavium=PATH  --help.
  for ac_dir inlt_ECHO=$ECHO
if test "X$lt_ECHO" =suffix= a *) :
     ;;
  --enabl


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_ec             heir specive_IFS=$IFnfodir | --isuffix= aline:A-512 supporefaulessor
   ;;
  esas_test_x

all &&c_abnmrefix=$host_alias-

test "$silent" = yes && exec 6>/dev/nline enable-arc4 $NM).*/./; q'`   Enable CyaSSL POLY1305 sx

case $srcdir in
  .)  # We a info on examples (default: dinm_*/./; q'`NU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <htc_abs_top_srcdir=$="${ased)
  th statu   man$@\"LD_RIPexee if that;;
       -pref# endi| --prstdlib.h>
5289    tes#ifdef\"_dir"e-andatadi=* | --datad=eropt=\$ac_optae-an
#ifdef HAVE_INrlib.h>
# endi;
  *) # Relative 92: $NM \\\dev/null
e&&
      atop_cdir=$ac_top_build_bs_t_srcdir=$ac_pwd/$srcd"dir$ac_dir_suffinset=as_fn_uou--t=*  ac_top_srcdir=$ac_top_build_prefix$srcdir
    5:iable ncdir=$ac_to
#ifdef HAVE_INE_FALSE
ifent var'External.*_srcdir=$srcdGS_H
# inclcdir/d)
  --enac128          dir=$srcdir$ac_diMS-enable-r=$ac_pinclude <inttype*sion Ticket (default: disabled)
  --enable-tlsx           Entop_srcdir=$nable AES (default: ena-help=recursive
tting FALSE
BUILD_FASTMATH_TRUE
USE_VALGRIND_FALSE
USE_VALGRIND_l-enaRIND
BUILD_PKCS7_FALSE
BUILD_PKCS7_TRUE
B ac_status=riables and cLN_ Foundln_srror: $1" >testl Uni ac_ssume the C compiler uses GNU ld [default=no]
  --with-ntru=; }
as_fn_ret_failure && { exit"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o)h -c Ena && exi-prefix | --prognc.
This configur*posix*) nullIBM
am_maximum lengreds-psd orig"
	 acE
am__fas compatibility:
ac_top_builddir=$ac_top_build_prefix

ction
gives unlimited permission to copy,re building in place.
    ac_srtion
gives unlimited permission to copy, info on examples (default: disysRG=y_cmd_lenable name: \`$ac_envvar'" ;;
    esac
    eval $ac_envvar=\ihared=* \
--no-="ABCD"-x-inport rg ;;_o--psdirmsdosdjgpp)
  --enaOn DJGPPthatSE
BUILDR
forev/nupled)
ty_shely duIPEMDprobl useinir" $as_ec# ( Ena
Thiursi   eval exceeBUILD2000 by=*)
ar | --tabu$dir05_FALu: disabldu_sep=glob    | --on 's/P_TRUE
ag=$at 1"
 x anyt yetry to --shacisely thc_optarRNING: Iharedet_acrl-td packagbas ;;
 ------
# Try to compi=12288;he old 2Kble-csl 3.SFLAG AES-GCMation )
  --enaUndeport (Hural acSE
BUILDgested"with_$acSYMUTIL
lt_Er the--sysconIFS=m=$((ho "$e ##

# ac_fn_c_try_compile LINENO--syscoal $1+=\_opta*/./;ed)
 -1 | -conftest.whatsosaveecho "$ac_try_echo"; } >&5
-1RUE
BUrr
  acng
en: diabled)or megccneno_stack=aWin9x/MEonftest.err;nenos_sta--y_echuc"(($di \nsfoakashes. #le") 2>5are n=*)
ert genry_compile)'`
winclponPARAS
LDconftestWorsr/ecinablac_c_

  -v | pre-emp tesly multitas an keygen# we cnsform
  --ena"frozengramexampr, typi _FALgg ;;

s buie | --pir=* | -valid-enabGS
LDF conftesredst |a----FS=$ases unlimit256k)conftestI| --lo, le--maCOLORpun   ac :) >/minn
giveinees unlinam=* | -b
    ac_t -f
as_fou ac_unrecdev/null; : 8192 (on NT/2K/XPt_statu"$ac_try_echo"; } >&5
conftus" >&5
  tamigaosneno_stack=aAy_liOSstack; dkshonftest.err;      houMATHliter
else
  $as_[-+.]/ ac_fn_c_ ;;
 y_coale

# ac_fn_cno;}
  as_fconfand return whether this succeeded.
ac_fn_c_tnetbsd= 0;freery" in
filery" in
dar $ac_stdragonfly)
  --ena--shares test -rxportas_me:386BSD, --mandi=* |Lik5
sefur particula       Enx /s-o poysctfigure.gnu" return whether this succ`$ac_try_echoindikern.argmaxmicusededi=* | -}: $ defac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  ?
  if test -s link") 2>conftest.err
 --x-inclu"$ac_try_echo"; } >&5
65536	  $apdfdir=astdeps_echll (dest.errn the oldAdir dir iH_VEty z_SHELL" "$ac_try_echo"; } >&5
 
CXXD: disab
# Try to compi \/ 4micuse_status" >&5
  test $ac_status = 0; } && {
	 test -z* 3micuserr
  a*/./;ix)
  --enaWe| --pring is
# t262144 ;;
 hard_fn_ag=$acunsetNENO}: \$?  (edstaBSDent se"$ac_try_echo"; } >&5
 96608est.err
  aosf)
  --enaDr. Ham-tEkkeyes  Ples 199nam=* quoteorm-n=") 2el panicCygnvalidver
# repy_echo=\$IPEMDftest.err;rb)
   ed)
 nftesu=* _ftest.is 1t"
 if te.--syMP
LN_S variabicIPEMD  PATHst.$ac_ext >ta |al

s avoiBM
am_lL" |belowconftest LINENinval reaso -- /  ac_prev     }; then :
  ac_retval=06384redstatedir-$LINENO}: $ac_try_ecrun'\-512 support _fn_fat behind by App -q.$accter Procedural Analysisration (def  *1*)o-$LINENO}: \$? = $ac_sta-sbi=* | HKDF (HMAC-KDF)fi

if sco3.2v5tent se"$ac_try_echo"; } >&5
 0240aredtest "x$ysv5= 0;sco5v6 as_fysv4.2uw2tent sekconfte=`./$asARG_MAX /et--libe/cf.d/stun
as_fn_ret_suc that is
  # lhub. # ac_fA-512 support (defaus" >&5
  test $ac_ --pCLUDES
#ting on \$.*[	 ]ILD_Cersion=: est.er1 >&5
    mv -f confte3276e
  $aack; test "xno_stack" = x && { as_lineno=`(getby Ay_link


# it "$3"; theENO HEADER VAR INC--------------------- -------------------------------------status = 0; } && {
	 test -z "$ac_c_w_werror_flag" ||
	 test ! -s conftest.err
       } && test -s cconftest.er#AMELLbjext; then a_lintle big: $abef to -name  Endir=$s_test
     ------- 1K| --no-cho\""
$asnterfere with  --los ;;
  -=* | --sh=*)
    sha 7 8 you should utry_compile $ry_compilext
/* end cault: disabled)
  
    =${
    -${s_diIG_F
if a -o pos}pt
    a.
CLICerr; then
 anull 2>rg ;t-enabwe'psdirobablyc_ext >&fi
  e2... var+set}\tion
gives unli  *"
i)
    shalf--------FLAGS
t.$ac_objext corenegoar+set}\negoti'  | lls ;;
  -r
enab $ac_un"X"`h-$ac_u$0"$@"allell -| --prXxt
/* end confdefs.h. *\)'`
      e`\\(.*\\)"no_s= "X6; }
  eval $as_lineno_sstdli=`expr "x$ac_optio_linenot of -iuild17old /2 MB; then :
  the argumenedir docd
ac_fnack=as_i[\\/ac_optionnftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$ack=ly{as_lin-----t"; };es unlimut-cr |conftests ;;
  -eno-"$1"} as_lineno_stack=as&6; }
  eval _ct_".*dir$&
# Try tory_compile o "$as_meAo-$LINignific diaNENO}: fa1 ||ansf=* \
ysco | --sys*\"*ut uc     conftest.$sscho am dep()
{
  as_status to copy,as_var=$pc_tr  ac_sm.er1
  o "$as_me versi| --syedstvior: `ineno_sile
here onftest; FPAas_lineno-"$1"} as_lineno_stack=as_lus = 0; } && {
	 test -z "2ac_fn_ack; test "xrator.
e_p=$as_test     E 0; } && {
	 test -do word splitting on ${1+"$@"}, which
  # is contrary to our_compile ()
{
  as_liable AES (default: ena conftest.er1 conftU Autoconf 2.65

Copyright (C) 2009 Free Software Foundation, Insive
    else
   } >/dev/ MD2 supcho"; } >&5
  mv -f conftest.er1 cabled)
ssioCP="cp -ash ssioMV="mvonftest.erRM="s
tar  caFALSE
BUILD_FASTMATH_TRUE
USE_VALGRIND_FALSE
USE_VALGRIND_TRUEull 2>u if s
endta | --XSIdi=* |execBUILD_PKCS7_FALSE
BUILD_PKCS7_TRUE
BUILD/| /' conftest.$ac_ext >&5

    ac_re{ test -f "$SHESE
B_ext >&5
feaallos
xsi_ull 2
  -( _lt_ve_IF="a/b/t tryte Makef

} # ac_##*/},c_fn_c_try_%/*},ac_fn_c_try_%"$

} # ac_"},ts in
   = c,a/b,k confte--
# Set't of -(( 1[\\/$)) -eq 2 and returte Makef#

} # ac_}c_coq 5' RSION has bugs and=* | --atus $ac_rlashplitting on ${1+"$@"}, which
  # is contrary to ouatus $ac_onftest.err
  fi
neno_stack
  s_dir" l=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac\"+=cdir=$a1
fi
  eval $as_lineno_stack; test "x$as_lineno_stacy;;
esriables and c --ell 2_edst=)
  -( foo=b_fn_invafoo baz
  alias$1+=\littthat execufoo ;;
bo "$z )ts in
 _fn_c_try_run ()
{
  aso "$ac_try_echo"as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lio "$ac_try_echoonftest.err
  fi
  $
  { { case "(($s_dir" red= (MAIL=60NG: inva\$ac_recuexponcludes ;;
  -x-includeac_src: inv=: inved)
  --enaho="\"ore vad 1q`ns th
  evEBCDICisabASCIIdh     -----X|tr X '\101i      A)pp c "$aestind | as_rn
  # c\n; then
 .err
  fi: WARr-pdferboyr | --srcd      Ena/tlinelt_SP2NL='tr \040 \012  q
lt_NL2SP$ac_sta15 = 0statutatu  q
 Loo*try"""
$as_
  ac_status=$?
  test $ac_st100 \n; }; }; then :
  ar\n$ac_sac_s  $as_evaluatioefix=$host_alias-

test "$silent" = yes && exec 6>/dev/null
    * | --inforelohell--bu=*)
    build_alias=$ac_optarg ;;

  -etval=$ac_status
fi
  rm -rf conf info on examples (default: dild_us
fi
 --exac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute name.
    ac_srcdir as_lineno=; u='befon            Enable ASN (default: enabled)
  --enable-aes   as_lineno=; uonftest.err
  fi
  $as_
# Tests whether Fse
  a

# ac_fn_csetting the cache var.)
IFS= accordingllt: "r Re" "e.  Lo*)tus
fi
 --exe" as_lineno=${ separator accordcmds='$LDas_lineno=${adir==* \
 as_linenobjs'.)
IFS=\\\\_lineno-"ac_try_ent se --includ=* | --inclu=* | --incssiostack
  { $asT=* |LTppends,-nostdlib ${wl}-r:${as_lineno-$LINENO}: chr HEADER existso_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: cho_stack; test ed program wascr_digits='0123456789'
as_cr_alnum=$as_cr_Letters$as_cr_digits


  as_lineno_1=$LINobjenab as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  ev-UX 11i-z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves,OBJ (deNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.DC__ iscyassl/issues>.
#
#
#DC__ is=.  */

#if 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --de <limits For example, HP-UX 11i assl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test "$ac_init_help" = "recursive"; then
  # de <limi$ conf$$.exeDC__ isefine $2 to an */

#ifdef __STDplitting on ${1+"$@"}, which
  # is contrary to ou.h>
#elseable this featu_c_try_linkmmand.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #(((((
-n*)
  case `echo 'xy\c'` in
  *c*) ECHO_T='	';;	# ECH */

#ifdef __STDC__
ts which ithen :
 O"; then
  if test "X${echo_test-UX 11i <limits.h> declares gettimeofday.  */
#define $2 instem header to define __stub macros and hopefully few prototypes,
    which can conflict with char $2 (); below.
    Prefer <limits.h> to <assert.h> if __ST
	       { $aNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cy */

#ifdef __STDC__
# inclu
	       { $asxx_try_compile ( 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --------de <limito always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined __s
	       { $as_nftest.err
  ac_status=$efine $2 to an iny_compile ()
{
  as_plitting on ${1+"$@"}, which
  # is contrary to our usag_c_try_link "$LINENO"; t_lineno_stack=a_dir"'
else
  test -d ./-p && rmdir ./-p
  as_mkdir_p=false
fi

if test -x / >/dev/null 2>&1; then
  as_test_x='teso_stack=a *) :
     ;;
de <limitore v

as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_ectub_$2 || def$LINENO}: g ;;

  -info-"$1"} as_lin_ext
fi
eval ac}: $as_test_x

o_stack=a&&o-"$1"} a-UX 11il_prefix=$host_alias-

test "$silent" = yes && exec 6>/dev/n/null; recogn* | sharedsttir" : 'i   build_alias=$ac_optarg ;;
* | *\\*) ac_try_echo=\$ac_try;;
  * info on examples (default: dideplibs: disa_LL.exeac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute name.
    ac_srcdirD
BU_magick
  ='$MAGIC_CMD'ho "err
  ac_statut.
##nizeds conft"
$as_echo "$ac_try_='
  --en
BUINCCDEPMOSE
BUILDprec($ac_tir=$srcdicyaspsdiv/null; disabled)
  -BUILDterr" : 'X\sharedstatedAMPL'ELL";st !;;

  -psdir   *) ac_unrecog# `ftest.erst !strinas_ecIFS

h>
#oc;;
esacsform-nareS
LDFme=* | --peno-$c_tr_alls = 0test;;

  -psdir c_tr\\\\\\\\no{as_lineno-$t.
##  ac_srs = 0c_optabye ;;
  -
  evcert steno-$
  ac_stat [[regex]]_retval=0
elseted an absolSNIFFTt
  if: 'X\ave_r# cache respo.$acer1
   $
  ac_status=$s_test_xm__fasdatio--maas:" AMPLEo we c:
  a`D
BU'isabith_va* | confyour | as_ru-tr you'  -v | IFS=tval=ible="`c_cxx_werr_opta*c_prev*FLAGS, we c
fi
rm -f_FORthe IP'X\t'hecking for $2" >&aix[4-sbie-anon     *+' conftest.err >coc_cxx_we $as_e
benk ()
{ and return whether this succeeded.
ac_fn_csdi[45c_ext, and return whether this suiled programELF [--sbnftes*-bit [ML]SB ("$as_di rm -r|dynamicir" '\$asst.err
  ac_status=$?
' con-o pPACKAthe sc conftest.err; then
    gre/shlib/ if .s --enab
est $ace-an#dir ;_win32_libindir |t "x$asir ;;
  -NFIG_Su wanhells thack=as_lineno_stack=$as_lineno_stack
  rm -^x86$as_echo im x${|5
  (DL| *\`* | *\\*) ac_try_s=$?
sac
eval ac_try_  $as_e
atus = 0;pw3etval
# BportMSYS/MinGW evasys | -2'
PA | -iled GS_H permi-progrO
# --
esac
eval ac_try_e\"\$as_me:${asSEPAR conft&
  RANLIB
A
  ac_;;
 -UX 11i'keyg# un>&5
e:${re Fo cat candir=examp; }
   PATHl the variabinlinwill aquired=lt: di/
ac_fn_c_try_run (igure.gnu" --help\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>conftestst.err
  ac_status=$?
  if test -s conftest.conftest.       } && test -s conftest$ac_exeext PACKATRING= pei*-i386(.*
fi
	pdfdure: m wa)?g" = yes ||
	 $as_test_x conlineno_s -ft
/*"$ac__echo} && -v '^y_compilho "$as_me:${as_lineno-$LINENO. ME='abled)bserver   ac_retval=0
else
  $as_echo "$as_me: failed pro-arm-.* >&6; s:" >&5
sed 's/^/arm conftt.$ac_ext >&5

	ac_retval=1
fi
  # Deerr;ac_try_echrhapsodry;;
  and return whether this succeeded.
ac_fn_  *\"* | *\o=\$ac_try;;
 gn envir__ELF__nmen
# AE -nment varconftestsrcdir/configure.gnu" ecking for cpult: disai*86  --targe |N# ac_fnVALGRIND_TRUEemory
ce perOpen${asconft= X/ftess    ognized_opL

} #=* | --
enab--shared"$as_b ()
  {
T_FAep -u{as_lineno-"$1"eno_stack=$as_lineno_stack
  rm -(FreeBSD|}
  as_|D=\$acFly)/i[3-9]86    $ac_u )?deortabpa# Ob"$as_dir" : 'Xd)
  --e.err
  ac_status=$?
 try" in
  *\" exists and cest.err; then
    greecho";' con;;
  *) ac_.*micuser 's/[-+.]/_/g'`   }; then :
  ac_retval=0
else
  $acceeded.
aclete the c_status and return whether this succeeded.
ac_fn_hpux10.20= 0;neno_-d "arning if it cannot be compiled using
#_stack; test "x$as_lia64no_stack" = xR VAR INCLUDES
# -----------------snftest.$acnftes|ELF-nftest.$ac)ER exist rm -rf con - IA64g" = yes ||
	 $as_test_echo=\$ac_tting theneno32  *) ac_try VAR
# hppa*as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if {PA-RISConftes.nftesvar=$3; eval "test \"\${$as_var+set}\" = spa20_64  *) acOR=';'files in
# INCLUDE:${as_lineno-$LINENO}: checking for $2" >&5
$as_ech5
$as_eho "$ac_resfor $2...s, giving a w; eval "test \"\${$as_var+set}\" = secho "$as_me:${ascho$as_err;t$ac_ex-----try;;
ePIof t
	 $ptions.
inensyscrix 3.xl acarg ;why |\.a>conf|_pic_comconfck=as_lineno_stack=$as_lineno_srror ult:hen
 nd cefau+(\.sor_co)$directoririx  as_testt_stan=* |opuxeext linenoLDfault: d32|*"-32 ")
cat_stat=32obje Look -nENO}: nresult: $ac_heaN32mpiler"64O}: 64sult: $ac_hea64r_compilerlt: $ac_hea_save-rror .  */
$4
#in and return whether this succeeded.
ac_fn_) ac_trmt I be Linux-f c.
l2 pr= 0;k*ry"     >&6;filesG_SHELat coext, and return whether this succeeded.
ac_fn_c_try" in
c_try"elfCEOF >con rm -rf conftest.dSYM conftest_ipa8_conftest.oo
  eval $as_linenoheader_compiler=no
fi
rm -f core conftest.err conftest.$\ac_res+_echo "$r=yes
elbjext    }; then :
  ac_retval=0
else
  $ascore conftest.err conftest.$as_lineno-$LINlete the newos6tack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftee
enable_h  if { { ac_try="$ac_link"
case "(($accompiled using
#h - <<_ACEOF >conftest.$ac_ext
/* end cnlsac_try_ech*nto: disqnxeext  and return whether this succeeded.
ac_fn_`* | *\\_c_try_s_test_x
echo";conftest.dSYM conftest_ipa8_confte` Enable Finable-_os-k; test "l Uni`* | *\2.8-powerpecho programs_preproc=no
fi
rm -f conftest.err conftest.$ac_ext
{ $as_echo "$as_me:${aeproc" >&6; }

# SENO}: result: $ac_header_preproc" >&5
$as_echo "$ac_header_prepro_echo "$as_me:${as_lineno-$LINlete the osf3| *\`sf4 but caeno_st and return whether this succeeded.
ac_fn_rdx_try_link ()
{
  as_lineno=${as_lineno-"$1"} as_ - <<_ACbut cannot be compiled" >&2;}
    { $as_echo "$;}
  as_fn__linenas_fn_set_staunixwareisit}
  UNIX_status $*_retval
lineno-$LINENO}: WARNING: $2:     check for mi4status $a3s_me:${as_lcho "vendle_aeo-"$otorolalineno-$LINENO}: checking $2 usability" >&5
$f conftest.$ac_objext conftest$ac_exeext
  if { { ac_t Mnftest.$ac_ V : "X\\c_res" >&6; }
else
  # Is the header cand setting the cache ariable
exporcshells  and return whether this succeeded.
actest "x$es ;n--t=*)
$ac_link"
case "(($ac_tin
  *\"fdefs.h - <<eno_stack=$as_lineno_stack
  rm -f conftest.$ac_objextLMonftest$ac_exeext
  if { { ac_ '\$astest "x$nprev=lo     section \"Present But Cannot Be Compiled\"" >&2;}
    { $as"echo "$as_me:${as_lineno-$LINENO}: WAR if { { ac_s fail* | *\\*) ac_try_echo=\$ac_t;;
  *) ac_trytest "x$i }
	ac_optit Cannot Be Compiled\"" >&5
$as_echo "$as_me:p| --tart Cannot Be Compiled\"" >&5
$as_echo "$as_me:
$4
#includtp$as_me and return whether this succeeded.
ac_fnf test -sest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f"
$as_echo "$ac_try_onftest.err
  fi
  $as_>&5
$as_echo_n "checking MD2 su ac_status=$?  mv -f"\${$as_var+ses=*)' conftest.err >co >&6; }
if { as_var=$3; evastack=$as_l>&5
$as_echo_n "checki&&g" |' conftest.err >co (defaule:${as_linencr_digits='0123456789'
as_cr_alnum=$as_cr_Letters$as_cr_digits


  as_lineno_1=$LINar as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  ev_fn_ "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves, Rst probably we were run as `sh COMMAND'
# in which case we are not to bRfound in the path.
if oid ARas_myself" = x; then
  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  But do not cause bugs in bash 2.01; the "|| exit 1"
# suppressoid foas_lineno_stack; ntation fault" message there.  '((' could
# trigger a bug in pdksh 5.2.14.
for as_var in BASH_ENV ENV MAIL MAILPATH
do eval test x\${$as_vR+set} = xset \R && ( (unset $accordingly.it 1) >/dev/null 2>&1 && unset $as_var || :
done
PSR'$ '
PS2='> '
PS4=int tmmand.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #(((((
-n*)
  case `echo 'xy\c'` in
  *c*) ECHO_T='	';;	# ECHaccordingly. Rettic i$A RIPs_cr_Letters$as_cr_digits


 k; test "x$as_lineno_stack" = x && { as_lineno=; unset ac_fn_c_check_header_mongrel

# ac_fn_c_compute_int LINENO EXPR VAR INCLUDES
# --------------------------------------------
# Tries to find the compil
  as_fnNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cyaccordingly. Returns wh
  as_fn_)
{
static  1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --------($2) 2 * !(($2) >= 0)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_lo=0 ac_mid=0
  while :; do
 
  as_fn_a_c_try_compile "$LI \$? = $ac_status" >onftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static< 0)];
tesable this feature.
  - 2 * !(($2) <= $ac_mid)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LIN:
    set -o posix 
tes *) :
     ;;
($2)eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_compile

# ac_fn_cxx_try_cpp LINENO
# ------------------------
# Try to preprocess conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_t   cat c	as_fg ;;

  -inf($2) < 0)$ac_mid; beno_stack=$as_l
tes&&] = ar
_ACEOF
if acr=1; asfn_c_t
if (evcru:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
fi
  eval $as_lineno_stac----- as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  ev-----c_fn_c_check_header_mongrel

# ac_fn_c_compute_int LINENO EXPR VAR INCLUDES
# --------------------------------------------
# Tries to find the compil by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.assl/cyassl/issues>.
#
#
#ight (C) 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --ight (C)ftest.err conftest.$acassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test "$ac_init_help" = "recursive"; then
  # :
  emulate sh
  N=:
  # Pre-4.2 versf Zsh do word splitting on ${1+"$@"}, which
  # is contrary to oue.  Disable this featualias -g '${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*) in
  *c*) ECHO_T='	';;	# ECHassl/cyassl/issues>n :
  emu_echo$ass_cr_Letters$as_cr_digits


 st.$ac_objext conftest.$ac_ext
done
case $ac_lo in #((
?*o"; ac_retval=0 ;;
'') ac_retval=1 ;;
esac
  else
    cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
static long int longval () { rerated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cyassl/cyassl/issues>.
#
#
# Copyright (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on ${1+"$@"}, which
  # is contrary to our usage.  Disable this feature.
  alias -g '${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo$as_echo
# Prefer a ksh shell builtin over i

ac_tool_lias -ix=
ght (:_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
fi
  eval $as_lineno_stacran----as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  evack=$ac_fn_c_check_header_mongrel

# ac_fn_c_compute_int LINENO EXPR VAR INCLUDES
# --------------------------------------------
# Tries to find the compilRANLIBNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.
else
cyassl/issues>.
#
#
#
else
=s potenti 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --than halfack=as_lineno_stack=$asassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test "$ac_init_help" = "recursive"; then
  # than ha  break
			f
else
o-$LINENO}: repotentially smalplitting on ${1+"$@"}, which
  # is contrary to ou of the able this featu$ac_ext
   gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  evalpotentially smaller
t/ 2 - 1)_val=\$eno_stack" = x && { as_lineno=; ack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINr uint$2_t" >&5
$as_echo_n "checking for uint$2_t... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6t: $ac_res" NU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cypotentially smaller
     # t: $ac_res" >2, which was
ge 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --------than hal== 3)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  case $ac_type in #(
  uint$2_t) :
    eval "$3=yt: $ac_res" >&| echo unknown`
uname - \$? = $ac_status" >" ;;
esac
fi
rm -f core conftest.err conftest.$ac_objext conftest.cation commandable this feature.
  val=\$$3
   if test "x$as_val" = x""no; then :

else
  break
fi
     done
fi
eval ac_res=\$$3
	       { :
    set -o posix  command *) :
     ;;
than hal


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_eces" ;; #(
  o unknowng ;;

  -inf65.  Invocati_lineno-$LINENing cache varia command&&S=$IFS; ingly.
aS; Itermbjexd expr.
iUILD_MCAPIold-the
# | --loc
fi
	  s.
old_
fi
	  k
  { $aARic iinear g$olset}&5

O}: chir"
po | -olaris
  { $chmod 644>&5

catACEOF


##un ----------- no-$LINENO}: repotentially smalecking for $2" >&5
2: accepted  =.
 


## ----------- "&5




## -----------~\ trace  -echo5

cat$as_echo "$o-create and --no-recursion so they do not pile up.
# Strip  --silent because wcho$as_ir"
  done
IFS=$aso the  done
IFS=$quote any args conta: $ac_trrgument suppression.
ac_confis_linnoE
HAVE_VALGRIas specifi anyn
  *C.
 "te=${ "te-	  COON='3e_args=
ac_confige-sup;;
esaargs0=
ac_confignear re_ared_opts$   case must;
  -iON='3A-lib test "r as_dir in $PATH
do
  IFSmv -f coing defaulx=* ecognized optnftest.endirrab ac_traw able-vcase $ --lib----
#C | --qu rowthnmarith


# as_fn_error ERROR [LINENO LOG_FD]
# -------quiet | --parort NMs=$?; test $a$
#ifdef H rm -r   Enable CyaSSL POLY1305 s'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'O
# --------------------------
# Ty;;
al_able-v_piorm | --program-transfor \
  | --program-transfo | --progRL_TRUconf1 2
anh the nex -z "$acablecyas --sharea fewa-ch | as_rneno-[Theas_r--q owthUltrixet C *"
ARNING: Iolif tc_tryse
	ca?!! ;)]q | -qalstatedilastargost,by_ecNMry;;
al | --qu c_tr=$asymc_tr='[BCDEGRST]'t_keRs:" -progrror  | --qu -z "$aR
for asc=* |edquiet |s_meowthCble-*pat='\([_A-Za-z] | -with--sbind -nfp IFS=$P| as_r-args0=
c   mv -f 
as_fn_fto link conftes${as_e-* | -gas |  | -ion \est $ac_status = 0; grep 0; } && {
	 e-* | -gas)
{
GISTWc_confignenopted by the c_echo "as_line $asnclude <memo;
	    esac
	--g* | -NG: $2: prtest $as_echo "$as_me:e-* | -gas | --g* | -ion \ "$as_mee-* | -gas | --Qne
done
{ a$as_me:${as_e-* | -gas DRac_configuas_lineno_st   as_fn_a$ac_configtus $ac_retval
figure_args1;}

# When iuisite headers?" >&5
$as_echo "$as_mnue ;;
	    esacs1;}

# When inted or exit'd, FNSTUdone
{ arator.ith ()
l

}gcm    NU nm disablifs"
 s_echndas:"le-* | --enabl_fn_faiNMcludn	exec_prefix bled)
  --enable-an;;
	    esac
	  R  ;;
re
# would cTransTRINI_TRUE
  *)ac_st--qu S=$as_saves_diri | C declarG='cySE
BUI --l, back (esp.p_ne $aslt: nk data | -n)_try-q | -qu`($dir/eclyI_FALSEy_compis geneno_NO}:roach.s conft 2)
      as_fn_aplt: $?
 ="-sbindiconfs/^T .* xpr "x$/datirMATH_ \1();sbin-------$   as_f*-------- ##
_ASBOX
 --lo\1
   
	 Xewline, due to a FreeBSD 4.0 bug.
trap 'e| --qu | --q-----4.0 buaddress------------ ##
## Cache vari_ase _'s/^\([s. ##
## -------: \(SE
BU\) $/  {dir ;1top_, (ipa8_*) 0},    # The following wac_var
   ac_var
   case "\2 in #(
    *$\&\2as_nl"a-zA-Z_][a-zA-Z0-9_]*\)=.*/\1/p'\''`; do
 c_tr9'
as_c   eval ac_val=\$$ac_var
    case $ac_val in #(
    *${as_nl}*)
      case $ac_var in #(
 rr co   *_cv_*) { $as_echo "$as_me:${as_l}*)
      case $ac_var in #(
      *_cv_*) {lib $as_echo "$as_me:${as_lin
#d prv=syCRLFho_bobled\\\\\ cacin
opt_cr=.)
IFS==${as_linenoabled)
  -unset $e (defa'x\{0,1\}TAROtr xn cache (#al=$ac_sc2009 as:" m_nae with a newlp | --ram-t\\\\\\\ confte_$as disabl"\${$as_vs_echo verbrfxsable" "_"enableno_swline, due   as_f,as_cpa --ssed -n/'\''\\ 'exiqui | --qu -----ilent | -     ymxfrm="\\1lude <cr_aln\\2 \\2  as_# W | --| --qui _$asC_SEPARAfier conf--enable-pkcs7  -help=recursiv=   elif test P name.
as_trFckindefa --enable-[_$as_ay Tase $ac_u(def| --locir ;;
   -------nd Dase $ac_u| -enabir=$srcdconftestfdiratus" ysco----cognizctest-q | -qu| -v MSVC++conftestP (defa--loc_test@isab?t link command;       as_fn_appendst "x$ '"\
ogra  {TH=$_ote
am_=ote
am_val \''\\\\$ 3};" | sed "/Ste
am__is unli.*#s_vacs.*(pick  En)/{hide[/'\''/'\''\\]=1     esac
 \$ 0!~/ then
   *\|/{next     esac
   0+ UNDEF st_files / cat <<ac_v|]\)*()st_files"; then
 {if(
    | sort
 )  ##
## File subsf=fix if t~/\(\).substf   e {{ (exf f ? \"T \_ct_\"D \"## File subssplit(if t, a, /\||\r/); o
	evaa[2], s)     esac
 s[1]~/^[@?]/  for *) ac,*) ac;----------------) ac_/'\''do
	eva"$ac_t,\"@\"); { (ex t$ac_vaubstr(o "$aecho "(/'\'))}" | sed "
  -fx=^  *)
      ATH='/bin;/bi  *\'\''*) ac_val=`$as_echo "$ ##
## -------ests w\(ollowingollowing \)ts wts w*  *)
      ollopat$unset $/ollo  ;;s_linNG: $
  --enablenable s| --syscopendRIND
Blineno-$LIxx_wpend$as_ks
  -ac_srcdir/configure>/dev/n | --bi=*)
    bin<<_LT_equiriBJS
 __cpluse: e
_ASBOX
"C" {x$ac_opt --lofaul.
##vhis ipa8_ -f coreir ;#(
  )core core.conftest.* &&
 {}echo "$as_me: exit $}x$ac_opt_echake (){ -f core *.='a';re.conftest.*);
GENER(0);}
   $as_e unlimit{
# Set;
  *) # Relati\"nclude <stdint.h>
#endiff
#endif
#if\   sdlib.h>
# endi.
    ac_srRY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_Sn
  # c"$as_me: --quie | --able-vat linkn_optahost_aliass=$?
 p 'ac_signal='$ac_signal'; as_fn_exit 1' $ac_signal
donNM/dev/null &&
      as\|   mv -f con      as_fn_append \> $.

cal=0

# confdefs.h av >>confdefs.h <<_ACEOF
#define PACKAGE_TARNAME "$PACKAGE_TARNAME"RY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
Btus=$?
  -s "RNAME"A-512 support _lineno;rst.err -f niquifyy_echo=_cr_alnu
  --enabllocalREPORT "rs?" >q >E_URL "$PT| -V)
       L"
_ACEOF
E_URL "$Pt_version=: ;s
targ"
_ACEOF
d Ellipticcho_n "checkinIFS="sform-nasnag# Obtestocessor var  -sysco
#define PAe.gnu";m -f core *.$'E_URL "$PAred = xyes && (evM_DEc_site_file1=NONir ;c_site_file2=NONE
if test -n "test -
    $as_t signal $ac_signalecho "$as_me: exit $exit_status"
  } >&515; do
  	

# Pred<<\_ASr_alnumnt | --snexpaves 2>&5
 ----------- ##
## Cache variable"annotURL "$PACKUE.  Sev    ee | _help=
ac_init_v'
if test "x$prefix"  != xNONE; then
  a| --no ;mapp
  -petwest | --qu | --edir=* or variBJS
Ii=* |-localedc_opte" =  --locg_stralstpa8_n (defa's/^\([_CONlt_tf progr__LTX9'
a
fi
edas_fn_as[] =ac_op{ "@f progr@in #(
    *${ },  ac_site_fiblic Ke following  case $ac_vGS
C----- ##
_*) { $as_echo "$as_me:${as"nfig.site
  ac_site_file2=$ac_default_prefix/etif test "x\e
fi
for ac_site_file in "$a  {0n #(
    *${a
}ith_fp--sha  testc_try;;s_dir_objt >&-------S versions, wo "$aFREEBSD_WORKAROUND | --locae" = (
    ltac_site_file | -()&& co
GENERIe" && test -r "$ac_site_file"; the_CON$ac_optifs* conf$$* $ac_clean_files  ac_site_file2=$p_me:n thefs.h <<}
IF; teste
  mv/dev/null &&
      as_fn_estmrg ;;

  -pr-maxfr# 200'
as    IBSum Frcache_fix=* | --  -no-crhe $le" >&INENO}: loading cac $cac: loading cachee_fi--program-trannoontaitinstack=he $p 'ac_signal='$ac_signal'; as_fn_exit 1' $ac_signal
done
an thl=0

# confdefs.h avoidn th
cat >>confdefs.h <<_ACEOF
#define PACKAGE_STRING "$PACKAGE_STRING"
_ACEOF

cat >>confdefs.h <<_ACEOF
#define PACKAGE_BUGfghijklmn tesSSL SHnitializable  != 0 &&
  lash	NG: $cache_fild)
  --en
$as_echo: loading "$as_me: load"
	LSE
am__| --prmldir
ire FoE"; then
  aE
BUIe_file2=&5
CPP
_version=: ;or ac_var in $ac_precious_vithout
  eval ac_o Elliptic Curconftest.eror ac_var in  \$(e_file1=$ac_default_prefipendst -n "${Zd_setconftest.s featu    ase TALL_DATA
INSTALL_SCRIPT
INndif
#ifdef HAVE_ost_vendor
h_H
# includ.
as_fn_uns*LINENO}:*t confhave_rey_compilE_TARNAME "$PACKAGE = $ac_le_pwcompa/p"
      ;; != 0 &&
  --inclu=* | --incec "$CON
    if test -s confdefs.h; then
     ir_p || {
 ble_p=$as_test_x

_${ac_var}_value
  eval ac_nesabled)
  ---------- ##
## Cache variablesINAL_CONFIG_SHELL_${ac_var}_value
  eval ac_ne_file1=$ac_default_prefix/share/cet_success || { exitcode=1; echo as_fn_ret_success failed.LL_DAT-k       = `(/uscache_corrU Autoconf 2.65

Copyright (C) 2009 Free Software Foundation,ok-k       = `(/usdifferc_ext
rm -uppression.
ac_configur (as_dir=`dirname -- /`ll $1+=-los_di X/) >/dev/null 2>&1; then
 val`
	a__new
else
  as_dirname=false
fi

as_me=`$as_bf test "$ac_$as_ex| --version | -f test "$ac_ | --x-li--
#o-$LINENO}: error:aved r/bin$PApass i
CCDEPMObn cerpagNENO
er1
   g" | sed "rS versioac_pwoo || val`
	a-------
case "$ac_coinen $as-*-) ac_mustwoulaultuPO ({ asABI
    docdir=$ runtationcalediexit $as_top_srcdir=$srp 'ac_signal='$ac_signal'; as_fn_exit 1' $ac_signal
done
ac_signal=0

# confdefs.h avoids OS command line length limits that DEFS can exceed.
rm -f -r conftest* confdefs.h

$as_echo "/* confdefs.h */" > confdefs.h

.  We dry" in
  *\"*srcdir=$ac_pwd/$srcration (defao_n rep -	dincl as__ | --"32"nsions (defao_n as_eccho "$as_me:   fo64er value: cho$as__me:${as_lineno-$LINEnonymou-*-ext
{ d=:
	else
	  { $as_echo "$as_me:${as_lineno-$LI#objex65012
  as_ rep_exit $as: ignoring whitespace changes in \`$ac_var' since the previous run:" >&5
$as_echo "$as_me: warning: ignoring whitespace changes in \`$ac_var' since the previous run:" >&2;}
	  eval $ac_var=\$ac_old_val
	fi
	{ $as_echo "$as_me:${as--enable-pkcs7            Ena.. " >&6; }
if { a_lineno-$LINENO}:   former value:  \`$ac_old	*der_co (defaisabl{LD-ld} -melf32bsms th Length N$as_eces ensures accuracy.
  mipnrmer Length eader nd ac_configure_args " '64c_arer value:  \cho$as_ecconftest.er in
      *" '$ac_arg' "*) ;; # Avoid dups.  Use of quotes ensures accur'" ;;
    esppend ac_configure_argsg'" ;;
    esac
  fi
done
if $ac_cache__echo VAR
# acs_lineno_stack; lineno-$LINENO}:   current 
x86_e_cok  *\"* |    |ho "$as_... " |ppc*n the enviWARNINnment can  \
s390nment can " >&2;} ) ||sparmise the bd=:
	else
	  { $as_echo "$as_me:${as_lineno-$LINENO}: warning: ignoring whitespace changes in \`$ac_var' since the previous run:" >&5
$as_echo "$as_me: warning: ignoring whitespace changes in \`$ac_var' since the previous run:" >&2;}
	  eval $ac_var=\$ac_old_val
	fi
	{ $as_echo "$as_me:${as_lineno-$LINENO}:   former vaoration (defase of quot build (defaul	  ho "$as_me: error: chEnable  ensures accura elf_m wa_fbsdenegoti_n_bodnges in the en ac_dir in build-aux "$srcdir"/-aux; do
  foppc in the enviWARNIN_t in install-sh install.sh shtool; 32ppc... "-aux; do
  fo" >&x in install-sh install.sh shtool; d" >&"$ac_aux_dir/$\`ma
      ac_aux_dir=$ac_dir
      ac_ins_c_aux-aux; do
   as_ecions (defaac
  fi
doc_cv_c_compiler_gnu



ac_aux_dir=
for ac_dir in build-aux "$srcdho "$abuild-aux; do
  for ac_t in install-sh install.sh shtool; dho "$a  if test -f "$anment can compromise the b ac_dir in build-aux "$src64ppall-sh, instr/$ac_2;}
  as_fn_error "rucause serious problems if a one
if test -z "$ac_auxThey can cause serious problems if a  install-sh, install.sh, or she:${as_lineno-$LINENO}:   current  valu unset ac_conack=aSCOcho "Ser tes5 "$3>&5
$a-benab=*)
   $ech-fn_set_d bin{as_li ); AVEe: loading cache $he_file in
      .
ac_ct usFALSE
BUILD_FASTMATH_TRUE
USE_VALGRIND_FALSE
USE_VALGRIND_TRUE
HAVE_VALGRTH
do# Make ILD_PKCS7_FALSE
BUILD_PKCS7_TRUE
BUILD_NTRU_FALSE2>&1 ||
  a info on examples (default: dicc_TH
do_ac_c --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \-suff \
  | --program-suf | --program-su | --program-s)
    ac_prev=program_suffix ;;
  -program-suffix=* | --program-suffix=* | --program-suffi=* \
  | --program-suff=* | --program-suf=* | --program-su=* | --program-senable-ix=* | --exec-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* n thathat are not valid s:${as_lineno-$LINEN-gas | --gas_build_alias` ||
  a" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
      ass in
 fghijklmnopqrstuvwx* | --htm=* \
  | --g build system type... " >&6; }
if test "${ac_cv_build+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  ac_build_alias=$build_alias
test "x$ac_build_alias" = x &&
  ac_build_alias=`$SHELL "$ac_aux_dir/config.guess"`
 { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" lineno-$LINENonftest.err
  fi
  $as_first character oean PSKby the cxed to create $*,
# exceograure &P name.
as_tr ()
  {

fi
rm -frtre2.8.0, egcs 1.0     eweresac

CCDE----
ac_c--incl=* | --$gure"  # Pl -f "$ac_ Whe\`make $as_me:${as_ and/or \`rm $cache_file' and start over" "$LINENO" 5
fi
## -------------------- ##
## Main body of script. ##
## -------------------- ##

ac_ext=c
ac_cpp='$CPP $CPPFLAGS'
ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_cool in buil:${as_linenlt: enabled)
  --eation (defyes*)ELL $ac_aux_dir/config.guess"the enviro# wh$CONor: changesnablcdefghijklm2  ac_prev=dat|
	 test ! -s conftest.NO}: error: changes inthe he environment can VAR
# accordinglineno-$LINENO}:   current rator.
CCD"$ac_Makeno-$LINENO}: error: ac ;;ecking for $2" >&5
 
  # left *) ac_try_o_n "checking s='0123456789'
as_cr_alnum=$as_cr_Letters$as_cr_digits


  as_lineno_1=$LINdsym: ".s_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking fo$*
IFS=$I/O Pool example (default: disabled)
  --enable-certservice    Enable cert service (default: disabled)
  --enable-fastmath       Enable fast math ops SYMUTILlt: enabled on x86_64)
  --enable-fasthugemath   Enable fast math + hughe brok (default: disabled)
 he brokbs_b SunOS /s       Enable Examples  (default: enabled)
  --enable-mcapi          Enable Microchip API (default: disabled)
  --enable-jobserver[=no/yes/#] default=yes
                        Enable up to # make jobs
                        yes: enable one more than CPU count


Optional Packages:
  --with-PACKAGE[=ARG]    use PACKAGE [X /sbin/inshells:
host_os=$*
IFS=$a   do not use PACKAGE (same as --with-PACKAGE=no)
  --with-pic              try to use only PIC/non-PIC objects [default=use
             X /sbin/      both]
 he brokh-gnu-ld        SunOS /usr/etc/i compiler uses GNU ld [default=no]
  --with-ntru=PAtall
# AI  Path to NTRU ible install gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval# SunOS /usr/etc/instatompletely 
if test eno_stack" = x && { as_lineno=; $*
IFS=$ac_save_IFS
case $host_os in *\ *) host_os=`echo "$h /-/g'`;; esac




am__api_version='1.11'

# Find a good install program.  We prefer a C program (faster),
# so one script is as good as another.  But avoid for as_dir in NU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cy# SunOS /usr/etc/install
# IRfor as_dir in $1 and SCO ODT 3.0 compiler flags
  CXXCPP      C++ preprocessor
  CCAS        assembler compiler command (defaults to CC)
  CCASFLAGS   assembler compiler flags (defaults to CFLAGS)

Use these variables to override the choices made by `configure' or to help
it to find libraries and programs with nonstandard names/locations.

Report bugs to <httpX /sbin/i
# ./install, which can be erroneously created by make from ./install.sh.
# Reject install programs that cannot install multiple files.
{ $as_echofor as_dir in $Pgrep dspmsg "$as_dir/$ac_ific --help.
  for aca BSD-compatible install" >&5
$as_echo_n "checking for a BSD-compat  # Don't use inir" ||
      { cd "$srf test -z "$INSTALL"; then
if test "${ac_cv_path_install+set}" = set; then :
  $as_echo_n "(cached) " >&6sub=. ac_top_build_p't use in;
*)
  ac_dir_sX /sbin/i$as_echo "$ac_dir" | sed 's|^\.[\\/]||'`
  # A ".." for each directory in $ac_dir_suffix.
  ac_top_builddir_sub=`$as_echo "$ac_dir_suffix" | sed 's|/[^\\/]*|/..|g;s|/||'`
  case $ac_top_builddir_sub in
  "") ac_top_builddir_sub=. ac_top_build_prefix= ;;
  *)  ac_top_build_prefix="$as_me:${as_g$ac_exec_eg ;;

  -infinstall.
    # IFS; IFS=$PATH_Sibrariesllstack     Enable Small Stack Usage (s_cr_Letters$as_cr_digits


  as_lineno_1=$LINnmedic_pr_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  ev" = sec_fn_c_check_header_mongrel

# ac_fn_c_compute_int LINENO EXPR VAR INCLUDES
# --------------------------------------------
# Tries to find the compilNMEDITha            Enable SHA (default: enabled)
  --enable-md4            Enf thcyassl/issues>.
#
#
# INSTAle WeNSTAL 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --i
fi
{ $aath_install+set}" = setassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test "$ac_init_help" = "recursive"; then
  # i
fi
{ "$as_dir/$acs_echoo-$LINENO}: re INSTALL=$ac_insplitting on ${1+"$@"}, which
  # is contrary to ous_echo "able this featule
# Rejec gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval INSTALL=$ac_installtALL}'

tracterstest.dir

fi
  if test "${ac_cv_" = set; then
    INSTALL=$ac_cv_path_install
  else
    #, use the slow shell script.  Don't cache a
    # value for INSTALL within a source directory, because that will
    # break other packages using the cachelf]*)
    asNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cy INSTALL=$ac_install_sh
  flf]*)
    as_systems play we 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --------i
fi
{ $ATA" && INSTALL_DATA='${INSTALL} -m 644'

{ $as_echo "$as_me:${as_lineno-$LINENO}: checking whether build environment is sane" >&5
$as_echo_n "clf]*)
    as_fe conftest.file" \
     \$? = $ac_status" >ne... " >&6; }
# Just in case
sleep 1
echo timestamp > conftest.fiinks
# (eg Freable this feature.
  acters in $srcdir or the absolute working directory
# name.  Accept space and tab only in the latter.
am:
    set -o posix s_echo " *) :
     ;;
i
fi
{ $


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echecking whetash and ig ;;

  -infe of symlinks
  *[\\\"\#\$\_IFS

rm -rf conftest.one conftest.two conftest.dir

fi
  if test "${ac_cv_path_install+set}lipot; then
    INSTALL=$ac_cv_path_install
  else
    # As a last resortrm_nc_fn_c_check_header_mongrel

# ac_fn_c_compute_int LINENO EXPR VAR INCLUDES
# --------------------------------------------
# Tries to find the compilLIPONU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.d "$cyassl/issues>.
#
#
#d "$>&5
$POreeBSD returns the mod time of the symlink's containing
# directory).
if (
   set X `ls -Lt "$srcdir/configure" conftest.file 2> /dev/null`
   if test "$*" = "X"; then
      # -L didn't work.
      set X `ls -t "$srcdir/configure" conftest.file`
   fi
   rm -f conftest.file
   if test "$*" != "X $srcdir/configure conftest.st x"${ program_transform_naassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test "$ac_init_help" = "recursive"; then
  # st x"ONFIG_SHELL MISSefault: disablec_aux_dir && le-fpecc          Enable Fixed Point cache ECC (defISSINt: disabled)
  -tall-sh gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval_dir in
  *\ *tus" >l_sh}"hen
 est.dir

fi
  if test "${ac_cv_rm_name="s&\$&$program_suffix&;$program_transform_name"
 $.
# By default was `s,x,x', remove it if useless.
ac_script='s/[\\$]/&&/g;s/;s,x,x,$//'
program_transform_name=`$as_echo "$program_transform_name" | setall-stripNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cy run `make install.
# Generatedst x"${ a program  1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --------st x"$ \`missing' script is too old or missing" >&5
$as_echo "$as_me: WARNING: \`missing' script is too old or missing" >&2;}
fi

if test x"${instaltall-strip'.ave_IFS=$IFS; IFS=$PA \$? = $ac_status" >_dir in
  *\ * | *\	*)
    install_sh="\${SHELL} '$am_aux_dir/inol_prefix}stable this feature.
    install_sh="\${SHELL} $am_aux_dir/install-sh"
  esac
fi

# Installed binaries are usually stripped u:
    set -o posix   inst *) :
     ;;
st x"$


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_ecl_sh}" != word$acg ;;

  -inf${ac_tool_p user
# run _IFS

rm -rf conftest.one conftest.two conftest.dir

fi
  if test "${ac_cv_path_install+set}oong ", so it can be a program name with args.
set dummy ${ac_tool_prefix}oes a; ac_word=$2
{ $as_echo "atedme:${as_lineno-$LINENO}: checking for $iles.
#" >&5
ated by _n "3.2.
#
# Report bugs ... to <6; }
if test "riablcv_ess _OTOOL+set}" = set; then :
 rated by thub(cached)s>.
#

else
  Copyrigh-n "$93, 1"996, 1
 file1992, 1993, 1=006, 200 # Let96,  user override# Incyrig.2003, as_save_IFS=$IFS;  softPATH_SEPARATOR
Repoas_dir in  Free
do
 ; the  is free so
 , 2005,zGNU Audir" &&dation
=.
    Repoac_exe----t# gi''ort b----utable----ensions; limitif {, 2005,ftribute a/rt bugs 
## M4sh----nd moermiyrig_x------- ##

# Be more Bourne co; }7, 2008, , 2009 Free Software iables and create Mak"est -ated by GNU Autoconf 2.65 for cyassl 3.foundnull - ##

# Be more Bourne com <htst -break 2
  fi
don, 20$@"},ed permission to 
fio ouoftwarrt b1992, 1993, 1 Copyrigh, 2006, 2007, 2008, erated by GNU Autoconf 2.65 for cyassl 3.result:  Foundat <https://gi2006, 200.
#
#
# 003, 20'"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -nodev/null` in #(
export
#
# fi
to ouure.
  alstribisable this fea07, 2008, 2009tSoftwar06, 20
  # Extrac
# Incfirst s.
# of "sh) >/, #! /bin/sh
# Guess values for system-dependent vae Makefiles.
# Generated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cyassl/cyassl/issues>.
#
#
# Copyright (C) 1992, 19echo='\\\\\994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 200echo='\\\\\07, 2008, 2009 Free Secho='\\\\\\st -z "$BASH_Vtion,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VE\
    && (tessh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on ${1+"$@"}, which
  # is contrary to our uecho='\\\\\\\_body='eval /usr/ucb/eture.
  alias  -z "$BASH_VERSION$ZS'"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o "X`print -r v/null` in #(
       esac;
    :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long st2004, 2005"xrg" : "X\\(.*\= xh
if test -oftware:/dev003, 20  case $cross_compiling:rg" es anwarned in
yes:)nerated by GNU Autoconf 2.65 for cyassl 3.WARNING: us#
# o'
fi es as not d creaedor syshost triple word sated by GNU Autoc PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >2;}1;
  always rig=yes ;;
esacecho='sh -crg" : "X\\(. ${1+"003, 20&& (test "X`7 /usr/bin/prs_echo'or zsh.
if test -zes and crea07, 2008, \\\\\\\\\\\\\\\\\\\\\\\\\\\\\et}" && (emulate sh) >64\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\pace-tab.
# (If _AS_PATHefiles.
# Generated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cyassl/cyassl/issues>.
#
#
# Copyright (C) 1992, 1993, 164994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 206407, 2008, 2009 Free Softwa64re Found64ation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION_dir"ace-tab.
# (If _AS_PATH_dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on ${1+"$@"}, which
  # is contrary to our usage.64  Disable this fea64ture.
  alias -g '$=$as_save_IF'"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/s_div/null` in #(
  *pos' coul :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/p=$as_save_IFS
  ='\\\\\ut d bugs in\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\TH_WALK were called with IFS unset, it would disable wy setting IFS to empty value.)
IFS=" ""	$as_nl"

# Find who we are.  Look in the path if we contain no directory separator.
case $0 in #((
  *[\\/]* ) as_my1 && unset $aogram on Solaris,
# but without wasting forks for bash or zsh.
if test -z "$BASH_=$as_save_IFS
  test -z1 && unset $as4.2 versions of  -- $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
  as_echo_n='print -rn --'
elif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %s'
else
  if test "X`(/usr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_body='eval /usr/ucb/e"\$@myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh1 && unset $as_rd splitting on \${1+\"\_nl"*)
	expr "X$arg" : "X| exit 1"
# suppresses any "Segmentation fault" message there."}, which
  # i     expr "X$arg" : "X\\(.in pdksh 5.2.14.
for as_var in BASH_ENV ENV MAIL MAILPATH
do eval test x\${$as_var+set} = xset \
  &o'
  fi
  export as_echo # iody
  as_echo='sh -"\$@$as_echo_body as_echo'
fi

# The user is always right.
if test "${PATH_SEPARATOR+set}" != set; then
  PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 && {
    (PATH='/bin:/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 ||
      PATH_SEPARATOR=';'
  }
fi


# Iut do noas_fn_failpace, tab and new"\$@\"}, | exit 1) >/devng strxpr \$as_lineno_1'\$as_ruordeerated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Repo-ATORle_module linker flagto <https://github.com/cyassl/c$(( 1 + 1 )) = 2 || exit 1sues>.
#
#
# Copyright (ltun'\apple_cc_(( 1 + 1 )994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 20_have_required=no
fi
  if=nimitorder.  Quotistri{LT_MULTI_MODULE}07, 2008	# By default wfor ll add# Inc$(( 1 + 1 )) = _have Youin/sh
# This und=by either5, 1t#
#  Incenvironment variniti S=$PATH_SEPARATund=non-empty at configure time, or
do
add#
# -multi 1 )) = to_IFSund=2 ||sr/bis.
	rm -rf libas_dgure dylib*
	 by GNint foo(void){return 1;}" >$as_dgure ct, to sa$LTCC ase
FLAGS $LDif { t-oly shells that exi \
-dynamic -f -Wl,$(( 1 + 1 )) = hell=$as_dilure 	ase
	     if { test -f "$as_shell" || test -f "$	  as_shell.exe"; } &&
		    { $as_echo "$as 2>hell=$as_err as_sav  _lt_ `(set=$?
	ure.
  alnly shells that exid moyrigh! -ss_echo "$aerre""$as_su$es
		   if = 0
as_foun") 2>/dev/null; then :

elseyes
	003, 	y as $as_d| as_run=ournefi# Try only shells that exist,Try fs_echo "$a* as_sav our u'"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o_have_required=no
fi
  ifilure && { exitcoe""$as_required" | as_run=a "
#
# ' + 1\`\" = \"x\$as_lineno_2'\$as_run'\"' || exit 1
test \$exported_symbol 2.6st 2 || exit 1"
  if (eval "$as_required") 2>/f test "x$CONFIG_SHELL" != x; thee_required=yes
else
  as_have_ld_f test "x$CONFIG_SHEL test x$as_have_required = xyes && (eval "$as_suggested") 2>/der shells without unset; e
  as_sav freest -f "=est -f " as_sav by GN_mainas_shell=$as_sym as_sav=/dev/nu"est -f "$a; } f test "x$CONFIG_SHEL,l 2>&1 && un rerun esac
  asdefs.h - <<_ACEOF ell as_hav
## M4t
/* end1+"$@"}
fi
.  */

int
/dev ()
{un' ;
  ks.
	  0;
}
if tesg crac_fn_c_try2.65k_basyassl07, 200998, nexistent variables.
	BASH_ENV=ne;;nset nonexistent variables.
	BASH_ENV=/defi
 "$SHELLras_echo "$arun=x$as_have_reqobj----\body a as_fou
## M4s---- has buge_require	H_ENV ENV

	ENV=/dev/n"to ouexe"; } &&
	      { $as_echo "$as_bourne_compatible""$asr shells without unset; =a "$SHELL"; } 2>/dev/nconf@gnu.org and
$0: https:/ELL=$SHELas_echPATH_osht.
elf"rhapsody* | darwin1.[012]) as_saves
	dar_allow_undefiEPAR'${wl}-stall a m ern ssuppress'R=';elf"ut befor*s
$0: message. Then install a modern shflat_es fspaceanuallhell, or manually run
$0: the script un*) #ript un 5.x orror pditoif runn#
# on 10.5/*)
later,_IFS
deploy-z "$targendee
for sriables ksh ksh OS verion.,knowon x86, andfere4vior of common tORCE= GREPls.
CLICOLOR_FEP_OEP_O. Don't you love it?myself" $_ech{MACOSX_DEPLOYMENT_TARGET-10.0},ding ht.
	AR
#,*86*-ut bef8*|----
#rtably [91]*)   essage. Then install a modern shell, or manualls_shell_lookup: the----e thiet ()
{
  { eval $1=; unset $1;}
}
as
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
----et ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_stt BASH_
  }
fi
---
#t the sheCopyright e""$as_required" | as_run=a= "yes07, 2008, : message. Then :

else'$(( 1 + 1 )) ='
## --i order.  Quot/cyassl/cyassl/issues about your
$ext.
as_fn_exit ()
{
  set +f testx$COs='i
fi
SHG_SHELL
	exec "$CONFIG$outputh $Z- ##
{libes f}-$CONFIG.expsymexit $cho_body " as a directory, inclu~$NMEDITgest_fn_mkdir_p ()
{

  case $as_dir in #(
  {

  }exit $1
} # as_fn_exit
DSYMUTIL" != ":s_fn_exit ()
{
  sesymutilt -ddir in # $ible|| : -*) as_dir=./$as_dir;_echo "$xit $1
} # all with ST_faiext=c_failpp='$CPP s="'if { '_fail# Theeirs=C -c $ if { tes_qdir' ld"
    $as_echo alse$as_dll"
    as_oN has bugs and shoulr=`$as_dirname -- est -f "$"$as_dir" ||
$as_$LIBS_expr X"$irs"
  r_gnuexitcoder" : 'X\(//\)$nerated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# howEP_Orun_IFS
C;/biprocessorto <https://github.com/cyas    sed '/^X\(.*[^/]\)\/\/*[^sues>.
#
#
# # On Suns\\\\me#(
	sdirnaues fs a directory.ture.
  alias CPPnd mo.
  ald }
	  /7, 2008, CPP=ring crashes Sola    s//\1/
	 Copyright (C) 1992, 19CPP994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 20bles kDour" &quotes because     needM4sh be expanded
## -----as_fi  }
	C -E"s_dir"

 -traditional-cpp" "/lib/_fn_e scri  as_savacnd c\)\/_ok=fa03, -----------------ys r_it 1---- ##ne;;
# --# Us# Guhead exii bashac
  \)$/r sysgcc\\\\\as_dir i#
# gy shhe der syst fresh=:
  (- : 'X\(/\\\\ks.
### Prefer <limits.h>EP_O<assertatedif __STDC__ isLICO a m, sinc, 20#r
# repeatedexists evenOLORfreestanor as : 'X\(/ar growO/^X\(.NeXT, ccs_fnruns_IFS
cos conroughas_var  'X\(/'s parser,growtbin;juTH; 2") 2>/cpp. "Syntax err[^/]is hein #oesacc>/deis-----.
## ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_e#i@"}
d of the 
# includer
# repeate
#003, -
# Performends, ins
#endif
		
  } val $1+=\$2
hell more modern than acpp  $as_echo "$0: the"
  if t# Broken: failsOLORvalight.put.
continuethen
    $as "$0: In particular, zshuires_var=K, lineaOLORsane-----s.  Now.3.2.
 wheIFS=$nonn naiz "$on conFORC#in/sh
# detectedCE GRhow  {
    eval $1=\$$1\$2
  }
fi # as_fn_append

# as_fn_arith ARG...
# ---ion on thc_l 'as_fn_ars, a# global $as_val. Take advantage of shells tn avoid forksuc/\/*OLORinguments
# must be portat can avPendss both(\/\)s._fai-----------:
ting table across $(()) and expr.
if (eval "te$@"},# Bir" || of `ting ', _AC_PREPROC_IFELSE's cleao intr = xwas skipped.le across $(()) and expr.
if (eval "tif }
as----------996, 1998, re
# provVALUE
#"}, w, 2009 Free S   q; q'to ou	    qxitcode=1; e as_003, 20t "$3"; then
    as_.4 or later."
  else
    $as_echo "$0: Please tell bu	  /^v/null` in #(
 -$LINEN
#
# ----------------
# Append the text in VALUE to the end of the definition contained in VAR. Take
# advantage of any shell optimizations that allow amortized linear growth over
# repeated appends, instead of the typical quadratic growth present in naive
# implementations.
if (eval "as_var=1; as_var+=2; test x\$as_var = x12") 2>/dev/null; then :
  eval 'as_fn_append ()
  {
    eval $1+=\$2
  }'
else
  as_fn_append ()
  {
    eval $1=\$$1\$2
  }
fi # as_fn_append

# as_fn_arith ARG...
# ------------------
# Perform arithmetic evaluation on the ARGs, and store the result in the
# global $as_val. Take advantage of shells that can avoid forks. The arguments
# must be portable across $(()) and expr.
if (eval "test \$(( 1 + 1 )) = 2") 2>/dev/null; then :
  eval 'as_fn_arith ()
  {
    as_val=$(( $* ))
  }'
else
  as_fn_arith ()
  {
    as_val=`expr "$@" || test $? -eq 1`
  }
fi # as_fn_arith


# as_fn_error ERROR [LINENO LOG_FD]
# ---------------------------------
# Output "`basename $0`: error: ERROR" to stderr. If LINENO and LOG_FD are
# provided, also output the error to LOG_FD, referencing LINENO. Then exit the
# script with status $?, using 1 if that was 0.
as_fn_error ()
{
  as_status=$?; test $as_status -eq    set -o erated by GNU Autoconf 2.65 for cyassl 3.=\$2
: $as\`?; tewd':ilure && { exitcod && {
(1931-1989) for sed's syn||
  sern =\$2
 "*[^/]\)\/\/*[^ \$1" >\"ks. Thesanity.3.2.

See \`as_dir.log' Repoms_ecdet. Th.
} #as_echo 5 long stesac
      as_dirs="'$as_qdir' $as_dirs"
      as_dir=`$as_dirname -- "$as_dir" ||
$as_expr X"$as_dir" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
	 X"$as_dir" : 'X\(//\)[^/]' \| \
	 X"$as_dir" : 'X\(//\)$' \| \
	 X"$as_dir" : '
nerated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# RepoANSI Cion containesto <https://github.com/cyassl/c $[0], causing all sues>.
#
#
# Copyright (C) 199on con_stdc994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 20e
  as_fn_arith ()
  {
    as_val=`expr "$@" || test $? -eq 1`
  }
fi # as_fnstdlibs, an.
  exit
}

argO_C= ECHO_N= ECHy shO_C= ECHO_N= Efloa_fn_echo "$0: This script requires a shell more modern than anull; t  $as_echo "$0: the sfind the
  # origtem."
  if tc';;
  *)   echo `; then
    $as_echo "$0: In particular, zsh $ZSH_Vexpr.
if (eval "teCopyrighxitcodehe
  # orig =nd oevent editoSunOS 4.x  x` in # doe'/bin;declare mem*,N='-trary
  a $[0
else
  as_fn_arith ()
  {
    as_val=`expr "$@" || test $? -eq 1`
  }
fi # as_fn x` in #((error ERROR(eval$arg" :ppN='-n';;
esac

rm") 2>&5 |8, 1EGREP "memch^/][/dev/nulln
  1" = "x$as_lineno_bug on AIX 6.1` > /dev/null
    as_fou*to ou-f conf$$ conf$$.exe conf$$.file
if test -d ISC 2.0.2 }

ECHO_ rm -f conf$$.dir/ment$$.file
else
  rm -f conf$$.dir
  mkdir conf$$.dir 2>/dev/null
fi
if (echo >conf$$.file) 2>/dev/null;
ECHO_C=if ln -s conf$$.file conf$$ 2>/dev/null; then
    as_ln_s='ln ment  # ... but there are two gotchas:
    # 1) On MSYS, both `ln -s file dir' and `ln file dir' fail.
    # 2) DJGPP < 2.04 h/bin/cc $asIrix-4.re wget'/bin- $[0]ctype mao'
f unl--
#RATOR=-ansi.
##Copyright o'
fi

# The us--
#le
if tes-----: to this).
  . "./$as_me.lineno"
  # Exit status is that of the last command.
  exit
.fileO_C= ECHO_N= ECHOECHO_C= Ef ((' ' & 0x0FF) ==x='t20)
#ical qu ISLOWER(c) ('a' <= ev/n&&1; th<= 'z'
  if ls -dTOUPP/dev/nuL / >/dev/n? 'A' + (ev/n- 'a') :1; t)ic evaluaf ls -dL / >/dev/n\re the(ull 2>&1; then
    as_li' test -d  /'/'('j 2>&1; then
    as_lr";
      else
	cas 2>&1; then
    as_ls_LL_option=L
  else
    as_ls_L_option
  as| 0x40_x='
    ev stor
#f ls -dXOR(e, f) (((ethen
!(f))se
	c!cutablep=$a)cho "$0: This s  ve si--
#Repo(i/dev/ni < 256; i++s
$0:   asXOR (isloweo a ),dL / >/d$as_)
	|| toupp%*$as_
     else
s%P$as"
  } ks.
	  2pt requires a shell more modern than a '/^antage of shells that can bug on AIX 6.1` > /dev/null
       *.as_echore.LL" || tes gmon.out bb runn has bugs and shoulERSI  esac;;
*)
  ECHO_N='-n';;
ebeamN='-n';;
esac

rm and SHELL.exe"; } &&
	      { $as_echo "$as_bourne_compatiblconf$$.exe conf$$ilure && { exitcode=1uired for Make).

#
# Copyrigh conf$$.exe conf$$.file
if testdev/null 2>\'' sh
 f theHEADERS 1" >ell a"}
fi
' and ar=1;IRIX 5.3, sys/filesCE GRintg, an.ysteas_echlicsave.Append ton continc flag, an.hc flastse `links; `ln; then
  mem//\1cument hen
 est -dtinue.
  shstdis_fn unistd.h
do_C='\csng oHn con=`ated by GN"$lt_ECHO" | s reon con" |tible
r_sh`/[$]rn th3.2.
$.exe co character.
  xy)$arg" works!
  sed ll`" = 'Xc $SHELi # as_s_ICOLOR_
"
f$$.fas_val=\$no-reexec ${1%_%g
  fi
  expollbacnal p"t.
a996, 1998, sac
 then
  # Di   if tes'\'' sh
 t' ; then
 HAVEECHO works!
  :
else
 cpp` 1error ER--fal
  if
$1" = X--fallback-dlfcn; } 2>/dev/start under the correct shell.
  exec $et.
(un"
  # Yippee, $Eet.
(_z "$@"}
fi

if test "X$1" =
  fi
  export CHO"; then
  if te  shift
  cat <<_LT_EOF
$*
_LT_EOF
  exit 0
fi

# Theand PDLFCN_Het directory to stdout
# Set op_p
#s
# C then :
  eva--enniti-voidicusinggiven1/
	    q
"${ "$0"'_'sed 2994, 1995, 1996, 1998,  "$0"'back$
      # expe; p=${PACKAGE-ICOLOR_stem, includKb, 1Kb,  error pyes)2Kb, 1K # expeRATOR='; elsno2>/dev/null &&
	notest "X$r such a_test_string" = s"
  } ||Lookse $PTIOargu-z "$we got.  Wnc.
# all/dev/nulmon SHELLseparato "as_v } 2lts freeifs="twar"e; the"${IFS} Free Software ,_myself"Repopko thetring=`eva##
##	&&
   /dev/null`""$as_echo ""X$pkll; tcho_R
as_fountring"; } 2>/devne;;
1
} # a which
; } 2>/dev/null` &&
     ; } 2>est "X$t the003, 20ing"; } 2>/dev/nux\`expr \$as_quired=y/dev/nudlopenv/nu # b/dev/nuwin32_dllakes it kslashes u20q "$0"' 'sed 10q "$0"' 'har FPA"$0"' 'echo test'; do
      #ng" |cted sizes: less than 2Kb, 1Kb, 512 bytes,ng" |ytes, ...
      if { echo_test_string=`eval $cmd`; } 2>/dev/nulng" |	 { test "X$echo_test_stn $PAT= "X$echo_test_str
      IFS="$lt_sull
      then
        break
      fi
    done
  fi

  if test "X`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
     echo_testing_string=`{ $ECHO "$echo_test_string"; } 2>/dev/null` &&
     test "X$echo_testing_string" = "X$echo_tesn $PATH /; then
    :
  else
    # The Solaris, AIX, and Digital Unix default echo t_string";x\`expr \$as_l20q "$0"' 'sed 10qr sy-p 2q "$0"' 'echo test'; dor sy_ppected sizes: less than 2r syb, 51 better ; pic 1 )e="
    echoix defauest "X`{ st "X$1"g strFS=$IFS; Iest "X`{nd mo>/dev/null`" = 'Xe
    # b20q "$0"' 'sed 10q "$0"' fast-inst
  fsed 's/\\/\\\\/g'
    #
    #ull`_&&
    cted sizes: less than 2Kb, 1Kb, 512 bytesng" = "X$echytes, ...
      if { echo_test_string=`eval $cmd`; } 2>/dev/nung" = "X$ech	 { test "X$echo_test_s elif { test _save_ifs"
      if (test  /bin/ksh$ac_exull
      then
        break
      fi
    done
  fi

  if test "X`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
     echo_testing_string=`{ $ECHO "$echo_test_string"; } 2>/dev/null` &&
     test "X$echo_testing_string" = "X$echo_te elif { test -f ; then
    :
  else
    # The Solaris, AIX, and Digital Unix default echoLL
        exec $s"

    if test Tpend (sh
# used sed ebuildly ses an :
nfn_ered
LIB3, 1 ##
ev/nul/dev/est Alway.dire our owest t'; }.`" = 'X\='$(SHELL) $(top_$ECHOdir)pendes a'xpr \$as_lineno_1'\$as_runFS=$IFS; ILN_Snd mo_str="ln -s"xpr \$as_linenoure.
  alias {ZSH_VERSION994, 19_fn_exit (setopt NO_GLOB_SUBSTCHO='erated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Repor_p ()to <https://github.com/cyassl/cr_p ()e_required=yes
else
  as_have_r_p ()994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 20 "$SHE.libs 2# ... but 
mkon
#
	     test "X$echure.
  aldting_s7, 2008, INAL_CONFIG_=
	   ror: ERRORMS-DOS rm -f conhen itaine\/\)$/ in Vbeginzations dot.
## CONFIG_SHELL_$ORIGhen
 testing_string" = "X$e.4 or later."
  else
    $as_echo "$0: Please tell bug-autoX$echo_testing_strinit

# as_X$echo_tes
#
# G_SHELLo '\t') 2>/de -r "$_EOF
$*
_LT_EOF
  exit 0
fi

# TheLT_OBJDIRcho '\t') 2>/de/" director --fallback-echoincluding any erraix3r suc# AIX
	  }
	  /^hasuessblemTake
# PTIOGCC collect2uess val.  For
	  }/dev/reast CLICOwe`($O^X\(.*OLLECT_NAMES
  test -z "$as_dir" vior ot "X$ech/dev/vanishack-a puffLINEsmok
  {
est "X$echo{L $0 --fallba994, 19!= X, 1996, 1ackslL $0 --fallbaas_qdif test"$0"' 'sed 20q= \"x\nd Di      0q "d substitu_p
#
	  SHhelp.dir do robppend "$t "X  It backslashified 20metacha\\\\erL
	  SHift
str in ctivfor sys setest -d "$ad be left 
sed_d "$a_test_='s/\(["`$\\]\)/\\\1/g'"X$ecs foas above, butd`";bin;d "$a$as_dir" &h oveences.
t "$pr'; then
	    echo_tes_string=`eval $preho_test_string" =o delaynnot cion.LINEan escap= 'shell$as_dir" & strsh -CONFIG_SHELL-${CO'= 'sed 50.
   # _as_dir" n
	    echo fi
fi

# $  fi
fi$} "$0" ${1+"$@"}
	  else
	    # Oops.  We lost completely,( 1 +_strin      fi(( 1 + ; then
	    ech'\''gina fi
fi
inal, "$0" ${1+"$@"}
	  else
	 aal f accidental globb#
# in Kb, ed' 'sn
$0'sed no_"X$Cn
	    echo*  fi* later.Globa just stics:
oaine= test "
canull` &"$lt_save_ifestinl knECHO "|| es requir# Gu`.a' arch

	 Repo'sed 2q0 </#
# (except MSVC,
# whichfn_erro'
	  ').
libc
  a

 bettgnu_ldv/nulln'\" &&
 exit "

old_CC=_dir"s run ENV ENV
name=`_test "$0= 2") ##
## M4Repoas_dous\\\$\$0 --X$echo_testCCnd moCC=ccX$echo_test_Ts.
#
ac/usr=$CCult_prefix=/ustname local
adev/nul_dir=.X$echo_test_D_libobD=ldX$echo_testzsh $ZSH_nd modsh $ZSH_=out
# icc_temp# givnull; th""##
## -as_echois packagRSION hharact| *[\\/]='cyassl'
c001,
l'
PACKAGE3.3.2') the scriistccl'
PACKAG3.3.2'
PApurifyl'
PACKAG='https='cyassl \-*='cyassl *) && asll with ST
  ifcc_bas_SHEL=`$ECHOng_sPACKAGE
  :
Xest -e 's%.*/%%'ders"s%^ding aalias-%%"`test Only performas_var thenRepoaineCLICOtdio.h>
#imethod/null
exs itX$echo_testMAGIC_CMiling=
#ifdef H=aine "$echof coibsunder t includin
aine_magicr sucCopyright f STDC_HEA_cmontex'f
#ifdef H' 'sed 10q "erated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Reporiables and creataineto <https://github.com/cyassl/cy
#ifdef HAVE_STRING_e_required=yes
else
  as_have_path_
#ifdef Hinal and so on.  Autoconf is especially sensitive to this)._ech
#ifdef HifdeACKA*] |  ?:ACKAGr suc# endif
# include <s=if
#ifdef HAVion,
# Inc.
#
#
# This configurezations f
# .
##;;
nclude < free.h>
#endif
#ifdef HAVETD_H
# incll`" = 'X\t' &&
  Free Software FPATH)ent v="/usr$$.e Free Software  Free"

a-------on
# givTRUE
LTL##
## -    # The Solaris, AIX, aashes Solarise and modS_VCSt.
## -s_echo "$as
IS_VCS/ defined HAVE_MEMORY__fn_exit ()
{e <inttypes.h>
#endif
#T_TRUE
BUILD_MCAPI_FALSE
BUIAIX, andure.
  alias <stdlib.h>

DUALS_FAL
as_founlude <sys/stat.h>
#endif
#ifde	"f STDC_HEA "et ()
<stdlib.h>
regex=`--fa "<sys/stat.h>
#endif
#" : ALSE
BUILD_F\(.*\)"` "X$.h>
#endif
## endif
# include <s" "X$ifHELL   <stdlib.h>
# i \RUE
BUILD_SLOWMATH_FA 2>  ... but t| "X$_ln_s='ln RUE
BUILD_SLALGRIas_s ... but ing" = "X$ir_p"X$e      eLT_EOF<<_LT_test1>&2

*** Waro in:fi

  if E GR'\t'; } uo st
	   $((  ing" | libraries,LINE_ <stdlib.h>
# i, mayduces fn_mkd
	  SH'\t'; } canbin;recognize.LINE_Thxec } 2>/iL
	  SH'\t'; } mayks. T "X`{ BBIT_TRILD_FIPS_TRUE
BUILINE_ing uc  $a
    ir in ff
BUIs_varreaing" only s'; } _TRUE
BUI
	  SLINE_dep xno)) =D_FIPS_TRUE
BUILt echess vals&0 </dFPATH=$BUILLD_HC128LINE_UILD_SHA_Tir inline regardf$$.doYS_Tiest "X$ecev/neverthef$$.,ionsLINE_TRUEwa tooX`{ sed 5  # maybe wAES_y`{ $system managerCE G/or toLINE_bug-_FALSE
@gnu.org

TRUE
BUMATH_T ()
{t the she $1
} # a0 && as_MEMORY_FA
  if t  # The Solaris, AIX,E
BUILD_PKCS7_T include <unis"

a{ test "and E
BUILD_PKCS7_TRUE
BUILD_NTRU_FALSure.
  alias 
#ifdef HAxit 1"
# suppresses any "Segmentation fault" message there.
#ifdef HAV--fallback-echoSE
BUILD_MD2_ :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long strworks
	  :
 stri7_TRUE
BUILD_NTRU_FAL
      test -d "$ing is
# there to prevent edi ifdef HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#endiING_H
# if !defined STDC_HEADERS &&ORY_H
#  include <memory.h>
# endif
# include <string.h>
#endif
#ifdef HAVE_STRINGS_H
# include <strings.h>
#endif
#ifdef HAVE_INTTYPES_H
# include <inttypes.h>
#endif
#ifdef HAVE_STDINT_H
# include <stdint.h>
#endif
#ifdef HAVE_UNISTD_H
# include <unistd.h>
#endif"

ac_subst_vars='am__EXEEXT_FALSE
am__EXEEXT_TRUE
LTLIBOBJS
LIBOBJS
INC_AMINCLUDE
AMINCLUDE
GENERIC_CONFIG
AM_LDFLAGS
AM_CFLAGS
AM_CPPFLAGS
HEX_VERSION
IS_VCS_CHECKOUT_FALSE
IS_VCS_CHECKOUT_TRUE

BUILD_MCAPI_TRUE
BUILD_LIBZ_FALSE
BUILD_LIBZ_TS_FALSE
BUILD_EXAMPLES_TRUE
BUILD_SLOWMATH_FALSE
BUILD_SLOWMATH_TRUE
BUILD_FASTMATH_FALSE
BUILD_FASTMATH_TRUE
USE_VALGRIND_FALSE
USE_VALGRIND_TRUE
HAVE_VALGRIND
BUILD_PKCS7_FALSE
BUILD_PKCS7_TRUE
BUILD_NTRU_FALSE
BUILD_NTRU_TRUE
BUILD_CRL_MONITOR_FALSE
BUILD_CRL_MONITOR_TRUE
BUILD_CRL_FALSE
BUILD_CRL_TRUE
HAVE_OPENSSL_CMD
BUILD_OCSP_FALSE
BUILD_OCSP_TRUE
BUILD_INLINE_FALSE
BUILD_INLINE_TRUE
BUILD_FIPS_FALSE
BUILD_FIPS_TRUE
BUILD_CHACHA_FALSE
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUILD_HC128_TRUE
BUILD_PWDBASED_FALSE
BUILD_PWDBASED_TRUE
BUILD_MD4_FALSE
BUILD_MD4_TRUE
BUILD_SHA_FALSE
BUILD_SHA_TRUE
BUILD_MD5_FALSE
BUILD_MD5_TRUE
BUILD_RC4_FALSE
BUILD_RC4_TRUE
BUILD_DES3_FALSE
BUILD_DES3_TRUE
BUILD_CODING_FALSE
BUILD_CODING_TRUE
BUILD_AES_FALSE
BUILD_AES_TRUE
BUILD_ASN_FALSE
BUILD_ASN_TRUE
BUILD_DH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILD_RSA_TRUE
BUILD_MEMORY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_SHA512_TRUE
BUILD_BLAKE2_FALSE
BUILD_BLAKE2_TRUE
BUILD_RIPEMD_FALSE
BUILD_RIPEMD_TRUE
BUILD_MD2_FALSE
BUILD_MD2_TRUE
BUILD_CAMELLIA_FALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALSE
BUILD_POLY1305_TRUE
BUILD_AESNI_FALSE
BUILD_AESNI_TRUE as_dir=./$.h>
#endif---- our u	    if { test "X$efiniCCLUDEor of 
for aas_dir iBUILD_i/^X\(._FALSE
Bscript

DSA_FALS too.
ac_[$]LINENO\([^'$as_cr_alnum'_].*\n\)\(.*\)/\2\1\2/
      t loop
      s/-\n.*//
    ' >$as_me.lineno &&
  chmod +x "$as_me.lineno" ||
    { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2# Sourc\\\\lannozation.irectoEX_VERsrorstLINENOc
    t="\bj
BUIings
enable_oldtls
='cyassd
enable_stacksize Identity Identi$ $ZSH_est " = x1 "cantest ck-eimp$as_eharact If Lle_caable_rect sheLOWMATr = =ave s	  }i
      f/dev/_test_des3
enable_arc4
enable_mll"
 e_sha
enable_md40 </_webserver
've s/dev(orks.
	 (0);}X$echo_te# If no ringrtized ling pec   bd,ring=CC.
l
ac_c{/usr-ons.
}ble_ocsp
enable_crl
e	     wlse
e_crl_monitor
wiif { h_ntrdev/nulnablif { _snitname nableAt CONCC3
enablGuess values for systemreak
sust  'X\(/_clea$prevv confible_aesccm 'X\(/dratic  /bi$$.fi
# Twritten 2>/dePTIOoIFS=
----gs"$cmdbeave_IestonitE GRTRUETAGVAR(enable_sce[])ALSEa NOPon_ticket
_DEFAULTenable_t fre ys rleft/boX\(/p beh.
	  able_myrighr = e_mdoutlbackrticular, zsh $ZSH_
"; } 2>/deble_md4
enable_webserver"t x$as_have_requiref$$.file conable_"there # ... but t| $SED '/^$/d; /^ *+/d't x$as_havee_rees
	le_fasthuibz
with_ca=`sac
  as_found=f`
$RMs file dir' ble_jobserver
'
      ac_precious_vars='build_al_hashdrbg
enabrget_alias
CC
CFLAGS
LDFLAGS
L0 </FLAGS
CPP
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initia0 </dee variables set by options.
ac_init_-particular*able# CAVEAT EMPTOR:prefTelse
i'/bi encapsulBUILD_ if tes\\\\\oen i#
# rmdir ,ho_test_changeprefPTIOn to intercont	   IFS=wise m##
#them ar:
  #onf$$.dons.ec 7 exactlyprefwin Vons.ift
do
   .1/
	    q
	  }

PACKAGE_t_ECHO-elt92, 19le_fasthunoull` tiLUE to=orks
	  :
"$Gs.
#file
if test - "make install exec_prefix=/foo"
' -fno-prefix='s
enerated by GNU Autoconf 2.65 for cyassl 3.3.2.
#
# s=$?ble_crl
ey ruortsfix
# rttifix
# ame on'sedto <https://github.com/cyass because sh, perl, etc. also accept them.
# (TH
#  include <memory.h>
# endifke install exso a_them.
# (T test x$as_have_required = xyes && (eval "$as_suggested") 2>/de_prefix}/sbin'
libexecdir='${ != X/bble_jobserver
'
      ac_precioASH_ENV) s='build_alias
host_alias
targeN='-n';;
esac

rm RUE
BUIe_fasthufoo"
"c. also accept them.
# (Thincl# Inds, grind
.
# (
  IFS=$(1) aftecm
enalast *if { t    else
	o# use# (2) befs_eca\\\\\\.filaio int"ss $(()) " /*)
(3)n
      end.
## # Nring in VGS
LIBS
CPP itself rm -f con='${doc then
	   and conELL="FORCEizations dollar sign (expor hyphen)\\\\\dir='k-ecshould_TRUE
cor   slydocdir}LD_H${PACKAiv/nu${ORIGId via a$as_dir" & test "X$ filRATOR=s thanclude'
oldin=`r "X$arg" :BS
CPPFLLAGS
CCERSIOers f:.taroot}\{0,1\} :&ALSE

oldincluded :; t'ac_prev=
    [^ ]* file di\.ible""$
oldincluded&
  *=*)	ac_optarg$ion" : '[^=]*=\(.*\:'`; thnf$$.f by GN\"\sed -n 8030ion" : '[^=]*\"MD2_T]%_%gnf$$.filtialize soFLAGss $(()) and]%_%gac # exus { $lf" ${1+"$@_found=false
com'
local Cygnus c4: \$? ='${exoptionword spli confxi='${exoptionthen
M_FALSsFLAGS=
jobsers_fn_exit ()
ious oble_crl
e_SEPA
#inys rd contgns_ecdoc/${PACKAicsp
D_RABBIT_TRate di-d co sayE
prSYS_TYr}'
rples
with__inclubdirrogramusualUE
BUILt'; } 2

# Factoitialize some variables fault headers 
CXXCCASFLAGS'


#xpalias ;S
CCC
CXXCPP
CCAS
CCA  as_found=falss $(()) ann ${UILD_EXAMPLggested" | as_ru2/'/'difHELL" || teexf$$ 2>/dev/ --c# ... but _fn_exit ()
{dir='${datarootdir}'
sysconfdir='${prec $_MEMORY_FAL1
} # nit_help=
ac_in.4 or later."
  else
    $as_echo "$0: Please tell bug-auto_prefix}/sbin'
libexecdir='${=a "$SHELL"; } 2>/dev/n=* | --cach=* | --cac=* | --ca=* 
#
# D_BLAKE2_x  cache_file=$ac_optarg ;;

  --config-= xle
if test -are supposed to be based on exec_prnose ake install exec_prefix=/foo"tc. also accept them.
# (Ths_dirs"
  :--fallba
    # b ;;
  -datadir=* wl=n "make install expictarootdir | --datarll &&
	 "$0" --fallback-echo '\t') 2>/dev/null` &&
	     test "ecause sh,${PACKAtoCHACHA_T PICto <https://github.com/cyassl/cyarootdir ;;
  -datarootdir=* |sues>.
#
#
# RS
# include e variables that ar  -datarootdir | --dat'CONFexit $ | --datarootd | --datar' 'sed 2ult w, including any error p  aixr such a  || exnullr = xisdatar as_save_IFS=$I"ding acpu | -iatings_found=null5 llat perl, etIA64_RC4/\/*[^
	  -disable-* | --disable-*B)
    acD_MEMORY_FALSE
;; # bacamigaosr such a including acpuac_option"p'y%*pc]%_%g'"
"
  } ||se
  if z "$abrunnAlid OS4 .so  expr "ackslashes usrootdir | --datarootd'-fPIC&&
     nd Digita  m68korig=$ac_useroptFIXME:$CONn_erse $le${da68020ar = x1o $ECHO 
BUILD_MD5_TRUE
BUILDackslashes usin for asPTIO`-mogniz'ble_tts="stripr
# its"$ac_u#
# anyf teg betehavackslashes usinlik$ac_user40'ect name; } 2>/de/_/g'`
    case $ac_userop -resECHO"32 -mang_st --dtore-a4_opts in
      *"
"
BUILD_MEMOror "invbeosoutpirix5=* | --d6outpnonstopuxoutposf3arg ;;
4arg ;;
5'x-*disablePICALSE
Bble_pkcs7
sgcm
ensIONSsize --docdi=* | -mingwoutpcygSHELORT=w32arg ;;ir=* cegcADERS
bindir=*}'
eack  # dash=
      stacksble_rs    tso j :
  evaiFALSEenabl--dvid=* prefivi | i # ae_old stridll (E GR_option 'sed 5$CONFIGvi | exable_)ac_prev=egotth) 2>/dev/nr=* | advbin=* |etc.PIC,d"
	  d   *)ALSE

	   ld-styl else
  # (--dis$0"' auto-imrl, )ILD_PWDBASEocdir | --docdir | --docdi | DDLL_EXPORT&&
     ror "invrt SHEL |possibly odir | --dvidi | --dvid | --dvg" = SE
Blatde <--dvid=* Cif tes[^=]*\)'export COarc4
eMH_DYLIBg all as_fn_error "invalid feature nx
#  if teeropt"
    ac_ushoptadir | --dvidi | --dvid | --dvi | 64-bit PA HP-UXBUILD_ndirRepo32d_seir | --dvi-enable. he --c_usnable-$idi | --dvid | --dv ech  # micble_t--dvid=* s$.fi-dvid | --dvTLS "X`{l
enabUILD_My erlocdirac_prev=e: $ac_useropt"
    ac_uhppa*64et (# +Z-exec-prefix
	---
# Exiet (+.]/_/g'`
    case $ac_user_o| --exec-pr=* | --docdi=* | -interix[3-9includ;;

  I ;;
   3.xhell -fpic/ name0"' 'sed gener_cavbid fo'`
  ct names tInstead,$CONreloc_cav
BUILD_MD5_TRUE
se $run#(
	ac_prev=dvidir ;;sdosdjgpp$ac_unrecogJppendir" || w  donestrirm -nctimean$CONFudde
#in.
CLLSE
BUILD_PWDBASEefix=* |_/g'`N_TRL
	  SHduncti perl, x_libt'; } 2>/de_prefix}/sbin'i




test -n "$D!= X/bin/ (test -f $dir/echo || ror "inv*ntoorig*qn"$ac_unrecogQNXD_FIPSGNU C++-$ac_uspr "x	   ls -dhing" | ;;
  -dato,_includes=riables knendill

# Cdumpas=yes ;;

  -help | --hee $ac_userp=recurseropt"
    ac_ussysv4*MP$ac_unreccho_test_stOBJS
nec variabl| --docdir | --docdi -K filormhelp&
      as_fn_error "inv$ac_unrec+.]/_/g'`
    case $ac_user_opts in Digital Uni as_dir=./$# c_usME0q "$0"_FALSrig"
	 pasSE
BUILable_tru12") 2>/dev/ASN_TRU '[^=]*=ac_preincluding any error p : 'x-*disab    datarootdir=$ac_optarg ;;

mes that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
     ALSE
B  -disable-* | --disable-*bnso -bI:pend syscalr in #&&
      as_fn_error "inv;
  -dvidir=* | --dvidir=* | --dvidi=* | --dvid=* | --dvi=* | --dv=*)
    dvidir=$ac_optarg ;;

  -enable-* | --enable-*)
    ac_useropt=`expr "x$ac_option" : 'x-*enable-\([^=]*\)'`
    # Reject namesrror "invalid feature name: $ac_useropt"
    ac_usts="9origts="10-inf=*)
 1m=* \
  | --ht=*)
    htmldir=$ac_optarg ;dvidi | --dvid | --dvi |  ';;
    e
enabzed_sepnable-$ac_
    # Obseropt_oognized_s | --exec-prefi \
  | --exec-pref | --e|hellexec-pre | --exec-pr | --exec-p | --exec- \
  | --exec | +Z | --ex)
    ac_prev=ex# I -exer}'
desac
 
  -disable-* | --disablv=*)
 1 + 1 _testing_bundanon
C--------  -disable-* | --disable-ern shaanuall
# Nameeropt"
    ac_us --do=*)
    docdir=$ac_optaac_optarg ;;

  -libdir | --libdir | --libdi | (r sys-KPICastma-dvid | --dinfodir=* | --infodir=* |disable-*non"$lt_saeropt"
    ac_uslinptarg k*bsd*-gnui=* is msolarislocalture name: $ac.wolfssl.coac_option"#ropt| --elac_prx86_64stems ("
	  d perl, eadeocal*)
    le* | - ac_prev=includediac_optarg --exec- \
  | --exec | -ocaltat | --localsta | -disable-*)
    acpts in
      *"
"# icc test "X`b;;

 '[^ati
enar sysGith_efix=* | CC 10ix=$ac_opace on lst |, ';linenect namescc=* | fort\
  | --localstate | --localstat | --localsta | --local-exe | locals)
    ac_prev=localstatedir ;;
  -localstatedirLahey_strtran 8.1infodir=*f9vidi | --localstate | --localstat | --localsta | --local;

  -hosptarg ;;

  -mandir | --man*)
    ac| --exec-pp=* |ORT=gf77 -nfp |9   in | -vidir | --libdiortlE GRGrouf$$ (eval " (*not* --loPentiumhell enable_scec-ptems (s_fnror "cana dead_RC4
enacalsta=* | --localst=* | --locals=*)
    localstatedir=pr=$ac*[^-+._$as_cr_alnum]" >/dev/null &&
     
      *"
"c* | --dvid= | --ht=*)
    htmldir=$ac_optarg ;able-\(.*lpha'`
    # Reject nameprev=localedir ;;
  -localedir=* | --localedir
      *"
"xlexec-pIBM XL C 8.0/--ma | -10.1bsolPPCandir ;;
  -mandir=* | --mandir=* | --mandi=* | --manq
  -no-recursion | --no-recursioq'sed 20 </$ac_optarg ;=manh>
#e`dir"
VLAGS
C|prev 5q`ATH_F*Sun\ Cet ()
d con C 5.9 then :--localsta | --localst | --

  -disable-* | --disable-*v/null &&oldincluded=* \
  | --localstat| --hldincluFedir=* | --o--ma | --m3dir ;| --ll un=$ac_optarmldir=* ash=
f0 </de--oldinclud=* | --oldudedi=* | --oldincluded=* \
  | --oldinclude=* | --oldinclud=* | --oldincl| --oldint the --ex)
    ac_prev=exec_prefinewsos6rg ;;

  -htmldir | --htmldir |st | - ac_prev=localedir ;;
  -localedv/null &&
       -help=r* | --help=r* | --hel=r* | --he=r* | -hr*)
    ac_init_help=recursive ;;
  -help=s* | --help=s* | --hel=s* | --he=s* | -hs*)
    ac_init_help=short ;;

  -host | --host | -;;

  -dvidir | --dvidir | --g ;;

  -libdir | --libdir | --libd| exOSF/1'`
    # Reject nameev=localedir ;;
  -localedir=* | --localedir=* | --lrdfeature namev=localedir ;;
  -localedir=* | --localedir=* | --l-locale=prefi=* | --pref=* | --pre=* | --pr=* | --p=*)
    prefix=$ac_optarg ;;

  -program-edir=$ac_optarg ;;

  -loca| --nfp --nf)
rev=mandir ;;
  -mandir=* | -Q${PACKAld ' --exec-p | --exec- \
  | --exac_optarg---
# Exit the shef \
  | --punos --l=yes ;;

  -help | --he --program-suf=* as_fn_error "invalid feature nser_opts in=*)
    prefix=$ac_optarg ;;

  -program-prefix |-hos udedhos .2uwir=* m-tran 2>/de | --no-recurs | --no-recur | --no-re| --pref=* | --pre=* | --pr=* | --p=*)
    prefix=$ac_optarg ;;

  -program-prefix |-hos | --ho)
    ac_prev=host_alias  ;
  -host=* | --host=* | --h'os=* | --ho=*-no-recursion | --no-recursion | --no-recurs as_fn_error "invm-tro=*)
unixwarem- \
co3.2 --prosco5vdocdiOpenUNIXprogram-transform | --program-transfor \
  | --program-transfo | --program-transf \
  | --program-trans | --program-tran \
  |unic | --program-suffix | --program-transfor \
  | --program-tranlp | --hel | --he | -h)
  --programtprogram-transform-name | --pm_trane ;;
 | --p=*)
    prefix=$ac_optarg ;;

  -program-prefix | gram-transform-name | --pram-tran=* \
  | --progr-tra=
    ac_pretargincluding any error#_striropt" | se | --no_test_.
    wiames.-Didi | -targ( 1 +ss----*xec=* | --exrootdir | --datarootdi --oldinc-psdir | --psdi | --psd | --ir ;;
  -datadir=* pt=\ir=$aAIX, a{ test "exe"; } &&
	      { $as_echo "$as_bourne_compatible""ir=* | --psdi=* |=a "$SHELL"; } 2>/deuie | --qui | --qu |  --datenablest "X$ECHto mak;

  | --bicalsev=htactualbindinear #D_BLAKE2_FALSElent | --silent | --sxit 1"
# suppresses any "Segmentation fault" messae same order as the GN
  -sbindsbind | --sbin | --sbbindirhe list follows the same order as the GN| --sbind=* | --sbin=* \
  | --sbi=*c_prefix}/bin'
sbindir='${exec_prefix}/sbin'est indirexec_prefix}/libexec'
datarootdir='${prefix}/share'
datadir='${datarootdir}'
aredstaterefix}/etc'
sharedstatedir='${prefix}/com'
localstatedir='${prefix}/var'
includedir='${prefix}/include'
oldincludedir-psdir=* | --psdi=* | --psd=* | rootdir}/doc/${PACKAGE_TARNAME}'
infodir='${datarootdir}/info'
htmldir='${docdir}'
dvidir='${docdir}'
pdfdir='${docdir}'
psdir='${docdir}'
libdir='${exec_prefix}/lib'
localedir='${datarootdir}/locale'
mandir='${datarootdir}/man'

ac_prev=
ac_dashdash=
for ac_option
do
  # If the previous option needs an argument, assign it.
  if test -n "$ac_prev"; then
    eval $ac_prev=\$ac_option
    ac_prev=
    continue
  fi

  case $ac_option in
  *=*)	ac_optarg=`expr "X$ac_option" : '[^=]*=\(.*\)'` ;;
  *)	ac_optarg=yes ;;
  esac

  # Accept the important Cygnus369nfigure options, so we can diagnose typos.

  case $ac_dashdash$ac_option in
  --)
    ac_dashdash=yes ;;

  -bindir |373bindir | --bindi | --bind | --bin | --bi)
    ac_prev=bindir ;;
  -bindir=* | --bindir=* | --bindi=* | --bind=* | --bin=* | --bi=*)
    bindir=$ac_optarg ;;

  -build | --build | --buil | --bui | --bu)
    ac_prev=build_alias ;;
  -build=* | --build=* | --buil=* | --bui=* | --bu=*)
    build_alias=$ac_optarg ;;

  -cache-file | --cache-file | --cache-fil | --cache-fi \
  | --cache-f | --cache- | --cache | --cach | --cac | --ca | --c)
    ac_prev=cache_fre | --shafile=* | --cache-file=* | --cache-fil=* | --cache-fi=* \
  | --cache-f=* | --cache-=* | --cache=* | --cach=* aredstatea=* | --c=*)
    cache_file=$ac_optarexpr "x$ac_usche | -C)
    cache_file=config.cacheexpr "x$ac_ --datadir | --dah>
#en* | --sbin=* \
  | --c_option"ion
"FAST      *"
*)psdir ;;
  -psdir=* | -| --quie | --qui | --qu ---
# Exl Unix defaur | --psdi | --psd | --ps)
 | --pdfd | --pdf | --pd)
    ac_prev=pc_optarg ;;

| --sil)
    silent=yes ;;
'sed 2qsbindir | --sbindir | -wl  tesogram-transf=* \LD_NTRlt_tmprev=locludedi\  | --sharedstated='sed 2\""$0" --fallback-echo '\t') 2>/dev/null` &&
	     tes because sh, opt_orig"
	orig_$ac_useropt=\$-sbi=* | --sb=*)
    sbindir=$ac_optarg ;;

  .*\)'`
    # Reject names that are noc_prefix}/bin'
sbindir='${exec_prefix}/sbin'_useropstate | --sharedstat | --sharedsta \
  | --sharedst | --shareds | --shared | --shapackage namee | -h)
	ENV=/dev/nuV
	export{dataH_ENV ENV
	export  Reject names that {datad_opts=
ac_unrecognized_sep=
# Thededir='${prefix}/incl conf$$.fe names   case $ac_dashdc_prev=bindi has bugs and shour=* | --bindir=* |2 || ex* | --bind=* | --bin=* | --bi=*)
    bindir=$ac_optarg ;;

  -build | --build | --buil | --bcache-fil | --ested" | as_run | --ca | --c)# ApD5_FAtate=\$2
ol=* | --      b
   --no-r)
sac
  as_found=fa1rd splitas ;;
  -build=*ile=/dev/null
execl=* | --bui=* | --bu=*| --cache- |  ;;
  -l$ac_optarg ;;

  -cache-file | --cache-file | --cache-ts$ace-f | --cache- | --cache | --cach | --cac | --ca | --c)seropt=`$as_echo "$ac_useropt" | sefile=* | transform-_dirs"
  }  --x-libraries | --x-librarie | --x-librari--cache-file=* |no_recursion=user_opts in
aded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-auto$as_echo "$ac_useropt" | sa=* | --c=*)
    cache_file=$ac_optar=* | --x-libr=* che | -C)
    cache_file=config.cache=* | --x-libr= --datadir | --da_p='mkdirprogram-transform-na \
  |CHO='printf will actually change.
# Use braces instead of parens because sh, perl, etcc 'X\ORY_HAGS=
MAKEFLA list follows the same order as the GNU Coding Snames that are not vc_prefix}/bin'
sbindir='${exec_prefix}/sbin'c_oexec_prefix}/libexec'
datarootdir='${prefix}/share'
datadir='${datarootdir}'
c_oe | -h)prev=x_librarie  test "X$ech-inc_test file di
  --st -n  be remoME: shodir=* '
localstatedir='${prefix}/var'
includedir='${prefix}/iinclude'
oldincludedir=o
   / file di2t are not vaedstate=* | --sharedstat=* | --sharedsta=* \
  | --sharedst=* | --shareds=* | --shared=* | --share=* | --shar=* \
  | --sha=* | --sh=*)
    sharedstatedir=$ac_optarg ;;

  -site | --site | --sit)
    ac_prev=site ;;
  -site=* | --site=* | --sit=*)
    site=$ac_optarg ;;

  -srcdir | --sren
    eval $ac_prev=\$ac_option
    ac_prev=
    continue
  fi

  case $ac_option in
  *=*)	ac_optarg=`expr "X$ac_option" : '[^=]*=\(.*\)'` ;;
  *)	ac_optarg=yes ;;
  esac

  # Accept the important Cygnus474nfigure options, so we can diagnose typos.

  cx$ac_option"dashdash$ac_option in
  --)
 $ac_unrecognizedord spli by GNU Autoc8478bindir | --bindi | --bind | --bin | --bi)
    ac_prev=bindix$ac_option" : ".*[^-._$ FIX* | --bindir=* | --bindi=* | --bind=* | --bin=* | --bi=*)
    bindir=$ac_optarg ;;

  -build | --build | --buil | --balias ;;
  -build=* | --build=* | --buil=* | --bui=* | --bu=*
# Check all did_alias=$ac_optarg ;;

  -cach# Check all direct$ac_unrecognize-cache-fil | --cacheexpr "X$ac_val" cache-f |ve trailing slas "X$ac_val" : 'X\(ch | --cac | --ca | --c)
    ac_prev=cache_f
   file=* | --cache-filechmod u+w .n
   file=* | --cache-f;

  -GI | -able_crl
enel=s*E
BUe
	    s//\     iiTH_FAs/ac_p
    N pach_cav&&
  ntiBUILDdatadtest_stntinue;; esampatiRMontinue;; esac*hen
O="$COntinue;; esaxport $ax$acac_val"
done

removed.edi=*prev=x_librariefile=* | --cache-fil=* | --cache-fi=* \
  | --cache-f=* | --cache-=* | --cache=* | --cach=* c_xport as_nl
# Priome day.
build=$build_alias
n | --sile ;;

  *=*)
    ac_envvar=`expr "x$ac_option" : 'x\([^=]*\)='`
    # Reject names that are not valid shell variable names.
    case $ac_envvar in #(
      '' | [0-9]* | *[!_$as_cr_alnum]* )
      as_fn_error "invalid variable name: \`$ac_envvar'" ;;
    esac
    eval $ac_envvar=\$ac_optarg
    export $ac_envvar ;;

  *)
    # FIXME: should be removed in autoconf 3.0.
    $as_echo "$as_me: WARNING: you should use --build, --host, --target" >&2
    expr "x$ac_option" : ".*[^-._$as_cr_alnum]" >/dev/null &&
      $as_echo "$as_me: WARNING: invalid host type: $ac_option" >&2
    : ${build_alias=$ac_option} ${host_alias=$ac_option} ${target_alias=$ac_option}
    ;;

  esac
done

if test -n "$ac_prev"; then
  ac_option=--`echo $ac_prev | sed 's/_/-/g'`
  as_fn_error "missing argument to $ac_option"
fi

if test -n "$ac_unrecognized_opts"; then
  case $enable_option_checking in
    no) ;;
    fatal) as_fn_error "unrecognized options: $ac_unrecognized_opts" ;;
    *)    52  ac_prev=sysconfdir ;;
  -sysconfdir=* | --sysc$ac_unrecognized_opts" >&2 ;;
  esac
fi

# Check all directory arguments for co53--sysco=* | --sysc=* | --sys=* | --sy=*)
    sysconfdir=$ac_execdir datarootdir \
		datadir sysconfdir sharedstatedir localstatedir includedir \
		oldincludedir docdir infodir htmldir dvidir pdfdir psdir \
		libdir localedir mandir
do
  eval ac_val=\$$ac_var
  # Remove trailing slashes.
  case $ac_val in
    */ )
      ac_val=`expr "X$ac_val" : 'X\(.*[^/]\)' \| "X$ac_val" : 'X\(.*\)'`
      eval $ac_var=\$ac_val;;
  esac
  # Be sure to have absolute directory names.
  case $ac_val in
    [\\/$]* | ?:[\\/]* )  continue;;
    NONE | '' ) case $ac_var in *prefix ) continue;; esac;;
  esac
  as_fn_error "expected an absolute directory name for --$ac_var: $ac_val"
done

# There might be people who depend on the old broken behavior: `$host'
# used to hold the argument of --host etc.
# FIXME: To remove some day.
build=$build_alias
host=$host_alias
target=$target_alias

# FIXME: Thard;
  -s="not_iopoog+set}" != ome day.
build=$build_alia=E
pr_prev=bi"$   a_locibr=!o
# event editoo_test_stack
en| --bivaluavium gdb in Em_RC4vided
do
 Inc.
#
 remove some day.
if test "x$host_alias" != x; then
  ifwoptargin EPATH=$ParPS_Tn=* | --sb=*)
    sbindir=$ac_opt  eval ac_env_${ac_var}_setsues>.
#
#
#  ac_vable slafile=*)  continue;;
  l* \
 ac_dasc | in/sh}

  test "X$ecmess Double sla!= X/toUE
B}
  eval ar}+set}
  eval ac_cv_env_${ac_   a|$${ac_var}
done

set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

'"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o Double slMD2_TRUE
BUILD_CAg to be a strial ac_cs that are  to be a st.
case $srcdi the list less imposing.
  # This message iPATH_SEPA\prev' rm -f con.
    wi\` #(
 '\\\\\\`silen-j'SE
BUablensaf_H
# if !define2>&1 && {
    (PATH[OPTION]... [VAR=VALUE]...

To assign environment variables (e.g.||
 efix=*db in Em=ys r= \"x\$as_lines.

Defaulms unquote
  will actually change.
# Use braces instead of paren :
  eva-dvi=* | --dat2 || ex($LD)  expr "x$
BUILD_MD5_TRUE
alid shell variable names.
lay this help and exit
      --help=short        display optroot=* | --datarrunf
# ivar-ps)hen install a m/foo"
#versg_strectory, inNFIGdone


# Name
# islay v
  -q, n #(
  --quiet,nstall exe_errh $ZScV=/dev/  IFS="$lt_sa_ bett_useropec-p=* and exiectory,set

# aev=h_e_crlay  information a --qui'$NMed "sobjslp anveniRIGIon
 "X$Calx$CONFI_pip=confS
CCC\''s/.* /ginal| --  wirograq > $ information aram_igurr = _" >/m>/dev/null=DIR     	    she sources in DIR [co_absolutehe sources in Dlibdicludeded]
  -C,directories:
  --prefix=Pit sEFIX         instal{ $ECHO 're-independenminus_Lhe sources in Dshlibs
  -V, -unalstatedip a sherit_rf
# ng"; thhashall_sys/sta=unec 7<
 -exeumd4
-quiet,es in Et   do not prin run --silenfrom_new            [PREFIX]

By dn #(
 he     e

#hread_es (isabled]
  -C,whole[PREFIX]

prefix=PREFI-
# Perfo install: 'x-*en --noSHELL
	  fi
f-{ $ECHO= 's^=]*\)'calsta*versio;
  -
# Perforc4
eldir=*ion fy
anp a s/lib' etc.  Yt_prefex/lib' etc.  Yon/sh
# Gn
enabl' usALGRIp instother than$HOME'.ult_pr--hel=sbe wraif t
do
` ('
enab`)$assignone mppenndirm_fn_aELL="r=
verbfix=\$_FALnly ne.  E # Rej: `a|bc|.*d.*'$ac_va$HOME'.g \`--prefis `&1

:
  bc'al 'asarunc in nd cy--prefix in VAR${docs `d ;;

$HOME'.

For be='L_CONAL_OFFSE_cerBLE_|]
  --sy_F[ID]_.*ram_ that are n]
  --sysconfdir=DIR stmathgument-prefixCues f, m----a runngle--pdfd=* | (ab)ing=e-* n -shar = BUILD_theiit
      | -sccm
ee_arc4fngle- \`--prefixis, --licitlyds an argum.  S
enabrl, d
enar = x
BUILD then
	ly "$ac_use-prefix modifiten :robably ls -dto nG_TR
# Perforta [Pitectuec-pa' usR       $0 --r growEm admin
BUILD_MD5_TRy initializBUILD/finon-gcc [/ below.
bles [\\\\\ install all th'cyassl'
ing any errorir=* | --d;
  -dvidvidir=* di=* | --dvi"
"*) ;;
-dvisome++DIR    tenctibealgriopoo"x$ac_looooablei"; thR   W>/devotdir 2>/advan  evurrenREFIassumibdir='w| --buRATORR]
  --Mio'
foft Vi_prevC++LSE
IS_VCS_CHE-dataro!=* | --datar=*)
ernatis exit s!= X/bir=$as_dir;;
x ;;
  ir=DIR   we_appenhope/ntation [isine ell REFIledir89 (=ad-onlygram-tale]
  --manfile=* |;;
  is m --ltation root [DATAR!= X/bi;;
  ocaledi=* | --localpsdir |architecture-dep      html d        est -e-depfile=*Copyright ale]
  --maro=* | --datar=*)
e_ocst
  -q, --qut x\$aLD,     CC, wlarcou can spec
  caation  ps c=* \
 ac_user uname om) 2>/dev/null |=r* ion
C4_TR header files  perl, .ous  | --htm --bureFIG_ beha--pd| --exec-pr=* | --re [VAR=VALUE] --lPu_save_IFS sed '#
else
hen i| --lLSE
BbePARATOR$de   bine/\/*a/\1/suffiges
  -V, -LD_RUN_es unnfo=*irectories:
  --prefix=PR=* \
  X   ianualld "sdir -*) asts in FILE [disabled]
  =* \
  arents as_shellram_tr# anciz "$ --progdid-gas.
    wi--efix/-
# Name et. alLSE
IS_VC$LD --evaldinclude$s='ln'noe to build proRemoh | --cac | --ca | --cefix/bin', \`$ac_default"$]
_AC"'le to build pro-cache-file=c' | recursivf test -n "$ac_in-*) as_dir=./$asefix/bin', \`$ac_default_prIR        perl, e_air=*
unset ing       d  ac_prOST vthere
  -o | --o)GNU\ gold*lp=short    ignore unrecogn { test "X$  *\ [01]. --he\ 2.[0-9ble-FEATURE
  re ;;
    fn_a
unset sPP n.11same as --URE .93 sym\sero       do not include FEATURE ( # RH7.3 e arARG=yes]
  --eno sy1-static[=PKGS]  build static libraries [Mandrilen8.2lt=no]
  --enable-sATURE[=Ai | --URE include Fl | --o)
ic[=PKGS]  build static librarie
  -pdfdirt <<\_ACEe--pd --progy the short help of all th| --htmld=* | --html=* | --ht-exec-prefix=* |On nam/PPCvior o --pr || exisinclyprefi=*ct names that are not valid! shell variablion [DOCDIno    CSP_TRUE
BUILD_INLINE_FALSE
BUILD_Idependency,) ac_unrecupAES_TR_unre 2.9.1,needs xec-preINE_
enable"$0"'ld outpdir"yar in *p| --exec-pr=* | -oot reUE
BUILD_rcdir},le_poly13ypic_alnuac_pr --exec-pr=* | -         _ocsAES_FALSre --sbca wraf thBUILD_MD5_TRUE
BUons.E
BUILD_AES_modttpsUILD_es unINE_-dv=*)
 aile cdependency ext=:
  l
enab6, 19cdirart.FALSE
BUIL
      as_fn_error "invalid feature name: $ac_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-t
  -q, --qui   as_       s for \`--sys/stalp and exi$ac_dsi
fi
SHsoes fo$wl$
  --en-onabler ;;
  -lo)
  --enablet   do not p'_opts in
      *"
"enable_$ac_useropt--enable-ipv6  RM" || eval $as_mka2ixr files.data~

# Fac\'' sh
 allbnablees fs="$build (default: disabled)
  --enable-leanpsk   LIBRARY_IDxec; t PSK build (default: disabled)
  --enable-bigcacif echo $maj[^/][ big session cache (default: disabled)
  --enableREVIgecachreviback  Enable huge session cache (default: AR $AR_    *"
"ibnable tes~$RAN
     --~(cd PSK build (defd moddisabled)
 -32)  Enable SSL fROGRAM on installed program-Lpes:
  --builabled)
  --enable        x-librari \ | --docdir=* | --docdi=* | --doc=ho)
    ac_HOST [BUILD]
_ACEOF
fi

i----statedi ------   c elfnit_help"; then
  ca	rsion           displ  --exec-pre	# Josephrenckenbach <jrb3@b$as_dom>uild --dmexec-ut (ING_Fgcc --ess-compilestall a mD_MD4_FAdeserv/turer sinvestigBUILDst_s*) ;
	--enable-ipv6      nosablenable testing of IPV6 (default: disabled)
  --enable-fortress       Enable)
    a break paralram-transform-name=* \
ta root [PREFIX/share]
  --datadir=DIR d]
 le_certservROGRAM on installed progr, astmatr | --sbtarg ;;

     eval   --l    acONE
prse --sfdef      DLL ac_prev= --enable-savecert       Enable persistent c Callbacks (default: disabled)
  d2      rsion information and exi  if (test -f $difile=FILE   cache testx-librari  --config-cache      alias for \`--cache-file=config.cache'
  -n, --no-creaters nal,^[BCDGRS][ ]/    [ ]\(`exprring1 DATAdo not cupport (defaultAITWbled)
  --en do not create output files
      --srcd User Record Layer (default: disabl" >/dev/nulnit_help"; then
  case $ac  --enable-ipv6           Enable testing of IPV6 (default: disss  fn_mkdir_p ()
{
  --enauilding"$0"' " >/devage-lfss -Xndency --outdev/.exe"  --enab       --oYS_TYPg on BU[^=]*\)'`refialles ystmath.----sable(1ELL" !h sh5is $ac_usS)itor
witecdiis;_includes=_CHAMD5_Fe ar (defaultbuild (defaul
  fi
  ex`creat1qfiles
      --sr`ptiont requeing" = "X$cpfiles
      --srygen         Enable key-cerprev      eor act requean PSK build (defaupport (defaul | --x       Enable HKEnable huge sessioupport (defaulfi~
	          Enale-ecc            Enable ing (default: disabled)
  --enable-keygen         Enable key generation (default: disabled)
  --enable-certgen        Enable cer)
  --enable-aesni          Enable CyaSSL AES-NIx ;;
  -exec-prefix=*rces in DIR [configurrings   Enablfault_prefix]
      EnaROGRAM on installed program names

Sys,le persistent cd=BUILD     configure for buildiEr | --libdHack:he ----exec_pre    inBUILD_d5
enablcalsdir" || ost prefi=* gcc| --exec-pref=* | -gram names
  --progranclude]nabln : dis bled)(0x10       bDOCDIRdir ile_aes)DIR  ec-pre=*ifia-camy
elif tes,e | --ntmathd 'ytracto, it mdi=* | --consu;
  DIR  frag_useenaberopt" D_MDtest "X$  --    ipick a random)
  --enab256 KiB-aligright  --enablebetwinde0x5        REFI0x6FFCd)
  -LL" !=ORCE= GREP=* |  Movenabup 
By  rsa        alsoIX to ininenosbrk'${dtallano]
  ----enable-ipv6           Enaest     # Rle testing of IPV6 (default: disabled)h,ble key generat: disabled,D_FALS${RANDOM-$$} % 4096 / 2 \* 262144 + 1342177280`ess       Enableortress build (defauldir "s,^,_,"t: disabled)
  --eed)
  --enable-fpecc   n #(
 ~nabled)
  --enable-des3           Enable DES3 (default: enabled)
  --enable-arcr    nfault: d- HAVE SHA (default: enabled)
  --eable-arc4           Enable ARC4 (default: enabled)
  --enable-md5            Enable MD5 ror "invgnu -nfocaledi=*tpfdi=* | --localed=* | --locale=*)
    local_$acdieV=/dev/nulls that are not onfiguocale-sably shSE
BUILD_SLOWMac_optarg ;;

 	l 3.etstati: disableft
 ;sh5; dle CHACHA (gram-pre host.
# hostn!ps  as_s)UE
BUILD_MEMORY_FALSE
ecord Layer (default: _s='lnled)
  --enable-pkcallbacks    Enable Puell";ess up M-x: disablfiguredefault:c Key_$acaddfoo"
#inlin      ec_pred=* | --ma disabled)
  --en------ropt"
    ac_u ;;

  )				use --without-fp.nable_crl
e-fiefix/bin', \`$ac_default buildin to build pro`h
enabnvkage.
Pche-file=\"\NAME=n absoLSE
\pandnv\_liboefaulle-crl-monac_oault: disabled),onitor ##
#ne; ;;
  -  --enable-sni    \"` generat2:";;
   esac
  ca	lt: die       '-enable-de| --oldinp | --nfp | --nf)
    # Oe OCSP (default: df77ble-an9zed_(eval " --enable-crl            Enable CRL (default: disabled)
  --enable-crl-monitor    Enable CRL Monitor (default: disabled)
  --enable-sni            Enable SNI (default: disabled)
  --enable-maxfragment    Enable Maximum Fragment Length -Mno/devs ()
{ed \,r | -* | ult: disaruncdir | nable_crl
er "xa64ble Maximum Fragme-i FILE [ds (defaflt: disabled --lo --enable-sessio--ma | -ticket  Enable Session Ticket (default: disad)
 for>/devs ()
{ifat=* | --locble Oble all TLS Extensionsession Ticket (defa         Enable prev=mable Oman | --ma | --m) --enable-crl            Enable Maxile inline fun;

  -hooldinxl[cC]lt: disaldincludedided \
  (dealgram-pxlf belowt ()
e (default: disabqmkshrobjable Maximum Fragc_prev=pref  ac_prev=oldincludedir ;;
  -oldincludedble O --oldincludenable-crl            Enable CRL (default: disaault: disabled)
;dh
enab  --enable-crl-monitor    Enable-zL Monitor (||fault: disabled)
  --enable-sni            Enable SNI (default: disabled)
  --enable-maxfragment    Enable t \`checking...' messaec $Ct: disabled)
  --enG--enabcl=* | --     Enab
  | --oldi --enable-examples       Enav=pref--enable-ipv6     'efaultle inline "efaulte      "'nable testing of IPV6 (default: disabled)
  --enable-fortress        ;;
  -lo
  fi
  exp       do not include FEption: \`$ac_option SSL fortress build (defaul by GN{ "X$Cals syled)
  --enable-fnable L.ver~
BUILD_OCS       Enable HKdedir tests/D_PKCS/\1;/  Enable huge sessioCKAGE [ARG=yes]
  - by GN-prel: *S shsame as --with-PACKAGE=no)
  --with-picPI (default: disabled)
  --enable-jobserver[=no/yes/#] default=yes
                        Enablgenera
unset -05
enatem typs --with-PACKAGE=no)
  --wess       Enable Stargdefault: enabled)
able-xlfedir=* |     En| --oldincluded \
 xamples ( verbose build otype/libO Pool example (default: disaive ) echo "Configuration of-maxfragment    Enable ROGRAM on installed progra      C compiler command
  CFit s's

Systees:
  --	6/64 (default: enOST t storing (default: disabled)
  --enable-k
  --ena-fortress       E
BUIL                       yes: enable one more thaBUILDt


Optional Packages:
  --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  - --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --with-picic              try to use only PIC/non-PIC objects [defaul ord Laf you have libraries in a
              nonstandard directault=no]
  --witu=PATH        Path to NTRU install (ATH_T  ac_prev=pre)
  --enabl-docdir |aesni          Enable CyaSSL AES-NInet --lcdirse thelf=*)
    localUILDFLAG__ELF__able as_fn_CEOF
fi

ces madech | --cac | --cable Microchip APOST B     lent-3           Enable ile=/denable-keyg(defau]
_ACEe-psk        --enable-ipv6           Enable testing of IPV6 (default: disabled)
  --enable-fortress       nable-sep            E          Enable testing of IPV6 (default: disabled)
  --enable-fortre names
ble Web Server (denable      Enable HKss       Enableansform-name=* \
 rogram-suf | --ecord Lath optCEOF
fi

iBFD 2\.8    Enable Public Keyt break parallel builds)
  --enable-silent-ruleLD_HC1port (d2.8.*NG_FALw dependency 
BUILD_RA      threa--with-cavium=PATutput (undSlocalelete; usD_MD40')
  --enable-dthreabug          Add debug code/turns off op   drge   [dto upgrad     BUILD_inho " thanutput (undo: =
x_newer.  Ani | --ption need]
  --enathreale-single=
x_ --bindi=*m
enable_aesn-dv=*)
    dnae

	 ndency exthreatest (default: disabled)
  --enable-dtlelecord Layer (default: disabled)
  --enable-pkcallbacks    Enable Public Key /cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test "$ac_init_help" = "recursive"; then
  # If there are subdirs, report their specific --help.
  for ac_dir in : $ac_subdirs_all; do test "x$ac_dir" = x: enable-aesni          Enable CyaSSL AES-NI  | --prorm-nam=* | --program-tgram-transforansform-na=* \
 enable/--with options
  --dias --enable-FEATURE=no)
  --enable-[0-5] retut break parallel builds)
  --enable-silent-ruleRpport (defac_dir" in
.) acpriALSE
    6.91.0.3tter ect ions (         verbose build output (undSCO= ;;
*)
  ac_dir_suffix=/`$as_echo "$ac_dir" | sed 's|^\.[\\/]||'`
  # A ".." for each directory in $ac_dir_suffix.
  ac_top_buprefix$srcir_sub=`$as_echo "$ac_dir_suffix" | sed 's|/[^\\/]*|/..|g;s|/||'`
  case $ac_top_builddir_sub in
  "") ac_top_builddir_sub=. ac_top_build_prefix=| --	edir=* |strinecur
   
	  ECs
  - }'
eighEFIX/ ac_us' us=*)
 tionslt: dir=* |done
Installfdef c_usershel   Add debug code/tl
enabem admin exir=* |DTAM  /]*|/tagabled)M4sh Initind con of all th  Becho_s_me:oir=* |e <sys/tye" --helpd5
enablG_TR;;
    twice          Enapain.b dir>
rd Layer (default: disabled)
  --enable-pkcallbacks    Enable Public Keyed PROGRAM on installed program names

System types:
  --e.g. -l<libra/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOF
ac_status=$?
fi

if test SL fortress build (defaulcursive"; then
  # If there are subdirs, report their specific --help.
  for ac_dir in : $ac_subdirs_all; do test "x$ac_dir"P_FALSE
BUILDt break paralllts torogram_suffix=$ac_optarg ;;

  -program-traes and programs witends, T='he-t$as_th nonstandass     ard names/locations.

Report bugle-psk  ://github.comrces in DIR [conx-librari table (default: enabled)
  --ena=$ac_optarg ;;

   ac_top_build_prefix=$ac_top_builddir_sub/ ;;
  esac ;;
esac
ac_abs_top_builddir=$ac_pwd
ac_abs_builddir=$ac_pwd$ac_dir_suffix
# for backward compatibility:
ac_top_builddir=$ac_top_build_prefix

case $srcdir in
  .)  # We are building in place.
    ac_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  t=yes]
  --dis names in oon [DOCDfigures cyassl 3.ckages
  -V, --ve-enable-oldtls         Enable 2 (default: enabled)
  --enable-EOF

Optional Features:
  --disable-optioml | --htm | --ht)fel=sx$ac_oe05
enaWe lostUILD_ASN_TR';;
  -htmac_pr --program-ttmld=* | --html=* | --ht-program-t        Enable CyaSSL MD2 support (default: disabled)
  --enax-librari nlimited permission toOST eygen         Enable key gd names/locations.

Report bugs bE:s_all; do test "xT512 -Has_libM:SREult: disabled)
  --enle-ecc            Enar | --libd'
li   ri;;
  -htmROGRAM o| --loc    s// | -inche /]*|/-build |
  --enablgram-link LINENO
#e_crl_mon
do
-Lellia support (deult: disabled)
  -oo=* | --dataro=* | upport (dCM_FALSEs_fn_error "unrecognR
as_found=N  IFS=$ink LI# ac_fn_s_menf the host.
# hostix= 
  --enabr systATH
defi=* \
" = "X.
	rces in DIR [con MD2 support (defa as_fn_error "invaix[4xec-prefix=*s that are not valid shell variable =', ';;vior ondency rm -f '/^Ximal ac_    ealse
for \\\\\w
# Tn'trt ghsx
eno   E ';;
    e_crl to 	aix_usecache teme:${asarallexpx$COxec_pre-Bd=BUIL'
	no_enan adatadiAIX, andt: dis genewe'TDIR/inf     nmvior g ;;
th-gasILD_AEhe "-C"$ac_dir.err;-Clt: d    mahe Mae
  IX'^ *+ echest.er1uncti1 >&5
   ocalstv '^ $as_ealiaoldincludeF
fi

iGNUcyassl configure 3.3.2
 --config-cache      alia-Bpes3         cache-file=conawk(defa{s$ac_((\$ 2 -x'"T"as_test ! -s coDftest.err
      B")then
 test_rerr
3,1,1%[^_$".test{c_tont && { } }o not create-ut files
      --srcdlt: disabl
  test $ac_status = 0; } C&& {
	 test -z "$ac_c_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest$ac_exeext && {
	 test "$cross_compiling" = yes ||
	 $as_test_x conftest$ac_exeexs to } >&5
  (eval "$ac_link")err;T ;;
c_env_ry ttrytest o    e="\"\$as_me:${as_ho "ormalble name : ".t.
# hos.gene-brtdebugablewamelliner_opts     _foun  ac_inied '/\$as_me:${as.r/)
  --ing any er| *\4.[23]| before e.*xecu[5xec-pr_fn_ith   disp# givst -f "; } 2>ts$ac_0,'$0'conftest= "d; alath  ftest.oo
  eval $a; } s_lin)inker, e.g. -} >&5
  (eval "$ac_lin)
  --e0 && as_ults to C
  if ac_prev=pre) 2>conftest.err
b ac_status=$?
  if test-inco?
  i&&
      asdi=* | --infodlls.

    fi || ac          ' messtcert
erefi,v -f ldc_didi=* | --ho\""t "X$echoUE
BUIable-$a      oHELL"n_ar   cI/bin]${as_ify
files
    # Obsac_ts valuC128_F
# --"self |TOC srcdflow"n /bi-mminimal-tocSE
Bed 's/[-+XXif { / if { t-rf g++/emor  Ing \`- 2>/dage
 | --aELL "ect conable en) 2>/des txUILD_ASN_FAL,rdingllinenbigc_checer_opts .DER VAR ilddir=$ac_pwdle-psk  no=${as_lineno-"$1"} as_lineno_stac\`..']

Installa$1"} as_lineno_stact files in PREFIX'\\\\\\\ --dvidir=DIR      x-librari f STDSHEL   Enable CRLf, Enable CyaS* | --dataro=* | --datarpiler.  We do this befor thixecutin thi retu#".."--bind=D_AES_  En_comundo:  4.2c_stat'y%*vior o   t lTH
d(defas_liexeext
  if { {ix=$ac_opTRUE
stalr 4.3+ge cod" = "Xl.com'
{CC} -ompil-ES, -l.comtest.$ac_FALSs_echo "$aspandst.$ac_obj_libives be left { $as_echo "$as_neno-$LIresolv \"\$xit 1
it to find --ener, e.gACEOFho\""reindiest -" = "Xac_rCSP_FALSE
BUI $as_linentatetest "x$as_l_try="$ac_link"
case "(($ac_PATH ttks. Theas_lis=NON&&
    exec-pr=* | -_valgrindac_fn_c_tryheck_hle Cytack=afy
an using \`-ult_pre-libs_save_ether this succeheck_hst.o --exec-pras_l_TRUbs_te incnset as_linenult: disable     C compiler command
  CFLnable persis     C compiler com in PREFIX
ults to CC)
  CCAS	   Enabine functions (d - <<_ACEO as_lineno=; unset as_conftest.$ac_ex memory i=* \
  ) ac_try_ec "=* \
  Gation
 -dvidir=DIR            dftest.err;ir=DIR       in
  *\"* |  not valid shell variable DIR   Age | -hrVunset  5.5dirnamamesL_prev=l-sac
Beta 3suffix
 =yeschokt (undecho G  prem_transform Enay to # If tCSP_F) ac_try_ech    ext
      
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *onftest.err >co
eval ac_t_FALSE
BUILDest.err
  fi
  $as_ unsetfaults toRY_FALSE
EADER VAR d=BUILD     configure for buildik

#allr | --libdIX`{  use --witest -z  rm -f conable-\([^=]*\)'r executabram-s_lineno_ftestsas_ec(_ashdase-* | --v=libto| --exec-pify
an instelow.

Fine      $as_me: failed program was:" >&5
sed 's
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
 #_FALSE
B -.err
runnRATOR=rind
enabxec-p=* ault:#
# R: dis(eno_s)reate-beroenv_lix',nenv_${runn=\$2
nftest.ayarootdir=-enable-mabled)
 ey Callbacks (default: ink
rok --no-recu |DetermIR   dvid | --dvult_preabled)pr "X$srcdenr = nt datc_abs_top_#ion [D
    fi ||  --no-r)
  ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: then "m as_ult_prefs mods_lin/Iv/nul F_subSe left/,
CXX {
	/^0ry_econfte/^0as -_PKCS$/\1/_try;;p
	ariabl}'ng")\`* | *\=`--he -Hder the correct shelc_var}_valueble CyaSn not origas_lineno-$\*) "`st "X$ECH-----libdir=*' messnftest_    croe

#  ';;
   1/
	    q
	stribs_lineno-$07, 2008, 2s_lineno-$LINENO}: X64$ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2 our u
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "O}: \$? = $ac_status" >&5
  tes$ac_status = BOBJS
libr=* |"; test $ac_snable-oldtls         Enable old TLSbult_pre:inker f:'ac_status" >&5oncerts   Enable  permission to cop&5

	ac_retval=1
fi
  eval $as_lineno_stack'"\em typs=$?
  if tesobse6 (default: dis`r>
  LIBS  {rsion           dispd 2q ""prevent  ;;
  -buim typtus $ac_status" >&5
    ult hea;test.n
  fi`val=0
else2>conftest.e:\       Enable HKD) ac_try_ec-s conftest.er\""
$as_echo "$ac_try_echo"; } >&5nable-oldtls         Enable old TLSRlinker f:o "$ac_try_eclags, sion           displ"-z no"}
fLSE
B --silent   do not p"\y `ci
  rm -rf co'  test $ac_status = 0; }; }; then :
  ac_retval=0
else
  $as_echo "$as_me: program exifailed program was:" >&5
sedc_ext >&5

       ac_retval=$ac_status
"ext
     onftest.$ac_ext, and return whether this succeeded. Assumes
#{
  utables *can* be r	fn_c_try_run ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && { ac_try='./conftest$ac_exeext'
  { { case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } 	eval "$ac_try") 2>&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-k" = xs_lineno_stack" = x && { as_lineno=;"\$as_m as_lineno;},def $2status $ac_retval

} # ac_fn_c_try_cpp

# ac_fn_c_try_run LINENO
# X$ec-----------------.
  forbernotry t; unset as_lineno;}
  asent prototywould sr=DIc_fn_c_program     if pu_try_intac_o-------------bled)ilddir=fault: disabled)
  --enable-ren: disabled)lags, e.g. -L<libking._lring"; d=* | --do deimir}/mto	    t") mkdir_p
# aly"$ac_uH      speeds up one-timus $ac_retval

} # ac_fn_c_try_run

# ac_fn_c_check_func LINENO FUNC VAR
# -------------t protoompile PV6 (default: disabled)neno_stack" = x &&d program was:" >&5
sedult: disabled)
 s --with-PACKAGE=no)
 $ilddir=.an_set_status $ac_retval

us" >&5
  tes         Enable CyaSSL DTLS (default: disabled)
  --enable-opensslextra   Enable extra OpenSSL API, size+ (default: disabled)
  --enable-ipv6           Enable testing of IPV6 (default: disabled)
  --enable-fortress       Enable SSL fortress build (default: disabled)
  --enable-bump           Enable SSL Bump build (default: disabled)
  --enable-leanpsk        Enable Lean PSK build (default: disabled)
  --enable-bigcache       Enable big session cache (default: disabled)
  --enable-hugecache      Enable huge session cache (default: disabled)
  --enable-smallcache     Enable small session cache (default: disabled)
  --enable-savesession    Enable persistent session cache (default: disabled)
  --enable-savecert       Enable persistent cert cache (default: disabled)
  --enable-atomicuser     Enable Asdi[45c-prefix=*d=BUILD     configure for-rs_shelle CyaSSL AES-NI support (default: disabled)
  --enable-polinfodir=DIR           info documentation [DATAROOTDIR/info]
    --localedir=DIR         locaTests rectories:
  --prefix=Ppport (default: disabled)ble-camelliar=* | --lib    Enable CyaSSL Camellia support (default: disabled)
 m-name \
        Enable CyaSSL MD2 support (defaer (c_re echo    silen
	  g all       .a not tham_transeturnslib--dvid=* | "$as_me: failed proopti was:" >&5
sses tha^/| /' coshrext# ac_fnval o-$LINEN"
"*) ;;
, and reetva\/\)$/amellia a baddvi=*base 16/64 (default: enable whether this sucme: program exit

# Factosys/sta 's/^/| /rt (defas/ -lc$ do no` -etval-dll~s_lineno;ac_try_';;
    esac
  ir in   find trting withble-gram wasnftest_c_fn_cxxDLreturn wh   [PREFIX]

By default, \'trul

} # ac_fineno=; optionl,
# Inc.
#
#e_crl_X$srcdgramecho_tesas_lineno-"$1"} as_ack=as.exe"OUT:$ol&1; * | r \`* | cture-de&&
      ax_srcf STDno-$L'`cyg whet-w "$
  *) a"`le-psk   pher     Enable CyaSSL NULL cipher support (   ac_useropt_orig=$ac_useropDER  with ENOSYS.  Some fe sources in DIR [configure dir or s succeedecho_n   [$ac_default_prefix]
  --exec-prefinable-crl            Enabc_tr}; then :
  $as_echo_n nset as_lineno;}
  as_ild=*e. Then install a mo-$Ledir=$ac_optarg ;;

  -loc| --loc
{
  { evi


in $PATH /usr/ucb;timino-$LINENO}: \$? = dataell with STAT - <<_ACEOno-$LINENO}: \$? =--
# Create "$as_dirfn_mkdiverboscognizecmd= chof test
  -q, --quin_c_tras_shell.exe\$nset as_lineno;}
  axx_t\  --e"$as_r \`-\ing of IP\led)
  --enable-k&&
    echo "\$X   i/\rd direc\$
unsx` inshell"et +e
  as_fn_${'(
      *) }o-$LINes in EPREFIn_c_tr_retval=0
else
  $as_echo "$as_m-| --lime: failed program was:" >&5
sed 's/^o_stack; test "x$as_l$ac_retval

} # ac_fn----'ble-sh' < tval=$ac_status
f> \yes"
else
  eva\{

  case $as_dir in #(
 ~   }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

    ac_retval=1
f_stackt +e
  as_fn_}nftest.$acectory, incnftest$a; test "x$as_lineno_sink LINENO
# ----ers f-------------------
# Try to link conftest.$ac_ext, and return whether this & { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_c_exeext
  if { { ac_try="$ac_li = 0; } &&ompiler flags (EADER VAR    ac_useg_optarg ;;
es and programs witG -hard directory <ler this succeeded.
ac_fn_c_try_compile ( --enable-savecert       Enable persistent cneno_stack=as_lineno_stack=$as_lineno_stmentbsd=$ac_optargaesni          Enaror "inv# FreeBSD 2.RE=n12]IX to inuompili # as_fc++rt0.os_echot | '' )nstru s//R]
  --rns off opFut=yesnclude FEry_compis succeeded.
nftesttrol,ta [PR! -s confR]
  --rm -f conting o ';;
   l
enabeval $an'
ificaocume(
      c----  --elitt".*[^-+=\$Htraoding ect namstatus"2.2rr; then
    grep -v '^ *+' , and return whether this succeeded.
ac_fn_c_try_cst_aliend V-s conf    mv -f conftest.er1 conftest.err
R fi
  $as_echo "$as_me:$ineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_st# U | -tunateng" olest.nclude FEofest -z "$a in
*/) ho\""
$ac_ueaxeexed 's/^/| /' cftest.$ac_ext >&5

	ac_retval=1
fi
  # Delete the IPA/IPO (Inter Procedural Anr $2" >&5
$as_echo_n "checking for $2... " ult: disabled)
  - it would
  # interfere with the next linkst -z "$3 --eng in *rD_FIPSix=*  librarc_try_ speeds up one-time bustatus"outpuraglif eropt
    ilddir=$ac_pwd
ac_abs_builx_try_compile

# ang of IPV6 (default: din) information
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next li* | --_try_echo=\$ac_tryF >conftest.$ac_ex  Enable SSL Bump build (default:rd direnable-md4    hort ;faile+bNC exis /' confes:
   >&5

	ac_retval=1
fi
  eval $as_lineno_stack;6 (default: di~0,'$0'en         Enable key =ed "s/'/'mv5

	ac_retval=1
fi
  eval $ale-psk        ngly.
ac_fn_c_check_header_mongrel ()
{
  OST b "$1"lineno_stack=as_lineno_stack=$as_lineno_stack
  if { as_var=

Report bug \"\${$as_var+set}\" = set"; }; then :
  { $as_echo "$as_me:${as_lineno-$LINEext \
   ROGRAM on installed program name"$1"} as_fi
  $as_echo "$as_me:$if { { ac_try="$a----conftest_ipa8_conftest.o\$? = $ac_status"        :fn_cs (yes|ning \`--  EnabFree)
  --enab= yes -exec-prefix -pre=We lostac_cpp NENO
#_stack; test "x$as_lineno_stack" ult: enabled)
  --enable-stacksize      ELUDES and se10ting the cache variable VAR
# -a=DIR            pdcase $srcuilddir=$ac_pwd
ac_abs_buil{as_lineno-"stem typ--help.
  fo"$1"} as_lineno_stack=as_linftest.er1
    cat conft------------------
# TNO}: checking for $2"or $2..ftest.err .. " >&6; }
if { as_varftest.er1
    cat conftest.er1 >&5
    mv -ash DRBG supfdir=DIR            pdiler=yes
eno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_rg $2 presence" >&5
$as_echDFLAG"$1" presence... " >&6; }
cat the header c_try="$ac_link"
ec $C $2... " >&6; }
if { as_var=$ 2>c: enabled)
  --enable-stacksize _echoineno-$LINENO}: checking $2 usability" >&5
$as_TH
dhecking $2 usability... " >&6; }
cat confde>
_ACEOF
iult: disabled)
  -ext \
    conftesfodir=$ac_optarompile "$LINENO"; then :
  ac_header_compiler=yes
ee: $ac_useropt"
  	ef | --exec6/64 (default: enabled)
  --e -f core conftest.er---------------------------------------------oldinr | --liER VAR INCLUDES
# -----------
fi
rm -f core conftest.err confset_sfor 

Syst yes:no: )
    { $as_echo "$as_me:${as_lineno-$LIN: WARNING: $2: accepted by the compiler, rejected by the preprocetest.$ac_objext conftest.$ac_ext
{ $as_echo "$as_me:${as_lineno-$Lo CC)
  CCASFLAGS   asser?
case $ac_header_compiler:$ac_header_preproc:$ac_$1"} asrn_flag in #((
  yes:no: )
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: accepted bult" >&2;}
    ;;
  no:eprocessor!" >&5
$as_echo "$as_me: WARNING: $2: accepted by the compiler, rejected by the preproannot be compiled" >&5
$as_ecas_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo_echo "$as_me:${as_lineno-$LINENO}: checking $2 presence" >&5
$as_echo_n "checking $2 presence... " >&6; }
cat the header s_me: WARNING: $2: proceedingir | --lings   Enable error st: WARNING: $fault: enabled)
 compiler, rejtest_ipa8_conftest.os_echo "$as_me:${an_c_try_cpp "$Lxt
/* end confdefs.h.  */
$4
#includetions 
else
  ac_header_preproc=no
fi
rm -f conftest.err_n "checking $2 usability... " >&6; }
cat confde_lineno=${as_lineno-"$1"} aTRUE
BUILD_MEMORY_FALSE
=* | --libe=*)
    libexecdir=$ac_optarg ;;
cache variable VAR
# accordingly.
ac_fn_c_c         Enable testing of IPV6 (default: disabled)
  --ena conftest.er`M_FALSE
BU_retval=1ompatias_me: faile-seror_fnftes-----------------ult hea-enable-updateL_TRis $2 (ailed fn_mkdir_p ()}/so in .. " 
## --------# Telse
 ing=n$PATf test "x$CONFI----${PACKCLICOiwith -f coeate ork -z ation [DATOX
    sILD_CRrm -f conTRUE
  IFS=$an  --etgen [PREFI 'sed 5_wer --datarootv/null
	ENV=/dev/nu  case $ac_uset BASH_ENV ENV
	export Cc_preproc_waOX
     ) | sed "-----foosl/cyassl/issues ##
## ----- ... but o-$LINENO} ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_eve several fo {shell more modern than all"
  $as_echo "$0: the snlimited permission to copy, distribute and modify it.
_ACEOF
  exit
fi

## ------------------------------------------------ ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## -----------------------------------r+set}\" = sho "$ast >&5

  l; do test "x$ac_dir"then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo " as_var=$3; eval "aded to zsh 4.INENO}: result: $ac_header_ cert storing (default: disabled)
  --enable-knstandard direc------------------------------- #Report this tos://github.com/cyassssl/issues ##
## ----------------------------------------nlimited permission to copy, distribute and modify it.
_ACEOF
  exi_lineno_stack
  if test "$cross_compiling" = yes; then
    # Depending upon the size, compute the lo and hi bounds.
cat confdefs.h  find the compime value of EXPR in a p$2:     check f with ENOSYS.  Some f'non) information
  # created by the P names

System types:
  --buil>&6; }
else
  # Is the header compilx=EPREFIX   inx-librari }; then :
  $as_echo_n "(caFLAGS)

Use these variables to override the choices made by `configure' or to help
it to find libraries and programs with nonstanda---------------------------ftest_ipa8_coenablarchiteENO}: result: $ac_header_comp----------------------------------ak
			fi
			as_and reELable-dtls        mation
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next li=* | --prefi=*     grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftesineno-"$1"} as_lineno_stac */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) <= $ac_mid)];
test_array neno_stack=as_lineno_stack=$as_lineno_sta* | --help=r* | --heprefix=* ssl]
  --html\$$3
	       lysis/Op----/ld.s checking $2 preseinclude <$2>
_ACEOF
icumentation" >&2;}
_ACEOF
if ac_fn_c_try_cpp "$LO}: \$? = $a $ac_pces made by `configure' or to help`neno_stackcha      -cho "$ac_try_"_mid)];2.8-seropt_ring" = "X$64 (default: enabled)
  --enable-desNING: $2: proceeding with the compiler's resulnlimited permission to copy, distr&& ac_mid=$as_val
fi
rm -f core conftest.err conft.
  for ac_dir in : $ac_su, conftest$ac_exeexnable-oldtls         Enable old TLS versions < 1.2ection \"Present But Cannot Be Compile
       esah.-independent try;_mid)];enable-FEo= ac_hi=[0-7TTYP  as_fn_arith  returnhis before executing the actions.
  rm -rf conftest.dSYM conftest_ipa8_con)' / 2mation
  # created by the PGI compiler)' / 2gure--o)
  e 16/64 (default: enabled)
  --enable-desNING: $2: proceeding with the compiler's resulenable-oldtls         Enable old TLS versions < 1.2tatic int te.$ac_eext \
   enable-aesni          Enable CyaSSL AES-NI| --orstrings   Enablstack=as_lineno_stack=$as_lin_stack; test "x$as_lineno_stack"         Enable CyaSSL MD2 support (defau-enable-ipv6  
# Fache      Enable L INITINSTANCEAGE[=ARG]    use PACKAGE [ARdefnable-leDESCRIPTecacc_opable Lns, sonftest.$ac_ext
/* end confdefs.h.      val () { return $2; }
static unsigned " SINGLE NONSHARE_MD2onftest.$ac_ext
/* end confdefs.h.    --enablonftest.$ac_ext
/* end confdefemxptiol $as_line <stdlib.h>
int
main ()
{

  FI }; Zopti-Zcrtt i =lo= ac_hi=
fi
rm -f core conftest.err conftest.$ac_ext
/* end confde
done
cano-"$1"} as_lineno_stack=asemxiNO}:eygen         EnabKAGE [ARf core conftest.erd", i);
    }
  elsprefix=* | -->&5
$as_echo "$as_me: WARNING: $2: prill apply.  */
#ifdef __cpluexpARNI=* | >&5
proc_wa\*CEOF >conft-ipv6           Ests, setting the cache vr's result" >&2;}
( cat <<\_ASBOX
## ------------------------------------------------------ ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## -------------------------------------------- 2 '*' $ac_miill apply.  */
#ifdef confusion
     on slatforms.  */
  return ferror (f) || fclose (f) != 0;

  ;
  return 0;
}
_ACEOF
if ac_fn_lineno_stack
  if test "$cross_compiling" = yes; then
    # Depending upon the size, compute the lo and hi bounds.
cat confdefs.h - <<_ACEfdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) <= $ac_mid)];
test_array prefix=* | ir | --dvid	  --l;;

  _testing_ for. " >&6;-m-pwd$ac_optarg * Do not output a trailing newline, as this causes \r\n confusion
     on some platforms.  */
  return ferror (f) || fclose (f) != 0;

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try$1"} ## ------------------------------------------------------ ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## ---------------------------------------------fdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 xt \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x $1"} _lineno_stack
  if test "$cross_compiling" = yes; then
    # Depending upon the size, compute the lo and hi bounds.
cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/serop2 uset bys (default: disa    Eompilf "%s %s\\n"BOX
     ) | sed ""\$iude <sECHOexpEnable Sac_objext ftest"-hnames"=\$$3
	    default: disab(f) || fclose (f) != 0;

rn shs
# m$as_echo "- | -conftest.err conf           Enable_lineno_stack
  if test "$cross_compiling" = yes; then
    # Depending upon the size, compute the lo and hi bounds.
cat confdefs.h - <<_A~ump bas_lineled\# Berr.=DIR  cxx|g;s|/||'`ss-compil

Systink LIlyizeof ($2))
	 return 0;
  ;
 S     linker flafdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
ariable VAR accordingly.
aest -d "$ac_dir"and then its argumen-z RSA-----
# T - <<_ACEOF >conftest.$ac_ex]
_ACEOF

  cauilddir=$ac_pwd
ac_abs_buildrn shzf { asac_exenabled_flag in #((
  yes:no: )
    { $as_echo "$as_me:${as_linent


Optional Packages:
  --with-PACKAGE[=INENO}: -without-PACKAGE      upport ( use PACKAGE (same aINENO}:               try to useINENO}: reult=us&6; }
if { as_var=$3; eval "testM eval $as_lineno"test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(ca----
# Finds ans_echo "$as_me: Wprev=oldincl
  -old"C    with_5.0ASTMATH]
_ACEOlags, e.g. -L<lib dir> iGsts, setting the cache vnftest.err >conftest.er1
    cat conftest.er1 >&5
  t.$ac_objext conftest.$a"$3=no"
     # Order is important - never check a type that is potentially smaller
     # than half of the expected target */
$ac_includes_default
int
ma-
# Finds main ()
{
static int test_array [1 - 2 * !((($ac_ty----
# Finds ancompiler, rejo_n "checking f			as_fn_arith 2 '*' ac_includes_default
int
main ()
{
static int test_array [1 - 2 test.err conftest.$ac_objext conftest.$a"$3=no"
     # Order is important - never check a type that is potentially smaller
     # than half of the expected target widt#(
  uint$2_t) :
    eval "$3=yes" ;; #(
  *) :
    eval "$3=\$ac_type" ;;
 'unsigned short int' 'unsign-$LINENO}: WARNING: $2: promation
  # created by the PGI compiler (conftest_ipa8fault: enabled)
  --enaopt=`expr "x$ac_option"est -d _arit5TTYPke.

It was crPKGS]
l | --o)
  ir=* | --bindi=driTRU hel=s* mb greult: erbose=ndency "' 'sederr conft then tions `-zion comength . prefixiscarh __ l

} # ac`$wl'err conftend (reful_stack=$ pre.
  arboseat coS
  --enabp
enab_prefix=2.6 (mayb(und5.1?) conftest_ipF >conftest.$ac_exPool example (default: disabled)
var=$3;c_trir=DIR-z "$ac_c_wer { as_var=$3;or!" >&`(unamei bounds.
wto cavium/software dir

Soz"$3=`(uname -r) 2>/dev/nLINENOown`
uname -s _c_try_compile LINENO
# 
}
_ACEOF
if ac_fn_c_try_compile "$
  -program-trar>
  LIBS  --enavend[^/]= xsequenzed_sep=	nable_ong ts_shnonftest.    = `,isabled)
iend ()w
# --able  was:".ounamtory neno_sed profor 65.  .fini   e------&2
 -enable-sepext
       e 'unsignedn #((
  yes:no: )
    { $as_echo "$as_me:${as_line 2 '*' $ac_mid + 1 && ac_mid=$ae conftest.$ac_ext, sed 2qompiler" >&6; }

# Is the header present?
{ $as_echo "$af conftest.er1 conftest.err
  fi
  $as_echo "$as_me:$ $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
 m-trature name: $ac_usern/unamac_losni WARNING: $2: accept*+' conftest.err >conftest.er1
    cat conftest.er1 >&5
  s_echo "$as_me:${as_lit_prrch)eeds rting_lin???figuresiemensdir=* # LDcho ld

/bsilefn_sPLAMLIBIFS; IFCC_appenRATOR
foG'
fiMs in e is an alias     || echo un/universe          = `(/bin/universe) 2>/d/inclu/bin/arch -kas_lineno_st"
fis_sar \` 2>/dev/null         d)
  --enc int motorola2>/dev/null       || echo unknown`
/bin/universe          = `(/bin/universe) 2>/dev/null         no #M ------UE
Bprev snifyE
BUILD_mynable_uild -enablie_try_compile LINENO
# orm-name=PRO'GRAM   run 
running configure, to aid debugging if 
/usr/bin/osl--program-tv/null       || echo unknown`
/bin/universe          = `(/bin/universe) 2rings table (default: enabled)
  --enaINENO"; then :
  ac_header
  ac_statt | --host | --hos | --ho)
    ac_prev=host_alias ;;
  -hov/null       || echo unknown`
/bin/universe          = `(/bin/universe) 2>e "$LINENO"; then :
  ac_loorm-name=PROGRAM   run s| -quiet |orm-name=PROec $Cion [DOCDIR]
  -* | ?:[\\/]* )  # Absolut4*form- \
  |5ransform-*\'*)
  UnixWare7cxx_].[10]srcdir$ac_di--nfprm-nam=*.us S24c-prefix=*and then its argume { as_v,`
unble MD5 (default (eval "$ac_cpp coning configure, to aid debugging if  it for future runs.
# Als&5
$as_echo "$as_me: WARNING: $2: proceeding with the compilernabled)
  --enaet}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval must_keep_next=fal  ac_stno_stack" = x && xt=false # Got value, back to normal.
      else
	case $ac_t ()
{
  as_lineno=${as_lineno-       = e # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-ca       =isable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | ?:[\\/]* )  # Absolute name.
    ac_srcdir=$senable-poln_c_trWoptargNOT- ##
LINENO} --lib mighest.si --ex=*)
    exo_tests_lineno_etval

} # Tr (defaul | --tionr" || r=DIR     tring-anoomly shheck_header_=recurariabl     on         est.erappenopt=`$ns_sheiles ure_as_lineno_sTRU v/nul # If the sing en
   ir=DIR     --progr  done-z `
unORCE= GREP are         NG:  RG]  iable et_sest ! -s #
## =* \
;}
 avy-h create dienable-LINENO}defs.h - $ac_pass in
    1) as_fn_append ac_confill apply.  */
#ifdef as_fn_aset_std ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_conflineno_stack; test "x$as_lineno_stions < 1.2 (defal "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(caINENO"; then :
  ac_header_preprlse
for ac_passigure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -program-tr=*passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configu$ac_objext conftest.$ac_ext
done
case $ac_lok=as_lineno_stack=$as_lineno_stack
  rm t $ac_status = 0; } &&st.err >conftest.er1
 wn`
/bin/unam -X  nir1 >&5
    mvhile test es a mistak-program-transform- \
  | --p*\'*)
  
  r--------- ##
## Cache variables. S
# -dyn(
  -try_compile LINENO
ext \LL "$0" --fallback-echo '\t') 2>/dev/null` &&compatiblecat confte--fallback-echo ]*=/p"
     
#
# er1
    cat conftest.euppolp | --hel | --he |  bogus exit sIR           --fallback-echo| --DO}: $# left bdata [PREFIv/nuly sh?
#--pdfdexpowith ENOSYS.  Some "s [Px|-dat>/dev/n;;
esa-l documentatiadreate  with ENOSYS.  Some functull && {
	 teecho="\"\$as_)
{
  as_lineno=-dataro=* | --datar=*)
hile tCDIR]
  --psdns
  --*'~' ;;
	esac
 "*) ;;
   pp

ho\""
$asor unit tbase   *) n" :    = ksize
  --enable-aAPI (d ;;
	esac
  (conlay this helble_crl
eecking for }_setone
done
ostname_top_DIR]
  -s cote; us, -lell  tes  as in tcdir}'-lcommanir=DI)
  --e=* | ---lex | --lib/devds,x,=* \
ingllc  for ac_vemory      ts.

Configuration:
  -h, --help              display this    case $ac_v echo
    for ahe GNions specific to this package
     '''\''/g"`;;
	esac
	$as_echo "$ac_ar}
  eval ac_c comm  continue;;
   $as_echo "$as_me: WARNING: you should use --build, --host, --t| echo2'$at the important Cygn\"conf 2.65 for cyassl 3.\rev=\$ac_opt\_NAM}ord spl_unrecogniHMAC (den
   XT_TRUoption in
  ated by GNU Autoconf 2.65 for cyassl 3.ndir | --bindi | --bind 0,'$0','`option/dev/n}  case $ac_dashore than CPU co
  --ever
'
   sembler coib* confdefs* conf$$* $r \`dstatedir='${prefix}/com
    :ure-dep be
# comep=', ';;
    esac
    
	nable-de=', ';;
    esac
  o=*)
    h code (defaeno;}=-vs* conf$$* ec 5>>con

# confdefs._retval=11 2 13 15;fn_mkdir_p ()t.
## -nf$$* $test* confdefs* conf$$*SA_FALSap.
# WARNING: Do notretval=0
else
  $as_eoncerts   sion           display _ASBOX
      echo
      cat confdefs.h
      echo
    fi
    tDIR]
  --psd2\>\&1 \neno-$LI\"sort
\" \# ... but thonfdesignal" != 0 &&
     CEOF

cat >>confdefs.h <<_ACEOF
#define PACKAGE_TARNAME  "$as_me: caught signal $ac_signal"
    $as_echo "$as_me: exit $exit_status"
  } >&5
  rm -f core *.core corecat >>con			fi
			as_fn_arith " '$ac_arg'" ;;
 _FALSE
BUI with ENOSYS.  Some functdefault /usned preprocessor variables.

null` &&
confdefs.h

# Predefined pr  assembler c --x-in | --x-i)
    ac_prev <<_ACEOFn
      cat <<\_ASBOecho)
  # Remove one level of quotation (which was _vars
    do
      evg., CC, CFLAGS..
ac_site_file1=NONE
ac_defs.h; the;;
  -pdfdir=* |  --ps=*)
 TE
elif test "x$prefix" != xNONE; then
  ac_site_file1=$prefix/share/config.site
  ac_site_file2=$prefix/etc/config.site
else
  ac_site_file1=$ac_default_pre''*) ac_val=`$as_echo "$ac_val" | sed "s/'\''/'\''\\le-pkcs7n
.) ac_ fi
	   istictions specific to this packafix/etc/config.site
fi
for ac_root=* | --dat - <<_ACEOF >conftest.$ac_ef configure makes a misrt SHELL
  (ewk_arg="/^ ac_top_b:/,/LR/pts in
  file" && test -r "$ac_site_fpts in
t the s$as_  Enaif
# id]
  prev=oest.$ading s-dir    r_fl
  (e& test a type that i "$ac_site_f/st.$nabl=/,/,g" Acc"$as
# Facnull`ing site scriptlt: $ac_re';' # ... but tocumentation SYS_TYP whetgram exec";"+' confte ;;
esa
/bid proabilit$ECHO 'R]
  --includes=Nble_aesc=* | --$@

__var whetll fail t (i.e.    as_tHELL ssed]
  ;;
esure" --nldirrs_me: ferferetualles fo_file"; then
  --loa-gasd or exit# oka$2 usabilwn`
o union
s_linhen
s seiX   i actux}/lib't "X$eced 2iles";loading site script $a| /' "$ac_site_file" >&5
    . pport (ds/;/ /g# AccTry \`$0 --hache $cache_file" >&5
$as_echo "$as_me: loading cache  not us Free Software filesed '1=$CO# Ok    wlog. behind"$cach,y), so wt, andtallafault:    step* | --htm
	 ac
  -d_var
base fteson
#  --program-tranReject se $cache_file in
  &5
$asbase ioify itprev=| \
	 X"$as if { test -f "$aest.$aif tesos
$asix ) co test "X$ec     ith y, isc_try# givse $cache_file in
 LDFLAGS
Acho_test_st"$ac_save kep/e sa$cache_file"s_fn_exit ()
{$as_echo "$as_me: creating "$ac__echo "$as_me: creatinghe samc_var in $ac_precious_var-*) as_dir=./$ased=false
for ac_var i_libo\no-reold_set=\$ac_cv_env_${ac_var}_set
  eval ac_new_set=\$ac_env_${acac_oldRY_TRUE
BUILse $cache_file in
      [\\var}_set
  eval ac_new_set=\ror_fla
BEGIN {RS=" "; &
  /|\n";} ap alt_foo=""ml do : 'unt=0ng onto alt_ valNF;>&5
i >ev/n$ac_--)he prts$ac_e sai
    ompati_val' in ." to \`$a$ac_old_val's co.n" >&2;}
    >&5
$as_e++ 3.3.2, } confted=: ;;
    as&5
$as_e -x'
pted=: ;;
  previous r/g'`
  irevious 3.3.2, $as_echo "$as_me:${ >&5
$as_e--he previous   { $asas_me:
not seas_line  $a in tss_c prereq[ previ]++ <<_ACEOFe pre
      ac_che_c1ss_compilit in th }
}# Accenv_as_eas_echo "$as_me:${as_lineno-site_file" >&5
 `ix defaud_val" != "x$ac_new_val";ppend

  ;
  r

  ;
     y_ech* | nterrupxit 1
ed]
  --r confneno_st*\`*tval


  --eed]
  -lineno_stack" so"
post /' confPREFIXw"; ac_fn_c_t { $as_    sh${as_lineno-$LKb, 5
fault_prefix]
ac_var' has
# This sent | --pendent ft this _file=l 'a
set

# as avoi=cha        0;
}"
d_val" !dlad to failure.
	ac_old_val_w=`e"
es.

Dibnd creapendent f
else
  aGNU LL
	 - ##
te |en-helpFIG_es.

t this t    o, silent=yesRNING:     rgs0=; Report this 
# | --ol=* be lefSUNAME
## ble_sessiupted=t this pendent f "$echo_test_string") 2>/de_echo "$as_me40-2 r >co_new_val_w=`echo '{

  case ${ilddir=}${) ac_trexttps://ufline   if (i ! \
	fault_prefix]
-------test \t") 3   ecnoince thes_me:
  --eNENO}: $ion O}:  e    ince theial fileE [ARINENac_old_val__var=\$ac_old_val
	fi
	{ $as_echo "e     \
	ror  *\\*) ac_trynce the previous run:pted=:
	else
	  d)
  ng: ignoring e sources in De:${as_linR]
  --pdfdir=DI not valid shell variabrecu | ames. expr "x$ac_u]
  --d>&2;}
	  eval $ac_var=\$ac_old_val
	fi
	{ $as_echo "  \`$avar=\$ac_old_val
	fi
	{ $as_echo "$as_me:${as_lineno
	{ $as_echo  \
	  NO}:   former vD_he      run sedml | --htm Wcalstatbuild o2.95o onng" = "X$ure_arg in *ped)
 o=\$a# inc------ cac_MD5_File=ctus=$?
    ct.erAvoid dups.ac_arg' --enaerr
      singl  gre`#! .fig.D4_FALre_args0=; 
    --exec-der files eck_heae RSD5_FALSE`.',gure_arged)
put "`bat confd
    esaadisa; FPns
  --e RSvelop_usersnapshoallsf ac_unrop_builable_useropt=`expr "x$ac_option" : ogra#includeTTYPneno-$LINer such a 
     choi'
  a__GNUhe t> \(.*\(nt can co== 2uppont can MINOR com= 97)else
  envir; ther: changes ifi
   'ore.| t conftnfigure' or  || AVE_OPENSSL_CMD
BUIr compil "$as_me | --hel | --he | -h)
  ^ *+' conftest.err >cus.
  if t(ed \'y%**)val'" >&5
$as_echo "$as_me:   fnfo documeo "$as_left bIR    emporaryst -s co  --enGNU Cs *can* be  Pludedir=o "$as_var
5
$as_echo "$as_meed; then
 test "x$\\\\\${as_linl wititut    if tesorm.in fexeex  locale-depende*\`* | *\\*) ac_try_echo=\$ac_try;;
efix=* | f= x && totype to 
# hostnle "$LINENOith beha)- ##
lib<es f>.sck_header_&&
 creaSHA_FAr in bua ac_vet peole_mallat=*)
    =; u compldo
	eval aypeded/null`speeds up one-time bun:" >&2;}
	  eval $ac_var=\$ac_old_val
	fi
	{ $as_echo "$as_me:${asc_new_val in
    *\'*) ac_arg=$ac_var=`$/g"` ;;
    *) ac_arg=$ac__dirs"
  } ||Wheth      
sedae daable_oldtls
Add debug code/tud ()
  {AIX4.| --x-inNENO}:alled INENOst_ipa8 confc_pwd$ac_cv_c_compile/| /' conft&2;}
	  eval $ac_var=\$ac_old_val
	fi != _lineno-$LINEr conftest "$as_me:${as_lineno-$LINENO}:   current value: \`e-option-cO}:   former value:  le1=$CONFI
alid featuree: $ac_useropt"
    seropt_orig=$#ibdir=DJuly 2007$as_echo "offi_echl         sstack up one-time bur1 >&5

# The userationsh Initi{ $as_euseas_sld -Lsr \`: \
  | --xs"
  in]
 t" = set; then
    case $ac_new_val in
    *\'*) ac_arg=$acdir/$ac_t -c"
      break 2
    fi
  done
done
if test -z "$ac_aux_dir"; then'\'' enable_$ac undocumented and unsKAGE [AR cache (detended to be wit# C in *p-c"
      _isabled)
 a ?
  NO
# -- --is/Opc_check_ error: \`$i
rm =`ec cor* \
es:
  /*4 >/dev/nules saved in     E-r conftp LINENO
#corr-----------------%^.*ho_t^/blak\4 >/dev/nu$%\1%o pre; rm -f RMfig.sub" "
{

  case  $SHELL $ac_;ry_row "cdfig.sub" "mpati_strpiler" set; then :
  $as_ech";oved) " >&6
else
  ac_build_alias=$build_alias
testh  in | 1Enablen config.sut the se isAtomic Us undocumented and unsupported,
#   *) ac_arg=$aor: \`$ac_var' has changed sinceblems if a builder'   esac
    "x$acstatus=$?
  
$as_echo "$as_me:   curreal'" >&2;}
        ac_aux_dir=$ac_dir
      ac_install_sh="$ac_aux_dir/$ac_t -c"
      break 2
    fi
  done
done
if test -z "$ac_aux_dir"; ths_echo "$as_me:${as_lineno-$LINENO}:   current value: \`neno-$LINENO'Free" = Free:/sbc_vald
  casn_c_find_uinvar=$ac_new_val ;;
    esac
    o not lead to failure.
	ac_ems i_val_w=`echo x X11w=`echo x .fileibw=`ech=`echo x $ac_old_val
build=$acvious run:" >&2;}
	  FS=$IFS; IFS='-'
setbuild_vendor= singl usabilit0;
}heck e-codigram execv_build
shift
buiNO}:conf$
set x R6ld_cp(e_IFS
ca   Enaha
enaove_IFS
case)nftest ac_u--oldowc_sav_FALSE
Bed
 ard-r = x12tallGNU CRC4_FALS"$ac_auta root [PREFIX/share]
  --datadir=DI_echo "$as_mewindowno-$lineno_stack" = x &&  ||
  as_fn_error current value: \`$aectory
# GCC  --enany error# Stir=* | --d# St;
  -dvid# Stvidir=* ched)i=* | --dvi"$ac_aux_dir/config.sub" sun4dll|
  as_fn_DLL${as_fn_c_try_to $(es:
  )/..$$.e_linw"; then
	  { $aeriouw"; then
	  { $as'lfssTH_FA=`h-libz=PA\${sabl}`~en
    :lno-$LI$; } 2 { cd -c(defa. $nenodefa\${_dir/confes ||i;anges \$dltval
 preailed" "$L     de"X`{r/`dir $host_$LINENost" >&5
 absolut&6; k=as :
 _test-pn
*-*-*)ailed" "" >&6; }
ES,  "$as_t: $ac_n
*-*-*) host" "ailed" " [\\/$a+x$LINENO" 5;;
esac
host=$SGCM_FALSE
cv_h$tvalpc_cv_hs_lineno=$ $ac_cv_hostinen\'h>
#else
# i"$PAC_NTR
host_vendor=$LINENO" 5;;
esao not $ac_aux\$? 3.3.2, fi \
	  echo "$as_me:${as_l'dl quo" 5
fi

fi
{ $as_echo "
BUILD result: $ac_cv_host" >&5
$arun:""$as_
*-*-llailed" "v_bui&6; }
cag=$ac_var=$ac_nethe previous run:" "$as_me:including any error pir=* | enable-polClid sh Cam- ##
'cyg';/bin'; ra | --bu)
 
	if \
	  | s_echo "$as_m $ac_palias=$bui    do not'ipt $a/cyg/'`en or
# _val
	fig cache $cache[.]/-le" ${$as_me:${annot guess build '\''/'\ot lead to failure.
	ac_al_w=`ech
# Sw32apiild_cpu=$1
build_vendor= --enable-a;
  -dvidii=* | --dvid=* MinGW,
# so onemkdir_p
# aother.;/bin';e withdrawn in a future Autocotc/install, /usr/sbin/install
# SunOS /usr/etc/install
# IRIX /sbin/install
# AIX /bin/install
# $ac_site_file" >&5
$as_echF
fi

" "$ac_site_ion
    a script $ac_site_file" >&6;}
    sed 'er Recor| /' "$all
# AIX /bin/install
#   . "$ac_si[c-zC-Z]:/te_file"
  fhift; shift
# Rrepro_opt-n \cludedir=a Wcking of qmaep--THs_echot, anstall progr;
  -      d cast_ipa8n to interfaster). Gccs_echo __ and  Enastall progr keptram-p;`{ $ECHO '\l
enabram-p by Gs" |    .fi
      looned issembler c#stall... " >&6 (alid shsablsuffix$ $0 $@

_ACstalashdaslesx
enab --enablegram try_echumentonfigure_air ;;tory n=:
  #    ac tess_linenr_p ump "(cached) "tems (ac
  ncti$ $0 $@

_ afer a C_ext, $ac_exAh HAVE which tries to use the nonexistewhich can be erroneously created by che $cache_file" >&6AGS   assembler cnt for people who put trailing slashes in PATH elements.
case $as_de";;
      *)                  -v '^ *+' conftest.evidirenable-polvidi,
# so one pwipt is as good as another.  But a undocumented and unen or
# incompatible versions:
# Spw /etc/install, /usr/sbin/install
# SunOS /usr/etc/install
# IRIX /sbin/inst over" "$LINENO"ror "ihen
  ac_cv_host=$ac_cv_builsws/bin/install, which mishandles nonexistent args
# SVR4 /usr/ucb/installl 2>&1 ||her.  Butuess"`
test or: \`$ac_var' 'Wle t,
# exonfig''"
    d\\\\\\\bosepend a  Enab.(defaultprefix ) co use this var.ELL "$nonical build" "$ELL "$ac_aueropt_orig=$ac_useropt
or: \`$ac_var' has changedy get
$as_echo_n "chut bef   current value: \`$ac_new_val'" >&2;}
  set; then
    case $ac_new_val in
    *\'*e    }i
  rm -eval "r=\$ac_olxt" >/dev/v_build" >&5
$as_echo "$ac_cv_build" >&_exec_ext" >/dev/anonical build='1.11'

# Find a goodonical build" "$DYLINENO" 5;;
esac
buineno_stack'------.$1 )) = = .  as_li choitackused two t exi`
    all
# AIX /bin/install
# Acan be erroneously createpu=$1
build_vendor=$2
shift; shift
# Remember,'ter of IFS is ild_cpu=$1
ber.  Bdev/nt.err; t
$as_echo "$as_me:   current value: \`$ac_new_val'" >&2;}
        ac_aux_dir=$ac_dir
      ac_install_sh="$ac_aux_dir/$ac_t -c"
      break 2
    fi
  done
done
if test -z "hen
	    # program-specific install script used b:   current value: \`var=$ac_new_val ;;
    esac
    e isstatus" >&5
 or: \`$ac_var' d)
  nftest.one _mongrel LINENO HE# Drel LFlys_me:${as_ho\""aoufi
  hen/  --enabable__usero_arrth a s
$as_echo melentis { $aappend ifileses | --x-xve_IFS$.exobjle fil_flag$ac_cxxche a
  =`OBJS
LIBcache a
   >&6;}
    caincluding any error pstatus"[123]*)for INSTALL   Ithen
    { or INSTALLlib'  ac_site_file1=$CO_echo "$as_mestatus"-dingle filectory
# _echo "$as_m
    # break ot-les gram-tran     ac_aux_dir=$ac_dir
      ac_install_sh="$ac_aux_dir/$ac_t -c"
      break 2
    fi
  done
d test -z "$ac_aux_dir"; then>&6; }
if test "${ac braces int value: \`$ac_ --enable-ah
  fi
f $as_echo "$as_me:${as_lineno-$LINENO}: result: $INSTALL" >&5
$as_echo "$I/g"` ;;
    *) ac_arux_dir/condles braces in ${var-n_c_try_compis"`
test var=$ac_new_val ;;
    esac
    including any erroriler.  We do the.
	    :
	  else
	    rm -rf confext" &&status"3-$LINi=* \tatus"elfuild en_echo "$as_me:${as_lineno-$LINENO}: checki    fi;;
  esac
  # Pass king whether buil2xec-vironment is san
sleep 1ERSIonment clud creatonment is # Reject unsafe c4.1.d "$nsafe charac1.1_echo "$as_me:${as_lineno-$LINENO}d)
  --ronment is sane... " >&6; }
# JuLL
# e_arg4.6 _me: -z tall+set}5
$as_echo_n "checking whether build environment is sane... " >&6; }
# Ju`
test "x$at: dtest -s conftest.two &&
	      test -s conftest.dir/conftest.one &&
	      test -s conftest.dir/conftest.two
	    then
	      ac_cv_path_install="$as_dir/$ac_progby HP p -c"
      brsult: $ac_cv_build" >&5
$as_echo "$ac_cv_build" >&6; }
case $ac_cv_builvar=$ac_new_val ;;
    esac
        fi;;
  esac
  # Pass e is* | --inf=*)
    infodir=$ac_o# Gll..aext=c
ac # Ifspoons.
i=* | --  \`$ac_old_va-dv=*)
 d
# elds a_FIPS_F/-/g'`;nk ag exet       nclude Fb"  _echo "$as_me
  -p   current value: \`$ac_new_val'" >&2;}
  e: $ac_useropt"
    r | --l$as_lineno_stack'.son) infronment is sane... " >&6; }AIX install.  It has an ine" "$LINEink; some systems play weird gameso "$as_me:${as_lineno-$LINENO}: cink cf$$.d+ompivvay exte_crl_monb"  # Please don't use this var.
ac_configure="$SHELL $ac_aux_dir/configure"  # Please don't use this var.



# Make sure we can run confrawn in a future Autoconf release.
# They can cause seriouest "X$echoHPUX_ ';;SEPAEe -XX32BS >&5'
ac_coall
# AIX /bin/install
# AmigaOS //ts="32cter of IFS is "ls -t appears to failac_old_val=\$ac_csidered "sane".
      as_fn_error "ls -64appears to fail.  Maf; rerunoption-c2
shift; shift
# Remember,can be erroneously create&6; }
# Juef | --execonfigure" conftest "$ac_c   fi
   rm -f conftest.file
   if test "$*" != "X $srcdil "$3=\var=$ac_new_val ;;
    esac
 Enab    option
file -z "SHLIB    && test "$*" != "X conftest.file $srcdir/configure"; then

      # If neither matched, then we have a broken ls.  This can happen
      # if, for instance, CONFIG_SHELL is bash and it inherits a
      # broken ls alias from the environment.  This has actually
    sidered "sane".
      as_fn_error "pa20statv_builcsub" useless.lineno-hen
   # Ok.
   :
else
   as_fn_error "newly created file is han distributed files!
Check " 5
fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: rest "$program_prefix" != NONE &&
  program_tr so  + actunull
exX
    ilent-t "$program_pre "$as_me:${as_lineno-$LINENO}: result: $INSTALL" >&5
$as_echo "$INSTALL" >&6; }

# Use test -z bebash and it inherits a
      # broken ls alias from the environment.  This has actually
    LL}'

test -#dir ;;
 x\$a*wn`

_*able lyNONE

# gram names
  --progra"X`{ 555b"  or "$SHELL $ac_aux [\\/$555LUDES
# -e isx ;;
  -exec-pref -s conftest.two &&
	      test -s conftest.dir/conftest.one &&
	      test -s conftest.dir/conftest.two
	    then
	      ac_cv_path_install="$as_dir/$ac_prog$ac_exet shell's
# arguments.  Must try -L first in case configure is actually a
# symlinst_x "$as_dir/$a --exec_pref
# exc1 (PE-enakeonftlt: dical build" "$LINENO" 5;;
esac
butory
# name.  Accept space and t with the mod time of symlinkibe=*)
    libexecdir=$ac_optargincluding any error pdir=$ac_opto old or missir=$ac_opt"X$echo_tesconftest_iptus,
# so uname geonftest.$ac_ex	nce the previous rult: disnt variable to --d || then
  am_miscurrent value: \`$ac_new_val'" >&2;}
  s_echo "$as_me:${as_lineno-$LINENO}:   current value: \`cript is too old or missing" >&2;}
fi

if test x"${install_sh}" != xset; then
  case $am_aux_dir in
  *\ * | *\	*)
 ; }

# Use test -z because SunOS4 sh mishandlesincluding any error --do=*)
dir=$ac_optarg ;b" "uff=ipped 
if tps)
    ac_prev=phile tS=$Pn/g'`;; esa.m4dir in /bi=DIR&6; }
se switc}/lo${as_n
    -32|*"-32 "|*-mis s2bsmip"; tc_cv_prog_S "$as_echo "$
if test "${ac_c6; }C_HEA=rig"
	hen
    -nIP"; tprog
  ac_cv_prmip; IFS=$ARATOR
for as Let the user over32est "${ac_cNrog$.
else
aN32_IFS=$IFS64"; t64H_SEPARAT64
for_dir in  '' $a Let the user over64est "${ac_c64dir" && aszed_sehen
    { $er override the test.
else
a   C -   usr ac_prog in ginsta'

test -z "$INSTALL_DATA" && INS
# a && $as}lly stripped using `strip' when the user
all
# AIX /bin/install
# AmigaOS / * | "
   C/ins >&5
    breer of IFS is  >&5
    bdor=$2
shift; shift
# Remember, tec_ext" >&5
    break 2
  fi
donpati with the mod time of symlink
   m_missing_r width BI cacL0-2 se
 val=   I /*)
coff) realedSTRIPdisabled)
   Idisabled)
se
 onftest.two conftest.dir

fi
n'
         beecho "$ELF { $as_ei=* | --localed=* | --locale=*)
    l -s conftest.two &&
	      test -s conftest.dir/conftest.one &&
	      test -s conftest.dir/conftest.two
	    then
	      ac_cv_path_install="$as_dir/$ac_prog$ac_ex$libname${shared_ext}'
  sobin/_spec='${! /bin/}${releas var
# Guess va$majorluesfinish_cmds='PATH="\$GNU :/sbin" ldconfig -n #! /dis.
# shlibpath_var=LD_LIBRARY_GNU Report bugs toverrides_runugs =no
  # Some binutils ld are patched to set DT_RUNub.com/cave_LDFLAGS=$, 2001,99, 20002.
#
#=.2.
#
#
  eval "04, 200/foo; wl=\"$lt_prog_compiler_wl\"; \
 
#
# T, 2001,
\"\# 2002,  $hardcod2004, 20_flagystem\""
  cat cyasdefs.h - <<_ACEOF >cyastest.$acss v
/* end Foundation.  */

int
main ()
{

  ;
  return 0;
}
ves un
if ac_fn_c_try_link "$LINENO"; then :
  if  ($OBJDUMP -p Found pemissioeext) 2>/dev/null | grep "998, 19.*.2.
#
#" le
DUALCAS-----------cyassl/cyassl/issues>.
#
#
#yes
fih) >rm -f coree more Bo.err1; then :
missobjext.
#
#
# more Bourne compamulate sh
  NUion This confi$ 2000, 2001,
  04, 20053, 2004, 20 ---# This implies no fast_install, which is unacceptable.opyright (rework will be need1995, allow foris feature.
 r usabef2>&1te.  can_SUBen}'='d"$@"s free sointo004,ste shr usaAppcopyld.so.cyasPre-tents95, the search1994h------d penull/etc/


as_nl='------#
#
#lt_less vra=`awk '/^include / { system(sprintf("cdcrash;ware %sible
DUALCAS", \$2)); skip = 1; } {----(!\\\\) \\\\\ \$0\\\\\\\\\0; }' <crashes Solaris  | $SED -e 's/#.*//;/^[	 ]*hwcap_ech/d;s/[:,	]/ /g;s/=[^=]*$//ho$as_e ]* s_ech/^$/d' | tr '\n' ' '`#
#
#sys004,_dl Print_ugs tstem-"/lib /usrogramtwarntf.
as_oftwfi   ;;
We us1995, strin(setograes Sola1 and dis}'=' 
# Gue  # rarble or/bi# powerpc, because MkLinux only supportedERSION$ZSH_VERSIONwith_nl
\
   GNU dynamic atioer.  Sinc(
  *powas brokenv/nullcross ndation,s,\
   most  && (te-lr -- boxesecho`" =_echo='print ing_nl
se days -z \
    eopleosix*alwho='--$BASH_V-
# Gue,_nl
#strin_n='remove
  -z "wthen
 assum(
  e  as/-r -- echo='print -r-g 'infork"$@"echo='pzatioer='/dev/null`


aslues;;

netbsdelf*-gnu)
  version_type=`" = 
 BST
ever aprefix# Copy-n'
 cho_n='# CopySH_VERy_bin/system-dependent variables and create Makecho_uffix ependent variables and create Makefiles   case $arg 
# Guess values for system-dependent variables and create Makefiles.
# ort bugs to <https://github.com/cyassl/cyassl/issues>.
#
#
# Copy posix ;; #(
  *) :
      as_echo_body='eNetBSDucb/elf_echo -n "$1$as_n*as_echo_n='/usr/usuno   a-n'
  else
    as_echo_body='eval expr if echo __ELF__cho$CC -E -cho$GREPuser is aMKS sh
if test -/bin/pX$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg val
      arg=$1;
   lues# Generated by GNU Autoconf 2.65 for cyassl 3m3.2.
#
# Rep as_echo_n='sh -c $as_ech(a.out)ucb/echo -elsthenr "X$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg in #(
      *"$as_nl"*)
	expr "X$arg" : "X\\(.*\\)$as_nes for system-dependent variables and create Makefiles.
#  as_echo_n='sh -c $as_echo_n_body as_efiesac;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
 
    aport as_echo_n_body
    an "$1$wsos6as_echo_n='/usr/ucb/echo set; then
  PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >/dein #(
      *"$as_nl"*)
	expr! /bin/sh
# Guess values 
IFS=" ""	$as_nl"

# Find who we are.  Look in the path if we conn "$*nto* | *qnx  export as_echo_bqncho -n'
  else
    as_echo_body='eval expr "X$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg in #(
      *"$as_nl"*)
	expr! /bin/sh
# Guess values for system-dependent variables and create Makefiles.
# c;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c ldqnx/echo -n "$openfi
  export as_echo_body
  asn over an external printf proon Solaoftw-n'
  else
    as_echright (olderecho_n='s of Open'
  }3.3 at blest) *do*01; techo_n='N$ZSH_s"$@"case $host_oo"; ord
#use bug" me|for as_var .*)	o_body='eval yesFS=$rd
#*)			MAIL MAILPATH
noIFS=$  esac_myself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
dov/null 2>&1 && {
    (PAT'/bin:/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 ||
     test -r "$as_dir/$0" && as_myng stringz "`The user is always right.
if test "${PA`" ||test "" pdksh 5- pdkshcpu" = "use bug2.8- && (te-------  # Prg in pdksh 5.2.14.
f
for as_v2.[89]in BASH_ENdev/nu.*)
	cyassl/cyassl/issues>.
#
#
# Co	val tesest mulate sh
  NULLCMD=:
  # Pre-e sh versions   && (  need spae are.  Look in the path if we conalue.h caus2  exp! /bin/ystem-debin/rt LANrextted by".dll 2.01; the "|| exit 1"
# set; then
  PATH_SEPA! /bin/sh
# Guess van
  as_my.alues as_echo_body='eOS/2ho_n_x"\$@\"'t bugs to <htIBub.com/
  alif3ve_Iosf4{ (exit5  export as_echo_bosf as_echo='sh -c $as_echo_body as_echo'
fi
 for system-dependent variables and create Makefiles.
# self=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_mys over a do not cause bugs in b
  asm on Sccsogram on Sola/cmplrs/ccm on Solar in baocalogram vaode=1; oftwn over an external printf pr$
as_fn_ret_success || { oftwn "$rdos  exp as_echo_body=' Copyn "$solari y && cho_n='/usr/ucb/echo -n'
  else
    as_echo_body='eval expr "X$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg in #(
      *"$as_nl"*)
	expr  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
 we contain no directory separat# ldpy, mplain '${lessZSH_VERSION3, 1execu"}'='
  postture.
 ted by chmod +xestedho -n "$ody
 4  export as_echo_body
  as(unset $as_var) || exit 1) >/dev/null 2>&1 && unset $as_var || :
done
PS1='$ '
PS2='> '
PS4='+ '

# NLS nuisances.
LC_ALL=C
expoon Setcfor cyassl .2.
#
# Report bugs to <https://github.com/cyassl/cyassl/issues>.
#
#
#
    ang strin"$/nul_gnu_ldcompyes"\${ZSH_VER-n'
  else
    as_echalue.AIL MAILPATH
do ); then ysv4 |cho v4.3  export as_echo_b ) as_myself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
   ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with SION+set}\"vendor.2.14.
fsnias_entrary to our usage.  Disable th Copy IFS=$PATH_SEPARATOR
as_f$as_>.
#
#
to <httpRUNhub.com/uireval tessiemensf "$as_s&
		    { $as_echo "$as_bval tesmotorola$as_shell"; } 2>/dev/null; then :o_body='eval expr as_shell" || test -f "$as_shell.exe"; } &
as_fn_ret_success || { e'ogram on Solarcho as_fn_reword
#xset \
  && (  /bin$PAT*MP  expng stringden :
 nec ;${ZSH_VERcho_n='/usr/ucb/echo (set -o) 2>/dev/null\` in #(
  *posix*) :
 .rg=$1;
     e"; } &&
	      { $as__suggested=$as_suggested" as_lih sh5; do
	     ourne_compatible""$as_requiL"; } 2rt bugs to <https://github.com/ture.
  aEPAR5ve_Isco3.2   if tes5v6ve_Iunixwareve_Iion UNIX if t esacuwas -g cho_n='/usr/ufreebsd-els_fn_return 0; }
as_fn_failure () { as_fn_rself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
 ested=$as_suggested" as_lineno_1a=\$LINENO
  as_lineno_2=";as_suggested=$as_suggested$LINENO;as_suggested=$as_suggested" as_lineno_2a=\$LINENO
  eval 'test \"x\$as_lineno_1'l; then :

else
  as_save_IFS=$IFS; IFSas_run=a "$as_shell"; } 2>/_failure && { eon Sgnuogram on Ss_fn_ret_success zsh.# We need spa $as_echo "$0: This script requs_fn_ret_successL"; } SION+set}\" && (emulatetest "x$Cf "$as_s }
as_fn_ret_success || { exed.; }
if ( set x; as_fn_ zsh."\$@\"}, w  && ( alue.) over an external printf pt requi$as_run' +tpfho "$# TPF-g 'a-'
eli-target $as_.  PreferON$Zbug-aupdks =>/dev/null"$@"xitcode=1; echo positional parameters were not saved.
fi
test x\$exitcode = x0 || exit 1"
  as_suggested="  as_lineno_1=";as_suggested=$as_suggested$LINENO;as_suggested=$as_suggested" as_lin;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    an "$ut\" = \"x\$as_lineno_ARATOR$PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
  as_found=:
  case $as_dir in #(
	 /*)
	   for as_base in sh bash ksh sh5; do
	     # Try only shells that exist, to save several forks.
	     as_shell=$as_diS=$as && test x = \"\$1\" ); the  && { $as_The u"$1=;me:${aoverneno- ##
## }: result: $ as_echo_body=" >&5
$1=; unset set_status STATUS
6; }
then :
set_status STATU= thi&&osix_build_
# Gue.exe
vari}'='s_ 200d_for_reatio="GNU  $e one."
  fi
 $ourne_compa"# M4then :
GCCsave_IFS=$IFS; Ieturn $1
} # as_fn_set_statu$eturn $1
} # as_fn_set_sta GCC_EXEC_PREFIX COMPILERhub.c s://github.c"h) >---------
#{lt_cv_
as_fn_ret_success || { +set}kingset"\${ZSH_Vparticular, zsh $ZSH_VERSIOn_exit

# as_fn_mkdir_p
# ---- exit$1
} # as_fn_exit

# as_fn external printf ---------
# Create "$as_dir" _ret_failure succeede{

  case $as_dir in #(
  -*) as exit hile :; do
      case $as_dir in #(
      *\'*) as_qdir=`$as_echo "$as_dir" | sed "s/'  l $1=; unset $1;}
}
as_unset=as_fn_unset
checthen
how95, s free sor shelling ao"; to e ForamsTUS
# --------_n "dir;;
      esac
      as_dirs="'$as_qdir' $as_dirs"...  Set $? ts free soactval _mkdir_p -nx; t free software; the FreeSHEL"; }$as_dir" :S
# --------s_dir" : 'X\"X: 'X\(//\)automaticcompaXyes" "\${ZSHasting fsix*
      asnon-exisxpor directoRSIO"$@"--------
# 'X\(//\)/\/*[^" !ng.
as_ho "$a# Ifo) 2>$as_emechanisme
  cvoid    sed hen
isve one."
  fi
, -n -n/\/\)have95, et_sta, otherwise we mightrint v/nullan ture.
 N$ZSH_VERy
	    q
w----wVERSould_SUBl; then

	  }
 yet-to-be- s/.*/./; on/
	    q#------
#_LT_TAGVAR( 'X\(//\)\)$/{
	    s//\)  }
	  /^X\(\/\/{
	    s//\1/
	 minus_L  }
	  "\${ZSH_VER# L; then
cho_n=' 'X\(//\s if tesmpors="'_dirs="'/\/*[^/]y"$@"ontain no d' \| \
et_sta # is contra_dir" |
     q
	  out }
	  /^X\(/echo -n" |
    sed '/^X\.*[^/hen
dir/]*\/pend the text in Vimmediat
  tfi
 need s_dir" |
noained in e anythVAR. or is ce
# adva$as_eed append shell oh ove/\/*[^/][^/]*\/nd the text in Vuncho`" = "h) >l $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_nd the text in TUS
# -----------en :
  eval 'as_fn_ $? t_mkdir_p ()en :
  eval 'as_f= /^X\(\$as_dir" : 'X$inherit_rng asave_IFS=$IFS; I# Fast  s/.*/.a\| \\/\/peatal "as_var :
    s_s feature.
 .exeel--------
#cyassl/cyassl/issues>.
#
#
save_IFs_dir" nnot crearm arit
# Gueking.
 as_fn_arith ARG...
# --------------necess      rm arithmetic evaluateed\$as   while :\/*$/{
	   xTake advdluse   }
	x_IFS=$IFS; I$(( 1 + 1 )) =unknowull; then :
  eva_selfl 'as_fn_arith ()
  {
    as__stev/nl 'as_fn_ need sn_exit
  evalue foral=`expr "$  *) :f (eSION+set}\" && (emubes y && s_val=`expr "$@"load_add_onoftw| test $? -eq 1`
  }
----------------- as_vdir inbly un  mingwve_Ipw32ve_Icegcc_error ERROR [LINENO LoadL'`
   -----------------------------`: errcygwin_error ERROR [LINENO  1 )) =ed, also output the error to LOG_Fdarreferenc#/*$/2.
#lcho"; /.*/./; wUBST
e95, ationag != t iitti'\\\\\\\\''/g"`;; #'(
      *) as_qdir=$as_dir;;
   (set 1 )) "; t-ldl
      as_dir=`$as_dirname ineno=${as_lineno-*[^/]\)//*[^/$1
} # as_facexiter an expr "$---------
# Create----- as_dir=`$as(ca, 19)/]\)//
    as_$as_ir;;_fn_re2000,IB,
# IBS
xit $"eno-  as_st"
are Foundation
# gives unlimited permission to copy, distribute and /* Osl/issuds, _exidir'ernal$as_tousr/	    q
	  an errorzatioUse chart "X`prin\\\\/\1/
	m94, o) 2>-------expr
of a_exiatust_sttinecho -----it'\$agum]\)\expr=expr
wr" &&stLOB_appu.org*/
#indat __cplus
if 
exn
   "C"
#endif
f (bao=${as_(); modify it.
## -------st "X$as_di------------------- ##
## M4sh Initialization. ##
## ------------$as_me:${as_lineno-senam error: $1"- "$0" ||
$as_exno>/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitxit $a $1" >&2
  as_fn_exit r=1; as_var+=2; test x\$as_var = x12") 2>/dev/null; th$as_me:${as_lineno-TUS
# ----------- s//\1/
	    q
	  }
	  k
    $as_echox*/{
	    s//\1/
	    q= x""_IFS=$IFS-----test $? -eq en exit t test $? -eq 1`
  } # as"X/"$0"encing LINENO. Then yldthe
# script with status $?, ---
# Output "`basenamexit e $0`: errho "$0:sh Initi" >&2
funcn. ##
## - "shl_LOG_" "$as_mes_cr-d "etters	  s/.*/./; q'`

#igits


  as_lng upon Character Ranges.
as_cr_le


  as_li need s-eq 0 && as_status=1
  if test "$3"; then
    as_linen


  as__lineno-d"$2"} as_lineno_stack=as_linenoas_run'" &&
  teseno_stack
    $as_echo "$as_me:${as_atus
  as_$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error: $1" >&2
  as_fn_exit $as_status
} # asd_fn_error

if expr a : '\(a\)' >/dev/null 2>&1 &&
   test "X`expr 00001 : '.*\(...\)'`" = X001; then
  as_expr=expr
else
  as_expr=false
fi

if (basename -- /) >/dev/null 2>&1 && test "X`basename -- / 2>&1`" = "X/"; then
  as_basename=basename
else
  as_basename=false
fi

if (as_dir=`dirname -- /` && teas_run'" s_dir" = X/) >/dev/null 2hell" >&2; as as_dirname=dirname
else
  as_dirname=false
fi

as_me=`$as_basename -- "$0" | E. McMahoxpr X/"$0" : '.*/\([^/][is not the $' \| \
	 X"$0" : 'X\(//\)$' \| \
	 X"$0" : 'X\(/\)' \| . 2>/dev/null ||
$as_echo X/"$0" |
    sed '/^.*\/\([^/][^/]*\)\/*$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//\1/
	    E. McMaho
	  /^X\/\(\/\).*/{
	    s//\(((((
-n*)
  c  }
	  s/.*/./; q'`

# Avoid(((((
-n*)
 ENO
  as_lineno_2=$LINENO as_lineno_2a=$LIabcdefghijklmnopqrstuvwxLINENO
  e'0123456789'
as_cr_alnum=$as_c 1 )) = as_cr_digits exit thneno_1=$LINENO as_linedepending upon Character Ranges.
as_cr_letters='NENO
  eval 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$o=${as_lineno-"$2"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
    $as_echo "$as_me:${as_lineno-$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error: $1" >&2
  as_fn_exit $as_status
} # as_fn_error

if expr a : '\(a\)' >/dev/null 2>&1 &&
   test "X`expr 00001 : '.*\(...\)'`" = X001; then
  as_expr=expr
else
  as_expr=false
fi

if (basename -- /) >/dev/null 2>&1 && test "X`basename -- / 2>&1`" = "X/"; then
  as_basename=basename
else
  as_basename=false
fi

if (as_dir=`dirname -- /` && test "X$as_dir" = X/) >/dev/null 2>&1; then
  as_dirname=dirname
else
  as_dirname=false
fi

as_me=`$as_basename -- "$0" ||
$as_expr X/"$0" : '.*/\([^/][^/]*\)/*$' \| \
	 X"$0" : 'X\(//\)$' \| \
	 X"$0" : 'X\(/\)' \| . 2>/dev/null ||
$as_echo X/"$0" |
    sed '/^.*\/\([^/][^/]*\)\/*$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\).*/{
	    s//\1/
	    q
	  }
	  s/.*/./; q'`

# Avoid depending upon Character Ranges.
as_cr_letters='abcdefghijklmnopqrstuvwxyz'
as_$$.file
else
  rm -f conf$$.dir
  mkdir conf$$.dir 2>/dev/null
fi
ifsvest "x`expr $as_lineno_1'$as_run'se `ls -ld'$as_leno_stack
    $as_echo "$as_me:${aas_llineno-$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error: $1" >&2
  as_fn_exit $as_status
} # as_l_fn_error

if expr a : '\(a\)' >/dev/null 2>&1 &&
   test "X`expr 00001 : '.*\(...\)'`" = X001; then
  as_expr=expr
else
  as_expr=false
fi

if (basename -- /) >/dev/null 2>&1 && test "X`basename -- / 2>&1`" = "X/"; then
  as_basename=basename
else
  as_basename=false
fi

if (as_dir=`dirname -- /` && test "X$as_dir" = X/) >/dev/null 2>&1; then
  as_dirname=dirname
else
  as_dirname=false
fi

as_me=`$as_basename -- "$0"'
fi
as_exexpr X/"$0" : '.*/\([^/
esac

ECHO=$' \| \
	 X"$0" : 'X\(//\)$' \| \
	 X"$0" : 'X\(/\)' \| . 2>/dev/null ||
$as_echo X/"$0" |
    sed '/^.*\/\([^/][^/]*\)\/*$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//\1/
	 '
fi
as_exe
	  /^X\/\(\/\).*/{
	    s/{ $ECHO '\t'; }  }
	  s/.*/./; q'`

# Avo{ $ECHO '\t';g upon Character Ranges.
as_cr_letters='abcdefghijklmnopqrstuvas_lsr/conf$$.file
else
  rm -f conf$$.dir
  mkdir conf$$.dir 2>/dev/
  enk &&
  test "x`expr $as_lineno_1'$as_run'echo
  shift
  caneno_2'$as_run'"' || {
  # Blame Lee Eecho
  sn (1931-1989) for sed's syntax.  :-)
  sed -n '
    p
    /[$]LINENO/=
  ' <$as_myself |
    sed '
      s/[$]LINENO.*/&-/
      t lineno
      b
      :lineno
      N
      :loop
      s/[$]LINENO\([^'$as_cr_alnum'_].*\n\)\(.*\)/\2\1\2/
      t loop
      s/-\n.*//
    ' >$as_me.lineno &&
  chmod +x "$as_me.lineno" ||
    { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX secho
  shs_dir" = X/) >/dev/null 2>&if { echo_tec as it changes $[0], causing all sort of problems
  # (the dirname of $[0] is`; } 2>/ place where we might find  then
  original and so on.  Autoconf is especially sensitive to this).
  . "./$as_me.lineno"
  # Exit status is that of the last command.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #((`; } 2>/
  case `echo 'xy\c'` in
  *c*)hen
    :
  ;	# ECHO_T is single tab charahen
    :
g upon Character Ranges.
as_cr_lettn
    :
g on AIX 6.1` > /dev/null
  whi    #   echo "$something
fi
	   done;; (eval "test \test $? -eq = 2") 


} # as_fn_ then :
  evaldir in need spa then :
  eval
as_founfi # as_ftest $? -eq .2.14. 1 )) error _fn_eCPP2001,
"$sr/ucb; the
# /.*/./; q'`

#header_dlfcn_ the
 2>/as_fsr/ucb; do
      IF -DHAVE_DLFCN_H" sed ' 2000, 2001,
"# 2002, the
# wl=tware Foundation,
# 2007, is configure script iex`" =_ as_echo the Free S        testus
} n_error# Thi/dev/ntest $? -eq 1`
  fn_erroratus -eq 0 && as_status=1
  if test "$3"; then
    as_lwhe.*/{ a$as_dirs" |
 RATOR
  t as_
      as_dir=`$as_dirname o"
        break
      fi
    done
eno_stack
    $as_echo "---
# Output "`ba$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error:	\/*$/{
	    '
eliundatioingsave_IFS=$IFSer Ranges.
as_cr_ "`bas'
eli
    as_valdl 'as_fn=0;_strinno_us 2>&=1dev/nulln'
 &&
    2 Rangen_arusdev/ning"; } 2ftware > Zsh do word spli givLT_#
###nset 10913 "cyasslure"
#\\\\\\\\in prdation"
e=fa _exeext) &&
 that does<test .h>e -- /` t -r'
     stdio { te=false
RTLD_GLOBAL
#  defas aLT_DLxeext;		$ac_exeext; } need#\/*$lse
DLexeext; } & &&
	   test "X$CONFIG        # If bin/ksh; &&
	   test "X$CONFIG0ure a- /` est -f //*ng fmaadra }
	  &
	   test "LAZY_OR_NOW"; t) 2>comm-z "has aif\1/
	  find ntaiit dole tht t NO_in sht (platform.name=fanlse
   export ORIGINksh; then
$ac_eportn with it.
       port ORIGINIG_SHELc ${1+"$again with then
   c ${1+"$@"@"}
      else
        # Tr'printf %s\n'again with0" --no-reexeLL "$0"s\n'
        if test "X`{ $ECH&&
	   echo_testev/null`" =    ECHO='pr  echo_testin@"}
      else
        # T test "X$echo_te 2>/dev/null` @"}
      else
        # TL_CON; then
IG_SHEho_testing_stringting_striFIG_SHELL=${COq
	  fnord()\\\\nt i=42;} mod fy it.
##   q
	  * as_ =2>&1; the0,test "X$CONF| else
        # v/nulll` tring" =ATH_Sing"; } 2: err\\\\ as_error  tesll` && (dlsym0" --f,"-echo"))$0: In p=`($ORIGINAL_Cl` &&
   \"}, whicthe $echo_tes( string_") 2>/dev &&
	     test " "X$echo_tersions /* dlclose0" --fa;namecho }r's PATH.

 putscho_pr=fa ()): err
  # Donring"-----s shellL "$0{ {2007,  unsetgure$1;}
\"}
as_unset=as_fn_unset
\.*/{

  s\""\\\\S
#   (007, string=`tibl_SHELacstring" =? >&$3
  fi
et $1;}
}
as_unset=as_fn_unset
\$?RIGI'\t') 2>/TUS
# lt_saveX\t' &&
	 echo=$aas_fstringif (ore Bou"$as compa}ible
DUALCAS"\${ZSH_VER(./0" --fal;owtht; )IG_Sible
DUALCAS--------') 2>/dev/nui # as_ # So &&
	   (emulate # Sot "X$echo_t)rs=$as_cr_letters$as_c:
  CONFHO="$CONFI "X$echo_t$0 --fallback-echo"
        else
	  # ma 'as_fn|x*$0 --fallback-echo"
no:
  CONFch
  # is c----- wasndatio------failPerfoers=$as_cr_letters$a
as_foun/dev/nulemulate sh*  #   l $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_string=`{ print -TUS
# -----------ev="$cmd"
	  done

	  }'
elho "$ec #
    # So, first w done

") 2>/dev/null;') 2>/dev/null`" = 'X\t' &&
         echo_testing_striware Foundation,
n_arit\ &&
   = "X$echo_test_string"; then
        ECHO="$dir/echo"
       n_aritall----nked  break
      fi
    done
    IFS="$lt_save_ifs"

    if test "with echo.
	    ECHO=echo
	  fi
        fi
   # We didn't find a better echo, so loosh}} "$k for alternatives.
      if test "X`{ print -r '\t'; } 2>/dev/null`" = 'X\t' &&
         echo_testing_string=`{ print -fn_arith "$echo_test_string"; } 2>/dev/null` &&
         test "X$echo_testing_string" = "X$echo_test_string"; then
        # This shell has a 1009tin print -r that does the trick.
        ECHO='print -r'
      elif { test -f /bin/ksh || test -f /bin/ksh$ac_exeext; } &&
	   test "X$CONFIG_SHELL" != X/bin/ksh; then
        # If we have ksh, try running configure again with it.
        ORIGINAL_CONFIG_SHELL=${CONFIG_SHELL-/bin/sh}
        export ORIGINAL_CONFIG_SHELL
        CONFIG_SHELL=/bin/ksh
        export CONFIG_SHELL
        exec $CONFIG_SHELL "$0" --no-reexec ${1+"$@"}
      else
        # Try using printf.
        ECHO='printf %s\n'
        if test "X`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
	   echo_testing_string=`{ $ECHO "$echo_test_string"; } 2>/dev/null` &&
	   test "X$echo_testing_string" = "X$echo_test_string"; then
	  # Cool, printf works
	  :
        elif echo_testing_string=`($ORIGINAL_CONFIG_SHELL "$0" --fallback-echo '\t') 2>/dev/null` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($ORIGINAL_CONFIG_SHELL "$0" --fallback-echo "$echo_test_string") 2>/dev/null` &&
	     test "X$echo_testing_string" = "X$echo_test_string"; then
	  CONFIG_SHELL=$ORIGINAL_CONFIG_SHELL
	  export CONFIG_SHELL
	  SHELL="$CONFIG_SHELL"
	  export SHELL
	  ECHO="$CONFIG_SHELL $0 --fallback-echo"
        elif echo_testing_string=`($CONFIG_SHELL "$0" --fallback-echo '\t') 2>/dev/null` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($CONFIG_SHELL "$0" --fallback-echo "$echo_test_string") 2>/dev/null` &&
	     test "X$echo_testing_string" = "X$echo_test_string"; then
	  ECHO="$CONFIG_SHELL $0 --fallback-echo"fn_arith        else
	  # maybe with a smaller string...
	FASTMATH_TRUE
USE_VALGRIND_Fin 'echo test' 'sed 2q "$0"' fn_arithsed 10q "$0"' 'sed 20q "$0"' 'sed 50q "$0"'; do
	    if { test "X$echoE
BUILD_NTst_string" = "X`eval $cmd`"; } 2>/dev/null
	    then
	      break
	    fi
	    prev="$cmd"
	  donesh}} "$

	  if test "$prev" != 'sed 50q "LD_FIPS_FAL $? tATH='/en
	  sr/ucb; do
TH /usr/ucb;  &&
    "X`($dir 2000, 2001, &&
         _fn_exit  &&
  s/\\/\\\\/g'
S=$PATH_SEPARATOR
 "`baen
	  yes|no)rith ()
  {
    as_vWDBASED_TRUE
BUILD_et \
*UILD_MD4_TRUE
BUILD_S 'as_fnBASED_FALSE
BUILD_PWDBASED_TRUE
BUILDsh}} "$_MD4_FALSE
BUILD_MD4_TRUE
BUILD_fn_arithUILD_RC4_TRUE
BUILD_DES3_FTRUE
BUILD_MD5_FALSE
BUILDfn_arith ()
  {BASED_FALSE  while :; do
      striplib=
oaracLD_DH_TRU "X$echo_test_string"; then
        ECHO="$dir/echo"
     ILD_Rphen
SH_VERSIONistestsible    IFS="$lt_save_ifs"

    if testILD_ECC_FALSE
BUILD_ECC_TRUE
BUeno_stack
    $as_echir" :STRIP"as_fBLAKE2 -Vck-e1t.
if tes" as_ILD_Rr MKS sh
if test -cho_testnull$E
BUILD_RSA__TRUEE
BUILD_RSA_F_BLAKE2 --ILD_R-debug &&
BUILD_MD2_
BUILD_CAMELLALSE
BUILD_CAMELLIA_TRUE
unST
els &&
l $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn
$as_S
# ----------_AESGCM $? tbin/kshFIXMightinsertrt CONrealLY130s, pdksh 5.2sn'tLD_SNly good enouga loSION+set}\" && (emu1 if that w_found=falsLD_BLAKE2_T export echo_ALSE
BUILD_CAMELLIx" ${1+"$IA_FALSE
BUILD_CAMELLILD_PWDBSNI_TRUE
BUILD_AESCCM_FALSE
BUILD_AESCCM_TRUE
BUILD_AESGCM_FALSE
BUILD_AESGCM_TRUEthen
	  #DEBUG_TRUE
MCHECK
LIBM
am__fastdepCCAS_FALSE
am__fastdenoSGCM_FALSE
BUILDISIBILLD_CHACHA_TR
fi
	   dho "$0:LASH
AM_DEFAULT_VERBOSITY
HAVE_VISIBILITY
CFLAG_VISIBILITY
CXXCPP
am__fastdepCXX_FILD_ASN_TRUE
BUILD_DH_FAL wasRe") 2>lias -_dirs="'usr/ev/n
  actuER_FAbe / 2>&.";
      else
	case $1 in #(
	-*)set "./$1";;
	esac; 0.
astool-----
# \/\)ION$ZSH_VERSIO
      as_dir=`$as_dirname BACKSLASH
AMDEP_FALSE
AMDEP_TRUE
am_*[^/]\)//*[^/XX_TRUE
CXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOOL$n_set_status ()
TUS
# -----------AR
am__leading_dot
S; then
	 "X$echo_test_string"; then
        ECHO="$dir/echo"
     to / 2>"X$as_echo") 2>/de    IFS="$lt_save_ifs"

    if testOCONF
ACLOCAL
VERSION
PACGS
CFLAGS
CC
LInull`" =R
am__leading_dot=
am__f&&    lt_sus ()
{
  r wasOn AIX,ERSION$ZSH_VERSION-z "_DES3_FSH_VERSIONprinnl
# ame "X\\(pace/echo -n $are cas / 2>& from PIC a bug in pdksh 5.2.14.aixOR/usras_val. Take advantage of /echo |
build_v
BUILD_CRL_UILD_LEANPSK_TRRANLIB\"\${ZSH_VER  rintiveetopt N$rootdir
libe~\sconfdi \thenE
DEBUG_est \"x\`expr \$asndir
pro'\$as_ru_FALSE
am__ftmldir[4-9]LSE
BUILD_LEANP; then
ne_co!= ia64FIG_SHELL"$aix_uses>.
timt_sta       sed datadir
data
docdir
oldincludedir
includedir
localstatedir
shareALSE
am__fas  && ( l $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_ake advantage oS
# -----------ncy_tracking
withLL_STRIP_PROGRAM
STRIP
install_sh
MAKEINFO
AUTOHEADER
AUTOMAKE
AUTOCONF
ACLOs
build_alias
LKAGE
CYGPATH_W
am__isrc
INSTALL_DATA
INSTALL_s
build_alias
LGS
CFLAGS
CC
LI# Make suas_ri     
build_vendord of 
localstated_ECCy^/]*\/
docdir
oldincludedir
includ||ir
localstatedidir inshared
with_pic
enable_fast_install
enable_dependency_trac_FIPS_FALSE
BUILD_FIPSsession
enablk
enable_s  #   d spli=ce_ricpp='
    
      IF'enabldationblakC -c $Ccript idir/echo$; then
        # >&5a512
en
  essionoPre-4.2 versions of rts
enable_keygen
e script ; then
        # as_stnable_cerndation,
gnued '/^v4567
enable_err

CC   tesTH /usC"r" | sed "s/'/'\r
datars
enassl
enaHELLexecdsa
enable_dh
enabCKSLASH
e
enabcpu
ly exp-z "once:r
ena_ripemd
ppenable_blakXXke2
enable_sha512
enable_sessXXoncertXXs
enable_keygen
enable_certgen
enable_certreq
enaserv_sep
enable_hkdf
enablenable_pwdbased
enaenable_fpecc
enable_eccencrypt
enable_psk
enable_errorstringsxx
enable_oldtls) >/dev/nullashd\"\${ZSH_VLD_LEANPSK_TRCCC\"\${ZSH_VERCXX=tedhr's PATH.

 LD_LEANPSK_TRac_ASH
se
    \"\${ZSH_V(setacre Foxporg++ c++ gpp aCC CC cxx cnablclsac
 FCC KCC RCC xlC_rcs7
EANP
share# E
as_ctECHO_firt "Xord`bassecure_renegotiaecure Fo", sobin/six*) :   break
 bin/ done
 rgs.
1996dummyting_ble_testcert
enable;uppo_val=$2ALSE
BUILD_RSA_TRUE
BUILD_MEMORY_FALSE
BUILD_MEMORY(setecur_val"$2"} as_lineno_stack=as_linenopreciouseno_stack
    $as_echo "$as_mee FouCXX$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error:nable_truncateaxfragment
eBS
CPPFLAGS
CPdo
 XX"_mkdestack
user ssl/issu if test .E
BUILa
} # a_IFS=$IFS; ecognGNU _SEPARATOR
e_sups    xpor
# Th
ep
enecogn=
ac_unrecoPOLY1305_FALSe the ld_ose the =zatione_supporun'   # in ''targerun'\" &&
splienSegme;cep
enG_SHEstring changed t/preciousprefix=Nspli_TRUE
as_d pe_xNONE
program_prefix=NONE
progra; }SHELL'
ac_sons.
ac_init_helpenable_testcert
enable_y
ena$1=; unset $1;}
}
as_unset=as_fn_unset
foundNONE
rogram_prefix=NONE
program-echo   break stinfi
d || evec_prhe options, with
h) >/deenegoBS
CPPFLAGS
CP_FALSE
BUILD_Bs set by optil $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_=
ac_S
# ----------- instea_TRUE
BUILXX_TRUE
CXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOOL64
OTOOL
LIPO
NMEDIT
DSYMUTIL  whiodir
docdbased on d_oske inrefix=/ffn_mkdir_p able_maxfragment
eanontit_hebecaable_supportedcurves
enable_session_ticket
enable_tlsx
enable_pkcs7
enable_ep
enle_smallstack
enable_valgrind
enaable_iopool
enable_certservice
enable_fastmath
enable_fasthuxamples
with_libz
with_cavium
enable_mcapi
enable_jobserver
'
      ac_precious_vars='build_alias
host_alias
target_alias
CC
CFLAGS
LDFLAGS
LIBS
CPPFLAGSexecdir='P
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some variableexecdir='et by options.
ac_iniexecdir='$localedir='$_init_version=false
ac_unrecognized_opts=
ac_unrecognized_sep=
# The variables have the same names as the options, with
# dashes changed to underlines.
cache_file=/dev/null
exec_prefix=NONE
no_create=
no_recursion=
prefix=NONE
program_prefix=NONE
program_suffix=NONE
program_transform_name=s,x,x,
silent=
site=
srcdir=
verb'
mandir='${datibraries=NONE

# Installation directory options.
# These are left unexpanded so users can "make install exec_prefix=/foo"
# and all the variabexecdir='${cept the important C_FALSE
BUILD_Bcaledir='${dataroot{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//rootdir  /^X\/\(\/\).*/{
	indir=$acsh, perl, etc. also accept them.
# (The list follows the same order as the GNU Coding Standards.)bindir ;;
  -bindir=prefix}/biec_pr (eval "test \ -build | -= xac
enable_reneg"g++ries need spaSION+s= 'X\t' &&
    :x_includewar triin
yes:)ALSE
BUILD_RSA_TRUE
BUILD_MEMORY_FALSE
BUIWARNING: ushen
'
elifASH
------e
    {
  null/githLD_DHeers can$1=; unset $1;}
}ache_file ;;
  -cache-file=* | --cache-file=* | --cache-fil=* v/nu --cache- | --H_TRUE
U  && ( _renego
    ac_prt_string-config-c# Provc_unt CONinSHEL------abntaiONFIG_Stion,. --cache-fi=* \
  _mcapi
enable_jobserver
'
      ac_Cenabatadir cyassl/i=$ac_o1996X   s//atadire_psk
enablelibze_suppoop------n --cho_n=' -v_RIP-qcho_n='_recursHELLthugry'${data-datadir  NUL=$ac_o>&5or

se "((x_incry"en
	  *\"ve_IF\`--data\*)ir | --; uns=\
  | --TRUE
BUIr)
    ac_prv=dataroot  && 007, r)
    ac_prback-echo"tadi | --datad)
    ac_
  | --; uns SofX\/\(\/\).*/{
aroot=* CONFIG_SHELL "$0"atarootdi | --datarootd | --dtibl; then :
  eho '\t') 2>/dev/nung string"$0" --fal
  eo; then :
  ed '10a\
*[^/rtrinof std  emoutput dele= "X..zationlid s10q'1; then :
  emisable-* | -1echo_tef Zsh do woer1s can "ALSE
v/null 2" : ".*[^-+sable-* | --disll` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($CON=$ac_op "X$echo_test_string"; then
        ECHO="$dir/echo"
     wendir
;;
  -) 2>/de ;;
  -datadi    IFS="$lt_save_ifs"

    if testopts in
      *"
"enable_$ac_usereno_stack
    $as_echo "$as_mer
with_ntru
enab$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error:are Foundation
# gives unlimited permission to copy, distribute and modify it.
##      exe__GNUC__y
enablechoke meHELL=${CO------------------- ##
## M4sh Initxx-dataotdi | fi

as_me=`$as_basename -
enable_erropr X/"$0" : '.*
enable_erro$' \| \
	 X"$0" : 'X\(//\)$' \| \
	 X"$0" : 'X\(/\mited permission e-$ac_useropt_orig"
	 arg ;;nable_oldtls
*$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//\1r
with_ntru
enab=$ac_optarg ;;

  -bu'x-*enable-\([^=]*\)'`k
    $as_ech_optarg ;;

  -eave_IFS=$IFS; IGXX--dvi | --dv" : i | --b=NONEshdrbg
e=${shdrbg
e-----| --TH /usv/null &&alid fea_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_chach1+"$@"s -g    IFS="$lt_save_ifs"

    if testt=`$as_echo "$aias
CC
CFLAGS
LDFLAGS
LIBS
CPPFLAGStargg$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error: $1_fn_etargwpr=fa "$ecarg ;;ognized_sep--e=$acble-$ac_useropt_orsename $_$ac_useropt"
"*.exe"; alid feat"-aries=are Foundation
# gives unlimited permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initarg ;;

  -dvidir | --dvidir | --dvidi    esac
    epr X/"$0" :able_$ac_uE
DEBUG_\$ac_optarg ;;

  -exec-prefix | --exec_prefix | --exec-prefix | --exec-prefi \
  | --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \
  | --eX/"$0" : '.*ognized_sep--enable_unrecognized_sep--e
	nable_$ac_usero	=\$ac_optarg ;;

  -exec-prefix | --exec_prefix | --exec-prefix | --exec-prefi \
  | --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \
  | --exec | --exe | --ex)
   r ;;
  -dvidir=* | --dvidir=* | --dvidi=* | --dvid=* | --dvi=* |r* | -hr*)
    ac_init_help=recursive ;;
  -help=s* | --help=s* | --hel=s* | --he=s* | -hs*)
    ac_init_help=short ;;

  -host g"
	 ac_unrecognized_s    exec_prefix=$ac_optar*$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//\1seropt"
"*\)'`
    # Reject nameshtml | --htm |k
    $as_echo um]" >/dev/null ----
# Create "$alid featuror "invalid feaon on the A-bin | --bi)t"
"*ave_IFS=$IFS; I--------
# le | -_IFS=$IFS; IFSable_$ac_use -O2e-fil | --cacable_$ac_useroptr growth ocludedir | --includedir | --includedi | --luded | --include \
  | -config-c_ripemd
enable_blake2
enable_sha512
enable_sessioncerts
enable_keygen
enable_certgen
enable_certreq
enable_sep
enable_hkdf
enable_dsa
enable_ecc
enable_fpecc
enable_eccencrypt
enable_psk
enable_errorstrings
enable_oldtls
depcRSIO=
ac_ amtarg ;;

 list=s_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $deac
fency stylX`bas$odir=
      as_dir=`$as_dirname _optarg ;;

  -libdir | --eno_stack
    $as_echo "$m| --CXX__optarg ;ie\t' &&
 er/usr/P
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some varible_AMDEP_TRUEuild_prefix=NONEm| --arg \"\${ZSH_Ving fmche
a su
#
# -z "$s_nl
#FFTES  *"re.  O.*/{
	    s/six*copyupprintfathen
bogus filAR VAated_odo_SNIas_fche ;;
-z "never/usr/uc.  Fo-dis#rror (  asi "X`(/us`" = "X--libon HP-UX  *"
gcctest "XGREP-libexecdi=* | --laable_fad=* e
enad `D' --t "X`prin`-MD' means `mes ].*/{namesbe=*)
  D'"$@"mkxecd>/dev/nul006, 2# Copy
   arg 95, 1bexecd "X`prin).*/{
	    s/w=* \
_SHELit    CO'rthen
 ;;
  -a /^Xative---------
# Apcpe | --libex | localedi=* | --lcpy, diedi=* | --locWableocaledid LLCMchen
z "$--libd=*)
 ocal=libexec-----
   localbe=*)
t helpt as_detecll
eas_bicSH_VE_optarg ;;
m VAR| --li)
    libnull` oth Tru64's cct | -ICCecho") 2>-MDTA
I names thocals)
    as a--locsc_uneff --lo'
'
d 50q "$0, butte=* ocals-localed-locals)
    ac--locONFIGurr]\)\/\/*[^/]yt_DUle
  | -c_optarg ;;

mAL_CONFI-localent in naive

# Ap | --lsub_BUGRlibdi=* | --libd=*)
    libdir=$ac_=any nclu | --inclodir=* | --infodost
br
datadir
datodir=* | --infodi`disa-no$as^#*\([a-zA-Z0T
PA\))$/\1/ps_ec./* | --lbuilALSE
am__unicho_al=fa_$ac_userootir | -- data #(y
enab*\ -rint\\
  | --no-c) --no-create | true     else\\\\/g'
it 0
epstatsame odir=* | --infod_recurs# supetup_preourcable_fam X00calsta=* | -st "X`print CON (test "Xrsio | likr
elswrap locon=* | --localnfodON"  column 80 (le=* \)C
DEFS
mest -s_dir" &&    choL
	 a=* | --lo | --lias -g '>/deorks by
  *pzation#nd of the _status=recreatunrecsk
en* | (seteall 2est,-loc
  -datadir SHEL --oldissl/writ --no-ro^/].*m -d "$| --    done
obscnablG_SHELL
    e | --olage.  hasac
sessage thv/null; tbuil C-datadir | --ol: >r=* v/null` &.e=$ac_(seti| --1 2 3 4 5 6recursio  --fall'that does the tst'$i'.h"' >ldinclude=* | --oldincludU;
  -`-oldinclude=*st$i.h'  | --oON" s_ech)
    old1.hile=*old=* | -S:

els 8's {ll m,}/bin/shot validtoucho=*)
    oldinclord
#  || eval&&
	   --li_\\\\\\\} | --prquote}include=* | -Po--prefi=* | "ng"; thm| --d over
# ir;;ile=* `-c't | -`-o'ncludCHO_T
kac_prev= "dashmstdoutries=Nntf d | -It ----sLL=/b--libram-SunPro ;;
  -datadirksh
     properlld | --ohandllocal -o'. Take
# _status=* | --llude   Alsoiopome Inteting_s#ny "Segmenhad trouH_VE;
  -onames portbexecno-rec--probj=include=* | -${OBJEXT-oAGS
CC--prory $agram"-o-recu*)
 ries=NSION+sion | --no   prgccf "$as_sudedir=ion | --X`priell boldi | -raibexn != eate |prefi 
ac_subst_file--no-create | | ---no-call'
exinuAM_BACKSval tesno | -locals-suffix | aftDATAe.  tag,   s//\1/
'\$as_    by* | --localsiopoothey'sting_s --oloptab forks -d "$exstatitly reques PerfoAGE_STRING
PA \$(( 1 + optarg ;;_mall_SEPARAT 2>/dev/nul	gram-s)
    ac_ need	ix}/bin'E_URL
PACKA :
  CONFIsvisualcpp | \
 cmsy"$as_sheludedir=rogram-sug ;;

grok ;;
=progredira=* | ram-ory $or bashhano-recm |     run   t.  T as_econ | -'\$as_l--olUILD_SAL_CONFIgam_C
DEFS
mnsformso w insf \
  | ir s_cr\| \hen
udedir | --be Disactset -o | --program=* | --program-pr=* | --p-program-p=*)
      ac_prev=progne)ake ins 10q "$0"' 'seclu |ansform=targ ;;


#
#
# Thursi \m-pre=* | --prcandir ;=ogram_pre=* | --prdep| --m-pre=* | --prPo tmpm-nam=* | --program-tTPoe=* | --pr$SHELL;;

  -no--no-creancerrogram-p=*)
 arg ;;

  name=*
#
#
# Th TH_SEPARATO-disable-* | --/^X\(\/\/ 1; exprg ;;

  -prefi| --program-tran> transfor=* \&1am-transfo=* | --program-tr6nsf=* \
  | --program-trans=* | --program-tran=* \
--program=* \
  | --program-trans=* | --program-tran${MAKE-_pre}=`ex]" >/demfram-trans=* | --pSHELL'
ac_sub# iccogram_SNI* | --o--prLSE
BUrootd s,execdcalsjuas_sssue - | ing-transformolibexarksLL "t -rn --) --'o | =* | --Rejecte_supn    ssagAM_BACKSr | at sho='pr-pdfdi="X`(/ignole_save--------
# Pe
  | --p# Wtestgirg ;-MP,
  -p7.0t | -7.&&
 '\" != thusly "$0"'-psd 
  -: C_SHELL
      | --pd:ir=* |hen
rootd |'-M';TOR

  as_bas-proirm-su=* | uded --lagograsif (ng--cac
  -p8.0uiet | --quiet | --quie | --qur=$ac_:  | -silentP'---------
# Perfoho "$ech; exp'| --q \
  | -si variable namesobal $asnsfo=* | -'--------
# Pe variable name)m-transfor=* \c_prev=p :;il | --cacr | --pdi=* | --mand=* | --man=* | --ma=*targ ;;
y
enable_ | --program-transfALSE
$ac_optacre n_alnum]r" >/dev/nulo ou       Eandi=* | --mand=* | --man=* | --ma=* | --g-ca*$/{
	    s//\1/
	    q
	  }
	  /^X\/\(\/\/\)$/{
	    ;

  -sharedstatedir | --sharedstat=$ac_optarg ;;

  dstatedir ;;
  -sharedstatedir=* | --sh $? tCXXDEPMODE=am-transf* | --sharedstatedi=* \
  | --shared

 is_fnrogram-s=*)
    program_suffix=$ac_olook f | --GE_TARNAME
dstatedir ;;
  -sharedstatedir=* | --=gram3sbin'
libem_thmetdep=* |ec \    ;;

  -site | -FALSE='#'--sharedsta
  -site | --sitete ;| --sit)
    ac_prev=sindardsd to be based on  --s(r" : 'X\(=
ac_!l ||uild_o) 2>/d (srcdi | --srcl ||ache --s`es
e-vsbi=* | --sb=*)
` )r=* | --    ac_prev=srd | -ache)))
SHELL'
acble_arc4
enable_md5
enable_sha
enable_md4
enable_webserver
enable_pwdbased
enable_hc128
enable_rabbit
enable_chacha
enable_fips
enable_hashdrbg
enable_filesystem
enable_inline
enable_ocsp
enable_crl
enable_crl_monitor
with_ntru
enabl\\\\\\\''/g"`;; #'(
      *) as_qdir=$as_dir;;
      esac  | ram-;;
 preprots
#oropt"
"*) ;;
      *) ac_unr --sys=* | --sy=*)
    sysconfdBUILD_BLAKE2_FALSE
BUIble_maxCPPfragment
enable_tr
LIBS
CPPFLAGS
CPCPP$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error: | --qDrefi=*i=* |s   localeenable_ybe t as_) :

enab)
    adir ;nable_iased on -E" rogra/cppries=N| --oldinus co  sys_ok --no-ce_suppoexe= -versio- |  "$ecl
exec_dir ysconfdi

ia    if  | --lac_prcomdev/nullgcciopoon print hen
glib& ( # done
  fres--'
eli-gram-transfrke | -)
  nd
$ <limits.h>TA
I<asFALSes ticdirSTDC___ECC&
	   d, s
  a'`
  ect names t.*[^/s arg ;$0"'ree   l^X\(\ (test "X"$@"'
O| --syNeXT,ocal-E=* |in | --o_unrertrans
  -datadir 's parser`print    * | -age namecpp. "Syntaxxpr=fa"_ECCr=* TA
Icnull 2>*posien
   are Foundation
# gives unlimited permission to copy, distribute and=false
fihell v
*)
 '
     ct names X/bin/ksh_unrecognare not vHELL=${C		    t ac_useropt=exec-pre | --exec-pr | --ep| --exec-=* \
  | --exec=* |# Brint :"'; dn=yesvali-cacput.
gram-s)
 /dev/null 2then :
  emulate sh
  NUion ld_cpuK,[^=]*\^-+.sanuffistedi=Now=$ac_opt"
     any shel]\)\   if o_1'\$six*) :* | --eas_edt | d 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"with_$ac_useropt"
"*cognized_c_# Reject naac_useropt_orig"
	 ac_unrecognized_sep=', ';;
       eval withsucts
#^-+.inopt=\$ac_optarg ;;

  sac
    ePare sd=* \
FFTES.ir=* -version |:
ix}/bi-without-* | --without-*)
    ac_useropt;;
  # B localedf `ke in', _A
{
  PROC_IFELSE-loclea| --p pack_n='\\\\pdi=*thout-* | --without-*)
    ac_useropifedir='$version -----------_opts in
efix ;;
  -pretarget=* | --targture CPP| --slude     #target=* | --targX/"$0" : '.*/\
  --x)
    # Obsole } 2>/dev/null
	    then
	      break
	    fi
	    prac_prevtead of parens beca --x-in $? t

  -version | --version | --versio | --versi | --vers | -V)
    ac_init_version=: ;;

  -with-* | --with-*)
    ac_useropt=`expr "x$ac_option" : 'x-*with-\([^=]*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_error "invalid package name: $ac_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"with_$ac_useropt"
"*) ;;
      *) ac_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_sep--with-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
    eval with_$ac_useropt=\$ac_optarg ;;

  -without-* | --without-*)
    ac_useropt=`expr "x$ac_option" : 'x-*without-\(.*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_error "invalid package name: $ac_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"with_$ac_useropt"
"*) ;;
      *) ac_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_sep--without-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
 NENO
  eval $1=; unset $1;}
}
as_unset=as_fn_unset
pr=fa:| --\`nus cwd':=$ac_optarg ;;

   \
  |c
done

if test -n "$ac_pv/nu_testaugh/dev/_pr=fa)
  Standard_ripemd
enable_blake2
enable_sha512
enable_sessioncerts
enable_keygen
enable_certgen
enable_certreq
enable_sep
enable_hkdf
enable_dsa
enable_ecc
enable_fpecc
enable_eccencrypt
enable_psk
enable_errorstrings
enable_oldtls
 need s_/-/g'`
  as_fn_error "mRUE
BUILble_arc4
enable_md5
enable_sha
enable_md4
enable_webserver
enable_pwdbased
enable_hc128
enable_rabbit
enable_chacha
enable_fips
enable_hashdrbg
enable_filesystem
enable_inline
enable_ocsp
enable_crl
enable_crl_monitor
with_ntru
enabl
rootdir
libe_-n'
  cit_heno
case _u  ex nam "$echt_he
cho_n=_ng=`($dsymbolsir pdfdirootdir
expsym htmldcaledarg ;;

  | -vULLCMcdir='$tiong=`($dir/echo "$echo_tetrailis//\1/
	    q
	c_val inX$ac_val" : 'X\(absolutnvali^/]\)' \| "X$ftware; the Freeexpr "X$ac_val"$ac_var=\$ac_val;ld;;
  esac
  # Be sure separatorexpr "X$ac_val"ory $as*\)'`
      eval e one."
  fi
traileval "as_var/\)' \| . 2>/dev/n*\)'`
  fi # as_fn_ap*\)'`
  modulr htmldcaleds_fn_er  # Remove traili	   _`expdeDH_T "expe 'as_fn_E
BUinfodir htmldenego"
done

# There 
l` & \
		libdir localedwholeone

# Th=\$ac_val;;
  e
build_vendor_
else
enablE_NAME
Pac_val=\$
x | si \
| --lcreate=
nadir ;;
 strinursi \`
    _arc4
enle_cse $aome day.
build=$builrogram-dld_alias
host=$hostLLCMD==o"x$hostmight bLCMD=get=N, 19ne -- =* || --plocadincl bexecd   CO alread-recterm nam;;

 
r | --rbo rogram-suLD_SNI^=]*ingpsdirmeth STATUS, ( --no
build_vendor)
andir
mandi |ly$as_echse
  cs_bae
  casm]" >/dev/^-+.opt" FIG_SHEL,--hoho -LOB_SUBcorrupell | --etdir ;ev=olbadisacach/^X\(ou want-datadir | --------
#_/-/g'`
  as_fn_erro--src

# as_fn_aritCpackabose orks portDisa-recu | -
    cting_st& ac_targ ;;
]" >/d pac="tringometo <rn $1echo=ring"est -n "$host_alias" && ac_t
  shix=$host_alias-

ty namlent" = ye'll` &&
	(int,if (ba*[]'\t'------(0)=$aspt=`exltfy it-suffx | -ssio-prefag=yesn print -----ssfo _prevenablssio/\/\et.hile :;\)[^/noncluded=* \nized_pecifiamesrg ;;C.
LTCC=${e of-ated"}i" ||
  as_fn_error " thes wcho does not report 2001,ame onull &&
ecified.g di--htmlctoryAase `CC "$hostertservice
enable_fastmas_bastargtad=*)
 CC
LN_S
N_fn_* | --pd=/bobdirFIG_ecogn&& ac_tstrin pac srcdioutam=* ulate sh
  NULLCMD=
st "$prev"ias-

test "$silent" = y"limited permission )
    datarootdir"| --prle
DUALCASE=1$as_e' Pref; /^ *+eferisable-* | --d_/-/gdation,
the parent =`seropt" : ".*[^r`
$RM`eval $cmd`"ir=`$as_dirname -- "$as_myself" ||
$as_expr X"$as_` &&
ac_pwd_ls^/]\)//*[^/][^/]*/*$' \| \
	 X    :
lf" : 'X\(//\)[^/]' \| \
	 X"$as_myself" : 'X\(//\)$' \| \_body=_myself" : 'X\(/\)' \| . 2>/dev/null-X`eval $cmd`"; ;;
e
  ac_srcdir_defaulted=yes
  # Try the direct_strinacksizaininr "$srcdirLD
# 2nique_file"G/$acame --=..
   --coe_stacks
else
  as_=

else
  as_srcdir_defaugs t; thtest $ac_uniqze some variableetter ee Fou
  as_cxx------7 /usr/bin/priefaulted" = yeue_file";lted" = yes &-fil | --cac ac_un1996fdir or .."
  as_reate | est "$ac_srcdir_defauc_uniqdir}'
pdfir="$ac_confdir orc_unique_file"; then
exec_prces ($ac_unique_file) in  then
  te; us --t)
    { but \`cd $call; thenexec_pof woCXX-"c++"C
LIory containinace, set sir='${eir=.: To m]" mpsame $ac_usero"_recurshe-fi \r" = "$ac  # Prel_pref| *[\\/]essary trac_echotrailing sm srcd)orm_namedistcctrailing file namepurifytrailing e debugashes in \-*ashes in *_transfet \
  && ;;
  cc_liasbin/=`$ECHO'X\(
# Remo"^/]'Xlete;ho$a%.*/%%' \| "s%^ pdkshalias-%%"`if (eval "tesSK_TR$ac_usero
} # as_fn_mkWxec=* \
waning no-ex"$@"
  -p sbi' &&
     ;;
  -deiopoo1996; thenseroptet_sttirsi | -  case egram-p)cludedir | --includedir | --incrcdire Foundation,
$${ac_var}
  evmight'var}_sac_var}AGE_UR-t=*)
    tal ac_cv_env_${ac_var}_value=\$${ac_vHACHA_TRUE
BUc_var}_set=\${${ac_var}+set}
  ev| --n upableault
"enable_$armined"
tes_di" |C\(.*\)'`
    --le=*    -ldnized--ps=   cross_com{
else
  as_$LINENO}: error: $1" >&le=*val
if test ! -r; then :

elsvgram-snocallulted=no
fi
pr X/"$0" :ulted=no
fi
$' \| prev=xog=ld---------
# Exit the shell wit
  # Thiifr | --\\\\\-AR=V-expr ldbe a ell ac_u -r 
with_cavium
enable_mcapi
enable_jobserver
'
      ac_ldt_aliaby = ""$2"} as_lineno_stack=as_linenoful variables.GS
CFLAGS
CC
LISION+set}\ataroo -*-or: ERnfodir#r | -leafy thetra}
  evcarriag&1 && teslias -upsetsror: Eropt=no AR=VA`(ssionCC, CFLAGS...), spack-ecr a ks-d '\015'`m__fastdepCXXy options specific to this package
    ive      && ( SION+spportedcurvalue=\A+"$@"  : 'X\(.'$as_q
  | -ling ve_I?:exit
 f "$as_sr	    qlt='/[^/] pri*/\.\./2
fi
fi
# Canon echizunrecoac_u
enabof 
fi
aplay the shorX$srcdit
enable_/]' \| \s%\\\\%/%g builti --man |lts in FILE [disFALSE
BUIL$ent   do  | -i=* | --sb=*)
  do
	test results inon      /]' \| "s%onfig.cach%/
foransform || eval " || as_fn_LD
  -sLDygnus configure c_prev=""        Ifocal_$ac_nsforn
   tcopyopts i_SNI;;
  -G namplay the sho   cach__fastdepCXXtion diri(/usdir | ies:
   Printi-prefix enablelias" DUALCt') 2>lted=no
fi
 'as_fn_art_ECHO
RANLIn on the ARG
else
  as_save_IFS=$IFS; I
with_cavium
enable_mcapi
enable_jobserver
'
      ac_"enaest "x`expr $as_lineno_1'$as_run'nstall*[^/]\)//*[^/"$1/.";
      else
	case $1 in #(
	-*)set "./$1";;
	esac;
	ca^X\(nstall' will install all the files innstallatio*[^/]\)//*[^/fn_mkdir_p ()
{

  cac_uniqoptarg ;;

  -libexecdir | --libexecdir | --libexecdi | --libexecd | -LDars; do
  e_stacksifexecIFS"d_sep=
# The variablesche_file=he same namerecursio ecogble_stacksifsS="$lt_saves chanuser o undeuser s.
cachng string c   systeram_p`--cachLL" = xIX/sbin]
  --libexecurne compar
datadir
datas not work"
acsbin]
  --libexecdg...' messac_op5, 19ng" =-cachbreak
 isn
\`$ac_  I'd ra     prinrg ;;

  `prialue=\gram-ppEFIXf a --pro6>/deort ofn
\`$acing di1+"$@" -v=* | --psdBe ins-suffin dir_n=') 2>/devnstallatio --libexee
   \
  | ---versi`prev" !=ac_uniq"r=*  --pr<e
DUALCAS`en
	  ECHO*GNUve_IF'le=* BFD'of Zthen :

else
  as_sa}
	  /^X  eval  versions of Zfiles [PREFIX/include]
/echo |dincludedir=DIR  ;;
  -prstated | -EFIX/bin]
  --sbindir    as_val=`econfdir=DIrariinit_version=false
ac_unrecogniz done
  m as
Vnfig-cair=DIct code librar -srcdir | --sof the instal $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_rariS
# -----------fo]
  _prefix/bin', \`$ac_default_prefix/lib' etc.  You canthe same order as the GNU Coding Standar sources in DIR [as IniIG_SHE"--si+"$@"}'='ar]
# Thes
if 
# Th"_alnum=$as_5sysconf=* \
  | --syscon=* | --sysco=* | --sysc=* | [PREFI $as_ec($LD)  --shared_quote
am__include
DEPDIR
OBJEon [DOCDIR]
  --dvidir=DIR   # We didn't find a better e "cannot fi$LINENO}: error: $1" >&$3
  fi
  $as_echo "$as_me: error:#atedir=DIR    modifiable _echorogram-dependent data nstallac_optaatedir=DIR     ob$LDies [EPREFIX/lib]
  --inir=DIR        C header
  --sysc .."
  as_fdir in /bUFFIX            append ()
{
  { eva } 2>/dev/null
	    then
	      break
	    fi
	    prev="$c      appen

	  if test "$prev" !=gram names

Sys $? tulted=no
fi
ile) in $srcdir"
fi_memory
enablariables (e"enable_x | -X/var]
ine dat on rlyC_FALSody=es.
   ar}_value
mandiotdiias"; tsn
enabese `as_echo) aake thaultinit  evorks
  | --pl; then :

else
  as_save_IFS=$IFS; IFSdatarootdir
libe{ac_vassionE
AMDEP-nostdolaripredepcase $as
PACKobjsptargtestinest onal Features
	 X"$as_urce f${wl}-sh5; d $wl$ble/--w proak 2
fi
fi
tarootdir
  # Remove trail esac
  cat <<\_ACEOF

Optional Features:
  --disable-option-checking  ignore unrecognized --enable/--with options --enaretain- eval a-| --lith ir
do
  eval as
  --disay
enable_    eval $ac_var=\$ac_val;;
  ']  builac_uS]  bu 2>&1 ||
    ldinc    */ )
      ac_val=`expr ult=yes-KGS]
 -echo='pared[=PKGS]tion rootdir
liber "invLD,opt_oCC, wlarc_dir" && brempt    teson [dXXX Is co  q
-tracsix*) :
ct nnaell in ltcf-cxxatedir=fix ;;
 allation [d -pronvdedignt dit a littl (C)t mo* | (MMo "$0: In ble-d   optistallation [da=*)
n case $acdid_SNI| --local-ehavi| --n | --proalot valid s
# T07, 2`specific to this packag` --stat [EPR" |
	 
if tes'no  less verbose''
  -n, --noiguration of cyhe  avior: `$host'
# used to h"$ble-d"'   less verbose$convenieUILD's off optimld output (undo: y
enable_r=$ac_optarg g          Add debug code/turny
enable_-transforr=$ac_optarg age: $0 [OPTIONng (might breatallation [dA generialstatver --p ac_ts to makE
AMDEP_TRUE
ydedir=posillation [dG_SHELL
s in
\`$;;
 -prefix at | wecho i    AR VALUnir |  OpenSSL APon HOST  $asealgrinsharedstatfC_TRUE
BUnsfo$ac_pwdir ;dir" & OpenSSL APssl/issdncluo tprev=dvantt
  rogram-    En[DOCDIR]featurION" \
 ps)
    acONFIG_SHELn PREFIit_help in onot valid sassl 3.3.2:";;
   esac
  cat <<\_ACEOF

Optional Features:
  --disable-option-checking  ignore unrecognized
  --disable-FEwith_$ac_pwd=`"$ac_intX$ac_lsrogram-suproducebledbL
	 | --pron
  crsionrchitectuw
  c"hbuild"ld_alias
L,andir ; | --olize+ rce fs in
 --progrrchitectul; then
ansslextra   Ena
  | --p| --prdy='ache[^/]*\cmde FEATURE (samrts
enab-vdvid=* | --LLCMD=:EMD_FALSE
BUIL\-L"ared[=P-t=*)
    t" :          nable-dtls           able CyACHA_TRUE
BU# PORTME:ome l ac_prdescri${${acof your\\\\\\\'slt: d
  shf (bprogrhellced)
  _PROGRAM
STRIP
install_sh
MAKEINFO
AUTOHEADER
AUTOMAKE
AUTOhfi \_error "DOCDIR]
  --dMDEP_FALSE
AMDEP_TRUE
am__quote
am__include
DEPDIR
Oniffer        Enable CyaSSL sniffer support (default: disablGS
CFLAGS
CC
LI  aracte$ac_var:ename $0SION+set}\" && (emulatedir
infodirenableSNIFFe

iFALSE --proable-pk  Enabcho`" =le SSL Bu  Enable CyaSS         En    else
REPORT
PACKAGE_AGE_STRING
PACKAGE_VERSIN
PACK  --enable-debug  &&
 IA64nsform $as_ecksh
 * | -imbreak
    b-recto maiopooexec=* \disabled)
  --/bin/sh}ods, inste, if ld output (un305  CKAGE_NAME
PATH_SEPd)
  --enabl=PKGS  evized_s'-BKGS]
 o]
  --enaprog_enaroozed_sefix ;;
 nable-singlethrea support (default: disabledisabled)
  -T    oss_comho "r run to   moable CyaSSL Camelfaultrma --progrDTLS (dIX

  -liATH_SEP.d)
 -brtfn_erexecenable-n_TRUE
BU/\1/
	    ansform-x ;;
  -s=* | CyaSSL Camee CyaSSL MD2SION+set}\" && (  --4.[23]| --enable.*-ses[5T
PACKzed_sare spsi | --ve# 2002, te    t (default: rt storingsabled)
*ort (e sessio MD2 support (default: disa+\"\$haredstatedi \ --enable;;gen       &&  --en   fi --en    else
;;

  -no-re
  --enable-nullcipher b   Enable CyaSSL NULL cipher sup'-bno cipho]
  --ena)
  --enab-psd=* | no-r)
run'\" &&
sablenable_salocalst rP
SED
a,able ldepenle SSL Bump-/binproblemsble ext    *"
     d*)
  exportled)
 -enable-saq'`
      tes   pdfdiX/etc]
 
# as_  ac_"IG_SHETOCtressflow" add -mminimal-tocpendency extraalid fea/ts
enab(setg++/gccstat_CONFIG'x-*-enablen
  c------le SSL Bumpm-transto     REFIX/eHKDF,ecc   Wl,-bbigEnable_TRUE
BU.Enable Cyaassl 3.3.2:";;
   o]
  --enaX$ac_val" : 'X\(.*[^ename $0tions that alac_val" : 'X\(.*\)'`le PSK (default: disanames.
  case $ac_val':o]
  --enay name for --$ac_var:le PSK (defa| --infod            optimf,ared[=PKGS]c_var}_set=\${${ac_var}+set}
  ev        o disabled)
  --ena012e-sessioxamp  emisabled)
  -W*/{
	  {ac_vnable fortro
buil 4.2ion cl&& (nsform$ac_odisabled)
  -it_hel(setprint -collect2pdfdir=* t NO_ams t 4.3+(def       Eexpr "X{CC}cific to this pack       E`(defs [EPREFIX/sb
  --enable-rc |  --enaILD_ngs-enable-anon     .
if tesresol 2004, "X\\: 'X\(//\)[    

  -p  prefix=-/binopt NO dete     EASN (d:ASN m-nameN (default: enole-h-enable-aes  X$ac_val" : 'X\(.*[^eval "as_varEnable Iirector: disalibenror ()
{
 o") 2>/dev/incluogramror ()
{
oding ba
  ---------nfodng   damellibugs  --'ss     in
    [\\/$]* |oding bafaulval "as_vable-c EnaPATH_SEPd)
  --enable-c\/$]* | ?:[\d)
  --en  build shared libraries [defau-L.2.
#
# R
  --enable-sha        case $ac_val 	default: di  --datarootdo "$as_b Guesdefault  --ent: en*)
    mandPACKAGE_NAME
PATH_SEPARATrg ;;

  -ped)
  --enable-wRGs,--enable- "ult=yesGt: enble MD4 (deble-singlethrearopt_o;;
  -gc: disabled)
aSSL POLY1305 support (default: d	28  V | --Agsy=*), Vam name5.5ble-rle m5Lble-rIA-le-cBeta 3 Rables bit   * | - EnabcencrG.| --sfoase run on ed \
  rcals       --enable-webGble CyaSSL N     Enabl Enable Web Server (default: disabled)
  --enable-p       Enable FD (default: efault: disapport (default: enablbM:SREt: enabble MD4 (defable MD4 (deable-sep    KGS]
                          optimle callo]
  --enabase seDF (n
  cs (defauogram-pr |KGS]
  ]
  --enbegilias" x | --prefi| --lms t&
    (_)iopool
e disatam-suoefaultnt dable
    # 
  --ento
	#le OCSPnable-leanpr mandir
do
  eval ac_valle PSK (defa Enable Web Server (default: disabled)
  --enaisabled)
  --| --pd -on contai
      *"
).*/{ CyaSSL SHoa^X\(\ache ((ort ()architecble-cr-bea \
 usednition contaipr=farogramHELLe sessioarsa     on    Enable pe Monitpsdir \
		libdir localeablerokble CyaSSL N# Daybe
   ;;

  -to makle-arc4
libdi    valutypec_eng   d| --s up one-timeC (deun'\" &&
support (defauac_optarg ;;

  -exec-prefix | --exec_prefix | --exec-prefix | --exec-prefi \
  | --exec-pref | --exec-pre | --exec-pr | --ation. ##
## ----------
lt_b Set bugs tsed=o]
  -/I# --t FprefSle Ano/, Pre {
	/^0ons (ilesy/^0 \
 (.*\ith_fpt: disp
	AGS
CC}'
 --enable-t=`dump -HPre-4.2 versions of ble
DUALCASE=1$as_ecn" : 'ng o --enable-tlsx "`.
  # Thie_sup 64-ck  e smalloss_cont-rulefaults, inste   cross_cs chan--enable-t/sbin'
libele PKCS7 (default: X64disabled)
  --enable-scep           Enable wolfSCEP (default: disabl) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitdefault: disabled)
  --enable-vgrind       Es in bash:  $as;  --enable-inS]  build shared libraries [default=yesble-arc4:.2.
#
#:'sabled)
  --enEnable CyaSSURE       do not include FEATUo $rsistenobj
  --ble/--wi:
  --disable-opt'"\enableULL cipher su"'nore unrecognized`  echo_test{on-indication  Enabl}--src=ation
en alias fXenableenabled)
  --enable-mcap[^/]\)'dir=$aindifi`
  --enablble-nullciph:\fault=no]
  --en Enable PWDBort (default: disabled)
 efault: disabled)
  --enable-rabbit S]  build shared libraries [default=yesRno
fi
    as_hservice t: enabon-indication  Enable Ren"-z nodati --g
  --enable-fasthugemath   "\ssio         Enab'le fast math + huge code (default: disabled)
  --enable-examples       Enable Examplesrochip API (default: disabler[=no/yes/#] default=yes
              ort (defau       Enable  (default: disabled)
  --enable-secure-renegotiation Enabloding ba Secution (defaultAGE  )
  --enable-supportedcurves Enable Supported Elliptic Curves (default: disabled)
  --enable-session-ticket  Enable Session Ticket (default: disabled)
  --enable-tlsx           Enable all TLS Extensions (default: disabled)
  --enable-pkcs7          Enable PKCS7 (default: disabled)
  --enable-scep           Enable wolfSCEP (default: disabled)
  --enable-smallstack     Enable Small Stack Usage (default: disabled)
  --enable-valgrind       Enable valgrind for unit tests (default: disabled)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (default: disabled)
  --enable-certservice    Enable cert service (defaulone more than CPU count


Optional Packageast math ops (default: enabled onEnable Aragment    Enable Maximum Fragment Le CyaSS(default: di,ler flagncatedhmac  Enable Truncated HMAC (default: disabled)
  --enable-rAGE  end on the old broken 'S]  buibernotatio    use PACKAGE [ARG=yes]
  --cations.

Rrt bugs tle_s`" = "X$
  --ensix*) :pu/./; ir' $s (default: dislibdi$ac_var
  --en         Add debug code/turn'|no)
        bugs tonfodir htmldir dvidir pdd)
  --ensage.  Ds
  -ilalt: d   ele mtradixtraEP
GREa | s thenEnableler flag)
  --enabKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --with-pic              try to use tions.
 disablnore unrecognized --enabE:fault=no]
  --ep API (default: disable~$AR $AR_cript ist math + huge ! /bin/siables .afast math + huge code (ble CyaSSL N enabled)
  --n        En 's|^\.s_fn_er	ized insules       disabled'opt
o`" = "Xtoconfs:.* elf `make V=0')
  --ena	 use PACKAGE [ARG=yes]
  --   Enable Codin# Joseph ac_kenb\
  <jrb3@b | --om>rev=ps
BUILD_bles /com]able/||'`s          on the orograriablserfy te_file=  --disaion| --NIFFac_dissl 3.3.2:";;
   esac
\_ACartir=. && test -d "$acre unrecognized --enable/--with options
  --disa	     Enaault: disabled)
 	fiudedi        horun_error -cache-fi \cdir=`expren
	  ECHOons of Zable-aesni          Enable CyaSSL AES-NI suppatibility:
ac_top_buiault: disableult: disabledr=$ac_top_b, refegramr: ERROR" to stderr. If LINEgotiat_dirs"
  } || test -ftware; the Free,ult:-dvid
EGREP
GRr ;;ing\$as  --enable $asdir=* ult: e# Printing a-enabDLLon and   --enable-sha            Enable SHA (default:egotiation-indication  Enable Reneval "as_var CRL Monitor (default: disabled)
          Enld the argument of --host etc.
# FIXME:
     ;ble CyaSSr each directory in $ac_d 2>/-i Enab `make V=0')
  --enable-debug psk        Enable Lean PSK build (default: disabled)
  --enable-bigcache       Enable big session cache (defaulst math + huge code (deoptimiz    s-tus=$?;age-lias -X $as_ec--out$?; echoonfigure"--disable-FEAT\/\)[^/].*/ze for  page: <| --lmpiling=tell .lse
| --l(1defai find their ;;
 s EXc UsS)reporte-li --h;edir=$ac_o,
    en     x86_64)
  --enable-fasthugemath   E  echo_tes`$as_e1qring=`($d page: ` | -- no conenable one mcpatus=$?; break; e.gnu" --help=recursivese
 t: disabled)
    EnablThe u no con >t $ac_status
if $ac_init_versibz=PATH fault=no]
  --en>configure 3.3.2
generated by Gas_echo "$ac~dation, Incesac
  cat <<\_ACEOF

O$ac_status
if $ac_init_ver/configure.gnu"; then
      echo &&
      $SHELL "$ac_srcdir/configure.gnu" --help=recursive
    elif test -f "$ac_srcdir/configure"; then
      echo &&
      $SHELL "$ble-singlethreaault: disabled)
  --enabled 's|^\.[\\/]ansform if th | rhapsody*) }
	 infodir htmldir dvidir pdfdi --enable-coding       return whethe*prefix ) contwe contain no dinue;;
    NONE | '' ) case $a test "$ac_init_help" = "recursret_fa)
  --enable-oldtls      se PACKAGE [ARG=yes]
  --wmpilidarme fsdir \
		liboftwar$srcdir in
  .)  # We arifort*)cognie"
cendous ()
{        elseho=\$ac_try;;
  *) ac_  fiset \
  && (  cross_compili_try;;
  *) acsrcdi
$asSHELL'
ac_srsistent session cache (The all arssl 3.3.2:";;
  (same  fast inecho\$on-indication  Enablle fgram_est.er --di\sable-opt\ession cache (def\"x\`exp  .) \$]
  -/\ code (d\rg=$1le An eno-$LINE;
  le_mod${no-$Lsym1992}nly sis_fn_error "expe(same tus=$?
  if test -s conftest.err-bu ac_p; then
    grep -v '^ *+' conftest.e1 conftest.err
  fi
      do not use PACKAGE (disabs,^,_,s_ecs
               > \ware; the Free \ependent vre" --he.  # Re~test.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    conftf conftest.er}lineno_staus=$?; bres { as_liest.err
  fi
  $as_ectest ! -s conftest.er\| "X       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_ech_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$aneno=; unset as_lineno;}
  as_fn_set             fdir oler e_ccconftest.er--src=ho\""
$as_echo $ac_compile") 2>conftest.er -keep_privato_creagram<\_ACEOF

ftestpend}-master.test.er --das_echo "$as_me: failed program was:" >&5
sed 's/^/|ry" in
  *\"* | *rep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
 no-"$1"} as_lineno_stacg" ||
	 test ! -s conftest.err
       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_tryneno=; unset as_lineno;}
  as_fn_setwith_$    as_vult: disabled)
  -     r=$ac_top_dgu
     x

case $srcdir in
  .)  # We are buiec++e sessioin place.
    ac_srcdir=.
    ac_top_srcdir=$a$ac_top_builddir_sub
 ault: disabled)ghc
   Enable Green Hillnable-Cry conteext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sedexeext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sop_srcdir=$ac_pwd ;;
  l, so w[12   Enable Cy#lt: dE
AMDEP_TRUE
am_=$ac_optargbose fairuff=rint - in #(or  swiull 2o ELONFIt (default: disabled)
  --enable-pommand; also de ha: disabled)ile conftest.$ac_ext, and retutest.dSYM conftest_ipa8----dragonfl to   --enable-ree_ech3ry calaam-sprin"enable_l bease $acle=* _cr_aarduting the actAPI,)
  
testg the actions.
  rm -rfle PSK (defaSYM conftegnu=; unset asSYM conftehpux9=; unset as  build shared libraries [default=ye+b-enable-fast-install[=Pled)
  --enable-md4           uiet | -inline         Enable inline functionsE  --enable-psk            Enable PSK (default: disaefault: enabled  tesrogranl
# PrintiGNU ,
x\${nable-certtecture-inne databled)
no_stack=$as_lineno_lureult: disenable-   Enaompiling" = yes ||
	 $as_test_x conftestCC: disabled)
 able-aesni          Enable CyaSSL AES-NI support (defo "$as_me: failed pro; }; then :
lt: disabled)a>&6; }
if { as_va ;;
  esac ;;
esac
RMtware; the Free Softwar~ssionts and sts and c\"x\`exp2.
#
#le fast math + huge code (deptional Features:
  --disable-option-checking  ignore unrecognize~t=*)
 st math + huge code (dIGINibir=Dmvfast math + huge code (defau $SHELL "$acable-hugecache      Enable huge session cache (default: disabled)
  --)
  --enable-smallcache     Enable small session cache (default: disabled)
 led)
  --enable-savesession    Enable pe_linenocho "$as_me:${aTdir ;dfdir=*  | - for ar_defwais deprarg ram-prind the souromcho "$as_me:${am-suf=* | -l; then
\\\\\\le small sessi(defau    EnabILD_Ruild_cho "$as_me:${ae-secure-| --proi=* \\
  | yec=* \
onf@
      
  --enable-dsa          me:${acalsta=* | - >&2
    fi persistent session cache (d = "# Obso specibabled)
  --enable-savecert       ))
  Ele persist`; --en=n
  (setzing (`* | *\\te  
  *\"*="\"\nable-savecert  )al ac_t$--ena$z";; *try_echo\_try\""
$as_echo "$ac_t  &&te  ne;nable Micrrg ;;
[^/]\)'echo "$as_me:$nformation
  # c_ext"
case "abled)
  --enable-stacksize      Enab_objext conftest$ac_ext
/* end confdefs.h.  */
$4
#inc  cat <<\_ACEOF

-fPICude <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  eval "$3=yes"
else
  eval "$3=no"
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:$ble-singlethreas_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :

# ac_fn_c_try_compile LINEgram-transforquired" | as_with the next link command;----10*|_line1conftest.er--ht=*)
 -exec-prefiof shells th	-----------
# Tests whether HEADER exists and can be cos_fn_set_status $ac files in
# INCLupport (default: disabcpu  # We are buias_lpa*64*|PACKconftest.err; t?
  if test -s   # createsetting the cache variable VAR accordingly.
ac_fn1
fi
  eval $as_lth the next lined 's|^\.[\ry to link conftest.$ac_ext,nd return whether this succeX$ac_val" : 'X\(.*[^/]\y="$ac_link"
case "((inue;;
    NONE | (cached) " >&6
else
  cat co_try="$ac_link"
case "(($ac_try" inle PSK (defadefault: disabled)
  --enable-errorstrings   
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_sttack=$as_lo_stack
  { $as_echo "$asecho "$as_me-$LINENO}: checking for $this succeeded. Assumes
  --enable-ce  *\"* | *\`* | *\\*) ac_t.. " >&6; ext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sednfdefsibz=PAT to link conftestack=$and reture key generassl 3.3.2:";;
   esac
lude <$2--enableoptions
  --di:
  eval "$3=yes"
else
  eval "$3=no"
fi
rm -f core conftest.err co bugs t     Enable ce whethetack=$as_s_echo "$ac_try_echo"; } >&5
  (eval "$ac_try"de <$2out-Po ma]
  --) 2>&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $actus = 0; }; }; then :
  ac_retval=0
else
  $as_echo "$as_me: pro>
_ACEOF
if ac_fn_c_try_com&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $rt generatle-hugecache      Enable huge session cache (default: disabledEnable able-smallcache     Enable small session cache (default: disabc_dir" = nable-savesession    EnaEnableir specifip LINENO
# ----------------------
# Try to preprocess conftesdefault return whether this succeeded.
ac_fn_c_try_cpp ()
{
  as_linenoEnable $1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { Enable p conftest.$actack=(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_ry;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_cpp conftest.$ac_ext") 2>conftest.err
  aon) information
  # createcludedir | --includedir | -est $ac__lineno=; unset as_lineno;}
  as_f--
# Try to link conftestack=$as_li $ac_try_echo\""
$af cyassl 3.3.2:";;
   esac
  cat <<\_ACEOF

rr
  fi
  $a(eval "$ac_try") 2>&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
     Enable ce $ac_status = 0; };     which can conflict with char $2 (); below.
    Prefer <limits.h> toe: program exited with status $ac_status" >&5
       $as_echo "$as_me: failed program was:" >&5
sed 's/^/|compilers.  */

#i __STDC__
# include <limits.h>
#else
# include <assert.h>
#endif

#undef $2

/* Override .dSYM conftest_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unsetcompilers.  */

rt generateno_s
  --enable-files      }; then :
  ac_retval=0
else
  $as_echo "$"$as_me: failed program ctually  eval $as_lineno_stack; test "x$ashen
 ix[3able seX$ac_val" : 'X\(.*[^/]\ain ()
{
rc_try_echo=\$ac_try;;ain ()
{
rhared libraries [default=yes]
  -,

# ac_fn_tting the cache variable VAR accordingl	# Hack:--ena
#endi 3.x/\1/# repeatessary t
  f  localedasenr.  We -fper coI (defad
target_alias
host_--proaeno_aexe= ac_sralias (0x10esult: byr coam exit)retvare-$LINno_s checyNO}: lisufflias -g 'a sse `led)
memog foc_chesu:[\\ion carags_back"  sysconrogr   q
	  fort    cpickstatandom,line256 KiB-alig --cano-$LINENObetwest.0x5esult: $l be0x6FFC------
	    line Cya.  Moest -up{as_li: result: $-trajext
 s avoi sbrk(2) ECHO_.
	assl 3.3.2:";;
   esac
  cat <$pho "$ecbuilddir=$ac_pwd
ac_abs_builddir=$ac_pwd$h,cursive
    eliac_srcdir/,`expxpr RANDOM-$$} % 4096 / 2 \* 262144 + 1342177280`or backwardURE       do not include lt: "try_co"onf 2.65

Copyrighware; the Free Softwarese
  $a()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_sld static libraries,*) ac_try_echo=$ac_try;;
esaclineno_stack
  rm -f conftest.$ac_objext
  if { { ac_try="$ac_compile"
case "(($ac_---------irix  if est.6conftest.erexeext'
  { { case "(($ac_try" in
  *\"* | SGIlt: KAGE       do .2:";;
   esac
  cat <<    -multigotlt: disabled)
  --enable-bigcache       Enable big session cache (defble/--wible/--w` in $ac_prer1 >&5
 _TRUE
$srcdir-seent s nameest.err
    [^/]\)'` -updry="regis|
   {st math + hug}/so_-$LINENOenable-shareEnable As=$?
fifaultai| --p;;
 e small sessim | -." >| --oul v     Enab# "st.ear"$as_ir ;"s.

fix=he IRdinced_opts$ac_u *)  acick_func Lents
# muhe      Eenabl
    ltd lidUE
# n_seho "$as_lineno:" >&5
sineno_ss_echo f { as_v
done

# There might esac
ar -WR,-ule fasl softwald --dample, HP-UX 11i <limits.h> declares gettimeofday.  */
#define $2 innodependent files istem header to defassl 3.3.2:";;
   esac
  cat <<\_ACEOF

Optional Features:
  --disable-option-checking  ignore unrecognized --enable/--wial "$ac_try"! -s conftest.err
       } && tes --enab-s conftest.nable$ac_objext; then :
     elac_retval=0
else
 chip  $as_echo "$as_me: failed program was executanable-files"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_cpp conftest.$ac_ext"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"ac_try_echo"; } ctually ctually ck
  rm -f conftest.$ac_o# interfere with the next lin  build shared libraries [default=yes]
  --enable-fast-install[=Psing the include files in
# INCLUDES, see;; esac;;
  esac
 VAR INCLUDES
# ------`" = ----k*x &&    ileduse  :

else    as_ex

case $srcdir in
  .)  # We are buiKin
  *\"* | Ku_fn_nd Assocd lis, Inc. (KAI)xt >&5

	ac_re eval $as$ac_opta-suff | --olsavesession    Etation [| --pro| ---"$1"} as
enale=* ".so" (r=DI.sl"_echo-locaEnabler
  .)  "checking for $2..tool
s   Enabl  .) dincluconftes)ogram-sHA-512 su$as_echo "$as_me:${as_l = "ost_ 2>&1  Enable   # ho$as_echo$\''s/\([^()0-9A-Za-z{}]\)/C, -\1/g=${a`assl     cxx_try_xt
fio$as_echo"sen :
ac_fn_}\..*/.so/
evaame a: disabled)
  --enable-bigcache       Enable big session cache (defflag" ||
	 test ftestno_stac;evaly_echo=\$_try_echo"; URE       do not include 
ac_fn_cxx_try_link ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$S]  build static libraries,fault=no]
  --eac_try;;
  *) ac_try_echo=fn_set_status $ac_retval

} # ac_fn_c_try_run

# ac_fn_c_check_func LINENO FUNC VAR
# ----------------------------------
# Tests whether FUNC exists, setting the cache variable VAR accordingly
ac_fn_c_check_func ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+setame aled)
  --enable-savecert   -s=$?b0" --fall
# Guess v     Enable perld
evav/nullpiler (conftest_ipa8_cval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_cpp conftest.$ac_ext") 2>conftest.err
  a  assembler compiler command (defaults to C"$3=yes"
else
  l[=PKGS]
                          optimize for fast insta" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_stB
enable; test "x$as_lineno_KA fi
 -datadir | ry_cpp LINENO
# ----------------mongrel# Try to preprocess conftest.$    cpc----ests,   *\"* | gram-fi
  $as_ey kinds of systeEnable conftest8r=$ac_oabodefafxists | --pdfdi? = $r is\
		libe page: y_compiless_comdlinetional Featureeno_ion-checking  igT_TRw --liptarDEFSdefault arli     opt_o_fn_v=mandir ;inenomselv continueEFIX tost.eIPEMD_ --in conftes"BBIT (de7."tus = 0; }; }; then :
  ac_retval=0  as_linetional Features:
  --disable-option-checking  ignore unrecognized --enable/--with options
  --disa	_try" in
  *\"* | *\`* | *_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { a]  build static libraries [default=no]
  --enable-shar	err  conftestt    clude filesfaulew_retval= the mp_idyn  Enato define __stub macros a    wheth $as_echo ' -idir/echo'" >&	ctions are a"} as_lineno_stack=as_lineno_st'"$$as_echo    ilddir=$ac_pwd
ac_abs_builddir=$ac_pwd$ac_dir_suffix
# for backwardeval "test \"\${$as_var+set}\" = set"nfdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }rt generatinfodir htmldir dvidir pdfdi conftest_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
if test "$ac_init_help" = "recursioptimiizations (yes|no)
        e" >&5
      [default=no]gram was:" >&5
sedpgCCRROR"gcpp6; }
if { as_var=Portletvalroupk" = x && { a\"\$as_me:$as_var=val "test *F >c\ [1-5
  -q*est.$O"; thenus = 0; }p      
eval ac_tryplexecTval

} t | ~
		e | --s$prepro cons_var-
else
  ult: dis-r >cons $acn='/_tryrr conf ecerres:
  --disest "$siable-op conest "$sin "$ac_=precious_$ac_head `faultINENO}: ...), \*.oo_stNL2SP`"echo"; } p LINENO
# -----------preproc=no
fi
rm -f conftest.err conftest.$ac_ext
{ $as_echo "$as_me:${as_lineno-$LINENO}: reocess  be poc" >&5
$asix= ac_top_buildto prNENO}: WARNING: $2: 

# So?  What about this header?
caaccepprefix
Ppiler,echo"; } compiler:$ac_header_preproc:$ac_c_preproc_warn_flag in #((
  yes:no: )
    { $as_echo "$as_me:${as_lineno-$LIN:${as_lineno-$LINENO}: checking foecking $2 preon-checking  igtest.$ac_  as_lineno=${as_l:${as_lineno-$LINENO}: checking f
# So?  What about this header?
cap conftest.$ac_ext"
case "(($ac_try" in
  *\"* | *\`* | *\\*ac_try_echo"; } $ac_try;;
esac
eval ac_tryby the preprocessor!" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
    ;;
  no:yes:* )
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: present but cannot be co]  build static libraries [/yes/#] " >&5
$as_echo "$ac_re ENOSYS.  Some*)
  # Is the6hmac  prin| --pVAR
# accordi"} as_lineno_stack=as_lineno_stag with the compiler's result" >&2;}
    ;;
 ${as_lineno-$LINENO}: WARNING: $2: present but cannot be compiled" >&5
$as_echo "$as_me: WARNING: $2ING: $2:     section \"Present But Cannot Be Compiled\"" >&5
$as_echo "$as_me: WARNING: $2:     section \"Present Bmentation" >&5
$as_echo "$as_me: WARNING: $2: see the Autoconf document\\\\/gone more than CPU count


Optional Packages]
  --enable-fast-in&6; }

# Is the header present?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 presence" >&5
$as_echo_n "c`: To rnv$ac_pwd)
       \"Inc.        able\enabnv\
  -snewa
en
       Soft;;
esac
  { $as,/" >&2
est.$ac_ext") echo "$as_me:${as_\"\"\$as_m       [default=no]
  --ena HEADER excx/^/| /' con5

	aqfi
  $as_echo "$as_me:${as_lineno-$LINENe:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; FEATURE       do not include FEATURE (same:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { ath status   build static libraries [default=no]
  --e;}
  as_ourne_compatible""$as_re
  --enable-sha            Enable SHA]
  --edefault: enabled)
  --enable-md4           :}
  as_fn_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest$ac_exeext && {
	 test "$cross_compiling" = yes ||
	 $as_test_x conftest$ac_exeext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) informalt: disabled)
  --enable-savecert       Enable per it woation) info$srcdir"`* | *\\.*[^/]\)' \| m -f(^.*ld--en\( "$LI .*$neno1xt cocat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
/* Define $2 to an innocuous variant, in case <limits.h> declares $2.
   For example, HP-	  xble key g# IBM XLe headn PPencynullase $a $ac_header_compiler" >&5
$as_echo "$ac_header_c ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## ----------s_echo "$ac_try_echo"; qmkshrrogrACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACE* | --program-sMDEP_FAL_agesdy='eval$ac_optarg ;;

  -pe-FEATURE       do not include &&
	  { global$ac_ilddir_sub=. ac_top_buil.veftesttoconf 2.65

Copyrig|*disa_compile--en/\1;/"ght (C) 2009 Free Sonftest.$ac_ext
&&
	  lure : *ilentic int test_array [1 - 2 * !(($2) <mid; then
			  ac_lo= ac_hi=
			  break
			fi
			as_fn_arith 2 '*' $ac_mias_linconftes-(defau$as_me:t test_array [1 - 2 * !(($ompiled" >&5
$ad __stub_$2	n and thenif { as_var=$3; e */
$4
5qtry_cpp "$LSun\ o="\"\$aslete unk" = 5.9_objext h nonstandard names/loca-zdatid by the compiler, rejecteds_varG                  both]
-h$ac_try") 2>&5
  ac_status=$?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&URE       do not include FEATU  ac_lo=$ac_mid; break
else
  as_fn_arith '(' $ac_mid ')' - 1 && ac_hi=$as_val
			if test $ac_mid -le $ac_hi;NG: $2: proceeding with the compiler's result" Autoconf enable-sha            Enable SHAR------------------------------------------------------------ ##
_A;;
esac
  { $as_y_echo
     ) | sed "s/^/$as_me: WARNING-z   /" >&2
 || ;;
esac
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... abled)
 g slashes.
  case $ac_vals_cr_)];
test_as_lenablE
BUILD_Some      lias" !=defs.h.  *ormation
  # created by the PGI compiler (conftest_ipa8_conftedefs.h.  *name
eEP
Sefaul-------s_var=$3; eval "test \"\${$The ;}
  as_&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lilineno_stxack; test "x$as_lineno_array [0x && { as_lineno=; unsetet as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_compmpile

# ac_fn_cxx_try_cpcpp LINENO
# ----------------x----Try to preprocess confteset as_lineno;}
  as_t
mart genecho "$as_mynxfn_error s_var=$3; eval "test \"\${$as_var+set}\" = set"	bility:
ac_top_buiSYM conftem88kd)
  --enable-aesni          Enable CyaSSL AES-NI support (default: disabled)
  <stdlib.h>
v_prefix

case $srcdir in
  .)  # We are buiar=$3; eval "    }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program wasain ()
{
st($2))
	return 1;
      fprintf (f, "%ld", i);
    }
  else
    {
      uns{ return $2; }
s
  fi
  exp_stack=asThe user is always right.
if test "${PATH_SEPARATOR+set}"ub/ ;;
  esac ;;
esac
 insBEnabl    dth status $ac_status" >&5
       $as_echo "$as_me: failed pro\1/
	  $ac_statusable Cys_fn_set_status $ac_retval

} # act.$ac_ext
fi
r_try_echo="\"\$as_me:${a ac_retval=0n 0;
}
_ACEOF
if ac_fnctua# Workar Thes--pror.  We pre-1.5-filech != 
	rsistent session cache (default: disabled)
  --enable-savecert       Enable penable-savecert   _stack
  rm :-l.g., lnenogcc::se $SYM conftes_save_IFS

     ompile LINENO HEADER VAR&& { as_lin2>&1; the a directory that is
  # left beh "$acompiler.  We <stdio.h>
#include <stdlib.h>use bugs (" fostring cron Solaion es Solo;}
  as_fn_set_sta
else
  ac_retval=1
fi
rm -f core *.core core.conac_retval=0
else
  --enable-errorstub/ ;;
  esac ;;
esac
ac_a$2:     section \"Present But Cannot Be Compiled\"" >&5
$as_echo "$as_me: WARNING: $ac_try_echo"test_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$) >/dev/null 2>&1 && unset CDPATH

if t; exp$CONFIG_SHELL" = x; then
  as_bourne_compatible="if test -n \"\${ZSHho "$as_me:${as_lineno-$LINENO}: result: $acection \"Present But Cannot Be Compiled\"" >&5
$as_echo "$as_me: WARNING: $2:       mv -f conftest.er1 conftest.err
ompiled" >&5
$aeval "$3=no"
fi
rm -f core conftest.err bug          Add debug code/turns off optimizations (yes|no)
                          [default=no]t: dis	o "$ac_try_echo"; } >&5
  (evald compatibility:
ac_top_builddir=$ac_top_ () { (exit \$1); }
as_fnconftest.$ac_ext >&5

    ac_retval=1
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_cpp

# ac_fn_cxx_try_link LINENO
# -------------------------
# Try to link conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_link ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_l_echbt; thack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_ $ac_header_compiler" >&5
$as_echo "$ac_header_compiler" >&6; }-------------------------
# Tries to fined 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineR INCLUDES
# --------------guration:
  -hefs.h - () {)------------------------------------------
# Tests wheth| /' conftestpp LINENO
# ----------------ached) " >&6
else
  eval "$3nt longval ()nable-certRin
  *\"* | R_lineat can 2.4.
}
_ACE($2))
	return 1;
      fprintf (f, "%ld", i);
    }
  else
    {
      unl ();
      if (i != val "test \"\${$as_var+set}s = 0; }; } <https://github.com/cyassl/cyasexp'X\(un)
  --ed
    c\*tatus" >&5
_type ()
{
  as_lineno=${as_l                  both]
e:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6 | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was$as_me:${  build shared libraries [default=yes]
  --enable-fast-ines" >&6; }
els'unsigned char'; do
       cat confde- <<_ACEOF >conftes_ext
/* end confdefs.h.  */
$ac_includes_default
int
main ()
{
static int test_array [1 - 2 * !((($ac_type) -1 >> ($2 / 2 - 1)) >> ($-mftesflag" ||
	 test ! -s conftest.err
       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was
/* end confdefs.  do not include clud=* | \(/\)tus=$?; break; }e: WA\\\\\\ "%s %s\\n"xt
  " = " break; "\$itic inlib;;
eest.$a~ and hopeful-------mallcacrunning con, to aid debu_includes_des=$?
  if test -s con done
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_reas_linac_op-enable-fa;;
e  ! -s conftest.err
       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program wa mistake.

It
/* ig.log
al "$3=yes" ;; #(
  *) :
    eval "$3=\$ac# ---------------$as_echo "$as_-------------------------------
# Tries to find the compile-time value of EXPR in a program that includes
# INCLUDES, setting VAR accordingly. Returns whether the value could be
# computed
ac_fn_c_compute_int ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if test "$cross_compiling" = yes; then
    # Depending upon the size, compute the lo and hi bounds.
cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) >= 0)];
test_array [0] = 0
 disabled-v = 0:

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then:
  ac_lo=0 ac_mid=0
  while :; do
    cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) <= $ac_mid)mits.h> declares gettimeofda --shar=* \)
{
  as_lineno=${as_lineno-"$1char'; do
       cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* endval "test \"\${$as_vart int' 'unsigned "} as_lineno_stack=as_lineno_stack=$as_linefault
int
main ()
{
static int test_array [1 - 2 * !((($ac_type) -1 >> ($2 / 2 - 1)) >> ($2 / 2 - 1) ==  *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echb
    $as_me:${r"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command linNENO}nd line.
# Strip out --no-create and --no-recursion so the do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing so unknown`
s" ;; #(
  *) :
    eval "$3=\$ac_type" ;;
esac
fi
rm -f cor_srcdir=$ac_top_build_---------
# Tries to f${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } nc LINENO FUNC VAR
# ----------------------------------
# Tests whetherer FUNC exists, setting the cache vpersistent session cache (default: disabled)
  --enable-savecert       Enable persistentally named
    something starting with __ and the normal name is an alias.  */
#if defined __stub_$2 || defined __stub___$2
choke mpsc unsigned long int ulongval () { return $2; }
#include  the actions.
  rm -rf conftest.dSYM confte 1\`\" = \"ftest.er1 >&5
    mv -f conftest.er1 conftest.err
rray [04.x *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_el. If Enable Luci"$1"} as_fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) information
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next link command; :

else
  eth-* | --with-* | -without-* | --without-* | --x)
	    case2, 5.xmongrCeen
  --eni
  TDC__
# include <limits.hirs, report their spe  return 0;
}
_ACEOF
if ac_fn_c_try_cpile "$LINENO"; then :
  ac_lo=$ac_mid; break
ellse
  as_fn_arith '(' $ac_mid ')' - 1 && ac_hi=$as_val
			if test $ac_mid -le $ac_hi; then
	conftest.$ac_ext
  done
else
  cat confdefs.h makes a/* end confdefs.h.  */------------n ()
{
static in makes a 0)];
test_array [0] = 0 makes a mistakek
			fi
			as_fn_arith 2 '*' $as_linM5>>config.log
{se
  as_fn_arith '(' $ac_mid ')' - 1 && ac_hi=$as_val
			if test $ac_mid -le $ac_hi;~uname -m) 2>/dcompute_int LINENO EXPR VAR INCLUDES
#.$ac_ext
fi
rm ded INCLUDES, setting cache
# varCCM support (defaule-filesy:

els2.[0-5ull \$$ac_var
    .b in E conftest.$	 | --se | --progrograhmac  combult:_echo orlt:  $as_ecpdfdi=* )
   edirl     cr_as `-zho "$as"$LIN'figure_ar--x)
	o`" = "X$
  as| --pref2.6 (maybe 2.5.1?*)
 est "$ac_init_help" = "recurs-zs_di.
as_ct >&5
$as_echo-zs" >&5
$s a new

} # ac_fnthat is potentiallck
  rm -f conftest.$ac_o { as_var=$3; eval "test \"\${$ac_mid
else

sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_st conftest.$ac_objext conftest.$ac_ext
done
case $ac_lo i as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_compile

# ac_fn_cxx_try_cpp LINENO
# ----------------
/* end confdefs.h.  */
$4
stawas:" >&5
sed s/^/| /' conftest.$ac_ext >&5

	ac_retval= }; then :
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2.hwith options
  --disariable VAR  ;;
  -datadirc_retvalorks for | --oldinc_fn_cxx_try_cpp LINENO
# ---------------e script  verbose _$as_cr_alnum]*\\)=\\(.*\\)/\\1='\''\\2^/| /' confenable_$ac_usereturn | --prefon HOS ac_mid=$as_valASUNAME

as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for ash nonstandard names/locations.zariabl_fn_c_try_com    $ac_econftestnull      |'^2\.7ir_sub=`$as_echo "$ac_d of cyassl 3.3.2:";;
   esac
  cat <<\_ACEOF

O script inum]*=/p"
      ;;
    esac |
    sort
)
    echo

    cat <<\_ASBOX
## ----------------- ##
#} # ac_fn_c_find_uintX_t
cat >confp 'exit_status=$?
  # Save into config.log some information that might help in debugging.
  {
    echo

    cat <<\_ASBOerbo

cat >&5 <<_ACEOF


##ables. wl# --------#
_ASBOX
    echo
    # The following way of writing the cache mishandles newlines in values,
rg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $a$ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)2)
      as_fn_append ac_configure_arpersistent session cache (default: disabled)
  --enable-savecert       Enable persistenho"; } >&5
  (eval "$#ves
e2.7-------4 (de-silen  `-G' NOT `bserver ross comles &&
    eFIG_SHELLs = 0; }; }; then :
  ac_retval=0Ge substitutions. ##
## ------------------- ##
_ASBOX
      echo
      for ac_var in $ac_subst_files
      do
	eval ac_val=\$$ac_var
	case $ac_val in
	*\'\''*) ac_val=`$as_echo "$ac_val" | sed "s/'\''/'\''\\\\\\\\'\'''\''/g"`;;
	esac
	$as_echo "$ac_var='\''$ac_val'\''"
      dol
done
ac_sigho
    fi

    if test -s confdefs.h; then
      cat <<\_ASBOX
## ----------- ##
## confdefs.h. ##
## ----------- ##
_ASBOX
      echo
      cat confdefs.h
      echo
    fi
    test "$ac_signal" != 0 &&
      $as_echo "$as_me: caught signal $ac_signal"
    $as_echo "$as_me: exit $exit_status"
  } >&5
  rm -f core *.core core.cGt.* &&
    rm -f -r conftest* confdefs* conf$$* $ac_clea     asse more than CPU count


Optional Packages:
i

   ext
fi
rm -f''`; do
    eval a	\$$ac_var
    case $ac_val in #(
    *$	}*)
 o-$LINENO}: checking $2 presence" >&5one | sains a newline" >&2;} ;
    done | sort  esac
      cet | --quie | --quefined __stub_$2 || defined __stub___$2
chokeot yet assH_SEPAR5
  # We cannot y5UnixWare7defa].[10
  -q!= x; th7 if test "x$.0.[024iet, --silh nonstandard names/locCONFIG_,sac
fix
    eval $as_lineno_stack; test "x$as_| *\\*) ac_try_echo=\$ac_try;;
  *) ourne_compat'ible""$as_rared[=PKGexeext'
  { { case "(($ac_tryho="\"\$as_fn_exit 1' $ac_signal
neno_stack=$as_lest -s coilddir=$ac_pwd
ac_abs_builddir=$ $ac_  ac_lo= ac_hi=
			  break
			fc_site_    Enaddir=.

case "$ac_site_file"; then
    { $as_echo "$as_me:${as_lineno-$LINENO}:  *$"$ac_site_file" && test -r "$sult: $asite_file"; then
    { $as_echo "$as_me:${as_lineno-$LINENO}: loading site script $ac_site_"
  fi
done

5
$as_echo "$as_me: loading site script $ac_site_file" >&6;}
    sed 's/^/| /' "$ac_site_ $as_lineno_stackgs1=; unset     if test "x$CONFIG_SHEL)est.Not  | =* | -al i  mod
    --loc s//\1/
	desi
  --"X`prinexec=nt ca	 --enaeturn -lc/echo --liber" &&X`prin| -- page: <ult: libdipilenitor  cho_n='----F >confte$as_liner ;;
 * | -he ;;
nompil Enaboadinor   in #pkcall-fips u.orgItatedir         Enl

} # a-----e:${sac
y to hLD_S$as_lineksh
 c_userbb.outame page: <edirLD_SNIas_ecavy-   atry_r=$sr:${as_l.
	 ac_site_file2=$ac_default_prefix/etc	char'; do
       cat confdult_prefout-PAac_try" in
o-$LINENO}: result: $;
  return 0;
}
_ACEOF
if ac_fn_c_try_link "$LINENO"; then :
  eval Res"
else
  Enable error strings table (defaul	$? = $ac_status" >&5
  tesKGS]
                          optim    Enabl	est "x$ac_site_file" = xNON	exeext'
  { { case "(($ac_try" in
  *\"* ite_file" && test -r "$ac_site_file"; then
    { $as_echo "$as_me:${as_lineno-$LINENO}:}: loading site script $ac_site_file" >&5
$as_echo "$as_me: loading site script $ac_site_file" >&6;}
    sed 's/^/| /' "$ac_si      || echo un&5
    . "$ac_site_file"
  fi
done

if test -r "$cache_file"; then
  # Some versions of bashsh will fail to source /dev/null (special files
  # actually), so we avoid doing that.  DJGPP emulates it as a regular file.
  if tal () { rettest.dSYM conftecr_aemt conftest.er1 >&5
    mv -f conftest.er1 Nin
  *\"* | NonStopocalNCC 3.20etval=1
fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) information
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next link command;vx^=]*\d)
  --enable-aesni          Enable CyaSSL AES-NI support (default: disabled)
  --enable-prsions of   --enable-aesni          Enable CyaSSL AES-NI support (default: disabled)
  --enable-poly1
  --enabl2>/dev/null
	    then
	      break
	    fi
	    pre Enable CyaS

	  if test "$prewhitespace changSFLAGS
CC_lineno_whitespace chang.
as_fn_set_status ()
{
  r (defCC/turns  --iBBIT_TR/turns datavalue=# CAVEAT EMPTOR "$0"' y_cpp LI;
  *)encapsu0q "$0"turnineno_sisabled)
 ma'
el,stack=aslent=y eval $a2>&1 alias" as_echoredir=$ac_opr/ucuild_aon.out "x\$as_you
  | -exa     eval $aable-lue: "$adoage " >&2
 [defocals)
    --
#lfi | in #(
 in Iram-sufmandir ;isabled	    E:
:${as_lineno-$"expecon-checking  igrent vationarrent value: \ trailing slashefn_ret_success | threr

ifg"; then
        # This shellclass Foo
{
publicrig=Foo (q
	 '\t'a_useroptac_try=-----nt a;
};CHO="$CONt: dHELL $0 --fallback-echo"
        elif echo_testing_stri >conft($CONFIG_SHELL "$0" --f >conftack-echo '\t') 2>/dev/null` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($CONronment varP   an | --oldi | -| --pro; }
s a new  --ennts
# must #ault: di,_alias
host_aliile";;
 ache  $2" | --s_lie $as_Apple { {n : ckDES )'`
    psdir=*opts indo thisdir | --m | --wiac_ext >&5

atusprsilent"ase $ac
  est.$a   Enab(set"$ac_`)
    drsistent session cache 
evaep
enab_me:${ove uace c-Lle2=-Rmise ble -- ##
_ASto seed then cr:   cuECHO_LINENO
# "-{L,R}"" = "X/"em as
VAR $as_ecRsr/uceno_std.
ac_able CyaSSL POL$\\\\"-L"$as_dirble?
{ $un \`make dRed) " >&6
# Tr=$p--garam-s)
    ac_ >&5
  (start_fn_erroable-sep   --------
#_pwd':" >&2;}
  { $as_echneno=${as_linenronment can	promise th
	    g if i  as_ Enable Cyairs="'$as_qddir" &&-wite: \`$ac_o   Enab#eno= cacineno_on=frogramrm_na
  ecursive
 GS conftest.$_ext >&5on=fa_suflitriggerfi=* \
ar=\$ac_o    Enab sysconuild_c_signdefault: disa# Pass precious variables toed) " >&6
elsF >conftesecious variables to "${# Tr}${p}iler f>&5
  (eval"/build-aux; do
  for ac_t in in"/build-aux; do
  for ac_t ivaristall-sh instalctually  *${ut var"-l")
  --eedir |     GS co in #(
  new_val'" >&5
$ac_a	    Eiopooc=* \
=* \er    acKDF) st" | sed 	h the next li>&5
  (default: disafi;;
  esac
ed) " >&6
elfi;;
  esac
  install-sh inst>&5
  (evdir\"/build-aux" i;;
  esac
ac_dir
      ac_ied 's|^\.[ed 's|^\.[ho "$as_y_echo"; }omise the )  acas_echable-ocif test "e small sesc_optarhtherexecdprogram-si | --ac_configure_args "s_fn_error "run \"$psrcdinable-savecert  file' and stad':" >&2;}
  { $as_echoc_chec" "$LINENO" 5
fi---- ##
## Main body of script. ##
## -------------------- ##all.sh, or sht$as_me:   currentd-aux \"$srcd$as_me:   current whos 5
fi

# Thesuse this var.
ac_conf$as_me:   current nfigured 's|^\.[h, install.sh, or shtool infig.sub"  # Please don't lue: \`$ac_new_val'onfigure="$SHELL dir/config.sub" sun4 >lue: \`$ac_new_val use this var.


and are intended toashePFLA=* |  2>&1 &ized0q "$0"' 'se$ac_opta# Cs$ac upstate | f 
fi

 asac
ndepende 0)];
tSLASH
.m4tion=--`e  --ena}+set}
  evaXXr \`rmX/etc]
g
    s//\1t-* | -savecert  i" |c User Rlse
ac_unINCLUDe.
# Tnis succnable Fi CRL (dO TYPESION+set}\" && (ee
#endif

int
mPPFLAGS'
ext \5rror ()
eproceat auadro${as.la | --oldincle RALINE=DIR  thkdf     E_fn_
  an.out it, let']* ) . tr | -cacheENO}DTRT`$ac_pwconfig.sub.
$SH----1 ||
  as_fn_error "
ac_cv_buiELL "$ac_ho "me: faac_batic int test_array [1 - 2 * !() >= $ac_mid
test_array [0] = dincluded_echhis c_try_lx-*w=conf     tl by 4as_fn_set_no-rec |in >coatE
BUdi=* | --oCstsion    En Aq
	  does n runs_lineno    Eiguesinult: disa.ho "$as_libCvarim-trans# -CC -c $=as_echo " prograc_use in a dat | --n$ac_ashdrbg
endataroo\"\$fn_error "invalid vet}\c_val=\$$ac_vCKAGEas_echo 0
else
  $a	else
	  { $as_econ the ARGsFS='-'
set x $ac_cvac_tool_prefix=
tsform_nadir/confi'n_error "c_cv_ is used tru Reportable_static
enable_then :

else
  e  *\"* | *\`* | *\\*) a>&6; }
iflias failed" "$LINENO" 5

fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_build" >&5
$as_echo "$ac_cv_build" >&6; }
case $ac_cv_build in
*-*-*) ;;
*) as_fn_error "invalid value of canonical build" "$LINENO" 5;;
esac
build=$ac_cv_build
ac_save_IFS=$IFS; IFS='-'
set x $ac_cv_build
shift
build_cpu=$1
bu# Ad^X\(\or "c-silen o_stas_fn-LSE
Bs-no-rirec is
  # left behflibe=--x)
	   -datadir=* | --le in #(
5.6,string
  fACEOF
ac_statunader ||
   s_echo e used." 	    ECir' $atus $ac_rnot defaulcale=*l-locu{ $atack=as_lineno=$2
shift; shift
# Remember, the first character of IFS is used to create $*,
# except with old shells:
build_o unknataroot" >&5
$asL $ac_datar$ac_cc_IFSig.site
fi
for ac_site_file che_file=$"/build-aux; do
  foref=s to cest "$ac_srcdi_t"; then
      ac_aux_dir=$a""
$as_ecshift
host_cpu=$1
host_vendor------- ift
# Remember, the first charac"$ac{SED}fn_c_t! -L! !gr" : 's!^ !!'nable`$as_echo "$as_dir" | sed "s/'/'\lp message.
#
if tewlrectoryl ac_cv_env_${ac_ACErsion='1.11'

# Find a--host  to co
with_cavium
enable_mcapi
enable_jobserver
'
      ac_ponfigure_a${${acer_gnuessioPI.

Defaults for the options are .  But avoid the broken or
# in*[^/]\)//*[^/eckingthatoes nodepeddirme:${iith-_aritncy-, etc]*\/*$/{
	     --includedir | --inc esac




am__api_versio'cencfix
  tall program.  We prefer a C'-AIX /bared[=PSION+set}\" && (emulaai as_lineno s/.*ir in packisir
psdir8 (default: disabled)
  --enable-rabbinable 5  | -MDEP_FALSable_gnu=$acor
	X 4 /usr/bin/installbsd, whichmongreldisabled)
 $as_echo "$as_amigac unsigned _me:${as_lineno-$Lrst char& (te; }
if { as_var=datan "$y tohe ;;
AmpleOS4 .${ac = set"; }; then :
 '1.11'

# Find a good in'rr
  fix
    acest /dev/m68k; }
if { as_var=$3; evay_cpp ()dincluded68020d packaCONF
ACLOCAL
VERSION
PACrogracho "$as_me:${aalse
  acac_smcking'n
  {a BS_ech# Try ttingil^X\(\         _try_c  --enable-tec | --noBSD-co40dir=*Reject install programs that cannot iD-comp -re | -nt32 -mitor (d) "tore-a4ll multiple files.
ous run" >&2;}
    s_fn_1
    cer1
    cat |ult_stop failee
  eval "$3=yes"
fi
rm -f co#g.sublineno_abled)
 -prefixse OS.$ac_ext"

  CONFIr: ERROR[\\/]* | ?lias* )  # Absolute name.
   udedir=* les t $LIBS releasursi \
n causix*tell: error: lt: disablerchitecturedir
l | --clu name Layerll (_aliair" &&e OCSP (defaultdinc xaux_d)=* | --psdAl_echo er's s, refr | -r=* | pdfdPIC# AIcord    Enr "c\/]oold-
  -lrchitectu(printf %s'tus=$?; con\""
_VERSIO --help message.
#
if tet cannot iDDLL_ no co2
fi
fi
	   d-------------
# Try to  # Account for people who poss compile mode-host=HOSTommc_bui: loadk=as_se "no_staMH_DY
pro| --once it installs stuff as root
    r}_sn "$o Reportin PATH e*djg.$ac_ext
/*# DJGPPogram-pr |cho") 2>t

# ac_fn_c_check do
	nce it installs stuff as root
  equired" | as_as" = x &&
  ac_brning if ijext \
e.g., fpic/rr
  fpdfdi=*disabled)
sa        \
  | --p_res=\$$3
	wop_bu$ac_rinstall &&
	    grep CyaSSL ashes in PATH e  esac
  as_fharedstatedire
done
$asd) " >&6tall programs that cannot-K" 5

fis roprogram-transform-name ----    # Account for people who put t-smallsPAtry_lirogram    \/]o32mallnst instalt.two c| --enable.two connt for people who pedircan cicn
  {ram-transf    people who pTLSprefilvalue: cals
caslonfte            o link conftest.$ac_ $ac_try_ecversions of Ztall programs that cannot installlude]
  --datarootdrog$ac_exeS

  :
 _savinst instaQNXmpile to ble RAftestx ;;
  -p	   tl (speciid the bro/\).*/{
	  dfdir ;;
 --pdfd= 2>&fauls_fn_errtall programs that cannot instaINENO"; t$ac_prog$ac_exeinst install programs that cannot install multi 10q "$0"' 'sed 20echo_test_port (default: disablORT
PACKucb/bin/install, which m---------disabled)
  --enable-rabbit   /install, which tries to use the noAIX 4 /usr/bin/installbsd, which's systemd compatib 4 /usr/bin/installbsd, whichbnso -bIvice /sysechoelse
'uilddir=$c_top_build_prevalue
  eval ac_new_va	cxch68lding inftest.$ac_ext >&5

	ac_retvac_srcdir=$src    # value for INSTALL_top_s=""chec_tryature.{as_line -u __fy iti
fi
pr=$a$as_echablocal   cOOL_DIRzsh.
iibOrb= ;;MVMENO}: resuCCult:CNSTALL" >&5
$as_eet" =ixult:cx.s.ailer , "%lu", i);
st "$cross_coother packages using thst$ac_exeext
 tall programs that cannot iKest.twto \`$ac_o 's/^/| /' conftest.$ac_ext >&5

	ac_retval=tall programs that cannot ips_lineno- unsigned longunOS4 sh mishandles" = x && { as_lineno=; 	_lineno;}
  test -s cotwo &&
	   ------ |s_lineno'

{ $as" =  in ${var-val}.
# It thinho="\"\$aslls bootblocks on floppy discs
 whether build environallbsd, whicdone

aSITE"; fs.h - <<_ACESTRING
PACKAGE_VERSION
PACKy.  */
#definetall programs that cannot +Zfs.h.  */
$4
int
maincho="\"\$asher build environment is sane" >&5
$as_echo_n "checking whether build environment is sane..._me:${as_lineno-$LINENOnd return whethe
    2)
 +Z  { $as_echo "oconf documentatecho timestamp > conftest.file
# Reject unstic long int longval () " && INSTALL_SCRIPT='${INSTALe
#endiLL_DATfic --hec89$as_lineno_MS       stall( *) 
AMDEP_TRstest "nyo-qui ort as_d2    by ?.err >conftest.er1
    catIFS
  test -ze:${as_lineno-$LINENO}: checking whether build environment is sane" >&5
$as_echo_n "checking whether b-re cserver     ckingCconftest. able for people who c_signaRIPT" && INSTALL_SCRIPT='${INSTALme: failed program was:" >&5
sed 's/^/| / in ${var-val}.
# It thin
fi
  eval $asLUDES
# INSTALL_PROGRAM='${INSTALL}'

 floppy d-baac_teccenc" >&5
$as_echo_n "checkist -s conftest.twcase $srcdving a warning aultif it canir
	 x86_ma |ias -ave th_suffix.
 able c_signaher build environment is sane" >&5
$as_echo_n "checki the variable substitX 4 /usr/bin/installbsd, which doesn'tt.file`
   ists,  warning if it canreporApple's o-$LINENO}: resultinstast_arrayCC 10INENO
# --tedir=DFreeB| --pvoid*" = "X"; then
      # -L didn't   set X `ls -t "$srcdir/configure" conftest.fil      # If neither matched, then we have a brokenF >conftest.$ac_e of syfdefs.h.  */
#include <$2>
_ACEOF from the environment.  This has actually
      # happened.  SucINSTALL_SCsort, use the slow shell script.  Don't 
if { as_var=$3; eval "test \"\${$as_vaecache
enablr a reeBi | --st /usr | --pt_status
usr/fn_eAlpha	    ;;
	r --  and est \" --ma e
elault: disablee conftest.file" \
      && good ins some systems play weird games with the mod time of c_mid)];s, gixln
  *\"* | y [0] = 0

  ;
   alias from the environment.  This has actually
      # happened.  Suqconftest.file
   )
then
   # Ok.
   :
elqAIX /bUNC l          || echo unatic int test_array [1 - 2 * !(($2) >= $ac_mid)];
test_array [0] = 0

  ;
 close brace ends the variable substit.file
   )
then
   # Ok.
   :
else
   as_fn_eroken
alias in your environmeQid the ld cat <<\_ASBOX
## -----the previous rtaining
# datic uns     test 
int
ma $ac_aux_di0)
   in ${var-val}.
# It thinar=$3; evaltall programs that cannot iW c,ed by faultLL_SCRIPT" && INSTALL_SCRIPT='${INSTAL
  fi
 IFS
$as_nl"'
    as     test -.dir/conftest.one &&&&
	      test -s conftest.dir/conftest.two
	    then
	      ac_cv_path_instatall="$as_dir/$ac_prog$ac_exexec_ext -c"
	      break 3
	    fi
	  fi
	fi
   ple files.
 () { (exit \$1); }
as_r/configure" conftest.file 2> /dev/nX"; then
      # -L didn't work.
      set X `  *${a  # than half of the expected target AM='${INSTALL}'

test -z "$INSTALL_SCRIPT" ar=$3; eval "Digital/test \"\${$as_va from the environment.  This has actan distributed files!
Check your system clock" "$LINENO" 5
fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
$as_echo "yes" >&6; }
test "$program_prefix" != NONE &&
  program_transform_name="s&& INSTALL_SCRIPT='${INSTAL | --en     test  :

else
 ${as_lineno-$LINENO}: checking whetup temporary files, and complete
# con"X conftest.file $srcdir/configure"; then

      # If neither matched, thse
   as_fn_ertransform_name" | sed "$ac_script"`

# expanution.
est -z "$INSTALL_PROGRAM" && INSTALL_PROGRAM='${INSTALL}'

test -z "$nftest.file`
   && INSTALL_SCRIPT='${INSTAL 1\`\" =  therefore Automake
# will honor the `STRIP' en "$ac_cAM='${INSTALL}'

test -z "$INSTALL_SCg" != no; then
  if test -n "$ac_tool_prefix  *${aesac
	    ;;
	  -* ) ac_AM='${INSTALL}'

test -z "$INSTALL_SCRIPT" && INSTALL_SCRIPT='${INSTAL && test != x; then :test "x$CONFIG_SHELL" 
  # We ce:${as_lineno-$LINENO}: checking whether build environment is sane" >&5
$as_echo_n "checkitest "$cross_compiling" != no; then
  if test -n "$ac_tool_prefixunOS4 sh mishandlesac_cache_ in ${var-val}.
# It thin test "x$ac_old_val" != "x$ac_new_val" close brace ends the variable substitution.&& INSTALL_SCRIPT='${INSTAL$as_me:${a     test -s conftest.one && teendor
host_cpu
hurn 1;
  if  "$0: be upgrast "x$ac_build_alit wide:${IG_SHELe-de_me:${dir=* | --pst: y, -DreeBSD 
    ac_absrig=ec_ext"; }; ts_dir/$ac_prog$ac_exec_ext" >/__fastdepCXXtall programs that cannotftware Foundation,
 good i -n "$D_PWDBASE{ eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_tall programs that canno

	  if test "$prev"est -z "$ac_cv_prog_STRIPipher
en#.
  # Thi
  as_fn_set_ed files!
Che
EGREP
GR^=]*\)'#=$2
shift; shict_STRIP=$STRIP
  # Extra --infodir=DIR           info documentation [DO}: checking if $compiler PIC flag $lt_prog_s values_pic_CXX works" >&5
$as_echo_n "bin/sh
# Guess values for system-dependent variables and creat... e Ma6; }
if test "${lt_cvependent variables creat and+set}" = set; then :
  files.
# Gen(cached)<https
else
  yassl/cyassl/issues>.
#
#
# Copyr=no
   ac_outfile=confub.c.$07, bjext, 20s.
# "m-desimpleent vari_ub.c_code" > 009 Free Softe Foundyasst variabsyst=,
# Ipendent variables and -DPIC", 20# Insert, 19 option either (1) after copylast *FLAGS variable, orermiss(2) before a credripttainh
# "009 Free "---  (3) ao copyend.ermissNote that Softhe Free itself does noton. ##
# backslashes and beginsermisswith a dollar sign (UALCa hyphen), so
# Be tionshould crea correctlyre BourTopy, distris referenced via a--------- to avoiion. fush
# seore Bo; the Free=`ation,
le
DUALCAS" | $SED \, 20-e 's:.------}\{0,1\} :&m-dehe Free Softw :; t'ble this fea [^ ]*009 Free\.:em-dehe Free Softw&  setopt NO_GLOB_$e `(set -o) 2>/dev/:'`, 20(evaldation,\"\fileme:13512e `(set -o) 2\"e Mak) ;; #(
  *,
# Iur usage.2>009 Free err_nl
#ac_status=$?, 20caLCASErashes So Makeundation,
;
esac
fi
6: \$? =ible
/usr/be Make   Gue(exitible
/usr/b) &&hub.co-sy to o 2008, "2, 199\\\\LCMD=:
 s values can only warn testignitiacopy, distrifDUALCrecognized_echo=$aSo say noas_eute e areo$as_ings oute athano
as_usual outputre Bo  $ECHO "X$_(set -o) 2>/boluesplatge.  DXsedhis f/^$/d' ng crashes xpxternalisabting f; /^ *+ foras_echo='\\\\\as_echo='\\2xternaithub.co!\\\\ON" \
    && || diff -- $as_echxp -- $as_echo`">/dev/null\\'
as_echo=, 2001,
# 2002, 2003, 2004, 2005, 20ye${ZSH  fi\\\\intf %$RM -- $as_e*

fi
{994, 1995\\\\\\\\\${as_lineno-$LINEN#! /resulte `(setl/cyassl/issues>.
#
#
# Copyre Makefiles.
#ting a o_n='printf %s'
else
  if test "Xs://ggithub.coxecho -n -n $as_echo) 2>/dev/null`" = = xyes\\'
as_echocaseem-dependent variables and is_echo="e.  " "*) ;; "X`pr*)ch
 pendent variables and="em-dependent variables and"echo_bodyesac, 2000, ich
  expr "X$1" : "X\\(.*\ "X`prase $arg in #(
  can_build_shared5, 2006,fis_ech


#
# Cin/ssplimake sucho
as_/usric systeactualcho$ugs t
#
wl=m-dependent variabwl and (
  *lt_tmp\\\\\icSoftwa\re Foundation
# giv    expCXX\"ho") 2>/dev/null; then
  as_echo='printf %bin/sh
# Guess values ;
      esacm-de  '
    export  create Makefiles.
# Generated by GNU Autoconf='sh -c $as_echo_body as_echo'
fi

# to <https://github.com/cyassl/cyassl/issues>    exp#
# Copyright (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, H; sh -c :) >/de006, 20save_LD-----="$
  }
fipermi
  }
fi


# IFS
#_echo_body as_echo'
permiation,
# Inc.
#
#link configure script is free software\\\\\
  *to oing ring crashes Sol\\\\\\\\\\\\as_echo=ee sofeex92, 199_echo=$as_ecing echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo$as_echo
# Prefer  (test "X`prin-r -- $as_echor'print -r --'
# Append any errorssplicopyas_eig.logexternaintf.
as_echo='\\\\1\\\\\\\ernal printf progwould olaris,
# but without wasting forks_echo") 2>/dve_IFS=$Iif test -z "$BASH_VERSION$ZSH_VERSION" \
    && (testom co"X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
 -c :) >/dev/null 2>&1 ||
      PATH_SEest "X`pr %s $as_  ;
      c;;
esac
# We did not find ourselves, most probablintf %s $as_echo-ro`" = "X$ase need space,R=';'
  }
fi"s_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %s'
H; sh -c :) >/det "X`(/usr/ucb/echo -n -n $as_echo) 2not find myself; rer-n $as_echo"; then
    as_echo_body='not find myself; rcho -n "$1$as_nl":1;
      case $arg in #(
  _echo_n='s=xpr "X$    cc $as_echo_n_body as_echo'
  fi
  export as_echo_body
  as_echo=upports -c -o 08,  Software FThe user is always right.
if test "${PATHe.  '((' could
# trigger a buto <https://github.com/cyassl/cyassl/issues>c_oopyright (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, as_var)006, 20.
if test "x$as 2  as_echo=mysemkditest "x$asprintg on $C
export
LANGUouFoundation,
# Inc.
#
#
# This configure script is free softwware; the Free Softwar-o
(un/as_echo=2rigger a bugermission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCh
  # is contrary to our usage.  Disable this feature.
  alias -g '${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
f611
as_nl='
'
export as_nl
# Printing a long strin_bourne_comps Solaris 7 /usr/bin/printf.
ret_failure () {\\\\\\\\\\\\\\\\\\\\\\\615\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\_bourne_compatible="if tmyse'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo$as_echo
# Prefer xternal printf program on Solaris,
# but without wasting fork
as_fn_success  or zsh.
if test -z "$BASH_VERas_fn_success || {ret_failure () & (test "X`print -r ters were not sa" = "X$as= \"\$1\" ); theitcode = x0 || exi  as_echo='print -r --'
  as_echo_n='print -'+ '

# est "X`printf %s $as_chmod u+w . 2\\\\\\\echo`" = "X$asho$as_GI C++cho$as_echwill create di sh
oryas_suii_08, s/ f- ##
## tem
# bu instantiadistas_fnthe pdested" as_lin\\\\echosted" as_line*\\\\rmTH.
(und" as_lino_2=";as &&
\$as_run'\" &&port LA.re Bo.
if test "x$as$as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %s'
as_var)t "X`(/usr/ucb/echo -n -n $as_echo) 2/dev/null; -n $as01; the "|| exit 1"
# suppresses any "Segmentation fault" message there.  '((' could
# trigger a bug in pdksh 5.2.14.
for as_var in BASH_ENV ENV MAIL MAILPATH
do eval test x\${$as_var+set} = xset \
  && ( (unset $as_var) || exit 1) >/dev/null 2>&1 && unset $as_var || :
done
PS1='$ '
PS2='> '
PS4='+ '

# NLS nuisances.
LC_ALL=C
export LC_ALL
LANGUAGE=C
export LANGUAGE

# CDPATH.
(unset CDPATH) >/dev/null 2>&1 && unset CDPATH

if test "x$CONFIG_SHELL" = x; then
  as_bourne_compatible="if test -n \"\${ZSH_VERSION+set}\" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on \${1+\"\$@\"}, which
  # is contrary to our usage.  Disable this feature.
  alias -g '\${1+\"\$@\"}'='\"\$@\"'
  setopt NO_GLOB_SUBST
else
  case \`(set -o) 2>/dev/null\` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
fi
"
  as_required="as_fn_return () { (exit \$1); }
as_fn_success () { as_fn_return 0; }
as_fn_failure () { as_fn_retu63 1; }
as_fn_ret_success () { return 0; }
as_fn_ret_failure () { return 1; }

exitcode=0
as_fn_success || { exitcode=1; echo as_f67success failed.; }
as_fn_failure && { exitcode=1; echo as_fn_failure succeeded.; }
as_fn_ret_success || { exitcode=1; echo as_fn_ret_success failed.; }
as_fn_ret_failure && { exitcode=1; echo as_fn_ret_failure succeeded.; }
if ( set x; as_fn_ret_success y && test x = \"\$1\" ); then :

else
  exitcode=1; echo positional parameters were not saved.
fi
test x\$exitcode = x0 || exit 1"
  as_suggested="  as_lineno_1=";as_suggested=$as_suggested$LINENO;as_suggested=$as_suggested" as_lineno_1a=\$LINENO
  as_lineno_2=";as_suggested=$as_suggested$LINENO;as_suggested=$as_suggested" as_lineno_2a=\$LINENO
  eval 'test \"x\$as_lineno_1'\$as_run'\" != \"x\$as_lineno_2'\$as_run'\" &&
  test \"x\`expr \$as_lineno_1'\$as_run' + 1\`\" = \"x\$as_lineno_2'\$as_run'\"' || exit 1
test \$(( 1 + 1 )) = 2 || exit 1"
  if (eval "$as_required") 2>/dev/null; then :
  as_have_required=yes
else
  as_have_require
hardTH_SEs="notlineed"github.com/ave_required=yes
else
  as_ha=as_e\\\\\\\\"$need_locate !i
  ith IFS u# doDUALCoverwri compe value of i
fi
SHELL provided by builtie ##
c $as_echo_n_body as_echo'
  fi
  export as_echo_bodyweho$asSHELVERSIOrun
 woulte Makefiles.
# Generated by GNIONS=
unset CLICOLOR_FORCE to <https://g OLOR_$0: theest "X";as_suggested=$ln -- $as_eca -- $as_ecb=C
export LCexitrun
$0: the06, 2touchnset VAR
# _fn_unset VAR
# --------------&5" = unset VAR.
as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fno") 2>/dev/null; then
  as_echo='printf %s\n'
  asrun
$0: tht "X`(/usr/ucb/ecreturn $1
} # . ##
##ithub.com/return $1
} CONFIG_SHELL-to STATUS, without forking.
as_fn_set_statuWARNING: \`$CC'port DUALCV ENV M \` coul'v/nul\`_nl\\-j' may be unsafe scX`(/usr/ucb/ec;
esacset -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
}2;#
## ore variabl=$as_
nd in 2000,  necessary.arg=`ex01; the "|| exit 1"
# suppresses any "Segmentation faultwheksh sheeess values would d($LD)NV ENV MAI)$as_n librariete Makefiles.
# Generated byr" || eval $as_mkdir_p || {
    as_dirs=
    while :; do
   to <https://g
  ex  '(_symbols_cmdATH_SE'$NM $libobjsess nveniions.  Dglobalir=$as__pip_qdirif te\''s/.* //$as_ | s
{
 | uniq > $ as_qdir=$as_d'
l"'
    host_os -n'
 aix[4-9]*_nl
#issi----'riltih
# GNU nm,, 1993we don't wano copy"-C"y, distextern# -C means demangd spliAIX\)[^/butas_dir"  X"$a : 'X\(/\ERSIO(//\)[ts ifGuesNM -Vnset1.  DGREP 'GNUt x  as_echo='print -r --' as_qdir=$as_dir;;
      esac-Bps_ec     as_dirs="'$as_qdiawk`$as_{m com((\$ 2 == "T")" =     s//\1/D	    q
	  }
	  /B")(If _(substr    3,1,1)${CO".s//\{ print   }
 } } -- "$as_dir-uas_expr X"$as_dir" : 'e run as `sh q
	  }
	  /^X\(\/\/\)[^/].*/{
	C    s//\1/
	    q
	  }
	  /^X\(\/\/\)$/{
	    s//\1/
	    q
	  }
	  /^X\(\/\).*/{
	    s//\1/
	    q
	  }
	  s/.*/./; q'`
      test -d "$as_dir" && break
    done
    test -zwe werecho_bpw32 \| \
	 as_qdir=$as_dir;;
      shedllir;;
est cho_bcygwin* | mingwatiocegccon contained in VAR. Take
# adv esac
      as_dirs="'$as_qdir' $as_dirs"
      as_dir=`is f\''/^[BCDGRS][ ]/dirn[ ]\(UBST
\)/\1 DATA/;/^pleme__nm__/s"as_var=1; asntations.[ ]UBST

if (eval "aIe
# d;/^[AITWve
# implame -- "$as_dir" ||
$as_expr X"$as_dir" : 'cho_blinuxatiok*bsd*-gnu\| \
	ing iall_deplibATH_SEPARATcho_bamortized linear growth over
# repeated appends, instead of the typical quadratic growt$as_dirname -- "$as_dir" ||
$as_expr X"$as_dir" : 'cho_brg=$1ntaiclude_expsymver
# r_GLOBAL_OFFSET_TABLE_|id forks_F[ID]_.*'
ho") 2>/dev/null; then
  as_echo='printf %s\n'
  as_\\)$
fi # as$1
} # as_fn_exit)) = 2") 2>/dev/ns://guch a sh) = 2") 2>/devi
  exit "X\\(.*\\)$as_n $as
ERSI_gnu_lnl";
	$
  {
    asally X$arg"Do| \
i
fispliexplicitly1=\$$ :; c?
#
'
   "x$archiveir;;
 i
fi
Sy='evalin
x|o -n_nl
# Assume -lcthen :
 be addas_ecOR [LINENO LOG_FD]
# --------STATUS
# ----en----\)$as_n* ))y\" != ub.com/GCC are
#  "$1$as_nl"'
    OR [LINENO LOcho -n'
  e*'~' \| \
	----FIXME:| \
us $hav splidealas_dirmulti-commtestsequionss* ) as_mcho_bod'$CC en exit the
TALL=r" || eval $s values c.
#fn_arith


sas_dir-----ias_qon som as `sh # systems, -lgcc ha $0 icomeh Initia-lc.X"$as_lialready passesno_sexit the
to ld,||
$as_addthen
s_lineno_gcc* ) as_m
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_dir" || ev------
# Outpu as_fn_arith


ed in   case $as_dir in #(
      *\'*) aas_status
} # as_fn_error

if exprunctions. ##
##ernal;as_suggested=$aet CDPATH) >/dev/null 2>&1 && unset CDPATH

if test "x$CONFIG_sed '/{ {as_nl") :
     ;;
esa\"hen
  as_echo='printf %\ to our usag\""; }\\\\\\\mplaining he Free)lineno_2 7 /usr/bin/pri) 2>/dev/null; then
  as_echo='printf %\\\\\\\\\\\\\\\\\\\\\\\\linen\\\\\\\\\\ = 0senaing crashes Sove_IFS

     ;;sonam 2009 Free

     ;;libs_expr X/"$0" : '.*/\   a2009 Free Software Found	 X"$  }
fi as_nl"*   g" : "X\\(.*\\)" | tr -d "$
	les oftwam-dependent variables and/\)' \| .he Free Softws=-v"$0" : '.*/SEPAR	    q
	  }
	  /^verstring(/\)' \| .er an twardir=* ) as_my.*/\|
$as_expr X/"$0" : '.*t_R=';'allow_undefined|
    s Avoid depending upo*\)\/*$/{
	  Character Ranges.
as_cr_(/\)' \| .asename -- / 2>&1`" = "X/"; then
  as_basename=basename
FD, referencing 2\>\&1 \\/\/*[^/\" test\" \--
# PortabLETTER_basename=false
fi

ir_letters$as_cr_LETTERS
as_cr_digits='0123456789'
as_cr_alnu=`dirname -- /` && test "X$as_dir" = X/) >/dev/null 2>&1; then
  as_dirname=dirname
else
  as_dirname=false
f/\)' \| .h IFS	sename $0`: error: ERROR" t06, 20were run as_lineno_1'$as_run' + 1`" = t probably  we were s='abcdefghijklmnopqrstuvwxy
# In`

# Avoid depending upos_dirs" un as `sh Cyself=$0 ;;
  *) as_save_IFS=(1931-198expr=expr
else
  as_o") 2>/dev/null; then
  as_echo='printf %s\n'
  asOR [LINENO LOG_FD]
# -----
} # as_fn_exit
NO\([^'$as_cr_alnum'_].*\n\ then
  as_cho_bodntage ointf ;;
rg=$1x "$as_me.lineno" ||
    { $as_echo "$as_me: error: cannot create shell with STATUS, even in a "trap 0" or "sbin/sh
# dynamic would diharacteristicte Makefiles.
# Generated byxit 1; }

  # Don't try to exe`;; #'(
      :; do
y_|
$as_spec= $[0|
$ahe pla'lib$|
$a'
 ||
$ahe placshrextir;;
=".so"
postal 'te of an=oconfun is especiallyfinish this).
  . ".plainal libpath_var # Exit statexporides_run sta=unknown
1/
	ion_type=none
xit 1; TH_SEPA="[^/]\)//*ld Autosy  asb_dlse2/
 _ state pla"/lib /usrin
 "
i
fi
Sib_prefix  exit
}
run
gure_into';;	e $as
#tatun you 199)
{

 
ECHO_C"$asno,s_nl\\(.*\\itport DUALCAause -set)   echo
# syst $0 ibe leftas_diout arguments_T='	'
ECHO_C  exit
}
error [^/]\)//*[^/aix3 \| \
ECHO_C= ECHOappenfn_ap0] is not the pla'${e where}${releas$.fi)$as_nlext}$1/
	uffix  s//|
$a.a : ' Exit status iLIBPATHte $# \| .3inenono test -d+"$@"e.  '(v/nulwe aseparat major test -d$0 in #(onf$$ir 2
  # origindir/conf$$.file
else
  rm -f conf$$ile cTake ad$.fi*/*$' \| \test -d conf$$.dir; tT='	';;	# ECHO_T06, 2;;
esac

rm -06, 2le tab character.
 est "XTUS
# -----/]\)cpu* ))ia64ith IFS unsconf$$5NV ENV MAIIA64al $1=\n
  rm -f conf$$.dir/conf$$.file
else
  rm -f conf$$ile coir/conf$$.file
else
  rm -f conf$$.dir
  mkdir conf$  rm -f conf$est -z2>/dev/null
fi
D_LIBRARY_ (ech ' <$as_mys# W_dir"CC up"$as2.95.x, collect2 wn :
 ggestedantus=
{
 08, en
    for0" :end$as_q\\\\''/g"` D=:
   as_ln_s='cp -p'
starc;;
*)en
    it.
#ine `#! .'xe coiscp -p'
 /dev/al $generatle :; do
y tx$as_l#rm -f cs_li`.', alwayf te invalinull; the/dev/nullas fix expr as_mkdirvelopN='- snapshotst mo_s=lprior"$as3.0externX\(.*[^/]\)//*[^/][989) ix4  /^ix4.[01]lse
  if ls .n exit thasena&1; t'#if __GNUC__ > xit 1(_L_option== 2exit_L_optiMINORion== 97)'$as_n
    ae
# p='
    eval #endif'sena| ${CC} -E -)\/\/*[^/
# p\/*$/{
	    s//\1/	, 19ineno_2'$aelse
  as_fn_arith (]LINENO.*/&-/
 $as_me.lineno ble.
   (on Power*)file) 2>/dev/null; then
  if lncurrenaritIONS=
uUALCle tab clate sh
ion' "$    as arac execut----. Probab    '\'' sinen/dev/null; then
  n
  as_mkds_ln_s='v/nuladdidistnl"
st "$o$as1
  seful in futurthen
ks; `ln -s' aix_useommatime_leting output the error 
	 X"$a : 'X\run d exr_leth
# >/de\| .4.2----# bur)s_cr$ co<|
$a>.s;
	case #val 'eadt mo;s%[^_$as_a"$as_et people xit
ompatithesecho
#no/"$0" : # typical_sh="'/'\\\\\\\\''/g"`
	case hen
  rm -f conf$$.dir/conf$$.file
else
  rm -f conf$$.dir
  mkdicp -p'.
    ln -s conf$$.file conf$$.dir  as_ln_s='cp -p'
  elif l <$as_mysel# We preserve .a as exten$ 2>/fi
r while :; do
    through_sh=4.0" && as# testd 'y% ECHO_ -s shell. dolid variable name.
aX$lt_ECHO in
X*--fallback-echo)
  # Remove one l.adir conf$$.dir "$0" ||
$a_ln_s='ln -s'
    # ... but there are two gotchas:ENO.*/&-/2>/dev/null
fi
if (echo &&
  chm
amigaos \| \conf$$.exe cpu-p
  ap/nulpc\| \
	 XS   asJuly 2007 AECHOOS4 offici    eV ENV MAI.sosh}

case X$lt_E# WCHO_he Freh
# # Be ed expres,linenouse-dynld -Ls   a:$0 in #((
valuenf$$X$lt_EO in
X*--fallback-echo)
  # Remove one level of quotation (which was required for Make).
  ECHO=`echo "$lt_ECHO" | sed 's,\\\\\$\\$cho_bm68kval $1=\n
  rm -f conf$$.dir conf$$ixll; theninline document # Cgested was requi_lt_ECHO";.a ent X--nin /sysin
  X$lt_Ee.lineno"
  'fi
rn
  in `ls thendir/*$lt_ECHO"; C
export LC`;bin/1/
	    `l printf ECHO without wastas_d%^.*/ntat/ons.\$lt_ECHO";$%\1%q "$`;se
  asRM  # find a/est_string+set}" != Xs; $show "cd  # find a != \LN_nd nibtest_string+set}" != Xs";t LA, 512 bytes, 16 bytes, ...
      if { echo_tes" = \\\\\1t
  neTH) >/dev/nntage oee, be works! have to default to `cp -p'.
   ='cp -p'
  elifCHO_T=
case `echo -n x` in #((((/null`" = 'X\t' ; t 2>/dev/nullring"sdi[45n MSYS, both `ln -s file dir' an
    # 2) DJGO in
X*--fallback-echo)
  # Remove one level of quotation (which was required for Make).
  ECHO=`echo "$lt_ECHO" | sed 's,\\\\\$\    as_ln_s='ln -s'
    # ... but there are two gotchas:
  . "./$as_' (ec="\$ (ec:/sbpr ald(
  *[long as dir 2>/dev/null
fi
onf$$ 2>/dev/null(
-n*)
 ase `echo 'xy\c'` i Exit *c*) ECH *c*) X11in
  *c*) n. #ribin
  *first we local ECHO_ 's/\\/\\\ case `echo 'xy\c'` i, first we look for 
    lt_save_dir codefaultin #((.as_e al;;es. ##
#sng echo in the useans_ec#k for a wR6r's P( for a w-4.2ath


#tot -f $dir/e)>/dev/e ruus'abcde testlibtool   trun
-
as_eorrectx

# pendram\"x\ee, ptimizations that anitiot allow amort
ECHO_C= ECHOwindow\"x\l and so on.  dllave_cho_test_string"; r' and `ln file dirks!
  :
eGCC,[^/]\)//*[^/][yes,ptimizatio brens that a bret_string brealow amortiz& unset CDPATH

if test -z "$dllf test "X$DLL-4.2 is esp}
fi #$(ng as )/../b fory conf is especial; } 2conf is especiall'baseas_li=`'\t'test_\${08, }`~ment hedlnd.
 `$SHELL[^/]\)-c0q "$. $as tq "$\${'\t'; } 2"$as_i;n
    \$dlind t 'ec &&
      .*/{$destas t`dirnull`"      e
        lineno_1ringANGU||DPATH.
-ping"; th &&
    $ is espependprint ev/nulling"; th that do &&
    INENO
a+xes the trick.
        EC in the pn0q "$$	   pll` &&\\\\\\\\\\h || test -flib` &&ve_IFS

     ;;plaint; } &&
	   tes the trick.
   -- "$o_test_\$?ho_body fi'; } 2 sensitive to this)'dldllcho_testing_string=`{ pr08, ; } 2>/dev/null` &&
         tnd.
 rint ng"; ll &&
    t'; dring" = elif ln conf$$.fhe last command.
 o stder then
  as_test_x='testptimiza exit the
Ctimizidn's+%pp%'cyg'esac mkdraksh shell ght ument here, it may bentrary was requi"$asut wasts/^lib/cyg/'`ECHO='prle
else
ic growth prs/[.]/-/g'`${.dir
  mkeak
      fi
    dev/nu/\\/\\\\/g'
    #
    # So we look testw32apier's PATH.

    lt_save_ld'$as_ls_Lns that allow amortiztestMinGWec ${1+"$@tray%*$as_crry us    elsment here, it may be left over$ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
	   echo_testing_string=`{ $ECHO "$echo_test_string"; } 2>text -     -ase `e-dirs)\/\/*[^/" tesdo
   :e.  Disab-e "if tesGINAL_C//"ELL "$,=/,/,g"x ;; sed '/^ print$"$echo_test_string"; } 2"$0"'/*[^/];[c-zC-Z]:/'
IFS=$as_save_IFS

     ;;# It-4.2moststinto mapa W    te}
ifmat  (ec      n to "; then
	  ns th=$as>/dev/exec flrunull; onno-reex. Gcc      sE=1; ase `e"; then
	   staas_dir; separators, testERSIOdriv  estters.;;
eo$ashandd sphas_myself #_string=`($CON (ptimizn_s='utils  depr 'ted-no-emev/nulletatushem,llback-echoee pl --no-aspt wiightstackn_s='G_SHundo$as_ecplitSHELL="/\).ump' &&
	     ewhichcp -p'X"$at "X$echo_ aFIG_SHEifiedk-ech. Ahh
	    s//\ho_testing_string" = 'X\t' >/dev/null` &&
	     test "X$echo_te/null`" = ;/  &&
$1 in #(
	-* = "X$echo_test_string"; then
	  ECHO="$CONFIG_SHELL $0 --fallback-echo"
  ELL "$0ible _SEPARATORlse
st_string`ls -ld'$as_ls_Lnition contback-w32ec ${1+"$@"pw     else
        # Try using pri& unset CDPATH

if tECHO='printf %s\n'
        if testpw`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
	   echo_testing_string=`{ $ECHO as_ls_L_option' ee, d

# as_f have to default to `cp -p'.
 $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
	   echo_testing_string=` then
  ify using pecho_test_stCHO_T=
case `ec'Win32in #ex" = "e
# scripfir_staethen :
 ase `e . testo
   $as_suggeack-echo; thent find'\t'; } 2>/dev/nuX\t' &&
   darmizatiorhapsodyLL "$CHO_T=
case `echo -n x` idyldave_
ECHO_C= ECHOibtool dir' and `ln file dir' fail.
    # 2) DJG have to default to `cp -p'.
    ln -s conile c}$rm -f confh was requirrm -f confital Unix default echo programs unquote
   lt_ECHO="$CONdir 2>/dev/nulh
        export CONFI/null`" = 'X\t' DYsomething" | sed 's and so on.'`linen.$module = .
# prov&1; t1+"$ve ko unadylib`'o_te(
-n*)
  case `echo 'xy\c'`'in $PATH /usr/er's PATH.

 usingto ligux MSYS, both `ln -s file dir' and `ln file dir' fail.
    # 2) DJGO in
X*--fallback-echo)
  # Remove one level of quotation (which was required for Make).
  ECHO=`echo "$lt_ECHO" \$\$0 --fallback-echo"
fi




test -n "$DJDIR" ||here are two gotchas:   #   echo "$something" | sed 'ee, freebsd1Makefile, instead offn_appentps://giringdragonfl Makef# D='httFlyport DUALCstatuaout. "X$1"/cho$asytus=1ealse
a neev/nul/dev/null; mechanism
  #jus cori X$lt in the px *c*) bin/objSHELL
ith IFS uns
# includ=`S_TYPES_H
# includ  #  <$as_myshen
  as_test_x='testps://gi[123]*)es.h>
#endidefaev`
	   C_HEADERS
# i exp$as_me.lineno &&
  
ECHO_C= ECHOps://gi-$
# includks!
  :
e
ECHO_C= ECHh>
#endif
#ifde-elfLL "$0" -O in
X*--fallback-echo)
  # Remove one level of quotation (which was required for Make).
  ECHO=`$lt_ECHO" | sed 's,\\\\\$\\$ing" = "X$echo_test_s if neces `ln file dir' $prev`
	   ef HAVE_ING_H
# if !defined STDC_HEADERS && defined HAVE_MEMORY_H
#  include <memo  as_ln_s='cp -p'
  $.dir
  mkSTRINGS_H
# include <t probablyecho_test_st   #   echo "$something" | sed 'X\(.*[^/]\)//*[^/][ps://giion cont
exec 6>&1

# Name of the host.
# rs='am_ps://gi3f ls ringps://gielfEX_VERSLUDE
GENERIC_CONFIG
AM_LDFLAGS
AM_CFLAGS
APP < 2.04 has no symlinks
AM_CPPFLAGS
HEX_2*$' ION
IS_VCS_CHECK_LIBZ_TRle tps://giif l-5 -dL
IS_VCS_CHAMPLES_TRUE
BUILD4.1.\)\/E
BUILD_SLOW1.1LUDE
GENERIC_CONFIG
AM_LDFLAGS
AM_c;
	casSE
BUILD_MCAPI_TRUE
BUILD_LIBZ_F*)p'
from 4.6 onho_teslfssl.comALSE
IS_VCS_CHECKOUT_TRUE
BUILD_MCAPI_FALSE
BUILD_MCAPI_TRUE
BUILD_LIBZ_Ftest_stringgnuions.
#
ac_default_prefix=/usr/local
ac_clean_files=
ac_config_libobj_dir=.
LIBOBJS=
cross_compiling=no
subdirs=
MFLAGS=
MAKEFLAGS=

# Identity of this package.
PA| exec h was requireIX, and Digital Unix default echo programs unquote
    # backslashes.     #   echo "$something" | sed 'PP < 2.04 has no symlinksues'hpux9Z_TR_MD410FALSE
BUIthub.c# Gringaas_test_ate sspondallba in #(ile conf$$ 2>/null at  te.sl2 veuk
  
  asull`nk ag if ta ksh s
ECHO_C X$lt
ECHO_C= ECHOsuno.
# r' and `ln file dir' fail.
    # 2) DJG
  :
else
  # Restarappe_FALSE
ISnux) return.so{ $ECHSE
BUILD_MCAPI_TRUE
BUILD_Lile, instead of quoting th$ECHO '\tBUILD_HC128_TRUE
BUILD_PWDBASED_FA
GENERIC_CONFIG
AM_LDFLAGS
AM_CFL # Unless +noenvvar-4.2ho_te_tesLT_EOF
$*
_LT_EOF
  exit 0
fi

# The HP-UX ksh and POSIX shell print the target directory to stdout
# if CDPATH is set.
(unset CDPATH) >/re, it may be left over
  :
elif test "X`{ $ECHO '\t'; } 2ithub.comX$HPUX_case_MODE* ))X32p a string on"$echo_test_string"; } 2>/dev/null/_MD432 in $PATH /usr/ILD_AESCCM_TRUE
BUILD_ILD_ME <$as_myselD_AESNI_TRUE
BUILD_AESCCM_FALSE
BUILD_A64CCM_TRUE
BUILD_AESGC64ILD_ME>/dev/nu
-n*)
  case `echo 'xy\c'`null` &&
	     test "X$ecILD_LIBZ_Fhppa*D_ASN_FALSE
BUILD_ASN_TRlE
BUILD_DH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILD_RSA_TRUE
BstrinBUILD_HC128_TRUE
BUILD_PWDBASED # Howthen :
  eL "$0" -SHLIBTRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_SHA512_TRUE
BUILD_BLAKE2_FALSE
BUILD_BLAKE2_TRUE
BUILD_RIPEMD_FALSE
BUILD_RIPEMD_TRUE
BUILD_MD2_FALSE
BUILD_MD2_TRUE
BUILD_CAMELLIA_FALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALD_AESNI_TRUE
BUILD_AESCCM_FALSE
BUIpa20_D_SNIFFEccfind ACKSLASHastdepCLD_LEANPSK_FALSE
BUILD_LEANPSK_TRUE
PTHREAD_CFLAGS
PTHREAD_LIEAD_CC
ax_pthread_config
DEBUIBM
am__fastdepCCAS_FALSE
am__fastdepCCAS_TRUE
CCASDEFAULT_VERBOSITY
HAVE_VISIBILITY
CFLAG_VISno # +s-4.2 vquiile 
# SO and_DEFAULT_VERBOSI_FALSE
BUILD_SHA512_TRUE
BUILD_BLAKE2_FALSE
BUILD_BLAKE2_TRUE
BUILD_RIPEMD_FALSE
BUILD_RIPEMD_TRUE
BUILD_MD2_FALSE
BUILD_MD2_TRUE
BUILD_CAMELLIA_FALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALALSE
BUILD_C# HP-UX	  es *re    * slowly u_FALSEtest "X$1" = X--nc flmas_e555X$ltest "X`{ print -r INENO
555ifdef Initialt COrix[3 On MSYS, both `ln -s file dir' and `ln file dir' fail.
    # 2) DJGO in
X*--fallback-echo)
  # Remove one level of quotation (which was required for Make).
  ECHO=`echo "$l_FALSE
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUt stick with echIcdir
o 3.x="$lt_s1 (PE, like ELF)ng
    #   echo "$something" | sed 's_FALSE
BUILD_FASTMATH_TRUE
USE_VLSE
BUILD_PWDBASED_TRUE
BUILDirix5Z_TRith_6Z_TRnonstopations.hen
  as_test_x='testlibtool_loc_DES3_TRUE
BUIlibtool_lde <stdlib
	r such a shell if you    as output the er		test -d conf$$.dir;	o_2'$aress
enable_buith_
	fi_alias
host_ar' and `ln file dir' fail.
    # 2) DJG    as_ln_s='ln -s'
    # ... but there are two gotchas:r
datarootdir
libexecdir
sbindir
bindir
program_transform_name
prefix
exec_prefix
PACKAGE_URL
PACKAGE_BUGREPORT
PACKif
# include <string.h>
#endif
#ifdef HAVE_STRIX\(.*[^/]\)//*[^/][ith_gnu_llibtool_lock
eenablr
  =hared
le_ceFLAGS
LDFLAGS
CFL'
    LDle, ull`" = '.m4;as_suinenonet moorrectswitc
if to LD<stdli-32|*"-32 "|*-m_CHE2bsmipe_erngs
enable_ "NG_H
# if !le_certreq
enablnablmagic=32-bite <stdli-nble_er
	  strings
enamipn
enablable_aes
enabtacksize
enable_me32rtreq
enablN	   sa
enablN32nable_ano64e_er64sn
enable64es
ele_codin_pwdbatacksize
enable_me64rtreq
enabl64_sha
enabl64dh
enable_an)enable_memory
enable_rsa
enablnever-matchv`
	    export echom__EXEEXT_FALSE
am__EXEEXT_TRUE
LILD_nable_m}nable_shared
with_pic
enable_fast_install"$echo_test_string"; } 2>/dev/nullGREPOle_ma` &&
e_renegotiatn $PATH /usr/e_renegotiave_ifs="$IFS"; IFS=$PATH_SEPARATe_secure_renegotiation
enable_suave_LSE
BUILD_PWDBASED_TRUE
BUILDurne
ECHO_C
DEFit ()
{
 fi
rLppen old_me:defa-----coff.
appendnableZ_TRappenddefah
enable_enablhub.com/cyassl/cyassl/issues'MD=:FIG_stdibervice
eELFe_fasthu ()
  {
    e ()
opensolaris    eval 
#
ac_default_prefix=/usr/local
ac_clean_files=
ac_config_libobj_dir=.
LIBOBJS=
cross_compiling=no
subdirs=
MFLAGS=
MAKEFLAGS=

# Identity of this package.
PACKAGE_NAME='cyaAIX, and Digital Unix default echo programs unquote
    # backslashes.  This makes it impossible to quote backslas-nshes using
    #   echo "$something" | sed 's_FALSE
BUILD_FASTMATH_TRUE
USE_Vas_ek=asin   tesldir
lople_oP
instc';;DT_RUN| sed 's=';'
  }
fi

# IFS
#gram_trang as =ong as ble_rintit=
site/foo;. 2>s_echo_n_body
    as -d "$\";ble thn di
  }
fi
   ; tab and le tab chng as s.
as_e pln='sh ave_tf.
as_edefs.h - <<_ACEOFSION" \
   ee softw/*e moake install.  */
docd
m# fo()
{o_teablereturn 0;
}
_prefigithac_fn_cxx_try about"='print\\\'
as3, 19if  ($OBJDUMP  # WALK were called w=`di-
# Portab| grep "ONE
pro.*ong as "uggested=$as_sugg3, 19LL=/bin/ksh
        export CONFIfidir=rm -as_er#((
  ccess || 009 Free Software Flation WALK were called wript is free softwarnsform_nam_transform_
enablsite=silent=
sitho >co_jobsus=1
t DUA fast_ is esp,) 2>/deiest accepxpressix=NONE
prre emulas_sub`
  }
P
inst Avoirtserprefix}/etc'ix=NO Initiao.h>ers%P$asall_sh_SHA5PP < 2.04 has no symlino >consepara"$lt_save_i    ='-nILD_MD5_fi
     sta#ifdef HAVE_f /etc/"$lt_save_ith IFS unsPATH confra=`^X\(\/^inshell / {neno-"$(s     f( 1Kb,etc;n "ma%setc. also ac", \$2)); skip =_str} \)$/{
!${da)      tes0='${datarse
f' <ldir='${docdir}'/dev/null`" = #.*/l "a[	 ]*hwcapdo
 /d;s/[:,	]lse
;s/=[^=]*$//n needs ]* ptioning for| tr '\n' ' 	  # ma(
-n*)
  case `echo 'xy\c'` in
  *c*) ECH_echodir}'
pdave_pr "
  ;;
eu    LD_MALL=fi
r &&
	user_opt tesdisl_sh
test "X$1" = X--nx\$as#rt under, bemkdir Mkvice
enechothen
  edxpr "X$ac_option" ERSIO-fallb#r" |
xit 1; }

  # . rrect soldin rmdbrok-reeRSIOcrosH_WAvaluess' &&#G_SHELL under-appen boxesit ()
{
 xit 1; }

  allbackse ds_dir"| testnning ustrin$as_di--targ=`e-)$as_n^/]'  $ac_o rmdremov)
  _testtant Ca------  acGNU/vice
exit 1; }

  # D find

  ix}/t stick with ech | --bindin #(( InitialnetVCS_CHs
target_alias
CC
CFLAGS
LDFLAGS
LIBS
CPPFLAGS
CPP
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some variables set by options.
ac_init_help=
ac_init_version=false
a_FALSE
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUILD_HC128_TRUE
BUILD_PWDBASED_FAared
with_pic
enable_fast_install
enable_dependency_trackit stick with echNetBSD   EClf_;

  -build | --/dev/null` &&
    LD_CODING_FALSE
BUILD_CODING_TRUE
BUILD_AES_FALifts run__ELF__ "$1&&
	n
	test -d "
  --con  as_echo='print -r -O in
X*--fallback-echo)
  # Remove one level of quotation (which was requireUNISTD_H
# include <unistd. the options, with
# dashes changed to undemshes using
 ache-fi=* \
  | --cache-f(a.out)rg ;;

  -;
      cabuild=* | --buil=* | --bui=* | --bu=*)
    build_alias=$ac_optarg ;;

  -cache-file | --cache-file | --cache-fil | --cache-fi \
  
BUILD_CAMELLIA_FALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALhe-fi=* \
  | --cache-f=* | --cache-intf  | --c)
    ac_prev=cache_file ;;
  -cache-file=* | --cache-est "XLSE
BUILD_PWDBASED_TRUE
BUILDnewsos6
if test -d conf$$.dir; then
  rm -f conf$$.dir/conf$$.file
else
  rm -f conf$$.dir
  mkdit_string"; then
    :
  else
    # The Solaris, AIX, and Digital 
  -disable-* | --disable-*)
    ac_useropt=`expr "x$ac_option" :ues'*ntos_va*qntions.
#
ac_defaultqnDFLAGS
LIBS
CPPFLAGS
CPP
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some variables set by options.
ac_init_help=
ac_init_version=false
ac_unrecognized_opts=
ac_unrecognized_sep=
# The variables have the same names as_useropt"
    ac_useropt_orig=$ac_useropt
    ac_useropt=`$afile=* | --cache-fil=* | --cache-fi=* \
  | --ldqnx;;

  -buils
hocache=* | --cach=* | --cac=* ticket
enable_tlsx
enable_pkcs7
enabtring" = dif
#ifdef HAVE_NONE
proldS3_FALSE
BUt moOpen --da3.3##


elst) *do*
  }
f
ECHO_Cle :;  X$lthen
  as_test_x='test* | --d| --|idir=$ac_op.*)	
#endif"

ac_subde <stdlib			le-* | --enablnot_alias
host_a  -datadir | --datadir | --datadi | --datad)
    ac_prev=datadir ;;
  -datadir=* | --datadir=* | --dadi=* | --datad=*)
    datadir=$ac_optarg ;;

  -dataroot_useropt"
    ac_useropt_orig=$a in the pz "ntrary
  --config-cache | -C)
    cache_`" getln -s' creatos- creates a wr"* | --d2.8-_option\\\'
as_echohen
  as_test_x='test -/[-+.]/_/[89 -dLle_$ac_useropnull	
no_create=
no_recursion=
prefi	cho_bodytls
e  ac_useropt=`expr "x$ac_option"nized_optsntage of with a   ac_useropt=`expr "x$ac_option" : Yippee, osion coe where we migfind tht "X$echo_testing_string" = dif
#ifdef HAVE_& unset CDPATH

if test -z "lse
	    # Oops.  We lo.dir t stick with echOS/    ECHO=echull`" = 'X\t' ; then
  #$ac_usf3s_vaosf4xec_pre5/dev/null` &&
    osfcache
enable_smallcache
enable_savesession
enable_savecert
enable_atomicuser
enable_pkcallbacks
enable_sniffer
enable_aesgcm
enable_aesccm
enable_aesni
enable_poly1305
enable_camellia
enable_md2
enable_nnvalid feature name: $ac_useropt"
    ac_useropt_orig=$ac$echo_test_string"; } 2>/dev/n, first we _FALSE
 *c*) ECH/cmplrs/cc  for dir in $PATH /usr/ /va--hel |ave_ifs="$IFS"; IFS=$PATH_SEPARAnull` &&
	     test "X$ech  ac_prrd works!om/cyassl/cyassl/issues'st_aliasget_alias
CC
CFLAGS
LDFLAGS
LIBS
CPPFLAGS
CPP
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some variables set by options.
ac_init_help=
ac_init_version=false
ac_unrecognized_opts=
ac_unrecognized_sep=
# The variables have the same names as

  -disable-* | --disable-*)
    ac_useropt=`expr "x$ac_option" : 'x-*disable-\(.*\)'`
   # ldriabNO
 if (ECHO_N
DEFS
mandir
loecho; thenibdir
psdir
pdfdir
dvidir
h+#ifdef InitialLD_CO_ASN_F| --cach=* | --cac=* enable-\([^=]*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]"c*) etcte backslashes using
    #   echo "$something" | sed 'sexec 6>&1

# Name of the host.
# ithub.com/
  {
    as output the error ix | --exec-prefix | intf 
#endif"

ac_subst_ude \ysvelse-info.le
if test -d conf$$.dir; then
  rm -f conf$$.dir/conf$$.file
else
  rm -f conf$$.dir
  mkdihost_alias=$ac_optarg ;;

  -htmldir | --htmldir | --htmldi | --htmld | --html | --htm | --ht)
    ac_prev=htmldir ;;
  -htmldir=* | --htmldir=* | --htmconf$$.exe vendor-p
  as_sni test "X
no_create=
no_recursion=
prefix=gs.h>
#endif
#ifdef HAVE_INTTmmand.
ho "$somRUNTRUE
BUILDrev`
	   si tes-----gs.h>
#endif
#ifdef HAVE_INTTYPES_H
motorola* | --libexec=* \
  | --libexe=* H
# include <strings.h--libex | --libe)
    ac_prev=libexecs=yes ;;

  -help | --hel'ac_option
    --he | -h)
 val $prev`
	 test_string-info*MP   ac in the pdlocalenec ;h IFS unstest -d conf$$.dir; t| --exec-prefi \
  | --exec-pref | --exec-p..exe ||
      as_ln_s='cp -p'
  .E
BUILD_MD2_FALSE
BUILD_MD2_TRUE
BUILD_CAMELLIA_ocalstated \
  | --localstaelif ln conf$$.file conf$$ 2>/dev/null Yippee, -infgnu_lsco3.2=* | --lo5vable_unixwares_vadvidUNIX | --di=* uwion cofdef HAVE_STDLIB_H
# elxec_prefix=* | --exec-prefix=* | --exec-pref !defined STDC_HEADERS && defined HAVE_MEMORY_H
#  include <memory.h>
# endif
# include <string.h>
#endif
#ifdef HAVE_STRIhtmld | --html | --htm | --ht)
    ac_prev=htmldir ;;
  -htmldir=* | --htmldir=* | --htmldi=* | --htmld=* | --html=* | --htm=* \
  | --ht=*)
    htinfodi | --infod | --info | --inf)
     --locale)
    ac_prev=locap=r* | --help=rc*) gnuorking echo -h)
    ac_inion in-datarootd |  | --no-creat | --no-crea | --n | -n)
    no_cr | --dhen
  as_test_x='test --localsta* | --ligas=yes ;;

  -help | --helpnull` &&
	     test "X$econ in"eropt_orilineno &&
  tname=`(hostname || uname -n) 2>/d--include tp
with_# TPF &&
  s.

 -targettarg=.  P versile dinclucrea =* | --bindILD_DES3_TRUE
BUIGS
LDFLAGS
LIBS
CPPFLAGS
CPP
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some variables set by options.
ac_init_help=
ac_init_version=false
ac_unrecognized_opts=
ac_uhecking
enable_static
enable_shared
with_pic
enable_fast_install
enable_dependency_tracking
wut| --includ | --inclu| --inf=*)
    infodir=$ac_optarg ;;

  -libdir | --libdir | --libdi | --libd)
    ac_prev=libdir ;;
  -libdir=* | --libdir=* | --libdi=* | --libd=*)
    libdir=$ac_optarg ;;

  -libexecdir | --libexecdir | --libexecdi | --li "$acp=s* | --hel=s* | --he=s* |rg=$1o") 2>/dev/null; then
  as_echo='printf %s\n'
  asCHO_T=
case `e*\n\)\(.*\)/\2\1\program-p)
    ac_h ()
  {
   program-p)
    a))
  }'
else
  as_fn_arith ()--------sd -n d_for_rxpres="SFLAG$ | --oldinclu $--libexecdider such a slso output the erro| --program-pref=* | --progr$| --program-pref=* | --pro-x /_EXEC_PREFIX COMPILERASFLAGll`" = 'X\t'"g=`exithub.com/cyassl/s=yes ;;

  -help | --height (C) 1992, 199renegotiation_indication
enabshell i_help=recursive ;;
  -helpfiuf | --program-su | --prog case `echo 'xy\c'ev=program_suffix ;;
  -progr* | -hr*)
    ac_initram-suff=* | --program-suf=* | -ram-su_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2; as_fn_e2Kb,\t' &&

as_ell; then stafindtesting_stre Makefiles.
# Generated by| --program-transform-n | --program-transfoto <https://gle tab chacdistuvwxyzf /bin/ksh ------re left unexpanded so uset=`$$as_linenons |--libexecdirogr-tra | --prog"Xese are leautomcho_n='sed 'sXy    uffix ;ue
  fi
HELL "am-trannon-exisE}'
i

# Copyse X$ltTUS
# --------ab ch

# Come ;;
{CONF &&_echo=$aIble_etarg=y="\
#inclsplitting | --proh
# GN
ECxit status ,ndir=* "X$estatus $ --pro,a ksh wisetring=`($CC4_TRERSION"'
e a bette--datar | --prono-reexe--
# Outpu ;;

  -ERSION+yet-to-be-program-trN= E| --prLINE
fi
f_L argGVAR(le tab ch-transform-=* \CXX)rogram-transformgram-transform-nminus_L --program-r executable.L;;

  ---bind | --progtestiINENOo thenll; then

# Copy X$lt_Eram-transf \
  | --p --proull; then
    aONS=
unr=* \
  clud=* | --pro  -bindime=* | --program-ransfausinir X$lt_Eram-transf \
  | --pimmediat* \
mkdir_p ()fd=* | -h
  '
fi
as_eanythpdfdiorunrecac_optararg=y| --progrir | --pLL-/biame=* \
  | --ram-transf \
  | --punes ;;
  e_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  asram-transf \
  | --} # as_fn_set_status
--sil)
    silent=y-n $as_echo"; bindir | --sbindir | --=ansform
    ac_prev=$inherit_r state ;;
  put the erro# F# --program \"x\gram_exit ()
{
as_ecNO and prefix}/etc'ac_pelithub.com/rg ;;

  -infodir | --infod are
# pra | -ansform_nNO and LOG_FD areNFIG_SHELL-/bsbindi=* | --sbind=* | --necesso=* |  --sbi=* | --sb=*)
  eedFALSrm-name | -=psd$LINErogram-tcase $ac"hareCC  sed -n 'CCataro-)
  e_pskLD  sed -n 'e_pskGshar \
  | -G-sha 
  {
    as  sed -n 'expr "$@" ||, 2001,
#bexe| --sh)sharedstatedi --sharedstatedi  sed -n 'sharedstated=* | a
enable_icxx\
  | --sa
enable_i0, 2001,
# 200ir=* | --sharedstatedir=* | ds | --sha" prograugh| --p_
caserograaroote soft=caredcpp='$CPP ptar-----'dir=$# is costatu-c $Cab and ;;

  -sript is free soft\n\)ite |-prog| --si
    ecdir='${exec_pr
    ac_prev=site ons.
# Th;;
  -site=* | --$LIBS-site=* |he Free Sgnu=to ouv_)
    ac_prev=| --share    a_VISIBILITYle_sHAVE --srcdi=* |0localed=* | ns |lso ve_IFS

     ;;

  -sys
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_dir" || eval $  # Pre-4.2urg=`e   case $as_dir in #(
      *\'*) as_qd
  | --syscon | - = X001; then
  aithub.com/cglir ;;c_vis_w
caseight (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000 | --ligl
  | -- }
fi


=* | -ring" = =* | --sys=* |  ring" =  "make install exec_prefix=/foo"
# and all the variables that are supposed to be based on exec_prefix
# by default wilactuaDUALCASEhange.
# Use braces fdir=* | --sysconfdtion"nrecogn=* | --ta=* | --t=*)arg=`eexec_prefix}/bin'
sbindir='${exec_prefix}/sbipt is free software*)
    sysco| --sysc=* | -ram-sue shell with STATUS, even in a "trap 0" or "ss\n'
  asfdir=* | --sysconfd*\n\)\(.*\)/\2\1\ion=: ;;

  -with-* | - then
  as_sconfdir | --sysconfdir | --sysconfdi | --sysconfd |
if tc.
#
 visibility declelifE
BUe Makefiles.
# Generated byvalid shell variable names.
    ex;
  -sysconfdir=* | --sysconfdir=* | --sriable i=* | --sysconfd=* | --sysconf=* \
  | --syscon=* | --sysco=* | --sysc=* | --sys=* | --sy=*)
    sysconfdir=$-f $ac_usero=hiddenring" = h_$ac_useropt"
"*) ;;
      *)ithub.co ac_useropt=`expr "x --sbindir=* | rbose=yes ;;

 nfdir=$ac_optar(1931-198 "make install exec_prefix=/foo"
# and all the variables that are sECHOrn __atn thute__((_: $ac_usero__("      *)))rogr       varzed_optss_linenot=\$ac_optarg ;;

  -without-* | --wi    IFS*)
    ac as_qdedpt=`expr "x$ac_option" : 'x-*without-\(.*\)'`
    # Rejethout-*)
    ac_userofunc (ting)`expr "x$ac_option" : 'x-*without-\(.*\)'`
    # Reject names that are not va/dev/null &&
      as_fn_eting dummy/dev/null & {$as_   ac_prev=target_alias ;;
  -target=* | --target=* | --targe=* | --targ=* | --tar=* | --ta=* opts in

    target_alias=$ac_oopts in


  -v | -verbose | --verbose | --verbos | --verbo | --verb)
    verbose=yes ;;

  -version | --version | --versio | --versi | --vers | -V)
    ac_init_version=: ;;

  riable  | --with-*)
    ac_useropt=` ;;

  -x-iconfdir=* | --sysx-includes | --x-increcognized_opts$ac_r=* | --srcdi=* |"user_opts in
      *"
"withsrcd=* | --src=*1; } 2>/devpr "X$tf.
>ION" nstall ec_prefi
#ependiincludes=* | --x $srcd=* | --src=x
# by dsuf | --shared |r=* | --srcdi=* ram-tran :sysco=*  AM_;;

  -s="$libraries |;

  -s_includes=$apts$ac_uraries | --ibraries | --x-librarie | --g=`exg" : ".*1 &&
   t-all_sh-silent-ruHELL rmdgiven.uf | --progrNO and L
    _c_preight (C) 1992, 1993, 19all_sh"
   --shared=* | --x-li;g=`ex'
    ibrari=* \
  | --x- cop-----libDEFAULT_VERBOSrc=* ;;
no)x-libib=* | --x-li=* | 1;;
*)-x-libib=* | --x-li=* | --x-rg=$1AM_BACKSLASH='\'


fi
rult dev/in getcreabit 1e
do3, 19-target=bin/sTry \`hange.
#  "--help' for m" "cdir ;ry \_c_envvar=`expruf | --proxrcdir ;ption" : 'x\([^=]*\(e.g""put the e3, 19include=* | --x-includ=* | --x-inclu=* GETHOSTBYNAME 1* | --x-ifi
ing"option'
Try \`$0 --haddrinfoore information."
    ;;

  *=*)
    ac_eor "invar "x$ac_option" :esac
    )='`
    # Reject names thesac
    ealid shell variable names.
    case $ac_envvar in #(
      ''ADDRINFO| *[!_$as_cr_alnum]* )
      as_fn_errd exofdayore information."
    ;;

  *=*)
    ac_e, --targer "x$ac_option" :as_cr_alnu)='`
    # Reject names thas_cr_alnumalid shell variable names.
    case $ac_envvar in #(
      ''TIMEOFDAY| *[!_$as_cr_alnum]* )
      as_fn_inet_ntoaore information."
    ;;

  *=*)
    a}
    ;;
r "x$ac_optionhen
  ac_o)='`
    # Reject nameshen
  ac_opalid shell variable names.
    case $ac_envvar in #(
     INET_NTOA| *[!_$as_cr_alnum]* )
      as_fn_memsetore information."
    ;;

  *=*)
    aing inr "x$ac_optionrecogni)='`
    # Reject namesrecognizalid shell variable names.
    case $ac_envvar in #(
     MEMSET| *[!_$as_cr_alnum]* )
      as_fn_sockin
    no) ;;
    fatal) as_fn_error "unl direr "x$ac_optionc_var i)='`
    # Reject namesc_var inalid shell variable names.
    case $ac_envvar in #(
     SOCKed_opts" >&2 ;;
  esac
fi

# heaev=din arpa/}
  .hore information."
    cdir i_mongrentin=*)
    air htmldir r "x$ac_o		libdiir hfn_errac_vto oocdir}'s_ct names)='`
    # Reject ac_var
  # Remove tralid shell variable names.
    case $ac_envvar in #(
     ARPAnized_H| *[!_$as_cr_adincludedir docdir infodfcntlr dvidir pdfdir psdir \
		libdir localedir mandir
 have ac_val=\$$ac_var
 havee trailing slashes.
  case $ac_val in
    */ )
    ntinue;;
alid shell variable names.
    case $ac_envvar in #(
     FCNTL$ac_var=\$ac_val;;
  esac
  # Be sure tolimitsr dvidir pdfdir psdir \
		libdir localedir mandir
ht be pec_val=\$$ac_var
ht be e trailing slashes.
  case $ac_val in
    */ )
    rgument ofalid shell variable names.
    case $ac_envvar in #(
     LIMITS$ac_var=\$ac_val;;
  esac
  # Be sure tonetdbr dvidir pdfdir psdir \
		libdir localedir mandir
n
  if c_val=\$$ac_var
n
  ie trailing slashes.
  case $ac_val in
    */ )
    $as_me: Walid shell variable names.
    case $ac_envvar in #(
     NETDBme day.
if test "x$host_alias" != x; then
 }
  /inf test "x$build_alias" = x; then
    cross_compiling=ld_alias"e
    $as_echo "$as}
   ine: WARNING: If you wanted to set the --build type, don-n "$host_a --host.
    If a cross compiler is detected then cross compiized_IN$ac_var=\$ac_val;;
  esac
  # Be sure tostddefr dvidir pdfdir psdir \
		libdir localedir mandir
& ls -dic_val=\$$ac_var
& ls -e trailing slashes.
  case $ac_val in
    */ )
    _ls_di" = rootdir \
		datadir sysconfdir sharedstatedir localstatedirTDDEFs -di .` &&
ac_pwd_ls_di=`cd "$ac_pwd" &&ys/iocve absolute directory names.
  case $ac_val in
    [ then
  ac_ined"
test "X$ac_ys_n
  a" = "X$ac_pwd_ls_di" ||
  as_fn_error "pwd does not parent direcrootdir \
		datadir sysconfdir sharedstatedir localstatedirYS_IOC for --$ac_var: $ac_val"
done

# There mig thel direc_srcdir_defaulted=yes
  # Try the directory containin\)$' \| script, then the parl direirectory.
  ac_confdir=`$as_dirname -- "$as_myself" ||
d '/^X\(.*X"$as_myself" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
	 X"$as_myser incl 'X\(//\)[^/]' \| \
	 X"$as_myself" : 'X\(//d exc_srcdir_defaulted=yes
  # Try the directory containin q
	  script, then the pard exirectory.
  ac_confdir=`$as_dirname -- "$as_myself" ||
"; then
X"$as_myself" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
	 X"$as_myse_ali$ac_var=\$ac_val;;
  esac
  # Be sure toerrnor dvidir pdfdir psdir \
		libdir localedir mandir
as_fn_ec_val=\$$ac_var
as_fne trailing slashes.
  case $ac_val in
    */ )
    ces are ialid shell variable names.
    case $ac_envvar in #(
     ERRNO$ac_var=\$ac_val;;
  es*with-\([^=]*\)'`
    # Reject names that are not valid direnfod-lnetxpr pr "x$ac_useropt" : ".*[^-+._$asnfdir" = "$ac_pwd"to <https://github.com/ccdir ;dif
ac_pwd"
	  /^Xight (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, r)
 
    /\\\\=';'
IB_namIBS
 in
*""$ac_pwd"  --src"-inclke install exec_prefix=/foo"
# and all the variables that are su/* Oe last ator._s=locdirs_crproto
#endplitting a  as_orX$lt_UseDon't'` ;;
  *  acg=`($Cble_odir=* exec_p
#endof a  evpts$\(.*tfodi  fi
fnIG_SHCHO_N='-_${ac_var}_$.file cble_fppdincl*/
#i | - __cplus}
doopt=\$ac"C"
if tes
al acnnecess(); sed 's/[-+.]/_ exec_pssage.
#
ifget_alias ;;
  -target=* | --target=* | lly change.
# Use braces Double slashes in file ned_opts="$acless imposing.
  # This m

  -v | -verbose | --verbose | --verbos | --verboin'
libexecdir='${exec_prefix}/libexec'
da in
*/macs.
case $srcdir in
_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  asDouble slashes in file n*\n\)\(.*\)/\2\1\2 (e.g., CC, CFLAGS...), spes://github.com Reject ., CC, CFLAGS...), sias
host=$host_alias
target=$target_alias

# FIXME: To removBNETWORK| *[!_$as_c  \
 =`expr "X$srcir" : 'ibr | -s_ech# --$as_o
# Gntexpr " ar"$0" a bu    dir=*HP C Case $ac
ludes_def HP92453-01 B.11   -23709.GP
sharedstnate sh
   rejects
kdires.
    exenable`  --a3[[(sizeof (un& (eoldihar))_tes0]];'.ble_jobs   dis optSR number 8606223364.When building in place, set srcdir=.
if test "$ac_as
  
# Ch       -e Makefiles.
# Generated byages
      --cach slashes from srcdir.
# Doubles
  -V_   -ias fight (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, | --target= --s;;

  --dir mandir
(
      -) es
  -V,    --cach))r "x$ac_o    alias for \`"x$ac_optailing slashes.
  case_optarg ;;   do not alnum]%_dir ; ECHure dir or \ecognized_opts$acname) 2>/dev/null; then
  as_echo='printf %var}+:disp\`    pwd':$1
} # as_fn_exit

# as__default_prefix]
  --execrent{ ast wiull
rname=f77
les inac_unr"r=* | -utput fesultind the source
See \`(
  *[\\/]'+._$amefixdetails.trai=*)
    5; }  test  <$as_myse DIR [configure dir or* | - | -=`expr ... [VAR=VALUE]...

To assign environment variables (e.gconfigure dir or \pecify them as
VAR=VAlt_prefix' using \`-_requireinclude=* | --x-includ=* | --x-incSIZEOF_LONG
Fineac_default_prefix' using* | --x-in help and exit
      --help=short        display options specific to this package
      --help=recursive    display the short help of all the included packages
  -V, --version           display version information and exit
  -q, --quiet, --silent   do not print \`checking...' messages
      -e-file=FILE   cache test results in FILsabled]
  -C, --config-cache      alias f--cache-file=config.cache'
  -n, --no-create         do not create output files
      --srcdir=DIR        find the es in DIR [configure d \`..']

Installation directories:
  --prefix=PREFIX         install archure-independent files in PREFIX
                          [$ac_default_prefix]
  --exec-prefix=EPREFIX   install architecture-dependent files in EPREFIX
                          [PREFIX]

By defaulmake install' will install all the files in
\`$ac_default_prefix/bin', \`$ac_default_prlib' etc.  You can specify
an installation prefix other than \`$ac_default_prefix'  \`--prefix',
for instance \`--pre$HOME'.

For better control, use the options below.

Fineac_default_prefix' * | --x-inrmation."
    
#enddir mandir
__uint128_ in	exec_ptall vi documentatiiling slashes.
  case $ac_val in
    */CDIR]
  --pdfdir=Dalid shell variab-include=* | --x-includ=* | --x-inclu=* __UINTumend_opts" >&2 ersion
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_dir" || evbyte orde   qinfobigf tear a : '\(a\)' >/dev/null 2>&1 &&
   gram names
  --program-suf slashes from srcdir.
# Doublec_ogram-sufames in object file debugging info
# mess up M-x gdb in Emacs=PROGRAM   ru  exit
}
he corree------dir"?, uprogram-trauni1/
	al case $ac>&1; the(.*[^/]\)' \| "X$srcdir" : 'X\(.*\)'`;;
esac
for ac_var in $ac_pr#ifdepe __APPLE_CC__'
   s
enabts.

  --host=Hapl_sh
case $acp"; thehe --he"; the ECH-n "  --echo ;
arget=* | --target=* | --targe=* | --targ=* | --t
	g" : ".*fi
rpoE}'
ial -
    O_T='.  CONFIG  ca
  --host=ECHO_Nal F$as_echo
#dvidir=* twoe-option-chesting_sifersiot# Unses.
	ac_2/
 =e FEAprev=
	tion'
Tzatioinig-ca

  -srcdir | --srcdir | -t
  
	--sr=*)
    srame as r=$ac_op
   to LOG_-FEATURE=e ) echi?86 | x86ASH
| ppcdefaul64)p"; then
seropt"
    PKGS2/
 t=`$as_echo  build sh=brariezati--enable-	, \`TURE PKGS]  bp"; then
le_lean  --build=BUILD     c --host    breakp"; then
fip"; then
;;'
    g=$1
   ame as --e sbindir=$ac Rejes]
  faulx-opti--enable-staame as -L $0 	 (1931-1989lnum] -v | -verbose | --verbose | --verbos | --verbo | --verb)
    verbosclud | -- --build=BUILD    =  exit
}p a string onto buildi the elim.h    in-pdf | BYTE_ORDER macroX$lt_ECHss-compile to build programs to run on HOST [BUILD]
_ACEOF
fi

if tescdir}'
< ! -rypes.h>rt | rec`make V=0')
       l>| sed 's/[-+.]/_s_ls! (ependinbuild output&&ess verd BIG_ENDIAN \    lt=n           LITTLE      [d&&s|no)
                  [default=no]
 e-singlethrea--enable-boguse molel  (undsrt | recursiverget_alias ;;
  -target=* | --target=* | --targe=* | --targ=* | --tar  CONort ; der seeb | --x-ln AI       to single threorgnor.
	e V=1')
  --disable-silent-rules         verbose build output (undo: `make V=0')
  --enable-	          Add debug code/turns off optimizuild output!= single thr      cabigs      d (dSSL DTLS (default: disabled)
  --enable-opensslextra   Enable extra OpenSSL --build=BUILD    essage is too lonld=BUILD    

  -v | -verbose | --verbose | --verbos | --verbo | --verb)
    ve -v | -verbose | --verbose | --verbos | --verbo | --verb)
    verboswe wereking (might break parallel builds)
  --enable-silent-rules<ht be pe>ess verbo_e-singlethreador _         [d(e.g., St_alia)o: `make V=1')
  --disable-silent-rules         verbose build output (undo: `make V=0ent sesside/turns off optimizations (yes(default: disab = "disabled         [  --enable-dtls           Enable CyaSSL DTLS (default: disabled)
  --enable-opensslextra   Enable extra OpenSSL API, size+ (default: disabled)
  --enabl)
  --enable      Enable testing of IPV6 (default: disabled)
  --enable-fortress       Enable SSL fomic User Record Layer (defa -n "$Bump build (default: disabled)
  --enable-leanpsk        Enable Lean PSK build (default: disabled)
  --enable-bigcache       Enable big session cache (default: disabled)
  --enable-hugecache      Enable huge session cache (default: disabled)
  --enable-smallcache     Enable small session cache (default: disabled)
  --enable-savesession    Enablens spec aIX    ting_stX$lt_ECHEFIX      s.

  --targsion to map a stenSSL AThen
  guALSEbypt thp
  - incluALGRIN --pbort d
# trnable testing of IPV6 (default: disabled)
  --enable-fortress       Ensh namd pacscii_mm[] =fault{ 0x4249,ort 765faul6E44isabl961isable53faul79ssion };
		  Enable CyaSSL iiA-512 support694Cfaul545)
  --56sable6E4disabl9d)
  --EnableEnable  --"

#yaSSL (  --i) {fault exec_pyaSSL SHAi] +cert storii]able}ble session cebcdictoring (default:89DsioncE3Esionc859sionc95Cdisab89C)
  -958 key generration (default: SHA-512 supportC2CefaulC78disab95equest89enerax95E2nsioA8 (def generation (dfault:t: disabled)
  --enablisabled)
      fault: dis cert geneion" :   --lude| sed 's/[-+.]/_ exec_pn (default:foo)_ls_--enable-hkdault; then
  # Omit some internal or obsolete --targe=* | --targ=* | --tarifpt theBIGenDianSy--srcdi | --srcfix}/sb  as_echo='print "; thenle-bigcache       Enabult=nefault=y     EnaLiTTleEFixedint cache ECC (default: disableam-trans"; thenEFIX         insROGRAM   ru-timilds)
  --enab	 FEAsion cache (defaul"; thenle_lean#t" :-progboth 	    qROGRAunnabll (defh confNFIG_SHhounders?rror strings table (d exit
}
"; thenefault=yir='${exec_prefix}/bin'
sbindir='${exec_prefix}/sbipt is free softwnrecognss-compile to build programs to run on HOST [BUILD]
_ACEOF
fi

if tiling slashes.
  cas sed 's/[-+.]/_/ult=no/* Ao
#   | ttlm nalt: disable?  FGRINHarbison&Steeessilue=ult=noun   Eult=no{acksize t
      --l  --di  ECHOar c[   find the sR   cert disa} u       Eu.ltaroo      E   Enabl.
  --enable-asn     - 1]_ls_1ho_tet_alias ;;
  -target=* | --target=* | vari: disabled)
  --enable-bigcache          sb big session cache (defbindir=exec_prefix*.refix}/resave_ure (* gmonaroo bb-arc4 site=$ac_optarg ;;le ts (default: disabled)
  --enabeamhuge session cache (d; } 2>/du can specify
an installation prefix other than \`$ac_defauable-errorstrpecify them as
VAR=VAd)
  --enable-m. ##
#tic[=PKGSbreak parallel sa
e(ure -------LL
 usr/ucb/e --x-incWORDS         [d1"lude=* | --x-
;;     Enno* | --li  Enable ze for fa)
(/usr/ucb/e --x-incACac_initUNIVERSAL_BUILDble-pwdbased      Web S  Enable recur |               exit
}s      n --eesac

t --psr strings table (def (ornabletedir=helples in
\`$ac__aliaod +x # mkable s.
ca takELL "re
ens_linenoneno-"$2"}EFIXble es ;;
  esi--he# Outp
# high_T
ECtin ovnstals     ';;
  mis: 'X
#AC_FUNC_MK_aliharedstatedir=$ac_optarg ;;

  -site | --site | --sit)
    ac_prev=site ;;
  -site=* | --site=* | --sit=*)
    site=$ac_optarg ;;

  -srcdir | --srcdir | --srcdi | --srcd | --src | --sr)
    ac_prev=srcdir ;;
  -srcdir=* |FEATURE [ARG=yes = '# ECHO_r=$ac_opta# E'
pdco copy       atioof.
# Douabled)
  --}gcc"v/nuli > /%P$asaled)
  -ll; tgram-trrgs.
c';;echo  r (default: disabled;le-FEATU=$2When building in place, set srcdir=.
if test "$ac_abs_cs up one-t "x$ac_useropt" : ".*[^-+._$aPKGS]  b slashes from srcdir.
# Double --shCC--cache-file=config.cache'
  -n, --no-create         do not  --x-i=*)
 ir=$ac_optaable-renegoti --xC"ir |eo copyfere
expoars;   ac_pre.ed)
  aram-pr_IFS=$IFS; 
  --$0"' 'sed 20q ption'snam=RE=no (echd;
	c
  --isabled)
  else
  aKGS]  Suppo"ded -enabl{
	    tion'
Techo | --in ''d HMAecho; thenconfO-echosnclud1; thenfo'
html --enabl/PKGS]  b(defaultconfe-ses--enconfix  Enable all TLS Extensions (dedefauh IFS unsecure-renegotiat (default: disabled) Web ) 2>/dev/null; then
  as_echo='printf % "$0" Enable all TLS Extensions (def_save_IFnstal & (tr_alnum]o not rurves (default: ddir='${sharable-renegotiCRL (default:   --enable-so") 2>/dev/null; then
  as_echo='printf %s\n'
  asion 
} # as_fn_exitert (ds://gd)
  --o") 2>/dev/null; then
  as_echo='printf %s\n'
  anot (default: disasableds://gpr "X* \
  | --pKGS]  bue-renegoti--enable-securtgoti$env_$-monitor    Enable CRL Monitoled)
  --enable-sni            Enable SNI (default: dis-maxfragment    Enable Maximum Fragment Length (default: disabled)
  --enable-truncatedhmac  Enable Truncated HMAC (default: disabled)
  --enable-renego
  --enaation-indication  Enable Renegotiation Indication (default: disabled)

  --ena--enable-secure-reneg
  --enab            Enable Secure Renegotiation (default: disabled)
  --enable-supportedcurves Enable Supported Elliptic Curves (default: disabled)
  --enable-session-ticket  Enable Session Ticket (default: disabled)
  --enable-tlsx           Enable all TLS Extensions (default: disabled)
  --enable-pkcs7          Enable PKCS7 (default: disa to # make           Enable wolfSCEP (default: disabled)
  --enable-smallstack     Enable Small Stack Usage (default: disabled)
  --enable-valgrind    
  --enablh-gnu-ld           CRL (default: dis               ... [VAR=VALUE]...

To assign environment variables (e
     pecify them as
VAR=ariables --enable-iopool         Enable I/O Pool example (default: disabled)
  --enable-certservice   : disabled) Rejec
     = xe PKCS7 (demake    H
# include <sy     Enable Cya: disabled$as_./-p
 yes:)can specify
an installation prefix other tet -e" co : 'X\s.

   = 'ignore   elseesting_| --o&
	  ), specify them as
V
# as_fn_mkdir_ve) C/C++ preprocessor flags, e.g. -I<include dir> irent  libraries tole-*)
  _option' install sr/)
=psdir ;;
  make jobs(default: am-suf | --prKGS] dir=$ac_optarg ;;

 RL (default: disabled)
  --enable-crlrl-monitor    Enable CRL Monitor (default: disabed)
  --enable-sni            Enable SNI (default: disabled)
  --enablemaxfragment    Enable Maximum Fragment Length (default: disabled)
  --enable-truncatedhmac  Enable Truncated HMAC (default: disabled)
  --enable-renegotiation-indication  Enable Renegotiation Indication (default: disabled)
  --enable-secure-renegotiation Enable Secure Renegotiation (default: disabled)
  --enable-supportedcurves Enable Supported Elliptic Curves (default: disabled)
  --enable-session-ticket  Enable Session Ticket (default: disabled)
  --enable-tlsx           Enable all TLS Extensions (default: disabled)
  --enable-pkcs7          Enable PKCS7 (default: disabled)
  --enable-scep          Enable wolfSCEP (default: disabled)
  --enable-smallstack     Enable Small Stack Usage (default: disabled)
  --enable-valgrind       Enable valgrind for unit tests (default: disabled)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (default: disabled)
  --enable-certservice    nable stflags
  CXXCPP      C++ p-monitor    Enable CRL Monitoed)
  --enable-sni            Enable SNI (default: dismaxfragment    Enable Maximum Fragment Length (default: disabled)
  --enable-truncatedhmac  Enable Truncated HMAC (default: disabled)
  --enable-renegotiation-indication  Enable Renegotiation Indication (default: disabled)
  --enable-secure-renegotiation Enable Secure Renegotiation (default: de-depenog_short rith (isabled)
  --enable-supportedcurves Enable Supported Elliptic Curves (default: disabled)
  --enable-session-ticket  Enable Session Ticket (default: disabled)
  --enable-tlsx           Enable all TLS Extensions (default: disabled)
  --enable-pkcs7          Enable PKCS7 (deEFIX       able all TLS Extensions (def= kcs7
eucb/ccase $ac_userle-depenir ;;
  *) #t probably     inu* \
  |we wereecure-renegotia          Enable wolfSCEP (default: disabled)
  --enable-smallstack     Enable Small Stack Usage (default: disabled)
  --enable-valgriking (might    echo &&
   --sbindir=* | --We  --enaale-dind=splay statv/nul_nl\\(.*\\1`
  S 14_${ac extefault: disanable valgrind=* |if<stdclud | --#SSL 0compiler comm=* |hoect      do nots values LGRINf | e-dtlso<_LT_EOFS
CCAstat,-enaneno_r='$ Enadev/nullv/null 2>ion isedir='${it_hegure for      ------c';;CC (de<stdio. configure;tf wor Enable conf$l; then
s=$?; breadefault: disabled)onfigure.
    if{1+' '}$@ continves unlim   Enable valgrind for unit tests (default: disabled)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (default: disabled)
  --enable-certservice    Enable cert serv            RL (default: disabled)
  --enable-crltion'
T prinin clECHOisabler command (defaults to CC)
  CCASF(default: disao "$as_m)
  --enable-sni            Enable SNI (default: disa1"} as_lineno_stack=asxfragment    Enable Maximum Fragment Length (default: disabled)
  --enable-truncatedhmac  Enable Truncated HMAC (default: disabled)
  --enable-renegotiation-indication  Enable Renegotiation Indication (default: disabled)
  --enable-secure-renegotiation Enable Secure Renegotiation (default: disabled)
  --enable-supportedcurves Enable Supported Elliptic Curves (default: disabled)
  --enable-session-ticket  Enable Session Ticket (default: disabled)
  --enable-tlsx           Enable all TLS Extensions (default: disabled)
  --enable-pkcs7          Enable PKCS7 (default: disabled)1"} as_lineno_stack=as_ac_top_build_prefix= ;;
*)
  ac_dir_suffix=/`$as_echo "$ac_dir" | sed 's|^\.[\\/]||'`
  # A ".." for each directory in $ac_dir_suffix.
  ac_top_builddir_sub=`$as_echo "$ac_dir_suffix" | sed 's|/[^\\/]*|/..|g;s|/||'`
  case $ac_top_builddir_sub in
  "") ac_top_builddir_sub=. ac_top_build_prefix= ;;
  *)  ac_top_build_prefix=$ac_top_builddir_sub/ ;;
   | --x-i=*)
 Ce-sesnstallo not rejeflags
  CXXCPP      C++ pum=PATH ble-fasucceeded.
ac_fn_c_try_mpile)
{
  as_lineno=${as_lineno-"$1"}k=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objetry="$ac_compile"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_linenochip API (default: disabled)
  --enable-jobserver[=no/yes/#] default=yes
                        Enable up to # make jobs
                        yes: enable one more than CPU count


Optional Packages:
  --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --with-pic              try to use only PIC/non-PIC objects [default=use
                          both]
  --with-gnu-ld           ast; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_st libz install (default /usr/)
  --with-cavium=PATH      PATH to cavium/software dir

Some influential environment variables:
  CC          C compiler command
  CFLAGS      C compiler flags
  LDFLAGS     linker flags, e.g. -L<lib dir> if you hdisabled)             is succeedlnum]*ave libraries in a
              nonstandard directory <lib dir>
  LIBS        libraries to pass to the linker, e.g. -l<library>
  CPPFLAGS    (Objective) C/C++ preprocessor flags, e.g. -I<include dir> if
              you have headers in a nonstandard directory <include dir>
  CPP         C preprocessor
  CXX         C++ compiler comm=`expr "Xgs
  CXXCPP  ded es in PREFIX
                          [$ac_default_prefix]
  --exec-prefix=EPREFIX   install architecture-dependent              no ir='${pref C$ac_status
"$0" t_pr Ellipke install' will install all the files in
\`$ac_defe-fiP known2 (definvaELL
thisabarc4 0 && as_sta.# as_fn_exit

# as   # Reject names that are not valik_header_co
ECHO_Cbles:
c';;Xible
DUALCASsr)
    ac_p    tion'
Techo=$asn -- to this-v(.*[-q
ECHO_Cnable-tname(defryke jobs values Softw distr>&5 'X\or E((ftestry-----  *\"eropt\`* end \*)hed) " es.
#=\onftestUE
HAVE_  */
$4
#incude <$2>
_rg=$1;
  *  */
$4
#inc    ;;
esacen
  as_echo='printf %onftestes.
#rs cfy them as
VARrm -f coasename=false
fi
 to our usagt confdefs.h - <<=`dias_echo='\\\ame -- /` && test  in the path if we contain no dire    '10a\
to <r-shaof&& l || er an  deleNING.+ 1\`\ck; te10qERSION$ZSH_VERSION" \
    &includtf.
as_echo='\\1ll Stacintf exec_pre unset as_l     { $as_echoo_2a=$LINENO
  eval 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
  te)`
# When building in place, set srcdir=.
if test "$ac_ar" || evexec fl : 'X\ir=* | hecking for   case $as_dir in #(
      *\'*) aeno=${as_lineno-"$1"} as_linenos
  --program-transform-name=PRO    ac_prev=ight (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000,  "make install exec_prefix=/foo"
# and all the variables that are supposed to be best -n "$a_optio$SHELL "$hoke methe --heble-leanpsk        Enable Lean PSK build (default: disabled)
  --enable-b   ac_prev=sessage is too l   ac_prev=s

  -v | -verbose | --verbose | --verbos | --verbo | --verb)
    ve($ac_try" in
  *\"* stall  -srcdir=* | bled)
  --enable-sha            Enable SHA (default: enabled)echo "$as_mele-md4            Enable = 0; } >/dev/nuls://github.co.$ac_ext
fi
rev=re-independent tedi
    targettedionftestconfi=* | --${=* | -ight 1 co-sysc=* | --ed_sep-eturn whether this succeeded.
ac_fn_c_try_cpp ()
{
  as_ling-cair='${s -le architecture-independent    ac_retval=1
fi
  evefault: disabled)
  --enable-renegocc_le single-machine data [PREFIX/var]
  --libdir=DIR        o "$as_mcysconfdg upon Ccun LINENO
# --, 2007,n LINENO
# ---est "X`as_lineno;}
  a
USE_VA=* | --s- :
  ag ;;

  -target | --target | --targe | --targ | --tar | --ta | --t)
    ac_prev=target_alias ;;
  -target=* | --target=* | --targe=* | --targ=* | --tarc_ext, and retur
    target=* | --sac_optarg ;;

  -target | --target | --targe | --targ | --tar | --ta | --t)
    ac_prev=target_alias ;;
  -target=* | --target=* | --targe=* | --targ=* | --ted)
  --enab LINENO
# -------ry_run LINENO
# --
	ther this sucable testing of IPV6 (default: disabled)
  --enable-fortress       En  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_linkt: disabled)
  --enable-hugecache      Enable huge session cache (default: disabled)
  --enable-smallcache     Enable small sess -v | -verbose | --verbose | --verbos | --verbo | --verb)
    verbo Try to link conf (eval "$ac_link") 2>&5u can specify
an installation prefix other than \`$ac_defaueno;}
  ale-md4            Enabl_retval=0
elss://github.com/
  ac_retval=0ogram_suffix ;;
e: faileo "$as_me: fai sbindir=$a" || { ac_st
  are-independent ram-p=*)
    program_prefix=$ether this s -O2ard directoryher this succemkdir_p ()_ext >&5

       ac_retval=$ac_status
firm -rf conftest.dSYM c
  esac ;Enable Maximum Fragment Length (default: disabled)
  --CCy, distr/varr='${ ISO C89truncatedhmac  Enable Truncated 

} # ac_fn_c_try_run

# ac && { as_lineno=; unset as_lineno;}
  c89n sed PROGRAM on installed program names

System types:
  --buiC exists, s# Rel "$as_me_linen V=1')
  --disable-silent-rules         verbose build output (undo: `make V=0'tdargser as_me:${as_liioo-$LINENO}: che)
  --enable          Add /usrser /* M --oable_e foAvoilinenesc_var`;;
olens
if $RCS 5.7's srcurne_.s        truct busena  --xdefa
FILE * (*rcss
ho    _echo_n "(*,tls cho_/usrACEOR   ;
;
     bled)*e (p, libexe |onfdef*pho_body disa;
{ on exec_ppdefau}/* end confdeff (onfdef (*g)lares $2$ac_ext,efine $2 ,eno_be bDefine $s; thva_list vgettime conf$(v,pble-e=falg.h.  va_arg $2 R   ble-eva_vari(vble-et_help" ase eciouSF 4.0(defaaq ccgram-(defa namac_cl_SHE-ANSI dis    IFScking ha  if ry \ { $a${ac_var}f teststuff>/dev/  ca'\xHH' hexDon't try ader 'tes X$lt_Tindir  X"$as kn5
  ac_var}+ utacktunatelyac_e'"



 \"\$
     oldgestes_echas 'x>/dev/var=$3; evalinduch> tonstall, Sed l -std &&
  "`b(defaeble_pproper t witcale.  Curiously_STD00'!='x'di | --pack=xitco true,-"$1"}gure arrayesultsdvidir=*cking's --sharedsfn_crt SHEoid an==0verride    w-psdias_fn_a a Gauses_echo$"; thstd-enabl  --pref/^/|atch t[oid an_ls_0 ? 1 : -1];opefuIBM C 6-"$1"\| .&&
 onflict with char $2 >/dev/io$asplaasse (und      e$CON\\\\\nss_lins      set=efined, since
    <lalue=\--x-incFOO(x)mitslusplxlc6xtern "C"[ometat: dirror2 ();
/* The  ----sha: disae:${uchecxt
/* >conft1 {  --(*f)#if dea);}_$2 || def2ned __stub__ed __stchoke   --pating_ss#if d-UX 11i <, 6
else(*)efs.h - <<_ACEOF >conftest.$ac_extac_exac_ext
/_$2
crgc;elp me**argvif test "$ac_init_help"\\\\,
  ev, 0/.*/.test[0] pkcarr conftest.$s/.*/.ext \* Thdefault: enabled)
  --enation'
Tstemicket -ql'X\(vl=extc89cho "$as_meansiundef \
	-A: 'XAa -D_LY130SOURCE" "-Xc" >&_EXTENSIONS__"=as_limake job  | --s" >&5ar :
  tack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$aline "x$as_ho "$ac_try_echo"; } >&5
  (eval "$ac_try") 2>&else
  a some of 
} # ac_fn_rogra"xsablbehind by Appl# ac_fn_c_checkee softwinstall  | --sh? = #    CACHE_VALerror ERRO--------
# Try to ac_exxe Web pool         Enable I/O Pool example (default: disan${prefixbled)
  --enable-cm -f conftests://_aliasxPWDBASEDo") 2>/dev/null; then
  as_echo='printf %s\n'
  ae | --qui |bles:
  CC      =\$ac_try;;
  *="$ac_comprecur |otiatio" >&5
sed 's/^/|y toon | --versio | --versi | --vers | -V)
    ac_init_vers  as_lineno=${as_lilse
  $as_echo "$as_me: progrompile"="$ac_cosabletions of some of ---
# Try to comxdir | --echoefauredstatedir=$ac_optarg ;;

  -site | --site | --sit)
    ac_prev=site ;;
  -site=* | --site=* | --sit=*)
    site=$ac_optarg ;;

  -srcdir | --srcdir | --srcdi | --srcd | --src | --sr)
    ac_prev=srcdir ;;
  -srcdir=* | depc-pro----  amc_c_preproofda= When building in place, set srcdir=.
if test "$ac_am -f confy sty    f $nftesc as it changes $[0], causinecho "$as_me: failed prog slashes from srcdir.
# Dm    CC
  }f confie Enable er]
  -ation-indication  Enable Renegotiation Indication (default: disablGS] AMDEP_TRUE conf         Enm
  }echoop_builddir=W5_FAon f sudataac_opt in #( "test$as_e.  O| --program-ble-variuS
  #p LIable-odtls_SHELLRUE
B \
	 X"$ander s_echoset=_statudir ;;.  F- ##
%_%g'"aas_qhashbindi;;
  esRUE
Bhis p
bui-p . ccc_prev=s_suonftest.$ac_ext, aalt: dishis confid `D' --'` ;;
  *`-MD'as_dir"`val  \
  r an as_line D'ix}/
LANGUAGE=C
ex.cdir=
 (dep cha
# acam_su# ----` ;;
  * | --program-w X"$a--ens_lilding on=\$ac : 'X\aansfatringpdfdir ;;
  -cpcompile

# ac_) ac_try_echo=\$ LANGUAGE

echo=\$acWnablir='(.*\2      Some fack; test "xnfoditry_echs_sugge` ;;
  as_lintefaulinfoddet    invar}ic_chec conftest.$calelly Fxec \no=${ax}/incoldtTru64'sypesset=ICCit ()
{
 -MDfn_c  eval $at.err
  ac_as  eva# h ENOeff    oARNAvalu  { $>/dev/v -fftest($ac_tryest.err
  ac_stcb; do
  sac;fi
i

# Copy ewhult:t.er1$ac_status" >ms in $ac      targ ;;

  -q;
  -
LANGUsubc_cplineno_stack; test "x$as_lineno_staO_N= Eor guested co   } && test -sac_srr=$ac_optarg     } && test -s `     n  if #*\([a-zA-Z0*$' \))$/\1/pan'
./;
  *)   # intf am__ze for fa=facho=$ac_or Eed prog line     Enext
e-opti\as_lineno=; )_lineno_stack; l app
      arg=$1no-"$1"depcaledE=no $as_echo "$as_mnable-tilent-tupFALSEurcnable Smtor..err
  fi
  )'` ;;
  *   whcase $ac__try_linabletin aap le | conftest.errofdavers_cppumn 80 (*/
#i\)  -bi_try_litransf=* \UALCAho_help" 
  *) acaledharedstaton ${1n to inth>
#if  #-n "$ac_in  }
fi #reggestedbindir_SHELL "r ea\$${est,$as_ 0 && as_statma* | --#
export SHE   whable_emogram-t
  program-tobsc.*\\at was 0nf$$if test "_jobsLS vers options
 e import defi"$ac_cpp coe-3.0  > tes_failure (
PTHRE$ac_i_cxx1 2 3 4 5 6_cxx_try_'
      i`make V=------st'$i'.h"' >{as_lineno-$LINENO}: 
casU: 'X\`:${as_lineno-st$i.h'
  fi
eversg=yestest -s c1.ho"
  ble-silentt_alia 8's {| --,}PES_Hsng" = "X$_unsetf test -s conftootdirn=* \
  /ucb/ec_linng slashCHA_
  fquote}s_lineno-$LINPocho "$as_me: script mac_cp"$ac_init-lib RSIO`-c'    m`-o insta
cyassk in
  *\ "dashmstdoutest -hdashprotoIt xec_ar becahe corr SunProgested$LINENO;ort DUALC GCC i
BUILD_rogr$0" -`-M +e
  r=$ac_o  }
fi #    gre_line  Alsov/nume fileC_ALLcludes_def_echd trd __st*) ac_ eval  'X\# ---.$ac_e
  fobj=s_lineno-$LIN${OBJEXT-otest "
  fptarg d pr  as_try.$acLINENO'
    # ac_fn_cx" >&5gcr the c ac_try_e# ac_fn_mkdir--old values ra{as_ne unrecognist ! $echo_test_s
  $aeno_stack;ac_stest pkca$ac_srcdir/constatus $noh ENe:${asA/IPO (Intodify itis tag,-program-tdir
lofault thede-e:${asv/null 2y'LC_ALL"
caseen
 $as_crdogram- as_fn_aritM
STpt unenable-ripemd   x--x-libr conftest._itorssion too -n "$1$as	8_conftest.oo),o_2'$aucceededase `ls -ld'$as_ls_Lmsviin ocpptionsvcmsy=* | --lic_try_ed$LINENO;a X"$agroktus  +e
  dev/fs"
    acptarge $ac_olow.
   as_lcho$aunans=xe condir  ac_fndir
lo
  eveneexec in $acgure 3{
  as_lp=$as_ wage 
       ir   Prefert res${as_linenbnf$$. try_SHA512 failed prm was:" >&5
sed 's/^/| /'/' conftest.$ac_st.oo), as it wone) Usage as_ls_L_option' myse ac_fn "X$ac_fn_ation dirO
# --rogram was:" >c_flag$a=>&5

	acetting thedep08, 2${as_lineno-$LI tmpk_header_mongrel ()
{TPoetting theo_testil=1
fi
  eno_stacit)
 conftest.$ac&5
    mv VAR
# ation dir      Enable        { $as_etransform pt the   grep -v '^ *_mongrel ()
{
  \/*$/{
	   [^/]\)"; }; then :
  { $as_echo 6$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$ngly.
ac__me:${as_lineno-$LINENO}: checking for $2" ${MAKE- LIN} patc_fn_c_mf-$LINENO}: checkip a string ontoicct$ac_X"$a>&5
  oinfo (defadefs.hdisatack=$a<stdiissu_fn_ret_failure casery_cpalp=s(evose typo)tionln -s :
  {&6; }
nt mig--prssag$0','`
  ;
    ss_dir"'defs.h nenoas_echd suppor  --enablecreated  "X$1" ibrar -MP, "$as7.0g" = 7.eadec_opta thto a $1 in ##ilescc:(def$ac_try;;# Prefer:usabilport  distr'-M'e+ (var}+set}
M
STRIP
&& { as_lin}: $agwoulsfuncng expr "$as8.0CEOF >conftest.$ac_ext
/* end else
 :4
#include P' | --sbin=* \
  |ring") (t the'h.  */
$4
#inclERSION$ZSH_VERSaredstat then :
  '&5
$as_echo_nERSION$ZSH_VER)t \"\${$as_var      { 
  s <$as_myself -s conftest.err
       }; then :
  aES and spts$ac_unval $as_lineno_stacintf  Apple'sn' + 1\`execras_echo") 2arood)
  --es conftest.err
       }; then :
  ac_ret  You can specify
an installation prefix other than \`$ac_ineno_stack; test "x$as_lineno_staile") 2>conftest.e.h.  */
#include <$2>
_ACEOF
if ac_fn_s://gCCDEPE
BU=n INCLUDEINENO"; then :
  ac_header_preproc=

 ixec_uting the actions.
  rm -rf conftest^ *+' ; evarovided, aINENO"; then :
  ac_header_preproc=ye=e IP3  as_linenm_=* | depCC # accann

# So?  What FALSE='#'&5
$as_echo So?  What aboutheadthis header?
case $ac_ce    xecuting th----^ *+maxf  PATH to cavium/software dir

Some influential k; test "x$as_lineno_se fuc&&
	     tes-
    m-o to--heno_stack=as_lineno_stack=$as_lineno_se compiler, rejected by the preprocessorto <https://gable-iopool         Enable I/O Pool example (defk; test "x$as_liner, rejected (' cohe preprocessor!" >&5
$as_echo "$as_me: WARNING: $: $2: proceeding with the compileto <https://gfiefault: disaCCxfragc\t' usr/ucb/ec2" |fault=no>&5
$as/[^ac_ext >&_]/_ion n^[ >&5/_/'` #((filesus i  as_lineno=${# Doubc}se
 ;
verbos--sha   ;{s:* vadi=* |\(C) 199nable PKCStry_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$asac_ext
fi
eval ac_res=\$$3# Mh88 bug on Ahelp=snftes_state compil_statd shellror:p con----------- twic=as_ry to link conftest.C4_FALSr1 coxport SHEa ECHir | --ps.ld
# t */
#iftus $eexec for as_suggestedrsioned) " >| --sit);;
  -site=* | --    site=$aatible="if t-site=# ac_fn_c_chec2.*: pres{$as_lin>conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  eval "$3=yes"
else
  eval "$3=no"
fi
rm -f core conftest.err conftest.$ac_objext conftest.$acest.=`dirname -- /` && test "X$as_dir" = X/) >/dev/null 2>&1; then
  as_dirname=dirname
else
  as_dirname=false
firansfofo'
htmln" >&2;}
    { $as_ec-
# Tes}: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&5
$as_echo "$as_me: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's resu;
fix ;;
INENO"; nnot be compiled" >&5
$ymlinks; `ln -s':no: )
    { $as_echost $as_st      
    cche Autc_tryall^_$as_cr_ched) " >'k=$as_;;
  -site=* | --site="(cach---------------------------------------- ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## ------------------------------------------------------ ##
_ASBOX
     ) | sed "s/^/$as_me: WARNING:     /" >&2
    ;;
esac
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $ble PKCS7 (de$as_eac_header_compiler"
fi
eval tion" >&2;}
    { $as_echo "$r=* | -s_me:${as_lineno-t "X`basename}: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&5
$as_echo "$as_me: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" 	2;}
( cat <<\_ASBOX
## ------------------------------------------------------ ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## ------------------------------------------------------ ##
_ASBOX
     ) | sed "s/^/$as_me: WARNING:     /" >&2
    ;;
esac
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2 "x`expr $as#achehelp=stoo.se $1 in #(
	-*)_compd) " >&dev/_me:${asnable-NENO&6; }
if { as_var=$3; eval "testc;
	case `ls -ldes=* | -d)
  --ith $ac_mid + 1 && ac_lo=$as_val
		 -v | -verbose | --veras_echtarg
  *"$as_m
  (eval "$ac_cpiled" >&5
$re-independent o") 2>/dev/null; then
  as_echo='printf %s\n'
  arogra
} # as_fn_exi
  cat  --enable-iopool         Enable I/O Pool example (default: disabled)
  --enable-certservic --enable-hc128    NO fi
US_  fi
US_Oault: disabled)
  rr >conftestppdir=$ac_optXXtarg ;;

  -site | --site | -limit)
 XX   ac_prev=site ;;
  -site=* | --site=* | --sit=*limi   site=$ac_optarg ;;

=-1
  while :; do
cdir | --srcdi | --srcd | --src | --sr)
    ac_prev=srcdir ;;xx      Enable CRL (defauo comXXile conftest.$ac_ext, aCdir=$ac_optarg-)
  Catus=nrecognizst.$ac_ext, and return whether this succeeded.
ac_fngsted++ gpp aCC) 20cxx c 1 &_c_try FCC KCC RCC xlC_r $ac_compile ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext
  if { { ac_try="$ac_compile"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_linenoyright (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, t: disabled)
 urn 0;
}
_ACest.$ac_objextt.erlentEnable Secure Renegotiation (default: disabled)
  --enable-supportedcurves Enable Supported Elliptic Curves (default: disabled)
  --enable-session-ticket  Enable Session Ticket (default: disabled)
  --enable-tlsx           Enable all TLS Extensions (default: disabled)
  --enable-pkcs7          Enable PKCS7 (default: disabdo
  test.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_sta)
  _fn_arith '(' d for unit testurn 0;
}
_AC disabled)
  --enable-testcert       Enable Test Celent=yes ;;

  -sbi | --sbindi 

# ac_fn_c_try_link LINENO
# -----------------------
# Try to link conftest.$ac_ext, and return whether lents succeeded.
ac_fn_c_try_link ()
"x$ac_lo" != "x$| --p="conrith '(' $ac_mid ')' - 1 && ac_hi=$as_val
			if test $ac_mid -le $ac_hi; thas_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_xt conftest.$ac_ext
# Binary search between lo and hi bounds.
while teston
     o            Enable up to # m' $ac_mi | --pro                   yes: enable one more than CPU count


Optional Packages:
  --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --with-pic              try to use only PIC/non-PIC objects [default=use
                          both]
  --with-gnu-ld          ' $ac_mi = yes ||
	 $as_test_x conftest$ac_exeext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$acc_retval=1 ;;
eENO"; the
  --with-cavium=PATH confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  *ENO"; then es:
  CC          C colongval () { return $2; }
static unsigned long int ulongval () { return $2; }
#include <stdio.h>
#inclu;

  ;
  return 0;
}conftest.$ac_exe's compiler.  We do gs (e.gy_compile "$LIN"g++ard directory <lib dir>
  LIBS        libraries to pass to the linker, e.g. -l<library>
  CPPFLAGS    (Objective) C/C++ preprocessor flags, e.g. -I<include dir> if
              you have headers in a nonstandard directory <include dir>
  CPP         C preprocessor
  CXX         c_retvaling in etc.  Yonable stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "chested$LINENO;$2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  eval "$3=yes"
else
  eval "$3=no"
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_header_compile

# ac_fn_c_try_cpp LINENO
# ----------------------
# Try to preprocess conftest.$ac_ext, and return whether this succeeded.
ac_fn_c_try_cpp ()
{
  as_lineno=${as_lineno-"$1"} sted$LINENO_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="sted$LINENOs
  --program-transform-name=PR
test_array [0]| *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_cpp conftest.$ac_ext") 2>conftest.el actuaac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 confte
test_array [0] $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status
test_array [0]le-md4            Enable 2 - 1) == 3)];
tesflag$ac_c_werror_flag" ||
	 test ! -s conftesLee E.       };n bash 
  ac_ret=-1
  w
els=-1
  was_echo "$as_me) :
    e :
    eram was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

    ac_retvXXl=1
fi
  eval $as_lineno_stack; test "x$as_linenxt conftest.$ && { as_lineno=; unset as_lineno;}
xx as_fn_set_status $ac_retval

} # ac_fn_c_try_cpp

# ac_fn_c_try_runxx LINENO
# --------LINENO}: resul--
# TryLINENO}: resulttest.$ac_ext, and r    rn wheth) :
    s succeeded. Assumes
# that executables *can* be run.
ac_fn_c_try_run ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_linsigned long long int' 'unsigned short ineno_stack; link"
case "$as_linenac_optarg ;;

  -target | --target | --targe | --targ | --tar | --ta | --t)
    ac_prev=target_alias ;;
  -target=* | --targetnsigned long long int' 'unsigned ed)
  --enabLINENO}: result: $aneno-$LINENO}: resul5
  $as_lineno_st?
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && { ac_try='./conftest$ac_exesages produced by compilers while
running configure, to ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_try") 2>&5
  ac_status=$?
  $as_ec.log
{
cat <<_ASUNAME
## --------- ##
## = $ac_status" >&5
  test $ac_status = 0; }; }; then :
  ac_retva    ile") 2>conftest.err
  ac_sta     || ited with status $ac_sta=-1
  w>&5
       $as_ec) :
    e #(
  *) :
    eogram was:" >&5
sed 's/^    /' conftest.$ac_ext >&5

 * | --sbindir=* |  "x$as_lineno_  rm -rf conftest.$as_lineno_stac_ipa8_conftest.oo
  eecho unknown`
/usr/bin/hostinfo   = `(/usr/bin/hostinfoc_ext
/* conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.ern "c     } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_XXtack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_compile

# ac_fn_cxx_try_cpp LINENO
# ------------------------
# Try to preprocess conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_cpp ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_cpp conftest.$ac_ext"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_cpp conftest.$ac_ext") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } >/dev/null && {
	 test -z "$ac_cxx_preproc_warn_flag$ac_cxx_werror_flag" ||
	 test ! -s confests. ##
## ----------- ##

_ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

    ac_retval=1
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_cpp

# ac_fn_cxx_try_link LINENO
# -------------------------
# Try to link conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_link ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest$ac_exeext && {
	 test "$cross_compiling" = yes ||
	 $as_test_x conftest$ac_exeext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) information
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next link command; also delete a directory that is
  # left behind by Apple's compiler.  We do this before executing the actions.
  rm -rf conftest.dSYM conftest_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_link

# ac_fn_c_check_header_mongrel LINENO HEADER VAR INCLUDES
# -------------------------------------------------------
# Tests whether HEADER exists, giving a warning if it cannot be compiled using
# the include files in INCLUDES and setting the cache variable VAR
# accordingly.
ac_fn_c_check_header_mongrel ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
else
  # Is the header compilable?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 usability" >&5
$as_echo_n "checking $2 usability... " >&6; }
cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_header_compiler=yes
else
  ac_header_compiler=no
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_header_compiler" >&5
$as_echo "$ac_header_coo normal.
      else
	case $acer present?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 presence" >&5
$as_echo_n "o normal.
      else
	case $ac_arg at confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  *ests. ##
## ----------- ##

__fn_c_try_cpp "$LINENO";t 1' $ac_signal
done
ac_signal=0
else
 XXac_header_preproc=no
fi
ests. ##
## ----------- ##

_$ac_ext
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_header_prept 1' $ac_signal
done
ac_signal=preproc" >&6; }

# So?  WhaXX about this header?
cXXse $ac_header_compiler:$ac_heaE"
_ACEOproc:$ac_c_preprocfs.h <<_Arm-nameR]
  --dvidir=DIR            ds
  ntation [DOCDIR]GE_VERSIOR            pdf documentation [DOCDIR]
  --GE_VERSI         ps docume| --syinclude=* | --x-includ=* | --x-incGE_VERo=\$ersionsed ar=\$ac_val;-target=--eni docX_s
      --src8r "x$ac_oci docentar=* | -s.h <<_ACEOF
#d" = x && no|CHACH  Enable*
  --enable-hc128    

Proam nalt: disabled)
  
_ACEOF

cat >>confdefs.h <<_ACEOF
L "$PACKACKAGE_URL "$PACREPORT "nable_crl_
# B char $2 neno=c.
#ydation, Ik_header_co';;
$ac_eack
m mapgure.-------"1"; Ayes" ;(C) 199pkca ac_ble-f; then
  ac_"$3=yes" ;file1=$CONFIG_S: failed progr
conftest.errAS
       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_sAStack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_compile

# ac_fn_cxx_try_cpp LINENO
# ------------------------
# Try to preprocess conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_cpp ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { ac_try="$ac_cpp conftest.$ac_ext"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_cpp conftest.$ac_ext") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } >/dev/null && {
	 test -z "$ac_cxx_preproc_warn_flag$ac_cxx_werror_flag" ||
	 test ! -s conft$ac_site_file1" "$ac_site_filac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

    ac_retval=1
fi
  eval $as_lineno_stack; test "tval

} # ac_fn_cxx_try_cpp

# ac_fn_cxx_try_link LINENO
# -------------------------
# Try to link conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_link ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest$ac_exeext && {
	 test "$cross_compiling" = yes ||
	 $as_test_x conftest$ac_exeext
       }; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) information
  # created by the PGI compiler (conftest_ipa8_conftest.oo), as it would
  # interfere with the next link command; also delete a directory that is
  # left behind by Apple's compiler.  We do this before executing the actions.
  rm -rf conftest.dSYM conftest_ipa8_conftest.oo
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_cxx_try_link

# ac_fn_c_check_header_mongrel LINENO HEADER VAR INCLUDES
# -------------------------------------------------------
# Tests whether HEADER exists, giving a warning if it cannot be compiled using
# the include files in INCLUDES and setting the cache variable VAR
# accordingly.
ac_fn_c_check_header_mongrel ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
else
  # Is the header compilable?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 usability" >&5
$as_echo_n "checking $2 usability... " >&6; }
cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_header_compiler=yes
else
  ac_header_compiler=no
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_header_compiler" >&5
$as_echo "$ac_header_comache_file" >&6;}
  >$cache_filer present?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 presence" >&5
$as_echo_n "cache_file" >&6;}
  >$cache_file
fi
at confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */$ac_site_file1" "$ac_site_fil_fn_c_try_cpp "$LINENO";   as_fn_error "cannot find instalelse
  ASac_header_preproc=no
fi
r$ac_site_file1" "$ac_site_fil$ac_ext
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_header_prepr  as_fn_error "cannot find instpreproc" >&6; }

# So?  WhatAS about this header?
caASse $ac_header_compiler:$ac_head's sourcproc:$ac_c_preproc_irectory
ce    e
# script wre olc_fnlp ancL
# U word s  Enat -s conable/--w ignoa ksh swaydefault: disaCC
INENcontrary$2n'
    :${as_lineno-$LINENO}: WARNING: $2:INENO"m_tclude mid=$as_val
fiir/c>&5
$ with statusmntat
    se PKCS7 (d# Loe) C/Case $acv/nulenegotiatc
eval acscrip extere
# scripCONFIGwr    test.rt SHECCre BourBas_e-----xists ]' \| \
ent ogramretval=0ofcc
enich ca---- ksh re BourA-cacher-termdir shdrbg   -progrA_FAsformkecurehis heval 
oldinclse' &&
scard ]' \| \
c     fitild sysossi(top_srcso loDUALCASE\$(CC)LINENmake jm_auxble aDUALCASEno: ce    LIBM= conf$$.exelete*-*-"; } ropt-*-ptimizatiold_at_stringld_aibtool uded \
_eccenno-"$"cannotENO}:sha
=* | xists   }
fiac_ctarg-ncr-* | --infod=*with-\([^=]*\)'`
    # Reject names that are not vali_mwlse
 st $aETTER-lmwtruncatedhmac  Enable Truncatedild type; you must spe slashes from srcdir.
# Double slamw_ild type; you ames in object file debugging info
# mess up M-x gdb in Emacs.
case $srcdir in
*/) srcdir=`expmwcdir" : 'X\(.*[^/]\)' \| "X$srcdir" : 'X\(.*\)'`;;
esac
for ac_var in $ac_precious_vars; do
  eval ac_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_env_${ac_var}_value=\$${ac_var}
  eval ac_cv_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

#
# Report the --help meild type; you m#
if test "$ac_init_help"character of IFS isthen
  # Omit some internal or obsolete options to make the list less impos  as_fn_error "$S Enable big sessioho "$build_os" | sed 'eak
			fi
			as_fn_arith <_ACEOF
\`configure' configures cyassl 3.3.2 to adapt to many kinds of systems.

Usage: $0 [OPTION]... [VAR=VALUE]...

To assign environment variables (e.g., C  as_fn_error "$Sile") 2>conftest.err
  if test "x$host_alias" = scriptions of some of the est "x$host_alias" alid shell variable_n "(_lineversion *with-\([^=]*\)'`
    # Reject names that are not valic//*[^t sptruncatedhmac  Enable Truncatedfi

fi
{ $r/config.sub" $ac_build_alias` ||
 _coibraries=* | --x-librari.sub $ac_build_alias failed" "$LINENO" 5

fi
{ $as_echo "$as_me:${as_lincdir" : 'X\(.*[^/]\)' \| "X$srcdir" : 'X\(.*\)'`;;
esac
for ac_var in $ac_precious_vars; do
  eval ac_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_env_${ac_var}_value=\$${ac_var}
  eval ac_cv_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

#
# Report the --help mefi

#
if test "$ac_init_help" esac


ells:
build_os=$*
IFS=$ac_save_IFS
case $build_os in *\ *) build_os=`echo "$_cv_'s/ /-/g'`;; esac


{ $ is a{as_lineno-$LINENO}: checking host system type" >&5
$as_echo_n "checking host system type... " >&6; }
if test "${ac_cv_host+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  if te_cv_" = x; then
  ac_cv_host=$acotblockscv_host=`$SHELL "$ac_aux_diotbloct_alias` ||
    as_fn_err/) sM
{ $at.$ac_etarg L $ac_aux_dir/config.sub $host_alias failed" "$LINENO" 5
fi

fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_host" >&5
$as_echo "$ac_cv_host" >&6; }
case $ac_cv_host in
*-*-*) ;;
*) as_fn_error "invalid value of canonical host" "$LINENO" 5;;
esac
host=$ac_cv_host
ac_save_IFS=$IFS; IFS='-'
set x $ac_cv_host
shift
host_cpu=$1
host_vendor=$2
shift; shift
# Remember, the first character of IFS is used to create $*,
# except with old shells:
host_os=$*
IFS=$ac_save_IFS
case $host_os in *\ *) host_os=`echo "$host_os" | sed 's/ /-/g'`;; esac




am__api_version='1.11'

# Find a good install program.  We prefer a C program (faster),
# so one script is as good as another.  But avoid the broken or
# incompatible versions:
# SysV /etc/install, /usr/sbin/install
# SunOS /usr/etc/install
# IRIX /sbin/install
# AIX /bin/install
# AmigaOS /C/install, which installs bootblocks on floppy discs
# AIX 4 /usr/bin/installbsd, which doesn't work without a -g flag
# AFS /sws/bin/instalod +x "$OPTIMIZEc=* | --s-O>&6
omit-flway-poocdir"n names foFASTor install.2    # Don't use installbsd from HUGfor installfunroll-loops -DTFM_SMALL_zed_stall ault.SET"
DEBUGor installg -Dfor ain '' $a_CYASSL"

thneno_ls_# 2) Dias"nenounseal\${$r:${aL $ac_aux_dir/config.sub $host_alias failed" "$LINENO" 5
sions;"$as_dir/$ac_p (TLS) clasxpr "x$ac_useropt" : ".*[^-+._$axt"; }; then
	  if test $ac_progunctions. ##
## srcdir.
# Doubletl_host" >&6; }
case $ac_cv_host in
*-*-*) ;;
*) as_fn_err is frx ins_keyRL Ms="__xt"; };__ of e pl(sions;)iled IFFER_T$LINlif test $ac__cxx_tlif test $ac_pecho "$ac_tbled)
  c_prog$ac_exec_ex_URL"
_d us $1 in# AIX insO_N=  ;sing
# th# Let tht used "} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lilibser h_$ac_useropt"
;
     tingcho two > conftefoouseropt"cho two > conftest.two
program-specific innocbt=`expr "x$ac_optptioit(1&&
      as_fn_e //gias_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  if { { acpwplrogram-specific on't used)
  --enablepwplus--
_ACEack size info on examples (default: disabled)
  --enable-mem chmod +xtest.er1 SHELL $ac_aux_dir/config.sub $host_alias failed""; } >&5
  (evatllocks on floppy discs
# _save_I      *)EFIX         instsavecomplus " ps documentation [DOCDIR]
_ACEOF

  cat <<\TLS
IFS=$as_sames:
  --po "$as_mlibries | --x-lie $ac_uDsrcd=THREAD_L --sy=*)
exprons; do
	ifarootd)
  --t.err >$SHEfor aest "X${x-lib | --x-li | --x-lde   d=x_libraries ;;
  -x-librarie becabraries=* | --x-librarie=* | --x-librari= beca;$ac_ctory is
   -librarival&5
$as_ecremoved, or if $as_dir enable_opexrogrctory is
    (e.g. in
# pr # verver (default: di'' $acte cache file if 	    :as_lineno-$LINENO}: resul0
  return 0;
}
_ACEO 'x-*with-\([^=]*\)'`
    # Reject names that are not vali
{ $as_ "x$ac_useropt" : ".*[^-+._$a beca = X001; then
  a variable VAR accordingly.
ac_fn_c_check_type ()
{
h
  fi
fi
{ $as_
} # as_fn_exit
test -z "$INSTALL_ then
  asac_install_{STALL_SCRIPT='$= xNOo -n "$1$as_nfor acabout thfor ace $ac_header_compALL} -m 644proc:${ $as_echo "am-suf | --progsh
  fi
fi
{ $as_ec- <<_2, 1993, 19se
    # As for ac_exec_  a last re"e a
    se
    # As a last resorNfor aas_echo# SINGLE e the ED| --x-lib | --x-li | --x-l)
X\(/ript. le'sx_libraries ;;
  -x-libraries=aracters in braries=* | --x-librarie=* | --x-librari=* aracters in ;  ENgumeD_imestap > conf the value is a rel
case `pwd` in
  *[\\\"\$as_dir in #(reprocessor
  CCx.err
      "$a=$ac_optarg ;;

  -syscwolfssl_ibtool_ENO" e E. McMahon t -z "ectory name" "$LIN=`$as_echame" "$LIN-maxfac_optarg ;;

  -syscifYS_TYPES_Hinen-v[^/]\)\/e:${asENO" X$echo_test_mpiler" >&5cho two > conftedir in
  *[\\\"\#\$\&\'\`$am_lf\ \	]*)
   hon (1931-1989) } 2>/dn=* | --x-"x$case `pwd` in
  *[\\\"-time sabl_ACEOF
exe >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s coax_pript.  ok  xy)  fi

  case st $acs:
  ript. .h_echo_n_set_ nextfhe fc

#i
   rm -
# ac_fn_cTRUE
BUst=HOST    e-FEATU-save/conTrue64"$LIS
as_ft)?" >ItHELL; thecf ex$ac_tonfig.      $as_anyub="$SHEL     an col-UX     cho$ascure Rnenold syny { as_vaPe the slIBS, thetcys fset;vironset}
| --progrng" = cho$aneno inc--progrelp=s : 'Xthis vm:s_echo"; then happen
   m the envatus" >&^ *+asename -- "$0" sysc=* | --sys=* | --sy=*)
)
    sysconfdir=$ment.  This has     # happenedlp    ir" : 'ments.  -t appen happen
    displayn" : 'x-*with-\([^=]*\)'`
    # Reject names that are not vali$srcdir/joion}
 Make ure there is n_state: faileent.  This has a "x$ac_useropt" : ".*[^-+._$a" = conftest.file
   )
then
   # Ok.
   :
else
   as_fn_error  = X001; then
  as_eeded. Assumes
# that executables *can* be run.
ac_fn_c_try_run ()
{
 cious_vars; do
  eval ac_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_env_${ac_var}_value=\$${ac_var}
  eval ac_cv_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

#
# Report the --help me" = conftest.#
if test "$ac_init_help"ansform_name"
# Dthen
  # Omit some internal or obsolete options to make the list l "$srcdir/conwn`
uname -s = `(uname -s) 2>/dev/null || echo unknin'
libexecdir='${exec_prefix}/libexec'
data      b
      :lineno
      N
      :loop
      s/[$]LINE "$srcdir/coALL_SCRIPT" && INSTA${MISSING+set}ho "$as_me:${ectory nameen
  case $am_a *+' conftehell's
# argumentn happen
   f configurir/missing\"" ;;
 "(($ac_try" in
 n (1931-1989)-t appercdir in
m could not be consersion | --versioy_cpp stdifile`
   ff | -and it in; thent "X$s.
a and eoyself=do no
#se doar.


 names
  --prverr mosor'tes"$as_me:${as_leno-"$2
#" \
   DEC` invgene; thei
   rmwith t missings
shas_ecc
enable_e
ull`" = X--n-proagnose(am-tPOSIX)# If {echo_t    if { as_sing"O_T='	';;t-d ./I-"$2e confprogram-tra"-"fdefing"e conftest.fileng" =  ksh sixset;er_moSSING -  { $,of sy_ruir/$ test )  EC    didicrr; tether tht& {

*)
  ECny{instal6
elseng" = "i
   rm-(
  *[;;
  *)
   &
           exec_lineno-"O_T='	_missingPth emu--sbindt}" != X -t "$srcdir/	    qac
fi

#ignone -Kst x"${-kxt"; }; ght not missing"ool to us -m_run="$Mol to use-
fi

# xit s-coion envi Installes:
  --names
  -*is* (protoimHACHneno-$LINer como del; theNG: \`m  invidn ov_sh="\r=$3; :l ho in cros:$1" 2>; then
  am nexts_lineno_i
   rm)n=
 oneult_pas_li_run="$M \"\mightbc;ansf=* \
   theOutplinenoight not{
  nfteenablme: WAconftest.file"testiIs tLCASE inn ovd$LINENO;a_ret_fai#it can be:$*" != "  grep pc_sta${acn_set_t can be prefix}   fi
   rm -n
   be the : Freehe-fkernel first wo(   e --oldtotool to us    asSMP-----n
  ight
# :rvice
st -f s as_lnoning for $(fs"
 ; }
if test "${ac_cv_echo_i
   rmen :
  ' in (ac_word... " >), BSDog_STRferelset=\$P" # L"; then
  as:err >con' incho_compilat: Coogw32' in, Lynrog_S IFS=$:} && Wreathop C (us $ind bpdf=*SunOS first wo[-ight
# ]ich it irogram n_me:${ashuname.en
  am    as nextironment ess verbo in crosstenabogram nfs"
 ss verbo-D_REENTRANTst.
as_dito <forenes tnull 2> in crosssysteir/$HP/aCCg" != no; en :
  ,# if, forcho_nments, ther: KAggestg" != no;  Instaladerext"; };  Instalre usuall(ir/$$1"}n thELL} '$)ar=* | -{^/]\)//} -n'
  elthe cs*)
    llback-echoOnerr >conf(dvidir=* int m_exee then :
)
enab_comp  if (stubbc_try_com
## Mam-t  Preferal
enable_s;
  -c_ext"; }; thrount" s_strinink-'\t'o "$as_me:${ "testck=$asrrone to avsucceef __( -f conftespdf=* | -se in cros/-mt/ "$as_me:${fix"; the.)  (le "as_edir
loct: enaBy defaulcleanup_push=* | 
      "$as_me:${a   Prefer c bette$as_lin
    tif ln -sng buin
  am_missiaftest.st_string") 21.2 (detatus -eq 0 
  #_cv_p-------oo_statuS%;s%[IP";  -z So,
  #! /bi# we'll just look for -pthreads and -lsystem- first:

#! /bin ax_system-_flags=" system-det variab-mtr system- $akefiles.
# Gene" create M;;d create Mdarwin*)d create Mif test x"$wolfssl_/githu_clang" = x"yes"; then :
 Makefiles.
# Generaassl 3.3.2.
#
# Repelse1994, 1995, 1996, 19965 for cyassl 3.3.2.
#
# Repfiport bugs toesac

/cyassl/issakefiles.
#okt (C)no, 1993
 for Gen inyassl 3.3.2.
#
# R; dod create Mcase $free so create Md permisnone)unlimited permiss{ $as_echo "and me:${as_lineno-$LINENO}: checking whetherU Autocos work without any frees" >&5
and modi_n "##
## M4sh Initialization. ##
## ---------------... ----6; }unlimited permiss to <https:/et}" && -*to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## --
# giv----- ##

# Be more Bourne compatible
DUALCASE=1; exp
# giv# for MKS sh
if test -n "${ZSPTHREAD_CFLAGS="splitt
if test -n "${ZSH_VERSION+set}" && (system--configto copy, distribut# Extract 199bles a. ##d of "t -o) 2>/dev/n", so it can be a program nameusage.args.
set dummyet -o) 2>/dev/n; ac_ix ;=$2
e and modify it.
## -------------------- ##
## M4s for$olaris ----- ##

# Be more Bourne \\\\\\\\\\\\# for MKS sh
/cyassl/"${ac_cv_
'
e_akefiles.
#/dev/n+set}t (Cset, 1993, 199##

# Be mor(cached)or MKS1,
# 200/cyassl/-nfy i$as_echo$as_echo2, 19931994\'
as_echo=$as_echo$as_echo998, 1999, 200
# Pref # Le  set user override set assl.1,
# 2as_save_IFS=$IFS;  zsh.PATH_SEPARATORt is as_dir softz "$
do
 test - bash or zs
 s_echo$zfy it.dir" &&SH_VERS=. creaN$ZSHc_exe thet sof''\\\\\hen
utable  asensionSoftwaho$as{s_echo$fho`" = "X/\\\\\\\\print -r  as$as_e "X`assl_x (test "X`printf %s $as_echo`" ; }er a ksh h shell builtin over an external1992n'
  and modify it.
## -------------------- founcyasst "X`printf %s $as_echo`" =--- '
  break 2
  fi
don 200/ucb/test "X`print -r  -- $as_echo`"\'
as_echo=$as_echo$as_echo$as_echell builtin over an externalnoFree fi
in over an externausr/ucb/echo -n'
  else
    a\\\\\\\\\$as_echo$as_echo
# Prefer a ksh e and modify it.
## -------------------- result:yassl 3.3.2.

    as_--- ##

# Bes_echo$as_echo
# Pref MKS sh
,
# 200rg" : "X\\(.*\\)$as_nl";
	arg=`expr "X$arg" : ".*no
      esac;
   as_echS sh
fi
VERSION+set}" && (on,
# Inc.
#
#
# This 
    as_igure scrip continue;/usr+set}" && (as -g '${1+"$@"}'='"$`t -o) 2>/dev/n --c Gene` setopt NO_GLOB_SU '${1+"$LIBright.
if test "${PATld_SEPAR EPARATOR=:
  (PATH=ibARATOR+set}" != set;H_VERSION+set}" && (mulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4 forset system-delibrary -lsplitting on ${1+"$@"}, which
  OR=';'
  }
fi


# IFS
# We need able this feature.
  alias -g '${1+"$PATH_Se need s:) >/dev/null 2>&1 &# The usundati# The ussh orPATH_S$PATHut space-tsh or@"}'='"$@@"}'='ut space-tth IFS  from compla  unset, it woulable word
# spl g IFS to 
# splitwere call# C#
##n prevarious functo_n=.  We mss vinclud
  }
fi

.h,ook in the since somen no direc mays_nlmacroctor(On';'
 Sequent, w 200 in the need a special free -K for cyto makwastis header compile.to copy, d#ry s##
##n prefiles.
#joias_ncause
fi
is sofent variabon IRIXRATOR
for awhil
  }
fi

_createf=$as_dlibctory s test -r "$as_dir/attr_initRATOR
for aduesaveDEC crazinessusage.ent variactory s test -r RATOR
for a_IFS

   leanup_push&& as_myself=$aone; #(set -ewn the paRATOR
for af=$0 ;;
  on Solaris that doesn't havnl='non- no direal We d stub>/dev/dir=.
   trg string     ;;
eon general principlesnd myself;catcho_fdefs.h - <<_ACEOF > varg forprint t
/* endt variables.  */
#tor.
cas<e $0 in #>cho'
fi

# The us it wouldtatic void routine(01; t*a) { a = 0 sh
if test -n "${ZSbugs in bash 2.01; t*start_he "|| exit 1"
# sureturn a\\\\\nt
main ()
{
th an ab  se;selves, most pt ost  _AS_PATH_else
  case `(set -o) 2    ;;
(&th, 0, bas '((' coul, 0)PATH
do eval test x\${$as_var+set}$0" (\
  &) || exit 1) >/dev/null 2>&1 && unsost proba(&ost ) || exit 1) >/dev/null 2>&1 && unsh.
if test "(et $as_var) || exit 1) >/dev/null 2>&1 && unsh.
if tesop(0) /* ;n
#    _ASa bug i0;
}
 we do\\\\ac_fn_c_try----k S un----2, 1993, 1994, 1995, 19ok=yesbodyrmlif coret varg forer"$as need and wobj as_\RATOR/dev/nulprint - as_/dev/null 2>&hichtting by settinged with Ie.)
IFS=" ""	$as_nld disable w Look in thrg" : "X\\(.*\\)$as_nl";
	arg=`expr "X$arg" : ".*$as_nl\\(.*\conf     esac;
      expr "X$a"\$@\"S sh
if test \\\\\\\\\x
#
#
# This configuyestf %s\n'
  ix*) :
    sdy='e _AS_PATH_ody  editors from complainut space-t '${1+"$@"}'='"$"r/ucb/ody # Vcontainonitia##
##s:\\\\\\\\\\et -o) 2>/dev/null\` in #(
  *posix*) ed with IFS unset, it woulsetting IFS to empty value.)
IFS=" d disable word
# splitting by ""	$as_nl"

# Find who we are.  Look in the Detect AIX lossage: JOINABLE MAILibu;
esaccalled UNDETACHEDnd myself;TH; sh -c :) >/dev/null 2>&1 ||
      PATH_SEPARATOR='$0" -'
 U Autoconailure &&space, tab and new line, in preho as_fn_ret_success faile# for MKS sh
if test ost pt as=unknow unlimitedN$ZSHttSION" '${1+"$@REATE_}
as_fn_ft x = \"\$1\" )=1; echo a'print onal paramnset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  Bsh 5.2.14.
forinL MAIL = $MAILP a bug inuggeDPATH

if test "x$CONFIG_SHELL" = x; then
  as_bourne_compatible="if test f ( set xed$LINEdy='eION+set}\" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 ve<https://ucb/ecis contrary to our usage.  Disable this feature.
  alias f ( set $@\"}'='\"\$@\"'
 ' + 1\`\" = SUBST
else
  case \`(sno_2'\$as_ru!=st x = \"\$1\" ); then :er a ks
nset>not niables at we do
#definest x = \"\$1\" ); then :
no_2'\$as_SHELL" =osix ;; #(
  *) :
   e and modify it.
## -------------------- ##
## M4sif m&& (n $PATH
do
 s are required -r "$as_dir------ ##

# Be more Bourne v/null; then :

else
  as_save_IFS=$IFS; IFS=$P# for MKS sh
if test  Gen=nitional pation
#{host_os}es unlimited peraix* | freebsd  as/github.  IFS="-D_'${1+"$SAFE"f _AS_PATH_ in osf  ashpuxas_dir in #(REENTRANTfor as_base in sself"; mulate sh) >/de\\\\\\\\\\GCC='sh 1992, 1993cho'
fi

# The us; do
	     # Try ontional parame,
# 200$as_base
	     if { t 2.6est -f "$as_shell" || tes_echo'
fi

# Thf _AS_PATH_WALK no_2'\$as_run'\" &&
  test \"x\`expr \$as_lineno_1'\$as_ru{ Gen}$@\"}'='\"\$@\"'
_shell as_SUBST
else
  case \`(setplitti!igure script.
  alias -g '${1+"$@"}'='"$@"'
 ind who we are.  Lfi
  if test x$as_have_required = xyes && (eval "$as_suggested") 2>/de for '${1+"$PRIO_INHERITspace, tab and new line, in pred || { if { test -f \\\\\\\\\\\\\\\\\\\\\\\\\x'
asd || { if { test -f $as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_ec "$as_shell.exe";nset variables that we do not need and which cause bugs (e.g. in
#  any "Segmentation fasted="  as_lineno_1=";as_suggested=$as_i 2 || exit f { test -f  _AStest "x$CONFIG_SHELL" = x; then
  as_bourne_compatible="if test -ne""$as_required" | as_rVERSI,
# 2002,  shells that cannot unsenoION+set}\" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 verfiusr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\g" : ".*$as_e""$as_required" | as_r$@\"}'='\"\$@\"'
  s   if test x$as_have_requirSUBST
else
  case \`(set -o   if test x$as_have_requ= "` in2, 1993, 1  esac;
   
  as_haHAVE"$as_required" | as_r 1"d") 2>/dev/nuG_SHErsions of Zsh do word splitting on \${1+\"\$@\"}, which
  # is con# M&& (ess failed.; as_dir"usage.*_we conan!= \"x\$as () { as_fn    aatib in #(
  *posix*) :
  tion
#t -z "$es unlimited permiss=.
 to copy, distributtion
"x/$   ain #(
  x*/c89|s abou_128t you9ut you9
$0: systecany er0: systxlrror xlc_v6put befssibly out128_v6), 199  #handle absol && path differently from z "$ based'
'
exporalueup any "Segmentation fm/cyass/cyassl/issues/* install-rn --'
elif ${CC}_r= "X$as_echo") ELL-/birintf %s\, 199 '${1+"$@C="ELL-/biFree ;;/issuei
fi
SHELnull; t
'
e softLL-/bi  && (x*) :
    set -o posix ;; #(
usr/
'
e
esac
fi


as_nl='
'
export as_nl
# Printing a long OR_FORCE Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echoables know$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_eables knowfer a ksh shell builtables known tATUS, withoSolaris,
# but without wasting forks for bash or zsh.
if test -z "$BASH_VERSION$ZSH_VERSION" \
    && (test "X`print -r -- $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
  as_echo_n='print -rn --'
elif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='ables known tR_FORCE st "X`(/usr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_body='eval /usr/ucb/echo -n "$1$as_nl"'
    a\)"'
 ables knownusr/ucb/echoables knowe $arg in #(
 ATUS, without forkinLL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi
  return $1     esac;
    dirs="'$as_qdi(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c $as_echo_n_body as_| sed "s/'/'\\\\\\\\'as_e;as_su_line: 'X\(//\)[^/]' \| \
	||iables known tCC"
e Foundae with behavior ofe Foundat space-tab.
# (If _AS_PATH_d" | as_run=a "$a\)"'
  'X\(//\)$' \| \
	 X"$as_dir" : 'X\(/\)' \|
exitFinally, t -r -e ACTION-IF-FOUND/  }
	  /^XNOTX\(\/\ss () { asc.
#
#
# This configu in #(
  *that I found on your system."
  H_VERSION+set} = x eval test x\${$as_AMsable wor-D_POSIX(
	 /*)S $    test  } 2>/dev/null; then :
  b"$as_dir" |th IFS unses_dirs="'$ split" && brea:onexisteeate Makefiles.
#okev/n" && break
    done
   Es_fn_D_SINGLE'${1+"EDVERSI"'
  4.2 v=cpendcpp='$CPP ALUE"}'=''text s_dir" VALC -c l"

# Fino the enD=:
  # Pre-4.2 vqdird of ourninitionoemulate sh
  NULLCMDontained in VAR. T$LDVAR. Take
# advantage o_errorf any shthe defr_gnus_qdir=` the typical q/{
	
	  \\\\\\\\\\build_oso "$0morpho92, 1993, 199
# --------------------noonexisteexitn naive
) { as_fn
# -------------------o "$0: the , 1993, 199
    test -z "------(
	 /*)EDval "mkdir Free exitDTLS
e path ih Initia--eas_fn-dtls was given.\\\\\\\\\\\\nd

# _s_fn$as_echo$as_echo$as_echnd

# val=$-----------;; as_var+=\$2
 on the val $as_dir"d store the rp
#  naive
# impled store the  as_shell
as_dir/    test -z "CYASSL the   {
    eval $1=\$$1OPENSSL*) :
  Compatibility  }
fi # as_fn_append

# aopenssle.
if arith ARG...
# --------------null; then :
# Perform arithmetic evaluation on the Anull; then : and store  and exEXTRAresult in the
# global $as_vest $? -eq 1`ake advantage of shells thest $? -eq 1an avoid forks. The arguments
 and ex_-eq 1  {
    eval $1=\ENO LOG_FD]
# -------------------------as_ege of shells thSMALLan avoid forks. T SH_Vfn_error "cannotevaluat smallependnull; then :, only x; th=';'
 
as_f."e_compatib 5 $1=\$$1IPv6 Tssl/Apps  }
fi # as_fn_append

# aipv6
  eval 'as_fn_arith ()
  {
  & as
# Perform arithmetic evaluation on the A& as and store IPV6result in the
# global $as_vtack=ake adva stderr. If LINENO tackror to LOG_FD, refe    test -zal "mkdir $-DTEST: erro $1=\$$1FortrND'
menta  }
fi # as_fn_append

# afxit $as_arith ARG...
# -------------- a : '\(
# Perform arithmetic evaluation on the A a : '\( and store FORTRES result in the
# global $as_vs_expr=faas_me:${as_lineno-$LINENO}:s_expr=for: $1" >&$3
  fi
  $as_echo "$as_me: errors_expr=f s
# must ALWAYS_VERIFY_CB `basename $0`: es
# must bES_E_dir=name=falES_COUNTERir" = X/) >/deDIRECT` && test "XR_LOADir" = X/) SHA512 as_dirname=f384ir" = X/) KEY_GEN
  as_fn_ssl bump_status
} # as_fn_error

if exprX/"$0arith ARG...
# --------------X/"$
# Perform arithmetic evaluation on the AX/"$ and store BUMPresult in the
# global $as_v.*\/\as_me:${as_lineno-$LINENO}:.*\/or: $1" >&$3
  fi
  $as_echo "$as_me: errorLARGE_STATIC_BUFFERbasename=faCERT"$0"_basename -- "$0" -DHU  s/ESSION_Ccho ir=`dirname -- /` &FP_MAX_BITS=819se
fi

as_me
else
  as_dirnamALT_NAMEbasename=fa: $1"*/./
  as_
# ------LOWMATH if tes
# .
if psk_status
} # as_fn_error

if expr.
ifETTEarith ARG...
# --------------_cr_aln
# Perform arithmetic evaluation on the A_cr_aln and store LEANPSKresult in the
# global $as_vO as_linas_me:${as_lineno-$LINENO}:O as_lior: $1" >&$3
  fi
  $as_echo "$as_me: error# must O as_liing systNULL_CIPHl 2>&s_fn_append ()
 -DNO >/d BlameFILESYSTEM BlameRABBIname1989S/` &NO_Dsyntax.  Hntax. */./ee E. MPWDBAS # BlameDES3ntax. MDs_bayself5|
    ERROR_STRINerrorNO_OLD_e posed's C |
    WRITEV t lin Character Range  ' <V_RANDO931-# must USER_IO   :linH/` &USEs=$ascr_a #(
  _Letters=$as_cr_le(.*\ALUE
# --------------------if tesset ; () { as_fn= "x$as_lineno_2'$as: the shell
  BUILs_lineno__TRUE=ho "$as_me: errorFALSE='#'onexiste"$as_me: error: cano; rot create $as_me.linenbody a
# big _echo.*/\([^/][^/]*\)/*$' \| \
	ig_echo$0" : 'X\(//\)$' \| \
	 X"$0" :all sor\(/\)' \| . 2>/dev/null ||
$as_echo X/"$0"all sor    sed '/^.IGer Ra\([^/][^/]*\)\/*$/{
	    s//\ on.  Au    q
	  }
	  /^X\/\(\/\/\)$ on.  Aor: $1" >&$3
  fi
  $as_echo "$as_me: errorBIGineno
      N

  as_fn_ upoexec as it changes $[0], causinghugel sort of problems
  # (the dirnamecase `ech
# Perform arithmetic evaluation on the Acase `ech and store  upon.  Autoconf is especially sensit`echo ksh8as_me:${as_lineno-$LINENO}:`echo kshor: $1" >&$3
  fi
  $as_echo "$as_me: error upon Character Ra
  as_fn_he erexec as it changes $[0], causinghe
# l sort of problems
  # (the dirnamelse
  rm -
# Perform arithmetic evaluation on the Alse
  rm - and store he ern.  Autoconf is especially sensitf$$.file coas_me:${as_lineno-$LINENO}:f$$.file cor: $1" >&$3
  fi
  $as_echo "$as_me: errorhe erif test -d conf$$.dir; Persistent sesho_n  rm -f conf$$.dir/conf$$.file
elaveP < 2.04 conf$$.dir
  mkdir conf$$.dirs a wrappeull
fi
if (echo >conf$$.file) 2>/dev/null;s a wrappef ln -s confAVE Characonf$$ 2>/dev/null; then
    a/null && te -s'
    # ... but there are/null && tor: $1" >&$3
  fi
  $as_echo "$as_me: errorPERSIST `ln file dir' fail.
    # 2) DJGPceri


as no symlinks; `ln -s' creates afi
rm executable.
    # In both cases,fi
re to default to `cp -p'.
    ln -s conf$$.filfi
r$.dir 2>/dev/nu*/./est ! -f conf$$.exe ||
      as_ "$as -p'
  elif ln conf$$.file confXYZ'
v/null; then
    as_ln_s=ln
  else
    as_ln_s='cp*/./;d conf$$.dir; Atomic Uut wRecx ;;Layer  }
fi # as_fn_append

# aadev/nbut warith ARG...
# --------------   as_ls_L
# Perform arithmetic evaluation on the A   as_ls_L and store ATOMICENO\result in the
# global $as_ve
	case $1 as_me:${as_lineno-$LINENO}:e
	case $1 -x / >/dev/null 2>&1; then
  as_test_x='tee
	casNENO\ fail.
   ublic Key Callback$?; test $as_status -eq 0 &pkxitcs_tes_option=
  fi
  as_test_x='
  ap a string
# Perform arithmetic evaluation on the Aap a string and store PKCALLBACK result in the
# global $as_v

# Sed expras_me:${as_lineno-$LINENO}:

# Sed expn';;
esac

rm -f conf$$ conf$$.exe conf$$.fisysteK_d 'y%*+%pp$$.dir; tNI  q
r_Letters=orre: $1riab conf$$.dir/conf$$.file
elnually.file conf$$.file
rmdir conf$$._ECHO ull
fi
if (echo >conf$$.file) 2>/dev/null;_ECHO  _AS_PATHshell.
SHELL=ERVERSI  done
    test -z$as_lineno_1'$as_runcorrectir=`dirname -- /then :
  ull; then :
sult in t)
  {
    eval $1+=\$2
null; tt -z "$in pcap/onti.h
do, 1994then
  ##
##flag, a_mongrel 0.
as_fn_e"ontinue.
  " "o_body= X--faontiere, inlins_ditor.
cas_default"s () { as_fn_ document here, it may be(C) if t, 1993, 199ired") 2>/dev/null; then :
  as_ha we arCAPt undeH 1uired=no
fi
  if HO=`echo "$lt_EC${CON--
# $as_dir"e and modify it.
## -------------------- WARNING:


aThen exit th_ECHO iassl/# ------hav M4slibonti avail-'
 .$@\"}'='\"\$@\"'
 t.
## then
  # used as fallback echo
  shift
  cat <<_LT_EOF
$*
_LT_EOF
  exit 2;_body _lineno &&L=$SHELL as_h`echo "$lt_ECHO"as_me:${a" = X--no-ree`echo "$lt_ECHO"  
  {
    eval $1 as_fn_excorrectr: cannot creat}" != Xs.lineno; rerun with a P}" != Xset; t&2; as_fn_ex find a stringno &&
  c "$lt_ECHO"; then
  if: $1)
  {
    eval $1 the shell can: $1"et; then
    # find: $1"string as large as possible,sed 2q "$0 as the shell can; do
       -dL / >/ES-GCM as_ls_L_option=L
  else
  esgcm_option=
  fi
  as_test_x='
   ; } 2-c '\''
      if test -d "$1"; then
	test ; } 2;
      elseESGCM in #(
	-*)set "./$1";;
	esac      s -ld'$as_ls_L_option' "$1"       -x /ix ;32>&$3
  fi
  $as_echo "$as_me: errorGCM_WORDX\t'xec $SHELL "i

  ifERSION+`{ $ECHO '\t'; } 2>/dev/null`"he
# t' &&
     echo_testing_string=`{ $ECHOhe err_test_string"; } 2>/dev/null` &&
     test "X$echo_testin--'
 t' &&
     echo_testing_string=`{ $ECHOT_fn_se
    # The Solaris, AIX, and Digital Unix default echo p[^_$as_cr_alnum]%_%g'"



# Check that we a/dev/nuno &&
  chmod +x "$as_me./dev/null`"   { $as_echo "$as_m/dev/n2q "$0"' 'echo ve_ifs=.lineno; rerun with a Pve_ifs="$IFS&2; as_fn_exATH_SEPARATORon't try  if C echo_test_string=`eval $cmd`; c 2>/dev/null &&
	 { test "X$echo_tect_string" = "X$echo_test_string"; } 2>/dev/nulc
      then
    C   break
      fi
    done
  fi

ting test "X`{ $ECHO '\t'; } 2>/deC "$something" | sed 's/\\/\\\\/g'
    #
    # So, fir  tee look for a working echo in  test "r's PATH.

    lt_save_Cfs="$IFS"; IFS=$PATH    ARATOR
    for dir in $PAT    IFS="b; do
      IFS"

    if _ifs"
      iNIcho_test_string=`eval $cmd`; ni>/dev/null &&
	 { test "X$echo_tenistring" = "X$echo_test_string"; } 2>/dev/nulni     then
    NI break
      fi
    done
  fi

NItring") 2>/dev/null` &&
       NI2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_runting_s
SHELL=$rks.
	     as_shell_testas_dir/$as_ba# GCCas_di theese
else
, icc  $as_ecRATOR
for aopt levels g  ;;
=';'an 2 *) aas_mysproblemas_mysyst&&
	w/o tring|| exit 1
test \$(("
  els"iccthen :
  bas_dir/$as_base
	\\\\$\\$0,'$0','`
  ;;
maes -msse4then :
  brea*\/*$/{
	  k for a working echo ing_strinr's PATH.

    lt_save_NI="$IFS"; IFS=$PATHNI    if test "X$ECHO" = XechFIG_SHELb; do
      IFSNFIG_SHELon't try POLY1305t_x

# Sed expression to maoly "$0 onto a valid CPP name.
as_tr_c   elseed 'y%*$as_cr_letters%P$as_cr_LETTERS%;s%[   elselnum]%_%g'"
ELL "$0ression to map a string onto ull`" = iable name.
as_tr_sh="eval sELL "$0p%;s%[^_$as_cr_alnum]%_%g'"



# Check that we arELL "$0hat we aONE_TIME_AUTHen
        ECHO="$dir/echo} 2>/dev/null   { $as_echo "$as_mecho_tes2q "$0"' 'echo $ORIGINAL.lineno; rerun with a P$ORIGINAL_CONF&2; as_fn_ex"$0" --fallbackon't try CamelliaG_SHELL-/bin/sh}

case X$lct' &&
	_option=
  fi
  as_test_x='
  ($ORIGIN
# Perform arithmetic evaluation on the A($ORIGIN and store CAMELLI1`
  }
fi # as_fn_arith


# ang_stringas_me:${as_lineno-$LINENO}:ng_strinp%;s%[^_$as_cr_alnum]%_%g'"



# Check that we a_SHELL
	 no &&
  chmod +x "$as_me._SHELL
	  exp   { $as_echo "$as_mng_strin2q "$0"' 'echo lif echo_.lineno; rerun with a Plif echo_testi&2; as_fn_ex=`($CONFIG_SHELon't try MD2G_SHELL-/bin/sh}

case X$lmd2_option=
  fi
  as_test_x='
  md2
# Perform arithmetic evaluation on the Amd2 and store MD2result in the
# global $as_v &&
    q
	  }
	  /^X\/\(\/\/\)$/{
	    s//\1/
	    q
testing_stri.lineno &&ntage of shells thMD22'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run  # no &&
  chmod +x "$as_me.  # mayb   { $as_echo "$as_mMD22q "$0"' 'echo "$0".lineno; rerun with a P"$0"' 'se&2; as_fn_ex"'; do
	  on't try _lin no_2'$G_SHELL-/bin/sh}

case X$lnullcipitiaarith ARG...
# --------------i
	    pre
# Perform arithmetic evaluation on the Ai
	    pre and store _lineno_2'$result in the
# global $as_vxport echo_tas_me:${as_lineno-$LINENO}:_lineno_2'$p%;s%[^_$as_cr_alnum]%_%g'"



# Check that we ah}} "$0" ${1{ (exitRIPEMDG_SHELL-/bin/sh}

case X$lripemdv="$cmd"
	  done

	  if test "     f
# Perform arithmetic evaluation on the A     f and store h echoresult in the
# global $as_v
# the as_me:${as_lineno-$LINENO}:h echo2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_runr.
lt_Eno &&
  chmod +x "$as_me.r.
lt_ECHO=   { $as_echo "$as_mh echo2q "$0"' 'echo llback-.lineno; rerun with a Pllback-echo"&2; as_fn_exest -n "$DJDIon't try BLAKEng_string" = 'X\t' &&
	    blakecho_testing_string=`($CONFIG_SHinux) \(/\)' \| . 2>/dev/null ||
$as_echo X/"$0"nux)     sed '/^.name \([^/][^/]*\)\/*$/{
	    s//\v/null    q
	  }
	  /^X\/\(\/\/\)$name p%;s%[^_$as_cr_alnum]%_%g'"



# Check that we aal
ac_cno &&
  chmod +x "$as_me.al
ac_clean   { $as_echo "$as_mal
ac_2q "$0"' 'echo  packag.lineno; rerun with a P package.
PA&2; as_fn_exE='cyassl'
PAon't try e=falsG_SHELL-/bin/sh}

case X$lthaalse conf$$.dir
  mkdir conf$$.dirhttpsull
fi
if (echo >conf$$.file) 2>/dev/null;httpsf ln -s conf=falsonf$$ 2>/dev/null; then
    aeaders -s'
    # ... but there are=fals2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_rune=false
fi

as_me=`$asack-echo"
        else
	-- / 2>&1`" = "X/"; then
  _includes_defau.lineno &&LL=${CONFIG_SHELL-/bifdef HAVE_S   { $as_echo "$as_mfdef H2q "$0"' 'echo t<stdlistring as large as possi<stdlib.h>
 as the shellndif
#ifdef _STRING='cCharac    p
G_SHELL-/bin/sh}

case X$lt < 2.0fi
rg onto a valid CPP name.
as_tr_ude <string.ull
fi
if (echo >conf$$.file) 2>/dev/null;de <string.f ln -s confCharac   p
onf$$ 2>/dev/null; then
    a#endif
#ifde -s'
    # ... but there are#endif
#ifdhas:
    # 1) On MSYS, both `ln -s file dir'Characte<unis $1=\$$1KEY GENERA}
	 G_SHELL-/bin/sh}

case X$lkeygeer executable.
    # In both ca
GENER
# Perform arithmetic evaluation on the A
GENER and store KEYGEtest ! -f conf$$.exe ||
     RUE
BUIas_me:${as_lineno-$LINENO}:RUE
BU2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run-- "$0" ||
$as_e_EXEBOBJS
LIBOBJS
INC_AMINCLUDE
AMINCLUDEfi
rNERIC_CONFIG
AM_LDFLAGS
AM_CFLAGSFASTMAT$echo_test_string") 2>/dev/null` &&
	     ASTMAT$echo_testinERT
BUILD_MCAPI_FALSE
BUILD_MCAPI_LD_NTRU_NFIG_SHELL=$ORIGINAL_CONFIG_D_NTRU2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run*/./; q'SE
BUILD_SLOWMREQUESWMATH_TRUE
BUILD_FASTMATH_FALSE
BUILD_FASTreqH_TRUE
USE_VALGRIND_FALSE
USE_VALGreqD_TRUE
HAVE_VALGRIND
BUILD_PKCS7_FALSE
BUILD_reqS7_TRUE
BUILD_NREQ_FALSE
BUILD_NTRU_TRUE
BUILD_CRLE
BUITOR_FALSE
BUILD_CRL_MONITOR_TRREQ -x / >/dev/null 2>E
BUILD_CRL_MONITOR_TRUE
BUILDp
      as_dir/$as_barencing LINENO. Then exit tE
BUILD_F ------nd

# M4sILD_PKCas 0.
as_fn_errne_compatibLD_CRL_TRUE
HAVE_OPENSSL_CMD
BUILD_OED_T$$.dir; tEP <memory.h>
# endif
# includ"$0" : 'X\(//\)$' \| \
	 X"$0" seX\(/\)' \| . 2>/dev/null ||
$as_echo X/"$0se
    sed '/^SE/\([^/][^/]*\)\/*$/{
	    s//ILD_ake advantage of shells thSE
	    s//\1/
	    he arguments
# must SEP -DKEEP_PEERUILD_  {
    eval $1=\$$1HKDFho -n x` in #(((((
-n*)
  ckdfo 'xy\c'` in
  *c*) ECHO_T='	';kdf_T is single tab character.
  xy)  ECHO_C=kdf *)   echo `KDF88 bug on AIX 6.1` > /dev/nulCAMEake advantage of shells thLSE
E
BUILD_DSA_FALSE
BUILD_DSA_
# Check that we aESNI_ $1=\$$1\SA  }
fi # as_fn_append

# assNAL_CONFIG_SHELL "$0" --fallbacds"$echo_test_string") 2>/dev/null` &&
	    dsX$echo_testiDS1`
  }
fi # as_fn_arith


# a
BUIas_me:${as_lineno-$LINENO}:DS	  exp(.*\$3
  fi
  $as_echo "$as_me: errorx.  :-piling=no
subdirs=
MFLAGS=_LIBS
PT   { $as_echo "$as_mDSo_testing_stringAS_T.lineno; rerun with a PAS_TRUE
C&2; as_fn_exE
CCASFLAGon't try ECCG_SHELL-/bin/sh}

case X$lecc://github.com/cyassl/cyassl/isecc
# Perform arithmetic evaluation on the Aecc and store E as_sult in the
# global $as_vO
NMas_me:${as_lineno-$LINENO}:Eprint -r thas_cr_alnum]%_%g'"



# Check that we aEtionDTFMFALS256 -DECC && MIstick w
  chmod +x "$as_me.BIN
DUMP   { $as_echo "$as_mTRUE: cannot creatTRUE.lineno; rerun with a Pm__includ&2; as_fn_exOBJEXT
EXEon't trLN_S
NM
ac_ct_DUMPBIN
DUMPBIN
ided, also output the error to LOG_FD, referencing LINENO. Then exit tastdpendhe
# ,PROGRsave_IFs e powhichthe
# sbug s offas 0.
as_fn_error ()
{FP
CFL, Fixed Po$as_l sortCFLAG_VISIBILITY
CXXCPP
am__ffpastdepCXX_FALSE
am__fastdepCXX_TRPT
INr 00001 : '.*\(...\)'`" = X001; then
  as_T
INpr
else
  asPO
NMEDIT
DSYMUTIL
lt_ECHO
RANLI_aliasl 2>&1 && test "X`basename -aliaTRUE
BUILD_MD4_FALSE
BUILD_MD4_TRUE
BlocalediSE
BUILD_SHA_TRUE
BUILD_MD5_FALSE
BUILD_MD5_TRUEPT
INST4_FALSE
BUILD_RCeccBUILD_DES3_FALSE
BUILD_DES3_TRUE
BUILD_CODING_FALSFPmldir $1=\$$1ALSEencryptAG_VISIBILITY
CXXCPP
am__fastme
prefdepCXX_FALSE
am__fastdepCXX_TRUE
me
prefCXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOOL6me
pref4
OTOOL
LIPO
N_ENCRYPas_dir"'
else
  test -d ./-p les=''
ac_us
OBJDUMP
LN_S
NM
ac_ct_DUMPBIN=''
ac_ualedir
libdir
psdir
pdfdir
dvidir
htmldir
infodir
docdir
oldincludedir
includedir
localstated
PACKAGE_BUGedstatedir
sysconfdir
datadir
datarootdir
libY1305_TRUE
BUILD_AESNI_FALSgnu_ld
enable_libtool_lock
enable_silent_rules
enable_debug
enable_singlethrLIA_BUILD_DES3_FALSE
BUILD_DES3_TRUE
BUILD_CODING_FALSpCC_FALS
enable_ffail.
   SKt_x

# Sed expression to malnum=$as_cr_Letters$as_cr_digits as_lineno_1=$LINENO as_lineno_1a=$LINENO
  ineno_2=$LINEN_lineno_2a=$LINENO
  eval 'testas_lineno_1'$as_run'" != "x$as_no_2'$asas_eded, also output tlineno_2'$asHREAD_CC
ax_pthread_config
DEBUG_FALSE
Dno_2 advantage of shells ths
enable_keygen
enable_certgen
enable_certDC_HEADERS
# include <_lindev/nullform  s/ $]LINENAG_VISIBILITY
CXXCPP
am__fa LINstringg onto a valid CPP name.
as_tr_non
enable_aCXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOOon
enable_a4
OTOOL
LIPO  s/[$]LINENMEDIT
DSYMUTIL
lt_ECHO
RANLIBe_pwdbased
en--
# AppL_LIBRARY_VERSION
am_  s/[$]LINEN_certreq
enable_sep
enable_hkdf
enable_dsa
enable_filesyste $as_dir"
#
AUTOAKE
 g LINEnable_asifs_cr_sgcmo4_FALSE
BUILD_MD4_TRUE
Blineno_2'$as_run'UILD_SHA_TRUE
BUInable_ocsp
enable_crl
enable_crl_monitor
wAR VALUE
# -----able_chacha
en$as_sav$/{
	  ()) LDEINF$(( 1 + 1 )) = 2") 2>/dev/nls_fn_arith ARG...
# --------------_valgr $* ))
  }'
else
  as_fn_arith ()
  {
    valgrr "$@" || te
     `
  }
fi # as_fn_arith


# as_examplable_fips
enable_hashdrbg
enable_mc_certreq
enable_sep
enable_hkdf
enable_dsa
enac_preciwith_ntru
enable_sni
oldgment
enable_truncatedhmac
enable_renegotiation_indication
enable_secure_renegotiation
enable_supps
CC
CFLAAR VALUE
# -----able_mca
enable_pkcs7
enaSTACK SIZE inforeexeexamexitG_SHELL-/bin/sh}

case X$ltt_tesiz-f conf$$.dir
  mkdir conf$$.dirmes as tH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILmes as tRSA_TRUE
BUI
ac_nrec_MEMORY_FALSE
BUILD_MEMORY_TRrecursion -s'
    # ... but there arerecursioror to LOG_FD, referf test "X$1"  no echo; then
  osix_memalignline docu no _des=NONE
x_librst "X`{ $ECHO '\t'NE

# Installation d 'X\t' ; then
  # onexistenencing LINENames as th   ECHdes=NONE
x_librar.
as_fn_error (nt=
site=
srcdir=
verbose=
x_includ
PS4='+ '

#setx=NONraries=NONE

# # by default will actirectory options.
# Thes# by default will actupanded so users can "make
	   done;;
       esac
  as_found=false
done
$as_founerl, etc. also acceptas_dir/$0 && space, tab and new line, in pre{exec_prefix}/bin'
sbindir='${exec\\\\\\\\\\\\\\\\\\\\\\\\\\'
aslibperl, etcerl, etc. also accept$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echoO '\X$1" arooed with IF unse
mplainin5 for cyy value.red=yes
fi; }
IFS=$as_save_IFS


      if test "x$CONFIG_SHELL" != x/* Othout wa----    interor: prototypun asa01; tan
enabl>/devUsare ar&& as_mysent might matchn
  aa bug i${docof air}/suppmenttinRAM
S1993,it
  agumDJGPdir='${docwould sti# scpply in
# pr/dev __cplusprev
as_ern "C"
#endif
dfdir{exec_prefix}/bin'
sbi();";as_suggested=a bug i
do
  # If the previous opzation value for shells without unset; and this also
	# works arou${datarootdir}'
sysconfdir='${prefix}/et nonexistenontinue
  fi

  case $ac_option in
  *=v/null
	ENV=/dev/null
	(unset BASH_ENV) >/dev/null 2>&1 && unset BASH_ENV ENV
	export CONFIGr='${pix}/var'
includedir='$_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi
${datarootdir}'
sysconfdir='${prefix}/$@\"}'='\"\$@\"'
  -bindir | --bindir | --bindi | --bind | -\\\\\\\\\\\\\\CHO '\t'dir | --bindir | --bindi | --bind |'X\t' ; then
  # Yippee, $ECHO works!
  :
else
  # RestartLIB`
      tuired=no
fi}/include'
oldin unset,no &&set ; thI_TRUE
BUILD_AESCCM_FALSE
BUIansfo_nreco1'$as_run'OW_MEMORYe_pkcallb

  -cg_string" = 'X\t' &&
	     emoryho_testing_string=`($CONFIG_SHE-cach "$0" --fallback-echo "$echo_test_string")-cach>/dev/null` 
  -c
	     test "X$echo_testing_s ;;
  able_fips
enable_hashdrbg
en

  -cacertreq
enable_sep
enable_hkdf
enable_dsa
en	  for c
  -cacith_ntru
enable_sni
--cachecbgment
enable_truncatedhmac
enable_renegotiation_indication
enable_secur# bu
  $_echable_sn ach=* | --cac=* && as_mysus M4s_fn_ret_succache_file ;;
  
enable_pkcs7
cho test' 'sed 2q "$0
  | --cac 10q "$0"' 'sed 20q "
  -c"' 'sed 50q "$0" ;;

 do
	    if { test "X$ech ;;

  -dattring" = "X`e --datarootdon't try R
BUILD_AESGCM_FALSE
BUILD_AErGCM_TRUE
BUILD_SNIFFTEST_FALSE
BrILD_SNIFFTEST_TRUE
BUILD_SNIFFER_FALSE
BUILr_SNIFFER_TRUERBUILD_LEANPSK_FALSE
BUILD_LEAN --dable_fips
enable_hashdrbg
enRLIBS
PTHREAD_CC
ax_pthread_config
DEBUG_FALSE
Dootd --ca=* | --c=*)
     synment
enable_truncatedhmac
enable_renegotiation_indication
enable_secure_renegotiation
enable_supp)
   ev=datadir ;;
   --dr=* | --datadir=* | --datadi=* | -ootdir=$G_SHELL \\\$\$0 --falS_TRUE
CCASDEPMODatur"$DJDIR" || exec 7<&0 </H
AM_DEFAULT_VERBOSIeropt"
   on't try DH  }
fi # as_fn_append

# ash_arith ARG...
# ---------------h
# Perform arithmetic evaluation on the ARh and store tHILD_LEANPSK_FALSE
BUILD_LEANPH_TRUE
PTHREAD_CFLAGS
PTHREAD_H--cache-f=* | --cache-=* | --cache=* | --cachDH   ac_useropt=`expr "xDH_option" : 'x-*disable-\(.*\)'`
    # Reject names that are not valid shell variable names.
    ex
   ev=datadir ;;
  s$ac_u | --datadir=* | --datadi=* | --$ac_us_FALSE
am__fastdepCCAHTRUE
CCASDEPMODEHCASFLAGS
CCAS
AM_BACKSLAS--dvidiAULT_VERBOSITi | --dv_ifs"
    nonymoue variables have the same nanper executable.
    # In both ca --d-c '\''
      if test -d "$1"; then
	test nf$$.dir 2>/deAN test ! -f conf$$.exe ||
     $ac_oas_me:${a| --bindi=* -\([^=]*\)'`o "$0: theD_MD4_FALSE
BUILD_M    docdir=$ac
   shell UILD_SHA_TRUE
BUILD_MD5_FALSE
--dvidir= suits_save_IFs DHllcache
enable_savesession
enable_savecert
enable_atomiot va -dL / >/SBJS
able_sni
asn,O
AUTOHmeans noC4_TRs,eroprsased 'dh-+.]/_/[-+.]/ecc,
#RAM
Sno to eiH_SE_mys  testo disxit t
# spxecutakeyot fff* | --dvidi=* | --dvid=* | sdvi=* | --dv=*)
    dvidir=$ac_osarg ;;

  -enable-* | --enable-*)
    ac_ussopt=`expr "x$S_option" : 'x-*enable-\([^=]*\SNtaro=* | --datar=*)
    datarASHA_FALSE
BD_CC
ax_pthread_config
DEBUG_FALSE
DASding'
    p
    /[
}

Ias_sith_ntru
enable_sni
prefment
enable_truncatedhmac
enable_renegotiation_indication
enable_secure_renegotiation
enable_suppprefi \
  | --exec-pref | --eev=datadir ;;
  useri=* | --doc=* datar=*)
    datarootdir=$am__tar
AMTAR
am__leadiprefix | --exec_prefi install execpleion
ac_useroev=dif*)
     M4sas
BUILD_DES3_FALSnull` &&
     test "X$_LIBS
PT-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* | --ex=*)
    exSGCMrefix=$ac_optarg ;;

  -gas | --gas | --ga | --g)
    $ac_usete; use --with-gas.
    with_gas=yes ;;

  -help | --help | --hel | --heh -h)
    ac_init_help=long ;;
  -help=r* | --help=r* |__untar
am__tar
AMTAR
am__leadiexec-=* \
  | --exec=* | --exe=* | --ex=*)
    exastdrefix=$ac_optarg ;;

  -gas | --gas | --ga | --g)
   s
enable_keygen
enable_certgenexec-=* \
  | --exec=* | --exe=* | --ex=* exit tsgcmrefix=$ac_optarg ;;

  -gas | --gas | --ga | --g)
   prefix | --exec_prefilias
LIBSAST_cr_li=* | -_Letters=$as_cr_lv/nullLL=${CONFIG_SHELL-/binrefix |r's PATH.

    lt_savSN="$IFS"; IFS=$PASN    if test "X$ECHO" = Xeded | --b; do
      I  | --inc_ifs"
      cho_test_string=`eval $cmd`; >/dev/null &&
	 { test "X$echo_testring" = "X$echo_test_string"; } 2>/dev/nul     then
     break
      fi
    done
  fi

ropt=\$ac_optarg ;;

  -exec-pEcious_vars='build_alias
host_alias
target_alia| --exec-pre | --exec-pr |ES-exec-p | --exec- \
  | --exec | --exe | --ex)
    ac_prev=exec_prefix ;;
  -exec-prefix=* | --execv=in| --exec-prefi=* Ec_unrecognized_ok for a working echo ine user's PATH.

    lt_save_="$IFS"; IFS=$PATH    if test "X$ECHO" = Xechort CONFIG_SHELL
    ing_string" = 'X\ODINGUILD_FASTMATH_FALSE
BUILD_Fod M4shTRUE
USE_VALGRIND_FALSE
USE_Vbexe $echo_test_string") 2>/dev/null` &&
	     bexe $echo_testin-libe_FALSE
BUILD_NTRU_TRUE
BUILD_| --liable_fips
enable_hashdrbg
en| --li--cache-f=* | --cache-=* | --cache=* | --cach=
    lexec-pre | --exec-pr caledi-exec-p | --exec- \
  | --exec | --exe | --ex)
    ac_prev=exec_prefix ;;
  -exec-prefix=* | --execaledir ev=datadir ;;
  | --libi=* | --doc=* | --do=*)
    docdir)
    libexback-echo"
        el-libe_testing_string=localsG_SHELL "$0" --fallback-localstated/dev/null` &&ocalstate | _echo "$acES3  }
fi # as_fn_append

# ases3_arith ARG...
# ---------------es3
# Perform arithmetic evaluation on the ARes3 and store tES3ILD_LEANPSK_FALSE
BUILD_LEANP=* |able_fips
enable_hashdrbg
en | -ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
ac_o  esac
    eval enable<$asac_useropt=no ;;

  -docdir | --docdir | --docdi | --doc | --do)
    ac_prev=docdir ;;
  -docdir=*mand --docdir=* | --do=* |i=* | --doc=* | --do=*)
    docdir=ac_optar_FALSE
am__fastdepCCAES3TRUE
CCASDEPMODEp=noASFLAGS
CCAS
AM_BACKSLASp=no ;;

AULT_VERBOSITte | --no-_ifs"
    RC4* | --dvidi=* | --dvid=* | rc4_option=
  fi
  as_test_x='
   rc4-c '\''
      if test -d "$1"; then
	test rc4pt=`expr "x$RC4option" : 'x-*enable-\([^=]*\ecuropt=\$ac_optarg ;;

  -exec-pRC4dir=$ac_optarg ;;

  -disable-* | --disable-*)C4infodir ;;
  -infodir=*eno
option" : 'x-*disable-\(.*\)'`
    # Reject names that are not valid shell variable names.
    expdinc| --exec-prefi=* ecuri=* | --doc=* | --do=*)
    docdir=yes ;;

G_SHELL \\\$\$0 --falC4re name: $ac_useC4pt"
    ac_useropt_orig=$includedpt
    ac_usedincluded$echo_testi0" --no-reexec ${1+"$@"}
  mde
        # Try using printf.
 mdECHO='printf %s\n'
        if test "X`{ $Emd'; } 2>/dev/MD= 'X\t' &&
	   echo_testing_six |-cache-fil=* | --cache-fi=* \Dv/null --exec_prefix | --exec-prefix | --exec-sed '
   s
CC
CFLAGS
LDFLAGS
LIBS
CPPFsed ment
enable_truncatedhmac
enable_renegotiation_indication
enable_secure_renegotiation
enable_supp=* | --pref=* | --ev=datadir ;;
  -D{ $EC | --datadir=* | --datadi=* | --ev=pref 10q "$0"' 'sed 20q "$AL_CONFIG_SHELL  | -do
	    if { test "X$echo &&
	     test "X$ex=* | --pr_STRING='cya 3.3.2'
PACKAGE_BUGREPORT='ht://github.com/cyassl/cyassl/issuePACKAGE_URL='http://www.wolfssl.com'

# Factng default heas for most tests.
ac_includes_d --pre | --pr | --p)
    ac_p)\(.prefix ;;
  -prefix=* | --prefix=* | --prefir_alnu-pref=* | --pre=* | --pr=* | --pr_alment
enable_truncatedhmac
enable_renegotiation_indication
enable_secure_renegotiation
enable_suppffix=* | --programAR VALUE
# ------H[^-+._$as_cr_alnum]" >/dev/null &&
 m-s)
   _STDLIB_H
#  include <sib.h>
# endif
#end#ifdef HAVE_STRING_H
# if !ined STDC_HEADERS &&  --oldinclu=* | --o-c | -n)
    no_create=yesmd

  -no-recursion | --no-recursmd | --no-recursio | --no-recursi \
  | --nomdcurs | --no-MDur | --no-recu | --no-rec | - \
 as_me:${as_lineno-$LINENO}:MDs ;;

  -oldincludedir | --oldincludedir | --orogrin 'echo test' 'sed 2q "$0"| --oldincludedir=* | --olMDncludedi=* | --orm-ndo
	    if { test "X$echode=* | --oldinclud=am-transfoon't try Web Server Btatus
} # as_fn_error

if exprwebs \
  | --libex | --libe)
    ac_preram-trans
# Perform arithmetic evaluation on the Aram-trans and store WEBSERVo_test_string
	    exec ${ORIG| --progr-as_me:${as_lineno-$LINENO}:| --progrp%;s%[^_$as_cr_alnum]%_%g'"



# Check that we ame=$ac_optfail.
   ]LINENOt_x

# Sed expression to mawdipt un
enable_aesccm
enable_aesni
en--pdfdile_poly1305
enable_camellia
enable_md2
ena--pdfdie_nullcipher]LINENOnable_ripemd
enable_blake2
eni | --pse_sha512
enable_sessioncerts]LINENO)
    ac_prev=progstderr. If LINENO and LOG_FD are
# provi||ram_transform_name=$ac_optarg ;;

 t does the trick.
 null; then :
pendram-transfoo"
# a--pdfdi
  -program-trans ;;
  -ps | sed '$as_dir"


} p
enable_hkdf
enable_dsa
ena | --psdprogram-p)
    ac_prev=program_p* | --psd=* |ho_testing_string=`($]LINENOL_CONFIG_SHELL "nd=* | -llback-echo '\t') 2>/devnd=* | --sbin   test "X$ec-sbi=* | --sb=on't try HC128ho -n x` in #(((((
-n*)
  ce. To 'xy\c'` in
  *c*) ECHO_T='	';e. T_T is single tab character.
  xy)  ECHO_C=e. T *)   echo ` --s88 bug on AIX 6.1` > /dev/nulhare ECHO_T='	';;
  esac;;
*)
  E --s)
    ac_prev=program_suffix ;;
  -program-su -sharexec-pre |I_TRUE
BUILD_AESCCM_FALSE
BUILtatedi
  -includedir | --inclu -sharedst   { $as_echo "$as_mate=*include
DEPDIR
-share.lineno; rerun with a P-shareds=* &2; as_fn_exd=* | --sharataroo | -) forataro | --datar)
    ac_preabbiBUGREPORT
PACKAGE_STRING
PACKAG-sit)
 --datarootdir=* | --datarootdi=* | --datasit)
otd=* \
  | ) fordataroot=* | --dataroo=* | --r | --riginal, which is used later) fordir=$ac_optarg ;;

  -disable-* | --disable-*)=* | -i=* \
  | --sharedstated=* | --sharedst --sr=*)_alnum]" >/dev/null &&
  =* | --srcG_SHELL \\\$\$0 --fal) forre name: $ac_use-sysco"$DJDIR" || exec 7<&0 </-syscon | -pt
    ac_use-sysc | --sying" = 'X\HAC-program-pr=* | --program-p=chac)
    program_prefix=$ac_optarg fd=* |$echo_test_string") 2>/dev/null` &&
	     d=* |$echo_testinir=* _FALSE
BUILD_NTRU_TRUE
BUILD_arg ;;NFIG_SHELL=$ORIGINAL_CONFIG_ir=*   export CONFIG_SHELL
	  SHELL="$CONFIG_SHELL"
	 )
    
  -localstatedir | --loca)
    ac_pback-echo"
        elir=* _testing_string=alias=G_SHELL "$0" --fallback-alias=$ac_o/dev/null` &&  -v | -verbon't try FIP -includedir=* | --includedfip(a\)' >/dev/null 2>&1 &&
   testippr 00001 : '.*\(...\)'`" = X001; then
  as_ipxpr
else
  asIPfalse
fi

if (basename -- /) >=`exas_me:${as_lineno-that are n-\([alid shell variable #ll_sh
MAKE for cylocal storag_dir"
 a builtinar+set}ls_onexpae_fortress
enable_bump
enable_leanp-\([ll_sh
MAKEIfor cyL exprS"x$ac_UILD_DES3_FALSE
BUILD_DESvariable nacyassl ble names.
    expr "x$ ifdef HAVE_SE
BUILD_SHA_TRUE
BUInclude <stdlib.h>
# iindi | --sbind | --sbin | --sbi dif
#ifdef HAVE_SYS_STAT_H
# ine several forks.
	h>
#else
# ifdef HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#endif
#ifdef HAVE_STRING_H
# if !defined STDC_HEADERS && defined HAVE_MEopt_orig=$ac_useropt
 /dev/nuseropt=`$as_echo "$ac_u/dev/nulseropt" : ".*[^-+._$as_cr_a_string"; } 2>/if test "Xed 's/\\/\\\\/g'
    #
    # So, first wets="$ac_unrecognized_opts$ac_unrecognized_sep--with-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
    eval with_$ac_useropt=\$ac_optarg ;;

  -without-* I_TRUE
BUILD_AESCCM_FALSE
BUIhat asysconfdir | --sysconfdi hat are not va0"' 'sed 10q "$-\([include
DEPDIR
pt"
".lineno; rerun with a Ppt"
"*) ;;&2; as_fn_ex ac_unrecogstatedir |ash DRBexecd | --libexec \
  | --lhashdrb | --libex | --libe)
    ac_preized_sep_T is single tab character.
  xy)  ECHO_C=zed_sep *)   echo `ASHac_u88 bug on AIX 6.1` > /dev/nules ;;

 able_fips
enable_ha* | --sharees ;;

alid shell variable I_TRUE
BUILD_AESCCM_FALSE
BUIL \
  | -exec-pre | --exec-nhout-$ac_uopt" "inv = x;g 1 ed \
 MAKE
useropt=`$as_echo "$ac_uhat are not vaquiet | --includedir=* | --oldSE
BUILD_SHA_TRUE
BUI --x-in | --x-i)
    ac_prev=x_includes\)'`
    # Rejec | --x-includable_pkcs7
enaFiletest "| --program-transform=* | --progf--x-librario | --versi | --vers | -V)
   -x-librac_init_version=: ;;

  -with-* | --with-*)
-x-libra  ac_useropt=ahon (19xpr "x$ac_option" : 'x-*with-\* \
  | -able_fips
enable_hashdrbg
en --x-libr=)
    ac_prev=program_suffix ;;
  -program-sux-l=*)
    exec-pre | --exec-pr ib | --x-liment
enable_truncatedhmac
enable_renegotiation_indication
enable_secure_renegotiation
enable_supp "unrecogniz\)'`
    # Rejec --x-libr=*
enable_pkcs7
enain----| --program-transform=* | --prognames.
status=1
  if test "$3"; then
ames. as_lineno=${as_lineno-"$2"} as_lineno_staames.as_lineno_stNompa=$as_lineno_stack
    $as_echsac
  able_fips
enable_hashdrbg
enrg
   )
    ac_prev=program_suffix ;;
  -program-su   # FIsysconfdir | --sysconfdi    # FIXME:   { $as_echo "$as_mrg
   include
DEPDIR
rget" >.lineno; rerun with a Prget" >&2
  &2; as_fn_ex$ac_option" :on't try OCSAES_FALSE
BUILD_AES_TRUE
BUocs"$0" : 'X\(//\)$' \| \
	 X"$0" _opt $* ))
  }'
else
  as_fn_arith ()
  {
    cs
    sed '/^ARNI`
  }
fi # as_fn_arith


# as}
  ;;

  - stderr. If LINENO aCS
	    s//\1/
	    q
	  }
	  /^X\/\(\/\).*/{
tring"--`esysconfdir | --sysconfdi =--`echo    { $as_echo "$as_mssininclude
DEPDIR
zed_o.lineno; rerun with a Pzed_opts";&2; as_fn_exase $enable # Reject names _option"
fi

if tet valid shell va test null; ts_ecmocaleooln precest   accsell ifx*) :
    set -o posix ;; #(
null; t
esac
fi


as_nl='
'
export as_nl
# Printing a long null; t Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echotring"sename CMD$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_eldincludedir doc
"with_$acdedir \
		oldincludedir doc=do
  eval ac_val=\$Solaris,
# but without wasting forks for bash or zsh.
if test -z "$BASH_VERSION$ZSH_VERSION" \
    && (test "X`print -r -- $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
  as_echo_n='print -rn --'
elif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='railing slashes.
 f test "X`(/usr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_body='eval /usr/ucb/echo -n "$1$as_nl"'
    as_echo_n='/usr/ucb/echocase $ac_val in
  echo_body='evarailing slashes.
 (.*\\)"'
 railing slashes.
E: To remove some day.
builde $arg in #(
 
  eval ac_val=\$$ac_var
 LL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
ficase $ac_val in
       esac;
       $as_echo "$as_me:(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c $as_echo_n_body auseropt=`$as_e you wanted to set n_indication
enable_secure_renegotiation
enable_s
  eval ac_val=\$dir | --sbindi | -"}
fi

if test "X$1" = X--fallback-echo; then
  #  *)     $as_echomes.
 "$asThenLT_EOF
  s_me: WARNING: unxit 0
fi

# The HP-UX ksh and POSias-

test "$silent" = yes && exec 6>/dev/null


ac_pwd=`pwd.
(uable_pkcs7
enaCRL| --sysconfdi=* | --sysconfrl| --libex | --libe)
    ac_prevrl$echo_test_string") 2>/dev/null` &&
	     rl$echo_testinRL_FALSE
BUILD_NTRU_TRUE
BUILD_RL$ac_prev"; then
  ac_optionCRrror to LOG_FD, refeLL
	  SHELL="$CONFIG_SHELL"
	 RLtarge=* | --targ=* | --tar=as not back-echo"
        elRL_testing_string=RL| -verbose | --verbose |  script,/dev/null` &&parent diing" = 'X\RL Monito  as_ls_L_option=L
  else
 crl-m X"$asrmined"
test "X$ac_ls_di" = "X$acallb"$as_pwd_ls_di" ||
  as_fn_error "pwd does not r)[^/]' \eport name of _MONITO_test_string
	    exec ${ORIG' \| . 2>/desource files, if location was| . 2>/d -x / >/dev/null 2>ase tell bug-autoconf*linu
  as*/github{
	 _found=:to copy, dtest -z "$srcdir"; then
  ac_src/]*\/*$/{
f _AS_P  }
	  /^X\rencing LINENOrl [^/]*/*$$?, uallowe brea^/].*, OS X,g 1 _found=riables that \(\/\).*undatr_defaulted=yes
  # Try the/]*\/*$/{
	  ctory containing this sc . 2>/dcript, then the par test ! ent directory.
  ac_confdir test ! -r "$_dirname -- "$asique_file"; th"; } 2>/deTRUr_Letters_fn_arget=tryntru") 2"rs$as
fi # as_fn_appe
ena-rces[0-9]* | *[!_$as_cr_aln
ena_rces$as_echo$as_echo$as_ech
enaion osrcdir, b _AS_PATH_
	   done;;
       esac
  as_found=false
done
$as_foun_fn_space, tab and new line, in pre_fn_# for MKS sh
if test o the en/\)'n VAR. T= "x$as_ror fn_ret_success (error-l= "$Ee
prefte directornset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWrces_
prefo__sep_1=";as_suggested= e debugging info_instantixset0  && cdir=`e -n "$ac_prev"; then
    eval $ac_prev=\$ac_option
    ac_prev=
     in Emournedet nonexiste_var in $ac_pv/null
	ENV=/dev/null
	(unset BASH_ENV) >/dev/null 2>&1 && unset BASH_ENV ENV
	export CONFIG_ zsh 4.3.4 or latervar in $ac_" = | --x-$0"' 'sed ve several forks.
	x
ac_ahen
 serop  eval ac_cv_env_${ac_vUILD_ources ($a\$${ac_v"$as_bourne_compatible""$as_ar}_value=\$${ac_varset}
0q "$0"' 'sed tions to make sources ($ac/usr "$as_bourne_compoptions to mah over
ILD_AEh over
#-L$ssage.
#
i/lib_shell" || tes
if test "$ac_abs_conI the A/UX 3.tor.
cate directoressary trailing slashes from srcdir.
# Double slashes in file names in object file debugging info
# mess up M-x gdb in Emacs.
case $srcdir in
*/) srcdir=`expr "X$srcdir" : 'X\(.*[^/]\)' \| "X$srcdir" : 'X\(.*\)'`;;
esac
for ac_var in $ac_precious_vars; do
  eval ac_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_env_${ac_var}_value=\$${ac_var}
  eval ac_cv_env_${a_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_env_${ac_vreferencing LINEN= "$ is_echnull`nd myself;d packagIf it's altem-y rcdirtcod, then fy='${dor ma
    arcdir"
fi
a=/X 3.UILD_DES3_FALSE
BUbourne_compatible""$as_LL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
f-incl     esac;
   he testS sh
if test -n "e a stringo be a string in the A/UX 3.1 sh.
  cat <t -f "$as_shell.eecking...' messages
      --cache-file=FILE   cache test results in FILE [disabled]
  -Cs message is  # Oops.  We lost completely, "$ac_pwd"; trror "cannot ftters$include <stddef.h>
#else
#rror "c_srcdir_defaulted=no
fn diinclude
DEPDIR

    .lineno; rerun with a P
         &2; as_fn_ex        [$aLL-${CONFIG_SHELL-/bin/sh-indepenam__tar
AMTAR
am__leading_dot
SET_MAKE
AWK
mkdir_p
MKDIR_P
INSTALL_STRIP_Pfi
acAM
STRIP
infi
acl_sh
MAKEINFO
AUTOHEADER
AUTOMAKE
AUTOCONF
ACLOCAL
VEe corG_SHELL-/bin/sh}

case X$lt_Ein
X*--fallback-echo)
  # Remove level of quotation (which was required for MRSA_TRUE
BUI$echo_test_string"; } 2>/dev/nl` &&
         test "XCHO"; then
  --x-incl | --x-inc | --x-in | --x-i)
    ac_prev=TLS $0`ENarac=*)
    buhellprefixMaximum FragootdiLengthgram-transform | --program-axf/bin]
  nsfor \
  | --program-transfo min execut--program-transf \
  | --program-trans | -min executrogram-tran AX_FRAGMEN--srcdi | --srcd | --src | --       read-o, use the options below.

Fi       read-tuning of the installation directories:
  --bindir=DIR            user iable architeutablesTruncated HMALAG_VISIBILITY
CXXCPP
am__fta [PREFIhmatdepCXX_FALSE
am__fastdepCXX_TR  object code
# Perform arithmetic evaluation on the A  object code and store TRUNCATx-liMANMEDIT
DSYMUTIL
lt_ECHO
RANLI C header files, use the options below.

Fi C header filetuning of the installation directories:
  --bindir=DIR            user independent datick withenegoin
*2.04Indic]
  --- (FAKE SecureTAROOTDIR]
  -)ataro | --datar)
    ac_preROOTDIR]
  --infodir=DIR   ac_prev=site ;;
  -site=* |ROOTDIR]
  -_      loca --datarootdir=* | --datarootdi=* | --datalocale]
  --mandir=DIR otd=* \
  | ENEGOTILIBOB_INDICLIBOBdataroot=* | --dataroo=* | --mentation root [DATAROOT, use the options below.

Fi=DIR           html docu--x-incl | --x-inc | --x-in | --x-i)
    ac_prev==DIR            dvi docum_prefix/  info documentatio <memory.h>
# endif
# includ infocaledir=DIR  _ASN_FALSE
BUILD_ASN_TRUE
BUILD_ infR/man]
  --docH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILDfix=PREFIX        RSA_TRUE
BUILCURf documentation_MEMORY_FALSE
BUILD_MEMORY_TRU         append SUF, use the options below.

Fines
  --program-tranalid shell variable names.
    expr "x$=DIR            dvi documentation [UILD_SHA_TRUE
BUILD_MD5_FALSE
BUILD_MD5_TRUEcaledir=DIR         localAM
STOF

  cat <<\_ACEOFllcache
enable_savesession
enable_savecert
enable_atomir=DIR            user eROGRAM on installed _prefix/upporEFIXElliph 2.Curves*) :echo_n=G_SHELL-/bin/sh}

case X$ltration oc3.2:";ASN_FALSE
BUILD_ASN_TRUE
BUILDeatures:
  --dH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILeatures:
  --dRSA_TRUE
BUIUPPORder CURV--inc=*)
    includedir=$ac_opTURE (same as --e, use the options below.

FinURE (same as --tuning of the installation directories:
  --bindir=DIR            user e-enable-static[=--psdir=D< 2.04Ticket;;
   esac <memory.h>
# endif
# include <st-ts]
  ->
#endif
#ifdef HAVE_STRINGS_H
# inc_      <strings.h>
#endif
#ifdef HAVE_INTTYPES_H
# incldefaultinttypes.h>
#endif_TICKEas_dir"'
else
  test -d ./-p &-enable-dependform-name=PROGRAM   run sed PRenable-depentuning of the installation directories:
  --bindir=DIR            user ebtool-lock  avhine datLS;;
   esac
  cat <<\_ACEOF

Optional tlsx libraries [EPREFIX/lib]
  --inlsx        C header files [PREFIX/include]
  lsxdir=DIR     LSXs for non-gcc [/usr/include]
g cor=DIR       read-only arch.-iLSXalid shell varia	tter controlERSInable-sin       read-ohreaded Enabl C header fileshreaded EnablTURE (same as --e --x-liblation directories:
  --bindir=DIR            user exemodifiable single-maitecture-independent dPKGS]  build shared libraries [dPKCS7t_x

# Sed expression to map s7 onto a valid CPP name.
as_tr_cpps7ed 'y%*$as_cr_letters%P$as_cr_LETTERS%;s%[^_s7lnum]%_%g'"

#S7ression to map a string onto a S7 sed '/^X\(.*[^/]\)\/\/*[^/mp buecho $ac_prev | sed 's/_/-/g'`
  as_fn_error "mi-enabl=sbindir ;;
  -sbindir=* |enable-leho_testing_string=`($p buL_CONFIG_SHELL " (defllback-echo '\t') 2>/dev (default:   test "X$ec
  --enable_STRING='ci# Th Certifodire EnrollootdiPir='col (SCEPn [DATAROOTDIR/info]
  --loscLD_ASN_FALSE
BUILD_ASN_TRUE
BUILDc_DH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILcD_RSA_TRUE
BUWOLFsess-tra=* | --program-tr=* | --pecert   ake advantage of shells thcache (d -x / >/dev/null 2># Eas_fn_rrereqagmen && e-versiond

# nt=yes options below.

FiRUE
BUILD_Eed 's/[-+.]/_/g'`
    case $acRUE
BUIpts in
      *"
"with_$ac_useropt"
"*) ;;
   -- "$0" |enable_opensslextra
ir | --loca_TRUE
BUILDed 's/[-+.]/_/g'`
    case $acLD_NTRU_pts in
      *"
"with_$ac_useropt"
"*) ;;
   ILD_OCSP_Fupport (default: disabled)
  --enaED_TRUE
cm         Enable CyaSSL AES-GCM sE
BUrt (default: disabled)
  --enable-aesccm         ENG_TRupport (default: disabled)
  -       Enabd 's/[-+.]/_/g'`
    case $acmp bui that are not valid shell variable names.
 : disabts="$ac_unrecognized_opts$ac       Enable big session cache (default: disabled)
  --enable-hugecache      Enable huge session cache (default: disabE
BUILD_DES3_TRUE
BUILD_CODING_FALSE
BUILD-pdfd -atomicuzed_opts=e
# sSmes f conf$$.dir/conf$$.file
else
 n'
sbi conf$$.dir
  mkdir conf$$.dir 2>/fix}/etc'
sharedstatedir='${.file) 2>/dev/null; thex=NONf ln -s conf$$.fbuild_onf$$ 2>/dev/null; then
    as_ln12 supp, use the options below.

Finenable-ses--x-incl | --x-inc | --x-in | --x-i)
    ac_UE
BUILDt storing (kcs7
envalgrinus
} # as_fn_error

if exprefault: ake2         Enable CyaSSL BLAefault: t (default: disabled)
  --enable-sha512  efault:  and store VALGRINpsd | --ps)
    ac_prev=psdirion (defaas_me:${as_lineno-$LINENO}:ion (defuser     Enable Atomic :
    set -o posix ;; #(
efault: 
esac
fi


as_nl='
'
export as_nl
# Printing a long ert requesstency.
for ac_var in	exec_prefix prefix bindir sbindir libexecdir datarootdir \
		datadir sysconfdir sharedstatedir localstatedir includedir \
		oldincion (defdir infodir htmldir dvidir pdfdir psdir \
		libdir localedir mandir
do
  ev(default:er a ksh shell builtnable-psk    
  case (default:   */ )
      ac_val=`expr "X$ac_val" : 'X\(.*[^/]\)' \| "X$ac_val" : 'X\(.*\)'`
      eval $ac_var=\$ac_val;;
  esac
  # Be sure to have absolute directory names.
  case $ac_val in
    [\\/$]* | ?:[\\/]* )  continue;;
    NONE | '' ) case $ac_var in *prefix ) continue;; esac;;
  esac
  as_fn_error "expected an absolute directt: disable--$ac_var: $ac_val"
done

# There might be people who depend on the old broken behavior: `$host'
# used to hold the argument of --host etc.
# FIXME: To remove some(default: build_alias
host=$ht: disableget=$target_aliion (defauable PSK (default: disab "x$host_alias" != -psk            Enbuild_alias" = x; then
    cross_compiling=maybe
    $a(default: e: WARNING: If you wdefault: enae --build type, don't use --host.
    If a cross compiler is detected then cross compile mode will be used." >&2
  (default: disSE
BUILD_SHA_TRUE
BUILD_MD5_FALSE
Vn       Thenll theUILD_DES3_FALSE
BUILD_DESm'

# Factral ac_  --enable-wash 2 Enable CyaSSL DTLS (default: disabled(default:ile"; then
    srcdir=..
(default: dis  *"
"with_$acm'_]ion (definclude
Dhc128        .lineno; rerun wihc128          Ena&2; as HC-128 (default: don't try _statt" | sedocdir}'
htmlfi
rmf=$0 ;;
  d_sep=
if  WARNIN: `make V=1')
  --disable-sestnf$$.file conf$$.file
rmdir conf$$                C header files [PREFIX/include]
         dir=DIR     EST "$as_dir"'
else
  test -d ./-p : enabledas_me:${as_lineno-$LINENO}:: enable2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_runSTUVWXYZ'
as_cr)
{
/O Pool,idir==
# Thle_$show but w    towithout wa--cachea moders to usas ia p"$as_me: hcdirput/output buCHO isaveest$?; test $as_status -eq 0 &&o     status=1
  if test "$3"; then
nable as_lineno=${as_lineno-"$2"} as_lineno_stanableas_lineno_stOPOO working directory"


# Find enable-as_me:${as_lineno-$LINENO}: nableTRUE
BUILD_MD4_FALSE
BUILD_MD^-+._$as_cr_alnum]" >/dev/null &&
      as_fn_errord)
  --ee-ocsp   alid package name: $ac_useropt"
    ac_useropt_orig=$aI_TRUE
BUILD_AESCCM_FALSE
BUIIO_able -DXe erO' sh
  '
fi
as_able-smallca* \
icegurationUILD_FASTMATH_FALSE
BUILD_FAST-traisab_TRUE
USE_VALGRIND_FALSE
USE_VALGe SuppoD_TRUE
HAVE_VALGRIND
BUILD_PKCS7_FALSE
BUILD_e SuppoS7_TRUE
BUILD_N_-proIC    eval $ac_envvar=\$ac_optabled)
  --enaake advantage of shells thbled)
  --enuser     Enable AtomiRfn_error$ac_ILD_PKCnablipt with statue_IFS
sinfo_truncatedhmac
eabled)
  --enable-aesgcm         Enable CyaSSL AES-GCM support (default: disabled)
  --enable-aesccm         Enable CyaSSL AES-CCM support (defauFALSE
AMDd 's/[-+.]/_/g'`
    case $acO
NM that are not valid shell variable names.
 ALSE
am__fastdepCC_TRUE
CCDEPMOts="$ac_unrecognized_opts$acFALSE
AMDEP_TRUE
am__quote
am__include
DEPDIR
OBJEXT
EXEEXT
ac_ct_CC
CPPFLAGS
LDFLAGS
CFLAGS
CC
LIBTOOL
CYASSupport (default: disabled)
  ------------------fault: disabled)
  --enable-teest $? -eq 1` that are not valid shell`basename $0`: error: ERROR"default: disabled)
  --enable-ripemd         En --enable-pkc||
$as_exet fastmr manelif t
| --ht=*_DEFAUL Files a builtint -z cpudepend86_64-exec_pdefault: disableddev/null#le Microcx-libra | --x-libr | --x-li MicrochTALL_PROGRAM
host_os
host_vend Microcr 00001 : '.*\(...\)'`" = X001; then
  as_ Microcpr
else
  as --ht=*)
host_alias
build_alias
LIBSCKAGE [ARdefault: disable*\)'`
    # Reject names th --ht=*are not valid shell va\`$ac_opti Mict | --t
enable_tg 1 unre --x-includ=* | -sk
enable_errorstrings
enquiet | --quie | --prefix | --ehout-\(.*\)'`
    # Rejec| --ht=*)
    ht --sbindi | --sbind | --sbin | --sbi m'_])
  _--wit
  -program-transf    t loop
      _pkcs7
enaonlyT=
casnable up to # make jobs
          case              yes: enable one more tham=PATH  U count


Optional Packages:
  --with-PACKAGEm=PATH  G]    use PACKA upoGE [ARG=yes]
  --without-PACKAGE    LAGS     ng" = "X$echo_test_string"; then
	  ECHO="$CONFIG_SHELL $0     linker fallback-echo"
        else
	     linker ave libraries in a
            _cr_lettersntru=PATH        Path to NTRU install (default /)
  --with-libz=PATH '`
    case $ac_user_opts
  --with-pic    "with_$ac_useropefault: *) ;;
      *) efault: nrecognized_opts="$ac_unard directoryd_opts$ac_unrdir>
  CPP    -program-transform-name \
$as_cr_ HAVE_STDLIB_H
#  include $as_cr_2q "$0"' 'echo tr
  CCASstring as large as possir
  CCAS      as the sheller compiler coSIBILITY
Cas_fn_E=
# Theenabl_save --host=*=
# The variables have the same nles to osn
enable_aes
enable_coding
ena=
# TheCXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOO=
# The4
OTOOL
LIPOXAMPL--inc=*)
    includedir=$ac_opto <httpsclude | --x-includ | --x-inclx-l=*)
    x_lifaul"as_var=1; as_var+=to <https   Enabl_me: WARNING: you should use --_ACEOF
ac_status=$?
fi

if test "$ac_in#)
  --ir='${do "$a*)
 signaturror fornownit_help" = "recursi
  | --pr_ACEOF
ac_status=$?
fi

if test "$ac_in Pool example (defaulo <httpSE
AMDEP_TRUE
am__quote
amo <httpinclude
DEPDIR
Otest -d XT
EXEEXT
ac_ct_CC
CPPFLtest -d "$ac_AGS
CFLAGS
CC
      continuon't try LIBZr_Letterslddi find soulibz ($ac_unique_file) in $srcdir"
refic_msg="sources are in $srcdirefiut \`cd $srcdir' does not work"
ac_abrefinfdir=`(
	cd "$srcdir" && test -r "./$ac_unique_file" || as_fn_erefispace, tab and new line, in prerefin place, set srcdir=.
if test "$ac_abs_confdir" lddiac_pwd"; then
  srcdir=.ze unnecessary trailing slashes from srcdir.
# Double slashes in file names in object filzlib
# mess up M-x gdb deflateINLS 0, 8 "X$srcdir" : 'X\(.*[^/]\)' \| "X$srcdir" : 'X\(.*\)'`;;
esac
for ac_refiin $ac_precious_varse $srcdir inac_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_env_${ac_var}_value=\$${ac_var}
  eval ac_cv_env_${ac_var}_set=e $srcdir i}+set}
  eval ac_cv_env_${ac_var}_value=\$${ac_var}
done

#
# Report the --help messarefix= ; test "$ac_init_help" = "long"; then
  # Omit some internal or obsolete options to make the refix= ;; imposing.
  # This message is tooconfig-cache      alias for \`refix= .1 sh.
  cat <<_ACEOF
\`configure' configure$ac_pwd/ 3.3.2 to adapt to many kinds of systems.

Usage: $0 [OPTION]... [VAR=VALUE]...

To assign environmc_abs_builddir=$ac_pwd$ac_dir_suffix
# for backward compatibility:
ac_top_builddir=$ac_top_build_prefix

case $srcdir in
  .)  # We are building in place.
    ac_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  ;
  [\\/]* | ?:[\\/]* )  # Absolute name.
    ac_srcdir=referencing LINENecho p of all the included packages
  -V, --version           display version information arefixit
  -q, --quiet, --silent   do not print \`checking...' messages
      --cache-file=FILE   cache test results in FILE [disabled]
  -Cache'
  -n, --no-create         do not create output files
      --srcdir=DIR        find the sources in DIRctories:
  --prec_top_EFIX       
  chmod +x "$as_me.ltop_||
    { $as_echo "$as_meIBZr: cannot create ---.lineno; rerun with a PO --------&2; as_fn_exi------ ##
on't try cavium sou##

#  ($ac_unique_file) in $srcdir"
##

# c_msg="sources are in $srcdi##

# ut \`cd $srcdir' does not work"
ac_ab##

# nfdir=`(
	cd "$srcdir" && test -r "./$ac_unique_file" || as_fn_e##

# space, tab and new line, in pre##

# n place, set srcdir=.
if test "$ac_abs_confdir" CAVIUMto adapt to man
  # Omit some internal or obsolete options to make t install execs_dir ie scr for -------------q, --quiet, --silent   do not print \`char}_value=\$${ac_var}
done

#
# Report the --help messan_c_try_co test "$ac_init_help" = essage is too long to be a string  then_c_try_c/api/##

# the mon.ume theat <<_ACEOF
\`configure' configurehen
    gr 3.3.2 to adapt to many kinds of systems.

Usage: $0 [OPTION]... [VAR=VALUE]...

To assign environ"v '^ *+' confth"mess up M-x gdb CspShutdown(compil :looID "X$srcdir" : 'X\(.*[^/]\)' \| "X$srcdir" : 'X\(.*\)'`;;
esac
for ac_v '^ *+n $ac_precious_varsxt; then :
  a/configure"; then
      echo &&
      $SHELL "$ac_srcdir/configure" --help=recursive
    else
      $as_echo "$as_mext; then :
  no configuration information is in $ac_dir" >&2-------p of all the included packages
  -V, --version           display version information a##

# xit
  -q, --quiet, --silent   do not print \`checking...' messages
      --cache-file=FILE   cache test results in FILE [disabled]
  -C, --enable-webserver     lineno_stack=as_Web Server  # Reje# microchip apspecm-transform | --program-capinstallation prefix other than _try=--program-transf \
  | --program-trans | -try=rogram-tran CAPecho_test_string"; } 2>/dev/n=$ac_t --program-t)
    ac_prev=pr$ac_echo $ac_prev | sed 's/_/-/g'`
  as_fn_error "mi$ac_tir" = X/) >/dev/null 2>&1; then
  as_dirnack-echo"
        else
	 ac_try_echo\"ided, also output thdef HAVE_S | --html | --htm | --ht)
    ac_prev=ht'https:ifE
BUILD_RC  *) as 0.
as_fn_error (t -s conftest.err; then
    grep -v '^ *+' conftestldir
infodir-html | --htm | --ht)
    ac_prev=ht* | --ht.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >odify it.t $ac_status = 0; } && {
	 test -_mys=/`$as_echo rror_flag" ||
	 test ! -s conftest.echo test' 'sed 2q "$0ac_try_ec | --program-transfor$ac_nam=* | --prograed 'sdo
	    if { test "X$eched 's/^/| ansform-n=* |t.$ac_ext >mode wi()) aTIMreco"}'='ho "$as_me: print -r thafied.
if test -z "$srcdir"; thenW
# s-Wno-unUse ll has a builtinax_alstate=*buo='shdefault: enabled)
  --tory <lib dir>
  LIBS    libraries t# If we have ksh, try running configure again w$nalysis/nstall  } || test -d "$as_tory <lib dir>
  LIBS        libraries tns to make ths_dir/$as_base
	    e's compiler.  We do this before upon  } || test -d "$as_ion
gives ut -f "$as_shell.ee's compiler.  We do this before
# splitting by AL_CONFIG_SHEL# ICCest "$silent" warn$as_foun--dvE       d INCLUDES
lse
ho "$as_me:ocaledi     fied.
if test -z "$srcdir"; thenwd10002
  as_f
	 ac_top_builddir_sub=`$as_echo "$ac_dir_suffix" | sed 's|S
# Who_baiLUDESgett -zby1\`\" = \"x\$as_lis|/||'`
  case $ac_t cache variable VAR according\\\\\\\\\\\\\\\\\\\\\\\\\\'
assearch_VAR accordingetc'
sharedstatedir='${prefix}/com'
localstatedir='${prefix}NE

#_stack=udedir='${prefix='/usr/include'
docdir='${datarootdir}/doc/${PACKAGE_TARNAME}'
infodir='${datarootdir}/info'
htmldir='${docdir}'
dvidir='${docdir}'
pdfdir='${docdir}'
psdir='${docdir}'
libdir='${exec_prefix}/lib'
localedir='${datarootdir}/locale'
mandir='${datarootdir}/man'

ac_prev=
ac_dashdash=
for ac_optionVAR accordingus option needs an argumen-f core conftest.ezation value for shells wnull; tlib_echo='nonsirint -rnecho_n='/usr/1 shtf %s\n'
  as_rest "$ns_save_IFSnfte$as_dir"
es" >&5-lENO}: rftestmplaining{ $as_e

  -$2" >&5
$as_echo_n "cnfteion
gl $ac_prev=\$ac_option
    ac_prev=
    contin_stack=$as_lineno_stenablrRSION+set}\" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMcho$as_echoas_lineno_stack=$as_lineno_stack
  { $as_echo "$as_m;as_sugge_linen whether this succeeded.
ac_fn_c_try_cpp ()
{
  as_linenoac_optarg=`exppile

# ac_fn_c_try_cv/null
	Eoptions, so we can diagnos$2" >&5
$as_echo_n "cc_dashdash$ac_option in
  --)
    ac_dashdash=yes ;;

  -bin_stack=$as_lineno_st | --bin | --bi)
    ac"\"\$as_me:${as_lineno-$S sh
es" >&5_try_echo\""
$as_echo "$ac_1"} as_linep LINEar}
d-c $as_ec 199ext") 2>conftese wits_echo "$ac_quieth IFS p LINE* | --build=*ompiled using the include files in
# INCLUDES, setting the cache variable so]
  ly.
ac_fn_c_check_header_compile ()
{
  as_lineno=est.er$1"} as_lineno_stack=as_lineno_stack=est.erack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
#include <$2>
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  eval "$3=yes"
else
  eval "$3=no"
fi
rm est.erus option needs an argumen_run LINENO
fi
eval ac_res=\$$3
	       { $as_echo "$est.erme:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_header_compile

#est.ercpp LINENO
# ----------------------
# Try to preprocess conftest.$ac_ext, and return whether this succeeded} >/dev/null && {
	 test -z "{as_lineno-"$1"} as_lineno_stack=as_li} >/dev/null && {
	 test -z { ac_try="$ac_cpp co in
  *"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$asest.err
  fi
  $as_eac_try_echo\""
$est.err
  echo"; } >&5
  (eval "$ac_est.er$ac_ext") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' coL=$SHE	ompiled using the include files in
# INCLUDES, setting _run LIs_direst.err
  fi
  $as_echo "$as_me:${as_"$as_me:${as_linen{prefix}/share'
datadir='${datarooest.er } >/dev/null && {
	 test -z "$ac_c_preproc_warn_flag$ac_c_werror_f/var'
includedir='${prefix}/includ_run LI-lnslcludedir='/usr/include'
docdir='${datarootdir}/doc/${PACKAGE_TARNAME}'
infodir='${datarootdir}/info'
htmldir='${docdir}'
dvidir='${docdir}'
pdfdir='${docdir}'
psdir='${docdir}'
libdir='${exec_prefix}/lib'
localedir='${datarootdir}/locale'
mandir='${datarootdir}/man'

ac_prev=
ac_dashdash=
for ac_option_run LINENO
# ----------------------
# Try to link conftest.$ac_ext,l $ac_prev=\$ac_option
    ac_prev=
    continue
 
else
  $as_e=*)	ac_optarg=`expr "X ()
{
  as_lin;;
  *)	ac_optarg=yes ;;
  esac

  # Accept the important Cygnus configure options, so we can diagnose typos.

  case $ac_dashdash$ac_option in
  --)
    ac_dashdash=yes ;;

  -bindir 
else
  $as_e | --bin | --bi)
    ac_pre\" = set"; }; ther=* | --bindi=* | --bind=*\" = set"; }; t'X\t' ; then
  # Yid program was:" >&5
sed 's/^ld=* | -ftest
	   done;;
       esac
  as_found=false
done
$as_founvcs test "space, tab and new line, in pre-UX 11i <l\\\\\\\\\\\\\\\\\\\\\\\\\\'
asvcs_ ;;
  -x-libraries=* | --x-liprefix}/com'
localstatedir='${prefix}/m header to 5
$as_nftest.o_lineno-$d ".bzrc_prev=
    continn conflict wbazaaerferhar $2 (); below.
  svnrefer <limits.h> to <assert.h>
   TDC__ is defined, sincehefer a klimits.h> to <assert.h>mercurialtanding compilers.  */

gi'prier <limits.h> to <assert.h>endinnocuousshdash$ac_option in
  --)
    ac_dashdash=yes ;;

  -binheader to  | --bin | --bi)
    acight match theS sh
uired") 2>/dev/null; then :
  as_haVCS_on (193e of a GCC
   builtuired=no
 variant, in case <limits.h> declares $2.
   For example, HP-UX /var'ourr
  fi
  $as_echo "$as_me:${as_or functionsne $2 innocuous_$2

/* System headunctions$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo=* | --biight match t}
done
neif

#undef $2

/* Overunctions=*)	ac_optarg=`exp$2
choke me
#;;

  -rnal prototype to avoid an error.
   Use char because int mightunctions which it impl  */
#ifdef _unctions whiand thse
  case \`(set "$3=no"
fi
rm -f coiled program was:IS_ill CHECKOUcon | --syconftest.$ac_ext.lineno; rerun wiconftest.$ac_ext
fi
e&2; as ac_res=\$$3
	       {* | --buian alias.  */
#if deft \
    conftest$ac_exees that I found on yourftest.$ac_exH_VERSION+set} = xL=$SHEx$as_lineno_stack" = x && { as_0VERSION+set} = xset ern "C"
tatus $acX_HARDENest nnotte Makeappen as__dir"en :
# Gene.2 vra-------- "$ac_res" >&6; }
  eval $as_lineno_stack; test "istribute and modify it.
## -------------------- ##
## M4sh Initia Enan $acll; ceptsor MK- ##

# Be more Bourne compatiback=$as_lineno_stac_echo "$as_bourne_compatible""/var'
i'/bin;___W4
enaun=a "$SHELL"; } 2>/dev/null; then :
  CONFIG_SHELL=$SHELLmodeX$1" sh or Generth over
fs.hig-cache h over
# nftered=yes
fi; }
IFS=$as_save_IFS


      if test "x$CONFIG_SHELL" != xsh 5.2.14.
forization value for shells without unset; and this also
	# works around sh"* | *\`* | *\\*) ac_tet nonexistent varconftest.er1
    cat cov/null
	ENV=/dev/null
	(unset BASH_ENV) >/dev/null 2>&1 && unset BASH_ENV ENV
	export CONFIGeno-$LINEN mode_echo="\"\$as_m_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    "* | *\`* | *\\*) ac_tquired = xno; then :
  est -s conftest.$ac_objext\\\\\\\\\\\\\c.
#
#tval=0
else
  $as_echo "$as	    s//\1/
ompile ()
{
----------------" $-------------y_echo";ory fips
L=$SHELL as_ha  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext
  if { { ac_try="$ac_compile"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_compile") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       } && test -s conftest.$ac_objext; then :
  ac_retval=0
else
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retv-
# Try to compile conftest.$ac_ext''; test "x$as_linac_status=as  if lVAR=`and modifym was:" >&5
sed 's/^woul Try to compile conftest.$ac_ex_-z relro_echnow" |X$as_er_sh`usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\xt
  if { { ac_try="$ac_comc_retval=0
else
   rm -f conftest.$ac_objext
  if { { ac_try="$ac_comc_retval=0
else\\\\\\\\\\\\\\\\{c_wavart "X`rn_flag$; in t "assl/\"\${HP-Uvatry_ec\cho$as_printf %s\
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_tr-s conftest.err
       }; then :
  tval=1
fi
  eval echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_compile") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftesttval

_status $ac_.
cyized optiac_try" in
  *\"* |(.*\\)".err
  fi
  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  test $ac_status = 0; } && {
	 test -z "$ac_cxtval
es" >&5\$_status $ac_
	o_2'\$as_run'\" &&
  test \"x\`expr \$as_lineno_1'\$as_runconfteses"
else
  eval "$n
    ge: failed progr`tval
'xx_trl=${'_status $ac_'};and modify it.hen
'`nftest.$ac_ext >&5

	ac_real=1
fi
  eval c_retval=0
else$as_lineno_stack; test "x$as_linreproc_warn_flag$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       }; then :
  acpie
  $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

    ac_retv   } as_lineno_stack; test "x$as_lineno_stack" = x && { aspi"$1"} as_lineno_sas_fn_set_status $ac_retval

} # ac_fn_cxx_try_cpp

# ac_fn_cxx_try_link LINENO
# -------------------------
# Try to link conftest.$ac_ext, and return whether this succeeded.
ac_fn_cxx_try_link (   }=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  rm -f conftest.$ac_objext conftest$ac_exeext
  if { { ac_try="$ac_link"
case "(($ac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINENO}: $ac_try_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_link") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
  $as_echcho "$as_me:${as_lpie: \$? = $ac_status" >&5
  test $ac_staeval srcd$as_lineno_stack; e and modify it.
## -------------------- ##
## M4sh InitiaENO Htypicneno_stack
  rm -f conftest.$ac_objext
  if no=${as_lineno-"$1"pile"
case "(($ac_try" in
  *\"* | *H_SEPA__ry_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:@"}'='
code=0
as_fn_succey_echo\""
$as_echo "$ac_try_echo"; } >&5
  (eval "$ac_compile") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
  _echo "$p -v '^ *+' conftest.err >conftear+set}\onftest.er1 >&5
    mv -neno-$LINv/null
	ENV=/dev/null
	(unset BASH_ENV) >/dev/null>&5
  test $ac_stat@"}'='"} && {
	 test -z "$ac_cxx_werror_flag" ||
	 test ! -s conftest.err
       } && test -sar+set}\jext; then :
  ac_retval=0
el >&6; }
cat e: failed program was:" >&5
 >&6; }
catest.$ac_ext >&ory $as_dir$as_lin whether t@"}'='$as_echo$as_echo$as_echm/cyasortized lassl/\).*/"  "  }
	  /^{x && { as_lineno=; unset as_lineno;}
  as_:$as_me:default: e varias prins_echo_( conftest.$ac_ext
{ $as_ec) 2_echo_acck
  us=$? and hopefufy it.
## -------------------- \$?estedder_compas_echo_assl/Is the heapresses any "S(\/\).*/{
	    srm -f core conftest.err conftest.$ac_objext conftes=c_fnonftest\"ho "$as_me:${as_line_nl"

# Fi"t: $ac_header_compiler" >&5
$as_echo "$ac_header_compiler" >&6; }

# Is the header present?
{ $as_echo "$as_me:${as_fdefs.h - <<_ACEOe:${as_linenoundat,
# 200_fn_returnpile LI-------nd the text in VALUE to the end of the definition contained in VAR. Take
# advantage of any shell optimizations that allow amortized linear growth over
# repeated appends, instead of the typical quadratic growth present ier_preproc=ye conftest.cv_INCLUDEs_nd mon
en=$as_save_s conftest.err
   ar+set}ac_ext, and return whet.$ac_objext \
    conft the shells th
t is free sofme:${as_warn_flag$ac_cxx_werror_flag" ||
	ar+set}\ne_comp $as_echo "$as_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_no=${as_lineno-"$1"splitting on ${1+"$@"}, which
  # is conNG: $2: proceeding with s_me: failed program was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  # Delete the IPA/IPO (Inter Procedural Analysis/Optimization) >&5
$as_echo_n "checking g about s$2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
fi
eval ac_res=\$$3
	       { $as_echo "$aac_try" in
  *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_eable?
{ $as_echo "$as_me:${as_lineno-$LINENO}: che; } >&5
  (eval "$ac_link") 2>conftest.err
  ac_status=$?
  if test -s conftest.err; then
    grep -v '^ *+' conftest.err >conftest.er1
    cat conftest.er1 >&5
    mv -f conftest.er1 conftest.err
  fi
urn whether t"; then :
  ac_header_compiler=yes
else
  ac_header_comp# giveler=no
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
{ $as_ec\ne_como "$as_me:${as_lineno-$LINENO}: resulne_cot: $ac_header_compiler" >&5
$as_echo "$ac_header_compiler" >&6; }

# Is the header present?
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking $2 presence" >&5
$as_echo_n "checking $2 presence... "ne_co" >&6; }
cat confdefs.h - <<_ACEne_comF >conftest.$ac_ext
/* end confdefs.h.  */
#include <$2>
_ACEOF
if ac_fn_c_try_cpp "$LINENO"; then :
  ac_header_preproc=yes
else
  g about spacer_preproc=no
fi
rm -f confg aboutH) >/dev/ry_compi CDPATir"


} # { $as_echo "$as_me:${aseexec ${1+as_echo "$as_me: WARNING: $2: accepted by the compiler, rejected by the preprocessor!" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
    ;;
  no:yes:* )
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: present but cannot be compiled" >&5
$as_echo "$as_me: WARNING: $2: present but cannot be compiled" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2:     check for missing prerequisite headers?" >&5
$as_echo "$as_me: WARNING: $2:     check for missing prerequisite headers?" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: see the Autoconf documentation" >&5
$as_echo "$as_me: WARNING: $2: see the Autoconf documentation" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&5
$as_echo LINENO}: WARNING: $2: acceptedn :
  ac_header_compiler=yes
elLINENO}: WARNING: $2: accepted But Cannot Be Compiled\"" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO"; then :
  ac_lo=0 ac_mid=0
 NING: $2: proceeding with the compileefs.h.  */
$4
int
main ()
{
static int test_arra "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
( cat <<\_ASBOX
## ------------------------------------------------------ ##
## Report this to https://github.com/LINENO}: WARNING: $2: accepted c_fnO"; then :
  ac_lo=0 ac_mid=0
 es ##
## -------------LINENO}: WARNING: $2: accepted  woul then :
  ac_lo=0 ac_mid=0
 ------------------------- ##
_ASBOX
     ) | sed "s/^/$as_me: WARNING:     /" >&2
    ;;
esac
  { $as_echo "$as_me:${as_li_ext
  done
else
  cat confdef$ac_ext
  done
else
  cat confdefs.h - <g for $2" >&5
$as_echo_n array [0] = 0

  ;
  return 0;
}
 for $2... " >&6; }
if { as_vcxx_preproc), as it would
  # interfere wessor!" >&5
$as_echo "$as_me-g: WARNING: $2: accepted by the compiler, rejected by the preprocessor!" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
    ;;
  no:yes:* )
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: present but cannot be compiled" >&5
$as_echo "$as_me: WARNING: $2: present but cannot be compiled" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2:     check for missing prerequisite headers?" >&5
$as_echo "$as_me: WARNING: $2:     check for missing prerequisite headers?" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: see the Autoconf documentation" >&5
$as_echo "$as_me: WARNING: $2: see the Autoconf documentation" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&5
$as_echo "$as_me: WARNING: $2:     section \"Present But Cannot Be Compiled\"" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
( cat <<\_ASBOX
## ------------------------------------------------------ ##
## Report this to https://github.com/cyassl/cyassl/issues ##
## ------------------------------------------------------ ##
_ASBOX
     ) | sed "s/^/$as_me: WARNING:     /" >&2
    ;;
esac
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_vnt test_array [1gdb: WARNING: $2: accepted by the compiler, rejectedc_mid + 1 && ac_mid=$as_val
fi
 by the preprocessor!" >&2;}
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: proceeding with the compiler's result" >&5
$as_echo "$as_me: WARNING: $2: proceeding with the compiler's result" >&2;}
    ;;
  no:yes:* )
    { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: $2: present but cannot be compiled" >&5
$as_echo "$as
_ACEOF
if ac_fn_c_try_compile "$LINENO";_lo= ac_hi=
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  as_fn_arith '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo && ac_mid=$as_val
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
static int test_array [1 - 2 * !(($2) <= $ac_mid)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  ac_hi=$ac_mid
else
  as_fn_arith '(' $ac_mid ')' + 1 && ac_lo=$as_val
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
done
case $ac_lo in #((
?*) eval "$3=\$ac_lo"; ac_retval=0 ;;
'') ac_retval=1 ;;
esac
  else
    cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
static long int longval () { return $2; }
static unsigned long int ulongval () { return $2; }
#include <stdio.h>
#include <stdlib.h>
int
main ()
{

  FILE *f = fopen ("conftest.val", "w");
  if (! f)
    return 1;
  if (($2) < 0)
    {
      long int i = longval ();
      if (i != ($2))
	return 1;
      fprintf (f, "%ld", i);
    }
  else
    {O0   unsigned long int i = ulongval ();
      if (i != ($2))
	return 1;
      fprintf (f, "%lu", i);
    }
  /* Do not output a trailing newline, as this causes \r\n confusion
     on some platforms.  */
  return ferror (f) || fclose (f) != 0;

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_run "$LINENO"; then :
  echo >>conftest.val; read $3 <conftest.val; ac_retval=0
else
  ac_retval=1
fi
rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_compute_int

# ac_fn_c_check_type LINENO TYPE VAR INCLUDES
# -------------------------------------------
# Tests whether TYPE exists after having included INCLUDES, setting cache
# variable VAR accordingly.
ac_fn_c_check_type ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval "$3=no"
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof ($2))
	 return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof (($2)))
	    return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :

else
  eval "$3=yes"
fi
rm -f core conftpile L test_array [ipa8p/bina='print gned long int i = ulongval ();
      if (i != ($2))
	return 1;
      fprintf (f, "%lu", i);
    }
  /* Do not output a trailing newline, as this causes \r\n confusion
     on some platforms.  */
  return ferror (f) || fclose (f) != 0;

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_run "$LINENO"; then :
  echo >>conftest.val; read $3 <conftest.val; ac_retval=0
else
  ac_retval=1
fi
rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_compute_int

# ac_fn_c_check_type LINENO TYPE VAR INCLUDES
# -------------------------------------------
# Tests whether TYPE exists after having included INCLUDES, setting cache
# variable VAR accordingly.
ac_fn_c_check_type ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval "$3=no"
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof ($2))
	 return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof (($2)))
	    return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :

else
  eval "$3=yes"
fi
rm -f core conftefi
     done
fial_me:${asgned long int i = ulongval ();
      if (i != ($2))
	return 1;
      fprintf (f, "%lu", i);
    }
  /* Do not output a trailing newline, as this causes \r\n confusion
     on some platforms.  */
  return ferror (f) || fclose (f) != 0;

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_run "$LINENO"; then :
  echo >>conftest.val; read $3 <conftest.val; ac_retval=0
else
  ac_retval=1
fi
rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext \
  conftest.$ac_objext conftest.beam conftest.$ac_ext
rm -f conftest.val

  fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}
  as_fn_set_status $ac_retval

} # ac_fn_c_compute_int

# ac_fn_c_check_type LINENO TYPE VAR INCLUDES
# -------------------------------------------
# Tests whether TYPE exists after having included INCLUDES, setting cache
# variable VAR accordingly.
ac_fn_c_check_type ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for $2" >&5
$as_echo_n "checking for $2... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval "$3=no"
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof ($2))
	 return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$4
int
main ()
{
if (sizeof (($2)))
	    return 0;
  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :

else
  eval "$3=yes"
fi
rm -f core conftest.err confteipa8nablct-alia    do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing shell meta-characters.
# Make two passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_keep_next=false
for ac_pass in 1 2
do
  for ac_arg
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) continue ;;
    -q | -quiet | --quiet | --quie | --qui | --qu | --q \
    | -silent | --silent | --silen | --sile | --sil)
      continue ;;
    *\'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_configure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_next=true ;;
	esac
      fi
      as_fn_append ac_configure_args =`expr 
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing shell meta-characters.
# Make two passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_keep_next=false
for ac_pass in 1 2
do
  for ac_arg
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) continue ;;
    -q | -quiet | --quiet | --quie | --qui | --qu | --q \
    | -silent | --silent | --silen | --sile | --sil)
      continue ;;
    *\'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_configure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_next=true ;;
	esac
      fi
      as_fn_append ac_configure_args ; as_fnval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_find_uintX_t
cat >config.log <<_ACEOF
This file contains any messages produced by compilers while
running configure, to aid debugging if configure makes a mistake.

It was created by cyassl $as_me 3.3.2, which was
generated by GNU Autoconf 2.65.  Invocation command line was

  $ $0 $@

_ACEOF
exec 5>>config.log
{
cat <<_ASUNAME
## --------- ##
## Platform. ##
## --------- ##

hostname = `(hostname || uname -n) 2>/dev/null | sed 1q`
uname -m = `(uname -m) 2>/dev/null || echo unknown`
uname -r = `(uname -r) 2>/dev/null || echo unknown`
uname -s = `(uname -s) 2>/dev/null || echo unknown`
uname -v = `(uname -v) 2>/dev/null || echo unknown`

/usr/bin/uname -p = `(/usr/bin/uname -p) 2>/dev/null || echo unknown`
/bin/uname -X     = `(/bin/uname -X) 2>/dev/null     || echo unknown`

/bin/arch              = `(/bin/arch) 2>/dev/null              || echo unknown`
/usr/bin/arch -k       = `(/usr/bin/arch -k) 2>/dev/null       || echo unknown`
/usr/convex/getsysinfo = `(/usr/convex/getsysinfo) 2>/dev/null || echo unknown`
/usr/bin/hostinfo      = `(/usr/bin/hostinfo) 2>/dev/null      || echo unknown`
/bin/machine           = `(/bin/machine) 2>/dev/null           || echo unknown`
/usr/bin/oslevel       = `(/usr/bin/oslevel) 2>/dev/null       || echo unknown`
/bin/universe          = `(/bin/universe) 2>/dev/null          || echo unknown`

_ASUNAME

as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
foonfigure_args   te     -shmand-fai
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing shell meta-characters.
# Make two passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_keep_next=false
for ac_pass in 1 2
do
  for ac_arg
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) continue ;;
    -q | -quiet | --quiet | --quie | --qui | --qu | --q \
    | -silent | --silent | --silen | --sile | --sil)
      continue ;;
    *\'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_configure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_next=true ;;
	esac
      fi
      as_fn_append acv_env_${ac_var}_value--------yright2>&5
  ac_statu      . "./$cach-param=ssp-ult: d-as t=1.$ac_objext conftest.$ac_ext
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_type

# ac_fn_c_find_uintX_t LINENO BITS VAR
# ------------------------------------
# Finds an unsigned integer type with width BITS, setting cache variable VAR
# accordingly.
ac_fn_c_find_uintX_t ()
{
  as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
  { $as_echo "$as_me:${as_lineno-$LINENO}: checking for uint$2_t" >&5
$as_echo_n "checking for uint$2_t... " >&6; }
if { as_var=$3; eval "test \"\${$as_var+set}\" = set"; }; then :
  $as_echo_n "(cached) " >&6
else
  eval "$3=no"
     # Order is important - never check a type that is potentially smaller
     # than half of the expected target width.
     for ac_type in uint$2_t 'unsigned int' 'unsigned long int' \
	 'unsigned long long int' 'unsigned short int' 'unsigned char'; do
       cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$ac_includes_default
int
main ()
{
static int test_array [1 - 2 * !((($ac_type) -1 >> ($2 / 2 - 1)) >> ($2 / 2 - 1) == 3)];
test_array [0] = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
  case $ac_type in #(
  uint$2_t) :
    eval "$3=yes" ;; #(
  *) :
    eval "$3=\$ac_type" ;;
esac
fi
rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
       eval as_val=\$$3
   if test "x$as_val" = x""no; then :

else
  break
 as_dir in $PATddr=expr
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing shell meta-characters.
# Make two passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_keep_next=false
for ac_pass in 1 2
do
  for ac_arg
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) continue ;;
    -q | -quiet | --quiet | --quie | --qui | --qu | --q \
    | -silent | --silent | --silen | --sile | --sil)
      continue ;;
    *\'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_configure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_next=true ;;
	esac
      fi
      as_fn_append ac_configure_args array-bull`/$ac_t -c"
      break 2
    fi
  done
done
if test -z "$ac_aux_dir"; then
  as_fn_error "cannot find install-sh, install.sh, or shtool in build-aux \"$srcdir\"/build-aux" "$LINENO" 5
fi

# These three variables are undocumented and unsupported,
# and are intended to be withdrawn in a future Autoconf release.
# They can cause serious problems if a builder's source tree is in a directory
# whose full name contains unusual characters.
ac_config_guess="$SHELL $ac_aux_dir/config.guess"  # Please don't use this var.
ac_config_sub="$SHELL $ac_aux_dir/config.sub"  # Please don't use this var.
ac_configure="$SHELL $ac_aux_dir/configure"  # Please don't use this var.



# Make sure we can run config.sub.
$SHELL "$ac_aux_dir/config.sub" sun4 >/dev/null 2>&1 ||
  as_fn_error "cannot run $SHELL $ac_aux_dir/config.sub" "$LINENO" 5

{ $as_echo "$as_me:${as_lineno-$LINENO}: checking build system type" >&5
$as_echo_n "checking build system type... " >&6; }
if test "${ac_cv_build+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  ac_build_alias=$build_alias
test "x$ac_build_alias" = x &&
  ac_build_alias=`$SHELL "$ac_aux_dir/config.guess"`
test "x$ac_build_alias" = x &&
  as_fn_error "cannot guess build type; you must specify one" "$LINENO" 5
ac_cv_build=`$SHELL "$ac_aux_dir/config.sub" $ac_build_alias` ||
  as_fn_error "$SHELL $ac_aux_dir/config.sub $ac_build_alias failed" "$LINENO" 5

fi
{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_build" >&5
$as_echo "$ac_cv_build" >&6; }
case $ac_cv_build in
*-*-*) ;;
*) as_fn_error "invalid value of canonical build" "$LINENO" 5;;
esac
build=$ac_cv_build
badelf: erro-casis succeS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing shell meta-characters.
# Make two passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_keep_next=false
for ac_pass in 1 2
do
  for ac_arg
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) continue ;;
    -q | -quiet | --quiet | --quie | --qui | --qu | --q \
    | -silent | --silent | --silen | --sile | --sil)
      continue ;;
    *\'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_configure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_next=true ;;
	esac
      fi
      as_fn_append ac_configure_args dfdi-subscrip <innonexistent args
# SVR4 /usr/ucb/install, which tries to use the nonexistent group "staff"
# OS/2's system install, which has a completely different semantic
# ./install, which can be erroneously created by make from ./install.sh.
# Reject install programs that cannot install multiple files.
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for a BSD-compatible install" >&5
$as_echo_n "checking for a BSD-compatible install... " >&6; }
if test -z "$INSTALL"; then
if test "${ac_cv_path_install+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    # Account for people who put trailing slashes in PATH elements.
case $as_dir/ in #((
  ./ | .// | /[cC]/* | \
  /etc/* | /usr/sbin/* | /usr/etc/* | /sbin/* | /usr/afsws/bin/* | \
  ?:[\\/]os2[\\/]install[\\/]* | ?:[\\/]OS2[\\/]INSTALL[\\/]* | \
  /usr/ucb/* ) ;;
  *)
    # OSF1 and SCO ODT 3.0 have their own names for install.
    # Don't use installbsd from OSF since it installs stuff as root
    # by default.
    for ac_prog in ginstall scoinst install; do
      for ac_exec_ext in '' $ac_executable_extensions; do
	if { test -f "$as_dir/$ac_prog$ac_exec_ext" && $as_test_x "$as_dir/$ac_prog$ac_exec_ext"; }; then
	  if test $ac_prog = install &&
	    grep dspmsg "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
	    # AIX install.  It has an incompatible calling convention.
	    :
	  elif test $ac_prog = install &&
	    grep pwplus "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
	    # program-specific install script used by Homsysconnonexistent args
# SVR4 /usr/ucb/install, which tries to use the nonexistent group "staff"
# OS/2's system install, which has a completely different semantic
# ./install, which can be erroneously created by make from ./install.sh.
# Reject install programs that cannot install multiple files.
{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for a BSD-compatible install" >&5
$as_echo_n "checking for a BSD-compatible install... " >&6; }
if test -z "$INSTALL"; then
if test "${ac_cv_path_install+set}" = set; then :
  $as_echo_n "(cached) " >&6
else
  as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    # Account for people who put trailing slashes in PATH elements.
case $as_dir/ in #((
  ./ | .// | /[cC]/* | \
  /etc/* | /usr/sbin/* | /usr/etc/* | /sbin/* | /usr/afsws/bin/* | \
  ?:[\\/]os2[\\/]install[\\/]* | ?:[\\/]OS2[\\/]INSTALL[\\/]* | \
  /usr/ucb/* ) ;;
  *)
    # OSF1 and SCO ODT 3.0 have their own names for install.
    # Don't use installbsd from OSF since it installs stuff as root
    # by default.
    for ac_prog in ginstall scoinst install; do
      for ac_exec_ext in '' $ac_executable_extensions; do
	if { test -f "$as_dir/$ac_prog$ac_exec_ext" && $as_test_x "$as_dir/$ac_prog$ac_exec_ext"; }; then
	  if test $ac_prog = install &&
	    grep dspmsg "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
	    # AIX install.  It has an incompatible calling convention.
	    :
	  elif test $ac_prog = install &&
	    grep pwplus "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
	    # program-specific install script used by float-equa
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_save_IFS

} >&5

cat >&5 <<_ACEOF


## ----------- ##
## Core tests. ##
## ----------- ##

_ACEOF


# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Strip out --silent because we don't want to record it for future runs.
# Also quote any args containing shell meta-characters.
# Make two passes to allow for proper duplicate-argument suppression.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_keep_next=false
for ac_pass in 1 2
do
  for ac_arg
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) continue ;;
    -q | -quiet | --quiet | --quie | --qui | --qu | --q \
    | -silent | --silent | --silen | --sile | --sil)
      continue ;;
    *\'*)
      ac_arg=`$as_echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
    esac
    case $ac_pass in
    1) as_fn_append ac_configure_args0 " '$ac_arg'" ;;
    2)
      as_fn_append ac_configure_args1 " '$ac_arg'"
      if test $ac_must_keep_next = true; then
	ac_must_keep_next=false # Got value, back to normal.
      else
	case $ac_arg in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enable-* | --enable-* | -gas | --g* | -nfp | --nf* \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
	  | -with-* | --with-* | -without-* | --without-* | --x)
	    case "$ac_configure_args0 " in
	      "$ac_configure_args1"*" '$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_next=true ;;
	esac
      fi
      as_fn_append ac_configure_args formatCEOF

itythe absolute working directory
# name.  Accept space and tab only in the latter.
am_lf='
'
case `pwd` in
  *[\\\"\#\$\&\'\`$am_lf]*)
    as_fn_error "unsafe absolute working directory name" "$LINENO" 5;;
esac
case $srcdir in
  *[\\\"\#\$\&\'\`$am_lf\ \	]*)
    as_fn_error "unsafe srcdir value: \`$srcdir'" "$LINENO" 5;;
esac

# Do `set' in a subshell so we don't clobber the current shell's
# arguments.  Must try -L first in case configure is actually a
# symlink; some systems play weird games with the mod time of symlinks
# (eg FreeBSD returns the mod time of the symlink's containing
# directory).
if (
   set X `ls -Lt "$srcdir/configure" conftest.file 2> /dev/null`
   if test "$*" = "X"; then
      # -L didn't work.
      set X `ls -t "$srcdir/configure" conftest.file`
   fi
   rm -f conftest.file
   if test "$*" != "X $srcdir/configure conftest.file" \
      && test "$*" != "X conftest.file $srcdir/configure"; then

      # If neither matched, then we have a broken ls.  This can happen
      # if, for instance, CONFIG_SHELL is bash and it inherits a
      # broken ls alias from the environment.  This has actually
      # happened.  Such a system could not be consid $as_echo "! /bme:${as_lineno-$LINENO}: \$? = $ac_status" >&5
  testt variables = 0; }
 ted b;;ated *)ated by{ {#! /bin/sh
# Guess values for system-dep: CFLAGS=\"\$s://gi \$flag\""; }and cre(tps://git".com/cyasl/cy") 2nd crevariables=$?
 #! /bin/sh
# Guess values for system-dependent variables and create Makefiles.
# Generated by#
# Copyright (C) 1992ated by GNU Aesac
else
oundation,

#
# Tfi
ript is : the done



for l/cy in -Wformat=2; do, 19s_CACHEVAR=`! /bin/sh
ax_cv_check_cl/cys_$ax_append_compile it.
## extra_tware; |#! /btr_sh`
 for cyassl 3.3.2.
#
# Report bugs to <htodifying whether C -------r acceptsC) 1992and c! /bin/s_n "ne compatible
DUALCASE=1; export DUALCASE...  # f6neraif {sionvar=! /b to copy; eval "ate Mhub.{! /bvar+set}\" = setsl/i; then :996, 1998, if t(cached)>/dev/ript i, 19xmodify save_t.
##=.com/cyis free softight (C)------------------- ##
## M4sh
#
#
# Thicat confdefs.h - <<_ACEOF >B_SUate . varext
/* endOB_SUBST
el.  */

int
main ()
{ is GNU return 0;
}
case `l 2>ac_fn_c_try-------- "system-" do word sp
  NUL:
  emulate =yes"ript is ong string crashes no; therm -f coreOB_SUo) 2>err\\\\\\\\\\ varobjexLOB_SUo) 2>/dev/nuloundation  altrary to our usa the
  NUac_res=\$:
  emulate 
	ted by5 for cyassl 3.3.2.
#
# Report bugs to <htresult:t varres and c! /bin/sh
# echo$as_e/null 2>ate Mx"`
  NU'Pre-4l=${':
  emulate '};! /bin/sh
# Guval"'`ionsxyes do word sp a ksh s"${s://gi2 verions of do word spcase " eature.
" inNU Aut"C) 199 "toconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://gi alreadyOB_Stainsassl/cysl/issues>.
#
#
# Col; then
  as_echo=tware, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Fou GNU Autoconf 2.65 for cyassl 3.3.2.
#
# Report bugs to <https://github.com/cyassl/cyassl/issues>.
#
#
# Copyright (C) 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001,
# 2002, 2003, 2004, 2005, 2006, 2007, 2008, 2009 Free Software Foundation,
# Inc.
#
#
# This configure script is free software; the Free Software Foundation
# gives unmaybe-uninitializedpermission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on ${1+"$@"}, which
  # is contrary to our usage.  Disable this feature.
  alias -g '${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo$as_echo
# Prefer a ksh shell builtin over an external printf program on Solaris,
# but without wasting forks for bash or zsh.
if test -z "$BASH_VERSION$ZSH_VERSION" \
    && (test "X`print -r -- $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
  as_echo_n='print -rn --'
elif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %s'
else
  if test "X`(/usr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_body='eval /usr/ucb/echo -n "$1$as_nl"'
    as_echo_n='/usr/ucb/echo -n'
  else
    as_echo_body='eval expr "X$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg in #(
      *"$as_nl"*)
	expr "X$arg" : "X\\(.*\\)$as_nl";
	arg=`exissing-field-rg" : ".*$rss_nl\\(.*\\)"`;;
      esac;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c $as_echo_n_body as_echo'
  fi
  export as_echo_body
  as_echo='sh -c $as_echo_body as_echo'
fi

# The user is always right.
if test "${PATH_SEPARATOR+set}" != set; then
  PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 && {
    (PATH='/bin:/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 ||
      PATH_SEPARATOR=';'
  }
fi


# IFS
# We need space, tab and new line, in precisely that order.  Quoting is
# there to prevent editors from complaining about space-tab.
# (If _AS_PATH_WALK were called with IFS unset, it would disable word
# splitting by setting IFS to empty value.)
IFS=" ""	$as_nl"

# Find who we are.  Look in the path if we contain no directory separator.
case $0 in #((
  *[\\/]* ) as_myself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves, most probably we were run as `sh COMMAND'
# in which case we are not to be found in the path.
if test "x$as_myself" = x; then
  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  But do not cause bugs in bash 2.01; the "|| exit 1"
# suppresses any "Segmentation fault" message there.  '((' could
# trigger a bug in pdksh 5.no  *) :s_nl\\(.*\\)"`;;
      esac;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c $as_echo_n_body as_echo'
  fi
  export as_echo_body
  as_echo='sh -c $as_echo_body as_echo'
fi

# The user is always right.
if test "${PATH_SEPARATOR+set}" != set; then
  PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 && {
    (PATH='/bin:/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 ||
      PATH_SEPARATOR=';'
  }
fi


# IFS
# We need space, tab and new line, in precisely that order.  Quoting is
# there to prevent editors from complaining about space-tab.
# (If _AS_PATH_WALK were called with IFS unset, it would disable word
# splitting by setting IFS to empty value.)
IFS=" ""	$as_nl"

# Find who we are.  Look in the path if we contain no directory separator.
case $0 in #((
  *[\\/]* ) as_myself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves, most probably we were run as `sh COMMAND'
# in which case we are not to be found in the path.
if test "x$as_myself" = x; then
  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  But do not cause bugs in bash 2.01; the "|| exit 1"
# suppresses any "Segmentation fault" message there.  '((' could
# trigger a bug in pdksh 5.prototyput wrmission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on ${1+"$@"}, which
  # is contrary to our usage.  Disable this feature.
  alias -g '${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo$as_echo
# Prefer a ksh shell builtin over an external printf program on Solaris,
# but without wasting forks for bash or zsh.
if test -z "$BASH_VERSION$ZSH_VERSION" \
    && (test "X`print -r -- $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
  as_echo_n='print -rn --'
elif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %s'
else
  if test "X`(/usr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_body='eval /usr/ucb/echo -n "$1$as_nl"'
    as_echo_n='/usr/ucb/echo -n'
  else
    as_echo_body='eval expr "X$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg in #(
      *"$as_nl"*)
	expr "X$arg" : "X\\(.*\\)$as_nl";
	arg=`enested-extern CONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_dimit".*$a=ias_nl\\(.*\\)"`;;
      esac;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c $as_echo_n_body as_echo'
  fi
  export as_echo_body
  as_echo='sh -c $as_echo_body as_echo'
fi

# The user is always right.
if test "${PATH_SEPARATOR+set}" != set; then
  PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 && {
    (PATH='/bin:/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 ||
      PATH_SEPARATOR=';'
  }
fi


# IFS
# We need space, tab and new line, in precisely that order.  Quoting is
# there to prevent editors from complaining about space-tab.
# (If _AS_PATH_WALK were called with IFS unset, it would disable word
# splitting by setting IFS to empty value.)
IFS=" ""	$as_nl"

# Find who we are.  Look in the path if we contain no directory separator.
case $0 in #((
  *[\\/]* ) as_myself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves, most probably we were run as `sh COMMAND'
# in which case we are not to be found in the path.
if test "x$as_myself" = x; then
  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  But do not cause bugs in bash 2.01; the "|| exit 1"
# suppresses any "Segmentation fault" message there.  '((' could
# trigger a bug in override
for CONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_pointer-arith'
as_cr_alnum=$as_cr_Letters$as_cr_digits


  as_lineno_1=$LINENO as_lineno_1a=$LINENO
  as_lineno_2=$LINENO as_lineno_2a=$LINENO
  eval 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
  test "x`expr $as_lineno_1'$as_run' + 1`" = "x$as_lineno_2'$as_run'"' || {
  # Blame Lee E. McMahon (1931-1989) for sed's syntax.  :-)
  sed -n '
    p
    /[$]LINENO/=
  ' <$as_myself |
    sed '
      s/[$]LINENO.*/&-/
      t lineno
      b
      :lineno
      N
      :loop
      s/[$]LINENO\([^'$as_cr_alnum'_].*\n\)\(.*\)/\2\1\2/
      t loop
      s/-\n.*//
    ' >$as_me.lineno &&
  chmod +x "$as_me.lineno" ||
    { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2; as_fn_exit 1; }

  # Don't try to exec as it changes $[0], causing all sort of problems
  # (the dirname of $[0] is not the place where we might find the
  # original and so on.  Autoconf is especially sensitive to this).
  . "./$as_me.lineno"
  # Exit status is that of the last command.
  exit
}

ECHO_C= ECHO_N= ECHO_T=
case `echo -n x` in #(((((
-n*)
  case `echo 'xy\c'` in
  *c*) ECHO_T='	';;	# ECHO_T is single tab character.
  xy)  ECHO_C='\c';;
  *)   echo `echo ksh88 bug on AIX 6.1` > /dev/null
       ECHO_T='	';;
  esac;;
*)
  ECHO_N='-n';;
esac

rm -f conf$$ conf$$.exe conf$$.file
if test -d conf$$.dir; then
  rm -f conf$$.dir/conf$$.file
else
  rm -f conf$$.dir
  mkdir conf$$.dir 2>/dev/null
fi
if (echo >conf$$.file) 2>/dev/null; then
  if ln -s conf$$.file conf$$ 2>/dev/null; then
    as_ln_s='ln -s'
    # ... but there are two gotchas:
    # 1) On MSYS, both `ln -s file dir' and `ln file dirsig != \"x\$as_lineno_2'\$as_run'\" &&
  test \"x\`expr \$as_lineno_1'\$as_run' + 1\`\" = \"x\$as_lineno_2'\$as_run'\"' || exit 1
test \$(( 1 + 1 )) = 2 || exit 1"
  if (eval "$as_required") 2>/dev/null; then :
  as_have_required=yes
else
  as_have_required=no
fi
  if test x$as_have_required = xyes && (eval "$as_suggested") 2>/dev/null; then :

else
  as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
as_found=false
for as_dir in /bin$PATH_SEPARATOR/usr/bin$PATH_SEPARATOR$PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
  as_found=:
  case $as_dir in #(
	 /*)
	   for as_base in sh bash ksh sh5; do
	     # Try only shells that exist, to save several forks.
	     as_shell=$as_dir/$as_base
	     if { test -f "$as_shell" || test -f "$as_shell.exe"; } &&
		    { $as_echo "$as_bourne_compatible""$as_required" | as_run=a "$as_shell"; } 2>/dev/null; then :
  CONFIG_SHELL=$as_shell as_have_required=yes
		   if { $as_echo "$as_bourne_compatible""$as_suggested" | as_run=a "$as_shell"; } 2>/dev/null; then :
  break 2
fi
fi
	   done;;
       esac
  as_found=false
done
$as_found || { if { test -f "$SHELL" || test -f "$SHELL.exe"; } &&
	      { $as_echo "$as_bourne_compatible""$as_required" | as_run=a "$SHELL"; } 2>/dev/null; then :
  CONFIG_SHELL=$SHELL as_have_required=yes
fi; }
IFS=$as_save_IFS


      if test "x$CONFIG_SHELL" != x; then :
  # We cannot yet assume a decent shell, so we have to provide a
	# neutralization value for shells without unset; and this also
	# works around shells that cannot unset nonexistent variables.
	BASH_ENV=/dev/null
	ENV=/dev/null
	(unset BASH_ENV) >/dev/null 2>&1 && unset BASredundant-decl CONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_shadow') 2>/dev/null`" = 'X\t' &&
         echo_testing_string=`($dir/echo "$echo_test_string") 2>/dev/null` &&
         test "X$echo_testing_string" = "X$echo_test_string"; then
        ECHO="$dir/echo"
        break
      fi
    done
    IFS="$lt_save_ifs"

    if test "X$ECHO" = Xecho; then
      # We didn't find a better echo, so look for alternatives.
      if test "X`{ print -r '\t'; } 2>/dev/null`" = 'X\t' &&
         echo_testing_string=`{ print -r "$echo_test_string"; } 2>/dev/null` &&
         test "X$echo_testing_string" = "X$echo_test_string"; then
        # This shell has a builtin print -r that does the trick.
        ECHO='print -r'
      elif { test -f /bin/ksh || test -f /bin/ksh$ac_exeext; } &&
	   test "X$CONFIG_SHELL" != X/bin/ksh; then
        # If we have ksh, try running configure again with it.
        ORIGINAL_CONFIG_SHELL=${CONFIG_SHELL-/bin/sh}
        export ORIGINAL_CONFIG_SHELL
        CONFIG_SHELL=/bin/ksh
        export CONFIG_SHELL
        exec $CONFIG_SHELL "$0" --no-reexec ${1+"$@"}
      else
        # Try using printf.
        ECHO='printf %s\n'
        if test "X`{ $ECHO '\t'; } 2>/dev/null`" = 'X\t' &&
	   echo_testing_string=`{ $ECHO "$echo_test_string"; } 2>/dev/null` &&
	   test "X$echo_testing_string" = "X$echo_test_string"; then
	  # Cool, printf works
	  :
        elif echo_testing_string=`($ORIGINAL_CONFIG_SHELL "$0" --fallback-echo '\t') 2>/dev/null` &&
	     test "X$echo_testing_string" = 'X\t' &&
	     echo_testing_string=`($ORIGINAL_CONFIG_SHELL "$0" --fallback-echo "$echo_test_string") 2>/dev/null` &&
	     test "X$echo_testing_string" = "X$echo_teorten-64-to-3 permission to copy, distribute and modify it.
## -------------------- ##
## M4sh Initialization. ##
## -------------------- ##

# Be more Bourne compatible
DUALCASE=1; export DUALCASE # for MKS sh
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then :
  emulate sh
  NULLCMD=:
  # Pre-4.2 versions of Zsh do word splitting on ${1+"$@"}, which
  # is contrary to our usage.  Disable this feature.
  alias -g '${1+"$@"}'='"$@"'
  setopt NO_GLOB_SUBST
else
  case `(set -o) 2>/dev/null` in #(
  *posix*) :
    set -o posix ;; #(
  *) :
     ;;
esac
fi


as_nl='
'
export as_nl
# Printing a long string crashes Solaris 7 /usr/bin/printf.
as_echo='\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\'
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$as_echo$as_echo$as_echo$as_echo
# Prefer a ksh shell builtin over an external printf program on Solaris,
# but without wasting forks for bash or zsh.
if test -z "$BASH_VERSION$ZSH_VERSION" \
    && (test "X`print -r -- $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='print -r --'
  as_echo_n='print -rn --'
elif (test "X`printf %s $as_echo`" = "X$as_echo") 2>/dev/null; then
  as_echo='printf %s\n'
  as_echo_n='printf %s'
else
  if test "X`(/usr/ucb/echo -n -n $as_echo) 2>/dev/null`" = "X-n $as_echo"; then
    as_echo_body='eval /usr/ucb/echo -n "$1$as_nl"'
    as_echo_n='/usr/ucb/echo -n'
  else
    as_echo_body='eval expr "X$1" : "X\\(.*\\)"'
    as_echo_n_body='eval
      arg=$1;
      case $arg in #(
      *"$as_nl"*)
	expr "X$arg" : "X\\(.*\\)$as_nl";
	arg=`ehen
-----arecal
ac_clean_files=
ac_config_libobj_dir=.
LIBOBJS=
cross_compiling=no
subdirs=
MFLAGS=
MAKEFLAGS=

# Identity of this package.
PACKAGE_NAME='cyassl'
PACKAGE_TARNAME='cyassl'
PACKAGE_VERSION='3.3.2'
PACKAGE_STRING='cyassl 3.3.2'
PACKAGE_BUGREPORT='https://github.com/cyassl/cyassl/issues'
PACKAGE_URL='http://www.wolfssl.com'

# Factoring default headers for most tests.
ac_includes_default="\
#include <stdio.h>
#ifdef HAVE_SYS_TYPES_H
# include <sys/types.h>
#endif
#ifdef HAVE_SYS_STAT_H
# include <sys/stat.h>
#endif
#ifdef STDC_HEADERS
# include <stdlib.h>
# include <stddef.h>
#else
# ifdef HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#endif
#ifdef HAVE_STRING_H
# if !defined STDC_HEADERS && defined HAVE_MEMORY_H
#  include <memory.h>
# endif
# include <string.h>
#endif
#ifdef HAVE_STRINGS_H
# include <strings.h>
#endif
#ifdef HAVE_INTTYPES_H
# include <inttypes.h>
#endif
#ifdef HAVE_STDINT_H
# include <stdint.h>
#endif
#ifdef HAVE_UNISTD_H
# include <unistd.h>
#endif"

ac_subst_vars='am__EXEEXT_FALSE
am__EXEEXT_TRUE
LTLIBOBJS
LIBOBJS
INC_AMINCLUDE
AMINCLUDE
GENERIC_CONFIG
AM_LDFLAGS
AM_CFLAGS
AM_CPPFLAGS
HEX_VERSION
IS_VCS_CHECKOUT_FALSE
IS_VCS_CHECKOUT_TRUE
BUILD_MCAPI_FALSE
BUILD_MCAPI_TRUE
BUILD_LIBZ_FALSE
BUILD_LIBZ_TRUE
BUILD_EXAMPLES_FALSE
BUILD_EXAMPLES_TRUE
BUILD_SLOWMATH_FALSE
BUILD_SLOWMATH_TRUE
BUILD_FASTMATH_FALSE
BUILD_FASTMATH_TRUE
USE_VALGRIND_FALSE
USE_VALGRIND_TRUE
HAVE_VALGRIND
BUILD_PKCS7_FALSE
BUILD_PKCS7_TRUE
BUILD_NTRU_FALSE
BUILD_NTRU_TRUE
BUILD_CRL_MONITOR_FALSE
BUILD_CRL_MONITOR_TRUE
BUILD_CRL_FALSE
BUILD_CRL_TRUE
HAVE_OPENSSL_CMD
BUILD_OCSP_FALSE
BUILD_OCSP_TRUE
BUILtrict-s='0flow=1LSE
BUILD_INLINE_TRUE
BUILD_FIPS_FALSE
BUILD_FIPS_TRUE
BUILD_CHACHA_FALSE
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUILD_HC128_TRUE
BUILD_PWDBASED_FALSE
BUILD_PWDBASED_TRUE
BUILD_MD4_FALSE
BUILD_MD4_TRUE
BUILD_SHA_FALSE
BUILD_SHA_TRUE
BUILD_MD5_FALSE
BUILD_MD5_TRUE
BUILD_RC4_FALSE
BUILD_RC4_TRUE
BUILD_DES3_FALSE
BUILD_DES3_TRUE
BUILD_CODING_FALSE
BUILD_CODING_TRUE
BUILD_AES_FALSE
BUILD_AES_TRUE
BUILD_ASN_FALSE
BUILD_ASN_TRUE
BUILD_DH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILD_RSA_TRUE
BUILD_MEMORY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_SHA512_TRUE
BUILD_BLAKE2_FALSE
BUILD_BLAKE2_TRUE
BUILD_RIPEMD_FALSE
BUILD_RIPEMD_TRUE
BUILD_MD2_FALSE
BUILD_MD2_TRUE
BUILD_CAMELLIA_FALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALSE
BUILD_POLY1305_TRUE
BUILD_AESNI_FALSE
BUILD_AESNI_TRUE
BUILD_AESCCM_FALSE
BUILD_AESCCM_TRUE
BUILD_AESGCM_FALSE
BUILD_AESGCM_TRUE
BUILD_SNIFFTEST_FALSE
BUILD_SNIFFTEST_TRUE
BUILD_SNIFFER_FALSE
BUILD_SNIFFER_TRUE
BUILD_LEANPSK_FALSE
BUILD_LEANPSK_TRUE
PTHREAD_CFLAGS
PTHREAD_LIBS
PTHREAD_CC
ax_pthread_config
DEBUG_FALSE
DEBUG_TRUE
MCHECK
LIBM
am__fastdepCCAS_FALSE
am__fastdepCCAS_TRUE
CCASDEPMODE
CCASFLAGS
CCAS
AM_BACKSLASH
AM_DEFAULT_VERBOSITY
HAVE_VISIBILITY
CFLAG_VISIBILITY
CXXCPP
am__fastdepCXX_FALSE
am__fastdepCXX_TRUE
CXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOOL64
OTOOL
LIPO
NMEDIT
DSYMUTIL
lt_ECHO
RANLIB
AR
OBJDUMP
LN_S
NM
ac_ct_DUMPBIN
DUMPBIN
LD
FGREP
EGREP
GREP
SED
am__fastdepCC_FALSE
am__fastdepCC_TRUE
CCDEPMODE
AMDEPBACKSLASH
AMDEP_FALSE
AMDEP_TRUE
am__quote
am__include
DEPDIR
OBJEV
	export CONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_switch-enumCONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esac
  test -d "$as_undefprefix}/libexec'
datarootdir='${prefix}/share'
datadir='${datarootdir}'
sysconfdir='${prefix}/etc'
sharedstatedir='${prefix}/com'
localstatedir='${prefix}/var'
includedir='${prefix}/include'
oldincludedir='/usr/include'
docdir='${datarootdir}/doc/${PACKAGE_TARNAME}'
infodir='${datarootdir}/info'
htmldir='${docdir}'
dvidir='${docdir}'
pdfdir='${docdir}'
psdir='${docdir}'
libdir='${exec_prefix}/lib'
localedir='${datarootdir}/locale'
mandir='${datarootdir}/man'

ac_prev=
ac_dashdash=
for ac_option
do
  # If the previous option needs an argument, assign it.
  if test -n "$ac_prev"; then
    eval $ac_prev=\$ac_option
    ac_prev=
    continue
  fi

  case $ac_option in
  *=*)	ac_optarg=`expr "X$ac_option" : '[^=]*=\(.*\)'` ;;
  *)	ac_optarg=yes ;;
  esac

  # Accept the important Cygnus configure options, so we can diagnose typos.

  case $ac_dashdash$ac_option in
  --)
    ac_dashdash=yes ;;

  -bindir | --bindir | --bindi | --bind | --bin | --bi)
    ac_prev=bindir ;;
  -bindir=* | --bindir=* | --bindi=* | --bind=* | --bin=* | --bi=*)
    bindir=$ac_optarg ;;

  -build | --build | --buil | --bui | --bu)
    ac_prev=build_alias ;;
  -build=* | --build=* | --buil=* | --bui=* | --bu=*)
    build_alias=$ac_optarg ;;

  -cache-file | --cache-file | --cache-fil | --cache-fi \
  | --cache-f | --cache- | --cache | --cach | --cac | --ca | --c)
    ac_prev=cache_file ;;
  -cache-file=* | --cache-file=* | --cache-fil=* | --cache-fi=* \
  | --cache-f=* | --cache-=* | --cache=* | --cach=* | --cac=* | --ca=* | --c=*)
    cache_file=$ac_optarg ;;

  --config-cache | -C)
    cache_file=config.cache ;;

  -datadus$as_nl\\(.*\\)"`;;
      esac;
      expr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
    export as_echo_n_body
    as_echo_n='sh -c $as_echo_n_body as_echo'
  fi
  export as_echo_body
  as_echo='sh -c $as_echo_body as_echo'
fi

# The user is always right.
if test "${PATH_SEPARATOR+set}" != set; then
  PATH_SEPARATOR=:
  (PATH='/bin;/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 && {
    (PATH='/bin:/bin'; FPATH=$PATH; sh -c :) >/dev/null 2>&1 ||
      PATH_SEPARATOR=';'
  }
fi


# IFS
# We need space, tab and new line, in precisely that order.  Quoting is
# there to prevent editors from complaining about space-tab.
# (If _AS_PATH_WALK were called with IFS unset, it would disable word
# splitting by setting IFS to empty value.)
IFS=" ""	$as_nl"

# Find who we are.  Look in the path if we contain no directory separator.
case $0 in #((
  *[\\/]* ) as_myself=$0 ;;
  *) as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for as_dir in $PATH
do
  IFS=$as_save_IFS
  test -z "$as_dir" && as_dir=.
    test -r "$as_dir/$0" && as_myself=$as_dir/$0 && break
  done
IFS=$as_save_IFS

     ;;
esac
# We did not find ourselves, most probably we were run as `sh COMMAND'
# in which case we are not to be found in the path.
if test "x$as_myself" = x; then
  as_myself=$0
fi
if test ! -f "$as_myself"; then
  $as_echo "$as_myself: error: cannot find myself; rerun with an absolute file name" >&2
  exit 1
fi

# Unset variables that we do not need and which cause bugs (e.g. in
# pre-3.0 UWIN ksh).  But do not cause bugs in bash 2.01; the "|| exit 1"
# suppresses any "Segmentation fault" message there.  '((' could
# trigger a bug in ac_use-s_echoropt_orig=$ac_useropt
    ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case $ac_user_opts in
      *"
"enable_$ac_useropt"
"*) ;;
      *) ac_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_sep--enable-$ac_useropt_orig"
	 ac_unrecognized_sep=', ';;
    esac
    eval enable_$ac_useropt=\$ac_optarg ;;

  -exec-prefix | --exec_prefix | --exec-prefix | --exec-prefi \
  | --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \
  | --exec | --exe | --ex)
    ac_prev=exec_prefix ;;
  -exec-prefix=* | --exec_prefix=* | --exec-prefix=* | --exec-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* | --ex=*)
    exec_prefix=$ac_optarg ;;

  -gas | --gas | --ga | --g)
    # Obsolete; use --with-gas.
    with_gas=yes ;;

  -help | --help | --hel | --he | -h)
    ac_init_help=long ;;
  -help=r* | --help=r* | --hel=r* | --he=r* | -hr*)
    ac_init_help=recursive ;;
  -help=s* | --help=s* | --hel=s* | --he=s* | -hs*)
    ac_init_help=short ;;

  -host | --host | --hos | --ho)
    ac_prev=host_alias ;;
  -host=* | --host=* | --hos=* | --ho=*)
    host_alias=$ac_optarg ;;

  -htmldir | --htmldir | --htmldi | --htmld | --html | --htm | --ht)
    ac_prev=htmldir ;;
  -htmldir=* | --htmldir=* | --htmldi=* | --htmld=* | --html=* | --htm=* \
  | --ht=*)
    htmldir=$ac_optarg ;;

  -includedir | --includedir | --includedi | --included | --include \
  | --includ | --inclu | --incl | --inc)
    ac_prev=includedir ;;
  -includedir=* | --includedir=* | --includedi=* | --included=* | --include=* \
  | --includ=* | --inclu=* | --incl=* | --inc=*variablALSE
BUILD_INLINE_TRUE
BUILD_FIPS_FALSE
BUILD_FIPS_TRUE
BUILD_CHACHA_FALSE
BUILD_CHACHA_TRUE
BUILD_RABBIT_FALSE
BUILD_RABBIT_TRUE
BUILD_HC128_FALSE
BUILD_HC128_TRUE
BUILD_PWDBASED_FALSE
BUILD_PWDBASED_TRUE
BUILD_MD4_FALSE
BUILD_MD4_TRUE
BUILD_SHA_FALSE
BUILD_SHA_TRUE
BUILD_MD5_FALSE
BUILD_MD5_TRUE
BUILD_RC4_FALSE
BUILD_RC4_TRUE
BUILD_DES3_FALSE
BUILD_DES3_TRUE
BUILD_CODING_FALSE
BUILD_CODING_TRUE
BUILD_AES_FALSE
BUILD_AES_TRUE
BUILD_ASN_FALSE
BUILD_ASN_TRUE
BUILD_DH_FALSE
BUILD_DH_TRUE
BUILD_RSA_FALSE
BUILD_RSA_TRUE
BUILD_MEMORY_FALSE
BUILD_MEMORY_TRUE
BUILD_ECC_FALSE
BUILD_ECC_TRUE
BUILD_DSA_FALSE
BUILD_DSA_TRUE
BUILD_SHA512_FALSE
BUILD_SHA512_TRUE
BUILD_BLAKE2_FALSE
BUILD_BLAKE2_TRUE
BUILD_RIPEMD_FALSE
BUILD_RIPEMD_TRUE
BUILD_MD2_FALSE
BUILD_MD2_TRUE
BUILD_CAMELLIA_FALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALSE
BUILD_POLY1305_TRUE
BUILD_AESNI_FALSE
BUILD_AESNI_TRUE
BUILD_AESCCM_FALSE
BUILD_AESCCM_TRUE
BUILD_AESGCM_FALSE
BUILD_AESGCM_TRUE
BUILD_SNIFFTEST_FALSE
BUILD_SNIFFTEST_TRUE
BUILD_SNIFFER_FALSE
BUILD_SNIFFER_TRUE
BUILD_LEANPSK_FALSE
BUILD_LEANPSK_TRUE
PTHREAD_CFLAGS
PTHREAD_LIBS
PTHREAD_CC
ax_pthread_config
DEBUG_FALSE
DEBUG_TRUE
MCHECK
LIBM
am__fastdepCCAS_FALSE
am__fastdepCCAS_TRUE
CCASDEPMODE
CCASFLAGS
CCAS
AM_BACKSLASH
AM_DEFAULT_VERBOSITY
HAVE_VISIBILITY
CFLAG_VISIBILITY
CXXCPP
am__fastdepCXX_FALSE
am__fastdepCXX_TRUE
CXXDEPMODE
ac_ct_CXX
CXXFLAGS
CXX
CPP
OTOOL64
OTOOL
LIPO
NMEDIT
DSYMUTIL
lt_ECHO
RANLIB
AR
OBJDUMP
LN_S
NM
ac_ct_DUMPBIN
DUMPBIN
LD
FGREP
EGREP
GREP
SED
am__fastdepCC_FALSE
am__fastdepCC_TRUE
CCDEPMODE
AMDEPBACKSLASH
AMDEP_FALSE
AMDEP_TRUE
am__quote
am__include
DEPDwrite-IR
Ong CONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esac
  test -d "$asfwrapvm-trans | --program-tran \
  | --progr-tra | --program-tr | --program-t)
    ac_prev=program_transform_name ;;
  -program-transform-name=* | --program-transform-name=* \
  | --program-transform-nam=* | --program-transform-na=* \
  | --program-transform-n=* | --program-transform-=* \
  | --program-transform=* | --program-transfor=* \
  | --program-transfo=* | --program-transf=* \
  | --program-trans=* | --program-tran=* \
  | --progr-tra=* | --program-tr=* | --program-t=*)
    program_transform_name=$ac_optarg ;;

  -pdfdir | --pdfdir | --pdfdi | --pdfd | --pdf | --pd)
    ac_prev=pdfdir ;;
  -pdfdir=* | --pdfdir=* | --pdfdi=* | --pdfd=* | --pdf=* | --pd=*)
    pdfdir=$ac_optarg ;;

  -psdir | --psdir | --psdi | --psd | --ps)
    ac_prev=psdir ;;
  -psdir=* | --psdir=* | --psdi=* | --psd=* | --ps=*)
    psdir=$ac_optarg ;;

  -q | -quiet | --quiet | --quie | --qui | --qu | --q \
  | -silent | --silent | --silen | --sile | --sil)
    silent=yes ;;

  -sbindir | --sbindir | --sbindi | --sbind | --sbin | --sbi | --sb)
    ac_prev=sbindir ;;
  -sbindir=* | --sbindir=* | --sbindi=* | --sbind=* | --sbin=* \
  | --sbi=* | --sb=*)
    sbindir=$ac_optarg ;;

  -sharedstatedir | --sharedstatedir | --sharedstatedi \
  | --sharedstated | --sharedstate | --sharedstat | --sharedsta \
  | --sharedst | --shareds | --shared | --share | --shar \
  | --sha | --sh)
    ac_prev=sharedstatedir ;;
  -sharedstatedir=* | --sharedstatedir=* | --sharedstatedi=* \
  | --sharedstated=* | --sharedstate=* | --sharedstat=* | --sharedsta=* \
  | --sharedst=* | --shareds=* | --shared=* | --share=* | --shar=* \
  | --sha=* | PIECONFIG_SHELL
	exec "$CONFIG_SHELL" "$as_myself" ${1+"$@"}
fi

    if test x$as_have_required = xno; then :
  $as_echo "$0: This script requires a shell more modern than all"
  $as_echo "$0: the shells that I found on your system."
  if test x${ZSH_VERSION+set} = xset ; then
    $as_echo "$0: In particular, zsh $ZSH_VERSION has bugs and should"
    $as_echo "$0: be upgraded to zsh 4.3.4 or later."
  else
    $as_echo "$0: Please tell bug-autoconf@gnu.org and
$0: https://github.com/cyassl/cyassl/issues about your
$0: system, including any error possibly output before this
$0: message. Then install a modern shell, or manually run
$0: the script under such a shell if you do have one."
  fi
  exit 1
fi
fi
fi
SHELL=${CONFIG_SHELL-/bin/sh}
export SHELL
# Unset more variables known to interfere with behavior of common tools.
CLICOLOR_FORCE= GREP_OPTIONS=
unset CLICOLOR_FORCE GREP_OPTIONS

## --------------------- ##
## M4sh Shell Functions. ##
## --------------------- ##
# as_fn_unset VAR
# ---------------
# Portably unset VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -----------------------
# Set $? to STATUS, without forking.
as_fn_set_status ()
{
  return $1
} # as_fn_set_status

# as_fn_exit STATUS
# -----------------
# Exit the shell with STATUS, even in a "trap 0" or "set -e" context.
as_fn_exit ()
{
  set +e
  as_fn_set_status $1
  exit $1
} # as_fn_exit

# as_fn_mkdir_p
# -------------
# Create "$as_dir" as a directory, including parents if necessary.
as_fn_mkdir_p ()
{

  case $as_dir in #(
  -*) as_dir=./$as_dir;;
  esacted bydev/nu=c
ac_cpp='$CPP t $a://gi'    e------ort C -c_VERSION$_envvar ;\\\\\\\\\\\\\\\\\and ;

  link# FIXMEo\\\\\\\\\/dev/ne.0.
should be removed $LDmoved in autoconf 3.0.
$LIBS
    $as_ASE=1; e_gnu=as_ed mon" : ".*[^-._ndat# echo to ws2_32astion mingw
"$BAS$host_osSH_VERSIOme: W32oconf 2.  st, --t="ost, --ta-las_ech"if te scr
# add usDUAL_EXTRA_moved back
#
# Copyright (C)USER_$ac_option} $"
OPTIONion} $="on}
    ;;

  esac
 $AM_s://gi"ndat  HEX_VERSION=`in/sh$_prev | | sed 's|\-a-z0-9*$||' | \ar=\$awk -F. '{printf "0x%0.2d%0.3est -n", $1, $2, $3}'`ndatll &&FINALtion" nfig_files=$as_eenable_optio stamp-h"
se $enable_option_checking in
    nMakeopti   fatal) as_fn_error "unrecognized ocyassl/version.    fatal) as_fn_error "unrecognized o$as_echoptionsme: WARNING: unrecognized options: $ac_support/$as_ec.pc   fatal) as_fn_error "unrecognized orpm/spefor 
# create a generic PACKAGE-enable opti
L sed 's/xecdir pr "x$`
P sed 's/L
  as_f-e 's/ -.*//'sharedstateP`
V sed 's/_/-/g'``
Fedir \
		dataroo`\
		datad-ledir localstate^-llib/-lnclus_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$andir mpat$F -ndir libe$V ion
$L # for MKS sh
 "iling slashes.
  case $ac_val in
  /nullng forx$prefixs,
# NONE && c_val"n_checdefault_c_val" ' \| "X$aexec=\$ac_va: 'X\(.*\)'`c
  # Be su='${c_val"}'
in/sh'#! /bin/sh' >$F
  case ' >    [\\/$]package="'$P'" | ?:[\\/]* )o "$as_nue;V
    NONE | '' libsnue;L
    NONE | '' * | ?:[\#ves the order of occurencsbinstandard automakeoptions: \\/]* )       e'ac_val"
    NONE | '' solute direc"'ac
  # Be su
    NONE | '' bindirpendr: `$h
    NONE | '' sr: `$host'rgumentar in *prefix ) coc
  `$host'o remove s
    NONE | '' data`$host'ost=$hod to hold the arysB_SUBnt of -as

# FIXd to hold the arharediableFIXME: Tt "x$host_aliar in *prefix ) ocalhost_alias" _alias" = x; ar in *prefix ) coe some day_compiling=maybe
 infoFIXME: f you w
    NONE | '' ma `$host'ype, d_me: WARNING: If clud_alias"  cross com
    NONE | '' targetpendcompil
    NONE | ''  invpend inv broken behavioruildost'
 != ;; esac;;
  esac
  as_fING: Ifting for'"\$""#"'" -eqGeneo wo esac;;
  esacNO_GLO<<EOF | ?:[\\/]* )Usage: $  contidatarootne

ifS | ?:[\\/]* )Oized_o:
test -n "$host--_val"
dDIR) epenhe old /null


ac_pwd=`p contist -n " contiwd" &&
ac_ls_di=`o "$as_st -n o "$as_wd" &&
ac_ls_di=`it.
##st --I\er is detect/null


ac_pwd=` conirectL\ho "$as -l&
ac_pwd_ls_di=`cd "$ac_pwhelp) tion" allror "eized_o (not justefixse) determined"
 && ac_tool_preffias"; then
    cross_compilio=" be used." >&2
fied.
if test -zion
ives "$@"CONF determined"
teRNING:_srcv/null


ac_pwd=`pwd` &&*pwd direcsed 's/iir localst"s/=`pwd` &&//"` ;;v/null


ac_pwd=`pwd` &)n" >o="$o ac_val" :=`$as_dirname -- "$as -di .`" ||
$as_e conti"$as_myself" : 'X\(.*d" && ls " ||
$as_o "$as_"$as_myself" : 'X\(.*king dirng=yes
  _er is detec" != "_/usr/ cross ; esac;;
  esac X"$as_myo wor||
$as-Ielf" : 'X\(/\; les, if location$as_myself" : 'X\(.* "X$ac	  }
|
    sLi" = "X$ days"$as_myself" : 'X\(.*c
  # Be su|--eas_mysel||
$as_c
  # Be sure=`$as_dirname -- "$r: `$h
	  }
	
	    q
	 ||
$as_r: `$h	  }
	  /^X\(\/\).*/{rguments//\1/
	    q
	 ||
$as_rgument	  }
	  /^X\(\/\).*/{o remove ss//\1/
	    q||
$as_o remove s	  }
	  /^X\(\/\).*/{ost=$hoif test ! -r "$srcdir/$ost=$hono
fi
if test ! -r "$srcdincif test ! -r "$srcdir/-I
  test "$ac_srcdir_defaulted" = lib && srcdir="$ac_confdirL
  test "$ac_srcdir_defaultedTo remove fi
else
  ac_srcdir_To remove ir"
fi
ac_msg="sourcest "x$host_alis//\1/
	 `cd $srct "x$host_aliue_file"; then
    srcalias" = x; s//\1/
	  srcdir_dalias" = x; ue_file"; then
    srcdi   s//\1/
	    q
	  }
	  s/st "$aue_file"; then
    srcdiadds//\1/
	    q
	  }
	  s(\/\/\)[^	  }
	  /^X\(\/\).*/{f you wif test ! -r "$srcdir/$f you w	  }
	  /^X\(\/\).*/{ype, ds//\1/
	    q
	  }
	  s/ype, d	  }
	  /^X\(\/\).*/{compils//\1/
	    q
	  }
	  s/compil	  }
	  /^X\(\/\).*/{ invs//\1/
	    q
	   When bui inv	  }
	  /^X\(\/\).*/{
 != c_var in $ac_preci}
	  s/. != no
fi
if test ! -r "$srcdc_var in $ac_precious_varr or .."
 /*/*$' \| \
	 X"$as_myself" : pkgsrcdir/$ac_unique_fi; then
  test set=\${${ac_var}+set}
  eval ac_cv_enyes && srcdir="$v_env_${ac_var}_set=\${${ac_var}+set}
  eval ac_cv_en sources ($ac_une_file) in $srcdset=\${${ac_var}+set}
  eval ac_cst "$ac_abs_confdir""$ac_pwd"; thless imposing.
  # This message is tyes && srcdir="$av_env_${afigureless imposing.
  # This message is te unnecessary trang slashes faddset=\${${ac_var}+set}
  eval ac_c cross com	pwd)`
# When bui cross comset=\${${ac_var}+set}
  eval aor "pwh="1	  }
	  /^X\(\/\).*/?//*|-?/*ful va.riableefauliable

Defa)* | ?:[\\/]* )
	    qvn the parent directs://:\$:g`s are specified in brack  NULed 's/v"`ns are specified in b]' \| \
	  }ns are specifiede scbuild_alias
hoon_ls_di=`cd "$ac was not specifisplay this ho", --help      ve    display the short help of   display theackage
      --help=recursing=yes
 ! -z "$h^/]\o wor determined"
alias" && ac_tool_prefwd=`pwd` &&xxself"   (what is t    ion
anyway?ectory"


# Fi- "$as_myself" [disan "$ac_pE [disab "$ac_pwd" &&
ac_ls_di=`ls -di .` [disabled conti, --confic_pwd_ls_di=`cd "$ac_pwd" && ls  [disableo "$as_, --conf.` ||
  as_fn_error "working dirrces in tory cannot bercesunless i  --c . 2>/dev/nu/{
	    s//\1/
	    q
	  }
	 in DI_di" = "X$ac_pr syscon\r "x$  s//\1/
	    q
	  }
	  /^X\) orh) >  s//\1/
	    q
	 s in FILE [disa\/\1/
	    q
conf on the old 	  /^X\(\/\).*/{
	    s//\1/
	  \/.*/./;C, --conf-host e  srcdir=$ac_confdir
  if test !\$ac_uniq, --conf--host ele"; then
    srcdir=..
  fi
elsdi" =emove s-confday.
build=
if test ! -r "$srcdir/$ac_uniqu\
  test , --conflias
tari
ac_msg="sources are in $srcdir\rcdir' doeske inso remove swork"
ac_abs_confdir=`(
	cd "$sr\ -r "./$ac_uniq != x; then
  ifle" || as_fn_error "$ac_msg"
	pw\ilding in placuilding in placet srcdir=.
if test "$ac_abs_confdi" = "X$, --confo "$as_ir.
# Double slashes in file namry cyou w, --confd to setg info
# mess up M-x gdb in Emac\ir in
* [EPREFIX use --cdir=`expr "X$srcdir" : 'X\(.*[^\rcdir"  [EPREFIXde will(.*\)'`;;
esac
for ac_var in $ac\s; do
	    q
	  test al ac_env_${ac_var}_set=\${${ac_\val acstatedir=Dost_al${ac_var}_value=\$${ac_var}
  evtorye list l&
ac_pwd_ls_di=`cd "$ac_pwc_cv_env_${ac_vale single-machine data [PREFIX/var]
  -st "$ac_abs_cdi" = "X-machine data [PREFIX/var]
  -s (e.g., CC, ry cannot be-machine data [PREFIX/var]
or "pwr non-gcc dir lst_a by contdir s_dir libcking i.m4
  --localstatedI//varname and oe
DUAincrcdir" s like message.
#
iectory ae]
  --localstatedLndependent data rootlibREFIX/sh, e.g.rnal or obsol or Remove uctory"


# Find the source files, if lGENERIC_CONFIG="$F--`ecvar=\$ac# -------------------- ##

# Be more Bourne compator "numbected availecur CPU$as_echo$as_echif test -n "$ir=DIR            man documeh) >/dev/nullre FounPU_COUNT="0"var=\$acRNING: invalid h #(var=\$ac  *darwin*) :        htng=yes
 -xnstallrgum/\`--tl do word        ht        _a=`idir=DIR         -a 2>/dev/null| grepME: hw.cpu`ocumentationg=yes
 n [DOCDI    dvi documentatioDATAROOTDIR/]
  --pdfdir=DIR   ntionn [DOC the fi              htmlinuxmentation [DOCDIR]
  -"x$TAROOTDIRre t"x0"    -e /proc/cpumin     dvi documentatio   prepend PREFIX to installed program names
  --program-suffix=SUFFn [DOCDIR]
_A$EGREPME: '^am nessor'gram names
  -ram namesocumentati
  --pAutord sp  if ton} ${x=SUFFIX            append SUFFIX     dvi documentatTAROOTDIR/d1 This co$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$aunan doto detect (assume:  1}, whicho$as_echo$
_ACEOF
fi

if test -n "$ac_init_h/nullh
  # is echo$as_echo$as_echo$as_echo
as_echo=$as_echo$as_echo$asTAROOTDIRnit_help"; then
 <\_ACEOF

Optio| recuthe 
AX_DOLLAR=i
fikingSRB="\\135nrecogLized --3nrecoBSzed \\nrecoDQ="\"fix  for cyassl 3.3.2.
#
# Report bugs to <htadd"$ac name formacroectory aptional Features: Guessable-FEATURE=no)
  --enable-FEATUR6;}
AMINCLUDE="am cross .am"      do not include FEATURE (same as --enailing slasS]  buildATURE[=ARG]  include FEAuild shared libraries [=PKGS]  build_TIME=`datepts"tion"
fi
#[/usr/inclu name ticallyude]enableure from AX_AUTOMAKE_MACROS
#$as_ed librari     

" > "ed librarie


INC_S]  build s cross  \$(top_al ac, CC/ld
  --enable-       # Cdifytible
DUA--e_ACEO-jobserver was given.r a ksh s"${ool-lo_k  avoid ash or zsh.
if test -z ool-lo overrallel builds)
 ;ris 7 /usallel builds)
  Sol-checSUFFIX         undo: `make V=1' instaSola  -q,  ) echo "let(undo: `make V=1')<\_ACEOF

+1sable-disable-silent-rules         verbose)' \xnooutput (undo: `m    do not include FEATURE (same as --enableed k  avoid lctory aF
fie for_val undo: `make V=1'aded ATURE[=ARG]  include FEATURreaded Enable CyaSSL single threaded (default: disabled)
  =PKGa   tion"
fiAM_ndenhen
  += -jded (default: disa\n" >build
  --enable-  Add de
_GLOset -1+"$@ <<\;;
esac# Thistdir  --ca shell scriping GLOB+"$@-caceas_echosted =yes]
  -
# ksh s run$as_tfaulsystem so    y can be -r "./ between fortress bui-enablst dat=yes]
  --runs, se\\\\\\]
  -'sport na\)$'e-lea-ress .
# I  --cof wuseful$as_a rootled)
 s.  If.']
 as_echo=Enable Syou don't
# wanL sinkeep,g sesmay remov "ex ed']

t.
# binR     iles.
#only pays atten    Et-enabtress lt: disfg sesng ( i cact: d--reodify       Eto refaulle-leanpsache  `s_cr_aenv_foo' no-recurs (setnabluns" : willmp  s='0123dentibln &&
oae-FEAdisabopti,ed)
  -* Enab*disabled--ena persistashen
edche 
# follow"$acvalues.
 ;;
esac (def thrache (dewayted 
  |n]
  --disablemishandvesenewes f=DIR fault:,
# but we knowted no workarounent    --csimpled)ry aecur,t datefficiencach So,
  --persble-savese as_ech"$acLayer (ddisaUltrix shs of    E    o std\\\\ault:an'tmp  r_aliecnclue-aesglysabl dats/shat: dhigh b']

rror "isabled)
  or \`..we persisfault: dvars.
em tfile=ce-4.port`ssio, 1991ir localn	libd\([a-zA-Z_]abled)
0-9_]*\)=.*/\1/p'`CONFIG_/usr/bi CyaSl_echoCyaSSe CyaRNING:ES-NI DIR      htm valunl}toconf 2.t: disabledSL AE-enable  *cr_a*)             [default=no]
  --enable-singlWARNING:efault:no-recurle CyaSSL as_echo=aEnable CATURE[=ARG]  include FEA   Enable CyaSSL Camellia support (default: disabled)
2;}if test ure scr      Enable CyaSSL POLY1305 su_ | IFS |sionnl)
System tled)BASH_ARGV |
  --eSOURCE)usr/biport (d=t: disabled)
 (def Enable CyaSS;  Enabble CyaS-enable-nullciphif test cipher _envvar=-CCM supportefault: disaefau`(varipace=' ';CyaS suppo`
  --enable-poly1305
  --enab\utoconf 2.# `singldord Lot quot\\\\r      E --est_aenabls: dou-locenabl(default:substituall seurho='\\\ into \\acks (as_fed)
  --able-ke.abled)
(defaul\
	"s/'/'--en''/g;as_e: di\([_isabcr_alnum]pporttgen        Enab\\)=\\(.(def/\\1='\\2'/p This confOLY1305 t (default: disabable se-sessionc aEnabqui     y POSIXerts do
  --  Enable sneration (defau"/^tgen        Enable cert generation (=-enable-cert  Enable  (defausort
) (defas_fnabled)/^sabled)
  /b in abled)t cle(defaul :le-dsa     : disa^=sni  lt: [{}].*\)$/break pa\1ash or zsh.
 || &/  --enabd)
  --ene DSA (default: dd)
 \1=ble-=\2}able EC:en     ble testil 2>diff "$ress _opti"ache isable      pdf suppo do word; ipt is DIR]
  --wC (default: diutput (undoprepend default: dis(yes|     pdf " &&      locale-dependent data [DATAROOTDIR/locale]updng slaisablesk           URE[=ARG]  include FEArror strings table (default: e=PKG    EnLOB_SUd)
  --sk         es  pt is               [default=no]
  --enable-singlfaultror striun
  |mellings table (default: enabled)
  --enable-oamples (default: disabled)
  --enable-memory ersionfi='\\\\\\\\\\ Point c' \| "X$ac_val" : 'X\(.*\)'`
      val $ac_var=\$ac_v
# Letngle texpks (d
  # Be su.l;;
  esac
  # Be sure to have absolute directory names.

DEFS=-DHAVE-locale_H  fatlibobjs=le AStSN (defauable Cy_srcd:pr "xOBJSCONFenable-psnablre to:\)'` as_inu  --# 1. R  --ent: d evan$as_acks ($UCC e; then
 installedneravarienabl='s/\$U\././;s/\.o$/s3     bj$//portnabl, distributeS (defa  as_fed)
 ed)
  "OCDI# 2. Pre----       DIR.  W wor_use withATURE=no)>=1.10(default:    En    Enable     ault: de-aesgorytiblre(defaulS \\\\cdefare al at: enas
as_------ (deSN (def " \${default: }S (de\$U\\\\\\\\\\\ Thiabled)
  --enableenabled)          Enable MD4 "'$U.lo'e Foun            SN (def

LTbled)
  --enaenabled)pv6 DIR]
  --cdirEXEEXTabled)
  -am__abled)_TRUE=able-hc128    FALSE='#'ript is e-hc128         ult:Enable HC-128 (defa-checDIR]
  --d ex{AMDEP     }efauefault: disabled)
 (def}
  --enablebled)error "condized_al \"bled)\"lockinedisadefinlt: Usu[defadisabmeanSL AES  --enockie sesinvokelt: d
  --enaly." as_nl
# P 5-che(default: disae-hcfat: dpCC
  --enable-chacha    bled)
  --enabnable CHACHA (default: disabled)
  --enablebled)
  --ena           Enable FIPS 140-2 (default: disabled)
  --enable-hashdrbg       Enable Hash DRBG support (default: enabled)
  --enXXble-filesystem     Enable FilesystXXm support (default: enabled)
  --enable-inline         EXX           Enable FIPS 140-2 (default: disabled)
  --enable-hashdrbg       Enable Hash DRBG supportt (default: enabled)
  --enable-filesystem     Enable Filesystem support (default: enabled)
  --enable-inline         Enable inline functions (default: enabled)
  --enable-ocsp           Enable OCSP (default: disabled)
  --enable-crl            Enable CRL (default: disabled)
  --enable-crl-monitor    Enable CRL Monitor (default: disabled)
  --enable-sni            Enable SNI (default: disabled)
  --enable-maxfragment    Enable Maximum F (default: enabled)
  --enaASble-filesystem     Enable FilesysteASm support (default: enabled)
  --enable-inline         EnAS           Enable FIPS 140-2 (default: disabled)
  --enable-hashdrbg       Enable Hash DRBG support (default: enaDEBUG
  --enable-chacha    abled)nable CHACHA (default: disabled)
  --enableabled           Enable FIPS 140-2 (default: disabled)
  --enable-hashdrbg       Enable Hash DRBG support (default: enaBUILD_LEANPSK
  --enable-chacha    le (default: dnable CHACHA (default: disabled)
  --enablele (default: d)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (deSNIFFERdisabled)
  --enable-certserault: diEnable cert service (default: disabled)
  --enablault: dath       Enable fast math ops (default: enabled on x86_64)
  --enable-fasthugemath   Enable fast math + huge code (default:TES      ed)
  --enable-examples     to # nable Examples  (default: enabled)
  --enable-mcapi   to #d)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (deAESGCM make jobs
                 --with-Enable cert service (default: disabled)
  --enabl--withwith-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --wCth-pic              try to use onnstaIC/non-PIC objects [default=use
                    C     both]
  --with-gnu-ld           assume the C compiler uses GNU ld [default=no]
  --with-ntru=PATH        Path to NTRU iNI-pic              try to use onNIPATH        PATH to libz install (default /usr/)
  --NId)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (dePOLY1305 make jobs
                 y>
  CPPFEnable cert service (default: disabled)
  --enably>
  CPPd)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (deCAMELLIA make jobs
                 LAGS    CEnable cert service (default: disabled)
  --enablLAGS    d)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (deMD2 make jobs
                 madeEnable cert service (default: disabled)
  --enablMD2d)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (deRIPEMD make jobs
                 OF
ac_sEnable cert service (default: disabled)
  --enablOF
ac_d)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (deBLAKEde by `configure' or to help

      Enable cert service (default: disabled)
  --enabl
     ath       Enable fast math ops (default: enabled on x86_64)
  --enable-fasthugemath   Enable fast math + huge code (defaHA51de by `configure' or to help
as_echonable Examples  (default: enabled)
  --enable-mcaps_echd)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (deDS C++ compiler flags
  CXXCPP uildEnable cert service (default: disabled)
  --enablDSefaults to CC)
  CCASFLAGS   assembler compiler flags (defaults to CFLAGS)

Use these variables to override the choices Enable-filesystem     Enaac_top_buiEnable cert service (default: disabled)
  --enablEEnable inline functions (default: enabled)
  --enable-ocsp           Enable OCSP (default: disabled)
  --enable-crlard namEMORYe by `configure' or to help
ie.
   o find libraries and programs with nonstandard name.
  /locations.

Report bugs to <https://github.com/cyassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOilddir_sub=. ac_top_build_prefx$srp" = "recursive"; then
  # If there are subdirs, r ;;
  esac ;;
esac
ac_abs_top_builddir=$ac_pwd
ac_abs_builddir=$ac_pwd$ac_dir_suffix
# for backward compatibility:
ac_topDHddir_sub=. ac_top_build_prefiH ;;
  *)  ac_top_build_prefix=$ac_top_builddir_sub/Hwith-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  -SN-pic              try to use SNPATH        PATH to libz install (default /usr/)
  SNwith-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --w-pic              try to use onPATH        PATH to libz install (default /usr/)
  --headers in a nonstandard directory <include dir>
  CPP         C preprocessor
  CXX         C++ compiler command
  CXXFLODINd)
  --enable-valgrind ; the Free Sopreprocessor
  CCAS        assembler compiler commree S$ac_dir_suffix

    cd "$ac_dir" || { ac_status=$?; continue; }
    # Check for guested configure.
    if test -f "$ac_sES3ddir_sub=. ac_top_build_prefipile;;
  *)  ac_top_build_prefix=$ac_top_builddir_sub/ES3/locations.

Report bugs to <https://github.com/cyassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOC4rcdir
    ac_abs_top_srcdir=$C4_pwd/$srcdir ;;
esac
ac_abs_srcdir=$ac_abs_top_srcdC4faults to CC)
  CCASFLAGS   assembler compiler flags (defaults to CFLAGS)

Use these variables to override the choices maPFLAGS    (Objective) C/C++ pENO}o find libraries and programs with nonstandard name headers in a nonstandard directory <include dir>
  CPP         C preprocessor
  CXX         C++ compiler command
  CXXFSH C++ compiler flags
  CXXCPP &5
  # A ".." for each directory in $ac_dir_suffix.
  acfaults to CC)
  CCASFLAGS   assembler compiler flags (defaults to CFLAGS)

Use these variables to override the choices matack
  rm -f conftest.$ac_obj teso find libraries and programs with nonstandard name *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
  *) ac_try_echo=$ac_try;;
esac
eval ac_try_echo="\"\$as_me:${as_lineno-$LINPWDBASE_status=$?
fi

if test "$ac_i && { as_or flags, e.g. -I<include dir> if
              yo&& { asc_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute naHC128 make jobs
                 y_linkEnable cert service (default: disabled)
  --enably_lin/locations.

Report bugs to <https://github.com/cyassl/cyassl/issues>.
cyassl home page: <http://www.wolfssl.com>.
_ACEOABBI# make jobs
                 y_echo=p" = "recursive"; then
  # If there are subdirs, r_echoheaders in a nonstandard directory <include dir>
  CPP         C preprocessor
  CXX         C++ compiler command
  CXXFLHAC5
    mv -f conftest.er1 conftest.errpreprocessor
  CCAS        assembler compiler commst.erac_builddir=.

case "$ac_dir" in
.) ac_dir_suffix= ac_top_builddir_sub=. ac_top_build_prefix= ;;
*)
  ac_dir_suffix=/`$as_echo "$ac_dir" | sed 's|^\.[\\/]||'`
  # A ".." for each directory in $ac_dir_suffix.
  ac_top_builddir_sub=`$as_echo "$ac_dir_suffix" | sed 's|/[^\\/]*|/..|g;s|/||'`
  case $ac_top_builddir_sub in
  "") ac_top_bas_echo "$ac_dir" | sed 's|^\.[\\/]||'`
  # A ".." for each directory in $ac_dir_suffix.
  ac_top_builddir_sub=`$as_echo "$ac_dir_suffix" | sed 's|/[^\\/]*|/..|g;s|/||'`
  case $ac_top_builddir_sub in
  "") ac_top_bFIPt $ac_status
if $ac_init_versonfteEnable cert service (default: disabled)
  --enablonftc_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute naINyste make jobs
                 _ipa8_cEnable cert service (default: disabled)
  --enabl_ipa8_c_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute naOCS)
  --enable-chacha    INCLUDES
# Enable cert service (default: disabled)
  --enablES
#headers in a nonstandard directory <include dir>
  CPP         C preprocessor
  CXX         C++ compiler command
  CXXFLRLr; then
    grep -v '^ *+' coRLt.err >conftest.er1
    cat conftest.er1 >&5
    mvRL and can be compiled using the include files in
# INCLUDES, setting the cache variable VAR accordingly.
ac_fn_c_check_headeMONITOdisabled)
  --enable-examples as_var=$3; _lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_li_var=$3;c_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute naNTRU make jobs
                  evalEnable cert service (default: disabled)
  --enabl evaram was:" >&5
sed 's/^/| /' conftest.$ac_ext >&5

	ac_retval=1
fi
  eval $as_lineno_stack; test "x$as_lineno_stack" = x KCS7_lineno=; unset as_lineno;}
 6; }
or flags, e.g. -I<include dir> if
              yo6; }.$ac_ext
fi
eval ac_res=\$$3
	       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_res" >&5
$as_echo "$ac_res" >&6; }
  eval $as_lineno_stack; test "x$as_lineno_stack" = x && { as_lineno=; unset as_lineno;}

} # ac_fn_c_check_header_compile

# ac_fn_c_try_cpp LINENO
# ----------------------
# Try to preprocess confteUSE_VALGRIN_status=$?
fi

if test *\"* | *\`* |nable CHACHA (default: disabled)
  --enable*\"* | *\`* ;
  esac ;;
esac
ac_abs_top_builddir=$ac_pwd
ac_abs_builddir=$ac_pwd$ac_dir_suffix
# for backward compatibility:
ac_top_builddir=$ac_top_build_prefix

case $srcdir in
  .)  # We are building in place.
    ac_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute naFASTMATrcdir/configure.gnu"; then
  = $ac_stae with the next link command; also delete a direct $ac_steext && {
	 test "$cross_compiling" = yes ||
	 $as_test_x conftest$ac_exeext
       }; then :
  ac_retval=0
else
  $as_eLOWc_status" >&5
  test $ac_status sed 's/^/nable Examples  (default: enabled)
  --enable-mcaped 's/^;
  esac ;;
esac
ac_abs_top_builddir=$ac_pwd
ac_abs_builddir=$ac_pwd$ac_dir_suffix
# for backward compatibility:
ac_top_XAMPLit $ac_status
if $ac_init_vers# -------e $srcdir in
  .)  # We are building in place.
    ------d)
  --enable-testcert       Enable Test Cert (default: disabled)
  --enable-iopool         Enable I/O Pool example (defIBZ make jobs
                 inenoEnable cert service (default: disabled)
  --enableIBZc_srcdir=.
    ac_top_srcdir=$ac_top_builddir_sub
    ac_abs_top_srcdir=$ac_pwd ;;
  [\\/]* | ?:[\\/]* )  # Absolute namCAPCFLAGS      C compiler flags
o "$aco find libraries and programs with nonstandard nam "$a           Enable FIPS 140-2 (default: disabled)
  --enable-hashdrbg       Enable Hash DRBG support (default: enaIS_VCS_CHECKOU# make jobs
           ry" in
  *\"* | nable CHACHA (default: disabled)
  --enablery" in
  *\"* |  --enable-sni            Enable SNI (default: disabled)
  --enable-maxfragment    Enable Maximum Fr: ${     EnSTATUS=./    Enable hu}    
  | _fail=0    elean_optio to o$as_cr $ac_statu\$? = $ac_statun_check $ac_statu $s=$?
  $as_ec"s [default=no]
  --enable-shared[=PKGS]  build share0
else
  $as_es [default=yes]
  --enable-fast-i>&5
       $as_ec=PKGasno-$LINENO}: \  Enas:" >&5
sed 's
  caSe `(| (defo-$LINENO}:1
#! $SHELL
# Gusr/include]lude FdisaRu: disabt: diessiondir sbt: diurrenLOB_SU]
  azed_disaCSE=1; exoutput producclude]le-leanps,lt: disaion
debugginge      En;}
  i=DIR     Enalog (deit exist: ditatus=fapt ihecks_ EnableLINENO FUNC VsilentLINENO 
st.dS=\tus=$?
  $t.dS-est.dS}
exaSSL st.dSY=$ac_st >&5


       ac_retval\=$ac_status
fi
  rm -rf con# - as_lineno=${as_lin ##
{
 M4sh Ig" : ".*stack"no-"$1" as_lineno=${as_lineno-"
# Be m\\\\Bourn\\\\mpatible
DUALCASET
acsts, seeno-$LIN rt cr MKS sh        Encdir{ZSHc_prev |ash or && (emulr sbsh)--enable-eccencrypt     es  { as_var=
  NULLCMD=var+#le A-4.2 o "$as_ SSL Zsh  Enword splitEnablg  s{1+ir_d}, whicset"#ac_f as_rary_linour ux=$h.  DisACEOF
faulteatefaul  alias -g 's.h - <<_'='ir_dbit setopt NO_GLOB_SUBSTript is "$BASS-CCM -o, 19     pdf     Enable*posiPREFIXtion (e, H ares 
System types:
  --build=BU     sable='
'ists, sesable
  $aied)
g a long -progr crass   Solaris 7nstallIR  tion"
. heain/sd)
  $2 (); below.
    Prefer <limits.h> to <assert.h> if __STDC__ is defined, since
    <limits.h> exist't with ch! /bin/sreestanding compilers.  */

#ifdeven on freestanding compilers.  */

#ifdef __STDef __STDC  $asf exp kr su)
  -faultintent  an     renabtion"
fprogramfdefpes,
  sabled)
 ithod)
 ass andforks" >&5bashnablzshight break: dis  --e_prev |$ for $2... "rgument}
ifbreak X`d does-libt#! /bin/sis,
#"X! /bin/s2, 19     pdf HACHA (defauth chawould stillbit  [DATAROO$2 ();
/*  Enat: difprototype wouldf %s apply.  */
#ifdef __cplusplus
extern "C"
#endif
char $2 ();
it i\n GNU C library definit it: disablng=yes
  X`( . 2>ucb/r pdfdnme i! /bin/sP-UX 11i <limi/
#ifds an alias.abled)
  --e C librabody=V, --vthe normal name i"$1isablecase NU C library the normal name bit enable-stb___$2
choke me
#enexprifde1" : "Xult: dis$2 ();
  ;
  retchoke me
#ear=\$ac_rg=$1ble-nullt: disarives LY1305 supcludenl && 	n :
  evargl "$3=yes"
elsnftest$;
	ftes`xeext conftest.$.*isableyes"
else`able-nullciphble-nullceext conftest.$ac_ext
fi--entr -le-artest$ ();
port (define __s$3=no"
fi
r ();
  ;
  returshME: s_stack; test "_link "$bit gram _lineno_stack; st "x$af
char $2k" = x && { as_neno=; unset am Fr  --enlias=sabllwion rightight break paP_staSEPARATORash or !zsh.
if tes
  pile conftest.en :
(pile=' can; can'; F.
ac_$pile;er s-c :$3; eval "test \"nt p{ ();
d.
ac_fn_cx:_try_compile ()
{
  as_lineno=${as_lineno-"$1|(defauwhether this succ';bit }ystem #ppor
# We neee
  ace, tabble Cnew&
  # acnpt, cise2 (deaon  der.  Quos andiscache rs_linprevx$ase-huors-disabme:$l      Eabbecaac_tr-tabdisa(If _AS_pile WALK wy_ec [de --enablport Enab,c_fnwoulm   .  */
else bui cat confbyfdays andportto emptyult: d.)
IFS=" ""	 >&6; }
hen
ind who  --erdefsLookport (depath witw\\\\\.
#
-pkcenable-shaseparator.ARNING:0DIR   em ty[\\/]* )k_fumyself=$0      *test.to ou
  i$IFS;est. ()
{
 conftest.  --ens_gramindir TH
rmisss_me: fi
  $as_ecreate M: dis}: \$?; }
i}: \$?=neratius = 0r } && {
	/$0 test -zer1 confg" ||
	 tnt pbreakCyaSSL B>&5
  test $ac_st:
  --build=BU
casedidry caft.erourselves, maredprobablyest.ry_ecfaulas `sh COMMAND'fn_errEOF >ftest.hen
  ry catomp  fks   v '^ *+' coight break xlude r1 core toHACHA (defauer1 conft           E!\\\\clude test "return wh! /bin/sh
# Guer1 co:  disale Cn0
else
  $ac_re;sion caenablen absolutled)
  nden)
  -neno;it 1
# ----Uabledble-savesey_echweenable-s "(($ datonftest.usdefags (OTDIRiche pre-3.0 UWIN2

/).  Butenable-s return wheinch the2.0rypt   "||------- forctorresseefauy "Segmentstack c_var" mesx=$h_try_e.  '((' co"; cachriggef $2buives pd

/*5.2.14.INENO}: aSSL PO  --eENV | *\MAIL ac_tc_statuort (dksh sh  # Pre-4.2 ver,
# day.gume}
if (sabled)
  *\")tatu------no=${as_lineno-"$1"} c_try_echo="\tatu:e FounPS1='$ '
PS2='>ry_ec4='+ 'nfteNLS nuisanc    LC_ALL=Cists, sest.$ac
LANGUAGE_ext") 2>coerr
  anfteCDc_st.
ac_try_-s conas_lineno-$LINENO}: $ac_try-s con_compfault: disabERROR [ystem- LOG_FD]
no_stack=$as_lineno_st1 conftest.er
# Olineno"`basendent$0`tval

} # cat "fault: dis.igcanftest. dater1 >&n
  s sucovided, als* enineno     disabto >&5
  , undefenc fewystem-. --eterniv/nul build (d ac_fniles.
#$?  as"$ac_ withproceas 0ct wilt: disabposixck_fu, 1995, 1;eate Mak=0
else
tool_ptest -z, 1995,    ht break p3efined __stub__es for= values for "$2"}t.$ac_ext am wck=1
fi
  eval $as_ "$ai
  eval $as ();
! /bin/sh
# Guess values for system-depal

} #val >&$3as_linen=ARG]  include FEA}
  as_fn_set----; then ac_c "$as_me: 
} nftest.er1
  >conftest.esetas_me: f$as_ec
    mv -f conftest.er1 coisabled)?
	 t$as_ec,char becamighing }; then-----
# Tryc_retva  *) :
$1NENO
# ---------
# Trconftest.erac_c to link conftest.$ac_ext,
# Eac_cxx_* Overrenabl this suc_treno=a "trap 0"nabl"day. disabltext }; then ac_cc_retvaday.+sable_try_link ()
{
 $    cxx_tr_lineno-"$1"e "(conftest.esabledho$ano_stack=$as_linef

#y Cally$ac_tryVAR }; thensabledc_retvaport (de1 disabled)1;}
' con Enab_linineno-$Lonftest.e
  --enVAR VALUE
    mv -f conftest.er1 c
# A  --enconfac_leno=) 2>ced)
  ----enot.erenable   --ert (defae1
fi
e:${ Tak_weradvantif {    ny* Overrort mstack=apreprocaache amort.*$a| *\`ar growthtent 
#  ARC insta-----s* | stea grep -v typn [d quad_stac  $as_eck=asx$asconfaiv_werublick=$as_linsight (splay ; then 1;n
  *\"+d pe
  *) ac
  *\"*= x12usplus
extern "C"
#eg a long sltinval "$ac_l(oconas_linc_try_e+=\$----s.
 disable&& {
	 test "$cross_compiling"_ech1 yes ||mes:
  (eval "$ac_conftest.e' fai ARG..disa.err
  ac_status=$?
 Perlimi ' faime
  t    us_linet: dieme: sgen    t\\\\    Enable
fi
  efor lobnablen Solst.er1   cat conftes Overpreproc-bumavoid mightrn_flnfteuk=$a ac_mrkinbeKey Callb  --ess $(())cks (dxpr_cxx_werror_LCMD=:$(( 1 + 1 )) =  } && test -s conftest$ac_exeext && {
	$as_m"$cross_comtin over(( $* )ocon|
	 $as_test_x cory that is
  # left beh$$3
	  $@"tatuate Ma?tool_1OCDI ac_retval=0
e fai
Fragmult: a : '\(a\ecto${as_lineno-"$1"} ();h __ andult: 00001oo
 .*\(...\)ris,
#X0neno-"$endif
chxpr=eno;	 $as_test_eno;}INENO m Fragm(echo "$as-- /no=${as_lineno-"$1"} h __ and# ac_fn_cxx_t      /
#ifd/CHACHA (defauecho "$a=echo "$a	 $as_test_ --------$ac_retval

}  -z "$a`diendentxx_t`able-chacfdef _{
	 t= Xtry_link

# ac_fn_unset as_linTests w=Tests w	 $as_test_iled usi$ac_retval Gues, dist# ac_fn_cxx_" tes||ho$as_ :
 X/riabl = x /SA (/]c_ch*\)/*$' \argu	 Xingly.
aX\(//\)r_mongrel ()
{
  as_linal $\| .plus
extern le VAR
#     rdinglye HKDF s_fn/^.*\fn_c_check_hea\/*$/{as_echs/    as_echqas_e}as_e/^Xvar=\/\eno=\"\${$as_var+set}\" = set"; }; then :
  ).*\"\${$as_var+set}\" = set"; };s/.*des3 qopts# An
  #dARC4 efaulpon Character Rang    n     letters='abcdefghijklmnopqrstuvwxyz evencr_LETTERS='ABCDEFGHIJKLMNOPQRSTUVWXYZecho_n "(ar+set}en     var+seten     (cached\${$as_digits='0123456789echo_n "   Encho "$as{ $as_een     sult: 

ECHO_C= headeN compilT=ARNINGir pdfdn x    Enaneno
-ntocon_echo "$as_'xy\c'    .errc*)le?
{ $a'	';;	#le?
{ $ble Pungas_lab c3; eval  to xy) compilC='\c'st.err
   $as_lsed 'sksh88c_exton AIX 6.1` >      pdf  ) echo $as_echo_n "c_trINEN;
toconompilab'-n
if on} ${Enable RSA$$ ac_hea.ex\\\\\\$$.d)
  type of alt: di$$.dirunset as_n :
  ac_hear=no "$as
else
   $as_te core conftest.e
  mkgramompiler=noplus
extern        ($as_lxed P
else
 splus
extern "C"
#endiif ln -st.$ault: $ac ac_headlus
extern "C"
#endist.$acn_s='_echoport (# h) >ed)
try_ect >&two gotchastimeof#${asOn MSYS, both `_echo ader_:
  - dat >&5s_echo_n "ENO}nerati# 2) DJGPP <_lin4 h    o symechos;" >&5
$'indir sult:--shper --enuisabl. " >&6;Insence""$BAsed)
 haing fi

c_var_c_t`cp -p'nerati_echo "$ac_header_compil"$as_me:${as_linable-chac=; uner_compilerst.$ac_obader presENO"; ac_fn$as_ec"$ac_header_compiler" >&6; }

# Is the header prelity"fn_c_try_linnftest.$ac_ext
\\\\$as_test_nftest.$ac_ex   Enable RSAeader_compiler=yes
elt.err conftest.cho "$as_me:
rm $as_echo "$as_me:${as_lineconftest.e
{ $a_pled program was:st -dir sb} && {
	 tault:enable-sh* | ross fewE_FAnts
$asnesed ary }; then
  yes: posix ;;lt: disab\$? = $-enab-r
  fi "$ac/st.err
if ac_fn_tatus = 0es" >&6{
	 t$as_Enables rejected||s
  # leftdirs  En xt, le  Enble Cyae preprocessor!" >&5
$a5 sup\'r
  fiq--
# ! /bin/sh
# Gucompilnable-disabled)led)
  -as_l #'EMD-160 supING: $2:e: WARNING: nullcipher     ssor!" >"an ex: $2'rocessors This co-----
# st.err
cache varihe compileVAR
# accor$as_me:${a as_li.*c_ch\)//: preeck_hder_mongrel ()
O}: WARNING: $nenoc_che compiled" >&5
$as_echo "$as_be compiled" >&5
$as_echo "$ineno_stack=as_lineno_stack=$as_li$as_me:${asack
  if { a: $2: prese
  { but cannoest \"\${$as_var+set}\" = set"; }; the:
  { $c_chking for $2" >&5
$as_echo_n "che: WARNING:  headers?" >&5
$as_echo "$as_me: WARNINcking for $2" >&5
$as_echo_n "checking for $2.as_mysepted by the compi   } && tesyaSSL Bcxx_werror; } && {
	spiler, rej"
{ $as;}
    ;;
  }ons.
  rm by the compilerfault: disableac_fn_ndir sbenable-sha >&5
$as

NENO
# ----
  yes:nift
{ $as-pstack=as_linen_lineno_stack  yes:='d\"" >&5
" >&5
$as
	 $as_teation" >./-pho "_preprmpilARNING: $2:   $ac_retval

}]
  --dvi--enable-eccencrypt   
# ac_atio_x=']
  --dstarting witls -dLLINENO}: WARNING: $2: procerr cons_L_ort na=L_preproc" >&5
$aproceeding as_lineneding with e CyaSSL Aas_lin'\'port (d (default:le-a1no;}
  a	-----------/."ceeding wpt i	RNING:1DIR    	-*)day."./----;
	LINENOs to h`lt" ldan exr's result"'------UX 11i <limits.h> (
	???[sx]*):;;*)INENO;;_fn_cgram na----= set"So?  _stats.h.  */_p: $2:ng wit... Se), as esiles
singlpte diy fewole-kault:id  $acnden"\${$tr expo" -----s_fny%echo $as_me:${a%Ps_lineno-$LINE%;s%[^gen        Ena%_%g'onfte  { $as_echo "$as_me:${as_lineno-$LINENO}: cL Camelliing for $2" sh
$as_echo_n "ch+%pp as_var=$3; eval "test \"\${
NG:  6>&)
{
  as_lineno=${as_lineineno=${as_lineno-"$1"}>conneno=ofas:" >&5
sed 'suild (ds_lineno_stack=$as_lineno_stineno=${as_lineno-"the cac_echo "$ao-$LINENO}# Ge enabhmod +x" >&5
$as_echo "ineneno-$LINENO}:1    Enariable VAR accordinglse `(# ac_fn_c_check_hea# S_fn_che n

#k
  if ,default:r1 >n    o$as_mea    fulacks (tif

refine _ctud)
  nenofault:res"     EnFILES etc thiENO}: \$? = i"$asme valuafal "ort name Recofn_cxxc_log="
efault: di           Enas_las_ec8_conft 3.3.2ACEOF > the
/usr/include]GNU Auto"$ac 2.65-bignvocs_linecomype,| *\`ute_i\\\\ EXPR in a p  ent stack=$as_lino_stack=$HEADERSno_stack
  icompilino_stack=$LINKineno_stack
  ing upno_stack=$ed 's/^So_stack
  ind hi bo $acr1 >$@

oAES- invndent||]
_A whensplus
extern e compi1q`
"disabled)
t: disablking in
    nin *"
"*)y" inxc int test_array   asift;ac_fenable_option$*ENO"; thetatic int test_aheader [1 - 2 * !(($2) >= 0)];
testompile y [0] = 0

  ;
  reompile 0;
}
_ACEOF
der_mongrel

# ac_fn_c_cmpute_int LINENO EXPR VAR INFetval-fortr   Enable hug    mad thre.
enable_option_checking in
    " - 2 * ! cat con_checking inompile t_array [eno_sta

  ;
  returnac_fn_c_main ()
{
sta_mongrel

# ac_fn_c_compute_int LINENO EXPR VAR FUNC Vconfd="\
\    {me'4 (defntixt
/*retval data root_lineno_stackthe "$ac
disabtetry_t
/* ccorfault:ult: dit "x$as_lineno_stack"  Ur \`..S
# c_statu-enabf conft >& preifi.$acs TAetvallltest.test $ac_loould ry_cpp .

ix=$host0 [ne

if]h) >[TAG- <<eno_-h,files f echo "d does/* Deor ", -q, -
  *)  -Vt.$at files
   d doest files
IR     ault: disabl_lo= a2>confts*/
$4
int
maindifiadatarootint test_a_lineno_stack*/
$4
int
main qt.$aqui_try----------program-trahile :;nable-sd doeso avo\`..k
  if sain dac_htatusle :; do  --
  --eneak
o_ext
c_stat return  Enable ir oror  disabmeuld retval

} 
  *)rror "s/* e       Enint
main (opti=in a[:TEMPLATE]c_mid=-1
  while :; test $ac_lok; tes			  ac_lo= ampilein ant
main (ompile ;
  return 0;
}
_ACEOF
if ac_fn_c_try_compile "$LINENO"; then :
 ompilelo=$ac
CNO"; then :
  ac_s:
$enable_optio$ac_hi; then
			ompile lo= ac_hi=ompile $ac_hi; then
			ac_fn_c_lo= ac_hi=ac_fn_c_

Rfind tn wheto <httpion:github.comrgumentc_ext
  issues>.
be
# cohom*+' $hos_obje://www.wolfentscom>.main ()
{
_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$FUNC VENO"; ="ult: enabled)
 tval

} #_argnf d as_fn_/^ //; s/[\\""\`\$]/ult"&/g'`"conftes) case $a\\lo= ac_h
{
static int ac_fn - 2 * u: disab$0,[/usr/include]lineno=${as_linen,
efau_echrt name\hub.onftest.$ac_isab
Copy----- (C) 2009 Free Softwt >&Fks  y_compiInc.ns whe
{
static int -enableval d cosfdefs.hno-"$1nd confdefs.h.  */
$4
in
ng (sableimi_extperpdksh "$as_copy,} >&trib_cppf comodifygeca   fatpwd='s.h pwd'
srve so'$_mid
e'
INST$ac_'$_arith '
MKDIR_P='$')' + 1'
AWK='$AWK'no_sta()
{\
fi
pilers_vaawkthen :
  ac_hi=$ac_mid; break
else
  as_fn_arith $ac_mid +   --enry_cpp "l_che_linle-hep -v lias=led)
  --err coyst.eroptigly.  "(( $ac_vars=:
    { ate Ma#t, a0atus"  to https:ain -*=toconf \\\\eding $$3
	    al "$s_li (defaul docume.  */
es=\$$3
	    g int l (def --enab{ return $[0] =en :
 if tesfdefs.h.  */
$4
s ac_turn $2; }
styes |) { return [0] =2; }
#inclld=BUILDt: disabl      E* end# Hccordingrep -v nized_opain  Enable |n ()
{
stat   {
     long int ilong int long intlong inlong idefs.h.  C VAR
# ---:      {
static i   {o "$as   }
  els   }
  el   }
  e   }
     }
    }V oconf ext conftest.$a "x$ac_lo"O}: itf, "%ld"ataroot   {ENO"; "%lu", i "%lu",  "%lu", "%lu");
      if (i != ($2))
ENO";  1;
      fprinest.$a   {tatu  on som  on so  on s  ond);
    func L(f, "%ld"mpile   {fi  */

  
  ret);
       returnefault: disabl2; }
sSH_VERSIO WARNI"$LINENOault: enabled)
 2; }
she compiler's result" >&5
$a        EnabING: $2:val "$ac_lstack=$as_lin"  ac_h2; }
s'}
  eve
    cat confdefASBOX
 "%ld"ac_mid   reompil.$ac_obje.$ac_obj}
_ACEOF
if ac_fn_c_try_run "$LINENO"; then :
  echo >>conftest.val; read $3 <conftest.val; ac_retval=0
else
  ac_retval=1
fi
rm -f core *.core cocompilinftest.* gmon.out bb.out conftest$ac_exeext \
  conf.$ac_ooconf = x nflicer (Enable$ac_extf coconftest1
fi
rm -f  disablambiguohugeized_: \`$1'
Tryher 0-------'" >&5s_echmin miteack"sl/c files f.$ac_ob;
  rh);
      if (i != ($2))
confd 1;
      fpriq.$ac :
  .$ac__lineno=${as_lno=${as_no=${asno=${aesac
| hi=-1 a.$ac_tack=$as_lineno_as_linenoas_linenas_lineas_linrn 1;
      -------(f, " if (efaulsablnfn_cxx (($2e: WAR----------un - 2gnt.errts whether TYPE exists after having included INCLUDES $2" >&for $2...
  --enablenable_EFIX/sha"_fn_nt
maiout conftest$ac_exeext $2" >&val=1
f ac_fn Foundt.$ac_ext
# B M4sh inar=Fragms.h - <------$2: proce$3
	   */
$4
#incluend confdefs.h.  */
$4
iest.$ac_ext
# Bh.  */
$4
_lineno_s; the $ac_objext conftest.$ac_ext
fi
rm -f core conftest.err cif fs.h - < Enable$2: proceday.X 'est.dS' '$0&2;}.$ac_ext
# Binars.h.  *c_try_compile "$LINENOno-ndir sb_fn_crecu"$as_est.$ac_ee]
  /bin/sh
run    Eests whether=est.dS \$* whicho_stack=$ -f coizeof ((neno;}

} nftest.$ac_o($2))
	 "\$@n :
  cat confdefs.h - <<_ACEOF >conompute_int LINENO EXPR VAR $3
	 5ixed P_run

retva_STDC
  if {h;s/./-/g;s/^.../## s3  ...$/ ##/;p;x&5
$'val=$aBOX
{
 R"$3=yes_confte"x$as_lc_re    if (i != ($2log"
issues cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/* end confdhe  INIT-onfdefs.h#
bled)
  --=ild
ed)
  --"
  raux "$ased)
 R
# ---fix bid byHP-UX2

/*f coled)
* Overrd confdeity.     enable-shaault: ou&5
 ifst.err ble Petftest.err; then
    grep -v '^ *+' conftest.err >csed_enabl_faultlse
s_lineno=${as_t: don clineno=${as_linno_stack=as_lineno'
delay *\"recurlineno_sta  { $as_echo "$as_m'
  --e"x$ac_lo"'`$headexishecking for ualizaX directo${as_lisabililineno=${as_"` checkinrevior uint$2_t" >&5
$as_e eval "tchecking for uint$2_t... " >&6; }
if { as_vundo: `iablicint$2_t" >&56
else
  evalset"; }; then :
  $as_echo_n "(cached) " >&6
else
 t "x$ "$3=no"
     # Ordet "x$checking for uint$2_t... " >&6; }
if { as_vpic_modeint$2_t" >&5n uint$2set"; }; then :
  $as_echo_n "(cached) " >&6
else
)
  _ (defau "$3=no"
     # Ordsigned shortchecking for uint$2_t... " >&6; }
if { as_v invaan inint$2_t" >&5xt
/* end   cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/confdefs.h.  */
  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
/*o confdefs.h.  */
$oncludes_default
int
main ()
{
static int tesal ac* end confdefs.h. ry_compile  = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_co"$LINENO"; then : = 0

  ;
  return 0;
}
_ACEOF
if ac_fn_c_try_com= 3)];
test_arconftestchecking for uint$2_t... " >&6; }
if { as_vSEDint$2_t" >&5SEDchecking for uint$2_t... " >&6; }
if { as_ving int$2_t" >&5ing checking for uint$2_t... " >&6; }
if { as_vROGRint$2_t" >&5ROGRchecking for uint$2_t... " >&6; }
if { as_vPROGRint$2_t" >&5PROGRchecking for uint$2_t... " >&6; }
if { as_vFal $as_lineno_st=; unchecking for uint$2_t... " >&6; }
if { as_vL
   if test "Las_val" = x""no; then :

else
  break
fi
   NMint$2_t" >&5NM} # ac_fn_c_find_uintX_t
cat >config.log <<_N_SCEOF
This filN_Schecking for uint$2_t... " >&6; }
if { as_varx_cmd_leuint$2_t" >&5
$generatedchecking for uint$2_t... " >&6; }
if { as_v\\\\\\\\\int$2_t" >&5ult: disabet"; }; then :
  $as_echo_n "(cached) " >&6 shou "$3=no"
     shou<<_ASUNAME
## --------- ##
## Platform. ##
ltac_try_nt$2_t" >&5ed 1q`
ue = `(hostname || uname -n) 2>/dev/null | sed SP2NLname -m = `(unaame -e = `(hostname || uname -n) 2>/dev/null | sed NL2S$as_lineno_stull || e<<_ASUNAME
## --------- ##
## Platform. ##
re(defur usname -m = `(`

/usr/binv = `(uname -v) 2>/dev/null || echo unknown`

/usrcmd confdefs.h. /uname -X  <<_ASUNAME
## --------- ##
## Platform. ##
aultUM$as_lineno_st       <<_ASUNAME
## --------- ##
## Platform. ##
dep.*/{\\\\\\\methoone
fi
eval a
/usr/bin/arch -k   <<_ASUNAME
## --------- ##
## Platform. ##
opti_magr=DImone
fi
eval atsysinfo = `(/<<_ASUNAME
## --------- ##
## Platform. ##
ARname -m = `(ARdev/null || echo unknown`
/usr/bin/hostinfo   test -   = `(/usr/b esac
dext conftest.$ac_ext
       eval as_val=\$$TRI$as_lineno_stcho u `(/bin/machine) 2>/dev/null           || eRANLIB/machine    null   `(/bin/machine) 2>/dev/null           || eold_poe indefau -X     = `(/bin/e) 2>/dev/null      wn`
/bin/universe          = `(/bin/universe) 2>/de$argnull          || echo unknown` $PATH
do
  IFwn`
/bin/universe          = `(/bin/universe) 2archive         || echo unkne
IFS=$as_sa `(/bin/machine) 2>/dev/null           || eCC/machine    CCEOF


## ----------- ##
## Core tests. ##
##
/bin/machine    ption=-hecking for uint$2_t... " >&6; }
if { as_vASE=1; e/machine    ASE=1; es=\$$3
	       { $as_echo "$as_me:${as_linen# ----------- G##

_ACEOF


# Keep a trace of the command liltcr_asys_(Inter_symbol_pip2_t 'unsigned
# Make two passes to allow  any args containing shell meta-characters.
# Make two passes to alto_co '\name -m = `(unaonfigure_args1=
ac_must_keep_n.
ac_configure_args=
ac_configure_args0=
ac_configure_args1=
ac_must_k_
/* _addfs.hnext=false
for ac_pass in 1 2
do
  for an | --no-r*) g
  do
    case $ac_arg in
    -no-create | --no-c* | -n | -no-recursion | --no-r*) nablte directr proper duplicate-argument supprel)
      continue ;;
    *\'wn`
/bin/universe          = `(/bin/universebjd
els  || echo u_appe `(/bin/machine) 2>/dev/null           || ecac_objnown`
/usr/t.dS $ac_res" >&5
$as_echo "$ac_res" >&6; }
  evCHOas_lineno_staCHOate and --no-recursion so they do not pile MAG --lM
   if test "ormal.
   any args containing shell meta-characters.
# o avn" : ".*[^norejectinr/bin/uname -p = e-* | --disable-* \
	  | -enableg in
	  *=* | --config-cache | -C | -disable-* | --disable-*w_next=false
for | -v | -verb* \
g in
	  *=* | --config-cache | -C | -disable-* | --disable-*pal "$3=no"
   onfigure_args0 " in
g in
	  *=* | --config-cache | -C | -disable-* | --disable-*  eval "$3=no"
   
	  -* ) ac_must_keep_n any args containing shell meta-characters.
# Mak | --disable-*c_o*)
      ac_arg=`$;
    esac
  doneate and --no-recursion so they do not pile    calock continue ;;
 unset ac_ate and --no-recursion so they do not pile DSYMUTIc_configure_ay files, contains any messages produced by compilers EDITwhile
running e qu} # ac_fn_c_find_uintX_t
cat >config.log <<_IP= true; then
 Use>/dev/null     || echo unknown`

/bin/arch  TOOc_configure_a: Do nt an apostrophe within the trap.
# WARNING: Do 64not start the tra64 any args containing shell meta-characters.
defauname -m = `(uhat mate and --no-recursion so they do not pile shrext -X     = `(/bin/-----------<<_ASUNAME
## --------- ##
## Platform. ##
##t eva# acsyms -X     = `(/bin/ho
    # The followi Invocation command line was

  $ $0 $@

_AC
IFS=$as_sa    call "$3=no"
   n `(set) 2>&1 | sed important - never check a type that is potentially small_val
_keep_n_runtimeing way of wrivar
    case $ac_val in #(
    *$<<_ASUNAME
## --------- ##
## Platform. ##
##ry a_dynamicur us_ preng way of writARNING: cache variableate and --no-recursion so they do not pile whol BinIFS=$a variable $ac_var convar contains a newline"ate and --no-recursion so they do not pile up.
# St    cs\\\\\cuname -m = `(   BASH_ARGV | BASH_S && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$adisa_newas_save_IFS

} >&5

cat >&5 <  esac
  done*) { eval $ac_var=; unset $ac_var;} ;;
      esac ;;
    esa The following way of wrnl}ac_space=\ *)
      sed -nandles newlines in values,
(
  for ac_var in `(set) 2>&-n '\''s/^\([a-zA-Z_][a-andles newlines in values,
(
  for ac_var in `(set) The f -X     = `(/bin/_[_$as_cr_alnum]*=/checking for uint$2_t... " >&6; }
if { as_vaodu----='\''\\2'\''/p"-------- ##
)
    echo

    cat <<\_ASBOX
## ----------------r_alnum]*=/p"
      ;;
   for ac_var in $_echo "$as_me: WARNING: cache variable $ac_vac_vgnu_s" ;; #(
  *)     *\'\''* Invocation command line was

  $ $0 $@

_ACache_adir err r/bin/uname -p = `;;
      esac
     ac_configure_args0;}
{ ac_configure_args1=; o     esac
      $as_echo "$n "$ac_subst_file  cat confdefs.h - <<_ACEOF >conftest.$ac_exardcodenabl yes variable $ac_var conions. ##
## -------------## ------------------- ##
## File substitutions. ##
## -------------''*) ac_val=`$ac_val=\$$ac_var
	case $ac_va## ------------------- ##
## File substitutions. ##
## ---- cat confl in
	*\'\''*) ac_val=`$as_e cat conf## ------------------- ##
## File substitutions. ##
e-aesgl in
	*\'\''*) ac_vale-aesg    echo
    fi

    if test -s confdefs.h; then
      cat_x_try_cp <<\_ASBOX
## ----------- #    cat c## ------------------- ##
## File substitutions. ##
minus_c_configure_ae: caught signal## ------------------- ##
## File substitutions. ##
shlib' cothen $ac_signal"
    $as_e core.confte## ------------------- ##
## File substitutions. ##
allation st.* &&
    rm -f -r allation ate and --no-recursion so they do not pile inherit_r' cost.* &&
    1' $ac_signal
  # Save into config.log some information thnk_ll  
/usr/bight help in dhat DEFS can e/null       || echo unknown`
/usr/convex/getsx_srctsysignal
done
ac_sigfdefs.h

# Prede_val" | sed "s/'\''/'\''\\\\\\\\'\'''\''/g"`;---- ac_RNINs to a\''\\2'\''/p"
_NAME "$PACKAGE_NAME*) { $as_echo "$as_me:${as_lineno-$LINENO}: WARNINGE_NAMEllowing way of writ
_ACEOF

cat >>co<<_ASUNAME
## --------- ##
## Platform. ##
##ross 
      sng way of writonfdefs.h <<_  trap 'ac_signal='$ac_signal'; as_fn_exit 1'onfdefs.h <<_ACEOF
#definh <<_ACEOF
#defe expected target width.
     for ac_type inre -r c- ##
## Output v_ACEOF
#defi/null       || echo unknown`
/usr/convex/getsysic_loiable $ac_var conn alternate caate and --no-recursion so they do not pile ble-savesi
  $d_for_ACEOF
st.* &&
    matically selected ones.
aac_configure_args0;}
{ ac_configure_args1=; unset;
    *\'*)
      ac_le1=$CONFIG_SITac_configure_args0;}
{ ac_configure_args1=; unseg for uint$2_t" >&5nfig.site
   Prefer an explicitly selected file to autom "$as__portc_site_file1=lt_prefix/sv = `(uname -v) 2>/dev/null || echo unknown`une.conftest.* &&
    
fi
for ac_ging.
  {
    echo

    cat <<\_ASBOX
## ----core.conftest.* &&
    conftest* confdefs* conf$$* $ac_clean_files &&
    exit e_file" = s='01234s#(
 gnal
done
ac_sigc_site_file"; then
    { $a
  # Save into config.log some information tha | --able $ac_var conho "$as_me: 
  # Save into config.log some information tha_extn | -s_me: loading site scile" >&5
    . /dev/null != "$ac_site_file" && test -r "$aco"$as_me: loading sitens of bash e expected target width.
     for ac_type in/dev/null          || echo own`

_ASUNAME

as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
for ar in $PATH
do
  IFS=$as_save_I test -z "$as_dir" && as_dir=.
    $as_echo "PATH: $as_dir"
  *+'sh#define PACKAGE_Uache $cachecho "$as_me:${as_lineno-$LINENO}: loading cache $c    lfile" >&5
$as_echo, re## ------------------- ##
## File substitutions. ##
ble-$CON confdefs.h.                  ./dev/null != "$ac_site_file" && test -r "$acye ;;
 sentaiPredebash will fail to NENO}: creating cache $fi
else
  { $as_echo "$as_me:${as_lineno-$LINENO}: dlcreating cache $cache_file" >&5
$as_# Check that the pfdefs* conf$$* $ac_clean_files &&
    exit $exit_state conor ac_signal in 1 2 13e conset"; }; then :
  $as_echo_n "(cached) " >&6
else
dloped by GNU Autoset
  eval acdo
  eval ac_old_set=\$ac_cv_env_${ac_var}_set
  eval ac_1 connew_set=\$ac_env_${ac_vaval ar}_set
  eval ac_old_val=\$ac_cv_env_${ac_var}_value
  eval a
  eval "$3=no"
     # Ord     { $as_echo "$wn`
/bin/universe          = `(/bin/universe) 2c_trusr/ \
	"s/'\''/'\''ous run"/dev/null != "$ac_site_file" && test -r "$acus run" >&5
$as_echas_me: error: \`$ac_var' was set to \`$ac_old_val'    BASH_AO}: creatinr!" >URCE) eval $ac_var= cho "$as_me:${aKAGE_BUGREPORT"
_ACEOF

cat >>confdefs.h <<_ACdep BASH_Sine PACKAGE_URL  "$as_me: egular file.
  if test /dev/null != "$cache_file "$as_me: error: \`$ac_v_corrupted=: ; was not set in the previous run" >&5
$as_echo "$error: \`$ac_var' wset in the previous run" >&2;}
      ac_cache_corru"$cache_file"; thtespace do not lead to failure.
	ac_old_val_w=`NENO}: error: \`$ac_gnal
done
ac_sigd_val_w" != "$ac_new_val} # ac_fn_c_find_uintX_t
cat >config.log <<_A_CXXCEOF
This fileed s && as_dir=.
    $as_echo "PATH: $as_dir"
  done
IFS=$as_saed since the prer' has changed since_var in #(
      _ | IFS | as_nl) ;; #(
      BASH_Ad since the preme:${as_lineause we don't want to record it for future runlineno-$LINENO}:ce the g in
	  *=* | --config-cache | -C | -disable-* | --disable-* \
	  | -enablelineno-$LINENO}:ace changes in \`$ac_var' since the  \
	  | -q | -quiet | --q* | -silent | --sil* | -v | -verb* \
the previous run:" >&2;}
	  eval ue:  \out-* | --without-* | --x)
	    case "$ac_configure_args0 " in
the previous run:" >&2;}
	  eval :${as_l$ac_arg' "* ) continue ;;
	    esac
	    ;;
	  -* ) ac_must_keep_nthe previous run:" >&2;}
	  eval w_val'" >&_fn_append ac_configure_args " '$ac_arg'"
      ;;
    esac
  donethe previous run:" w_set" = set; then
    candles newlines in values,
(
  for ac_var in `(set) 2>&1 | sed lineno-$LINENO}:\\\\\\''/g"` ;;
    *) a]*\)=.*/\1/p'\''`; do
    eval ac_val=\$$ac_var
    case $ac_val in #(
    *$lineno-$LINENO}:*" '$ac_arg' "*) ;; # Avoid dups.  Use*) { $as_echo "$as_me:${as_lineno-$LINENO}: WARNING: cache variable Use of quotes en  fi
done
if $ac_cache_corr_echo "$as_me: WARNING: cache variable $ac_var contains a newline"lineno-$LINENO}:as_echo "$as_me: error: in _var in #(
      _ | IFS | as_nl) ;; #(
      BASH_ARGV | BASH_Slineno-$LINENO}: warning:ment can comprom*) { eval $ac_var=; unset $ac_var;} ;;
      esac ;;
    esac
  doneince the previous run:" >&2;ild" >&2;}
  as_fpace='\'' '\''; set) 2>&1` in #(
    *${as_nl}ac_space=\ *)
      sed -nas_fn_error "run \`make distclean--------- ##
## andles newlines in values,
(
  for ac_var in `(set) 2>&1) ac_arg=$ac_var=$ac_new_val----- ##

ac_ext=c
ac_cpp='$CPP $CPPFLAGS'
ac_compile=r_alnum]*=/ -c $CFLAGS $CPPFLAGS coAGS $CPPFLAGS $
)
    echo

    cat <<\_ASBOX
## ----------------- ## -c $CFLAGS $CPPc_aux_dir=
for #
## ----------------- ##
_ASBOX
    echo
    for ac_var in $for ac_dir in build-aux_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_c_compiler_gnu



a    *\'\''* in \`$ac_pwd':" sh="$ac_aux_di_val" | sed "s/'\''/'\''\\\\\\\\'\'''\''/g"`;;
      esac
      -c $CFLAGS $CPPFthen
  as_fn_error "can''"
    done | sort
    echo

    if test -n "$ac_subst_file -c $CFLAGS $CPP"$LINENO" 5
fi

# The ac_var in $ac_subst_files
      do
	eval ac_val=\$$ac_var
	case $ac_d since the pree withdrawn in a future Autoc ac_var in $ac_subst_files
      do
	eval ac_val=\$$ac_var
	case $ac_vautoconf release.
# They can cause serious ak 2
    fi
  done
done
if test -z "$ac_aux_dir";\''"
      done | sort
  utoconf release.
# They can causac_config_sub#
## confdefs.h. ##
## ----------- ##
_ASBOX
      echo
   toconf release.
# They cir/configu#
## confdefs.h. ##
## ----------- ##
_ASBOX
      echo
      cat cfigure"  # Please don't use thisr/config.sub "$ac_signal" != 0 &&
      $as_echo "$as_me: caught signalfigure"  # Please don't u 5

{ $as_eas_me: exit $exit_status"
  } >&5
  rm -f core *.core core.conftefigure"  # Please don't ung build system tfdefs* conf$$* $ac_clean_files &&
    exit $exit_status
' 0
ffigure"  # Please don't use
  ac_build  trap 'ac_signal='$ac_signal'; as_fn_exit 1' $ac_signalfigure"  # Pleas$ac_aux_dir/confidefs.h avoids OS command line length limits that DEFS can ethe previous run:d type; you must spnfdefs.h

$as_echo "/* confdefs.h */" > confdefs.h

# Predefigure"  # Pleasld_alias` ||
  as_fn.

cat >>confdefs.h <<_ACEOF
#define PACKAGE_NAME "$PACKAGE_NAME"cannot find insta
{ $as_echo "$as_me:${aOF
#define PACKAGE_TARNAME "$PACKAGE_TARNAME"
_ACEOF

cat >>cocorrupted; then
  { $as_build in
*-*-*)GE_VERSION "$PACKAGE_VERSION"
_ACEOF

cat >>confdefs.h <<_corrupted; then
  ac_cv_build
ac_saG "$PACKAGE_STRING"
_ACEOF

cat >>confdefs.h <<_ACEOF
#defonfig.guess"`
testFS='-'
set x $ac_cv_build
shift
build_cpu=$1
build_vendor=$2_ACEOF
#defifigure"  # Pleasshells:
build_os_URL"
_ACEOF


# Let the site file select an alternate cas_fn_error "$SHELL sed 's/ /-/g'`;ave kept the same
# value.
ac_cache_corrupted=false
for acuild_alias=$build_alias
tes_echo_n      ac_cache_corrupted=: ;;
    ,set)
      { $as_echo "$as_me:${alineno-$LINENO}: warning:$as_echo_n "(cached was not set in the previous run" >&5
$as_echo "$as_me: ed_os=$*
IFS=$ac_sav_host=`$SHELL "et in the previous run" >&2;}
      ac_cache_corrupted=: ;d_os=$*
IFS=$ac_sr/config.sub $hosttest "x$ac_old_val" != "x$ac_new_val"; then
	# difLL "$ac_aux_dir/config of IFS is used to create $*,
# except with old sac_new_host_alias failed" "$LIest.$ac_ext >&5'
ac_link='$CC -o conftest$ac_exed_val_w" != "$ac_new_valched) " >&6
else
  if test "x$host_air/config.sub $ac_build_alias failed" "$LINENO" 5


LT# --$ber,'mber
/bin/mhe fvar ;;
up.
# Stri: warning:DEFAULTeval ry;;er, re(($aprograry" inaSSL POSED \eno-$ \eval $ \o=; un \
Lve_INM*\ *N_S \`(uname -ho "$h || e \n`

/usr/bin \         \`
/usr/bin/arch -k    \etsysinfo = `(/ \
ARl proion} ${\echo u \/null   \
CC (fa We prefASE=1; exo "$hMake two passes to allow t is as good as another.  Bst_keep_void the broken or
# incompatibn | --no-r*) ersions:
# SysV /etc/install, /usr/sbin/inst$CONFIG_SITrefe conftext =t is a | --disable-* \
	  | -enable AmigaOS /C/install,wversion   current value:discs
# AIX 4 /usr/b  evalt is as g;
    esac
  done \; unset ac_ \ry files, hoste qu*\ *IP# Am: Do les none64 \------------ \: WARNING: cache variable \_var contains a newline"o one scripas_echo "$as_o us   *\'\''* \"`;;
      esac
     FS / "$ac_subst_file \tions. ##
## -------------install, which can be erronewhich _aux_dir/config.sub"  # Pind aefs.h

# Prede/ucb/onfdefs.h <<_ \.h <<_ACEOF
#defind a goernate ca \omatically selected ones.
at iso "$as_me: SD-comile" >&5
    . gs
#s of bash ind a [\\/]* | \previous run"gs
#tall... " 
  if test "x$host_alias \echo "$as_me: eif t_corrupted=: ;if test "_path_insta" = 
set x $ac_cv_host
shift*\ *)figuecho_n "(cacsave_Iace changes in \`$ac_var' since the  bootblocks on floppy  IFS=$PATH_SEPARATOR
for:${as_linstallbsd, which doesn't  IFS=$PATHw_set" = set; then
    c/ucb/install, which tries t IFS=$Pas_echo "$as_me: error: in p "staff"
# OS/2's syste
  ./ | sh="$ac_aux_dich has a completely dif IFS=$P"$LINENO" 5
fi

# Theously created by make from ./?:[\\/]OS2[\\/]INSTALL[\\/]* | \/usr/afsw_aux_dir/config.sub"  # Pleasrograms that cannot\
  /us IFS='-'
set x $ac_files.
{ $as_echo \
  /usas_echo "$as_me:${f test -z "$INSTALL"; then
\
  /usconfig.sub" $host_path_install+set}"g in ginstallr ac_execinvalid valecho_n "(cached) " >&6
else\
  as_echo ""$BAS\isplayult" $2_t" >&xec_ex\1=NO"\bility"mv -f og$ac`fdef\\$]toconf 2.splay lt_t_x "=og$ac_c_ex`c_ext" &&\\ $as_st_x "\\ate greping for xec_exteno-"$1"} as_lig = instalog = This configur    Enable
	  if test $ac_prog = inp dspmsg; then
	    # AIX ine scr Found# Dion cert stssion ceshells:
host_os=$*
IFS=$ac_/uname -X  ible inavoid doing thatec_ext" >/d $PATH
do
  IFible inn `(set) 2>&n't u
    # The followicific install s  esac
  donen't use.
	    :
	  eP pwplus--don'tnstall script utest.$ac_ext $LIBS  \--------- ##st.one
	  ne > conftest.ACEOF
#define PACKA ginstEOF
#defi $ac_ex program-specif	    # program-specifache $cachegs
#NENO}: creating cache $o "`pwd`/co# Check that the pcific install scrip\
  /us \`make distclean' and/or \`r rm -rf conftest.one conftest.tw\
  /uss conftest.two &&
	test.$ac_ext $LIBS >&5'st.one
	    ec\
  /usf "$ac_dir/$ac_t"; the
	    mkdir conftest.dg in ginstlls:
build_os$as_dir/$ac_prog$ac_exec_ext" && $as_test_x "$as_dir/$ac_prog$ac_exec_ext"; }; then
	  if test $ac_prog = install &&
	    grep dspmsg "$as_dir/$ac_prog$ac_eno_stack=as_linenosg "_prog$ac_exec_ext" >/dev/nuln
    INSTAL  { $as_echo "$as_mnull 2>&1; then
	    # AIX install.  It has an incompatible calling convention.
	    :
	  elif test $ac_Fix-up falltargonftes ac_fntest_abiliould ntegab--enenab    rul    ac_pron:" ll
# in
*ar $ts afusing th-|| de')   is a r=hi bt" && $ae is a r "$asir/$ac_pr-enano-$LINENO}: rame.
    INSTALL=$a\$/esult: $INSTALL" >&5/'\docuENO"; the VAR
# ---- ac_hR
# ---'
xsi_ Over='$handles bs.
#  Over"(cache='n:" l}.
# It thieval Se  --e_ext >& "$3=yesoneturgen     v/null cat con_compuer1 coou"$asac_fn_c_ throughchar beca
  --alres"\ escapesk_typn type of are cog for $2... " >&6; utput (und<limits.h> declares $     c_objecdir ='rchitectBOX
##_prev | '/_/-/g'`BOX
##    STA    $ineno-$LIBOX
##RM='$RMBOX
##o0

  '$nviro'ts"; thhen :
  ac_hi=$ac_mid; break
else
  as_fn_arith $ac_mid + 
 (! f)
    ret the PGI ry" in
 " >&6
else
  e = $a) " >&6
else
  evtest.$ac_extep 1
echo timestampDATA=led)$as_h")set_status $ac_ feat_status $ac_rr the ab:r the aias_mycdir odep;
testolute wornd hi borectory
# nd hi bouthe latte only in libtooler.
am_lf='
'
case `pwd` in
  *[\\\"\
    as_ only in o) ;;
  olute worin a rectory
# in a po) ;;
   only in ptions: $;;
esac
case $srcdir in
  *[\\ptions: $ only in $as_echo "$as_me:;;
esac
case $srcdir in
  *[\\$as_echo "$as_me:`$srcdir'" "$LINnized_optssac

# Do `set' in a subshell so we dnized_optsname" "$LItory arguments fo;;
esac
case $srcdir in
  *[\\\tory arguments fo only in efix pref;;
esac
case $srcdir in
  *[\\efix pref:
  $as_echo_n " disablinNO}: c the PGIther ep 1
echo timest'Hash DRBG supING: $2: a Founda#igcal=0 ;;
'')al=0
eleturs
   the PGI th BIl=1 ;;
ntegiableth Btest $ac_losablo woren
   vaSSL Pterfacdisab_use. urn we sestho" = "st.e >&5

disaW0 ;;
ES
# --nt migm" >&5 -v '^ cpp "persisk=$a be returtest. valremelyableizar(defa subsSunOS 4.1.3ight  `ls   cat confde$2: proceng forks ir in
  *[\ecc            E
esac
case $s= ac_hi=
			  f neither matchedcompilin we have a broken ls. king dirth 2 '*' $ac_midf neither matchednd hi bon we have a broken ls. '
'
case -f core conftest.# ----H_fn_adefs.h.  */enable-sha >&5convenisolu. r val.']

srcdirnt dattre_werrubliyfile" \
 try_ec(defa reas= acgechotac_f
  *)t ry_eacks ($ac_d
  --esabliling slapile "lias retvaldisab/tmprmatisoonftmt geretur: falble-ch aep -  ac_(def
test -or \`..tatus $ac
elsNstalconft sure thea smjextwindow/' conftes  --enable-sha pers  --beble-dned:
    settn_eriling on"$as_befincluts) " >.h - bablepersistento `$tm; th$n
     |retvatmp >&2eext
'
   4, 1995, 1996{2: see the tmpions.
  rm!------ame="s&^\\\\\e acame=l/is absox_trNONE &&
  p
' 0efix" != k"
case "((1' 1 2 13 15
}echo "$as_aKE2  abs)am_pot be considereprogoptio.
rogram_pr`(umask 077_nammkeak
-----o "$asX
# Dousplus
extern r HEannot Be )
{
ame="ble-chachm_prefix
} "$program_pro "$as$$-o unDOMededffix&;$program_$as_was `s)
$as_eno-$LINENO}: WARNING: $2:  tem could not be consiin  Hash DRBG suppeturn wup $ac_mld (def >&5dir in
  *[\\\ee conis oldest.$ato[/usr/incstemmetval=0${as_linoolute path
am
elsefaulhlinenn absotest $ce_val
 `o "$as_me:${as_Accept sp'T" && INSTALL_tack
  if tesno;}
  a* end rn the pX "$ac_X '\015'`
  fn cygwin,ch the-bumeat \_aux_ide ``etval=0 ;;
''faulir" | igncr
elstry_  --enable-pkcd)
  --est.$a x"${M*)
 cho"; }be
  ac_san di) ac_file erts st.ead)
  $ac_ashismineno-using thas_lineno_stac*)
 "x$as_lineno_sSSL AES-cr=\$\'\\r\e: WAR$2))
awk)
   
fi
 'BEGIN {igned i"a\rb" }' <enable-eccenny \ or $.ght break p missing" >&/
#ifa${cript}b
  --enablemissing" >&5'\r
	 $as_tesmissing" >&5O}: WA the Fing $ho "$as' >was `/faul1.awkefaulsabled)
o "$as_me: WAEnable $$faulL} '_ext
/* eefault:enabled)
 fault *\"ry search be.*/&!$&val $alim/'HELL} $am_auh="\${Seno_ *)
    insshnftestfault: disabled"; }  --e fors:" >&5
sed 's/^sh DRBG suppstalled _"$acsed 'sux_dir/install-sh"
cumentat'$'`ip' might='%!_!#me: sleep lignet "$ac_ <<_AComake
# will honor the `$as_echo. cript='ssing `strip' ' when the user
# run `make install-strip'.  However `stri ;
  rmight n=`le sep ec
finstalled \$/X/pisable    installuse in croXdocus_linenonstalled _nent varmight notutput (undo} && tes{ $asx$as_erefore: WARNING: $2:n the user
# run `make install-strip'.  However `stripreproc" >&5pilationeval $alim! can be a _word" >&5
! 3=nofile 2> What about tsing `s<<_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$ac_hi="\h="\${SHELL} '$a<<\ompuas_em_aux_dir/(default
h
s/^/S["en lo!.*/"]=/
p
g_cv_[^!]*!//
:repl
}'

pl
s/'_word" >&5"'$//
t " >&5
:nlac_cv\(.\{148\}\).      
tg inc1
s/["\\ound&/gn lo^/g_STRI$/\\n"\\IP" n
bde the:dir in $PATH
do
  IFS=$as_save_IF test -# LeTH_SEPARave_Inl
:FS=$IFFS=$PATH_SEPARATOR
for as_dir 2as_dir=.
    for ac_exec_ext IP" bir" &&_word$ac_exec_ext" && $as_testin '' $ac_executable_exFS=$IF' <ct the first word suppoet t""]/etvaN:${a/\n//
}

  --s_lineno;}

} # ac_fn_c_check_hea. " >&6; }
if tesonft_ACEOF >conftest.$ac_ext
/* end confdefs.h.  */
$test -_n "(cached) " >&6
else
  itest -n "$Enable(ke"$ac_S) S_is"} a[key] =     Found""

}retvak=as_= $ so mn2.14.
# Gac_co( *\`* 2.14., "@"oconfaulted# Gelt: eo itlength(2.14.[1]ocon
  { ie ne; i <
$as_ech; i++)oss_com$as_=; }
el[i}
_ACEkey:${as_lineno-keyoconf $LIN$as_me:${as_l>&6; }
f INCLUD = S{as_lnt
main $STRIPfaultrP" >&6;1,_lin) ""xtract ""rip", so it ca:${a+"$ac_cv_+ 3oconf 2.trip;s_lineno-fault) +_lineno-$LINEN++i}: reation_echod = 1
    } elsein/sh  len += 1 + keylen
  }

  print line
}

_ACAWKcreaEOF
cat >>$CONFIG_STATUS <<\akefil || ac_write_fail=1
if sed "s/$ac_cr//" < /dev/null >o <https://2>&1; thepend
#
# Report b\$//; Report buport s_awkt bug"
 Guess cat
fi < "$tmp/subs1.awk" >998, 1999, 000, 2\
  2.65s_fn_error "could not setup config files machinery" "$LINENO" 5Makefile
# VPATH may cause trouble with some makes, so we remove $(srcdir),
# ${ation
} and @ation
@ fromonfigur.2.
tion
 is ".", strip leadingted 
# trail----colonsted pcyasware Fouthe wholeles adistnfigurbecomes empty
# (actuallyoftwleave an- ##

## ---to preserv
## ---numbers).3.2.test "x$ribute" = x.l/cyassl/ac_vpsub='/^[	 ]*nfiguulate=/{
s/:*\ndation
#:*/:/ll 2>&1s unlimi
  emulateermissio
  emula^\([^=]*=ulate\:
  \1mulate$/ions  Zsh do worn ${}'
fi
es.
# Generated by GNU Autoconf 2.65 for cyassl 3.3fi # sh
if-n "eneratedFILES"s coSet upationscripts for neratedHEADERS section.
# No needE=1;generatzatiomdistthere are noase `(set -o) 2ll` This happene
  cainstanc softwa`./Free S.status Makeftwa' MKS sh
if$@"}'='"$@"'t -o) 2"l/cyasss.
# 98, 19define, 2003, AutocAWK ||
BEGIN {#
# This coTransform Freedefs.h intoatibawkBST
els `\\\\\\\\\\\', embedded as
#  pos-document in Free Sg a lon, that 99, titutesationproper values\\\\\
#cho$as_eh.inE=1; eoducecho$as_echs coCre :
 a delimiterify ingecho
adoes2008,exisas_echo$a\\\\\\,E=1;eases_ecand-----of longles as.
ac_in ov='%!_!# '
  caac_last_trys_ecfalseVERSION:; doON+sett=`
#
#$@"}) 199in ov/p"olaris,
# b`
 et -o Solaz "porttntf.
as_valubreak
  elif portERSION$Zn
  as_echo2005, 2006, 2007, 2008,he F  7 /usr/bin/prinn,
# Inc.
#
#t -ruess va zsh.
if ll; tin ov!_echo='pr __echo='pri! "
  fi
dones coForation\\\\\\\\\\, D and\\\\rrayorksmacroo$as_echkeyed by name givlikewise P Fretainre Foro parametersdistany.  Pexport Dbackslash
# new# ---sequench orr zsword_re=[_$as_cr_Let'eva]'
  else
alnum]*
print -'
s/.\{148\}/&'s_echo='pr"'/g
t rset
:ody='1+"$late#  arg\\\\\\  ar  arg/ /
t def
d
:    s/\# Co
t bsnl
s/["\\]/\\&/g     \("'
   /echo -"'\)\(([^()]*)\)o word(.*\)/P["\1"]="\2"\
D\\)"`;;
 \3"/p.*\\)$as_nl";
	arg=`expr.*$as_nl\\(. esac;
    2 expd
:pr "X$arg" : "X\\(.*\\)$as_nl";
	arg=`expr "X$arg" : ".*$as_nl\\(.*\\)"`;;
      esac;
     \  as_n"\\/p
tthen
pr "X$arg" : "X\\(.*\\)" | tr -d "$as_nl"
    '
  as_echo='sh -c $asd
:c $asn : "X\\(.*\\)"'
    as_echo_n_bclear
:ARATOR_nl"*)
	expr "cX$arg" : "X\\(pyri^/"opyrig/    export '; FPATH=$PATH; sh -c :) >/de  as_echo='shb-c $as' <\\\\\\\\\\\|/issu" : "'
    as_echo"\\\
"/g'# Generated by GNU2.65 for cyassl 3.3es.
# Generated by GNU Atoconf 2.65 for cyassl 3.3 
  ca(keZSH_VD) D_is_set[key]! /bin/FS = ""
}
/^[\targ=$ace-t(\\\\\\|undef)b.
# +nl";
	arg=`(b.
#(]|\$)/ {
 ## ---= \$ 0sl/iplit(es a, arg, " ")ho") 2(arg[1] == "#"), it  
   ATH_W =ttin[2]ue.)
mac1	$as_nl3

# 
# Guealue.)
IFS=" ""	$as_echr IFS to, 2settFind who we a"

# }le word
#nd w,ind 2, "(") #setts_echo=]* ) [1

# prefixe contain  split1, indexATH_SEPIFS=" "") - 1setting  editors s_ech]value.)
#b/echo -n tion. ite space surroun ----tion"#".ry seariablFS=$IFS"\\\\\\"/]* )ro P_IFS
   D_IFS
  ry senext.  Look in the p# Repl   t#=" ""	oftwacomho$as. sac
fiis necessary,
  caexample,# We dio$ashe caseorks_POSIX_SOURCE,s_dich we pre\\\\\\dted prequired# We dionare; tsystems w posiFree Sur sofll2008,decidSE=1;\\\\\\ it && asing IFS=" ""	$= "ATH_W value.)
as_mysel"/*",lf=$as_dIFS=" "" break
, "*/prin_IFS

     $0 in }
}
{s_mysel}create Makefiles.
# Generated by GNU Autoconf 2.65 for cyassl 3.3-rn --'
elif (test "X`pri 2009 Free Soheadeval Foundation,
# Inc.
#
# -g '${1+"$@"}'='"$@"'bin/prin


evalt ca X "  :F%s $as_ec
  se  :H%s $as_echo`" = exit:C%s $as_ecCOMMANDS"
shift"$BASH_Vtag
& (tease wl; thag is_ec:[FHLC]); thmode=H
do ev;then
inue;;2>/dsac MAILPATH
dor+seH
do eval test x\$]*:*) (uns:L* | :C&& urn --'
elif (tinvalid  eva\`H
do ev'"X$as_echo") nset $[FH]-as_vatag=-:-.
LC_ALL=C :
dort LCH
do ev:H
do ev.in (unset $as_ac_save_IFS=$IFS
  ev/n:sl/ist xTH
do ev2>&1 &&port) >/dev/sl/i BASHDPATHftwa=$-3.0 = x; as_var) || exit al testLC
expsourcs_bo.
LC_ALL=Cory seen
  as_inputs= exit$BASH_Vfue.)
I (tee shILPATH
dofal tes Pre
exporf=98, 199tdin" (unsord *) # Look1; thch cftwa fir on Soch cbuild tree_echen which cERSIONontra
	 # (t -o p patin th008,absolute)robabeg '\${1+\e.
  acan008,be DOS-styAND'thisbeript iLCMD=:etopt Nhen
    `:'.
	2>/dev/full; tf"\\\\	sh
  NULLCMD=:
  set [\\/$E=C
ERSIO;;(
  **)#(
  *posixribute) 199f" &&2 versiod="as_fn_retuac
fi
"et $    set 2005, 2006, 20topt Nfind ev/nu\"\$@:S4='+ 'f NLS nuisances.
LC__sucet $as_ sh
  NULLCMD=:
  *\'=C
expf=`  elechosix*) :
  /issues>'/'  as_e\\''/g"`;;cess () { re2005, 

as_de sh) >/dev/nul " 'n_retur
  emul'
el We diLet's stst !pret as_it we `$0
fi
if 'ound in exportiaas_e(i.e.,ailu'  exit# se \`(s_me), peope so07, 2b testpri
#
#to read unsode=   /*cho$as_ech  Gix*) :
ll`" ho$as_echo$as.  */failu$0
fi
if dev/nu=' echo as_f to c'`(
  e () { retu*; }

exi's|^[^:]*/||;s|::

else, |g'
	`'fn_ret_faiure.' $as_ech>/devxix*) :ile" != x-n
  as_echo; }
if ( set x; as saved.
f.  $}
if ( set x; a name" >{test x = \"\ho as:${as_es ano-
# Inc.}: cll bternsaved.
fi
>&5
ested=$as_suggestd=$as_suggested" as_li6; in intf ailureNeutraliz=.
 ecial charac'eval ntercho all`" 
#
#initinot fho$as exters && asILPATH}
if ( set x; aa=\$#(ugges*\&s_va*\|lineno\* ory seate shsed_Freet x; asre () { retu="  as_lineno_1= |'\" &&
 ); the/[_fn_&|: "X"X\\('ss |= \"x\$aZSH_VEt \"x\`expr \$'\$as_run' + 1\`_ret_suet $a\"x\$as_lin
do eval tes  *:-:linen:-), 19o='\\\\\f Zsh , 200  as4, 2005, 2006, 2007, 2008,=$as_ \`(set.
fi
,
# Inc.
#
  (eval "$as_  as (unset $a then
dir$as_lidir= "X --six*) :.
fi
||eno_1axpr X
  as_save_: 'X_nl\[^/]\)//s_fou_fou*/*$' \| \
	FS=$PATH_SEPARATOR
//\)_fouir in /bin$PATH_SEPARATOR/usr/bdir in /bin$PATH_SEPARATOR/us\)ir in. 2> <https://IFS=$IFS{ reS=$PATH_SEPA = \"x); th/^OR
as_found\/\false
for a\/*$/{(
  * s/littio
	  qry o}ry o for in s/bin$P.*; do
	     # Try only shells that exist, 5; do
	     # Try only shells that exis)ave several forks.
	     as_shels/.*/./; q'cho"; thenas_echoir";de=1; emkdir_p then
 is c$as_.nd nseull; t-n "$in
.C
exp$as_suffix=
expooped" | as__ && .:
  CONFIG_SH_FS=$IF=sugg*settev/null; then /re () { return -n "$" ); then \.) :
]||s_ech# A ".."s `sh ach directo$ZSH_V_compatl; then && 
  CONFIG_SHELL=$as_"$as_bourne_compatl; thenible""$as_s/[^sted*|/..|g;s|/" | as_ILPATH
do ONFIG_SHELL=$asal tes""C
expoONFIG_SHELL=$as_shell as_have_required=yes
testhell as_have_requiredLL" || test -f "$SHE/suggested"sugget $aac_abs&
	      { $a"$SHEpwd_SHELL=$L as_have_requir  as_found=fal
#1; th"$1$wards, mpatibility:_SHESHELL as_have_req as_have_requiren=a "$axit \$1al tes.)s_ruWsix ;; is cternin not f && as testion
=ion value
	   for shLL" || test -f "$SHEon valueLL=$SHELunset; and pwdsuggesesteds_va?:onexiste to A'\"\$@\"= "Xion value for shxit \$1  as_found=falret_suithout unset; a for sks around shells that caribute e""$as_rdid lativV=/dev/null
	ENV=/dev/nnot yet assume a denull
	(unset BASH_ENVlls without unset; and this also    if test x$ks around shells that cannot u/NFIG_SHELL
NFIG_SHELL=$unset; and d shells that as_have_requirepatible="if test -n \"\$Fsett#as_ru '((' couldset} -f "$SHELINSTALLal tes) :
   tent variablesac_0: In p=$0: In pae""$as_r has bugs an1+"$@"}
fi

    if td should"
   set CDPATHMKDIR_P=$    $as-f "$SHEL    $asarticular, zsh $ZSH_VERSIONe""$as/ $as_e    $as_e1+"$@"}
fi

    if ttell bug (unset $a
# This s.
# Generated by GNU Autoconf 2.65 for cyassl 3.3# Ieaturetempl x$aintf progknow about datarootdir, exped pn
  # FIXME:sac
fi

ck shcess faiare Fod a few years afr an2.60or zshanually ru_ a s=;  if i
SHELL=${Cseen=_SHE(( 1L-/bin/s='
/L-/bin/sh}
t, it requqout @L-/bssio/pfereoissioehaviinfoof commonlocaleof commonmanof com'=a "$a`t" me"print -\"\x$CONL
# Unset m\"gested" adev/nul"` } 2*L-/bin/sh}
 $as_eL-/bin/sh}
exportyesyes
re with b*|*ior of c- ## tools.
- ##LOR_FORCE=- ##_OPTION
		  ggested=$as_suggested$LINENO;as_suggestedWARNING:---------------- seas_mto ignor && as--L-/bin/sh}
set _sugs_lineno_1a=\$LINENO
  aunset $1;}
}
as_unset=as_fn_unset

# as_fn_set_status STATUS
# -------2;ariabfiles.
# Generated by GNU Aause bugs (e.g. in
# pre-3.0 i
fi
SHELL=${CONFIGmetes&re with b&$e with &f teUS, r of c&$et -e"0" or "s tools.
&$ tools.0" or "sLOR_FORCE=&$LOR_FORCE0" or "s_OPTION&$_OPTIO0" or "\\\${L-/bin/sh}
} a "trally ru&g'  $as_ech
# This cod$LINENO;asnfigurwto o`NFIG_SH' = `.'cripShell cst -n tional paraac mighot ca extrasubcript underdSoftware Bourantset
maiv/nulltc
fifeature?t_status

# as_fn_exit STATUS
# -----------------
SHELL
#  -*)  as_s}" &&
$  -*) asMakefiles.
# Generated by GNU Autoconf 2.65 for cyassl 3.3:t
/@[a-zA-Z_]  *) as_0-9]*@/!b
s|@}
if ( set x; a@|EP_OPTIO"x\`expr \|;t t
s&@
	      { $a@&assl/cyassl/iELL=$as&rs"
      as_dir=requires_dirname -- "$require ||
$as_ermissio_dirn for s ||
$as_e$0: the shX"$as_$0: the sh ||
$as_expr ' \| \
	 X"$' \| \
	 X 'X\(//\)[^/]' \| \
	 X"$as_dd shells that  ||
$as_e_dir=`$as_dirn-- "$as_ 'X\(//\)[^/]_dir=`$as_dirn\/*$/{
	    /\)' \| . 2>/dev//{
	    s//\1/
	  
	      { $a ||
$as_e0: In pX"$as_0: In p ||
$as_e    $asX"$as_    $as ||
$a_compi
SHELL=${CONFI
"lt" messdE GREP_OPTIO  -*)\equi--------------- | $\\\\posix
# 2002, 2003,>  tes or  2004, 2005, 2006, 2007, 2008,est x$as_have_required = xye

>/dev/null; t\1/
	    q
	  }
create directoryxporurn y unsac_o \$apr "X$1"/r_p
# ---------/p'    tesout"`;es Solaris s_fn_a"; }ir_p
# as_fn_append VAR V   arg\1/
	    q
  arg:*=--------------
# Appendnull; txt in VALUE to et VAR.
as_fn_unset ()
{
  { eval $1=; unset $1;}
}
as_uthen
    aa refern='/set
turevariaee s\`\1/
	    q
'
und infn_unset
be ATH_W.
ifucb/lthouintf %sif tn_failations.
--------------------
# Set $? to STATUS, nds, instead of the typical quadratic growth present in naive
# implementations.
if (eval "as_var=1; as_var+=2; test x# as
  rmne
    test Zsh -f "$SHELL" n :
 l tes_have_r----------rn (fn_arith ARGxt in""$as_rfn_arithaved.
fi
&& mv arithmetic  the resul; then :
 val "mkdir $as_dirs"
  } || test -d "$as_dir" || as_fn_error
$0: :H+set} = xset ; tht -o) set} =  were not saved.
fi
test x\$exitcodecannot fe () { ret/*ineno_1'\$as_run'\ */else
  as_&& t" me'  done
    tes\\\\\\\\\\\\-------------------in/sh
#='\\\\\ho$as_ecelse
  as_have_required=no
fi
  if test x$as_have_required = xye $as_echdifosix*) :e_requi|| test $? -eq=.
  as_fouyassl/cyassl/s_suggested=$as_suggested$LINENO;as_suggested----------s unchangeds_lineno_1a=\$LINENO
  are
# provided, also outpusted=$as Guess valu store the resul error: the
# glest $? -eq the result\
	fi # as_fn_arith


# as_fn_error ERROR [LINENO LOG_FD]
# --_suggdev/null;hen :
  eval 'as_fn_arith ()
  {
    as_val=$(( $* ))
  }'
else
  as_fn_arith ()
  {
    as_val=`lse
  as_have_required=no
fi
  if test x$a-"X$as_echo") 2>/fi sheomp$@\" the resul'ggesdex/nullFree S_n bash .
_am_arg  as_sugge"} # astamp_countias or  # an bash_fn_exit $as_status   && (tent she/null 2>&1 &&"; the # as_f
    # as_f:run'\" &&
='pris && (evrun'\" &&
expr a : '\(a\)'`S; IFr=exp a : '\(a\) + 1`r
$0: systeilure { rettime a : 1; thr=expr
e" >ll; then :

else
 ame=basenIFS=$IFS; IFS=$ame=basenRATOR
as_found=false
for as_dir in /bin$P=`dirname -- /`sr/bin$PATH_SEPARATOl 2>&1; then
  as_dX/) >/dev/null 2>&1; then
  a && as_dir=.
  as_found=:
  case $asame=false

	 /*)
	   for as_base in sh bash ksh sh5; do
	     # Try only shells that exist, to save several forks.
	     as_shell=$as_dir/$as_base
	     if { test -f "$as_shell" || test -f "$as_shell.exe"; } &&
		    { $as_/ a : -h&1 && test "X`bae porrtabC) unset VAR.
as_fn_unset ()
{
  { eval $1=; execu_suggested" as, moandss_lineno_1a=\$LINENO
  ages.
as_cr_letters='abcdefghijksted=uggested") ----------------if test -n \"\ dir/pftwar"/./;re not sAMDEP_TRUEi
test"se
fv/nul# AutoFreet 1
2 quoas_e--ters=argcho$ae
  cat" m, butas -grentsftwar_cr_Lx ;;liss_lioftw or 
  as_. exitcodplay safpatid only enatic turet" m_cr_Lifoftwdetecteno_1igiting && nt she '((' could
#001; n_ret_t" messagx"}'='"$@"'
  setle""$as_reset CDPA '((' could
# (unset $as_ = x; th/devm:
  ulate s# Sy it.MFe Softw as_upselvesture :

eoeatureneno_ry sepf=` { retum1; }

exi-einen:.h
  s_ech= xsehecka=$Lo poir" ||b/echoettentf %six*) :
dg string  oras -/=
  'o prou}
as_fnmatchst "x$$]LINENOs p
  d ` string .in'_line]LINENOre; t_ret_sure :

e   s;e Soas_feadurne ng oalinenon :
  evaen:loop
   Grep'$as_dir/"}, wh# ----f progrnough: loop
      spost-prore r]LINENOell";)\(.*\)/\2\ 'tesadve onew "$as_o$as_p  /[ell";  N
 to s
  eo.lineno" ||
    { $as. ##
# provide008,good eio po: AIX grep hateades anLINENO over  /[2048_linenal*/./;'softw shelhompaATH_rxpord&
  (evat 4000  $as_echpr "X$1"s,^#.*  :lineno
by a  b
   .*,X,-----f |
   dirnXOutput "`basename $0`: error:dirpar\$as_lihen :

else
 f |
 FS=$IFS; IFS=$f |
RATOR
as_found=false
for as_dir in /bin$Pase `echo sr/bin$PATH_SEPARATOxy\c'` in
  *c case `echo 'xy\c'` in
  && as_dir=.
  as_found=:
  case $asf |
 
	 /*)
	   for as_base in sh bash ksh sh5; do
	     # Try only shells that exist, to save several forks.
	     as_shell=$as_dir/$as_base
	     if { test -f "$as_shell" || test -f "$as_shell.exe"; } &&
		    { $as_echt the
# scrip\
  && (d=$as_suggesteE -*)_linenoationiv/nu  /[DEPDIR, am__includlittnd sm__igits] is no to cture      N
 NENO as_runntern`he F'/=
  ' if lnppend VAR s/^ if ln = /----1998mf"f$$.di>/dev/null if lnlt inll
fi
if (echs conf$$.fio gotchas:
  s conf$$.fi On MSYS, both `ln -s file dirs conf$$.filn file dir' fail.
   igitsJGPP < 2.04 has nigits On MSYS, both `ln -s# Wto ous------si2knr, Ure scbeble
DUABASHnriginalcore;
$0: the s$$.diUo gotchas:
  U-p'.
    ln -s conf$$.fF_faie mideo asency outre () { s_echeylinennf$$.fidoftwareoftw] is no$( if ln) which ir p
  ucceW  asvos_va
as_wice
  case \n_failthev/null;siMMANst approll";toted=ngsugge as_ln_s=et

ts  more o$as_e which  rerun w0: ts/null2>&1; th------
 `print -r error:s/^$as no symlin ./-pigits_nl\ as_ln_sl\\(r ./-p
  a"'$littSYS,lineno"
 /bi  sed '
  \ev/null

     and `l''
  n
  as_tU    Use
  ` && (teLINENO strvar=1;>/dev} 2>/dev/ram ot \"x\$-s file osix comman/$result inll
fi
if (ech  fv/null; then :

else
 _save_IFS=$IFS; IFS=$H_SEPARATOR
as_found=false
for as_dir in /bin$P_SEPARATOR/usr/bin$PATH_SEPARATOATH
do
  IFS=$as_save_IFS
  t -z "$as_dir" && as_dir=.
  as_found=:
  case $asr in #(
	 /*)
	   for as_base in sh bash ksh sh5; do
	     # Try only shells that exist, to save several forks.
	     as_shell=$as_dir/$as_base
	     if { test -f "$as_shell" || test -f "$as_shell.exe"; } &&
		    { $as_echtcode=1have_  fi
  as_dirmpatible""$as_requrerun w{ ret=$as_sugge  fi
  as_testet_succe{ re'# dummy'2001,
# Check that we arilureLL=${COVWXYZ'
a  "libtool_dige && { eSe-----wsix ;;    # ..on zshle con #(
tureopv/nu_mysd inallow ourequirechobcdefg thr: ca='ln -s'
 re Fal  /[\ escaph orters were no$@"}'{ZSH_VERSION+set}" e $0`: error:setopt NO_GLOB_SUBSTd=$as_su\"x\$afg  as_"${o----}T we artrap "$RM \"$hen
  #\"|
  it 1" 1 2 1]
# --c flg, and co"_requirett STLT_onf >001,1" = X--f#! $SHELLs co`$ECHO "$card ible""$as_%^.*/%%'` - Provs_mysix*) NO;ad library- a
	# neusupporot crvi'/usr#; echo as_f.
  . tice Boubyecho as ($PACKAGE$TIMESTAMP) $;
esac

# L

case was }
if ( seest  host `(xec  :

e|| u :

eln)ir=.
  as_foun; the1q`:ectoOTE: Calso  2.0_myselr" || n_s='lll && lost:eno &&
  ltd "$.sh.
#
re &Copyrir in(C) 1996,P-UX7ksh a8ksh a9, 2000l pri1l pri3l pri4l pri5 givtdout
# if CDPAT2006l pri7l pri8 Free Softwx ;;F-r "av/nu, Inc# Yi  WrittenelseGordon Matzigkeitksh a6  exit ac
fi providemman  /[GNU  exec $
  exig+set}" != _me:fPATHs >/dev/; you -o) redify ib$@\"ite co/or
# modifylongigina. Then rms  /[$]LIg+se echo l Public License=$as_ep"$0"sh this)tureDPATH) >/dev/null 2>&1 &;lems
   verdir_ 2 of----he 'sed 20,! -f(atarger of quo)usr/ stal     # ex
  exiAtead_suggestexcef quoypical q in 'sed 50q "$0"' 'sed 20 givifarge sible, as lk echo
  satest_strinaechogram! -f} 2>/deecho
testse a
	tconf$$.g+set}" != ,arge e sco symlink echo
  sope with 
# s:

esible, asing=`
    cho
arge pt in \${1+\rile
 /[$]aelf=g"; }Xset; then
    # findsible, as 2a=\$izeshop
  iatlonghift
  cuseful givinenWITHOUT ANYSet RANTY;='ln -s'
eve` &&
 $$.fi$LIN -n nt ! ed sMERCHANTABILITY! -fFITNESS FOR A PARTICULAR PURPOSE. in
X*CHO '\ in 'sed 50q "$0"' 'sed 20q1931-as_fdetail # Y
# Youhell if  # orreceihave ocop-n $aal $cmd`; } 2>/dev/null &&
	 {
# a for oftwag+set}" != ;ive
${1+\"\$@\COPYING. age.not,somethi_echan && downloa_s='cto chttp://www.gnu.org/lsed 20s/gpl.htmlthan
# ob
    this)or cternaical qDPATH) >/dev/null 2>&1 && unse giv51 Franklind's eet, Fifth Floor, Bost && MA 02110-13e taUSA.
\\\\\   p
    for cmdtaggeFIG_nal pa>&1 &s= 'X\t' 10q "$0"isBST
els.
availaticrt Ls="CXX topt ### \\\\\\LIBTOOLset ; ts coWion (re(eval rks }" != Xm4SHELLs/[$?
reak
_   # ex=$_string"; thet_strinrevi then
      cho"
   est "X     t     :impleis co a lic/dev/nuih or]\)//*old_libs=$as_lin& tetic fi
    done
    IFS="$lt_savha-no-

    if test "X$tring" O" = Xecho; theecho,est "X
  cyping_sobject impleis c.
pivar+set}X\t' &&
 fi
    done
    IFS="of qmO;asir -p.  Aas_fnlONFI..
 ho_t_st_striXecho; th2>/dev/null`\t') 2>/xec $n
  as.
xec _alias=$= "X$echo_ = "Xtest_s = "X$o_test_stos\t') 2>/$lt_savtring" ]\)//*echo_te that does r thatthe triest "X$E the tricl has A; theing"; } al printf progtrunc x$a as_ln.
SED=$lt_SEDopt NO4sh at helpcho_ avois_fnas_mnstriy triggexternrunn(1) of quotaas_e -n.
Xsed="\$SEDif l1s/^X//topt A
  # E /bin/ksh || astiness for bash orGREP &&
	HELLit.
 n ERENENO\(er.
EHELL-/binINAL_it.
  ler a mesexternport ORIGFHELL-/binsh
  it.
  BSD-e
  MS-G_SHELL"l  p
   o_2=$r.
NM &&
	NM fi
    donewe #(
  ringe
  hONFIlinks.
LN_S &&
	 Try print -rnf$$.f maximumues gthst_stred for st_sx_cmd_leen
  t "X`{ $Es coO2>/de${echo; then (normthen
"o").
 2>/xtyasslg_stris coEes.
aatic " = 'X\t' &&
	   echo_tstinexetring  tests co
      t le s_mkdiiginal andsf: eset".
lt_ing"; &&
	ing";s coturn.
    cho$as
    as_s.
SP2NL &&
	&
	 testtf works
 elif ec\\\\\\	  :
 .
NL2SL-/bin0" -llbas coHownf$$.ll buireIFS"atic  2>/deNENO
 .
     t_flrt Llt__string" = g_stringcmds'X\t' &&
	  ing_
      X$echo_symbol undpSHELOBJDUML-/binck-echos coMethoas_fnc*/&-/
      tl; then
o "$    if linen echo,  2>/dev.
dep" = _ev/nu_mng") 'X\t'ho_test_string"; theh shed for _savpt irentsho_test_string"; theself:-----magic".
$CONFIG_SHRIGI'X\t'"
	  export SH\t') 2>/archivSHELAR'X\t'AR
AR_FLAGy usin         { test "$0" fy itptern $ECHO "$STRIL-/bin --faAL_CONFIG_Scho_teif mkt_strif coold_SUBST $0 --fa.
RANLIB'X\t'string
ECHOs_me&&
	   ting_strin
	     echo_testing_&
	     euncho_testing_string=`($CONback-echo "$ecit.
  CIG_SHilSHELLTCC'X\t'CCs coX$ec     test " =   # TC     elif string\\\\\   avel oas_ln_of nme concho
# Prof $ echforksrawstring=## M4sCfi
rm -
global_tring=_pipeck-echo cv_sys_er string...
	  pr\\\\\\\\\\\\\\NFIG_SHELL $0 --fhell$as_echoC "$ala    ecller string...
	to_ced 5ev=:

	  for cmd in 'echo testest "X$sed 2q "$0"' 'sed 10q "$0"' 'sed 20qh a sm>&2;restestir'; do
	    if { test_= "X_
	    pecho_test_string" = "X`eval $cmd` test "$prev" 2>/dev/null
	    then
	      break
	    fi
	    prev=" exporliblf=$as_dwe weeded$cmd"
	  done

	  if test "$prev"rnat_run=a "$= 'sed 50q "$0"'; then
	    echo_test_string"}
	  else
\t') 2>/dev/  /[$]LItion=L
  eCONFIhen
    an ino/nulltring" _testingobjhave_o and opt N_mkdiELL
	  exporf$$ c`" = _mkdiST
else.
ocume &&
	 cument hAn runni /bin/ksh || test -f /ested" as "$1$as_nlmalle, iCONFIGCHOs coU/null`  COM theX$echo_tes=$LINENO
 export SH beg   aoftwa"$CON".
MAGIC_CMD=$O="$CONFI_striustlinenock'cp -p'
xporto-----   teng"; ?
L-${_ack-" != '| exec 7<&\\\\\
# CLINENnipustall$0 --fad DWARF debugstring=`ftware "$ltc OS X.
DSYMUTIMakefi) returnec 6>&1

# N also  er str
# NLOR_F  # maybeSVR3.2, LinuxNMEDIT"$0" --|| uec 6>&1

# Name of the fat} 2>/dev/ conf0 --fatname=`(hostnamLIP=$ECHO=/uss coldd/_retel_str$CON
# CoHELLach-O binho_tesSVR3.2, LinuxO
    "$ech
   c_clean_files=
ac_config_libob64 bitbj_dir=.
LIBOBJS=
cross_comp 10.4iling=n64o
subdirs=64>/devlons.
#
ac'X\t' &&
	   echo_tastinlibtringTRING=e copycho, so looy'X\t' &&
	   echo_t.sestinshrextting_strin/cyassl/cya\t') 2>/ed for Mak_ECHile) 2>/deexsting_string=`    IFS=$Pag_string_testing_wolfsslFS; symsting_strin.
ac_includes_defauls conadratic
fi
os quas_echell if you) >/2a=\$
fi

# Cwr

asrBST
else
------ing_oor moo "$ak then.
uadraticsONFIGd_for' &&ink	    
#endif
#ifdef STDC_HEADERs coDSoftw#(
  *ir/$lib"lf=$as_dble todules || exec
	  else
	 nclude <stdlib.e <stddef.h>
#ea    # expibobX$echo_te || exeg"; then
_HEADERS && ing_sGE_BUGR   # externayp.h>
  # ex_ory.=$
# endif
# i3.3.2'
PACKAGE_BUGRrunthene.
  auadratic.
run.
  incl=$ <strings.h3.3.2'
PACKAGE_BUGRNGS_H
# includeshlibtrings.h>
ypes.h>
#endis coIincles.h>
# ses.
# thiefas_fn_sese
 -r_p CKAGE_BUGRE<stdi
# in?types.h>
#eoverrides_ <strinf
#ifdef HAVendif"

ac_subst_else
  m Ini_stri_BUGRCHO=eFS=$IF_STRItest _sug	    #CLUDE
AMINCEMORY_g_striCKAGE_VEi
rm -f F}, whCHO=e     EC  es oplittesting_sineno_    #') 2>/ERSIGS
HEX_VERSIOoASE=t -r ECKOinkX$eci>/deoftwa-lNAMESTRI_BUG testsAMINCLUDE
GENSE
BUILD_LIBZ_TAGE_URL='hAVE_CHO=echo
	  UILD_EX,-------- thelt headRSION
IS_/dev/nsoUDE
AMINCLUDE
_FASTMATH_FAL_CONFIG_SHELL
	    exitst_string"; st_strs for most tests   echo_testing_strin$CONFIG_SHELL "$0TH_TRUE
USE_VALGRIND_FALback-echoVALGRIND_TRUE
HAVE_VALGRIND
BUIback-echo "$echo_tetring") 2>/dev/null` &t') 2>/dev/null` nullsho"
  i

# CBOBJS
LITRUE
BUILD_CRd 20qtion=L
  LL"
D
BUting_strinE
BUILD_INLnsteas   lBUILD_INL"n
$0_str_TRUing= 'X\\\\\\fragho$asmplemet" m thiu    008, how } 2
BUILDt" mINE_TRUE
BUI'$as_o-reexec ${1+"sing
    rdr_p (TYPES_H
# in_SHELL 

    if teE
BUILD__\\\\O" = XeRUE
BUILD_MD4_FALSUILD_NT tes-_STRIprint H
# include if !defined ST.
r cme <s
# inc_tringTH_FALSE
B5_TRUE
BUILD_RC4_FALSE
_FALRunE
BUILD_SHA_TRUE
BUILD_MD5_FALSE
BUILD_MD5_TRUE
dlBUILD_RC4_FALSE
BUILD_RC4_TRU
BUILD_AES_FALSE
B_FALSE
BUILDdlop pasho_testing_.
E
BUIL_ 'X\t' Xecho; thE
BUILUILD_ASN_TRUE
BUILDofuoting tneno
_FALSE
BUILD_DH_TRUelfD_RSA_FALSE
BUIILD_ELD_RSA_TRUE
BUILD_MEMOen
   then
LD_MC-f /bin/kE
BUILD_MEMORY_TRUE
BUILD_Ehen
   CC_FALSE
BUILD_ECC_Then
      #t') 2>/deLL "y it.
SE
BUILD_MECHOLSE
Blibstring=`(
BUILD_M

BUILD_MD2_FABUILD_MD2\t') 2>/LD_MCAPs/[$]LIN$lt_saILD_RIPEMD_L} &&
	L\$\$0t') 2>/dev/null` $lt_sa test "X$echo_testing_ECHOs.
#
acting_string=`(UILD_AESCCM_LL
    anguagas_suggfic     test "echo_te    testTDINT_Hch ca" = "X$eal $cmd`E
BUILD_?
oftw_gcc=$Gsting_HA_FALS$echo__saverks
offen
   H_VEunev/nus.
no^/].*ting" = 'X\t'ILD_rogBUILD_SNI_EANPSK_TRUE
PTH 2>/dev/nulpase of $[MCAPIER_TRe).
  EFALSE
BUILD_.
w$echo_tesAGS
PTHREAD_LIw8_FALAddl; theste" = "X$echo_t test a
	# neuBOBJS
LI" = "X$ecX\t'
PTHREAD_CFLAGS
PTHREAD_LIpUE
BUILD_D_SNIFFER_TRUEpr :
 t dynamfs"

ng to_STRnkhen
   AULT_VERBOSITY
HAVE_VISIBILen
      #Dntf E
CCASDEPsimultaneouslGREPX\t' ;-cation-{ prinons?
THREAD_LIc_o	    # OopsAGS
PTHREAD_LIc_o fi
    done
    IFS="&2; -lcCCASFLAGS
CCAS echo, so look for alternatives.| execc=$UILD_AESCCM_ODE
AMDE fi
    done
    IFS="diswhich  echo, so 
fi



AVE_STRIIR
OBting_n
   .
whichrnatives.
   _D_SNIastdepCAVE_STR    if test "X`{LIBTOOL
CYASSL_LIBRAR
CFLAG_VISIBILITY
CXXwhich reflexE_VEE
BUILsts.
\t' _fastdepAULT_ALSE
BUILDTALL_STRIP_PROGRAM
STRIP
CFLAG_VISIBILITY
CXXsix*) :
 _string" = "X$_INLINElyt heade.
#
ac_.
. ##
ILD_AESCCRAM
STRIP
instLL_DATA
INSTALL_SCRIPT
_FALSE
BUILDFALSE
BUILD_Scop-p'
  fconfi    no
PACKAGE
CYGPATH_.NLIB
AR
OBL-${s{ $ECc Cool,t_alias
host_alias
buh shell builtest "X$echo_testint headers for most tests
BUILD_AESCCFS=$_newSCCM_FALSE
BUILD_AESCCinfodir
docdih shell built   fi
  fi
mandir
localedir
ILD_Pnk>$as_me.lND_TRUE
HAVE_VALGRINDincludedir
includludes_default="\
ndir
program_transform_name
pLD_POLY1305_TRUE
BUILD_AESNIRUE
HAVE_VALGRINDUILD_AESCCM_FALSELD_AESCCM_TRUUILD_AESludes__TARNAME
PACKAGE_NRATOR
SHELLAGE_BUGREPORT
PACKAGE_STRING
   test "IB_H
#TH_FALSE
BUILD_SLOWLAGS
CCA# SoRUE
HAVE_VALGRINDIB_H
#ting_strinnable_depenenable_dRATOR
SHELL'
ac_th_gnu_ld
enable_l_FALSE
BUILD_HCvide a
	# neust we loold      :loD_SNIFnu_  ECSTALe_dtls
enaelse
SE
DEBa we mowinclcho, so look fe_singntations.  # maybemplemePSK_TS
LDFLAGntations.AULT_VERBOable_hugecache
enabl
enable_ipv6
eenfor:
  nonable_leanpsk
enab_LEANugecache
enable_smale_pkcallbacks
en
enable_ipoUE
BUILD_P\cyas so weMAKE.
LIBOy duxternXX_FALSE
esac
fim--falork  :
  ifnable_aesnintf program oTRUE
BUILDle_aesLL_SCRIPT
INSTAable_blake2
enable_sha512TDINTfhreaided/nulRIGINAL_FALS,FFER_TRUEaesccm
enable_aesni
enable_poly
g un5
enable_cameest "X$ble_md2
enable_nullcipher
enable_ripemd
e_TRUE
BUILDe2
enable_sha512enable_ogs
enable_oldtls
enable_stacno-reexec ${1+"$@"}
LD_CHACHA"-r.
  "ALSE
Doftwaa se_bod$ECHOneno_1=ings
enable_oldtlsoding
eorksize
enable_memory
e_md5
enabopt NO_Gto "yes"_nulonf$$.DIR/libLIBZ sh
am__unext}le_ecc
enable_fle_sep
enas_e 1) i
enatestingu_TRUgle_poly_TRUE
BUILDtion=LE
BUILD_MD4tion=Lrver
enable_pwdbased
enable_hc128
enable_rabbit
enable_chacha
enable_fips
enable_hashdrbg
enable_filesystem
# M4sh Inable_filesBOBJS
LIl; then
   ias_e" '\${1+\",i.se
  oss exec
# so unamneno_# --- ble_res.h>
#endi}et -o pn $asE
BUILDs	    ksh$denable_inline
ena/
	 ${1+\ble_ocsp
enable_cble_testcrver
enable_pwdbased
enabln_setL 1) LSE
Dle_chacha
enable_fips
enle_htesthdrbg
enable_filesystem
enable_inliminus_LE
BUILD_MD4server
rver
enable_pwdbased
enablSHLIBfigu_VAR= 1) emath
enable_examples
with_libz
with_cavium
enable_mcapi
enable_jobypes.h>
#endif
P
CCAS
CCASFLAGS'


# rver
enable_pwdbasedLAGS
CCASble_fast_enable_sO works!
  :
eamples
with_libz
with_cavt_versiondev/nul99, ho_n= "X$echo_testnd_cr_LETVE_SYSE
BUIL# Sog     "X$_TRUE
BUILDO works!
Initialize O works!
rver
enableyG_SH_strUG_FAaddsHAVE_STRINGS_  for    test "X$echo_tef wooHAVE_STRINGS_H    .
inherit_enabl=$_suffix=NONE
_FALSE
BUILD_OCSP_FAble_m
datatring"; } , with
#    kdirl; then
   ILD_RIPEMD_am__ftestho_test'cya# Installationelse
ixring" = "X$uadratic gNFIG"\$@\" \${1+\HECK
LIBM
fix| \
$0 --ONE
prE_TRUexec_prefix=/frver
enable_pwdbased

# Factoring dt_CC
C "x$as_mS
LDwayIFS; L_STe based=$
# by default will acAGE_URL='http://www.faultosed to be basedNSTALL_STe basedefault="\
#i also accept themopt Nk
enableho
asll if pt NO_Go_2=$LIl` &&
 p     tsh, perl, etc.$$.finsform_n
instalbin'
sbindir=e same order as tble_m
# by  && t
# Factam_sbin'
sbindir='${exrootdir='${prefo '\t') 2>/dewere run if !def
enabl_SHA512_F(, with
#ILD_RIPEM) -n '
  instalAM_DHEADERTRUE
BUILD_atedir='${popt NFALSEystemsCHO=ehen
    neutr_ln_s=ln
LL"
	      ALSE
BUILD/include'
docd 2>/dev/nulE
BUILD_Pp=
ac_init_versio.
  al\\\\\\\ names as ashes changeev/nuerlines.
cacev/nu\t') 2>/tion=L
 e_bu <stdint.hring=`(E
CCASDEPi



=$as_suggp=
ac_init_versiget_alias
hRUE
BUILD_Rdir='${exatarootdir}/locale'
mandFLAGS; then
 e_buCXXCot fih>
#endep=
# exitD_MCA2>/dev/be
enable_null`_echll builtortress
enablylstatdeplias
buprefix}/v -n "$ac_prD
BUI -n "$ac_prev"; tc_prev=\$ac_optest -nrev"; then
  $ac_prev
BUILD_CRL
  caLSE
BUILDISTD_H
# include certgeestedn  :
elseFALSE
BUILD_Sn
enable_cerenable_fast_edir='${datarootdir}/locale'
x=/foo"
#t the important Cygnus c) 2>/devEND` &&
         teso; thenLetters=$a shell r $as_aix3
		  back-ech\o; then
  # Avoid inlin (there; then
name problas_myn '
    Gring"llect2SHELL "$0 e
  ire; T_H
#as && --falle level COLLECT_LIBZS environho$asuadratic_CHECK | --bin
# vaD
BUId 20q uffE
BUImoke MKS sh
if X${* | --bind=* 
ECHO=$!= Xsetl/cyassl/* | --bind=* l 2>exec'
s ;;
  -build=s coashdash$as_suggested") 
*
_LT_  as_saux
man/*
_LT_EOFfaul
      s/[no_2a=\otdir
libck-e  case \bas([^' DJGPP getLL "$0ion" ::
  est "I_TRUEmixed CR/LFdo
  LF-t "x$if ech  Sie ty/nul_echt_suc001;  '${xtble_e,longas_echl_ret_vertthe osh=
focach pecc
enacache | --biache- allssting_H_W
 | -_linenwhy Codirune-fioldinclude h=
fo?sl/issu'/^Yippee, $ECHlibtooLSE
BUILD-fileing_s pos/q----*
_LT_"
  # Avoid inllse
  a|| (fn_arith1" = X--inue.
  lt_ECnt shexsi_libtoo001; yesashdash=yes  ;;

  -bindir | --bindiif tunompatCHO=e= "X$eho as_noPTIO_LINENO
  ev "$as_me: e
	  fi
CHO=echo
  sser's PAn ##

,REP
SAPPtypodrbg
enable_f givoo pocho"; #(
able_fir |NON  $aREPLACEMENT.
r=* | --datad()
 it nt she{1}/null; th//gir=* | --data_able_f Dis1%/*}${2O=${e
fi

if tdir ;;
  -datarootdir=* 3rootdir=et $a}tadir=* |bas}/inclo
  | --da| --datar-datar)| --dataro=* arootdir=* |##*/}boutadir=* | --data_and | --dataroo=* | --datadi=* | --datad=*)
  per\\\\\| --dataro=* |samer=* | --datadd 20q_CHACHALSE
BUILlt_sall; thst co= "X:t 0
datadir=$ac_optarg ;;

  -datarootdir |  stdout
# if CD-datarootdir | --dataroot | --datarootd | --da stdout
# if CDaroot \
  | --dataroo |stdout
# if CD>/dev/re
BUIdards."$ir ;;
  -datarootdidir  -cac}/inc:mes.
    fix}/incl ;;

  -g=$ac_useropt
    ac_userpt=`$as_echo "    datarootdirdir IMMANho$aVALGRIble_mbe k
BUIsynchron\t'; | --br=* | --data# So'`
    #s/[-+.]/.e
  iefficin
  ,!= "xo! -f "$leg :
  n artTYPES_file=$ac_inenfile | -dupliksh$a $as_SE
BUILa!= x;

   | --dataro | | --disable-*)-datar)
    ac_prev=datarootdir ;;
  -datarootdir=* | --datarootdir=* | --datarootdi=* | --datarootd=* \
  | --dtar=*)
    datarootdir=$ac_optarg ;;

  -disLSE
BBOBJS
INC_A'X\t' &&ep--disLSE
BUPREFIXsableSUFvidiD_LEof LIBZts i--dvidir=* | --dvible_m 2>/dev/nuller sb    er regex{ echo_t_echd=$as_sug,namehn
  perc evaligns_linen| --dv=* screv/nulla.
## ---ableot (ination (a "$a='${exort OS=
clyLD_Iot) | --da
    ac_pr-datar)# pdksh 5.2.14enable_ripdo ${X%$Y} corGPATH_W
ac_oth dir=* Ye_deexpr "osCCAS_TRU_body='evaee Sovendg* | ssionord_poly1_body='evs_echos,\\ariable names.arootdirootd_useropt
    ac_useropt=`$aeropt
    ac_useropt=#=* |}"_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
    case%"ataroi

#adir=* |op'
dolit| --daized_opts| --datar=*)
ized_optsunre=* | %=*_echo "$anrecognizes_fnac_o*= ac_unrecognlo2uild_cpu| --dap=', -datar)
    ac_prev=datar.lotdir ;;p=',seropt=`$a1%.lo}.${g_stri}otdir=* |2'$aoptarg ;;

  -exec-prprefix | --dataroot=* x\\\\\libobj-or-ERSION| --da --exe --datar=*)
 --ex
  -exec-pref*}.loac_unrecognarable_prevndi c-" = .. | --da_prev= --datar=*)
exec_seropt=`$(( $* ))cognized_sepenNFIG_SHs_fnTRING: 'x-008, tt_stnable_chyphe } 2-exec-pr --datar=*)
lenseropt=`$a#1 ac_u-bui=* | --bu=*)
xec "BoroptIG_SHELL"t are not vt \"x\$as   ac_prev=datadir ;;
  -datadir=* | --datadir=* | --datadi=* | --datad=*)
    datadir=$ac_optarg ;;

  -datarootdir | --datarootdir | --datarootdi | --datarootd | --dataroot \
  | --dataroo | --dataro | --datar)$$.file) 2su_aes2>/dev/_SLOWMATHle_des3
enec-pref
  -datarootdirere, it Xac_us
   g coif l corr= "X `ln | --buil | cho "$ac_useropt" | ing   ac_ie $0`: erroatarootdi=* | --datarootd=est "$3"; thir ;;
  -datarootdir=*ir ;;
  -datarootdiataro_echo ataroot=* | --dataroo=* | --dataro=* | --datar=*)
    datarootdir=-hs*)
    ac_init_help=shorts/[-+.]/"`ldir | --dv)
    ac_prev=dvidir ;;
  -dvidir=* | --dvidir=* | --dvidi=* | --dvid=* | --dvi=* | --dv=*)
    dvidir=$ac_optarg ;;

  -enable-* | --enable-*)
    ac_useropt=`expr "x$ac_option" : 'x-*enable-\([^=]*\)'`
    # Reject names that are not valid shell v| --dv)
    l; thenlf=$as_dep--dariable names.
    exp
    ac2prev=data.otdir ;;
    ac_useropt=`-hs*)
    atd=*se
  as_  as_ht_help=shors%^pref%%"--infodrun'incl\$%%ess ix | --ex=* | --inc=*)
    includedir=$ac_optarg ;;

  -infodir | --infodir | --infodi |nfod | --info || --dataroelp=ST
else:
myOPTIO fored_se'in w\(- Zsh \)=.plitt;q'ptarg ;;

  -s_fnir | libdir=//'_unrecognized_opts="$ac_unrecognized_opts$ac_unrecognized_se-hs*)
    ac_init_help=shorttarg ;;

  -lib `ln ac_useropt_orig"
	 ac_optarg ;;

  -libexecdir | --libexecdbase-htmlized_sep=', ';;
    esac
    eval enptarg ;;

  -exec-hs*)
    ac_init_help=shortp=',-libe)
    ac --exec-pre | --exec-pr | --exec-p | --exec- \
  | --exec |-hs*)
    ac_init_help=sho as_.[^.ch
 .lo/[$]ex)
    ac_prev=exec_prefix ;;
  -exec-prefix=* | --exec_prefix=* | --eull 2>"$@-libe)
    ac_-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-edir=* |1e `e".*"ir=.
  as_found runniHO '\t'; } 2ir | ashdash$ted") nt shel/issellecho as_adi | --datad)
    ac_prev=datadir ;;
  -datadir=* || --datvaho$as_eir |ho as_VALUEir | --d as_
BUI left unexpandVAR-exec-prho as_-datar)LICOLO$1+=\$2bout--exec=* | --exe=* alsta | --localst | --locals)
    ac_prev=localstatedir ;;
  -localstatedir=* | --localstatedir=* | --localstatedi=* | --localstated=* \=\$$1 | --lo--bui=* | --bu=*)
    bui/*)
	 he def| --c=*)
    cache_file=$ac_optarg ;;

  -,$-----ig-cache | -C)
    cache_file=config.cache ;;

  -datadir | -mvig.cache ;;

 t may be le bug oonfig.cacay be lx='
execlete; use --withouevaluation 1" = X--alstchmod +n' +ay be -mandback-echo; then
  # A-cre \
 2>/dev/null` &&
    TAGset ; t: CXXALSE
BUILD_CAMELLIA_TRUE
BUILD_POLY1305_FALSE
BUI_| --no-OLY1305_TRUE
BUILD_AESNI_FALSE
BUILD_AESNI_TRUE
BUILD_AESCCM_FALSE
BUILD_AESCCM_TR| --no-rUILD_AESGCM_FALSE
BUILD_AESGCM_TRUE
BUILD_SNI| --no-rST_FALSE
BUILD_SNIFFTEST_TRUE
BUILD_SNIFFER_FAL| --no-recuD_SNIFFER_TRUE
BUILD_LEANPSK_FALSE
BUILD_LEANPSK_TRUE
PTHREAD_CFLAGS
PTHREAD_LIBS
PTHREAD_CC
a| --no-rhread_config
DEBUG_FALSE
DEBUG_TRUE
MCHECK
LIBM
am__fastdepCCAS_FALSE
am__ ;;

  -oepCCAS_TRUE
CCASDEPMODE
CCASFLAGS
CCAS
AM_BACKSLASH
AM_DEFAULT_VERBOSITY
HAVE_VISIBILITY--oldinclu | --oldincl | CPP
am__fastdepCXX_FALSE
am__fastdepCXX_TRUE
CXXDEPMODE
ac_ct_CXX
CXX| --no-rS
CXX
CPP
OTOOL64
OTOOL
LIPO
NMEDIT
DSYMUTIL
lt_ECHO
RANLIB
AR
OBJDUMP
LN_S
NM
ac_ct_DUMPBIN
DUM| --no-r
LD
FGREP
EGREP
GREP
SED
am__fastdepCC_FALSE
am__fastdepCC_TRUE
CCDEPMODE
AMDEPBACKSLASH
AMDEP_FALSE | --pre=* | --pr=* | --p=am__include
DEPDIR
OBJEXT
EXEEXT
ac_ct_CC
CPPFLAGS
LDFLAGS
CFLAGS
CC
LIBTOOL
CYASSL_LIBRARY_VERSION
am__untar
am__tar
AMTAR
am--oldinclu | --oldincl | E
AWK
mkdir_p
MKDIR_P
INSTALL_STRIP_PROGRAM
STRIP
install_sh
MAKEINFO
AUTOHEAD--oldinclu | --oldincl | ACLOCAL
VERSION
PACKAGE
CYGPATH_W
am__isrc
INSTALL_DATA
INSTALL_SCRIPT
INSTALL_PROGRAM
host_os
host | --pre=* | --p_cpu
host
build_os
build_vendor
build_cpu
build
target_alias
host_alias
build_alias
LIBS
ECHO_T
ECHO| --no-reHO_C
DEFS
mandir
localedir
libdir
psdir
pdfdir
dvidir
htmldir
infodir
docdir
oldincludedir
includedir
locam-suff=* | --prharedstatedir
sysconfdir
datadir
datarootdir
libexecdir
sbindir
bindir
program_transform_name
prefix
exec_prefix
PACKAGE_URL
PACK| --no-recurs | --no-recur | --n
PACKAGE_VERSION
PACKAGE_TARNAME
PACKAGE_NAME
| --nATH_SEPARATOR
SHELL'
ac_subst_files=''
ac_u| --program-transform | --programking
enable_static
enable_shared
with_pic
enable_fast_install
enable_dependency_tracking
w| --nth_gnu_ld
enable_libtool_lock
enable_sile | --pre=* | --pble_debug
enable_singlethreaded
enable_dtls
enable_opensslextra| --no-rble_ipv6
enable_fortress
enable_bump
enable_leanpsk
enable_bigcache
enable_hugecache
enable_smallcache
enable_savesform-=* \
  | --prsavecert
enable_atomicuser
enable_pkcallbacks
enable_sniffer
enable_aeform-=* \
  | e_aesccm
enable_aesni
enable_poly1305
enable_camellia
enable_md2
enable_nullcipher
enable_ripemd
enable_blake2
enable_sha512
enable_sessioncerts
enable_keedi | --oenable_certgen
enable_certreq
enable_sep
enable_hkdf
enable_dsa
enable_ecc
enable_fpecc
enable_eccencrypt
enable_psk
enable_errorstrings
enable_oldtls
enable_stacksize
enable_memory
enable_rsa
enam=* | --program-tranon
enable_asn
enable_aes
enable_coding
enable_des3
enable_arc4
enable_md5
enable_sha
enable_md4
enable_webse| --no-r
enable_pwdbased
enable_hc128
enable_rabbit
enable_chacha
enable_fips
enable_hashdrbg
enable_filesystem
enable_inline
enable_ocsp
enable_c--sbindi | --sbind | --sbin | --sbi | --sb)
    ac_prev=sbindir ;;
  -sbindir=* | --sbindir=* | --sbindi=* | --scation
enable_secure_renegotiation
enable_supportedcurves
enable_session_ticket
enable_tlsx
enable_pkcs7
enable_scep
enable_smallstack
enable_valgrind
enable_testcert
enable_iopool
enable_c--sbindi | --sbind | --sbin | 
enable_fasthugemath
enable_examples
with_libz
with_cavium
enable_mcapi
enable_jobserver
'
      ac_preciou--sbindi | --sbind | --sbin | lias
target_alias
CC
CFLAGS
LDFLAGS
LIBS
CPPFLAGS
CPP
CXX
CXXFLAGS
CCC
CXXCPP
CCAS
CCASFLAGS'


# Initialize some variable--sbindi | --sbind | --s_init_help=
ac_init_version=false
ac_unrecognized_opts=
ac_unrecognized_sep=
# The variables have the same names as the options, with
# dashes changed to underlines.
cache_file=--sbindi | --sbxec_prefix=NONE
no_create=
no_recursion=
prefix=NONE
program_prefix=NONE
program_suffix=NONE
program_transfo | --pre=* | --px,
silent=
site=
srcdir=
verbose=
x_includes=NONE
x_libraries=NONE

# Installation directory optionsform-=* \hese are left unexpanded so users can "make install exec_prefix=/foo"
# and all the var--sbindi | --sbind | --sosed to be based on exec_prefix
# by default will actually change.
# Use br| --no-r instead of parens because sh, perl, etc. also accept them.
# (The list follows th--sbindi e order as the GNU Coding Standards.)
bindir='${exec_prefix}/bin'
sbindir='${exec_prefix}/sbin | -verbose | --verboxec_prefix}/libexec'
datarootdir='${prefix}/share'
datadir='| --no-recurs | -}'
sysconfdir='${prefix}/etc'
sharedstatedir='${prefix}/com'
localstatedir='${prefix}/var'
inclu--sbindi ='${prefix}/include'
oldincludedir='/usr/include'
docdir='${datarootdir}/;;
  -oldincludGE_TARNAME}'
infodir='${datarootdir}/info'
htmldir='${docdir}'
dvidir='${docdir}'
p=* | --targe{docdir}'
psdir='${docdir}'
libdir='${exec_prefix}/lib'
localedir='${datarootdir}/locale'
mandir='${datarootdir}/man'

ac_pefi | --pac_dashdash=
for ac_option
do
  # If the previous option needs an argument, assign it.
  if test -n "$ac_prev"; then
    eval | --n    ac_prev=
  ption in
  *targ ;;

  -wicontinue
  fi

  c
  -without-ithout-*)
   t=* | --targetarg=`expr "X$ac_option" : '[^=]*=\(.*\)'` ;;
  *)	ac_optarg=yes ;;
  esac

  # Accept the important Cygnus configure options, so we can d| --no-rose typos.

  cao-recursion | --ashdash$ac-/bin/sh	    s/":F) runnithen
  as_>=* |  s/	  s/.* 2>&1`" =ll; ENV ENV MA

2005, 2e.
 0as a dirort lean${datctua  *"
"with_$aONFIGor "cannl";
r cyassl  = 0ut-fp.2005, 2006, 2recog ssl if t=$as_suggenerated by GN || as_fn_error_echo$as_r=1; sIFS="$lt_savho$as_elogle con to od shLL "$0" lure suc_echo$as_e a longintf kdirow possi;;
 1 && =* | -ep=', ';;
    esa ;;
Uavectunately,e-fi OS  if te bac,$ech;;
    esaD_DH_=1; ecogni
BUIL#ositional paree Sorinting a longw{ exunre_linenoIFS="=x_init;letedi |as_ln_BUIL$$.fy--prcar{CON  S  :-)
xec$0"' 'dir | <https:/s_creffth-xve-caclondor
;;
    esac
' >$t_save_if=* | --ca(re)BUILif tes# Soho asE
BUILD_ret_failure succeile ccomilesya sh, ';;
   -inc w
  -#(
  *pontf %| --x-io "$echo_rbose= MKS sh
if $no_gumentc_pre----VERSION+setcs_sucre r& uns-l)
it $as a lon | -ll 2>--x-libsix}/ ac_pxec_r_p
#x_libraries ;;
  -x-libr
  {
raries ;;
  -x-lib --quie1=";ade=* 5=.
  as_fo
  documesep--without-$aac_un \
  | --x-librar=*2.65 f
    ac_preERSIOibra=* | ->;;
    esait_heUsfi

,pref=&&ut wi!= X/ber_o_tls_SLOWMATH--fauild$?! /bfound iit_heccess ntf %ac_unrecogized_t -o ineno

BUILo_test_sruev/nullthenac_optarg ;;_have_requie.
 $?s co\$\\$0,'$0','ac_unrecogs$ac_seroseate --x-libcho; thof quo_strin-----!= no-li | --xns that allow amortized linear growth over
# repeale names.
   of quot_FD, rle names.
    case ------------------
# Set $? to : \`$ac_envvar'" ;;
    esac
    eval $ac_envva# aseexee)
  ecer *=*)
 "
"w "X/"; ---" "X/"; R   # ..   $as_ech..."
   $as_echOutput "`basenamc 6>&un't tryi | --vec-pref=bt namachesi/dev "X/"; expr _tlsF\)/\builtxpr "ctaocrypt/src/fips.cas_echo "$as_me: WARNING:_>/de.c "X/""
	 six*) :
 usone
f quotan basho "$as_me: WARNING echo a_tlsias=$ac_option} ${h   $aOPTIONcould="cyassl/ac_opti.h"
fn_ari$;;

  esac

 "X/";  { ee

if$ac_opti
# Ouv"; then
  ac__alnum *   :lineno
FS=$Pac_unrecogac_optihe | 's/_/-/g'`
  as_fn_eron"
fi

if test -n "$ac_un 0
fi

# The HP is -2014 wolfSSL unseon"
fi

if test -n "$ac_unrecognized_opts"; then
  cat "X${echo_test_strinCyaSSLo) ;;
    fatal) as_fn_error "unrecognized options: $/on"
fi

if test -n as_fn_on"
fi

if test -n "$ac_#pUILDa oncas_l
fi

if test -n "$ac_ >&2 ;;
  esac
fi

# Checiis,
 __cpluspreftory arguments for consiex'[^= \"C\" { >&2 ;;
  esac
fi

# Checith_ftory arguments for consistency.
for ac_var>/devac_opt/null;;

  es    l 2>&1; tdefvali$as_mys$ir docd" ); the/-Ds/[$]LINE | --x-lib pdfdir]* | "\
		libe --no-0`: error:  noequal $ac psdir \ pdfdirdir locale=/ /[$]LINEare runni"nd ours $ashes.
  catory arguments for

  -infr "X$acf"; the 'X\(.*[^/]\)' \| "X$ac_val" : 'X\(.*\)'`
  )' \| "X$ac_val" : 'X\ Guess valu*\)'`
  ir docdw/plemgin -ucb/e\
		libon: \`sav$lt_sav"; then
  ac we artf %s'
el consistency.
for ac_var in	exec_prefix prefix bindir sbindir libexecdir}
		datadir sysconfdir sharedstatedir localstatedir includedir \
		oldinc
    : $udes=* Free Sosumm
ena "$as_me: WARNINGC
         ecoken beh_basenart undind=*    # expct shell WARNING WARNINGi

ifIE
USE_VALGRIFS=$IF:al=`exp$FS=$IFday.
build=$bS_SHA_Tory.st_aliasove somen in
 vendor-n in
  -day.
build=$bHec $CPU To remove some me day.
icpuday.
build=$bCBUILD_SNIf test "x$build_a$CCthen
    cross_ble_sf test "x$build_ali $stringthen
    cross++_compiling=maybe
    $as$CXXuild type, don't NG: If you wanted to seoss e --build type, donPPcted then cross compile moPPe --build type, doLIBcted then cross compile mLIBday.
build=$bD on sas_lind To remove som$ax_RSA_FALS on day.
build=$bWarldins$echac_unrest_alias
    v_wlias-

_ () 006,_alias" != x; ntf %-jf you wanted to set tcho; thjobport rday.
build=$bVCSdev/nuoutst.
    If a cro && exvct_strinxt io depey.
build=Feval $s =$target_aliasCHACHAthfilesst -n "$host_$ENABLED_SINGLETHR -o)Dday.
build=$b
   print g=maybe
    $as_ec_pwd_ls_
  seYSTEMday.
build=$bOpenn
  .file API To removeing direcOPENSSLEXTRAday.
build=$bho_tmathf test "x$build_aliang directASTMATHday.
build=$bsnLSE
Bf you wanted to set t_pwd_ls_dNIFFER directory contain>/dehen cross compile mhe parent dirTESTday.
build=$bARC4f you wanted to set s not speciff" :r X"$as_myselfES 'X\(.*[^/]\)//*[^/][^//]*/*$' \| seto"$as_myself" :-Nation was n' \| \
	 X"$as_myself" NI: 'X\(//\)$' \| \GCMg this script, then the parenAESGC Find the sour\| \Co X"$as_myself" |
    sed '/^X\(.*C^/]\)\/\/*[^/][DES3 'X\(.*[^/]\)//*[^/][^/]*/*$' \|/{
	then
    crossamellia ac_srcdir_defaulted=yes
  #CAMELLIt -z "$srcdir"NULL Cip
  # myself" : 'X\(/\)' \| .	  s_CIPHectory.
  ac_coMD5 'X\(//\)[^/]' \| \
	 X"$as_myselMD5: 'X\(//\)$' RIPEMD 'X\(.*[^/]\)//*[^/][myself" |_srcdi=$target_aliasHA 'X\(//\)[^/]' \| \
	 X"$as_myselSHt -z "$srcdir"SHA-512g this script, then the parentHA512: 'X\(//\)$' BLAKEr="$ac_confdir or .." test ! -rsource: 'X\(//\)$' keygens ($ac_unique_file) in $srcdirKEYGEove some i

ifcertre in $srcdir, but \`cd).*/{
	   ERTs not work"
ac_abs_creqdir=`(
	cd "$srcdir" && test -r "REQalias" != x; tC-128s ($ac_unique_file) in $srcdirHC128 fi
else
  ac_ABBITr_defaulted=no
fi
if test ! -r ; thethen
    crossHAC_file"; then
  test "$a).*/{
	   hes falias" != x; tacheDRBGs_dirname -- "$as_myself" |HASHile : 'X\(//\)$' PWDBASEir_defaulted=no
fi
imyself" |$srcdir alias" != x; tKDF 'X\(.*[^/]\)//*[^/][^/]*/*$' \| "X$! -r "$srcdir/$: 'X\(.*[^/]\)//*[^/][^/  srcdir=..
	 X"$as_myselfPSKus_vars; do
  eval ac_env_${ac_vaPSKset=\${${ac_vaoly130ac_unique_file"; the_var}_valuOLYl achost_alias"; tEANar}+set}
  eval ac_env_myself" |_cv_env fi
else
  ac_Sfile"; then
  test "$ac_srcdir_defRSe names in objD--help message.
#
if test "$ac_initDhelp" = "long"; Hus_vars; do
  eval ac_enal or obsole directory coECCus_vars; do
  eval ac_env_${ac_vaEho "$as_me: WARFPo long to be a string in d=yes
  # EOF
message is too l_ENCRYPen
  srcdir=.
fi
e A/UX 3.1 sy kinds : 'X\(//\)$' \SN 'X\(//\)[^/]' \| \
	 X"$as_myselfSnot work"
ac_aAnonymous c*/./; q'`
  srcdsed '/^X\(Nmove some  crossODt $1;m srcdir.
# Double slashes intions! -r "$srcdir/EMORY of some of the useful variablhe optday.
build=$bu/O POOL_cv_env_${ac_var}_set=\${${aIO --hmessage is tooRRORck-ecNG: 'X\(//\)[^/]'e A/UX 3.1  --help=sholp" = "long"; TL: 'X\(//\)[^/]' \| \
	 Xl or obsolTLto this packag'
PATLS V=* | --m as
VAR=VALUE.  SeeOLD_he short help of CSP 'X\(.*[^/]\)//*[^/][^/]*/*$' \|playthen
    crossRhelp              di\(\/\).*/{
	   Rp and exit
   CRL-MONITOR q'`
  srcdir=$ac_confdirCRL_e-file=set=\${${ac_va  # ss_me ses# expcachent"myself" ||AVESESremove some 
  -C, --config-bs_c| --no alias for \`--cach-r ": 'X\(//\)$' \todepCUas=$Record Laying tsed '/^X\(TOMICUSectory.
  ac_co "$0"' Key Call"$1$a of som_var}_valuKCALLBACKto this packagNTRif test "x$build_alir=$ac_confdir
TRUted" = yes && 
	 X"$as_myself" : 'X\(en the parent d! -r "$srcdir/O='prinFUILD_CHALf %s\ of  brackets.AX_FRAGaroo: 'X\(//\)$' Tin/ksh$d HMAlong to be a smyself" |TRUNCATor aMA2 to adapt to Renegoret_d thInd ;;
io in "$ac_init_ENEGOTIA

  eINDICac_deted" = yes && ecif te install' wilation directoriSECUREes in
\`$ac_deted" = yes && testing_sEllipifs"Curve
   _pwd_ls_dUPPORX]

CURV" : 'X\(//\)$' Sache   Tickeas_dirname -- You can spefile=_TICKEtput files
    lll theExtekdir_ed package         [LS compiler is dPKCS#7t}
  eval ac_env_${ac_var}_valuKCS7: 'X\(//\)$' ng inCEP--help=recursive    display WOLFr=DI instance \`-- ech Stack q'`
  srcdir=$ac_confdirSMALLd byC#
# Report thevalgr_faiunitc_enved packagx' using VALGRIN\(.*[^/]\)' \|LIBZ  install architecture-independe datmessage is tooCOMMANIf you wanted to sete A/UX 3.1XAMP seto some day.
buime: W
